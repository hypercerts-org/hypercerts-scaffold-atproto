{"id":"hypercerts-02l","title":"Epic: Migrate OAuth scopes from transitional to granular (production only)","description":"Currently OAUTH_SCOPE is a single constant 'atproto transition:generic' used in all environments. For production, we should use granular scopes that precisely describe what the app needs: repo access to hypercert collections + profiles, blob uploads, and RPC calls. For loopback (local dev), transition:generic must remain (ATProto requirement for loopback clients). The granular scope constants already exist in lib/hypercerts-sdk.ts (HYPERCERT_REPO_SCOPE, RPC_SCOPE, BLOB_SCOPE) but are unused. However, HYPERCERT_REPO_SCOPE is incomplete — it's missing collections the app writes to (app.certified.actor.profile, app.bsky.actor.profile, org.hypercerts.claim.attachment, org.hypercerts.claim.contributionDetails, org.hypercerts.claim.contributorInformation, org.hypercerts.helper.workScopeTag). Success: production deployments request only the granular permissions needed; loopback still uses transition:generic; all existing functionality works.","status":"open","priority":2,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-17T19:10:32.060816026+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:26:13.671302236+06:00","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-02l.1","title":"Update HYPERCERT_REPO_SCOPE to include all collections the app writes to","description":"## Files\n- lib/hypercerts-sdk.ts (modify)\n\n## What to do\nThe `HYPERCERT_REPO_SCOPE` constant (line 11-12) is missing several collections that the app and SDK internally write to. Update it to include ALL collections from `HYPERCERT_COLLECTIONS` that the app needs write access to.\n\nCurrent scope has these collections:\n- org.hypercerts.claim.activity ✓\n- org.hypercerts.claim.contribution ✓  \n- org.hypercerts.claim.evaluation ✓\n- org.hypercerts.claim.evidence ✓\n- org.hypercerts.claim.measurement ✓\n- org.hypercerts.claim.collection ✓\n- org.hypercerts.claim.rights ✓\n- org.hypercerts.claim.funding ✓\n- app.certified.location ✓\n\nAdd these missing collections:\n- org.hypercerts.claim.attachment (used by addAttachment in app/api/certs/add-attachment/route.ts)\n- org.hypercerts.claim.contributionDetails (SDK creates these internally during hypercert creation with contributions)\n- org.hypercerts.claim.contributorInformation (SDK creates these internally during hypercert creation with contributions)\n- org.hypercerts.helper.workScopeTag (SDK may create these for work scope references)\n- app.certified.actor.profile (used by profile create/update in app/api/profile/update/route.ts)\n- app.bsky.actor.profile (used by bsky profile create/update in app/api/profile/bsky/update/route.ts)\n\nUse `HYPERCERT_COLLECTIONS` from `@hypercerts-org/sdk-core` to build the scope string programmatically instead of hardcoding collection names. Import it and construct the scope like:\n\n```typescript\nimport { HYPERCERT_COLLECTIONS } from \"@hypercerts-org/sdk-core\";\n\nconst REPO_COLLECTIONS = [\n  HYPERCERT_COLLECTIONS.CLAIM,\n  HYPERCERT_COLLECTIONS.RIGHTS,\n  HYPERCERT_COLLECTIONS.LOCATION,\n  HYPERCERT_COLLECTIONS.CONTRIBUTION_DETAILS,\n  HYPERCERT_COLLECTIONS.CONTRIBUTOR_INFORMATION,\n  HYPERCERT_COLLECTIONS.MEASUREMENT,\n  HYPERCERT_COLLECTIONS.EVALUATION,\n  HYPERCERT_COLLECTIONS.ATTACHMENT,\n  HYPERCERT_COLLECTIONS.COLLECTION,\n  HYPERCERT_COLLECTIONS.FUNDING_RECEIPT,\n  HYPERCERT_COLLECTIONS.WORK_SCOPE_TAG,\n  HYPERCERT_COLLECTIONS.BSKY_PROFILE,\n  HYPERCERT_COLLECTIONS.CERTIFIED_PROFILE,\n];\n\nconst collectionParams = REPO_COLLECTIONS.map(c =\u003e `collection=${c}`).join(\"\u0026\");\nexport const HYPERCERT_REPO_SCOPE = `repo?${collectionParams}\u0026action=create\u0026action=update\u0026action=delete`;\n```\n\nAlso remove the comment \"// Future granular scopes (not yet used)\" on line 10 since they will now be used.\n\n## Don't\n- Change RPC_SCOPE or BLOB_SCOPE — they are already correct\n- Change how the SDK is initialized\n- Change any exports or the default export","acceptance_criteria":"1. HYPERCERT_REPO_SCOPE includes all collections from HYPERCERT_COLLECTIONS that the app writes to (at minimum: activity, rights, location, contributionDetails, contributorInformation, measurement, evaluation, attachment, collection, funding receipt, workScopeTag, bsky profile, certified profile).\n2. Collection names are sourced from the HYPERCERT_COLLECTIONS constant, not hardcoded strings.\n3. The scope string retains action=create\u0026action=update\u0026action=delete.\n4. The \"Future granular scopes\" comment is removed.\n5. npm run build succeeds with no type errors.","notes":"Merged into hypercerts-02l.2 — scope definition and env-switching are one atomic change","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-17T19:10:53.189448756+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:11:33.879319675+06:00","closed_at":"2026-02-17T19:11:33.879324735+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-02l.1","depends_on_id":"hypercerts-02l","type":"parent-child","created_at":"2026-02-17T19:10:53.19186715+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02l.2","title":"Make OAUTH_SCOPE environment-aware: granular for production, transitional for loopback","description":"## Files\n- lib/config.ts (modify)\n- lib/hypercerts-sdk.ts (modify)\n\n## What to do\nCurrently `OAUTH_SCOPE` in `lib/config.ts` (line 19) is a single constant:\n```typescript\nexport const OAUTH_SCOPE = [ATPROTO_SCOPE, TRANSITION_SCOPES.GENERIC].join(\" \");\n```\n\nThis is used everywhere regardless of environment. Change it so:\n- **Loopback (local dev):** keeps `\"atproto transition:generic\"` (ATProto requirement)\n- **Production:** uses the granular scopes: `ATPROTO_SCOPE + HYPERCERT_REPO_SCOPE + RPC_SCOPE + BLOB_SCOPE`\n\n### In lib/config.ts:\n\n1. Import `HYPERCERT_REPO_SCOPE`, `RPC_SCOPE`, `BLOB_SCOPE` from `./hypercerts-sdk` — BUT this creates a circular dependency since `hypercerts-sdk.ts` imports from `config.ts`. Instead, move the scope constants into `config.ts` or create a new file. The cleanest approach: move the granular scope construction into `config.ts` itself.\n\n   Import `HYPERCERT_COLLECTIONS` from `@hypercerts-org/sdk-core` (already importing from there) and build the scopes in `config.ts`:\n\n```typescript\nimport { ATPROTO_SCOPE, TRANSITION_SCOPES, HYPERCERT_COLLECTIONS } from \"@hypercerts-org/sdk-core\";\n\n// Granular repo scope — all collections the app needs write access to\nconst REPO_COLLECTIONS = [\n  HYPERCERT_COLLECTIONS.CLAIM,\n  HYPERCERT_COLLECTIONS.RIGHTS,\n  HYPERCERT_COLLECTIONS.LOCATION,\n  HYPERCERT_COLLECTIONS.CONTRIBUTION_DETAILS,\n  HYPERCERT_COLLECTIONS.CONTRIBUTOR_INFORMATION,\n  HYPERCERT_COLLECTIONS.MEASUREMENT,\n  HYPERCERT_COLLECTIONS.EVALUATION,\n  HYPERCERT_COLLECTIONS.ATTACHMENT,\n  HYPERCERT_COLLECTIONS.COLLECTION,\n  HYPERCERT_COLLECTIONS.FUNDING_RECEIPT,\n  HYPERCERT_COLLECTIONS.WORK_SCOPE_TAG,\n  HYPERCERT_COLLECTIONS.BSKY_PROFILE,\n  HYPERCERT_COLLECTIONS.CERTIFIED_PROFILE,\n];\n\nconst HYPERCERT_REPO_SCOPE = `repo?${REPO_COLLECTIONS.map(c =\u003e \"collection=\" + c).join(\"\u0026\")}\u0026action=create\u0026action=update\u0026action=delete`;\nconst RPC_SCOPE = \"rpc:app.bsky.actor.getProfile?aud=did:web:api.bsky.app%23bsky_appview\";\nconst BLOB_SCOPE = \"blob?accept=video/*\u0026accept=image/*\";\n\nconst GRANULAR_SCOPE = [ATPROTO_SCOPE, HYPERCERT_REPO_SCOPE, RPC_SCOPE, BLOB_SCOPE].join(\" \");\nconst LOOPBACK_SCOPE = [ATPROTO_SCOPE, TRANSITION_SCOPES.GENERIC].join(\" \");\n```\n\n2. Change `OAUTH_SCOPE` to be a function of the base URL (loopback detection already exists in this file):\n\n```typescript\nfunction getOAuthScope(baseUrl: string): string {\n  if (isLoopback(baseUrl)) {\n    return LOOPBACK_SCOPE;\n  }\n  return GRANULAR_SCOPE;\n}\n```\n\n3. Replace the current `OAUTH_SCOPE` constant (line 19) with the function call. Since `baseUrl` is computed at module level (line 154), place the scope computation after it:\n\n```typescript\nexport const OAUTH_SCOPE = getOAuthScope(baseUrl);\n```\n\n   Move this line to after `const baseUrl = getBaseUrl();` (around line 154), or keep the export at the top and make it lazy. The simplest: move the export to after line 154.\n\n4. Update the JSDoc comment above OAUTH_SCOPE to explain the dual behavior.\n\n### In lib/hypercerts-sdk.ts:\n\n5. Remove the now-redundant scope constants (`HYPERCERT_REPO_SCOPE`, `RPC_SCOPE`, `BLOB_SCOPE`) since they are now defined in `config.ts`. Keep the `export { OAUTH_SCOPE }` re-export.\n\n## Don't\n- Change the loopback detection logic\n- Change how `buildClientId` works (it already receives scope as a parameter)\n- Change the SDK initialization (it already uses `config.scope`)\n- Change `client-metadata.json/route.ts` — it already imports OAUTH_SCOPE from config\n- Break the circular dependency: config.ts must NOT import from hypercerts-sdk.ts","acceptance_criteria":"1. When running locally (loopback), OAUTH_SCOPE equals \"atproto transition:generic\".\n2. When running in production (non-loopback), OAUTH_SCOPE contains granular scopes: atproto, repo?collection=...\u0026action=..., rpc:..., blob?....\n3. The scope constants (HYPERCERT_REPO_SCOPE, RPC_SCOPE, BLOB_SCOPE) are removed from lib/hypercerts-sdk.ts.\n4. No circular dependency between config.ts and hypercerts-sdk.ts.\n5. All existing imports of OAUTH_SCOPE continue to work (from both lib/config and lib/hypercerts-sdk).\n6. npm run build succeeds with no type errors.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T19:11:23.981953959+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:25:47.051699941+06:00","closed_at":"2026-02-17T19:25:47.051699941+06:00","close_reason":"a3b2ede Make OAUTH_SCOPE environment-aware: granular for production, loopback for dev","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-02l.2","depends_on_id":"hypercerts-02l","type":"parent-child","created_at":"2026-02-17T19:11:23.983818358+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-02l.2","depends_on_id":"hypercerts-02l.1","type":"blocks","created_at":"2026-02-17T19:11:23.986580861+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02l.3","title":"Fix BLOB_SCOPE to allow all MIME types, not just image/* and video/*","description":"## Files\n- lib/config.ts (modify)\n\n## What to do\nLine 38 currently restricts blob uploads to only images and videos:\n```typescript\nconst BLOB_SCOPE = \"blob?accept=video/*\u0026accept=image/*\";\n```\n\nThe app uploads attachments of arbitrary types (markdown, PDF, GeoJSON, etc.) via the add-attachment flow. This causes the error: `Failed to upload blob: Missing required scope \"blob:text/markdown\"`.\n\nChange BLOB_SCOPE to use the wildcard MIME type `*/*` which matches any content type:\n```typescript\nconst BLOB_SCOPE = \"blob:*/*\";\n```\n\nNote the syntax difference: `blob:*/*` (colon, single MIME pattern) vs `blob?accept=...` (query params, multiple patterns). The `blob:*/*` form is the correct ATProto scope syntax for a single wildcard MIME type. The SDK's `hasPermission` function confirms `blob:*/*` matches any specific MIME type like `blob:text/markdown`.\n\n## Don't\n- Change any other scope constants\n- Add individual MIME types — use the wildcard instead","acceptance_criteria":"1. BLOB_SCOPE on line 38 of lib/config.ts is changed to \"blob:*/*\".\n2. npm run build succeeds with no type errors.","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":5,"created_at":"2026-02-17T19:40:26.168827229+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:41:47.569135839+06:00","closed_at":"2026-02-17T19:41:47.569135839+06:00","close_reason":"b403f8b fix BLOB_SCOPE to use wildcard */* for all MIME types","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-02l.3","depends_on_id":"hypercerts-02l","type":"parent-child","created_at":"2026-02-17T19:40:26.170442006+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02l.4","title":"Test: Remove RPC_SCOPE from granular OAuth scope","description":"## Files\n- lib/config.ts (modify)\n\n## What to do\nRemove `RPC_SCOPE` from the `GRANULAR_SCOPE` array. The app calls `app.bsky.actor.getProfile` via the public Bluesky API (`https://public.api.bsky.app`) in `lib/api/external/bluesky.ts`, not through the authenticated ATProto session, so this RPC scope permission is unnecessary.\n\n1. Remove `RPC_SCOPE` from the `GRANULAR_SCOPE` array (line 40-45). Change from:\n```typescript\nconst GRANULAR_SCOPE = [\n  ATPROTO_SCOPE,\n  HYPERCERT_REPO_SCOPE,\n  RPC_SCOPE,\n  BLOB_SCOPE,\n].join(\" \");\n```\nTo:\n```typescript\nconst GRANULAR_SCOPE = [\n  ATPROTO_SCOPE,\n  HYPERCERT_REPO_SCOPE,\n  BLOB_SCOPE,\n].join(\" \");\n```\n\n2. Remove the `RPC_SCOPE` constant definition (lines 36-37):\n```typescript\nconst RPC_SCOPE =\n  \"rpc:app.bsky.actor.getProfile?aud=did:web:api.bsky.app%23bsky_appview\";\n```\n\n## Don't\n- Change HYPERCERT_REPO_SCOPE, BLOB_SCOPE, or LOOPBACK_SCOPE\n- Change any other file\n- Remove BSKY_PROFILE from REPO_COLLECTIONS (that is still needed for bsky profile editing)","acceptance_criteria":"1. RPC_SCOPE constant is removed from lib/config.ts.\n2. GRANULAR_SCOPE no longer includes any rpc: scope.\n3. LOOPBACK_SCOPE is unchanged.\n4. npm run build succeeds with no type errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":5,"created_at":"2026-02-17T21:09:30.089104472+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T00:11:53.687086031+06:00","closed_at":"2026-02-18T00:11:53.687086031+06:00","close_reason":"510d690 cancelled: reversing decision, RPC_SCOPE will be added back","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-02l.4","depends_on_id":"hypercerts-02l","type":"parent-child","created_at":"2026-02-17T21:09:30.090941738+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02w","title":"Epic: Replace SDK read operations with native atproto repo calls","description":"## Why\nPhase 2 of the SDK removal migration. Phase 1 (hypercerts-acu) replaced the auth layer. Now we replace all read operations that use `ctx.scopedRepo.hypercerts.get/list`, `ctx.scopedRepo.records.get`, `repo.profile.getCertifiedProfile`, and `repo.profile.getBskyProfile` with direct `agent.com.atproto.repo.getRecord` and `agent.com.atproto.repo.listRecords` calls.\n\n## Goal\nAfter this epic, every read operation uses native `@atproto/api` Agent methods. No `scopedRepo` read calls remain. All `@ts-expect-error` comments on read operations are removed.\n\n## Reference pattern\n`lib/queries.ts` already has working native atproto read helpers:\n- `getRecord\u003cT\u003e(rkey, did, agent, collection)` → `agent.com.atproto.repo.getRecord({ repo: did, collection, rkey })`\n- `getRecordWithURI\u003cT\u003e(uri, agent, fallbackCollection)` → parses AT-URI then calls getRecord\n\n## Scope\n17 read operations across these files:\n- Profile reads: app/page.tsx, providers/SignedInProvider.tsx, app/profile/page.tsx, app/bsky-profile/page.tsx, lib/create-actions.ts (getActiveProfileInfo), app/api/profile/update/route.ts (2x), app/api/profile/bsky/update/route.ts (2x)\n- Hypercert list: app/hypercerts/page.tsx\n- Hypercert get: app/hypercerts/[hypercertUri]/page.tsx (2x), app/hypercerts/[hypercertUri]/edit/page.tsx\n- Generic record gets: lib/create-actions.ts (getMeasurementRecord, getEvaluationRecord, getEvidenceRecord, getContributorInformationRecord)\n\n## Constraints\n- Do NOT touch write operations (create, update, delete) — those are Phase 3-4\n- Profile update routes have both reads and writes — only migrate the reads, keep @ts-expect-error on writes\n- The app must build (pnpm build) after all tasks complete\n- Keep @hypercerts-org/sdk-core in package.json (still needed for type imports in other files)\n\n## Success\n- All read operations use agent.com.atproto.repo.getRecord or listRecords\n- No scopedRepo read calls remain\n- pnpm build succeeds","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-03T17:47:03.470887659+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:58:54.271176057+06:00","closed_at":"2026-03-03T22:58:54.271176057+06:00","close_reason":"52ffa54 All Phase 2 read operation tasks completed and verified","labels":["scope:medium"]}
{"id":"hypercerts-02w.1","title":"Migrate profile reads to native atproto getRecord — page.tsx, SignedInProvider, profile/page, bsky-profile/page, getActiveProfileInfo","description":"## Files\n- app/page.tsx (modify)\n- providers/SignedInProvider.tsx (modify)\n- app/profile/page.tsx (modify)\n- app/bsky-profile/page.tsx (modify)\n- lib/create-actions.ts (modify — only getActiveProfileInfo function, lines 16-29)\n\n## What to do\nReplace all `repo.profile.getCertifiedProfile()` and `repo.profile.getBskyProfile()` calls with direct `agent.com.atproto.repo.getRecord()` calls. These are all read-only profile fetches.\n\n### Pattern for certified profile reads:\n```ts\n// Old:\nconst profile = await repo.profile.getCertifiedProfile();\n\n// New:\nconst profileResult = await agent.com.atproto.repo.getRecord({\n  repo: agent.assertDid,\n  collection: 'app.certified.actor.profile',\n  rkey: 'self',\n}).catch(() =\u003e null);\nconst profile = profileResult?.data?.value as Record\u003cstring, unknown\u003e | null ?? null;\n```\n\n### Pattern for bsky profile reads:\n```ts\n// Old:\nconst profile = await repo.profile.getBskyProfile();\n\n// New:\nconst profileResult = await agent.com.atproto.repo.getRecord({\n  repo: agent.assertDid,\n  collection: 'app.bsky.actor.profile',\n  rkey: 'self',\n}).catch(() =\u003e null);\nconst profile = profileResult?.data?.value as Record\u003cstring, unknown\u003e | null ?? null;\n```\n\n**IMPORTANT**: Profile records use rkey `'self'` — this is the ATProto convention for singleton records (one per repo). Verify this by checking the SDK source or testing.\n\n### File-by-file changes:\n\n**app/page.tsx** (line 29-32):\n- Currently: `await personalRepo.profile.getCertifiedProfile().catch(() =\u003e null)`\n- Replace with getRecord call using `agent.assertDid` and collection `'app.certified.actor.profile'`\n- The `personalRepo` variable is from `getAgent()` — it's already an Agent\n- Fields used: `profile?.displayName`, `profile?.handle`\n\n**providers/SignedInProvider.tsx** (lines 18-28):\n- Currently stubbed out with TODO comment (from Phase 1 hotfix)\n- Re-implement: get agent, call getRecord for certified profile\n- Fields used: `profile?.avatar` (passed to `convertBlobUrlToCdn`), `profile?.handle`\n- Need to re-add `import { getAgent } from '@/lib/atproto-session'` and `import { convertBlobUrlToCdn } from '@/lib/utils'`\n\n**app/profile/page.tsx** (line 22):\n- Currently: `await repo.profile.getCertifiedProfile()`\n- Replace with getRecord call\n- Fields used: displayName, description, pronouns, website, avatar (via convertBlobUrlToCdn), banner (via convertBlobUrlToCdn)\n\n**app/bsky-profile/page.tsx** (line 21):\n- Currently: `await repo.profile.getBskyProfile()`\n- Replace with getRecord call using collection `'app.bsky.actor.profile'`\n- Fields used: displayName, description, avatar, banner\n\n**lib/create-actions.ts** — `getActiveProfileInfo` (lines 16-29):\n- Currently: `ctx.scopedRepo.profile.getCertifiedProfile()`\n- Replace with: `ctx.agent.com.atproto.repo.getRecord({ repo: ctx.targetDid, collection: 'app.certified.actor.profile', rkey: 'self' })`\n- Note: uses `ctx.agent` and `ctx.targetDid` (from getRepoContext), NOT agent.assertDid\n- Fields used: profile.displayName, profile.handle\n- Remove the `@ts-expect-error` comment on this read\n\n### Profile record rkey:\nThe rkey for profile records is `'self'`. This is standard ATProto convention. Verify by checking: `grep -r 'self' node_modules/@hypercerts-org/sdk-core/` or the lexicon definitions.\n\n## Don't\n- Don't touch write operations in profile update routes (those are Phase 3-4)\n- Don't change any non-profile code\n- Don't remove @ts-expect-error comments on write operations","acceptance_criteria":"1. All 5 files use agent.com.atproto.repo.getRecord for profile reads\n2. No repo.profile.getCertifiedProfile() or repo.profile.getBskyProfile() calls remain in these files\n3. All @ts-expect-error comments on profile READ operations are removed\n4. Profile data (displayName, handle, avatar, etc.) is correctly extracted from getRecord response\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-03T17:47:30.624612393+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T18:06:15.361689544+06:00","closed_at":"2026-03-03T18:06:15.361689544+06:00","close_reason":"8897381 Migrate profile reads to native atproto getRecord","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-02w.1","depends_on_id":"hypercerts-02w","type":"parent-child","created_at":"2026-03-03T17:47:30.630241808+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02w.2","title":"Migrate profile reads in API update routes — profile/update and bsky/update","description":"## Files\n- app/api/profile/update/route.ts (modify — reads only)\n- app/api/profile/bsky/update/route.ts (modify — reads only)\n\n## What to do\nReplace the profile read operations in these API routes with direct `agent.com.atproto.repo.getRecord()` calls. These routes have BOTH reads and writes — only migrate the reads in this task.\n\n### app/api/profile/update/route.ts — 2 reads to migrate:\n\n**Read 1 (line 46)**: Existence check\n```ts\n// Old:\nexistingProfile = await repo.profile.getCertifiedProfile();\n\n// New:\nconst existingResult = await repo.com.atproto.repo.getRecord({\n  repo: repo.assertDid,\n  collection: 'app.certified.actor.profile',\n  rkey: 'self',\n}).catch(() =\u003e null);\nexistingProfile = existingResult?.data?.value as Record\u003cstring, unknown\u003e | null ?? null;\n```\nRemove the `@ts-expect-error` on this line.\n\n**Read 2 (line 108)**: Read-after-write\n```ts\n// Old:\nconst updated = await repo.profile.getCertifiedProfile();\n\n// New:\nconst updatedResult = await repo.com.atproto.repo.getRecord({\n  repo: repo.assertDid,\n  collection: 'app.certified.actor.profile',\n  rkey: 'self',\n}).catch(() =\u003e null);\nconst updated = updatedResult?.data?.value as Record\u003cstring, unknown\u003e | null ?? null;\n```\nRemove the `@ts-expect-error` on this line.\n\n### app/api/profile/bsky/update/route.ts — 2 reads to migrate:\n\n**Read 1 (line 38)**: Existence check\nSame pattern but with collection `'app.bsky.actor.profile'`.\n\n**Read 2 (line 92)**: Read-after-write\nSame pattern but with collection `'app.bsky.actor.profile'`.\n\n### What to keep as @ts-expect-error (writes — Phase 3-4):\n- `repo.profile.createCertifiedProfile(createParams)` (line 76)\n- `repo.profile.updateCertifiedProfile(updateParams)` (line 103)\n- `repo.profile.createBskyProfile(createParams)` (line 64 in bsky route)\n- `repo.profile.updateBskyProfile(updateParams)` (line 87 in bsky route)\n\nThese write operations stay suppressed until Phase 3-4.\n\n## Don't\n- Don't touch the write operations (createCertifiedProfile, updateCertifiedProfile, createBskyProfile, updateBskyProfile)\n- Don't change form data parsing or validation\n- Don't change response shapes","acceptance_criteria":"1. Profile read operations in both routes use agent.com.atproto.repo.getRecord\n2. @ts-expect-error removed from read lines, kept on write lines\n3. Existence check still works (null when profile doesn't exist)\n4. Read-after-write still returns the updated profile data\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T17:47:47.10957949+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T18:02:44.274501408+06:00","closed_at":"2026-03-03T18:02:44.274501408+06:00","close_reason":"973f3f1 Migrate profile reads to native atproto getRecord","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-02w.2","depends_on_id":"hypercerts-02w","type":"parent-child","created_at":"2026-03-03T17:47:47.116695471+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02w.3","title":"Migrate hypercert list read in app/hypercerts/page.tsx to native listRecords","description":"## Files\n- app/hypercerts/page.tsx (modify)\n\n## What to do\nReplace `ctx.scopedRepo.hypercerts.list({ limit: 100 })` with `ctx.agent.com.atproto.repo.listRecords()`.\n\n### Current code (lines 34-43):\n```ts\nconst [hypercertsResult, pdsUrl] = await Promise.all([\n  // @ts-expect-error\n  ctx.scopedRepo.hypercerts.list({ limit: 100 }),\n  resolveSessionPds(session),\n]);\nconst { records } = hypercertsResult as {\n  records: Array\u003c{ record: any; uri: string }\u003e;\n};\n```\n\n### New code:\n```ts\nconst [hypercertsResult, pdsUrl] = await Promise.all([\n  ctx.agent.com.atproto.repo.listRecords({\n    repo: ctx.activeDid,\n    collection: 'org.hypercerts.claim.activity',\n    limit: 100,\n  }),\n  resolveSessionPds(session),\n]);\n\n// listRecords returns { data: { records: Array\u003c{ uri, cid, value }\u003e } }\nconst records = hypercertsResult.data.records.map(r =\u003e ({\n  uri: r.uri,\n  record: r.value,\n}));\n```\n\n### Key differences:\n- `listRecords` returns `{ data: { records: Array\u003c{ uri: string, cid: string, value: unknown }\u003e } }`\n- The SDK returned `{ records: Array\u003c{ record: any, uri: string }\u003e }` — note the field is `record` not `value`\n- Need to map `value` → `record` to match what the template expects downstream\n\n### Fields used from each record:\n- `record.image` — blob ref, used with `getBlobURL` and `OrgHypercertsDefs.SmallImage`\n- `record.title` — string\n- `record.shortDescription` — string\n- `record.workScope` — string array\n- `record.createdAt` — date string\n- `uri` — AT-URI string, used for routing\n\n### Also:\n- Remove the `@ts-expect-error` comment\n- Remove the `as { records: ... }` type assertion (no longer needed)\n- The `OrgHypercertsDefs` import from `@hypercerts-org/sdk-core` on line 18 is used for image type casting in the template — keep it for now (it's a type import, will be cleaned up in Phase 5)\n- Use `ctx.activeDid` as the repo parameter (this lists the active user's hypercerts)\n\n## Don't\n- Don't change the template/JSX rendering\n- Don't change the image blob URL logic\n- Don't remove the OrgHypercertsDefs import (still used for image type)","acceptance_criteria":"1. ctx.scopedRepo.hypercerts.list is replaced with ctx.agent.com.atproto.repo.listRecords\n2. @ts-expect-error comment is removed\n3. Records are mapped from listRecords format ({ value }) to template format ({ record })\n4. Collection is 'org.hypercerts.claim.activity'\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T17:48:06.622921172+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T18:09:54.445667058+06:00","closed_at":"2026-03-03T18:09:54.445667058+06:00","close_reason":"b702dac Migrate hypercert list to native listRecords","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-02w.3","depends_on_id":"hypercerts-02w","type":"parent-child","created_at":"2026-03-03T17:48:06.626710662+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02w.4","title":"Migrate hypercert get reads in detail and edit pages to native getRecord","description":"## Files\n- app/hypercerts/[hypercertUri]/page.tsx (modify)\n- app/hypercerts/[hypercertUri]/edit/page.tsx (modify)\n\n## What to do\nReplace `viewCtx.scopedRepo.hypercerts.get(decodedUri)` with a direct `agent.com.atproto.repo.getRecord()` call. There are 3 instances total (2 in detail page, 1 in edit page).\n\n### How scopedRepo.hypercerts.get(uri) works:\nIt takes an AT-URI like `at://did:plc:xxx/org.hypercerts.claim.activity/rkey123`, parses it to extract the DID, collection, and rkey, then calls getRecord. We need to do the same parsing.\n\n### Use the existing parseAtUri helper:\n`lib/utils.ts` exports `parseAtUri(uri)` which returns `{ did, collection, rkey } | null`. Use this.\n\n### Pattern:\n```ts\nimport { parseAtUri } from '@/lib/utils';\n\n// Old:\nconst cert = await viewCtx.scopedRepo.hypercerts.get(decodedUri);\n\n// New:\nconst parsed = parseAtUri(decodedUri);\nconst certResult = parsed ? await viewCtx.agent.com.atproto.repo.getRecord({\n  repo: parsed.did,\n  collection: parsed.collection || 'org.hypercerts.claim.activity',\n  rkey: parsed.rkey,\n}).catch(() =\u003e null) : null;\nconst cert = certResult ? { record: certResult.data.value } : null;\n```\n\n### app/hypercerts/[hypercertUri]/page.tsx — 2 reads:\n\n**Read 1 (line 32)** — in `generateMetadata`:\n```ts\n// @ts-expect-error\nconst cert = await viewCtx.scopedRepo.hypercerts.get(decodedUri);\n```\nReplace with getRecord pattern. Fields used: `cert.record.title`, `cert.record.shortDescription`\n\n**Read 2 (line 128)** — in page body:\n```ts\n// @ts-expect-error\nconst cert = await viewCtx.scopedRepo.hypercerts.get(decodedUri);\n```\nReplace with getRecord pattern. Fields used: `cert.record.image` (destructured), `cert.record` (rest spread to component)\n\n### app/hypercerts/[hypercertUri]/edit/page.tsx — 1 read:\n\n**Read 1 (line 66)**:\n```ts\n// @ts-expect-error\nconst cert = await viewCtx.scopedRepo.hypercerts.get(decodedUri);\n```\nReplace with getRecord pattern. Fields used: `cert.record.image` (destructured), `cert.record` (rest spread to form)\n\n### Important:\n- The `ownerDid` is already extracted from the URI earlier in each file via `extractDidFromAtUri(decodedUri)`\n- The `viewCtx` is created with `getRepoContext({ targetDid: ownerDid })` — so `viewCtx.agent` is authenticated as the current user but we read from `ownerDid`'s repo\n- Use `parsed.did` (or `ownerDid`) as the `repo` parameter\n- Remove all `@ts-expect-error` comments on these read lines\n- Keep the `OrgHypercertsDefs` import (used for image type casting in the template)\n\n## Don't\n- Don't change the image blob URL logic\n- Don't change the template/JSX rendering\n- Don't remove OrgHypercertsDefs import","acceptance_criteria":"1. All 3 scopedRepo.hypercerts.get() calls replaced with agent.com.atproto.repo.getRecord\n2. AT-URI is parsed using parseAtUri to extract did, collection, rkey\n3. @ts-expect-error comments removed from read lines\n4. cert.record shape is preserved for downstream template usage\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-03-03T17:48:26.929471737+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T18:08:18.467098671+06:00","closed_at":"2026-03-03T18:08:18.467098671+06:00","close_reason":"4d2f976 Migrate hypercert get reads to native getRecord","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-02w.4","depends_on_id":"hypercerts-02w","type":"parent-child","created_at":"2026-03-03T17:48:26.933008227+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02w.5","title":"Migrate generic record reads in create-actions.ts to native getRecord","description":"## Files\n- lib/create-actions.ts (modify — 4 read functions only)\n\n## What to do\nReplace the 4 generic `ctx.scopedRepo.records.get({ collection, rkey })` calls with `ctx.agent.com.atproto.repo.getRecord()`. These are: getMeasurementRecord, getEvaluationRecord, getEvidenceRecord, getContributorInformationRecord.\n\n### All 4 functions follow the exact same pattern. Current code (e.g., getMeasurementRecord, lines 155-168):\n```ts\nexport const getMeasurementRecord = async (params: {\n  did: string;\n  collection: string;\n  rkey: string;\n}) =\u003e {\n  const { did, collection, rkey } = params;\n  const ctx = await getRepoContext({ targetDid: did });\n  if (!ctx) { throw new Error('...'); }\n\n  // @ts-expect-error\n  const data = await ctx.scopedRepo.records.get({ collection, rkey });\n  if (data?.value) {\n    data.value = await resolveRecordBlobs(data.value, did);\n  }\n  return JSON.parse(JSON.stringify(data));\n};\n```\n\n### New pattern for all 4:\n```ts\nexport const getMeasurementRecord = async (params: {\n  did: string;\n  collection: string;\n  rkey: string;\n}) =\u003e {\n  const { did, collection, rkey } = params;\n  const ctx = await getRepoContext({ targetDid: did });\n  if (!ctx) { throw new Error('...'); }\n\n  const result = await ctx.agent.com.atproto.repo.getRecord({\n    repo: did,\n    collection,\n    rkey,\n  });\n  const data = result.data;\n  if (data?.value) {\n    data.value = await resolveRecordBlobs(data.value, did);\n  }\n  return JSON.parse(JSON.stringify(data));\n};\n```\n\n### Key differences:\n- `ctx.scopedRepo.records.get({ collection, rkey })` → `ctx.agent.com.atproto.repo.getRecord({ repo: did, collection, rkey })`\n- The native API returns `{ data: { uri, cid, value } }` — need to unwrap `.data`\n- The `did` parameter is already available in each function — use it as the `repo` parameter\n- Remove the `@ts-expect-error` comments\n\n### Functions to update (all in lib/create-actions.ts):\n1. `getMeasurementRecord` (~line 145-168)\n2. `getEvaluationRecord` (~line 170-189)\n3. `getEvidenceRecord` (~line 191-210)\n4. `getContributorInformationRecord` (~line 212-230)\n\n### Error handling:\nThe native getRecord throws on 404 (record not found). The current code doesn't have explicit 404 handling — it just returns the data. Keep the same behavior. If you want to be safe, wrap in try/catch and return null on error.\n\n## Don't\n- Don't touch getActiveProfileInfo (handled in task .1)\n- Don't touch any write operations (addContribution, addEvaluation, etc.)\n- Don't change the resolveRecordBlobs call\n- Don't change the JSON.parse(JSON.stringify(data)) serialization","acceptance_criteria":"1. All 4 generic record read functions use ctx.agent.com.atproto.repo.getRecord\n2. @ts-expect-error comments removed from all 4 read operations\n3. repo parameter is set to the did from function params\n4. resolveRecordBlobs still called on data.value\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T17:48:47.317515383+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T18:09:29.571198715+06:00","closed_at":"2026-03-03T18:09:29.571198715+06:00","close_reason":"62abe19 Migrate generic record reads in create-actions.ts to native getRecord","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-02w.5","depends_on_id":"hypercerts-02w","type":"parent-child","created_at":"2026-03-03T17:48:47.321582077+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-02w.6","title":"Verify build passes after all Phase 2 read migrations","description":"## Files\n- Any file that fails pnpm build due to Phase 2 changes\n\n## What to do\nAfter tasks .1 through .5 are complete, run `pnpm build` and fix any remaining type errors caused by the read migrations.\n\n### Expected issues:\n1. **Type mismatches**: The native `getRecord` returns `{ data: { uri, cid, value: unknown } }` — callers may need type assertions on `value` to match what they expect\n2. **Missing imports**: If any file needs `parseAtUri` or other utils that weren't imported before\n3. **Null handling**: Native getRecord throws on 404 instead of returning null — callers that relied on null returns may need try/catch wrappers\n\n### Process:\n1. Run `pnpm build`\n2. Fix each error:\n   - Type errors on read results: add appropriate type assertions\n   - Missing imports: add them\n   - Null handling: wrap in try/catch where needed\n3. Repeat until build succeeds\n\n### Verify no scopedRepo reads remain:\nRun: `grep -rn 'scopedRepo.*get\\|scopedRepo.*list\\|scopedRepo.*profile' --include='*.ts' --include='*.tsx' app/ lib/ providers/`\nThe only remaining scopedRepo references should be WRITE operations (create, update, delete) which are Phase 3-4.\n\n## Don't\n- Don't rewrite write operations\n- Don't change business logic\n- Don't add new features","acceptance_criteria":"1. pnpm build succeeds with exit code 0\n2. No scopedRepo read operations remain (get, list, profile reads)\n3. Only scopedRepo WRITE operations remain with @ts-expect-error comments\n4. All read operations use agent.com.atproto.repo.getRecord or listRecords","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-03T17:49:02.580107888+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T18:11:21.087616705+06:00","closed_at":"2026-03-03T18:11:21.087616705+06:00","close_reason":"af42bf1 Verify build passes after Phase 2 read migrations","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-02w.6","depends_on_id":"hypercerts-02w","type":"parent-child","created_at":"2026-03-03T17:49:02.584825526+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-02w.6","depends_on_id":"hypercerts-02w.1","type":"blocks","created_at":"2026-03-03T17:49:02.592578785+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-02w.6","depends_on_id":"hypercerts-02w.2","type":"blocks","created_at":"2026-03-03T17:49:02.5994908+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-02w.6","depends_on_id":"hypercerts-02w.3","type":"blocks","created_at":"2026-03-03T17:49:02.606054234+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-02w.6","depends_on_id":"hypercerts-02w.4","type":"blocks","created_at":"2026-03-03T17:49:02.612954086+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-02w.6","depends_on_id":"hypercerts-02w.5","type":"blocks","created_at":"2026-03-03T17:49:02.620032356+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-0df","title":"Fix: logout() server action in create-actions.ts does not clear user-did cookie","description":"## Problem\nThe `logout()` server action in `lib/create-actions.ts` (lines 47-53) only revokes the OAuth session via `sdk.revokeSession(session.sub)` but does NOT clear the `user-did` or `active-did` cookies:\n\n```ts\nexport const logout = async () =\u003e {\n  const session = await getSession();\n  if (!session) {\n    return;\n  }\n  sdk.revokeSession(session.sub);\n};\n```\n\nAfter logout, the `user-did` cookie remains set. On the next request, `getRepoContext()` and `getAuthenticatedRepo()` will find the cookie, attempt `sdk.restoreSession(userDid)`, and fail (since the session was revoked). They return `null` gracefully, so the user is effectively logged out — but the stale cookie persists indefinitely.\n\nThis means:\n1. The cookie is never cleared, accumulating stale data in the browser\n2. If the session store (Redis) is cleared but the cookie remains, the user appears logged in to the browser but gets 401s from all API routes\n3. The `active-did` cookie (set by `switchActiveProfile`) is also never cleared\n\n## Fix\nClear both cookies in the logout action:\n```ts\nexport const logout = async () =\u003e {\n  const session = await getSession();\n  if (session) {\n    sdk.revokeSession(session.sub);\n  }\n  const cookieStore = await cookies();\n  cookieStore.delete('user-did');\n  cookieStore.delete('active-did');\n};\n```\n\nNote: The `/api/oauth/logout` route may also handle cookie clearing — verify there is no double-clearing and that the server action and API route are consistent.\n\n## Evidence\n`lib/create-actions.ts` lines 47-53: no cookie deletion on logout","status":"open","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:09.385798088+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:09.385798088+06:00"}
{"id":"hypercerts-0h4","title":"Epic: Improve error messages across the scaffold","description":"Users (including the developer) see generic errors like 'Failed to add measurement', 'Authentication failed. Please try again.', and 'Unable to get repository context' with zero context about what actually went wrong. This makes debugging impossible without checking server logs. Success looks like: every user-facing error toast includes a specific reason (e.g. 'Failed to add measurement: session expired, please log in again'), and every server-side throw includes the operation name and relevant IDs. Key constraint: don't expose internal implementation details or secrets in user-facing messages.","status":"open","priority":2,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-17T18:08:29.842621842+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:15:50.724143802+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-0h4.1","title":"Improve error messages in server actions (lib/create-actions.ts)","description":"## Files\n- lib/create-actions.ts (modify)\n- lib/repo-context.ts (modify)\n\n## What to do\n1. In `getRepoContext()` (repo-context.ts): when returning null, log a structured message. No throw needed since callers check null.\n2. In `create-actions.ts`: replace every `throw new Error(\"Unable to get repository context\")` (lines 84, 123, 142, 160, 178) with a message that includes the function name and operation, e.g. `throw new Error(\"addEvaluation failed: could not establish repository context. The user session may have expired or the target DID is unreachable.\")`. Each of the 5 occurrences should name its own function.\n3. `switchActiveProfile` line 32: change to `throw new Error(\"Cannot switch profiles: no active session found. Please log in first.\")`\n\n## Dont\n- Do not change function signatures or return types\n- Do not add new dependencies\n- Do not expose DIDs or internal IDs in the error message string","acceptance_criteria":"1. Each of the 5 \"Unable to get repository context\" errors now includes the function name and a human-readable reason.\n2. switchActiveProfile error mentions logging in.\n3. All existing tests (if any) still pass: `npm run build` succeeds.\n4. No new dependencies added.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T18:08:43.417341174+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:12:44.367017986+06:00","closed_at":"2026-02-17T18:12:44.367017986+06:00","close_reason":"12f11f9 improve error messages in server actions and repo context","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-0h4.1","depends_on_id":"hypercerts-0h4","type":"parent-child","created_at":"2026-02-17T18:08:43.419018618+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-0h4.2","title":"Improve error messages in API route handlers","description":"## Files\n- app/api/certs/create/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/profile/update/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n- app/api/auth/callback/route.ts (modify)\n- app/api/auth/logout/route.ts (modify)\n\n## What to do\nFor each API route catch block that returns a generic error JSON:\n1. `certs/create/route.ts` line 57-60: change `\"Error creating hypercert\"` to include the original error message: `\"Failed to create hypercert: ${(e as Error).message}\"`. The if-branch on line 54-55 already does this — just make the else-branch consistent.\n2. `certs/add-location/route.ts` line 21-29: the missing-field error message is buggy — it shows the fields that ARE present instead of the ones that are MISSING. Fix the ternary logic: `Missing${!locationType ? \" locationType\" : \"\"}${!lpVersion ? \" lpVersion\" : \"\"}${!srs ? \" srs\" : \"\"}`.\n3. `auth/callback/route.ts`: include the error type/message in the logged output (it already does `console.error` but check the redirect includes a useful `auth_error_description` param).\n4. `auth/logout/route.ts`: include error message in response: `\"Logout failed: ${(e as Error).message}\"`.\n5. `profile/update/route.ts` and `profile/bsky/update/route.ts`: ensure catch blocks return `{ error: \"Profile update failed: \u003creason\u003e\" }` not just generic messages.\n\n## Dont\n- Do not change HTTP status codes\n- Do not change request/response shapes (field names)\n- Do not add new dependencies","acceptance_criteria":"1. certs/add-location validation error correctly lists MISSING fields (not present ones).\n2. Every catch block in the 7 files returns an error message that includes the original Error.message.\n3. `npm run build` succeeds.\n4. No HTTP status codes changed.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-17T18:08:57.623378908+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:12:57.58991568+06:00","closed_at":"2026-02-17T18:12:57.58991568+06:00","close_reason":"4a0ef47 Improve error messages in API route handlers","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-0h4.2","depends_on_id":"hypercerts-0h4","type":"parent-child","created_at":"2026-02-17T18:08:57.625200023+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-0h4.3","title":"Improve error toasts in mutation hooks to show actual error reason","description":"## Files\n- queries/hypercerts/use-add-attachment-mutation.ts (modify)\n- queries/hypercerts/use-add-location-mutation.ts (modify)\n- queries/auth/use-login-mutation.ts (modify)\n- queries/auth/use-logout-mutation.ts (modify)\n- queries/profile/use-update-profile-mutation.ts (modify)\n- queries/profile/use-update-bsky-profile-mutation.ts (modify)\n- components/measurement-form.tsx (modify)\n- components/evaluation-form.tsx (modify)\n- components/contributions-form.tsx (modify)\n\n## What to do\nEach of these files has an `onError` handler that shows a generic toast like `toast.error(\"Failed to add attachment\")` while the actual error with a useful message is sitting right there in the `error` parameter. Apply this pattern to all of them:\n\n```ts\nonError: (error) =\u003e {\n  console.error(\"Add attachment error:\", error);\n  const message = error instanceof Error ? error.message : \"Unknown error\";\n  toast.error(`Failed to add attachment: ${message}`);\n},\n```\n\nSpecific files and their current generic messages to fix:\n- use-add-attachment-mutation.ts line 42: \"Failed to add attachment\"\n- use-add-location-mutation.ts line 35: \"Failed to add location\"  \n- use-login-mutation.ts line 18: \"An error occurred while logging in.\"\n- use-logout-mutation.ts line 18: \"Logout failed\"\n- measurement-form.tsx line 119: \"Failed to add measurement.\"\n- evaluation-form.tsx line 64: \"Failed to add evaluation\"\n- contributions-form.tsx line 108: \"Failed to create contribution\"\n\nNote: use-update-profile-mutation.ts and use-update-bsky-profile-mutation.ts already have the pattern `error instanceof Error ? error.message : fallback` — leave those as-is, they are fine.\n\n## Dont\n- Do not change the console.error lines\n- Do not change onSuccess handlers\n- Do not add new dependencies\n- Do not touch use-update-profile-mutation.ts or use-update-bsky-profile-mutation.ts (they already do this correctly)\n- Do not touch use-create-hypercert-mutation.ts (it already does this correctly)","acceptance_criteria":"1. All 7 listed onError handlers now include error.message in the toast string.\n2. use-update-profile-mutation.ts and use-update-bsky-profile-mutation.ts are NOT modified.\n3. use-create-hypercert-mutation.ts is NOT modified.\n4. `npm run build` succeeds.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T18:09:11.866376784+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:15:17.544874959+06:00","closed_at":"2026-02-17T18:15:17.544874959+06:00","close_reason":"4a5cc49 Improve error toasts to include actual error message","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-0h4.3","depends_on_id":"hypercerts-0h4","type":"parent-child","created_at":"2026-02-17T18:09:11.868235319+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-0h4.3","depends_on_id":"hypercerts-0h4.2","type":"blocks","created_at":"2026-02-17T18:09:28.813024226+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-0h4.4","title":"Improve AuthErrorToast to show specific auth failure reasons","description":"## Files\n- providers/AuthErrorToast.tsx (modify)\n- app/api/auth/callback/route.ts (modify)\n\n## What to do\n1. In `AuthErrorToast.tsx`: the else-branch (line 21) falls back to \"Authentication failed. Please try again.\" when there is no `auth_error_description`. Improve this to at least include the `authError` code itself: `Authentication failed (${authError}). Please try again.` so the user sees something like \"Authentication failed (invalid_request). Please try again.\"\n2. In `callback/route.ts`: ensure that when redirecting on auth error, the `auth_error_description` query param is populated with the actual error message from the caught exception, not left empty.\n\n## Dont\n- Do not change the access_denied case (line 18-19), that message is fine\n- Do not add new dependencies","acceptance_criteria":"1. AuthErrorToast else-branch includes the error code in the displayed message.\n2. callback/route.ts sets auth_error_description in the redirect URL when an error occurs.\n3. `npm run build` succeeds.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-17T18:09:21.768834643+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:11:55.187895481+06:00","closed_at":"2026-02-17T18:11:55.187895481+06:00","close_reason":"676c9bb improve AuthErrorToast to show error code in fallback message","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-0h4.4","depends_on_id":"hypercerts-0h4","type":"parent-child","created_at":"2026-02-17T18:09:21.770563684+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-0j8f","title":"Fix: post-write getBlobURL call in certified profile update route uses fragile as-unknown-as cast on stored blob object (from hypercerts-fpyy.5)","description":"Review of hypercerts-fpyy.5 found a type-safety issue in app/api/profile/update/route.ts.\n\n## Problem\n\nAfter writing the certified profile record, the route reads it back with `getRecord` and then calls `getBlobURL` on the returned `avatar`/`banner` fields (lines 189–204):\n\n```ts\ngetBlobURL(\n  updated?.avatar as unknown as Parameters\u003ctypeof getBlobURL\u003e[0],\n  repo.assertDid,\n  pdsUrl,\n)\n```\n\n`AppCertifiedActorProfile.Record.avatar` is typed as `$Typed\u003cUri\u003e | $Typed\u003cSmallImage\u003e | { $type: string }`, but `getBlobURL` expects `BlobRef | string | undefined`. The double cast hides a genuine type mismatch.\n\nAt runtime this works incidentally because the stored blob object (`blob.original`) has a `ref` property that `getBlobURL` checks for — but this is undocumented and fragile. If the certified profile lexicon is ever updated to store a URI string instead of a blob object, this will silently return `undefined` and the avatar URL will be empty.\n\n## Fix\n\nEither:\n1. Add a helper that safely extracts a URL from the certified profile's avatar union type (checking for `$type: 'blob'` and extracting the CID, or `$type: 'org.hypercerts.defs#uri'` and returning the URI string directly), OR\n2. Extend `getBlobURL` to accept the certified profile avatar union type explicitly.\n\nThis is lower priority than hypercerts-ei70 (the validation failure) but should be addressed to avoid future breakage.","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T13:06:29.915923582+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T13:58:10.820055747+06:00","closed_at":"2026-03-04T13:58:10.820055747+06:00","close_reason":"8cb56d0 Fix: replace fragile as-unknown-as cast with type-safe getCertifiedProfileImageURL helper","dependencies":[{"issue_id":"hypercerts-0j8f","depends_on_id":"hypercerts-fpyy.5","type":"discovered-from","created_at":"2026-03-04T13:06:32.026426125+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-0lg","title":"Epic: Replace ePDS OAuth session cookie with Redis-backed state store","description":"The ePDS OAuth callback frequently fails with 'No OAuth session cookie found' because the epds-oauth-session cookie (sameSite: strict) is not sent on cross-site redirects from the ePDS authorization server back to our callback. The fix is to store transient OAuth state (codeVerifier, dpopPrivateJwk) in Redis keyed by the state parameter — exactly how the standard ATProto flow already works via RedisStateStore. This eliminates the cookie dependency entirely. Success: ePDS login flow works reliably without the session cookie. Key constraint: do not change the standard ATProto flow (Flow A) at all.","status":"open","priority":1,"issue_type":"epic","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:16:06.128799835+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:16:06.128799835+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-0lg.1","title":"Add RedisEpdsStateStore class to redis-state-store.ts","description":"## Files\n- lib/redis-state-store.ts (modify)\n\n## What to do\nAdd a new exported interface and class to the existing file:\n\n```typescript\nexport interface EpdsOAuthState {\n  codeVerifier: string;\n  dpopPrivateJwk: JsonWebKey;\n}\n```\n\nAdd a new class `RedisEpdsStateStore` with the same get/set/del pattern as the existing `RedisStateStore`, but:\n- Prefix: `\"epds-oauth-state:\"` (stored as a const `EPDS_STATE_PREFIX`)\n- Stored type: `EpdsOAuthState` instead of `NodeSavedState`\n- TTL on set: 600 seconds (same as `STATE_EXPIRATION_SECONDS` — reuse the existing constant)\n- Methods: `set(state: string, data: EpdsOAuthState): Promise\u003cvoid\u003e`, `get(state: string): Promise\u003cEpdsOAuthState | undefined\u003e`, `del(state: string): Promise\u003cvoid\u003e`\n- Uses the same `redisClient` import already in the file\n\nThen export a singleton instance from `lib/hypercerts-sdk.ts`:\n```typescript\nimport { RedisEpdsStateStore } from \"./redis-state-store\";\nexport const epdsStateStore = new RedisEpdsStateStore();\n```\n\n## Don't\n- Modify the existing `RedisStateStore` or `RedisSessionStore` classes in any way\n- Change any existing exports or their types\n- Make the class generic — keep it simple and concrete","acceptance_criteria":"1. `RedisEpdsStateStore` class exists in `lib/redis-state-store.ts` with `set`, `get`, `del` methods\n2. Uses prefix `epds-oauth-state:` (distinct from `oauth-state:`)\n3. `set` stores JSON with 600s TTL via `redisClient.set(key, JSON.stringify(data), { EX: 600 })`\n4. `get` returns parsed `EpdsOAuthState` or `undefined`\n5. `del` removes the key\n6. `EpdsOAuthState` interface is exported with `codeVerifier: string` and `dpopPrivateJwk: JsonWebKey`\n7. `epdsStateStore` singleton is exported from `lib/hypercerts-sdk.ts`\n8. Existing `RedisStateStore` and `RedisSessionStore` classes are unchanged\n9. `npm run build` passes (no type errors)","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-25T12:16:21.005695787+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:20:27.900917701+06:00","closed_at":"2026-02-25T12:20:27.900917701+06:00","close_reason":"5b3c9a2 Add RedisEpdsStateStore class and epdsStateStore singleton","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-0lg.1","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:16:21.008473785+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.2","title":"Update ePDS login route to store state in Redis instead of cookie","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n\n## What to do\nReplace the cookie-based session storage with Redis-backed state storage.\n\n### Imports to add:\n```typescript\nimport { epdsStateStore } from \"@/lib/hypercerts-sdk\";\n```\n\n### Imports to remove:\n```typescript\nimport { encodeEpdsSessionCookie, EPDS_SESSION_COOKIE_NAME } from \"@/lib/epds-session-cookie\";\n```\n\n### Changes in the GET handler:\n\n1. **Remove lines 70-74** (the `encodeEpdsSessionCookie(...)` call). Replace with:\n```typescript\nawait epdsStateStore.set(state, {\n  codeVerifier,\n  dpopPrivateJwk: privateJwk,\n});\n```\nThis should go right after the PAR response is parsed successfully (after line 67), before building the auth URL.\n\n2. **Remove lines 88-94** (the `response.cookies.set(EPDS_SESSION_COOKIE_NAME, ...)` block entirely). The response should just be the redirect with no cookie set on it.\n\n3. Keep everything else unchanged — PKCE generation, DPoP keypair generation, PAR request, auth URL construction, redirect response.\n\n## Don't\n- Change the PAR request logic or DPoP proof generation\n- Change the auth URL construction\n- Touch any other route files\n- Import or reference epds-session-cookie.ts","acceptance_criteria":"1. `epdsStateStore.set(state, { codeVerifier, dpopPrivateJwk: privateJwk })` is called after PAR succeeds\n2. No reference to `encodeEpdsSessionCookie` or `EPDS_SESSION_COOKIE_NAME` anywhere in the file\n3. No `response.cookies.set` call for the session cookie (the response is a plain redirect with no Set-Cookie for epds-oauth-session)\n4. The `epdsStateStore` import comes from `@/lib/hypercerts-sdk`\n5. PAR request, DPoP proof, PKCE, auth URL construction are all unchanged\n6. `npm run build` passes","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":20,"created_at":"2026-02-25T12:16:35.169313325+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:24:40.551843199+06:00","closed_at":"2026-02-25T12:24:40.551843199+06:00","close_reason":"87d1920 Replace cookie-based session storage with Redis in login route","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-0lg.2","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:16:35.171583105+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-0lg.2","depends_on_id":"hypercerts-0lg.1","type":"blocks","created_at":"2026-02-25T12:16:35.174638424+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.3","title":"Update ePDS callback route to read state from Redis instead of cookie","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\nReplace all cookie-based session reading with Redis-backed state retrieval.\n\n### Imports to add:\n```typescript\nimport { epdsStateStore } from \"@/lib/hypercerts-sdk\";\n```\n\n### Imports to remove:\n```typescript\nimport { decodeEpdsSessionCookie, EPDS_SESSION_COOKIE_NAME } from \"@/lib/epds-session-cookie\";\n```\n\n### Changes in the GET handler:\n\n1. **Replace lines 31-63** (the entire cookie read + decode + state verify block) with:\n```typescript\n// 2. Retrieve OAuth state from Redis\nconst oauthState = await epdsStateStore.get(state);\nif (!oauthState) {\n  console.error(\"[oauth/callback] No OAuth state found in Redis for state:\", state);\n  return NextResponse.redirect(new URL(\"/?error=auth_failed\", config.baseUrl));\n}\n\nconst { codeVerifier, dpopPrivateJwk } = oauthState;\n```\nNote: state verification is no longer needed because the state IS the key — if Redis returns data for that state, it matches by definition.\n\n2. **Add cleanup after successful token exchange** (after line 103, after confirming `tokenResponse.ok`):\n```typescript\n// Clean up the transient OAuth state from Redis\nawait epdsStateStore.del(state);\n```\nPlace this AFTER the token exchange succeeds (after the `!tokenResponse.ok` check), not before. If the exchange fails, the state should remain so the user can retry within the 600s TTL.\n\n3. **Remove lines 174-181** (step 14 — the block that deletes the `epds-oauth-session` cookie from the response). This is no longer needed since there is no cookie to delete.\n\n4. Keep everything else unchanged — DPoP key restoration, token exchange, NodeSavedSession construction, Redis session write, user-did cookie set.\n\n## Don't\n- Change the token exchange logic\n- Change the NodeSavedSession construction or Redis session write\n- Change the user-did cookie (that stays — it is set on the direct response to the user, not a cross-site redirect)\n- Delete the state before the token exchange succeeds\n- Touch any other route files","acceptance_criteria":"1. `epdsStateStore.get(state)` is called to retrieve OAuth state (no cookie reading)\n2. No reference to `decodeEpdsSessionCookie` or `EPDS_SESSION_COOKIE_NAME` anywhere in the file\n3. No `req.cookies.get(...)` call for the session cookie\n4. `epdsStateStore.del(state)` is called AFTER the token exchange succeeds (after `tokenResponse.ok` check)\n5. No block that deletes the `epds-oauth-session` cookie from the response (old step 14 is gone)\n6. The `user-did` cookie set block (old step 13) is unchanged\n7. DPoP key restoration, token exchange, NodeSavedSession construction are all unchanged\n8. `npm run build` passes","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-25T12:16:51.278680675+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:24:08.058348685+06:00","closed_at":"2026-02-25T12:24:08.058348685+06:00","close_reason":"2039877 Update ePDS callback route to read state from Redis instead of cookie","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-0lg.3","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:16:51.280644596+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-0lg.3","depends_on_id":"hypercerts-0lg.1","type":"blocks","created_at":"2026-02-25T12:16:51.285324907+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.4","title":"Delete lib/epds-session-cookie.ts and remove all references","description":"## Files\n- lib/epds-session-cookie.ts (delete)\n\n## What to do\nDelete the file `lib/epds-session-cookie.ts` entirely. This file contains `encodeEpdsSessionCookie`, `decodeEpdsSessionCookie`, `EPDS_SESSION_COOKIE_NAME`, and the `EpdsOAuthSessionData` interface — all of which are no longer used after the login and callback routes have been updated.\n\n### Verification before deleting:\nRun a grep across the codebase to confirm no file imports from `@/lib/epds-session-cookie` or `lib/epds-session-cookie`. If any reference remains, do NOT delete — report the reference as a blocker.\n\n### After deleting:\nRun `npm run build` to confirm no broken imports.\n\n## Don't\n- Delete the file if any import reference still exists in the codebase\n- Modify any other files in this task (the import removals happen in the login and callback tasks)","acceptance_criteria":"1. `lib/epds-session-cookie.ts` does not exist\n2. No file in the codebase imports from `@/lib/epds-session-cookie` or `lib/epds-session-cookie`\n3. `npm run build` passes","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":10,"created_at":"2026-02-25T12:17:00.784762732+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:26:32.081104448+06:00","closed_at":"2026-02-25T12:26:32.081104448+06:00","close_reason":"c5e426d Delete lib/epds-session-cookie.ts — no longer referenced","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-0lg.4","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:17:00.787117794+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-0lg.4","depends_on_id":"hypercerts-0lg.2","type":"blocks","created_at":"2026-02-25T12:17:00.790645217+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-0lg.4","depends_on_id":"hypercerts-0lg.3","type":"blocks","created_at":"2026-02-25T12:17:00.793197996+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.5","title":"Fix: epdsStateStore.del(state) called after token exchange succeeds but before sub validation — leaked state on invalid sub (from hypercerts-0lg)","description":"Review of hypercerts-0lg found: In app/api/oauth/callback/route.ts, epdsStateStore.del(state) is called at line ~80 immediately after tokenResponse.ok is confirmed (step 9→cleanup), but BEFORE the sub validation check at step 10a. If tokenData.sub is missing or not a DID, the route redirects to /?error=auth_failed — but the Redis state has already been deleted. This is actually correct behaviour (state should be consumed once the code is exchanged), but the placement is misleading: the comment says 'Clean up the transient OAuth state' yet it sits between step 9 and step 10, not at the end of the happy path. More critically, if sessionStore.set() throws (Redis down after token exchange), the state is already deleted and the user gets a 500 with no session written — they cannot retry without starting a new login. The del should be moved to after sessionStore.set() succeeds, or wrapped in a try/finally that still deletes on error. Evidence: callback route lines 80-83 vs lines 95-105 (sub validation) and lines 140-145 (sessionStore.set).","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:32:17.170406123+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:36:49.563913536+06:00","closed_at":"2026-02-25T12:36:49.563913536+06:00","close_reason":"Subsumed by 0lg.6 — GETDEL eliminates the separate del() call entirely, making placement irrelevant","dependencies":[{"issue_id":"hypercerts-0lg.5","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:32:17.171924631+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.6","title":"Fix: no atomic get-and-delete on Redis state — double-callback race condition allows state reuse (from hypercerts-0lg)","description":"## Files\n- lib/redis-state-store.ts (modify)\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\n\n### 1. `lib/redis-state-store.ts` — Make `get()` atomic via GETDEL\n\nChange `RedisEpdsStateStore.get()` to use `redisClient.getDel(key)` instead of `redisClient.get(key)`. This atomically reads and deletes the key in a single Redis round-trip, eliminating the race window where two concurrent callbacks can both read the same state.\n\n```typescript\nasync get(state: string): Promise\u003cEpdsOAuthState | undefined\u003e {\n  const key = `${EPDS_STATE_PREFIX}${state}`;\n  const data = await redisClient.getDel(key);\n  return data ? (JSON.parse(data) as EpdsOAuthState) : undefined;\n}\n```\n\nThe `del()` method stays as-is (it is still useful for explicit cleanup in other scenarios), but it will no longer be called from the callback route.\n\n### 2. `app/api/oauth/callback/route.ts` — Remove the separate `del()` call\n\nRemove the line:\n```typescript\n// Clean up the transient OAuth state from Redis\nawait epdsStateStore.del(state);\n```\n\nThis line (currently around line 83-84) is no longer needed because `get()` now atomically deletes. Removing it also eliminates:\n- The del-placement bug (0lg.5): no separate del to misplace\n- The del-error-propagation bug (0lg.9): no separate del that can throw and redirect a successful auth to an error page\n\n## Don't\n- Change `RedisStateStore` or `RedisSessionStore` (only change `RedisEpdsStateStore`)\n- Change the login route\n- Add any new imports or exports\n- Change the `set()` or `del()` method signatures","acceptance_criteria":"1. `RedisEpdsStateStore.get()` uses `redisClient.getDel(key)` instead of `redisClient.get(key)`\n2. No `epdsStateStore.del(state)` call exists in `app/api/oauth/callback/route.ts`\n3. The `del()` method still exists on `RedisEpdsStateStore` (unchanged)\n4. `RedisStateStore` and `RedisSessionStore` are unchanged\n5. `npm run build` passes","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:32:27.721104579+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:38:41.443450796+06:00","closed_at":"2026-02-25T12:38:41.443450796+06:00","close_reason":"b6e41a0 fix: atomic get-and-delete for ePDS OAuth state","dependencies":[{"issue_id":"hypercerts-0lg.6","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:32:27.723266183+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.7","title":"Cleanup: oauthSessionSecret / OAUTH_SESSION_SECRET is now dead config — remove or document (from hypercerts-0lg)","description":"Review of hypercerts-0lg found: lib/config.ts still exposes config.oauthSessionSecret (mapped from OAUTH_SESSION_SECRET env var) and the comment on that field still says 'Must be 32+ characters. Only needed if ePDS login is used.' Now that lib/epds-session-cookie.ts has been deleted and the Redis-backed store requires no secret, this env var and config field are dead. Operators who read the config will be confused about whether they still need to set OAUTH_SESSION_SECRET. Either remove the field entirely (and update any deployment docs/env templates) or update the comment to say it is no longer required. Evidence: lib/config.ts line ~220 oauthSessionSecret field; grep for OAUTH_SESSION_SECRET returns zero hits in application code after the deletion of epds-session-cookie.ts.","status":"open","priority":3,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:32:37.345170978+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:32:37.345170978+06:00","dependencies":[{"issue_id":"hypercerts-0lg.7","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:32:37.346810625+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.8","title":"Fix: DPoP private key stored in Redis without TLS — add TLS support to redis.ts (from hypercerts-0lg)","description":"Review of hypercerts-0lg found: The DPoP private JWK (including the private 'd' parameter) is stored as plaintext JSON in Redis via RedisEpdsStateStore. The redis.ts client has no TLS configuration — the socket config only sets host/port/reconnect/timeout, with no 'tls: true' or certificate options. The .env.example documents cloud Redis with only REDIS_PASSWORD, no TLS flag. In production, if Redis is accessed over a network (cloud Redis, Railway, Upstash, etc.), the private key material transits and rests unencrypted.\n\nEvidence: lib/redis.ts lines 4-15 — no tls field in socket config. lib/redis-state-store.ts line 68 — JSON.stringify(data) stores dpopPrivateJwk including 'd' parameter. .env.example lines 48-51 — cloud Redis example has no TLS env var.\n\nFix required:\n1. Add REDIS_TLS env var support to lib/redis.ts (set socket.tls = true when REDIS_TLS=true)\n2. Document REDIS_TLS=true in .env.example for cloud Redis deployments\n3. Consider whether AES-256-GCM encryption of the JWK value at rest (before storing in Redis) is warranted, similar to the approach used in hypercerts-6ge.7 for the cookie\n\nSeverity: high — private key material exposed in transit/at rest without TLS on cloud deployments.","status":"open","priority":1,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:33:39.544009956+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:33:39.544009956+06:00","dependencies":[{"issue_id":"hypercerts-0lg.8","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:33:39.545526265+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0lg.9","title":"Fix: epdsStateStore.del() not wrapped in try/catch — successful auth redirects to error page on Redis failure (from hypercerts-0lg)","description":"Review of hypercerts-0lg found: In app/api/oauth/callback/route.ts, the call to epdsStateStore.del(state) (line ~83) is NOT wrapped in its own try/catch. It sits inside the outer try/catch that redirects to /?error=auth_failed on any exception. If Redis throws during del() (e.g. connection lost between the successful token exchange and the cleanup), the outer catch fires and the user is redirected to an error page — even though their session was already written to Redis and they are fully authenticated.\n\nEvidence: callback route — epdsStateStore.del(state) is called after tokenResponse.ok check and after sessionStore.set(), but before the redirect response is built. The outer catch at the bottom of the handler redirects to /?error=auth_failed unconditionally.\n\nFix required: Wrap the del() call in its own try/catch that logs the error but does not rethrow. The key will expire naturally after 600s via TTL. Example:\n  try {\n    await epdsStateStore.del(state);\n  } catch (delErr) {\n    console.error('[oauth/callback] Failed to delete OAuth state from Redis (non-fatal):', delErr);\n  }\n\nFile: app/api/oauth/callback/route.ts","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:33:50.913376712+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:36:49.707271381+06:00","closed_at":"2026-02-25T12:36:49.707271381+06:00","close_reason":"Subsumed by 0lg.6 — GETDEL eliminates the separate del() call entirely, so there is no del() to wrap in try/catch","dependencies":[{"issue_id":"hypercerts-0lg.9","depends_on_id":"hypercerts-0lg","type":"parent-child","created_at":"2026-02-25T12:33:50.91576978+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0p8","title":"Fix: epds-oauth-session cookie deletion missing security flags (from hypercerts-dhz)","description":"## Review finding from hypercerts-dhz (ePDS OAuth integration)\n\n**File:** app/api/oauth/callback/route.ts, lines 148-151\n\n**Problem:**\nThe `epds-oauth-session` cookie is deleted with only `maxAge: 0` and `path: '/'`:\n```ts\nresponse.cookies.set(EPDS_SESSION_COOKIE_NAME, '', {\n  maxAge: 0,\n  path: '/',\n});\n```\n\nThe original cookie was set with `httpOnly: true`, `secure: config.isProduction`, `sameSite: 'lax'`. Some browsers (notably Safari) require the deletion Set-Cookie to match the original cookie's security attributes to successfully delete it. If the flags don't match, the browser may ignore the deletion, leaving the signed session cookie alive past the callback.\n\n**Fix:**\n```ts\nresponse.cookies.set(EPDS_SESSION_COOKIE_NAME, '', {\n  maxAge: 0,\n  path: '/',\n  httpOnly: true,\n  secure: config.isProduction,\n  sameSite: 'lax',\n});\n```\n\n**Secondary finding:** The `user-did` cookie (line 140-145) is missing `sameSite`. This is consistent with the existing `app/api/auth/callback/route.ts` flow (which also omits it), but both should have `sameSite: 'lax'` to prevent CSRF on the session cookie.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:28:35.521697454+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:41.380204923+06:00","closed_at":"2026-02-24T17:12:41.380204923+06:00","close_reason":"Already fixed in dhz.12 (commit c323ec7). Cookie deletion now includes httpOnly, secure, sameSite: lax flags (callback/route.ts lines 147-153).","dependencies":[{"issue_id":"hypercerts-0p8","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:28:40.514868639+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-0xi","title":"Fix: DPoP nonce retry only fires on HTTP 400 — misses 401 use_dpop_nonce errors (from hypercerts-dhz)","description":"Review of hypercerts-dhz found: In lib/epds-helpers.ts fetchWithDpopRetry() (line 161), the nonce retry is gated on `res.status === 400`. However, RFC 9449 §5 specifies that a server MAY return HTTP 401 with error=use_dpop_nonce when a nonce is required. The ePDS skill reference (epds-login SKILL.md) also shows the retry should fire on any non-ok response that includes a dpop-nonce header, not just 400.\n\nIf ePDS ever returns 401 for the nonce challenge (which is spec-compliant), the retry is skipped and the caller sees a permanent failure. The fix is to remove the `res.status === 400` guard and retry whenever `dpop-nonce` header is present and the response is not ok:\n\n```typescript\nif (!res.ok) {\n  const dpopNonce = res.headers.get('dpop-nonce');\n  if (dpopNonce) {  // remove \u0026\u0026 res.status === 400\n    // retry with nonce\n  }\n}\n```\n\nEvidence: lib/epds-helpers.ts line 161: `if (dpopNonce \u0026\u0026 res.status === 400)`","status":"closed","priority":1,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:25:40.94783643+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:27.336509425+06:00","closed_at":"2026-02-24T17:12:27.336509425+06:00","close_reason":"Already fixed in dhz.11 (commit b4f7ed9). Line 161 now checks res.status === 400 || res.status === 401.","dependencies":[{"issue_id":"hypercerts-0xi","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:25:44.632546991+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-18a","title":"Fix: email input Enter key triggers handle login form submit (from hypercerts-dhz.8/dhz.10)","description":"Review of hypercerts-dhz.8 and dhz.10 found: The email InputGroupInput (line 97-103 of components/login-dialog.tsx) is inside the outer \u003cform onSubmit={handleSubmit}\u003e that handles handle-based login. Pressing Enter in the email field triggers handleSubmit(), which calls loginMutation.mutate(handle) with whatever is in the handle field — not the email login flow. This is a UX bug: the user types their email, presses Enter expecting to start the ePDS flow, but instead triggers the handle-based ATProto login (or a login with an empty handle). Fix: either (a) move the email section outside the \u003cform\u003e tag, or (b) add onKeyDown={(e) =\u003e { if (e.key === 'Enter') { e.preventDefault(); /* trigger email login */ } }} to the email input. Evidence: login-dialog.tsx line 42 opens \u003cform onSubmit={handleSubmit}\u003e, line 118 closes it — the email InputGroup at lines 92-116 is inside this form.","status":"closed","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:26:18.196284932+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:13:07.895480176+06:00","closed_at":"2026-02-24T17:13:07.895480176+06:00","close_reason":"Already fixed in dhz.16 (commit dcac8b3). Handle and Email are now separate components (HandleForm and EmailForm) with separate forms. Email form uses window.location.href, not a form submit. No cross-form Enter key issue.","dependencies":[{"issue_id":"hypercerts-18a","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:26:27.318748627+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-1c2","title":"Nit: eslint.config.mjs overrides react-compiler/react-compiler from 'error' to 'warn' with no explanatory comment","description":"Review of eslint.config.mjs found a silent severity override with no explanation.\n\nEvidence:\n- eslint.config.mjs line 11: 'reactCompiler.configs.recommended' — this sets 'react-compiler/react-compiler': 'error'\n- eslint.config.mjs lines 12-16: immediately overrides to 'react-compiler/react-compiler': 'warn'\n- No comment explains why the recommended severity is being downgraded\n\nThe override is intentional (PR description says 'warn mode') but without a comment, future maintainers may not understand why the recommended config is being overridden. This creates confusion about whether the override is deliberate or accidental.\n\nFix: Add a comment above the override block explaining the intent, e.g.:\n// Downgrade from recommended 'error' to 'warn' — react-compiler violations are advisory during migration\n{\n  rules: {\n    'react-compiler/react-compiler': 'warn',\n  },\n}","status":"open","priority":4,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T14:19:36.976799726+06:00","created_by":"kzoeps","updated_at":"2026-02-27T14:19:36.976799726+06:00"}
{"id":"hypercerts-1e9","title":"Fix: Navbar has no mobile menu — nav links hidden on mobile with no alternative (navbar.tsx)","description":"## File\\ncomponents/navbar.tsx lines 71-94\\n\\n## Severity\\nMEDIUM — Mobile responsiveness\\n\\n## Problem\\nThe navigation links (Hypercerts, Create) are hidden on mobile with 'hidden md:flex'. There is no hamburger menu or alternative navigation provided for mobile users. On small screens, signed-in users can only access the user dropdown (avatar button) but cannot navigate to /hypercerts or /hypercerts/create without typing the URL directly.\\n\\n## Evidence\\n```tsx\\n\u003cdiv className=\"hidden items-center gap-1 md:flex\"\u003e\\n  \u003cLink href=\"/hypercerts\"\u003eHypercerts\u003c/Link\u003e\\n  \u003cLink href=\"/hypercerts/create\"\u003eCreate\u003c/Link\u003e\\n\u003c/div\u003e\\n// No mobile menu equivalent\\n```\\n\\n## Fix\\nAdd a mobile navigation option. Options:\\n1. Add nav links to the user dropdown menu (simplest fix)\\n2. Add a hamburger button that opens a mobile drawer/sheet\\n3. Move nav links into the dropdown for all screen sizes\\n\\nSimplest fix — add links to the existing DropdownMenuContent:\\n```tsx\\n\u003cDropdownMenuItem\u003e\\n  \u003cLink href=\"/hypercerts\"\u003eHypercerts\u003c/Link\u003e\\n\u003c/DropdownMenuItem\u003e\\n\u003cDropdownMenuItem\u003e\\n  \u003cLink href=\"/hypercerts/create\"\u003eCreate\u003c/Link\u003e\\n\u003c/DropdownMenuItem\u003e\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:49.86223198+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:49.86223198+06:00"}
{"id":"hypercerts-1hr","title":"Epic: Resolve remaining CodeRabbit comments for PR #7 (batch 2)","description":"Resolve 7 remaining open CodeRabbit inline review comments on PR #7 (quick-fixes branch). Covers: JWK allowlist security fix, logout error leak, evaluations loading/error race, image-uploader accessibility, loose URL check, stale step label, and prettierignore for lexicons. 3 earlier comments (add-location, profile/update, SignedInProvider) were already fixed by epic hypercerts-tlw.","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-20T22:01:37.61466316+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:10:54.539021728+06:00","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-1hr.1","title":"Switch JWK key stripping from denylist to allowlist","description":"## Files\n- app/jwks.json/route.ts (modify)\n\n## What to do\nCodeRabbit flagged: security issue at line 68.\n\n**Original comment:**\n\u003e Private key stripping may be incomplete for RSA JWKs. The filter only removes `d`, `use`, and `key_ops`. For EC keys (ES256, which ATProto typically uses), this is sufficient. However, if RSA keys are ever used, the fields `p`, `q`, `dp`, `dq`, `qi`, and `oth` are also private and would be leaked through this endpoint. Consider using an allowlist of public fields instead of a denylist.\n\n**Suggested fix:**\nReplace the `PRIVATE_KEY_FIELDS` denylist approach (lines 51-68) with a `PUBLIC_JWK_FIELDS` allowlist:\n\n```typescript\nconst PUBLIC_JWK_FIELDS = [\n  \"kty\", \"crv\", \"x\", \"y\",   // EC public fields\n  \"n\", \"e\",                  // RSA public fields\n  \"kid\", \"alg\",              // Metadata\n] as const;\nconst keys = (privateKey.keys as Array\u003c{ [key: string]: unknown }\u003e).map(\n  (jwkWithPrivate) =\u003e {\n    const publicJwk = Object.fromEntries(\n      Object.entries(jwkWithPrivate).filter(\n        ([k]) =\u003e PUBLIC_JWK_FIELDS.includes(k as (typeof PUBLIC_JWK_FIELDS)[number]),\n      ),\n    );\n    return {\n      ...publicJwk,\n      key_ops: [\"verify\"],\n    };\n  },\n);\n```\n\n**Context:**\nThe route serves the public JWKS endpoint at /jwks.json. The current denylist on line 51 only removes [\"d\", \"use\", \"key_ops\"] which is insufficient for RSA keys. An allowlist is safer because any new private fields added to the JWK spec would be excluded by default.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818882972\n\n## Dont\n- Dont change the existing env var check or JSON parse logic (lines 16-30)\n- Dont change the JWK structure validation guard added by hypercerts-tlw.6\n- Dont change the response headers or cache control\n- Dont introduce new dependencies","acceptance_criteria":"The PRIVATE_KEY_FIELDS denylist is replaced with a PUBLIC_JWK_FIELDS allowlist containing only public JWK fields (kty, crv, x, y, n, e, kid, alg). The key_ops: [\"verify\"] is still added to each key. No RSA private fields (p, q, dp, dq, qi, oth, d) can appear in the response. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-20T22:01:57.986701636+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:09:02.111848049+06:00","closed_at":"2026-02-20T22:09:02.111848049+06:00","close_reason":"9555b4f security: replace JWK denylist with PUBLIC_JWK_FIELDS allowlist to prevent RSA private field leaks","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-1hr.1","depends_on_id":"hypercerts-1hr","type":"parent-child","created_at":"2026-02-20T22:01:57.988622139+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-1hr.2","title":"Remove raw error leak in logout route","description":"## Files\n- app/api/auth/logout/route.ts (modify)\n\n## What to do\nCodeRabbit flagged: security issue at line 21.\n\n**Original comment:**\n\u003e Raw error message leaked to the client. `(e as Error).message` may expose internal details (e.g., Redis connection errors, session store internals) in the API response. This is also inconsistent with the callback route which deliberately uses a generic error code. Additionally, if `e` is not an `Error` instance, `.message` will be `undefined`.\n\n**Suggested fix:**\n1. Replace the error response to use a generic message instead of the raw error:\n```diff\n     return NextResponse.json(\n-      { error: `Logout failed: ${(e as Error).message}` },\n+      { error: \"Logout failed\" },\n       { status: 500 },\n     );\n```\n2. Keep the existing server-side logging of the full error (the console.error on the line above).\n\n**Context:**\nThe logout route catch block on line 17 already logs the full error server-side with console.error. The client response should not include the raw error message. The auth callback route already uses a generic error pattern — this should be consistent.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818882953\n\n## Dont\n- Dont change the console.error logging line\n- Dont change the HTTP status code (keep 500)\n- Dont change the try block logic\n- Dont introduce new dependencies","acceptance_criteria":"The logout route catch block returns { error: \"Logout failed\" } (or similar generic message) without including (e as Error).message or any dynamic error content. The console.error still logs the full error. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-20T22:02:06.556297444+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:07:29.960897456+06:00","closed_at":"2026-02-20T22:07:29.960897456+06:00","close_reason":"b2c46cb fix: remove raw error leak in logout route","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-1hr.2","depends_on_id":"hypercerts-1hr","type":"parent-child","created_at":"2026-02-20T22:02:06.558641957+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-1hr.3","title":"Fix loading/error state race in evaluations section","description":"## Files\n- components/hypercert-evaluations-section.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug at line 87.\n\n**Original comment:**\n\u003e Loading and error states can render simultaneously. If `isLoadingLinks` is false but `isLoadingDetails` is true while `isErrorLinks` is also true, both the skeleton and error panel will render at the same time. Consider using an if/else-if chain or guarding error rendering with `!isLoading`.\n\n**Suggested fix:**\nReplace the three separate conditional blocks (lines 75-93) with a single if/else-if chain:\n\n```tsx\n{isLoading ? (\n  \u003cdiv className=\"space-y-4\"\u003e\n    \u003cEvaluationSkeleton /\u003e\n  \u003c/div\u003e\n) : isError ? (\n  \u003cdiv className=\"glass-panel rounded-xl p-6 border border-red-500/20 bg-red-500/5\"\u003e\n    \u003cp className=\"text-sm font-[family-name:var(--font-outfit)] text-red-500\"\u003e\n      Failed to load evaluations.\n    \u003c/p\u003e\n  \u003c/div\u003e\n) : (\n  \u003c\u003e\n    {evaluations \u0026\u0026 evaluations.length \u003e 0 ? (\n      // ... existing content rendering\n    ) : (\n      // ... existing empty state\n    )}\n  \u003c/\u003e\n)}\n```\n\nThe key change: the three separate `{isLoading ? ... : null}`, `{isError ? ... : null}`, `{!isLoading \u0026\u0026 !isError ? ... : null}` blocks become a single ternary chain so only one state renders at a time.\n\n**Context:**\nThe component has `isLoading` (combined from isLoadingLinks and isLoadingDetails) and `isError` (combined from isErrorLinks and isErrorDetails). Currently lines 75-93 render them as three independent blocks, allowing skeleton and error to show simultaneously.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818882983\n\n## Dont\n- Dont change the useMemo hooks or data fetching logic\n- Dont change the EvaluationSkeleton or error panel styling\n- Dont change the content rendering (evaluations.map)\n- Dont introduce new dependencies","acceptance_criteria":"The three separate conditional render blocks ({isLoading ? ...}, {isError ? ...}, {!isLoading \u0026\u0026 !isError ? ...}) are replaced with a single if/else-if ternary chain so only one of loading, error, or content renders at a time. The skeleton never appears alongside the error panel. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-20T22:02:16.462598935+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:08:00.042161481+06:00","closed_at":"2026-02-20T22:08:00.042161481+06:00","close_reason":"9555b4f fix: replace parallel conditional blocks with if/else-if chain in evaluations section","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-1hr.3","depends_on_id":"hypercerts-1hr","type":"parent-child","created_at":"2026-02-20T22:02:16.464476487+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-1hr.4","title":"Replace label with button for keyboard accessibility in image uploader","description":"## Files\n- components/image-uploader.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug (accessibility) at line 98.\n\n**Original comment:**\n\u003e `\u003clabel\u003e` without an associated input is not keyboard-accessible. This `\u003clabel\u003e` is not linked to any input (no `htmlFor`, no nested control) and is only clickable via the `onClick` handler. Unlike `\u003cbutton\u003e`, a `\u003clabel\u003e` is not keyboard-focusable by default, so keyboard-only users cannot trigger the file picker. Replace with a `\u003cbutton type=\"button\"\u003e` for proper semantics and keyboard support.\n\n**Suggested fix:**\nReplace the `\u003clabel\u003e` element (lines 87-98) with a `\u003cbutton type=\"button\"\u003e`:\n\n```diff\n-            \u003clabel\n-              onClick={() =\u003e inputRef.current?.click()}\n+            \u003cbutton\n+              type=\"button\"\n+              onClick={() =\u003e inputRef.current?.click()}\n               className={cn(\n                 \"flex flex-col items-center justify-center w-full h-full cursor-pointer border-2 border-dashed border-create-accent/20 hover:border-create-accent/40 hover:bg-create-accent/5 transition-all duration-200\",\n                 isBanner ? \"rounded-none\" : \"rounded-full\",\n               )}\n             \u003e\n               \u003cCamera className=\"w-5 h-5 text-create-accent/50 mb-1\" /\u003e\n               \u003cspan className=\"text-[10px] font-[family-name:var(--font-outfit)] font-medium text-muted-foreground\"\u003e\n                 Upload\n               \u003c/span\u003e\n-            \u003c/label\u003e\n+            \u003c/button\u003e\n```\n\nAlso add `aria-label=\"Upload image\"` to the button for screen reader support.\n\n**Context:**\nThe `\u003clabel\u003e` on line 87 triggers a hidden file input via onClick but has no htmlFor or nested input, making it inaccessible to keyboard users. A `\u003cbutton\u003e` is natively focusable and keyboard-activatable.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818883002\n\n## Dont\n- Dont change the hidden file input element\n- Dont change the onClick handler logic\n- Dont change the className styling (keep it identical)\n- Dont change the Camera icon or Upload text\n- Dont introduce new dependencies","acceptance_criteria":"The \u003clabel\u003e element is replaced with \u003cbutton type=\"button\"\u003e. The button has an aria-label attribute (e.g., \"Upload image\"). The onClick, className, and children (Camera icon + Upload span) are preserved. No \u003clabel\u003e element without htmlFor remains in the upload trigger area. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-20T22:02:26.664518801+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:05:05.276566115+06:00","closed_at":"2026-02-20T22:05:05.276566115+06:00","close_reason":"836c8e0 fix: replace label with button for keyboard accessibility","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-1hr.4","depends_on_id":"hypercerts-1hr","type":"parent-child","created_at":"2026-02-20T22:02:26.668938447+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-1hr.5","title":"Fix loose includes(https) URL check in measurement view","description":"## Files\n- components/hypercert-measurement-view.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug at line 118.\n\n**Original comment:**\n\u003e `includes(\"https\")` is a loose URL protocol check. `uri?.includes(\"https\")` would match any URI containing \"https\" anywhere in the string (e.g., a path segment). Use `uri?.startsWith(\"https\")` for a more precise protocol check.\n\n**Suggested fix:**\nReplace on line 117:\n```diff\n-                    uri={uri?.includes(\"https\") ? uri : getPDSlsURI(uri)}\n+                    uri={uri?.startsWith(\"https\") ? uri : getPDSlsURI(uri)}\n```\n\n**Context:**\nThe URILink component receives a uri prop. The check determines whether to use the uri directly (if it is an https URL) or to wrap it with getPDSlsURI(). Using includes would incorrectly match URIs like \"at://did:plc:xxx/app.certified.https.something\" that contain \"https\" in a path segment.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818882990\n\n## Dont\n- Dont change the URILink component itself\n- Dont change getPDSlsURI function\n- Dont change other files (note: hypercert-evaluation-view.tsx has similar patterns but is NOT in scope of this comment)\n- Dont introduce new dependencies","acceptance_criteria":"Line 117 of hypercert-measurement-view.tsx uses uri?.startsWith(\"https\") instead of uri?.includes(\"https\"). No other lines in this file are changed. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-20T22:02:34.355413896+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:09:42.87324151+06:00","closed_at":"2026-02-20T22:09:42.87324151+06:00","close_reason":"87309cc Fix loose includes(https) URL check in measurement view","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-1hr.5","depends_on_id":"hypercerts-1hr","type":"parent-child","created_at":"2026-02-20T22:02:34.357378692+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-1hr.6","title":"Fix stale Step 2 of 6 label in form skeleton","description":"## Files\n- components/form-skeletons.tsx (modify)\n\n## What to do\nCodeRabbit flagged: style issue at line 116.\n\n**Original comment:**\n\u003e Stale step label: \"Step 2 of 6\" in a 5-step flow. The PR objectives mention correcting hardcoded step labels for a 5-step flow, but `ContributionFormSkeleton` still shows \"Step 2 of 6\". The TODO comment suggests this step is currently disabled — if so, this is a low-risk inconsistency, but it may confuse future contributors re-enabling this step.\n\n**Suggested fix:**\nOn line 115, change the stepLabel from \"Step 2 of 6\" to \"Step 2 of 5\":\n```diff\n-      \u003cFormInfoHeaderSkeleton stepLabel=\"Step 2 of 6\" /\u003e{\" \"}\n+      \u003cFormInfoHeaderSkeleton stepLabel=\"Step 2 of 5\" /\u003e{\" \"}\n```\n\nAlso check if there is a TODO comment nearby and update or remove it to reflect the current 5-step flow.\n\n**Context:**\nThe contributions form step is currently disabled (TODO comment on line 115-116 area). The skeleton shows \"Step 2 of 6\" but the active flow has 5 steps. Note: contributions-form.tsx line 138 also has \"Step 2 of 6\" with a TODO — fix that too if present in the same file, but the CodeRabbit comment specifically targets form-skeletons.tsx.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818882980\n\n## Dont\n- Dont change other step labels in other skeleton components\n- Dont change the FormInfoHeaderSkeleton component itself\n- Dont introduce new dependencies","acceptance_criteria":"The stepLabel in ContributionFormSkeleton (form-skeletons.tsx) reads \"Step 2 of 5\" instead of \"Step 2 of 6\". Any associated TODO comment is updated to reflect the 5-step flow. npm run build succeeds.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-20T22:02:44.689477066+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:05:31.252659769+06:00","closed_at":"2026-02-20T22:05:31.252659769+06:00","close_reason":"a701aa0 fix: update stale Step 2 of 6 label to Step 2 of 5","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-1hr.6","depends_on_id":"hypercerts-1hr","type":"parent-child","created_at":"2026-02-20T22:02:44.693037491+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-1hr.7","title":"Add lexicons/** to .prettierignore","description":"## Files\n- .prettierignore (modify)\n\n## What to do\nCodeRabbit flagged: style issue at lexicons/types/app/certified/location.ts line 53.\n\n**Original comment:**\n\u003e Add `lexicons/**` to `.prettierignore` to prevent formatting churn on code regeneration. This file is marked `GENERATED CODE - DO NOT MODIFY` (line 2) but has been formatted with Prettier. The `.prettierignore` file does not currently exclude the `lexicons/` directory, so re-running the code generator will produce unformatted output and create diff noise. Add `lexicons/**` to `.prettierignore` to align with the ESLint `globalIgnores` already added in this PR.\n\n**Suggested fix:**\nAdd `lexicons/**` to the .prettierignore file. Place it near the other directory ignores:\n```diff\n .beads/\n vendor/\n+lexicons/\n```\n\n**Context:**\nThe .prettierignore currently has: node_modules/, .next/, out/, build/, coverage/, *.tgz, pnpm-lock.yaml, package-lock.json, .beads/, vendor/. The ESLint config already ignores lexicons/ via globalIgnores. This makes Prettier consistent with ESLint.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818883016\n\n## Dont\n- Dont remove any existing entries from .prettierignore\n- Dont modify any lexicon files themselves\n- Dont introduce new dependencies","acceptance_criteria":".prettierignore contains a lexicons/ or lexicons/** entry. All existing entries are preserved. npm run build succeeds.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-20T22:02:53.128077996+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-20T22:07:24.626093248+06:00","closed_at":"2026-02-20T22:07:24.626093248+06:00","close_reason":"407c93c Add lexicons/ to .prettierignore","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-1hr.7","depends_on_id":"hypercerts-1hr","type":"parent-child","created_at":"2026-02-20T22:02:53.130168278+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-1u2","title":"Fix: dcb.5 only fixed 2 of 3 routes — auth/logout fix was already done in prior epic (hypercerts-1hr.2)","description":"Review of hypercerts-dcb.5 found: The task spec listed 3 routes to fix (add-location, profile/update, auth/logout), but commit f5fbb5d only touched 2 files (add-location and profile/update). The logout route fix was already applied in an earlier commit (b2c46cb, hypercerts-1hr.2), so the net result is correct — all 3 routes are clean. However, the dcb.5 close reason and commit message do not mention auth/logout, which could confuse future reviewers.\n\n**Current state (correct):**\n- app/api/auth/logout/route.ts: returns `{ error: \"Logout failed\" }` — no leak ✓ (fixed by hypercerts-1hr.2)\n- app/api/certs/add-location/route.ts: returns `{ error: \"Internal server error\" }` — no leak ✓ (fixed by dcb.5)\n- app/api/profile/update/route.ts: returns `{ error: \"Profile update failed\" }` — no leak ✓ (fixed by dcb.5)\n\n**Action:** Update the dcb.5 close reason or add a note clarifying that auth/logout was already handled by hypercerts-1hr.2 so the task is fully satisfied. No code change needed.","status":"open","priority":3,"issue_type":"task","owner":"kzoepa@gmail.com","created_at":"2026-02-24T14:52:42.508022207+06:00","created_by":"kzoeps","updated_at":"2026-02-24T14:52:42.508022207+06:00","dependencies":[{"issue_id":"hypercerts-1u2","depends_on_id":"hypercerts-dcb.5","type":"discovered-from","created_at":"2026-02-24T14:52:48.863972032+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-23q","title":"Fix: CI workflow does not trigger on loopback branch — PRs from loopback won't be checked","description":"Review of .github/workflows/ci.yml found: the workflow only triggers on pushes/PRs to 'main' and 'develop' branches (lines 5-7). The active development branch is 'loopback', which is not in the trigger list.\n\nEvidence:\n- .github/workflows/ci.yml lines 4-7:\n  on:\n    push:\n      branches: [main, develop]\n    pull_request:\n      branches: [main, develop]\n- The current branch is 'loopback' (git branch shows * loopback)\n- All 156 changed files are on the loopback branch\n\nThis means no CI runs on any push to loopback, and no CI runs on PRs targeting loopback. Developers get no automated feedback.\n\nSuggested fix:\nAdd 'loopback' to the branch lists, or use a wildcard:\n  on:\n    push:\n      branches: [main, develop, loopback]\n    pull_request:\n      branches: [main, develop, loopback]\n\nOr if loopback is a long-lived integration branch, consider triggering on all branches:\n  on:\n    push:\n    pull_request:","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:41.881134389+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:41.881134389+06:00"}
{"id":"hypercerts-26k","title":"Fix: CI workflow missing type-check and build steps — TypeScript errors won't be caught","description":"Review of the loopback branch CI/CD tooling found: .github/workflows/ci.yml (line 14) only runs 'pnpm lint' and 'pnpm format:check'. There is no 'pnpm build' step and no 'npx tsc --noEmit' step. This means TypeScript type errors and Next.js build failures will NOT be caught by CI — they will only surface when a developer runs 'pnpm build' locally or when a deployment fails.\n\nEvidence:\n- .github/workflows/ci.yml lines 34-38: only Lint and Format check steps\n- Commit e9efe7f intentionally removed the build step (message: 'remove build step from CI workflow (hypercerts-amn)') — but no type-check step was added as a replacement\n- DEVELOPMENT.md line 298 documents 'npx tsc --noEmit' as the way to check types, but this is not in CI\n- 'pnpm build' currently passes (verified locally), so re-adding it is safe\n\nSuggested fix:\nAdd two steps to .github/workflows/ci.yml after 'Install dependencies':\n  - name: Type check\n    run: npx tsc --noEmit\n  - name: Build\n    run: pnpm build\n\nIf build is too slow for CI, at minimum add the type-check step. Also add a 'type-check' script to package.json: 'tsc --noEmit' so it can be run as 'pnpm type-check' consistently.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:32.699949028+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:32.699949028+06:00"}
{"id":"hypercerts-276","title":"Fix: proxy.ts is dead code — localhost redirect never executes, README/docs claim is false","description":"Review of proxy.ts and the Next.js build output found that the localhost→127.0.0.1 redirect is never executed.\n\nEvidence:\n- proxy.ts exists at project root with a 'proxy()' function and 'config' matcher\n- Next.js middleware MUST be in a file named 'middleware.ts' (not proxy.ts) to be auto-loaded\n- find command: no middleware.ts exists anywhere in the project\n- grep: proxy.ts is not imported by any other file\n- .next/server/middleware-manifest.json: { 'middleware': {}, 'sortedMiddleware': [], 'functions': {} } — NO middleware is registered\n- next.config.ts line 8: 'skipProxyUrlNormalize: true' — this option is also unnecessary since no middleware runs\n\nImpact (HIGH):\n- README.md line 45: 'The app automatically redirects localhost to 127.0.0.1' — this is FALSE\n- .env.example line 17: 'MUST use 127.0.0.1 (not localhost) for loopback oauth to work' — correct, but the 'automatic redirect' claim in README is misleading\n- Users who access http://localhost:3000 will NOT be redirected; OAuth will fail silently\n\nSuggested fix:\nRename proxy.ts to middleware.ts at the project root. Next.js will automatically pick it up. The 'config' export with matcher '/:path*' is already correct middleware config format. No other changes needed.\n\nAfter renaming, verify:\n1. .next/server/middleware-manifest.json shows the middleware registered\n2. Accessing http://localhost:3000 redirects to http://127.0.0.1:3000","status":"open","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:48:08.770453871+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:48:08.770453871+06:00","dependencies":[{"issue_id":"hypercerts-276","depends_on_id":"hypercerts-7g9","type":"discovered-from","created_at":"2026-02-27T11:48:14.087553998+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3","title":"Epic: Add hypercert editing, deletion, and management capabilities","description":"## Why\nCurrently there is no way to edit a hypercert after creation. Users need to be able to:\n- Edit a hypercert's core fields (title, description, dates, image)\n- Edit/update existing measurements\n- Add more measurements, evaluations, contributions, and evidence to existing hypercerts\n- Delete measurements, contributions, evaluations, and evidence\n- Delete entire hypercerts\n\n## What success looks like\n- From the hypercert detail page, the owner can click Edit to modify core fields\n- From the detail page, the owner can add new measurements/evaluations/contributions/evidence\n- From the detail page, the owner can delete individual measurements/evaluations/contributions/evidence\n- From the detail page, the owner can delete the entire hypercert\n- All operations use the SDK's high-level methods (scopedRepo.hypercerts.update, .delete, .updateMeasurement, .addContribution, records.delete)\n- Only the hypercert owner sees edit/delete controls\n\n## Key constraints\n- The SDK already supports all CRUD operations (hypercerts.update, hypercerts.delete, hypercerts.updateMeasurement, hypercerts.addContribution, records.update, records.delete)\n- OAuth scopes already include create+update+delete permissions\n- The existing create forms (measurement-form, evaluation-form, evidence-form) should be reusable for 'add more' flows\n- The HypercertsBaseForm already has updateActions and certInfo props designed for edit mode\n- Ownership check: compare session DID with the DID extracted from the hypercert AT-URI\n\n## Scope\nAPI routes + server actions + React Query hooks + UI for all edit/delete operations on hypercerts and their related records (measurements, evaluations, contributions, evidence).","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T11:18:03.876316055+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:55:14.256202458+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-2a3.1","title":"Consolidate /api/certs into single route: POST (create) + PUT (update)","description":"## Files\n- app/api/certs/route.ts (create)\n- app/api/certs/create/route.ts (delete after migration)\n- lib/api/hypercerts.ts (modify — update create endpoint from /api/certs/create to /api/certs)\n\n## What to do\nConsolidate hypercert create and update into a single REST-style route at `/api/certs/route.ts`.\n\n### 1. Create `app/api/certs/route.ts`\n\n**POST handler** (create) — move the existing logic from `app/api/certs/create/route.ts` verbatim:\n```typescript\nexport async function POST(req: NextRequest) {\n  // Exact same logic as current create/route.ts\n  // Parse FormData, build CreateHypercertParams, call ctx.scopedRepo.hypercerts.create()\n}\n```\n\n**PUT handler** (update):\n```typescript\nexport async function PUT(req: NextRequest) {\n  try {\n    const formData = await req.formData();\n    const ctxPromise = getRepoContext();\n    const hypercertUri = formData.get(\"hypercertUri\") as string | null;\n\n    if (!hypercertUri) {\n      return NextResponse.json({ error: \"Missing hypercertUri\" }, { status: 400 });\n    }\n\n    const title = formData.get(\"title\") as string | null;\n    const shortDescription = formData.get(\"shortDescription\") as string | null;\n    const description = formData.get(\"description\") as string | null;\n    const startDate = formData.get(\"startDate\") as string | null;\n    const endDate = formData.get(\"endDate\") as string | null;\n    const imageRaw = formData.get(\"image\");\n\n    const ctx = await ctxPromise;\n    if (!ctx) {\n      return NextResponse.json({ error: \"Could not authenticate repo\" }, { status: 401 });\n    }\n\n    // Build updates object with only provided fields\n    const updates: Record\u003cstring, unknown\u003e = {};\n    if (title !== null) updates.title = title;\n    if (shortDescription !== null) updates.shortDescription = shortDescription;\n    if (description !== null) updates.description = description;\n    if (startDate !== null) updates.startDate = startDate;\n    if (endDate !== null) updates.endDate = endDate;\n\n    // Handle image: File = new image, string \"null\" = remove, absent = no change\n    let image: Blob | null | undefined;\n    if (imageRaw instanceof File) {\n      image = imageRaw;\n    } else if (imageRaw === \"null\") {\n      image = null;\n    }\n\n    const data = await ctx.scopedRepo.hypercerts.update({\n      uri: hypercertUri,\n      updates,\n      image,\n    });\n    return NextResponse.json(data);\n  } catch (e) {\n    console.error(\"Error updating hypercert:\", e);\n    return NextResponse.json(\n      { error: `Failed to update hypercert: ${e instanceof Error ? e.message : String(e)}` },\n      { status: 500 }\n    );\n  }\n}\n```\n\n### 2. Delete `app/api/certs/create/route.ts`\nRemove the old file after moving its logic into the new route.\n\n### 3. Update `lib/api/hypercerts.ts`\nChange the two functions that POST to `/api/certs/create`:\n- `createHypercert()` line 33: change `/api/certs/create` → `/api/certs`\n- `createHypercertFromParams()` line 62: change `/api/certs/create` → `/api/certs`\n\n## Dont\n- Do NOT touch add-attachment or add-location routes (they stay separate)\n- Do NOT add DELETE handler (delete is a server action)\n- Do NOT change the create logic — copy it exactly from the existing file","acceptance_criteria":"1. app/api/certs/route.ts exists with POST and PUT exports\n2. app/api/certs/create/route.ts is deleted\n3. POST handler has identical logic to the old create route\n4. PUT handler accepts FormData with hypercertUri + optional fields\n5. PUT returns 400 if hypercertUri missing, 401 if unauthenticated\n6. lib/api/hypercerts.ts updated to POST to /api/certs (not /api/certs/create)\n7. TypeScript compiles with no errors\n8. npm run build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-25T11:18:22.997444471+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:43:34.43684401+06:00","closed_at":"2026-02-25T11:43:34.43684401+06:00","close_reason":"d127dae Consolidate /api/certs into single route: POST (create) + PUT (update)","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.1","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:18:22.999002749+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.10","title":"Add delete buttons to evidence and evaluations sections on detail view","description":"## Files\n- components/hypercert-evidence-section.tsx (modify)\n- components/hypercert-evaluations-section.tsx (modify)\n\n## What to do\nSame pattern as task 2a3.9 (measurements delete) but for evidence and evaluations sections.\n\n### For each section:\n1. Destructure `isOwner` from props\n2. Import `useDeleteRecordMutation` from `@/queries/hypercerts`\n3. Import `DeleteConfirmDialog` from `@/components/delete-confirm-dialog`\n4. Import `useQueryClient` from `@tanstack/react-query`\n5. Track the AT-URI for each record alongside the record data (same pattern as measurements — build URI from `{ did, collection, rkey }` link objects)\n6. When `isOwner` is true, render `\u003cDeleteConfirmDialog\u003e` on each card\n7. On successful delete, invalidate the relevant queries\n\n### Evidence section specifics:\n- Query key to invalidate: `[\"hypercerts\", \"evidence\"]`\n- `itemType=\"evidence\"`\n- `itemName` can use the evidence title or shortDescription\n\n### Evaluations section specifics:\n- Query key to invalidate: `[\"hypercerts\", \"evaluations\"]`\n- `itemType=\"evaluation\"`\n- `itemName` can use the evaluation summary\n\nLook at how each section currently renders its items and add the delete button in a consistent position (top-right of each card).\n\n## Dont\n- Do NOT add edit functionality\n- Do NOT show delete buttons when isOwner is false/undefined\n- Do NOT change the card layout significantly","acceptance_criteria":"1. Delete buttons appear on evidence cards when isOwner=true\n2. Delete buttons appear on evaluation cards when isOwner=true\n3. Both are hidden when isOwner is false/undefined\n4. Clicking delete shows confirmation dialog for each\n5. Confirming delete calls deleteRecord with the correct AT-URI\n6. After successful delete, relevant queries are invalidated\n7. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-25T11:21:13.085943563+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:51:39.964825199+06:00","closed_at":"2026-02-25T11:51:39.964825199+06:00","close_reason":"367057d Add delete buttons to evidence and evaluations sections","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.10","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:21:13.088094622+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.10","depends_on_id":"hypercerts-2a3.6","type":"blocks","created_at":"2026-02-25T11:22:45.928425367+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.10","depends_on_id":"hypercerts-2a3.7","type":"blocks","created_at":"2026-02-25T11:22:46.009764285+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.10","depends_on_id":"hypercerts-2a3.8","type":"blocks","created_at":"2026-02-25T11:22:46.111227077+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.11","title":"Add 'Add more' buttons to detail view sections (measurements, evaluations, evidence, contributions)","description":"## Files\n- components/hypercert-detail-view.tsx (modify)\n- components/hypercert-measurements-section.tsx (modify)\n- components/hypercert-evidence-section.tsx (modify)\n- components/hypercert-evaluations-section.tsx (modify)\n- components/hypercert-contributors-section.tsx (modify)\n\n## What to do\nWhen `isOwner` is true, add an \"Add\" button at the bottom of each section that navigates to the create flow for that record type, pre-populated with the hypercert URI.\n\n### Implementation approach:\nUse Next.js `\u003cLink\u003e` to navigate to a new route: `/hypercerts/[hypercertUri]/add/[type]` where type is `measurement`, `evaluation`, `evidence`, or `contribution`.\n\nFor each section component, when `isOwner` is true, render at the bottom:\n```tsx\nimport Link from \"next/link\";\nimport { Plus } from \"lucide-react\";\nimport { Button } from \"@/components/ui/button\";\n\n// At the bottom of the section, after the list of items:\n{isOwner \u0026\u0026 (\n  \u003cdiv className=\"pt-4\"\u003e\n    \u003cButton asChild variant=\"outline\" size=\"sm\" className=\"gap-2 font-[family-name:var(--font-outfit)]\"\u003e\n      \u003cLink href={`/hypercerts/${encodeURIComponent(hypercertUri)}/add/measurement`}\u003e\n        \u003cPlus className=\"h-4 w-4\" /\u003e Add Measurement\n      \u003c/Link\u003e\n    \u003c/Button\u003e\n  \u003c/div\u003e\n)}\n```\n\nThe `hypercertUri` prop is already available in measurements, evidence, and evaluations sections. For contributors, it needs to be passed down (already added in task 2a3.7).\n\n### Section-specific labels:\n- Measurements: \"Add Measurement\" → `/add/measurement`\n- Evidence: \"Add Evidence\" → `/add/evidence`\n- Evaluations: \"Add Evaluation\" → `/add/evaluation`\n- Contributors: \"Add Contributor\" → `/add/contribution`\n\n**Note**: The actual `/add/[type]` pages will be created in a separate task. This task only adds the buttons/links.\n\n## Dont\n- Do NOT create the add pages yet\n- Do NOT show buttons when isOwner is false/undefined\n- Do NOT use modals/dialogs for the add flow","acceptance_criteria":"1. \"Add Measurement\" button appears in measurements section when isOwner=true\n2. \"Add Evidence\" button appears in evidence section when isOwner=true\n3. \"Add Evaluation\" button appears in evaluations section when isOwner=true\n4. \"Add Contributor\" button appears in contributors section when isOwner=true\n5. All buttons are hidden when isOwner is false/undefined\n6. Each button links to the correct /add/[type] route with encoded hypercertUri\n7. TypeScript compiles with no errors","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-25T11:21:29.784383584+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:46:52.142165772+06:00","closed_at":"2026-02-25T11:46:52.142165772+06:00","close_reason":"6fc37f1 Add 'Add more' buttons to detail view sections","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.11","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:21:29.786186842+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.11","depends_on_id":"hypercerts-2a3.7","type":"blocks","created_at":"2026-02-25T11:22:46.196104863+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.12","title":"Create /hypercerts/[hypercertUri]/add/[type] pages for adding records to existing hypercerts","description":"## Files\n- app/hypercerts/[hypercertUri]/add/[type]/page.tsx (create)\n\n## What to do\nCreate a dynamic route page that renders the appropriate form for adding a measurement, evaluation, evidence, or contribution to an existing hypercert.\n\n### Route: `/hypercerts/[hypercertUri]/add/[type]`\n- `hypercertUri` — URL-encoded AT-URI of the hypercert\n- `type` — one of: `measurement`, `evaluation`, `evidence`, `contribution`\n\n### Implementation:\n\n```typescript\n\"use client\";\n\nimport { useParams, useRouter } from \"next/navigation\";\nimport { useMemo } from \"react\";\nimport dynamic from \"next/dynamic\";\nimport { ArrowLeft } from \"lucide-react\";\nimport Link from \"next/link\";\nimport { Button } from \"@/components/ui/button\";\nimport type { CreateHypercertResult } from \"@hypercerts-org/sdk-core\";\nimport {\n  MeasurementFormSkeleton,\n  EvaluationFormSkeleton,\n  EvidenceFormSkeleton,\n} from \"@/components/form-skeletons\";\n\nconst MeasurementForm = dynamic(() =\u003e import(\"@/components/measurement-form\"), {\n  loading: () =\u003e \u003cMeasurementFormSkeleton /\u003e,\n});\nconst EvaluationForm = dynamic(() =\u003e import(\"@/components/evaluation-form\"), {\n  loading: () =\u003e \u003cEvaluationFormSkeleton /\u003e,\n});\nconst HypercertEvidenceForm = dynamic(() =\u003e import(\"@/components/evidence-form\"), {\n  loading: () =\u003e \u003cEvidenceFormSkeleton /\u003e,\n});\n// ContributionsForm is currently disabled, so use a simple placeholder for now\n```\n\nThe page should:\n1. Decode `hypercertUri` from params\n2. Build a minimal `CreateHypercertResult` object with just `{ hypercertUri, hypercertCid: \"\" }` — the forms only use `hypercertUri` from this object\n3. Render the appropriate form based on `type`:\n   - `measurement` → `\u003cMeasurementForm hypercertInfo={info} onNext={goBack} onBack={goBack} /\u003e`\n   - `evaluation` → `\u003cEvaluationForm hypercertInfo={info} onNext={goBack} onBack={goBack} /\u003e`\n   - `evidence` → `\u003cHypercertEvidenceForm hypercertInfo={info} onNext={goBack} onBack={goBack} /\u003e`\n   - `contribution` → Show a message \"Contribution form coming soon\" (since contributions-form.tsx is disabled)\n4. `goBack` navigates to `/hypercerts/${encodeURIComponent(hypercertUri)}`\n5. Show a back button at the top linking to the hypercert detail page\n6. Show a title like \"Add Measurement to Hypercert\"\n7. For invalid `type` values, show \"Unknown record type\"\n\n### Layout:\nUse the same layout pattern as the detail page:\n```tsx\n\u003cmain className=\"relative min-h-screen noise-bg\"\u003e\n  \u003cdiv className=\"relative z-10 max-w-4xl mx-auto py-8 px-4 space-y-6\"\u003e\n    {/* Back button */}\n    {/* Title */}\n    {/* Form */}\n  \u003c/div\u003e\n\u003c/main\u003e\n```\n\n## Dont\n- Do NOT create a layout.tsx for this route\n- Do NOT implement the contribution form (its disabled in the codebase)\n- Do NOT modify any existing form components","acceptance_criteria":"1. File app/hypercerts/[hypercertUri]/add/[type]/page.tsx exists\n2. Navigating to /hypercerts/{uri}/add/measurement renders MeasurementForm\n3. Navigating to /hypercerts/{uri}/add/evaluation renders EvaluationForm\n4. Navigating to /hypercerts/{uri}/add/evidence renders HypercertEvidenceForm\n5. Navigating to /hypercerts/{uri}/add/contribution shows placeholder message\n6. Invalid type shows \"Unknown record type\"\n7. Back button navigates to hypercert detail page\n8. TypeScript compiles with no errors","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-02-25T11:21:50.495135205+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:49:11.930009963+06:00","closed_at":"2026-02-25T11:49:11.930009963+06:00","close_reason":"f855a99 add /hypercerts/[hypercertUri]/add/[type] page for adding records","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.12","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:21:50.496986733+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.12","depends_on_id":"hypercerts-2a3.11","type":"blocks","created_at":"2026-02-25T11:22:46.279957068+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.13","title":"Add Edit and Delete buttons to hypercert detail view header","description":"## Files\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\nWhen `isOwner` is true, add Edit and Delete buttons in the hero section of the detail view.\n\n### Edit button:\n- Links to `/hypercerts/${encodeURIComponent(hypercertUri)}/edit`\n- Uses `\u003cButton asChild variant=\"outline\" size=\"sm\"\u003e` with a `\u003cLink\u003e` inside\n- Icon: `Pencil` from lucide-react\n- Label: \"Edit\"\n- Position: top-right of the hero section, next to the title\n\n### Delete button:\n- Uses `\u003cDeleteConfirmDialog\u003e` component (from task 2a3.8)\n- Uses `useDeleteHypercertMutation` hook (from task 2a3.6)\n- On successful delete, redirect to `/hypercerts` using `useRouter().push`\n- `itemType=\"hypercert\"`\n- `itemName={record.title}`\n- Trigger: `\u003cButton variant=\"outline\" size=\"sm\" className=\"text-destructive\"\u003e` with Trash2 icon and \"Delete\" label\n\n### Layout:\nAdd a flex row at the top of the hero section:\n```tsx\n{isOwner \u0026\u0026 (\n  \u003cdiv className=\"flex items-center gap-2 justify-end\"\u003e\n    \u003cButton asChild variant=\"outline\" size=\"sm\" className=\"gap-2 font-[family-name:var(--font-outfit)]\"\u003e\n      \u003cLink href={`/hypercerts/${encodeURIComponent(hypercertUri)}/edit`}\u003e\n        \u003cPencil className=\"h-4 w-4\" /\u003e Edit\n      \u003c/Link\u003e\n    \u003c/Button\u003e\n    \u003cDeleteConfirmDialog\n      itemType=\"hypercert\"\n      itemName={record.title}\n      isDeleting={deleteMutation.isPending}\n      onConfirm={() =\u003e deleteMutation.mutate({ hypercertUri })}\n      trigger={\n        \u003cButton variant=\"outline\" size=\"sm\" className=\"gap-2 font-[family-name:var(--font-outfit)] text-destructive hover:text-destructive\"\u003e\n          \u003cTrash2 className=\"h-4 w-4\" /\u003e Delete\n        \u003c/Button\u003e\n      }\n    /\u003e\n  \u003c/div\u003e\n)}\n```\n\n### Imports needed:\n- `useDeleteHypercertMutation` from `@/queries/hypercerts`\n- `DeleteConfirmDialog` from `@/components/delete-confirm-dialog`\n- `useRouter` from `next/navigation`\n- `useQueryClient` from `@tanstack/react-query`\n- `Pencil, Trash2` from `lucide-react`\n- `Link` from `next/link`\n\nOn successful delete, also invalidate hypercert queries and redirect:\n```typescript\nconst router = useRouter();\nconst deleteMutation = useDeleteHypercertMutation({\n  onSuccess: () =\u003e {\n    router.push(\"/hypercerts\");\n  },\n});\n```\n\n## Dont\n- Do NOT show Edit/Delete buttons when isOwner is false/undefined\n- Do NOT create the edit page yet (that is a separate task)\n- Do NOT change the existing layout of the hero section significantly","acceptance_criteria":"1. Edit button appears in hero section when isOwner=true\n2. Delete button appears in hero section when isOwner=true\n3. Both buttons hidden when isOwner is false/undefined\n4. Edit button links to /hypercerts/{uri}/edit\n5. Delete button shows confirmation dialog\n6. Confirming delete calls deleteHypercert and redirects to /hypercerts on success\n7. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-02-25T11:22:08.168479805+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:51:59.946561727+06:00","closed_at":"2026-02-25T11:51:59.946561727+06:00","close_reason":"1d01010 Add Edit and Delete buttons to hypercert detail view header","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.13","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:22:08.170986109+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.13","depends_on_id":"hypercerts-2a3.6","type":"blocks","created_at":"2026-02-25T11:22:45.420886087+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.13","depends_on_id":"hypercerts-2a3.7","type":"blocks","created_at":"2026-02-25T11:22:45.511763341+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.13","depends_on_id":"hypercerts-2a3.8","type":"blocks","created_at":"2026-02-25T11:22:45.600025827+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.14","title":"Create /hypercerts/[hypercertUri]/edit page for editing hypercert core fields","description":"## Files\n- app/hypercerts/[hypercertUri]/edit/page.tsx (create)\n- components/hypercerts-edit-form.tsx (create)\n\n## What to do\nCreate an edit page that loads the existing hypercert data and presents a form to update it.\n\n### 1. `app/hypercerts/[hypercertUri]/edit/page.tsx` (Server Component)\nThis is a server component that:\n1. Decodes `hypercertUri` from params\n2. Extracts ownerDid from the AT-URI\n3. Gets session and repo context\n4. Verifies ownership (session.did === ownerDid) — redirect to detail page if not owner\n5. Fetches the hypercert record via `ctx.scopedRepo.hypercerts.get(decodedUri)`\n6. Resolves the image URL if present (same pattern as the detail page)\n7. Renders `\u003cHypercertsEditForm\u003e` with the record data\n\n```tsx\nexport default async function EditHypercertPage({ params }) {\n  const { hypercertUri } = await params;\n  const decodedUri = decodeURIComponent(hypercertUri);\n  // ... fetch record, check ownership ...\n  \n  return (\n    \u003cmain className=\"relative min-h-screen noise-bg\"\u003e\n      \u003cdiv className=\"relative z-10 max-w-4xl mx-auto py-8 px-4 space-y-6\"\u003e\n        \u003cButton asChild variant=\"ghost\" size=\"sm\"\u003e\n          \u003cLink href={`/hypercerts/${hypercertUri}`}\u003e\n            \u003cArrowLeft className=\"size-4 mr-2\" /\u003e Back to Hypercert\n          \u003c/Link\u003e\n        \u003c/Button\u003e\n        \u003ch1\u003eEdit Hypercert\u003c/h1\u003e\n        \u003cHypercertsEditForm\n          hypercertUri={decodedUri}\n          record={cert.record}\n          imageUri={imageUri}\n        /\u003e\n      \u003c/div\u003e\n    \u003c/main\u003e\n  );\n}\n```\n\n### 2. `components/hypercerts-edit-form.tsx` (Client Component)\nA client component that:\n1. Accepts `hypercertUri`, `record` (HypercertClaim), and optional `imageUri`\n2. Pre-populates form fields from the record: title, shortDescription, description, startDate, endDate\n3. Uses `useUpdateHypercertMutation` from `@/queries/hypercerts`\n4. On submit, calls the mutation with only changed fields\n5. On success, navigates back to the detail page\n\n**Form fields** (reuse the same styling as HypercertsBaseForm):\n- Title (Input)\n- Short Description (Textarea)\n- Start Date (DatePicker)\n- End Date (DatePicker)\n- Background Image (file input with preview — show existing image if imageUri provided)\n\n**Do NOT include**: rights (immutable), work scope (separate concern), contributors (separate flow)\n\nThe form should show a \"Save Changes\" button and a \"Cancel\" link back to the detail page.\n\n```tsx\n\"use client\";\nimport { useState, FormEventHandler } from \"react\";\nimport { useRouter } from \"next/navigation\";\nimport { useUpdateHypercertMutation } from \"@/queries/hypercerts\";\nimport type { HypercertClaim } from \"@hypercerts-org/sdk-core\";\n// ... other imports\n\ninterface HypercertsEditFormProps {\n  hypercertUri: string;\n  record: HypercertClaim;\n  imageUri?: string;\n}\n```\n\n## Dont\n- Do NOT allow editing rights (they are immutable)\n- Do NOT allow editing work scope in this task\n- Do NOT reuse HypercertsBaseForm directly (it is too coupled to the create flow with contributions, rights, etc.) — create a simpler edit-specific form\n- Do NOT add loading states for the page itself (its a server component, Next.js handles that)","acceptance_criteria":"1. File app/hypercerts/[hypercertUri]/edit/page.tsx exists as server component\n2. File components/hypercerts-edit-form.tsx exists as client component\n3. Edit page fetches existing record and passes to form\n4. Edit page redirects non-owners to detail page\n5. Form pre-populates title, shortDescription, startDate, endDate from record\n6. Form shows existing image preview if imageUri provided\n7. Submit calls useUpdateHypercertMutation with changed fields\n8. Successful update navigates back to detail page\n9. Cancel link navigates back to detail page\n10. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":60,"created_at":"2026-02-25T11:22:31.442837313+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:54:06.265902468+06:00","closed_at":"2026-02-25T11:54:06.265902468+06:00","close_reason":"445a0f6 Add edit page and form for hypercert core fields","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-2a3.14","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:22:31.444852628+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.14","depends_on_id":"hypercerts-2a3.1","type":"blocks","created_at":"2026-02-25T11:22:46.362091545+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.14","depends_on_id":"hypercerts-2a3.6","type":"blocks","created_at":"2026-02-25T11:22:46.450695982+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.14","depends_on_id":"hypercerts-2a3.7","type":"blocks","created_at":"2026-02-25T11:22:46.531689742+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.15","title":"Fix: measurements delete button uses shared isPending — all cards show spinner simultaneously (from hypercerts-2a3.9)","description":"Review of hypercerts-2a3.9 found: In components/hypercert-measurements-section.tsx, a single `useDeleteRecordMutation` instance is shared across all measurement cards. When any delete is in progress, `deleteMutation.isPending` is true for ALL cards, causing every card's DeleteConfirmDialog to show the spinner/disabled state simultaneously. The evidence and evaluations sections correctly scope isPending per-item by checking `deleteRecordMutation.variables?.recordUri === recordUri`, but measurements does not. Fix: add the same per-item isPending check: `isDeleting={deleteMutation.isPending \u0026\u0026 deleteMutation.variables?.recordUri === measurement._uri}`.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:09:19.126099325+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:15:36.445332534+06:00","closed_at":"2026-02-25T12:15:36.445332534+06:00","close_reason":"9b382ea fix: scope isPending to per-item in measurements delete","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.15","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:09:19.127471743+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.15","depends_on_id":"hypercerts-2a3.9","type":"discovered-from","created_at":"2026-02-25T12:11:02.856398446+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.16","title":"Fix: edit page uses encoded URI in back-link but detail page expects encoded URI — verify round-trip (from hypercerts-2a3.14)","description":"Review of hypercerts-2a3.14 found: In app/hypercerts/[hypercertUri]/edit/page.tsx line 69, the back-link uses `/hypercerts/${hypercertUri}` where `hypercertUri` is the raw (still URL-encoded) param from Next.js. This is correct for the back-link. However, HypercertsEditForm (line 83) receives `decodedUri` (the decoded AT-URI) and internally re-encodes it for the cancel link (line 30: `encodeURIComponent(hypercertUri)`). This double-encode path is fine, but the edit page's own back-link (line 69) uses the raw encoded param directly — if the param was double-encoded by the browser this could break. Low risk but should be consistent: use `encodeURIComponent(decodedUri)` in the back-link for clarity and consistency with how the detail page and add page construct their hrefs.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:09:28.00967684+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:19:33.994395605+06:00","closed_at":"2026-02-25T12:19:33.994395605+06:00","close_reason":"142aafd fix: use encodeURIComponent(decodedUri) in edit page back-link for consistency","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.16","depends_on_id":"hypercerts-2a3.14","type":"discovered-from","created_at":"2026-02-25T12:11:02.907728193+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.17","title":"Fix: edit form submits with no changes — no guard or user feedback (from hypercerts-2a3.14)","description":"Review of hypercerts-2a3.14 found: In components/hypercerts-edit-form.tsx, the handleSubmit function (line 67) builds an updates object with only changed fields. If the user clicks 'Save Changes' without modifying anything, the updates object will only contain `{ hypercertUri }` with no actual field changes. The mutation will still fire and make a PUT request to /api/certs with an empty update. This wastes a network round-trip and gives no feedback to the user that nothing changed. Fix: before calling `updateMutation.mutate(updates)`, check if any fields beyond `hypercertUri` are present. If not, either show a toast ('No changes to save') or disable the Save button when the form is pristine.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:09:35.174565896+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:15:55.3237575+06:00","closed_at":"2026-02-25T12:15:55.3237575+06:00","close_reason":"e8e6589 Fix: guard edit form submit when no changes, disable Save button when pristine","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.17","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:09:35.175812199+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.17","depends_on_id":"hypercerts-2a3.14","type":"discovered-from","created_at":"2026-02-25T12:11:02.960564847+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.18","title":"Fix: add/[type] page is a client component — no ownership check, any user can navigate directly (from hypercerts-2a3.12)","description":"Review of hypercerts-2a3.12 found: app/hypercerts/[hypercertUri]/add/[type]/page.tsx is a client component ('use client' on line 1). It has no ownership check — any authenticated user can navigate directly to /hypercerts/\u003csomeoneElsesUri\u003e/add/measurement and submit the form. The edit page (hypercerts-2a3.14) correctly does a server-side ownership check and redirects non-owners. The add page should do the same. Since it's a client component, the ownership check would need to be done either: (a) convert to a server component wrapper that checks ownership and passes hypercertUri down, or (b) add a client-side check using the session DID vs the DID extracted from the URI, and redirect if not owner. The forms themselves may also lack server-side auth, but the page-level guard is the first line of defense.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:09:43.323657015+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:16:46.142920035+06:00","closed_at":"2026-02-25T12:16:46.142920035+06:00","close_reason":"6ef8ee3 fix: add server-side ownership check to add/[type] page, converting to server component wrapper that redirects non-owners","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.18","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:09:43.325017468+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.18","depends_on_id":"hypercerts-2a3.12","type":"discovered-from","created_at":"2026-02-25T12:11:03.014628426+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.19","title":"Fix: add/[type] page title uses font-outfit but h1 on edit page uses font-syne — inconsistent heading font (from hypercerts-2a3.12)","description":"Review of hypercerts-2a3.12 found: In app/hypercerts/[hypercertUri]/add/[type]/page.tsx line 128, the page title h1 uses `font-[family-name:var(--font-outfit)]`. In app/hypercerts/[hypercertUri]/edit/page.tsx line 77, the h1 uses `font-[family-name:var(--font-syne)]`. The existing design system uses font-syne for headings (h1, h2, h3) and font-outfit for body/labels. The add page's h1 should use font-syne to match the edit page and the rest of the app.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:09:49.49357124+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:16:52.546834418+06:00","closed_at":"2026-02-25T12:16:52.546834418+06:00","close_reason":"066714c fix: h1 on add/[type] page uses font-syne (via add-record-form.tsx refactor)","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.19","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:09:49.495317044+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.19","depends_on_id":"hypercerts-2a3.12","type":"discovered-from","created_at":"2026-02-25T12:11:03.068100476+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.2","title":"Add API route: POST /api/certs/delete — delete a hypercert","description":"## Files\n- app/api/certs/delete/route.ts (create)\n\n## What to do\nCreate a POST route handler that deletes an existing hypercert.\n\n**Request**: JSON body with:\n- `hypercertUri` (string, required) — AT-URI of the hypercert to delete\n\n**Implementation**:\n1. Call `getRepoContext()` — return 401 if null\n2. Parse JSON body, extract `hypercertUri`\n3. Call `ctx.scopedRepo.hypercerts.delete(hypercertUri)`\n4. Return JSON `{ success: true }` on success\n5. Return 400 if `hypercertUri` missing\n6. Return 500 with error message on failure\n\n**Note**: The SDK delete does NOT cascade to related records (measurements, evaluations, etc). That is acceptable for now — orphaned records are harmless in ATProto.\n\nFollow the same error handling pattern as `app/api/certs/create/route.ts`.\n\n## Dont\n- Do NOT cascade-delete related records (measurements, evaluations, evidence, contributions)\n- Do NOT use FormData — use JSON body since there are no files","acceptance_criteria":"1. File app/api/certs/delete/route.ts exists and exports POST handler\n2. Sending JSON { hypercertUri } returns { success: true }\n3. Missing hypercertUri returns 400\n4. Unauthenticated request returns 401\n5. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-25T11:18:32.279567709+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:29:45.462880468+06:00","closed_at":"2026-02-25T11:29:45.462880468+06:00","close_reason":"9d99f8d superseded: delete moved to server action, API route not needed","labels":["scope:small","wontfix"],"dependencies":[{"issue_id":"hypercerts-2a3.2","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:18:32.281935043+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.20","title":"Fix: error/not-found states in edit page and detail page use bare \u003cdiv\u003e with no styling (from hypercerts-2a3.14)","description":"Review of hypercerts-2a3.14 and hypercerts-2a3.7 found: Several error/not-found states render unstyled bare divs: (1) edit/page.tsx line 26: `\u003cdiv\u003eInvalid hypercert URI.\u003c/div\u003e`, line 43: `\u003cdiv\u003eRecord not found\u003c/div\u003e`. (2) app/hypercerts/[hypercertUri]/page.tsx line 68: `\u003cdiv\u003eInvalid hypercert URI.\u003c/div\u003e`, line 74: `\u003cdiv\u003ePlease log in to view hypercerts.\u003c/div\u003e`, line 77: `\u003cdiv\u003eRecord not found\u003c/div\u003e`. These render as plain text with no layout, no styling, no navigation back. They should use the glass-panel pattern with a proper error message and a back link, consistent with how other error states in the app are displayed.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:09:56.933902045+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:17:33.372778836+06:00","closed_at":"2026-02-25T12:17:33.372778836+06:00","close_reason":"81b9266 fix: style error/not-found states with glass-panel pattern","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.20","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:09:56.935266152+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.20","depends_on_id":"hypercerts-2a3.14","type":"discovered-from","created_at":"2026-02-25T12:11:03.121651524+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.21","title":"Fix: delete button trigger in detail view missing aria-label (from hypercerts-2a3.13)","description":"Review of hypercerts-2a3.13 found: In components/hypercert-detail-view.tsx lines 90-97, the custom trigger Button for the hypercert-level DeleteConfirmDialog has no aria-label. The button contains a Trash2 icon and the text 'Delete', so the text provides some context, but the icon-only state (if text is ever hidden on small screens) would be inaccessible. More importantly, the default trigger in DeleteConfirmDialog (line 42-50 of delete-confirm-dialog.tsx) does have an aria-label, but when a custom trigger is passed, the aria-label responsibility falls on the caller. The detail view's custom trigger should add `aria-label=\"Delete hypercert\"` to match the default trigger's pattern.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:10:04.477713269+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:15:41.946413033+06:00","closed_at":"2026-02-25T12:15:41.946413033+06:00","close_reason":"fb4e18a fix: add aria-label to delete button trigger in detail view","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.21","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:04.479056081+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.21","depends_on_id":"hypercerts-2a3.13","type":"discovered-from","created_at":"2026-02-25T12:11:03.173138443+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.22","title":"Fix: delete button overlaps card content on small screens — absolute positioning without padding guard (from hypercerts-2a3.9)","description":"Review of hypercerts-2a3.9 and hypercerts-2a3.10 found: In measurements, evidence, and evaluations sections, the delete button is positioned `absolute top-3 right-3` inside a `relative` wrapper div. The underlying card components (HypercertMeasurementView, HypercertEvidenceView, HypercertEvaluationView) were not designed with this overlay in mind and have no right-side padding to prevent their content from being obscured by the 32px (h-8 w-8) delete button. On narrow viewports or when card content extends to the right edge, the delete button will overlap text or other interactive elements. Fix: add `pr-12` (or similar) to the card's top-right area when isOwner is true, or restructure the card header to include the delete button in the flex layout rather than using absolute positioning.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:10:14.099708516+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:21:55.082631289+06:00","closed_at":"2026-02-25T12:21:55.082631289+06:00","close_reason":"0bfa45d fix: move delete button into card header flex layout to prevent overlap on small screens","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.22","depends_on_id":"hypercerts-2a3.9","type":"discovered-from","created_at":"2026-02-25T12:11:03.22730218+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.23","title":"Fix: PUT /api/certs missing ownership check — any authenticated user can update any hypercert (from hypercerts-2a3.1)","description":"## Review finding from hypercerts-2a3.1\n\n**File:** app/api/certs/route.ts (PUT handler, line 69)\n\n**Problem:**\nThe PUT handler calls `getRepoContext()` with no `targetDid`, so `ctx.scopedRepo` is scoped to the caller's own `activeDid`. However, the `hypercertUri` in the request body can point to *any* DID's record. If the SDK's `hypercerts.update()` does not enforce that the scoped DID matches the URI's DID, a logged-in user could update another user's hypercert by supplying a foreign AT-URI.\n\n**Evidence:**\n```ts\n// route.ts line 69\nconst ctxPromise = getRepoContext(); // no targetDid — scoped to activeDid\n// ...\nconst data = await ctx.scopedRepo.hypercerts.update({\n  uri: hypercertUri, // could be at://did:plc:SOMEONE_ELSE/...\n  updates,\n  image,\n});\n```\n\n**Fix:**\nExtract the DID from `hypercertUri` using `parseAtUri`, then verify it matches `ctx.activeDid` before calling update. Return 403 if they don't match:\n```ts\nconst parsed = parseAtUri(hypercertUri);\nif (!parsed) return NextResponse.json({ error: 'Invalid hypercertUri' }, { status: 400 });\nif (parsed.did !== ctx.activeDid) return NextResponse.json({ error: 'Forbidden' }, { status: 403 });\n```\n\nSame pattern should be applied to `deleteHypercert` server action (lib/create-actions.ts line 224) — it also calls `getRepoContext()` without validating that the URI's DID matches the session's activeDid.","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T12:10:20.27232228+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T12:20:01.26219698+06:00","closed_at":"2026-02-25T12:20:01.26219698+06:00","close_reason":"2e20eb3 Fix: add ownership check to PUT /api/certs and deleteHypercert","labels":["scope:small","scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.23","depends_on_id":"hypercerts-2a3.1","type":"discovered-from","created_at":"2026-02-25T12:11:32.075269587+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.24","title":"Fix: edit form date comparison uses toISOString() vs record string — timezone mismatch may cause false dirty state (from hypercerts-2a3.14)","description":"Review of hypercerts-2a3.14 found: In components/hypercerts-edit-form.tsx lines 78-81, the dirty check for dates compares `startDate.toISOString()` against `record.startDate`. The record's startDate is stored as whatever string the SDK returns (likely a date-only string like '2024-01-15' or a full ISO string). `new Date('2024-01-15').toISOString()` returns '2024-01-15T00:00:00.000Z' which will never equal '2024-01-15', causing the date to always be included in the update payload even when unchanged. This means every save will include startDate/endDate even if the user didn't touch them. Fix: normalize both sides of the comparison to the same format (e.g., compare date-only strings, or parse the record date through `new Date()` and compare ISO strings).","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:10:22.961002883+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:19:24.371589713+06:00","closed_at":"2026-02-25T12:19:24.371589713+06:00","close_reason":"97171bd fix: normalize date comparison to YYYY-MM-DD to prevent false dirty state","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.24","depends_on_id":"hypercerts-2a3.14","type":"discovered-from","created_at":"2026-02-25T12:11:03.281737814+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.25","title":"Fix: contributors section shows '0 contributors' count even when loading (from hypercerts-2a3.11)","description":"Review of hypercerts-2a3.11 found: In components/hypercert-contributors-section.tsx lines 81-84, the contributor count subtitle is always rendered: `{displayContributors.length} contributor(s)`. If `contributors` prop is undefined (not yet loaded or genuinely empty), `parseContributors(undefined)` returns an empty array, so the count shows '0 contributors' even during the loading state. The measurements, evidence, and evaluations sections conditionally render their count only when items.length \u003e 0. The contributors section should do the same — only show the count when displayContributors.length \u003e 0, and hide it when loading or empty.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:10:29.048048107+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:16:02.326667525+06:00","closed_at":"2026-02-25T12:16:02.326667525+06:00","close_reason":"9b382ea fix: hide contributor count when loading or empty","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.25","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:29.049297535+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.25","depends_on_id":"hypercerts-2a3.11","type":"discovered-from","created_at":"2026-02-25T12:11:03.337547553+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.26","title":"Fix: useUpdateHypercertMutation missing query invalidation — detail page shows stale data after edit (from hypercerts-2a3.5)","description":"## Review finding from hypercerts-2a3.5\n\n**File:** queries/hypercerts/use-update-hypercert-mutation.ts\n\n**Problem:**\n`useUpdateHypercertMutation` has no `queryClient.invalidateQueries` call in its `onSuccess` handler. After a successful update, the hypercert detail query (`queryKeys.hypercerts.detail(uri)`) remains stale. The user is navigated back to the detail page (via `options?.onSuccess` → `router.push`), but they see the old data until the cache expires.\n\n**Evidence:**\n```ts\n// use-update-hypercert-mutation.ts\nonSuccess: (data) =\u003e {\n  toast.success('Hypercert updated successfully!');\n  options?.onSuccess?.(data);  // no invalidation here\n},\n```\n\n**Fix:**\nAccept an optional `hypercertUri` in the hook options and invalidate on success:\n```ts\nimport { useQueryClient } from '@tanstack/react-query';\nimport { queryKeys } from '@/lib/api/query-keys';\n\nexport function useUpdateHypercertMutation(options?: UseUpdateHypercertMutationOptions) {\n  const queryClient = useQueryClient();\n  return useMutation({\n    mutationFn: (params: UpdateHypercertRequest) =\u003e updateHypercert(params),\n    onSuccess: (data, variables) =\u003e {\n      queryClient.invalidateQueries({\n        queryKey: queryKeys.hypercerts.detail(variables.hypercertUri),\n      });\n      toast.success('Hypercert updated successfully!');\n      options?.onSuccess?.(data);\n    },\n    // ...\n  });\n}\n```","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T12:10:29.688881664+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T12:15:43.254911806+06:00","closed_at":"2026-02-25T12:15:43.254911806+06:00","close_reason":"d5b249d fix: invalidate hypercert detail query after update","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.26","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:29.690166658+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.26","depends_on_id":"hypercerts-2a3.5","type":"discovered-from","created_at":"2026-02-25T12:11:32.133479463+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.27","title":"Fix: add/[type] page — 'contribution' type shows 'coming soon' placeholder but Add Contributor button is live (from hypercerts-2a3.12)","description":"Review of hypercerts-2a3.12 found: In app/hypercerts/[hypercertUri]/add/[type]/page.tsx lines 93-98, the 'contribution' case renders a 'Contribution form coming soon' placeholder. However, in components/hypercert-contributors-section.tsx line 135, the 'Add Contributor' button links to `/add/contribution` and is shown to owners. This creates a broken UX: the owner clicks 'Add Contributor', navigates to the add page, and sees 'coming soon'. The button should either be hidden until the contribution form is implemented, or the page should show a more informative message explaining why it's not available yet (e.g., 'Contribution management is coming soon. Check back later.') with a back button. Currently the page title also says 'Add Contribution to Hypercert' which is misleading.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:10:36.725260619+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:17:07.382790821+06:00","closed_at":"2026-02-25T12:17:07.382790821+06:00","close_reason":"9b382ea fix: hide Add Contributor button until contribution form is implemented","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.27","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:36.726922583+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.27","depends_on_id":"hypercerts-2a3.12","type":"discovered-from","created_at":"2026-02-25T12:11:03.394425047+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.28","title":"Fix: useDeleteHypercertMutation missing query invalidation — list page shows deleted hypercert (from hypercerts-2a3.6)","description":"## Review finding from hypercerts-2a3.6\n\n**File:** queries/hypercerts/use-delete-hypercert-mutation.ts\n\n**Problem:**\n`useDeleteHypercertMutation` has no `queryClient.invalidateQueries` call. After a successful delete, the hypercerts list query (`queryKeys.hypercerts.lists()`) and the detail query remain stale. The deleted hypercert will still appear in the list until the cache expires.\n\n**Evidence:**\n```ts\n// use-delete-hypercert-mutation.ts\nonSuccess: () =\u003e {\n  toast.success('Hypercert deleted successfully!');\n  options?.onSuccess?.();  // no invalidation\n},\n```\n\n**Fix:**\n```ts\nimport { useQueryClient } from '@tanstack/react-query';\nimport { queryKeys } from '@/lib/api/query-keys';\n\nexport function useDeleteHypercertMutation(options?: UseDeleteHypercertMutationOptions) {\n  const queryClient = useQueryClient();\n  return useMutation({\n    mutationFn: (params: { hypercertUri: string }) =\u003e deleteHypercert(params),\n    onSuccess: (_data, variables) =\u003e {\n      queryClient.invalidateQueries({ queryKey: queryKeys.hypercerts.lists() });\n      queryClient.invalidateQueries({ queryKey: queryKeys.hypercerts.detail(variables.hypercertUri) });\n      toast.success('Hypercert deleted successfully!');\n      options?.onSuccess?.();\n    },\n    // ...\n  });\n}\n```","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T12:10:37.472468439+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T12:16:28.209817963+06:00","closed_at":"2026-02-25T12:16:28.209817963+06:00","close_reason":"9b382ea fix: add query invalidation to useDeleteHypercertMutation","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.28","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:37.473988749+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.28","depends_on_id":"hypercerts-2a3.6","type":"discovered-from","created_at":"2026-02-25T12:11:32.194604456+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.29","title":"Fix: edit form image upload has no 'remove image' option — can only replace, not clear (from hypercerts-2a3.14)","description":"Review of hypercerts-2a3.14 found: In components/hypercerts-edit-form.tsx, the image section allows uploading a new image but provides no way to remove the existing image. The API (PUT /api/certs) supports `image=null` to remove the image (per the spec in hypercerts-2a3.5), and the UpdateHypercertRequest type includes `image?: File | null`. However, the edit form never sets `updates.image = null`. A user who wants to remove their hypercert's background image has no way to do so. Fix: add a 'Remove image' button (visible when imagePreview is set) that clears the preview and sets a flag to send `image: null` on submit.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:10:44.205475226+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:20:21.518373976+06:00","closed_at":"2026-02-25T12:20:21.518373976+06:00","close_reason":"6bfb1f3 fix: add Remove image button to edit form","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.29","depends_on_id":"hypercerts-2a3.14","type":"discovered-from","created_at":"2026-02-25T12:11:03.456947115+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.3","title":"Add server actions: deleteHypercert, updateMeasurement, deleteRecord","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\nAdd three new server actions to `lib/create-actions.ts`:\n\n### 1. `deleteHypercert`\n```typescript\nexport const deleteHypercert = async (params: {\n  hypercertUri: string;\n}) =\u003e {\n  const ctx = await getRepoContext();\n  if (!ctx) {\n    throw new Error(\n      \"deleteHypercert failed: could not establish repository context. The user session may have expired or the target DID is unreachable.\"\n    );\n  }\n  await ctx.scopedRepo.hypercerts.delete(params.hypercertUri);\n  return { success: true };\n};\n```\n\n### 2. `updateMeasurement`\n```typescript\nexport const updateMeasurement = async (params: {\n  measurementUri: string;\n  updates: {\n    metric?: string;\n    value?: string;\n    unit?: string;\n    measurers?: string[];\n    startDate?: string;\n    endDate?: string;\n    methodType?: string;\n    methodURI?: string;\n    evidenceURI?: string[];\n    comment?: string;\n  };\n}) =\u003e {\n  const ctx = await getRepoContext();\n  if (!ctx) {\n    throw new Error(\n      \"updateMeasurement failed: could not establish repository context.\"\n    );\n  }\n  // Map measurers to SDK format if provided\n  const sdkUpdates = { ...params.updates } as Record\u003cstring, unknown\u003e;\n  if (params.updates.measurers) {\n    sdkUpdates.measurers = params.updates.measurers.map((did) =\u003e ({ did }));\n  }\n  return ctx.scopedRepo.hypercerts.updateMeasurement(params.measurementUri, sdkUpdates);\n};\n```\n\n### 3. `deleteRecord`\nA generic delete action for any record type (measurement, evaluation, evidence, contribution):\n```typescript\nexport const deleteRecord = async (params: {\n  recordUri: string;\n}) =\u003e {\n  const ctx = await getRepoContext();\n  if (!ctx) {\n    throw new Error(\n      \"deleteRecord failed: could not establish repository context.\"\n    );\n  }\n  const parsed = parseAtUri(params.recordUri);\n  if (!parsed || !parsed.collection || !parsed.rkey) {\n    throw new Error(\"deleteRecord failed: invalid AT-URI format\");\n  }\n  await ctx.scopedRepo.records.delete({ collection: parsed.collection, rkey: parsed.rkey });\n  return { success: true };\n};\n```\n\nImport `parseAtUri` from `@/lib/utils` (already used elsewhere in the codebase).\n\n## Dont\n- Do NOT create separate delete functions for each record type — one generic deleteRecord is sufficient\n- Do NOT modify existing functions\n- Do NOT remove the \"use server\" directive at the top of the file","acceptance_criteria":"1. deleteHypercert function exported from lib/create-actions.ts\n2. updateMeasurement function exported from lib/create-actions.ts\n3. deleteRecord function exported from lib/create-actions.ts\n4. All three use getRepoContext() and throw if null\n5. deleteHypercert calls ctx.scopedRepo.hypercerts.delete\n6. updateMeasurement calls ctx.scopedRepo.hypercerts.updateMeasurement\n7. deleteRecord parses AT-URI and calls ctx.scopedRepo.records.delete\n8. \"use server\" directive remains at top of file\n9. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-25T11:18:46.617976642+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:42:21.476788069+06:00","closed_at":"2026-02-25T11:42:21.476788069+06:00","close_reason":"9de3179 Add deleteHypercert, updateMeasurement, deleteRecord server actions","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.3","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:18:46.619425636+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.30","title":"Fix: measurements section invalidates all measurement queries instead of just the current hypercert's (from hypercerts-2a3.9)","description":"## Review finding from hypercerts-2a3.9\n\n**File:** components/hypercert-measurements-section.tsx (lines 47-52)\n\n**Problem:**\nThe measurements section uses a raw string array `[\"hypercerts\", \"measurements\"]` as the invalidation query key, which is inconsistent with the rest of the codebase and invalidates ALL measurement queries across ALL hypercerts. The evaluations and evidence sections correctly use `queryKeys.hypercerts.evaluations(hypercertUri)` and `queryKeys.hypercerts.evidence(hypercertUri)` respectively.\n\n**Evidence:**\n```ts\n// hypercert-measurements-section.tsx lines 47-52\nconst deleteMutation = useDeleteRecordMutation({\n  onSuccess: () =\u003e {\n    queryClient.invalidateQueries({\n      queryKey: [\"hypercerts\", \"measurements\"],  // ← raw string, no URI scoping\n    });\n  },\n});\n```\n\nCompare with evaluations section (correct):\n```ts\nqueryKey: queryKeys.hypercerts.evaluations(hypercertUri),  // ← scoped to this hypercert\n```\n\n**Fix:**\n```ts\nimport { queryKeys } from '@/lib/api/query-keys';\n\nconst deleteMutation = useDeleteRecordMutation({\n  onSuccess: () =\u003e {\n    queryClient.invalidateQueries({\n      queryKey: queryKeys.hypercerts.measurements(hypercertUri),\n    });\n  },\n});\n```","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T12:10:46.816588217+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T12:16:13.485122912+06:00","closed_at":"2026-02-25T12:16:13.485122912+06:00","close_reason":"9b382ea fix: scope measurement query invalidation to current hypercert URI — already implemented as part of hypercerts-2a3.15","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.30","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:46.818511307+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.30","depends_on_id":"hypercerts-2a3.9","type":"discovered-from","created_at":"2026-02-25T12:11:32.253988624+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.31","title":"Fix: extractDidFromAtUri is duplicated in edit/page.tsx and detail/page.tsx — should be shared utility (from hypercerts-2a3.14)","description":"Review of hypercerts-2a3.14 found: The `extractDidFromAtUri` function is defined identically in both app/hypercerts/[hypercertUri]/edit/page.tsx (line 12-15) and app/hypercerts/[hypercertUri]/page.tsx (line 12-16). This is code duplication. The function should be extracted to a shared utility (e.g., lib/utils.ts or lib/at-uri.ts) and imported in both places. This is a maintainability issue — if the regex needs to change, it must be updated in two places.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-25T12:10:50.265111059+06:00","created_by":"kzoeps","updated_at":"2026-02-25T12:15:52.875600621+06:00","closed_at":"2026-02-25T12:15:52.875600621+06:00","close_reason":"2d06eea refactor: extract extractDidFromAtUri to shared utility","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.31","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:50.26636071+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-2a3.31","depends_on_id":"hypercerts-2a3.14","type":"discovered-from","created_at":"2026-02-25T12:11:03.512375034+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-2a3.32","title":"Fix: PUT /api/certs and server actions leak internal error messages in 500 responses (from hypercerts-2a3.1)","description":"## Review finding from hypercerts-2a3.1\n\n**File:** app/api/certs/route.ts (lines 57-62, 117-122)\n\n**Problem:**\nBoth POST and PUT handlers include the raw SDK/internal error message in the 500 JSON response body:\n```ts\nerror: `Failed to create hypercert: ${e instanceof Error ? e.message : String(e)}`\nerror: `Failed to update hypercert: ${e instanceof Error ? e.message : String(e)}`\n```\nThis can expose internal implementation details, SDK internals, or PDS error messages to the client. The error is already logged server-side via `console.error`.\n\n**Fix:**\nReturn a generic message to the client and keep the detail server-side only:\n```ts\n// Log full error server-side (already done)\nconsole.error('Error creating hypercert:', e);\n// Return generic message to client\nreturn NextResponse.json({ error: 'Failed to create hypercert' }, { status: 500 });\n```\n\nNote: The existing `apiClient` and `apiClientFormData` in client.ts will surface this message to the UI via `APIError`, so the generic message is what users will see in toast notifications.","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T12:10:57.402250098+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T12:16:15.946544136+06:00","closed_at":"2026-02-25T12:16:15.946544136+06:00","close_reason":"2d06eea fix: return generic 500 messages in PUT/POST /api/certs","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.32","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:10:57.403890885+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.32","depends_on_id":"hypercerts-2a3.1","type":"discovered-from","created_at":"2026-02-25T12:11:32.315364795+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.33","title":"Fix: edit form submits mutation even when no fields changed — wastes a round-trip (from hypercerts-2a3.14)","description":"## Review finding from hypercerts-2a3.14\n\n**File:** components/hypercerts-edit-form.tsx (handleSubmit, lines 67-85)\n\n**Problem:**\n`handleSubmit` builds an `updates` object with only changed fields, but if nothing changed, it still calls `updateMutation.mutate({ hypercertUri })` — an update with no actual changes. This causes an unnecessary network round-trip and SDK write.\n\nAdditionally, the date comparison on line 78-81 is fragile:\n```ts\nif (startDate \u0026\u0026 startDate.toISOString() !== (record.startDate ?? ''))\n```\nIf `record.startDate` is stored in a different ISO format (e.g., without milliseconds), this will always detect a change even when the user didn't touch the date.\n\n**Fix:**\n1. Guard against empty updates:\n```ts\nconst hasChanges = Object.keys(updates).length \u003e 1; // more than just hypercertUri\nif (!hasChanges) {\n  router.push(detailHref);\n  return;\n}\nupdateMutation.mutate(updates);\n```\n\n2. Normalize date comparison — compare only the date portion (YYYY-MM-DD) or use the same format the SDK stores:\n```ts\nconst startDateStr = startDate?.toISOString().split('T')[0];\nconst recordStartStr = record.startDate ? new Date(record.startDate).toISOString().split('T')[0] : '';\nif (startDateStr \u0026\u0026 startDateStr !== recordStartStr) updates.startDate = startDate!.toISOString();\n```","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T12:11:10.252148807+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T12:13:51.86306803+06:00","closed_at":"2026-02-25T12:13:51.86306803+06:00","close_reason":"010be13 duplicate of hypercerts-2a3.17","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.33","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:11:10.253362437+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.33","depends_on_id":"hypercerts-2a3.14","type":"discovered-from","created_at":"2026-02-25T12:11:32.376007593+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.34","title":"Fix: UpdateHypercertResponse type may not match SDK return shape (from hypercerts-2a3.5)","description":"## Review finding from hypercerts-2a3.5\n\n**File:** lib/api/types.ts (lines 48-51)\n\n**Problem:**\n`UpdateHypercertResponse` is hand-declared as `{ uri: string; cid: string }`, but this is not derived from the SDK's actual return type of `ctx.scopedRepo.hypercerts.update()`. If the SDK returns a different shape (e.g., `CreateHypercertResult` or a different object), the TypeScript type will be wrong at runtime even though it compiles.\n\n`CreateHypercertResponse` correctly uses `type CreateHypercertResponse = CreateHypercertResult` (imported from the SDK). `UpdateHypercertResponse` should follow the same pattern.\n\n**Evidence:**\n```ts\n// lib/api/types.ts\nexport interface UpdateHypercertResponse {\n  uri: string;  // hand-declared — not verified against SDK\n  cid: string;\n}\n```\n\n**Fix:**\nImport the SDK's update result type (if exported) and use it:\n```ts\nimport type { UpdateHypercertResult } from '@hypercerts-org/sdk-core';\nexport type UpdateHypercertResponse = UpdateHypercertResult;\n```\nIf the SDK does not export a named type, at minimum add a comment noting this is unverified and add a runtime check in the route handler.","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-25T12:11:22.027694668+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T12:15:42.627302236+06:00","closed_at":"2026-02-25T12:15:42.627302236+06:00","close_reason":"fb4e18a fix: derive UpdateHypercertResponse from SDK's UpdateResult type","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.34","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T12:11:22.029210547+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.34","depends_on_id":"hypercerts-2a3.5","type":"discovered-from","created_at":"2026-02-25T12:11:32.435168612+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.4","title":"Add server action: addContribution (unwrap the stub)","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\nThe existing `addContribution` function in `lib/create-actions.ts` is currently stubbed out (returns `true` immediately with a TODO comment). Unwrap it to actually call the SDK.\n\n**Replace the current stub** with a working implementation:\n\n```typescript\nexport const addContribution = async (params: {\n  hypercertUri: string;\n  contributors: string[];\n  contributionDetails: {\n    role: string;\n    contributionDescription?: string;\n    startDate?: string;\n    endDate?: string;\n  };\n}) =\u003e {\n  const ctx = await getRepoContext();\n  if (!ctx) {\n    throw new Error(\n      \"addContribution failed: could not establish repository context.\"\n    );\n  }\n\n  return ctx.scopedRepo.hypercerts.addContribution({\n    hypercertUri: params.hypercertUri,\n    contributors: params.contributors,\n    contributionDetails: params.contributionDetails,\n  });\n};\n```\n\nThe SDK method `hypercerts.addContribution` accepts:\n- `hypercertUri: string`\n- `contributors: Array\u003cContributorIdentityParams\u003e` — strings (DIDs) work directly\n- `contributionDetails: ContributionDetailsParams` — the object with role/description/dates works directly\n- `weight?: string` (optional, omit for now)\n\n**Remove** the `// TODO addContribution in SDK needs to be updated` comment and the `// eslint-disable-next-line @typescript-eslint/no-unused-vars` comment.\n\n## Dont\n- Do NOT change the function signature (keep the same params shape)\n- Do NOT add weight parameter yet\n- Do NOT modify any other functions in the file","acceptance_criteria":"1. addContribution no longer returns true immediately\n2. addContribution calls ctx.scopedRepo.hypercerts.addContribution\n3. The TODO comment and eslint-disable comment are removed\n4. The _params underscore prefix is removed (params is now used)\n5. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-25T11:19:01.097668884+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:41:24.669653529+06:00","closed_at":"2026-02-25T11:41:24.669653529+06:00","close_reason":"9de3179 Unwrap addContribution stub to call SDK","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-2a3.4","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:19:01.099378006+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.5","title":"Add client API function: updateHypercert","description":"## Files\n- lib/api/hypercerts.ts (modify)\n- lib/api/types.ts (modify)\n\n## What to do\n\n### 1. Add types to `lib/api/types.ts`:\n\n```typescript\nexport interface UpdateHypercertRequest {\n  hypercertUri: string;\n  title?: string;\n  shortDescription?: string;\n  description?: string;\n  startDate?: string;\n  endDate?: string;\n  image?: File | null; // null = remove image\n}\n\nexport interface UpdateHypercertResponse {\n  uri: string;\n  cid: string;\n}\n```\n\n### 2. Add `updateHypercert` function to `lib/api/hypercerts.ts`:\n\n```typescript\nexport async function updateHypercert(\n  params: UpdateHypercertRequest,\n): Promise\u003cUpdateHypercertResponse\u003e {\n  const formData = new FormData();\n  formData.append(\"hypercertUri\", params.hypercertUri);\n\n  if (params.title !== undefined) formData.append(\"title\", params.title);\n  if (params.shortDescription !== undefined) formData.append(\"shortDescription\", params.shortDescription);\n  if (params.description !== undefined) formData.append(\"description\", params.description);\n  if (params.startDate !== undefined) formData.append(\"startDate\", params.startDate);\n  if (params.endDate !== undefined) formData.append(\"endDate\", params.endDate);\n\n  if (params.image === null) {\n    formData.append(\"image\", \"null\");\n  } else if (params.image) {\n    formData.append(\"image\", params.image);\n  }\n\n  return apiClientFormData\u003cUpdateHypercertResponse\u003e(\"/api/certs\", formData, { method: \"PUT\" });\n}\n```\n\nNote: `apiClientFormData` currently hardcodes `method: \"POST\"`. You need to pass `{ method: \"PUT\" }` via the options parameter. Check `lib/api/client.ts` — the `apiClientFormData` function accepts `options?: Omit\u003cRequestInit, \"body\"\u003e` as third param, and spreads it with `...options`. Since `method` is in `RequestInit`, passing `{ method: \"PUT\" }` will override the default POST.\n\n### No deleteHypercert client function needed\nDelete is handled as a server action (`deleteHypercert` in `lib/create-actions.ts`), so no client API function is needed.\n\n## Dont\n- Do NOT add a deleteHypercert function (its a server action now)\n- Do NOT modify existing functions\n- Do NOT change existing type definitions","acceptance_criteria":"1. UpdateHypercertRequest and UpdateHypercertResponse types exported from lib/api/types.ts\n2. updateHypercert function exported from lib/api/hypercerts.ts\n3. updateHypercert sends FormData via PUT to /api/certs\n4. Only non-undefined fields are appended to FormData\n5. image=null sends string \"null\", image=File sends the file, image=undefined skips\n6. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-02-25T11:19:35.396412366+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:45:22.127160674+06:00","closed_at":"2026-02-25T11:45:22.127160674+06:00","close_reason":"23d0eb9 Add updateHypercert client API function and types","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.5","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:19:35.397983949+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.5","depends_on_id":"hypercerts-2a3.1","type":"blocks","created_at":"2026-02-25T11:22:45.060237219+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.6","title":"Add React Query mutation hooks: useUpdateHypercertMutation, useDeleteHypercertMutation, useDeleteRecordMutation","description":"## Files\n- queries/hypercerts/use-update-hypercert-mutation.ts (create)\n- queries/hypercerts/use-delete-hypercert-mutation.ts (create)\n- queries/hypercerts/use-delete-record-mutation.ts (create)\n- queries/hypercerts/index.ts (modify — add exports)\n\n## What to do\nCreate three React Query mutation hooks following the exact pattern of `use-create-hypercert-mutation.ts`:\n\n### 1. `use-update-hypercert-mutation.ts`\n```typescript\n\"use client\";\nimport { useMutation } from \"@tanstack/react-query\";\nimport { toast } from \"sonner\";\nimport { updateHypercert } from \"@/lib/api/hypercerts\";\nimport type { UpdateHypercertRequest } from \"@/lib/api/types\";\n\ninterface UseUpdateHypercertMutationOptions {\n  onSuccess?: (data: Awaited\u003cReturnType\u003ctypeof updateHypercert\u003e\u003e) =\u003e void;\n}\n\nexport function useUpdateHypercertMutation(options?: UseUpdateHypercertMutationOptions) {\n  return useMutation({\n    mutationFn: (params: UpdateHypercertRequest) =\u003e updateHypercert(params),\n    onSuccess: (data) =\u003e {\n      toast.success(\"Hypercert updated successfully!\");\n      options?.onSuccess?.(data);\n    },\n    onError: (error) =\u003e {\n      console.error(\"Update hypercert error:\", error);\n      toast.error(error instanceof Error ? error.message : \"Failed to update hypercert.\");\n    },\n  });\n}\n```\n\n### 2. `use-delete-hypercert-mutation.ts`\nCalls the `deleteHypercert` **server action** from `@/lib/create-actions` (NOT a client API function).\n```typescript\n\"use client\";\nimport { useMutation } from \"@tanstack/react-query\";\nimport { toast } from \"sonner\";\nimport { deleteHypercert } from \"@/lib/create-actions\";\n\ninterface UseDeleteHypercertMutationOptions {\n  onSuccess?: () =\u003e void;\n}\n\nexport function useDeleteHypercertMutation(options?: UseDeleteHypercertMutationOptions) {\n  return useMutation({\n    mutationFn: (params: { hypercertUri: string }) =\u003e deleteHypercert(params),\n    onSuccess: () =\u003e {\n      toast.success(\"Hypercert deleted successfully!\");\n      options?.onSuccess?.();\n    },\n    onError: (error) =\u003e {\n      console.error(\"Delete hypercert error:\", error);\n      toast.error(error instanceof Error ? error.message : \"Failed to delete hypercert.\");\n    },\n  });\n}\n```\n\n### 3. `use-delete-record-mutation.ts`\nCalls the `deleteRecord` **server action** from `@/lib/create-actions`.\n```typescript\n\"use client\";\nimport { useMutation } from \"@tanstack/react-query\";\nimport { toast } from \"sonner\";\nimport { deleteRecord } from \"@/lib/create-actions\";\n\ninterface UseDeleteRecordMutationOptions {\n  onSuccess?: () =\u003e void;\n}\n\nexport function useDeleteRecordMutation(options?: UseDeleteRecordMutationOptions) {\n  return useMutation({\n    mutationFn: (params: { recordUri: string }) =\u003e deleteRecord(params),\n    onSuccess: () =\u003e {\n      toast.success(\"Record deleted successfully!\");\n      options?.onSuccess?.();\n    },\n    onError: (error) =\u003e {\n      console.error(\"Delete record error:\", error);\n      toast.error(error instanceof Error ? error.message : \"Failed to delete record.\");\n    },\n  });\n}\n```\n\n### 4. Update `queries/hypercerts/index.ts`\nAdd exports for all three new hooks.\n\n## Dont\n- Do NOT modify existing hooks\n- Do NOT add query invalidation (handled in the UI layer)\n- Each file must start with \"use client\"","acceptance_criteria":"1. Three new files created in queries/hypercerts/\n2. All three hooks exported from queries/hypercerts/index.ts\n3. Each hook uses useMutation with toast notifications\n4. useUpdateHypercertMutation calls updateHypercert from lib/api/hypercerts\n5. useDeleteHypercertMutation calls deleteHypercert server action from lib/create-actions\n6. useDeleteRecordMutation calls deleteRecord server action from lib/create-actions\n7. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-25T11:19:54.542826435+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:48:27.960843151+06:00","closed_at":"2026-02-25T11:48:27.960843151+06:00","close_reason":"3408b02 Add React Query mutation hooks: useUpdateHypercertMutation, useDeleteHypercertMutation, useDeleteRecordMutation","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.6","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:19:54.545460839+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.6","depends_on_id":"hypercerts-2a3.5","type":"blocks","created_at":"2026-02-25T11:22:45.240086188+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.6","depends_on_id":"hypercerts-2a3.3","type":"blocks","created_at":"2026-02-25T11:22:45.330839409+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.7","title":"Add ownership check utility and pass isOwner to detail view","description":"## Files\n- app/hypercerts/[hypercertUri]/page.tsx (modify)\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\n\n### 1. In `app/hypercerts/[hypercertUri]/page.tsx`:\nCompute `isOwner` by comparing the logged-in user DID with the DID from the hypercert URI.\n\nThe session is already fetched on line 71: `const [viewCtx, session] = await Promise.all([...])`.\nThe owner DID is already extracted on line 67: `const ownerDid = extractDidFromAtUri(decodedUri)`.\n\nAdd:\n```typescript\nconst isOwner = Boolean(session?.did \u0026\u0026 ownerDid \u0026\u0026 session.did === ownerDid);\n```\n\nPass `isOwner` as a prop to `\u003cHypercertDetailsView\u003e`:\n```tsx\n\u003cHypercertDetailsView\n  hypercertUri={decodedUri}\n  record={certWithoutImage}\n  imageUri={imageUri}\n  isOwner={isOwner}\n/\u003e\n```\n\n### 2. In `components/hypercert-detail-view.tsx`:\nAdd `isOwner: boolean` to the props interface. Accept it and pass it down to the child section components:\n- `\u003cHypercertMeasurementsSection hypercertUri={hypercertUri} isOwner={isOwner} /\u003e`\n- `\u003cHypercertEvidenceSection hypercertUri={hypercertUri} isOwner={isOwner} /\u003e`\n- `\u003cHypercertEvaluationsSection hypercertUri={hypercertUri} isOwner={isOwner} /\u003e`\n- `\u003cHypercertContributorsSection contributors={contributors} isOwner={isOwner} hypercertUri={hypercertUri} /\u003e`\n\nThe child components do NOT need to be modified in this task — they will accept `isOwner` as an optional prop and ignore it until the UI tasks wire it up. TypeScript will not error because we are adding an extra prop that the components dont destructure (React ignores unknown props on custom components).\n\nActually, to be type-safe, add `isOwner?: boolean` to each section component props. In each of these files, find the props type/interface and add `isOwner?: boolean`:\n- components/hypercert-measurements-section.tsx\n- components/hypercert-evidence-section.tsx\n- components/hypercert-evaluations-section.tsx\n- components/hypercert-contributors-section.tsx\n\nJust add the prop to the type — do NOT use it yet.\n\n## Dont\n- Do NOT add any edit/delete buttons yet\n- Do NOT change the visual appearance of any component\n- Do NOT modify the session check logic","acceptance_criteria":"1. isOwner boolean computed in page.tsx by comparing session.did with ownerDid\n2. isOwner passed as prop to HypercertDetailsView\n3. HypercertDetailsView accepts isOwner and passes it to all 4 section components\n4. All 4 section components accept isOwner?: boolean in their props\n5. TypeScript compiles with no errors\n6. No visual changes to the page","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-02-25T11:20:25.574762489+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:43:10.965070869+06:00","closed_at":"2026-02-25T11:43:10.965070869+06:00","close_reason":"0b31ff2 Add ownership check utility and pass isOwner to detail view","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.7","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:20:25.576915182+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.8","title":"Add reusable DeleteConfirmDialog component","description":"## Files\n- components/delete-confirm-dialog.tsx (create)\n\n## What to do\nCreate a reusable confirmation dialog for delete operations. This will be used by multiple sections (measurements, evaluations, evidence, contributors, and the hypercert itself).\n\n```typescript\n\"use client\";\n\nimport {\n  AlertDialog,\n  AlertDialogAction,\n  AlertDialogCancel,\n  AlertDialogContent,\n  AlertDialogDescription,\n  AlertDialogFooter,\n  AlertDialogHeader,\n  AlertDialogTitle,\n  AlertDialogTrigger,\n} from \"@/components/ui/alert-dialog\";\nimport { Button } from \"@/components/ui/button\";\nimport { Spinner } from \"@/components/ui/spinner\";\nimport { Trash2 } from \"lucide-react\";\n\ninterface DeleteConfirmDialogProps {\n  /** What is being deleted, e.g. \"measurement\", \"hypercert\" */\n  itemType: string;\n  /** Optional item name/title for the description */\n  itemName?: string;\n  /** Whether the delete is in progress */\n  isDeleting: boolean;\n  /** Called when user confirms deletion */\n  onConfirm: () =\u003e void;\n  /** Optional trigger element — defaults to a trash icon button */\n  trigger?: React.ReactNode;\n}\n\nexport function DeleteConfirmDialog({ itemType, itemName, isDeleting, onConfirm, trigger }: DeleteConfirmDialogProps) {\n  return (\n    \u003cAlertDialog\u003e\n      \u003cAlertDialogTrigger asChild\u003e\n        {trigger || (\n          \u003cButton\n            variant=\"ghost\"\n            size=\"icon\"\n            className=\"h-8 w-8 text-muted-foreground hover:text-destructive\"\n            aria-label={`Delete ${itemType}`}\n          \u003e\n            \u003cTrash2 className=\"h-4 w-4\" /\u003e\n          \u003c/Button\u003e\n        )}\n      \u003c/AlertDialogTrigger\u003e\n      \u003cAlertDialogContent\u003e\n        \u003cAlertDialogHeader\u003e\n          \u003cAlertDialogTitle\u003eDelete {itemType}?\u003c/AlertDialogTitle\u003e\n          \u003cAlertDialogDescription\u003e\n            {itemName\n              ? `Are you sure you want to delete \"${itemName}\"? This action cannot be undone.`\n              : `Are you sure you want to delete this ${itemType}? This action cannot be undone.`}\n          \u003c/AlertDialogDescription\u003e\n        \u003c/AlertDialogHeader\u003e\n        \u003cAlertDialogFooter\u003e\n          \u003cAlertDialogCancel disabled={isDeleting}\u003eCancel\u003c/AlertDialogCancel\u003e\n          \u003cAlertDialogAction\n            onClick={onConfirm}\n            disabled={isDeleting}\n            className=\"bg-destructive text-destructive-foreground hover:bg-destructive/90\"\n          \u003e\n            {isDeleting ? \u003c\u003e\u003cSpinner className=\"mr-2\" /\u003e Deleting...\u003c/\u003e : \"Delete\"}\n          \u003c/AlertDialogAction\u003e\n        \u003c/AlertDialogFooter\u003e\n      \u003c/AlertDialogContent\u003e\n    \u003c/AlertDialog\u003e\n  );\n}\n```\n\n**Note**: The AlertDialog components from shadcn/ui should already exist in `components/ui/alert-dialog.tsx`. If they dont, install them first with `npx shadcn@latest add alert-dialog`.\n\n## Dont\n- Do NOT add any business logic — this is purely a presentational component\n- Do NOT import any mutation hooks or API functions","acceptance_criteria":"1. File components/delete-confirm-dialog.tsx exists\n2. Exports DeleteConfirmDialog component\n3. Uses AlertDialog from shadcn/ui\n4. Accepts itemType, itemName, isDeleting, onConfirm, trigger props\n5. Shows confirmation text with item type/name\n6. Confirm button shows spinner when isDeleting=true\n7. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-25T11:20:43.109134168+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:42:02.805685278+06:00","closed_at":"2026-02-25T11:42:02.805685278+06:00","close_reason":"10af334 Add reusable DeleteConfirmDialog component","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.8","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:20:43.111338917+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2a3.9","title":"Add delete buttons to measurements section on detail view","description":"## Files\n- components/hypercert-measurements-section.tsx (modify)\n\n## What to do\nWhen `isOwner` is true, show a delete button next to each measurement card. When clicked, it should delete the measurement record.\n\n### Changes:\n1. Destructure `isOwner` from props (already added as optional prop in task 2a3.7)\n2. Import `useDeleteRecordMutation` from `@/queries/hypercerts`\n3. Import `DeleteConfirmDialog` from `@/components/delete-confirm-dialog`\n4. Import `useQueryClient` from `@tanstack/react-query`\n5. Inside the component, call `useDeleteRecordMutation` and `useQueryClient`\n6. For each measurement card, if `isOwner` is true, render a `\u003cDeleteConfirmDialog\u003e` with:\n   - `itemType=\"measurement\"`\n   - `itemName={measurement.metric}` (the metric name)\n   - `isDeleting={deleteMutation.isPending}`\n   - `onConfirm` calls `deleteMutation.mutate({ recordUri })` where `recordUri` is the AT-URI of the measurement\n\n**Getting the measurement URI**: The measurement links from `useMeasurementLinksQuery` return `{ did, collection, rkey }` objects. The AT-URI is `at://${did}/${collection}/${rkey}`. You need to pass the measurement URI alongside the measurement data. Modify the `useMemo` block to also track the URI for each measurement.\n\nCurrently measurements are collected as:\n```typescript\nif (q.isSuccess \u0026\u0026 q.data) items.push(q.data.value as Measurement);\n```\n\nChange to track URI too:\n```typescript\ntype MeasurementWithUri = Measurement \u0026 { _uri: string };\n// ...\nif (q.isSuccess \u0026\u0026 q.data) {\n  items.push({ ...(q.data.value as Measurement), _uri: `at://${measurementLinks[idx].did}/${measurementLinks[idx].collection}/${measurementLinks[idx].rkey}` });\n}\n```\n\nOn successful delete, invalidate the measurement queries:\n```typescript\nonSuccess: () =\u003e {\n  queryClient.invalidateQueries({ queryKey: [\"hypercerts\", \"measurements\"] });\n}\n```\n\n### Visual placement:\nPosition the delete button in the top-right corner of each measurement card using absolute positioning or flex layout.\n\n## Dont\n- Do NOT add edit/update functionality for measurements in this task\n- Do NOT change the measurement card layout significantly\n- Do NOT show delete buttons when isOwner is false/undefined","acceptance_criteria":"1. Delete button (trash icon) appears on each measurement card when isOwner=true\n2. Delete button is hidden when isOwner is false/undefined\n3. Clicking delete shows confirmation dialog\n4. Confirming delete calls deleteRecord with the measurement AT-URI\n5. After successful delete, measurement queries are invalidated (list refreshes)\n6. TypeScript compiles with no errors","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-25T11:21:01.337191472+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:51:06.123707298+06:00","closed_at":"2026-02-25T11:51:06.123707298+06:00","close_reason":"1d01010 Add delete buttons to measurements section on detail view","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-2a3.9","depends_on_id":"hypercerts-2a3","type":"parent-child","created_at":"2026-02-25T11:21:01.338772443+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.9","depends_on_id":"hypercerts-2a3.6","type":"blocks","created_at":"2026-02-25T11:22:45.684948918+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.9","depends_on_id":"hypercerts-2a3.7","type":"blocks","created_at":"2026-02-25T11:22:45.76789743+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-2a3.9","depends_on_id":"hypercerts-2a3.8","type":"blocks","created_at":"2026-02-25T11:22:45.848977011+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2gc","title":"Fix: addContribution missing ownership check before putRecord (from hypercerts-czy.2)","description":"Review of hypercerts-czy.2 found: addContribution() in lib/create-actions.ts (lines 128-162) calls putRecord with repo: ctx.activeDid but never validates that hypercertParsed.did === ctx.activeDid. If a caller passes a hypercertUri belonging to a different DID, the ATProto server will reject the write with an opaque authorization error instead of a clear 403. The original SDK (ctx.scopedRepo.hypercerts.addContribution) handled this internally. Fix: add an explicit check before the putRecord call: if (hypercertParsed.did !== ctx.activeDid) throw new Error('Forbidden — URI DID does not match active session DID.');","status":"open","priority":1,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:06.37701055+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:06.37701055+06:00","dependencies":[{"issue_id":"hypercerts-2gc","depends_on_id":"hypercerts-czy.2","type":"discovered-from","created_at":"2026-03-04T11:17:09.581011668+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-2wn","title":"Fix: OAUTH_SESSION_SECRET read directly from process.env in epds-session-cookie.ts, bypassing config (from hypercerts-dhz.4)","description":"Review of hypercerts-dhz found: lib/epds-session-cookie.ts reads OAUTH_SESSION_SECRET directly from process.env (line 26) instead of using config.oauthSessionSecret from lib/config.ts. This is an inconsistency — all other env vars are read through the centralized config object.\n\nThe practical risk: if the env var name is ever changed or aliased in config.ts, epds-session-cookie.ts will silently continue reading the old name and throw 'OAUTH_SESSION_SECRET is not configured' even when the config object has the value.\n\nAdditionally, config.ts already stores oauthSessionSecret (line 200) but it is never used — dead config field.\n\nFix: change getSessionSecret() in epds-session-cookie.ts to import and use config.oauthSessionSecret instead of process.env.OAUTH_SESSION_SECRET directly. This makes the secret consistent with how all other env vars are accessed.","status":"closed","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:26:37.303468201+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:13:09.669753012+06:00","closed_at":"2026-02-24T17:13:09.669753012+06:00","close_reason":"Already fixed in dhz.14 (commit 98de1d5). epds-session-cookie.ts line 27 now reads config.oauthSessionSecret instead of process.env directly.","dependencies":[{"issue_id":"hypercerts-2wn","depends_on_id":"hypercerts-dhz.4","type":"discovered-from","created_at":"2026-02-24T16:26:44.868852389+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-3bd","title":"Fix: useAddLocationMutation does not invalidate any query cache after success","description":"## Problem\n`queries/hypercerts/use-add-location-mutation.ts` (lines 27-38) has an `onSuccess` handler that only shows a toast — it does not invalidate any TanStack Query cache entries. After a location is successfully added, the UI will continue showing stale data until the user manually refreshes.\n\nCompare with `use-add-attachment-mutation.ts` (lines 32-38) which correctly invalidates `queryKeys.hypercerts.evidence(variables.hypercertUri)` after success.\n\n## Fix\nAdd cache invalidation in the `onSuccess` handler. The mutation params include `hypercertUri`, so use it to invalidate the relevant query:\n\n```ts\nonSuccess: (_, variables) =\u003e {\n  toast.success('Location added successfully!');\n  queryClient.invalidateQueries({\n    queryKey: queryKeys.hypercerts.measurements(variables.hypercertUri),\n  });\n  options?.onSuccess?.();\n},\n```\n\nAlso add `const queryClient = useQueryClient();` at the top of the hook (currently missing).\n\n## Evidence\n- `queries/hypercerts/use-add-location-mutation.ts` lines 27-38: no `queryClient.invalidateQueries` call\n- `queries/hypercerts/use-add-attachment-mutation.ts` lines 32-38: correct pattern to follow","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:23.386065309+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:23.386065309+06:00"}
{"id":"hypercerts-3hy","title":"Non-standard: application_type sent in PAR request body — RFC 9126 does not define this parameter","description":"Review of hypercerts-i1m.1 (commit f880d7b) found that `application_type` is being sent as a parameter in the PAR (Pushed Authorization Request) body.\n\n## Problem\n\n`application_type` is defined in RFC 7591 (OAuth Dynamic Client Registration) as a **client metadata field**, not as an authorization request parameter. RFC 9126 (PAR) and RFC 6749 (OAuth 2.0 core) do not define `application_type` as a valid authorization request parameter.\n\nSending it in the PAR body is non-standard. The ePDS server may:\n- Silently ignore it (most likely, harmless today)\n- Reject the request with an `invalid_request` error if it validates parameters strictly\n\n## Evidence\n\n```typescript\n// app/api/oauth/login/route.ts:34-43\nconst parBody = new URLSearchParams({\n  client_id: clientId,\n  redirect_uri: redirectUri,\n  response_type: \"code\",\n  scope: OAUTH_SCOPE,\n  application_type: config.isLoopback ? \"native\" : \"web\",  // ← non-standard PAR param\n  state,\n  code_challenge: codeChallenge,\n  code_challenge_method: \"S256\",\n});\n```\n\nThe correct place to communicate `application_type` is the client metadata document (`/client-metadata.json`), which `buildClientMetadata()` already does correctly (lines 278, 300 of `lib/config.ts`).\n\n## Risk\n\nLow risk if the ePDS server is lenient. High risk if the ePDS server validates PAR parameters against the OAuth spec strictly. Should be verified against the actual ePDS server behavior and either documented as intentional (with evidence the server accepts it) or removed from the PAR body.","status":"open","priority":3,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-02-26T15:57:48.54434912+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T15:57:48.54434912+06:00","dependencies":[{"issue_id":"hypercerts-3hy","depends_on_id":"hypercerts-i1m.1","type":"discovered-from","created_at":"2026-02-26T15:57:51.870897107+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-3jr","title":"Fix: DPoP proof missing exp claim — unbounded replay window (from security review)","description":"## File\nlib/epds-helpers.ts, lines 113-120 (createDpopProof function)\n\n## Problem\nThe DPoP proof JWT generated by `createDpopProof()` includes `iat` (issued-at) but not `exp` (expiration):\n```ts\nconst payload: Record\u003cstring, unknown\u003e = {\n  jti: crypto.randomUUID(),\n  htm: opts.method,\n  htu: opts.url,\n  iat: Math.floor(Date.now() / 1000),\n  // exp is MISSING\n};\n```\n\nRFC 9449 (DPoP) section 4.2 states that DPoP proofs SHOULD include an `exp` claim. Without `exp`, a captured DPoP proof remains valid indefinitely from the server's perspective (subject only to the server's own nonce-based replay detection). If the ePDS server does not enforce strict nonce replay detection, a captured proof could be replayed.\n\nThe `jti` claim provides some replay protection if the server tracks used JTIs, but without `exp` the server must store JTIs forever to guarantee uniqueness.\n\n## Fix\nAdd an `exp` claim with a short lifetime (e.g. 60 seconds):\n```ts\nconst now = Math.floor(Date.now() / 1000);\nconst payload: Record\u003cstring, unknown\u003e = {\n  jti: crypto.randomUUID(),\n  htm: opts.method,\n  htu: opts.url,\n  iat: now,\n  exp: now + 60,  // 60-second window per RFC 9449 recommendation\n};\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:02.985256998+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:02.985256998+06:00","dependencies":[{"issue_id":"hypercerts-3jr","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.698870184+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-3mq","title":"Fix: nyk.13 refactor reverted all 4 desktop left-alignment changes from nyk.14","description":"Review of hypercerts-nyk.14 found: the nyk.13 refactor commit (ce6a9cf, merged after 9a98af1) rewrote lib/atproto-branding.ts and reverted all 4 alignment changes that nyk.14 made.\n\nCurrent HEAD state of lib/atproto-branding.ts has:\n1. Line 86 comment: 'Desktop: right aligned' (should say left)\n2. Line 96: background-position: right center (should be left center)\n3. Line 146: background-position: right center (should be left center)\n4. Line 781: justify-items: end (should be start)\n5. Line 786: text-align: right (should be left)\n\nAll 4 acceptance criteria of nyk.14 are now failing:\n- AC1: horizontal logo above H1 is NOT left-aligned on desktop (right center)\n- AC2: certified-signin.svg H1 image is NOT left-aligned on desktop (right center)\n- AC3: desktop .grid.grow.content-center uses justify-items: end (not start)\n- AC4: .md:text-right still forces text-align: right\n\nFix: re-apply the 4 changes from nyk.14 to the current HEAD of lib/atproto-branding.ts:\n  - background-position: left center for logo (line ~96)\n  - background-position: left center for H1 certified-signin.svg (line ~146)\n  - justify-items: start in desktop media query (line ~781)\n  - text-align: left for .md:text-right in desktop media query (line ~786)\n\nEvidence: git show 9a98af1 shows the correct changes; git show ce6a9cf shows the massive rewrite that overwrote them; current file confirmed via grep.","status":"open","priority":2,"issue_type":"bug","owner":"kzoeps","created_at":"2026-02-17T19:02:01.435663195+06:00","created_by":"kzoeps","updated_at":"2026-02-17T19:02:01.435663195+06:00","dependencies":[{"issue_id":"hypercerts-3mq","depends_on_id":"hypercerts-nyk.14","type":"discovered-from","created_at":"2026-02-17T19:02:05.623349672+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-3po","title":"Fix: logout endpoint uses GET method — CSRF risk for state-changing operation (from security review)","description":"## File\napp/api/oauth/logout/route.ts, line 5\n\n## Problem\nThe logout endpoint is implemented as a GET handler:\n```ts\nexport async function GET() {\n  // ... calls session.signOut() and deletes user-did cookie\n}\n```\n\nGET requests are state-changing (they sign out the user and revoke the session). This violates the HTTP spec (GET must be safe/idempotent) and creates a CSRF risk: any page on any origin can trigger logout by embedding an `\u003cimg src=\"https://app.com/api/oauth/logout\"\u003e` or similar tag. Browsers send cookies with cross-origin GET requests (subject to SameSite policy).\n\nThe `user-did` cookie uses `SameSite: Lax` (default) which protects against cross-site form POSTs but NOT against cross-site GET requests triggered by `\u003cimg\u003e`, `\u003cscript\u003e`, or `\u003clink\u003e` tags.\n\nThe client-side caller (`lib/api/auth.ts`) already uses `method: 'GET'` for logout, so this is a consistent but incorrect design.\n\n## Fix\nChange logout to POST and add CSRF protection:\n1. Change the route to `export async function POST()`\n2. Update `lib/api/auth.ts` to use `method: 'POST'`\n3. Optionally add a CSRF token check (or rely on `SameSite: Strict` on the session cookie)\n\nAlternatively, keep GET but add an explicit `SameSite: Strict` cookie check or require a secret token in the request body.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:43.402957789+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:43.402957789+06:00","dependencies":[{"issue_id":"hypercerts-3po","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.941418198+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-3tq","title":"Fix: .vscode/settings.json missing editor.formatOnSave and prettier integration — DX gap for new contributors","description":"Review of .vscode/settings.json found it only contains a personal color theme and is missing project-standard editor settings that would help new contributors.\n\nEvidence:\n- .vscode/settings.json only contains: { 'workbench.colorTheme': 'Kimbie Dark' }\n- The project has prettier configured (.prettierrc.json) and eslint (eslint.config.mjs)\n- No editor.formatOnSave, no defaultFormatter, no eslint integration settings\n\nA new developer cloning this repo will not get automatic format-on-save unless they configure it manually. This defeats the purpose of having prettier + husky.\n\nSuggested additions to .vscode/settings.json:\n{\n  'editor.formatOnSave': true,\n  'editor.defaultFormatter': 'esbenp.prettier-vscode',\n  'editor.codeActionsOnSave': {\n    'source.fixAll.eslint': 'explicit'\n  },\n  'typescript.tsdk': 'node_modules/typescript/lib',\n  '[typescript]': { 'editor.defaultFormatter': 'esbenp.prettier-vscode' },\n  '[typescriptreact]': { 'editor.defaultFormatter': 'esbenp.prettier-vscode' }\n}\n\nAlso consider adding .vscode/extensions.json with recommended extensions:\n- esbenp.prettier-vscode\n- dbaeumer.vscode-eslint\n- bradlc.vscode-tailwindcss\n\nNote: This issue is related to hypercerts-kpe (personal color theme should be removed from this file).","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:48:46.130438863+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:48:46.130438863+06:00","dependencies":[{"issue_id":"hypercerts-3tq","depends_on_id":"hypercerts-kpe","type":"discovered-from","created_at":"2026-02-27T11:48:51.127612719+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-45q","title":"Fix: resolveRecordBlobs in blob-utils.ts calls getRepoContext for every blob in a record — N+1 session restores","description":"## Problem\n`lib/blob-utils.ts` `resolveBlobToUrl` (lines 8-24) calls `getRepoContext({ targetDid: ownerDid })` for every blob it resolves. `resolveRecordBlobs` (lines 30-57) recursively processes all fields in parallel, meaning if a record has 5 blob fields, `getRepoContext` is called 5 times concurrently.\n\nWhile `getRepoContext` is wrapped with React `cache()`, this deduplication only works within a single RSC render pass. In Server Actions (where these functions are called from `create-actions.ts`), the cache may or may not deduplicate correctly depending on the Next.js version and execution context.\n\nMore critically, `resolveBlobToUrl` also calls `getSession()` separately from `getRepoContext` (line 15), meaning each blob resolution does TWO cookie reads and potentially TWO Redis lookups for the session.\n\n## Fix\nHoist the session and PDS URL resolution out of the per-blob loop. Resolve them once before calling `resolveRecordBlobs`, then pass the resolved values down:\n\n```ts\nexport async function resolveRecordBlobs(\n  value: unknown,\n  ownerDid: string,\n  pdsUrl?: string, // pre-resolved, optional\n): Promise\u003cunknown\u003e {\n  // ...\n}\n```\n\nOr resolve session + pdsUrl once in the callers (`getMeasurementRecord`, `getEvaluationRecord`, etc.) and pass them through.\n\n## Evidence\n- `lib/blob-utils.ts` lines 15-23: `getSession()` + `getRepoContext()` called per blob\n- `lib/create-actions.ts` lines 155-159, 175-179, 195-199, 215-218: callers that trigger this","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:17.198338084+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:17.198338084+06:00"}
{"id":"hypercerts-4au7","title":"Fix: profile avatar/banner assigned without $type in union position (from hypercerts-phiw.4)","description":"Review of hypercerts-phiw.4 found: In app/api/profile/update/route.ts, the avatar and banner fields are assigned as 'avatarBlob.original as AppCertifiedActorProfile.Record[\"avatar\"]' without a $type discriminator. These fields are union types in v0.10.0:\n  avatar?: $Typed\u003cOrgHypercertsDefs.Uri\u003e | $Typed\u003cOrgHypercertsDefs.SmallImage\u003e | { $type: string }\n  banner?: $Typed\u003cOrgHypercertsDefs.Uri\u003e | $Typed\u003cOrgHypercertsDefs.LargeImage\u003e | { $type: string }\n\nEvidence: lines 114-119 and 179-184:\n  if (avatarBlob)\n    record.avatar = avatarBlob.original as AppCertifiedActorProfile.Record[\"avatar\"];\n  if (bannerBlob)\n    record.banner = bannerBlob.original as AppCertifiedActorProfile.Record[\"banner\"];\n\navatarBlob.original is a BlobRef object (from @atproto/lexicon), not a SmallImage or Uri object. The validator will reject this because the union discriminator ($type) is missing.\n\nThe assertValidRecord call at line 122 will throw at runtime when avatar or banner are provided.\n\nFix: Wrap the blob in the correct union variant:\n  if (avatarBlob)\n    record.avatar = { $type: 'org.hypercerts.defs#smallImage' as const, image: avatarBlob };\n  if (bannerBlob)\n    record.banner = { $type: 'org.hypercerts.defs#largeImage' as const, image: bannerBlob };\n\nNote: avatarBlob from uploadBlob().data.blob is already a BlobRef, not avatarBlob.original. The .original cast is also suspicious — uploadBlob returns { data: { blob: BlobRef } }, so the correct value is uploadResult.data.blob directly.","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T16:49:16.945585733+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:55:16.596464953+06:00","closed_at":"2026-03-04T16:55:16.596464953+06:00","close_reason":"3016d52 Fix: wrap avatar/banner blobs in typed union variants","dependencies":[{"issue_id":"hypercerts-4au7","depends_on_id":"hypercerts-phiw.4","type":"discovered-from","created_at":"2026-03-04T16:49:20.120454722+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-4cg","title":"Fix: raw OAuth error code from authorization server passed unsanitized to client (from security review)","description":"## File\napp/api/oauth/callback/route.ts, line 21\n\n## Problem\nThe `error` parameter received from the OAuth authorization server is forwarded directly to the client without sanitization:\n```ts\nconst error = searchParams.get('error');\n// ...\nredirectUrl.searchParams.set('auth_error', error);  // raw, unsanitized\n```\n\nWhile `error_description` is sanitized (truncated to 200 chars), the `error` code itself is not. RFC 6749 defines a fixed set of error codes (e.g. `access_denied`, `invalid_request`), but a malicious or misconfigured authorization server could return an arbitrary string in the `error` parameter.\n\nThis value is then read by AuthErrorToast.tsx (line 13) and displayed in a toast notification. If the error code contains HTML or script content, it could be rendered in the UI. Even without XSS (React escapes by default), an attacker-controlled error code could display misleading messages to users (e.g. `error=Your+account+has+been+suspended+contact+support@evil.com`).\n\n## Fix\nAllowlist the error code against known OAuth error codes before forwarding:\n```ts\nconst KNOWN_OAUTH_ERRORS = new Set([\n  'access_denied', 'invalid_request', 'unauthorized_client',\n  'unsupported_response_type', 'invalid_scope', 'server_error',\n  'temporarily_unavailable', 'interaction_required', 'login_required',\n  'consent_required', 'request_not_supported',\n]);\nconst safeError = KNOWN_OAUTH_ERRORS.has(error) ? error : 'authorization_failed';\nredirectUrl.searchParams.set('auth_error', safeError);\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:43:53.010398042+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:43:53.010398042+06:00","dependencies":[{"issue_id":"hypercerts-4cg","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.621819873+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-529","title":"Epic: Resolve CodeRabbit review for PR #9","description":"Resolve all open CodeRabbit inline review comments on PR #9. 14 comments across 13 files, grouped into 11 tasks. Skipping 1 false positive (lexicon version).","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-24T13:37:49.463035317+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:44:07.483516911+06:00","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-529.1","title":"Fix: await session.signOut() + safe error cast in logout route","description":"## Files\n- app/api/auth/logout/route.ts (modify)\n\n## What to do\n1. Line 11: Change `session.signOut();` to `await session.signOut();` — the signOut method revokes OAuth credentials server-side and must be awaited.\n2. Line 21: Replace `(e as Error).message` with `e instanceof Error ? e.message : String(e)` — the current cast yields 'undefined' for non-Error throws.\n\n## Don't\n- Change any other logic in the logout route\n- Modify the cookie deletion logic\n- Add try/catch around signOut (let it propagate to the existing catch block)\n\n## CodeRabbit refs\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844681978\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844681987","acceptance_criteria":"1. session.signOut() is awaited on line ~11. 2. Error message uses instanceof guard on line ~21. 3. tsc --noEmit passes. 4. No other lines changed.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T13:38:00.932081593+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:41:52.460798572+06:00","closed_at":"2026-02-24T13:41:52.460798572+06:00","close_reason":"8408ca4 fix: await session.signOut() + safe error cast in logout route","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.1","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:00.933629417+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.10","title":"Fix: make image upload trigger keyboard-accessible","description":"## Files\n- components/image-uploader.tsx (modify)\n\n## What to do\nLines 86-98: Replace the `\u003clabel\u003e` element (the upload trigger when no image is present) with a `\u003cbutton type=\"button\"\u003e`. The label is not focusable and keyboard users can't open the file picker.\n\nChange:\n- `\u003clabel onClick={...} className={...}\u003e` → `\u003cbutton type=\"button\" onClick={...} className={...}\u003e`\n- `\u003c/label\u003e` → `\u003c/button\u003e`\n\nThe button element is natively keyboard-accessible (focusable, responds to Enter/Space), so no additional key handlers are needed.\n\n## Don't\n- Change the inputRef or file input element\n- Modify the image preview section (when image IS present)\n- Change the Camera icon or Upload text\n- Add onKeyDown handlers (button handles this natively)\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682029","acceptance_criteria":"1. Upload trigger uses \u003cbutton type='button'\u003e not \u003clabel\u003e. 2. onClick still calls inputRef.current?.click(). 3. Element is keyboard-focusable. 4. tsc --noEmit passes. 5. Only the label→button swap changed.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-24T13:39:13.0113956+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:42:03.161816068+06:00","closed_at":"2026-02-24T13:42:03.161816068+06:00","close_reason":"5ff20c9 fix: make image upload trigger keyboard-accessible","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.10","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:39:13.012795063+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.11","title":"Fix: update ngrok URL to modern domain in DEVELOPMENT.md","description":"## Files\n- DEVELOPMENT.md (modify)\n\n## What to do\nLine 397: Replace `https://abc123.ngrok.io` with `https://abc123.ngrok-free.app` — the .ngrok.io domain is legacy and new accounts use .ngrok-free.app.\n\n## Don't\n- Change any other content in the ngrok section\n- Modify other troubleshooting sections\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682034","acceptance_criteria":"1. ngrok URL uses .ngrok-free.app domain. 2. No other content changed.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":5,"created_at":"2026-02-24T13:39:15.250345193+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:41:47.08527452+06:00","closed_at":"2026-02-24T13:41:47.08527452+06:00","close_reason":"b1d8972 fix: update ngrok URL to modern .ngrok-free.app domain","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.11","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:39:15.251928434+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.2","title":"Fix: $type checks in blob-display.tsx use wrong namespace","description":"## Files\n- components/blob-display.tsx (modify)\n\n## What to do\n1. Line 19: Change `\"app.certified.defs#uri\"` to `\"org.hypercerts.defs#uri\"` — the old namespace is incorrect.\n2. Line 24: Replace `[\"smallBlob\", \"largeBlob\", \"blob\"].includes(type || \"\")` with `[\"org.hypercerts.defs#smallBlob\", \"org.hypercerts.defs#largeBlob\"].includes(type || \"\")` — use fully namespaced $type values and remove non-existent \"blob\" type.\n\n## Don't\n- Change the type casts (keep $Typed\u003cOrgHypercertsDefs.Uri\u003e and $Typed\u003cOrgHypercertsDefs.SmallBlob\u003e)\n- Modify the JSX rendering logic\n- Change imports\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682000","acceptance_criteria":"1. URI check uses 'org.hypercerts.defs#uri'. 2. Blob check uses fully namespaced types and no plain 'blob'. 3. tsc --noEmit passes. 4. No other lines changed.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T13:38:07.639605741+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:42:16.572477351+06:00","closed_at":"2026-02-24T13:42:16.572477351+06:00","close_reason":"8408ca4 fix: use correct org.hypercerts.defs namespace in blob-display.tsx","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.2","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:07.64107673+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.3","title":"Fix: unsafe in operator on string workScope in base form","description":"## Files\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\nLines 63-74: The current code uses `\"\\\" in certInfo.workScope` which throws TypeError when workScope is a string (which it is per the lexicon type). Replace the entire workScopeStr/initialWorkScope block with:\n\n```ts\nconst initialWorkScope = certInfo?.workScope\n  ? certInfo.workScope.split(\",\").map((scope: string) =\u003e scope.trim())\n  : undefined;\n```\n\nThis removes the unnecessary union type check since workScope is always a string in the current schema.\n\n## Don't\n- Change the useState initialization that uses initialWorkScope\n- Modify the Hypercert import\n- Touch any other state initialization\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682018","acceptance_criteria":"1. No 'in' operator used on certInfo.workScope. 2. initialWorkScope is derived directly from string split. 3. tsc --noEmit passes. 4. Only lines 63-74 changed.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T13:38:14.004884503+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:42:50.592213273+06:00","closed_at":"2026-02-24T13:42:50.592213273+06:00","close_reason":"35f18d8 fix: replace unsafe 'in' operator on workScope with isWorkScopeString type guard","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.3","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:14.00645502+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.4","title":"Fix: validate JWK structure before .map() in jwks.json route","description":"## Files\n- app/jwks.json/route.ts (modify)\n\n## What to do\nAfter the JSON.parse try/catch block (around line 38-45), add a structural validation check before the .map() call:\n\n```ts\nif (\n  !privateKey ||\n  typeof privateKey !== \"object\" ||\n  !Array.isArray(privateKey.keys)\n) {\n  return NextResponse.json(\n    { error: \"ATPROTO_JWK_PRIVATE does not contain a valid JWK Set.\" },\n    { status: 500 },\n  );\n}\n```\n\nInsert this between the JSON parse catch block and the `const PRIVATE_KEY_FIELDS` line.\n\n## Don't\n- Change the existing JSON.parse error handling\n- Modify the key stripping logic\n- Change the response format\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844681996","acceptance_criteria":"1. Structural validation exists before .map() call. 2. Returns 500 JSON error if privateKey.keys is not an array. 3. tsc --noEmit passes. 4. Existing error paths unchanged.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T13:38:24.303467284+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:41:25.675804324+06:00","closed_at":"2026-02-24T13:41:25.675804324+06:00","close_reason":"b52766e Add JWK structure validation guard in jwks.json route","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.4","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:24.305323741+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.5","title":"Fix: guard contributors extraction against non-array values","description":"## Files\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\nLine 41: Change:\n```ts\nconst contributors = (record as any).contributors;\n```\nto:\n```ts\n// eslint-disable-next-line @typescript-eslint/no-explicit-any -- contributors field exists on record but not in HypercertClaim type yet\nconst contributors = Array.isArray((record as any).contributors) ? (record as any).contributors : undefined;\n```\n\nThis matches the defensive pattern used for workScope on line 39.\n\n## Don't\n- Change the eslint-disable comment (it's already there)\n- Modify how contributors is passed to HypercertContributorsSection\n- Touch any other lines\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682014","acceptance_criteria":"1. contributors uses Array.isArray guard. 2. Falls back to undefined (not empty array). 3. tsc --noEmit passes. 4. Only line 41 changed.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-24T13:38:29.503700479+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:41:20.020513526+06:00","closed_at":"2026-02-24T13:41:20.020513526+06:00","close_reason":"a2a7db6 guard contributors extraction against non-array values","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.5","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:29.505164424+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.6","title":"Fix: add develop branch to CI workflow triggers","description":"## Files\n- .github/workflows/ci.yml (modify)\n\n## What to do\nLines 3-7: Add 'develop' to both the push and pull_request branch lists:\n\n```yaml\non:\n  push:\n    branches: [main, develop]\n  pull_request:\n    branches: [main, develop]\n```\n\n## Don't\n- Change any other workflow configuration\n- Add additional branches beyond develop\n- Modify job steps\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844681975","acceptance_criteria":"1. Both push.branches and pull_request.branches include 'develop'. 2. YAML is valid. 3. No other workflow changes.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":5,"created_at":"2026-02-24T13:38:33.06231246+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:41:23.018084534+06:00","closed_at":"2026-02-24T13:41:23.018084534+06:00","close_reason":"f378fa7 Fix: add develop branch to CI workflow triggers","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.6","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:33.063933812+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.7","title":"Fix: deduplicate parseAtUri + filter unparseable AT URIs in contributor query","description":"## Files\n- queries/hypercerts/use-contributor-profiles-query.ts (modify)\n\n## What to do\n1. Line 57: Remove the local `parseAtUri` function and import `parseAtUri` from `@/lib/utils` instead. The local version lacks the 'at://' prefix validation that the lib version has.\n2. Line 78: Before creating useQueries entries, filter out contributors whose AT URIs fail to parse. Currently if parseAtUri returns undefined, the query will pass undefined did/collection/rkey to getContributorInformationRecord which will error.\n\nConcrete changes:\n- Add `import { parseAtUri } from \"@/lib/utils\";` to imports\n- Remove the local parseAtUri function (lines ~57-62)\n- In the useQueries array mapping (~line 78), filter: `identities.filter(id =\u003e id.needsResolution \u0026\u0026 id.identityRef \u0026\u0026 parseAtUri(id.identityRef.uri))`\n\n## Don't\n- Change the getContributorInformationRecord call signature\n- Modify the ContributorIdentity type\n- Change the query key structure\n\n## CodeRabbit refs\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682041\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682046","acceptance_criteria":"1. No local parseAtUri function exists in the file. 2. parseAtUri imported from @/lib/utils. 3. Contributors with unparseable URIs are filtered out before query creation. 4. tsc --noEmit passes.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-24T13:38:46.825830361+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:43:21.31343047+06:00","closed_at":"2026-02-24T13:43:21.31343047+06:00","close_reason":"01ee3ba Fix: deduplicate parseAtUri + filter unparseable AT URIs in contributor query","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-529.7","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:46.827437637+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.8","title":"Fix: safe error cast in add-attachment route","description":"## Files\n- app/api/certs/add-attachment/route.ts (modify)\n\n## What to do\nLine 143: Replace `(e as Error).message` with `e instanceof Error ? e.message : \"Unknown error\"` in the catch block. This matches the pattern used in other files.\n\nConcrete change:\n```ts\n} catch (e) {\n  console.error(\"Error in add-attachment API:\", e);\n  const message = e instanceof Error ? e.message : \"Unknown error\";\n  return NextResponse.json(\n    { error: `Failed to add attachment: ${message}` },\n    { status: 500 },\n  );\n}\n```\n\n## Don't\n- Change the console.error call\n- Modify the error prefix text\n- Touch any other catch blocks in the file\n\n## CodeRabbit ref\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844681990","acceptance_criteria":"1. Error message uses instanceof guard. 2. console.error still logs raw error. 3. tsc --noEmit passes. 4. Only the catch block changed.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":5,"created_at":"2026-02-24T13:38:52.034727629+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:41:17.887836627+06:00","closed_at":"2026-02-24T13:41:17.887836627+06:00","close_reason":"a2a7db6 fix: safe error cast in add-attachment route","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.8","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:52.037088509+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-529.9","title":"Fix: trim manual contributor inputs + guard nullish identities","description":"## Files\n- components/contributions-form.tsx (modify)\n- lib/contributor-utils.ts (modify)\n\n## What to do\n1. contributions-form.tsx line 70: In the manualContributors loop, trim before pushing:\n```ts\nfor (const uri of manualContributors) {\n  const trimmed = uri.trim();\n  if (trimmed !== \"\") mappedContributors.push(trimmed);\n}\n```\n\n2. lib/contributor-utils.ts line 98: In the else branch of the identity parsing, guard nullish values:\n```ts\n} else if (rawIdentity == null) {\n  identity = \"Unknown contributor\";\n} else {\n  identity = String(rawIdentity);\n}\n```\n\n## Don't\n- Change the isContributorIdentity or isStrongRef branches\n- Modify the ContributorIdentity type\n- Touch the contributors array (only manualContributors loop)\n\n## CodeRabbit refs\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682007\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/9#discussion_r2844682038","acceptance_criteria":"1. manualContributors are trimmed before push. 2. Nullish rawIdentity produces 'Unknown contributor'. 3. tsc --noEmit passes. 4. Only specified lines changed in each file.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-24T13:38:58.810705531+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T13:42:06.765896506+06:00","closed_at":"2026-02-24T13:42:06.765896506+06:00","close_reason":"e6fd34a fix: trim manual contributor inputs + guard nullish identities","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-529.9","depends_on_id":"hypercerts-529","type":"parent-child","created_at":"2026-02-24T13:38:58.812287299+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-52a","title":"Fix: displayContributors not memoized — causes useResolveContributorIdentities to re-run on every render (hypercert-contributors-section.tsx)","description":"## File\\ncomponents/hypercert-contributors-section.tsx line 33\\n\\n## Severity\\nMEDIUM — Performance / correctness\\n\\n## Problem\\nparseContributors(contributors) is called on every render without useMemo. Since parseContributors returns a new array reference each time, the resolvedContributors useMemo (line 40) has displayContributors as a dependency and will re-run on every render, even when contributors prop has not changed. More critically, useResolveContributorIdentities receives a new array reference on every render, which may cause useQueries to re-subscribe unnecessarily.\\n\\n## Evidence\\n```tsx\\n// Line 33 — new array on every render\\nconst displayContributors = parseContributors(contributors);\\n\\n// Line 40 — depends on displayContributors, so re-runs every render\\nconst resolvedContributors = useMemo(() =\u003e {\\n  return displayContributors.map(...);\\n}, [displayContributors, resolvedMap]); // displayContributors is always a new ref\\n```\\n\\n## Fix\\nMemoize displayContributors:\\n```tsx\\nconst displayContributors = useMemo(\\n  () =\u003e parseContributors(contributors),\\n  [contributors]\\n);\\n```\\n\\nNote: hypercerts-mrb.15 (profileMap rebuilt on every render) is a known issue. This is a related but distinct problem — the displayContributors array itself is not memoized.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:28.461376179+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:28.461376179+06:00"}
{"id":"hypercerts-5dq","title":"Fix: config.ts throws at module load time if env vars missing — crashes entire Next.js server","description":"## Problem\n`lib/config.ts` lines 311-327 validate required environment variables at module load time and throw if any are missing:\n\n```ts\nfor (const envVar of requiredEnvVars) {\n  if (!process.env[envVar]) {\n    throw new Error(`Missing required environment variable: ${envVar}`);\n  }\n}\n```\n\nAdditionally, `validateBaseUrl(baseUrl)` is called at lines 183-188 and also throws on invalid config.\n\nSince `lib/config.ts` is imported by `lib/hypercerts-sdk.ts` which is imported by nearly every server-side module, a missing env var will crash the entire Next.js server process at startup — not just the affected route. This is intentional for required vars, but the current list includes `REDIS_HOST`, `REDIS_PORT`, and `REDIS_PASSWORD` which are only needed for OAuth flows, not for read-only public pages.\n\nThe `config.redis` object (lines 240-244) uses the non-null assertion operator (`!`) on all three Redis env vars, which will produce `undefined` values at runtime if the validation is somehow bypassed (e.g. in test environments).\n\n## Fix\n1. Consider separating required-at-startup vars from required-at-use-time vars\n2. Replace `!` assertions on `config.redis` with explicit runtime checks at the point of use (in `lib/redis.ts`)\n3. Document clearly which vars are required for which features\n\n## Evidence\n- `lib/config.ts` lines 240-244: `!` assertions on Redis config\n- `lib/config.ts` lines 311-327: module-level throws\n- `lib/redis.ts` lines 6-7: already has fallbacks (`|| 'localhost'`, `|| '6379'`) that contradict the required-var check","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:30.326642141+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:30.326642141+06:00"}
{"id":"hypercerts-5jd","title":"Fix: missing sameSite attribute on user-did cookie set by ePDS callback (from hypercerts-dhz.6)","description":"Review of hypercerts-dhz.6 found: The user-did cookie set in app/api/oauth/callback/route.ts (line 140-145) is missing the sameSite attribute. The existing handle-based callback (app/api/auth/callback/route.ts line 12-17) also omits sameSite, but the ePDS callback is a cross-site redirect from the ePDS auth server (auth.\u003cpds-hostname\u003e), which means the browser may block the cookie if sameSite defaults to 'Strict'. Without sameSite: 'lax', the cookie may not be sent on the redirect back from the ePDS auth server, causing the user to appear not logged in even after a successful token exchange. Fix: add sameSite: 'lax' to the user-did cookie options in app/api/oauth/callback/route.ts, matching the sameSite: 'lax' already used on the epds-oauth-session cookie (line 92). Evidence: callback/route.ts lines 140-145 vs lines 88-94.","status":"closed","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:27:46.812138286+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:42.458571396+06:00","closed_at":"2026-02-24T17:12:42.458571396+06:00","close_reason":"Already fixed in dhz.12 (commit c323ec7). user-did cookie now has sameSite: 'lax' (callback/route.ts line 141).","dependencies":[{"issue_id":"hypercerts-5jd","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:27:52.136373687+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-5jk","title":"Fix: overly broad `img[alt*=\"Logo\"]` selector risks matching unintended images in atproto-branding.ts (from hypercerts-nyk.15)","description":"Review of hypercerts-nyk.15 found an overly broad CSS selector in lib/atproto-branding.ts.\n\nLines 94-95 and 120-123:\n```css\nimg[alt=\"Hypercerts Logo\"],\nimg[alt*=\"Logo\"] {   /* matches ANY image whose alt contains 'Logo' */\n  width: 200px !important;\n  height: 40px !important;\n  ...\n}\n\n@media (prefers-color-scheme: dark) {\n  img[alt=\"Hypercerts Logo\"],\n  img[alt*=\"Logo\"] {\n    filter: invert(1) !important;  /* inverts ALL such images */\n  }\n}\n```\n\nThe `img[alt*=\"Logo\"]` substring selector will match any image on the PDS OAuth pages whose alt text contains the word 'Logo' (case-sensitive). This could accidentally resize and invert partner logos, app icons, or any future image the PDS adds with 'Logo' in its alt text.\n\nThe intent is clearly to target only the Hypercerts app logo. The more specific `img[alt=\"Hypercerts Logo\"]` selector already covers the exact target.\n\nEvidence: lib/atproto-branding.ts lines 94-104 and 119-127.\n\nFix: Remove the `img[alt*=\"Logo\"]` fallback selector from both the base rule and the dark mode rule. Keep only `img[alt=\"Hypercerts Logo\"]`. If the PDS uses a different exact alt text, add that specific value instead of a substring match.","status":"open","priority":3,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-02-17T19:45:58.145044156+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:45:58.145044156+06:00","dependencies":[{"issue_id":"hypercerts-5jk","depends_on_id":"hypercerts-nyk.15","type":"discovered-from","created_at":"2026-02-17T19:46:01.668988638+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-5u7q","title":"Fix: null startDate/endDate in PUT /api/certs causes validation rejection (from hypercerts-fpyy.4)","description":"Review of hypercerts-fpyy.4 found a regression in app/api/certs/route.ts PUT handler.\n\n## Problem\nThe PUT handler supports clearing startDate/endDate by sending the string \"null\" as the form value (lines 231-234):\n```ts\nif (startDate === \"null\") updates.startDate = null;\nelse if (startDate !== null) updates.startDate = startDate;\n```\nThese null values are spread into the merged record:\n```ts\nconst record: OrgHypercertsClaimActivity.Record = { ...existing, ...updates, ... };\n```\nThen assertValidRecord is called. The ATProto lexicon validator rejects null for string fields unless the field is listed in the lexicon's 'nullable' array. The activity lexicon does NOT list startDate/endDate as nullable. The string validator returns: { success: false, error: 'must be a string' } for null values.\n\n## Impact\nAny PUT request that sends startDate=\"null\" or endDate=\"null\" to clear those fields will now receive a 400 validation error instead of succeeding. This is a regression — the feature worked before validation was added.\n\n## Fix\nBefore spreading updates into the record, convert null values to undefined (which the lexicon treats as field-absent):\n```ts\nif (startDate === \"null\") updates.startDate = undefined;\n// or: delete the key from the merged record after spreading\n```\nOr use delete on the merged record after spreading:\n```ts\nif (updates.startDate === null) delete record.startDate;\nif (updates.endDate === null) delete record.endDate;\n```\n\n## Evidence\n- Lexicon validator complex.js line 74: null is only allowed if field is in def.nullable array\n- Lexicon validator primitives.js string(): typeof null !== 'string' → returns error\n- activity.json: startDate/endDate have no 'nullable' designation","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T13:08:18.659255564+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T13:58:43.746286419+06:00","closed_at":"2026-03-04T13:58:43.746286419+06:00","close_reason":"0046ddb fix: convert null startDate/endDate to field-absent in PUT /api/certs","dependencies":[{"issue_id":"hypercerts-5u7q","depends_on_id":"hypercerts-fpyy.4","type":"discovered-from","created_at":"2026-03-04T13:08:18.713801599+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-5vs","title":"Fix: authenticate page logo width changed outside spec scope (from hypercerts-nyk.8)","description":"Review of hypercerts-nyk.8 found: commit 52ce32a changed the authenticate page logo width from 54px to 180px (desktop) and from 42px to 140px (mobile) in main.flex.flex-col.items-center \u003e h1.text-primary::before. The spec for hypercerts-nyk.8 explicitly states 'Don't: Change any color values or non-text-replacement CSS'. These logo width changes are out of scope for this task. Evidence: git show 52ce32a -- lib/atproto-branding.ts shows -width: 54px +width: 180px and -width: 42px +width: 140px in the authenticate page logo ::before block. These changes may be intentional improvements but were not part of the spec and violate the constraint. They should either be reverted or moved to a separate task with proper spec coverage.","status":"open","priority":3,"issue_type":"bug","owner":"kzoeps","created_at":"2026-02-17T17:01:02.24037314+06:00","created_by":"kzoeps","updated_at":"2026-02-17T17:01:02.24037314+06:00","dependencies":[{"issue_id":"hypercerts-5vs","depends_on_id":"hypercerts-nyk.8","type":"discovered-from","created_at":"2026-02-17T17:01:06.156873056+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-5w3","title":"Fix: missing aria-label on camera overlay buttons in image-uploader (from hypercerts-1hr.4)","description":"Review of hypercerts-1hr.4 found: two icon-only `\u003cbutton\u003e` elements in components/image-uploader.tsx have no accessible name, making them invisible to screen readers.\n\n**Evidence:**\n- Line 76–82: Banner camera-overlay button — `\u003cCamera\u003e` icon only, no aria-label, no visible text.\n- Line 106–112: Avatar camera-overlay button — `\u003cCamera\u003e` icon only, no aria-label, no visible text.\n\nThe task (hypercerts-1hr.4) correctly fixed the upload-trigger label→button swap and added aria-label='Upload image' to that button (line 89), but the two pre-existing camera overlay buttons were not addressed.\n\n**Fix:** Add `aria-label=\"Change image\"` (or similar) to both buttons at lines 76 and 106 of components/image-uploader.tsx.\n\n**WCAG reference:** 4.1.2 Name, Role, Value (Level A) — interactive controls must have an accessible name.","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-20T22:16:18.542082593+06:00","created_by":"kzoeps","updated_at":"2026-02-20T22:23:50.842012693+06:00","closed_at":"2026-02-20T22:23:50.842012693+06:00","close_reason":"78674a6 fix: add aria-label to camera overlay buttons for WCAG 4.1.2","dependencies":[{"issue_id":"hypercerts-5w3","depends_on_id":"hypercerts-1hr.4","type":"discovered-from","created_at":"2026-02-20T22:16:18.588801188+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-670","title":"Fix: NodeSavedSession.tokenSet.aud set to issuer instead of user PDS URL (from hypercerts-dhz)","description":"## Review finding from hypercerts-dhz (ePDS OAuth integration)\n\n**File:** app/api/oauth/callback/route.ts, line 123\n\n**Problem:**\nThe callback sets `aud: issuer` where `issuer = tokenEndpoint.replace('/oauth/token', '')` (the ePDS server origin). However, the ATProto SDK's `verifyIssuer()` resolves the user's DID via the PLC directory and returns `resolved.pds.href` — the user's actual PDS URL — as the `aud` value.\n\nThe SDK uses `tokenSet.aud` as the base URL for all authenticated API calls:\n```js\n// oauth-session.js line 91\nconst initialUrl = new URL(pathname, tokenSet.aud);\n```\n\nFor ePDS users, the issuer (ePDS server) and the user's PDS URL (from DID resolution) may differ. Setting `aud` to the issuer means all API calls will be routed to the wrong server, causing 404s or auth failures when the app tries to use the session.\n\n**Evidence:**\n- oauth-server-agent.js line 111: `const aud = await this.verifyIssuer(tokenResponse.sub)` where `verifyIssuer` returns `resolved.pds.href`\n- oauth-session.js line 91: `const initialUrl = new URL(pathname, tokenSet.aud)`\n- callback/route.ts line 123: `aud: issuer` (hardcoded to token endpoint origin)\n\n**Fix:**\nAfter token exchange, resolve the user's DID to get their actual PDS URL and use that as `aud`. The simplest approach is to call the PLC directory or use the ATProto identity resolver to get `resolved.pds.href` for the `sub` DID, then set `aud: resolvedPdsUrl`.\n\nAlternatively, for ePDS users where the PDS IS the issuer, verify this assumption holds by checking if `resolved.pds.href === issuer` and throw if not.","status":"closed","priority":1,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:28:19.639897609+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:28.876191619+06:00","closed_at":"2026-02-24T17:12:28.876191619+06:00","close_reason":"Validated as correct — for ePDS users, the PDS IS the issuer (ePDS is both authorization server and PDS). aud: issuer is the correct value. No code change needed.","dependencies":[{"issue_id":"hypercerts-670","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:28:24.330473896+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-6a6","title":"Fix: email and handle inputs have no accessible labels (from hypercerts-dhz.8)","description":"Review of hypercerts-dhz.8 found: Neither the handle InputGroupInput (login-dialog.tsx line 57-62) nor the email InputGroupInput (lines 97-103) have associated \u003clabel\u003e elements or aria-label attributes. The InputGroupAddon contains an icon (AtSignIcon / MailIcon) but icons are not accessible labels — screen readers will announce these inputs as unlabeled. The placeholder text ('kzoeps.bsky.social', 'user@example.com') is not a substitute for a label. Fix: add aria-label='ATProto handle' to the handle input and aria-label='Email address' to the email input, or add visible \u003clabel\u003e elements with htmlFor matching input id attributes. Evidence: login-dialog.tsx lines 57-62 and 97-103 — no id, no aria-label, no associated label element.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:28:05.537946486+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:28:05.537946486+06:00","dependencies":[{"issue_id":"hypercerts-6a6","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:28:09.686297453+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-6ge","title":"Epic: Resolve CodeRabbit review for PR #10","description":"Resolve all 5 open CodeRabbit inline review comments on PR #10. 5 comments across 4 files (lib/epds-session-cookie.ts, app/api/oauth/login/route.ts, app/api/oauth/callback/route.ts, lib/epds-config.ts, lib/epds-helpers.ts). Covers security (cookie flags), bug fixes (PAR error handling, cookie TTL, URL normalization), and error-handling (fetch timeout).","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-24T23:54:51.692045182+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:31:12.874726459+06:00","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-6ge.1","title":"Fix: DPoP cookie sent insecurely — enforce secure flag and strict SameSite","description":"## Files\n- lib/epds-session-cookie.ts (modify)\n\n## What to do\nCodeRabbit flagged: security issue at line 64.\n\n**Original comment:**\n\u003e Cookie with DPoP private key is sent insecurely in development — missing secure flag and uses weak SameSite policy. The dpopPrivateJwk (DPoP private key material) is stored unencrypted in the cookie. The implementation sets httpOnly: true but only enables the secure flag in production (secure: config.isProduction). In development, the cookie is sent as an insecure HTTP cookie, exposing the private key in plaintext on the network. Additionally, sameSite: lax provides weaker cross-site protection than sameSite: Strict.\n\n**Suggested fix:**\n1. Change `secure: config.isProduction` to `secure: true` so the cookie is always sent over HTTPS.\n2. Change `sameSite: \"lax\"` to `sameSite: \"strict\"` for stronger cross-site protection.\nThese changes should be in the cookie options object inside `encodeEpdsSessionCookie` or wherever the EPDS session cookie is set.\n\n**Context:**\nThe DPoP private key is sensitive cryptographic material. Sending it over plaintext HTTP (even in dev) is a security risk. The fix is a two-line change to cookie options.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't introduce new dependencies\n- Don't encrypt the cookie payload — just fix the flags as described","acceptance_criteria":"The cookie options for the EPDS session cookie have secure: true (not conditional on isProduction) and sameSite set to strict. No other code in the file is changed. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T23:55:05.902227261+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T23:57:24.857808419+06:00","closed_at":"2026-02-24T23:57:24.857808419+06:00","close_reason":"a16974d fix: enforce secure=true and sameSite=strict on EPDS session cookie","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-6ge.1","depends_on_id":"hypercerts-6ge","type":"parent-child","created_at":"2026-02-24T23:55:05.903713243+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-6ge.2","title":"Fix: PAR failure returns raw JSON — redirect to error page instead","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n\n## What to do\nCodeRabbit flagged: bug issue at line 63.\n\n**Original comment:**\n\u003e PAR failure returns raw JSON to the browser — should redirect to an error page instead. This is a GET endpoint that the browser navigates to directly (user clicks Login → browser GETs this route). If PAR fails, returning a JSON 502 response will display raw JSON text to the user. All other error paths in this handler (and the callback handler) redirect to /?error=auth_failed, so this path should be consistent.\n\n**Suggested fix:**\nIn the PAR failure branch (around lines 56-63), keep the existing error logging (console.error) but replace the `NextResponse.json(...)` return with:\n```ts\nreturn NextResponse.redirect(new URL(\"/?error=auth_failed\", config.baseUrl));\n```\nThis makes the PAR failure path consistent with all other error paths in the login and callback handlers.\n\n**Context:**\nThe login route is a browser-navigated GET endpoint. All other error branches redirect to /?error=auth_failed. This one inconsistently returns JSON.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify other error handling paths\n- Don't introduce new dependencies","acceptance_criteria":"When PAR fails (parResponse.ok is false), the handler redirects to /?error=auth_failed instead of returning NextResponse.json. The console.error log line is preserved. No other code in the file is changed. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T23:55:14.851907867+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T23:56:55.202026834+06:00","closed_at":"2026-02-24T23:56:55.202026834+06:00","close_reason":"ca2eeb9 Fix PAR failure to redirect to /?error=auth_failed instead of returning raw JSON","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-6ge.2","depends_on_id":"hypercerts-6ge","type":"parent-child","created_at":"2026-02-24T23:55:14.853494054+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-6ge.3","title":"Fix: user-did cookie maxAge may outlive session — align TTL or use session cookie","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\nCodeRabbit flagged: bug issue at line 144.\n\n**Original comment:**\n\u003e user-did cookie maxAge (1 week) may outlive the session. If the refresh token expires or the Redis session is evicted before 7 days, the user-did cookie will still be present, causing the app to think the user is logged in while the backend session is gone. Consider aligning the cookie TTL with the actual session lifetime, or implementing a server-side check that gracefully handles stale cookies.\n\n**Suggested fix:**\nThe simplest safe fix: remove the `maxAge` option from the `user-did` cookie `response.cookies.set` call (around line 138-144). This makes it a browser session cookie that expires when the browser closes, which is safer than a hardcoded 7-day TTL that can outlive the backend session.\n\nAlternatively, if there is a session TTL constant defined elsewhere (e.g., in Redis config or session store), use that value for maxAge instead of the hardcoded 7 days.\n\n**Context:**\nThe user-did cookie is set in the OAuth callback handler. It currently has maxAge: 60 * 60 * 24 * 7 (1 week). The backend session (Redis/refresh token) may expire sooner, leaving a stale cookie.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify the EPDS session cookie — only the user-did cookie\n- Don't introduce new dependencies\n- Don't change the cookie name or other cookie options (httpOnly, path, etc.) unless directly related to the TTL fix","acceptance_criteria":"The user-did cookie set in the OAuth callback route either has no maxAge (making it a session cookie) or uses a TTL value derived from the actual session/refresh-token lifetime instead of the hardcoded 7-day value. No other cookies or code in the file are changed. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T23:55:25.363667644+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T23:56:58.022731567+06:00","closed_at":"2026-02-24T23:56:58.022731567+06:00","close_reason":"f6874b2 fix: make user-did cookie a session cookie to prevent stale auth","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-6ge.3","depends_on_id":"hypercerts-6ge","type":"parent-child","created_at":"2026-02-24T23:55:25.365227428+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-6ge.4","title":"Fix: Trailing slash in epdsUrl causes double-slash in endpoint paths","description":"## Files\n- lib/epds-config.ts (modify)\n\n## What to do\nCodeRabbit flagged: bug issue at line 36.\n\n**Original comment:**\n\u003e Trailing-slash in epdsUrl would produce double-slash in endpoint paths. If NEXT_PUBLIC_EPDS_URL is configured with a trailing slash (e.g., https://pds-eu-west4.test.certified.app/), the derived endpoints become https://...//oauth/par. While most servers handle this, it is an easy source of subtle bugs.\n\n**Suggested fix:**\nAfter reading `config.epdsUrl`, strip any trailing slashes before constructing endpoint URLs:\n```ts\nconst normalizedUrl = epdsUrl.replace(/\\/+$/, \"\");\n```\nThen use `normalizedUrl` instead of `epdsUrl` when building `parEndpoint` and `tokenEndpoint`. The `authEndpoint` already uses the URL object so it is not affected.\n\n**Context:**\nThe function builds OAuth endpoint URLs by string concatenation: `${epdsUrl}/oauth/par`. If epdsUrl ends with /, the result has a double slash.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify the authEndpoint construction (it already uses URL object)\n- Don't introduce new dependencies","acceptance_criteria":"epdsUrl is normalized (trailing slashes stripped) before being used to construct parEndpoint and tokenEndpoint. A trailing-slash input like 'https://example.com/' produces 'https://example.com/oauth/par' (no double slash). No other code in the file is changed. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T23:55:33.812414909+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T23:57:00.371879176+06:00","closed_at":"2026-02-24T23:57:00.371879176+06:00","close_reason":"f6874b2 fix: strip trailing slashes from epdsUrl before building endpoints","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-6ge.4","depends_on_id":"hypercerts-6ge","type":"parent-child","created_at":"2026-02-24T23:55:33.814147554+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-6ge.5","title":"Fix: Add timeout to outbound fetch calls in fetchWithDpopRetry","description":"## Files\n- lib/epds-helpers.ts (modify)\n\n## What to do\nCodeRabbit flagged: error-handling issue at line 181.\n\n**Original comment:**\n\u003e No timeout on outbound fetch calls — risk of indefinite hangs. fetchWithDpopRetry makes up to two fetch calls to the ePDS server with no timeout or AbortSignal. If the ePDS server is slow or unresponsive, the request thread will hang indefinitely. In a Next.js API route context, this could exhaust the serverless function execution budget or block other requests.\n\n**Suggested fix:**\n1. Add a constant at the top of the file: `const EPDS_TIMEOUT_MS = 15_000;`\n2. Add `signal: AbortSignal.timeout(EPDS_TIMEOUT_MS)` to both fetch calls in `fetchWithDpopRetry` — the initial fetch and the retry fetch (inside the dpopNonce branch).\n\n**Context:**\nThe function makes up to 2 sequential fetch calls (initial + DPoP nonce retry). Both need the timeout signal. AbortSignal.timeout() is available in Node 18+ and all modern browsers.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify the DPoP proof creation logic\n- Don't introduce new dependencies\n- Don't change the function signature","acceptance_criteria":"Both fetch calls in fetchWithDpopRetry include a signal: AbortSignal.timeout(...) option with a reasonable timeout (e.g., 15 seconds). A constant for the timeout value is defined. No other code in the file is changed. TypeScript compiles without errors.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T23:55:41.043891672+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T23:57:21.614030243+06:00","closed_at":"2026-02-24T23:57:21.614030243+06:00","close_reason":"a16974d fix: add 15s timeout to fetch calls in fetchWithDpopRetry","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-6ge.5","depends_on_id":"hypercerts-6ge","type":"parent-child","created_at":"2026-02-24T23:55:41.045528167+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-6ge.6","title":"Fix: Harden cookie flags in callback route — align with login route security","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\nSeam issue from CodeRabbit PR #10 fix integration: The login route (app/api/oauth/login/route.ts) was hardened with secure: true and sameSite: \"strict\", but the callback route still uses secure: config.isProduction and sameSite: \"lax\" on both the user-did cookie (line 141-142) and the EPDS session deletion cookie (line 149-150).\n\n**Fix both cookie set calls in app/api/oauth/callback/route.ts:**\n1. Line 141: Change `secure: config.isProduction` to `secure: true`\n2. Line 142: Change `sameSite: 'lax'` to `sameSite: 'strict'`\n3. Line 149: Change `secure: config.isProduction` to `secure: true`\n4. Line 150: Change `sameSite: 'lax'` to `sameSite: 'strict'`\n\n## Don't\n- Don't change anything else in the file\n- Don't modify the user-did cookie maxAge (already fixed by hypercerts-6ge.3)\n- Don't introduce new dependencies","acceptance_criteria":"Both cookie set calls in app/api/oauth/callback/route.ts (user-did and EPDS session deletion) have secure: true (not conditional) and sameSite: strict. No other code in the file is changed. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T23:58:20.298812325+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T23:59:20.943314979+06:00","closed_at":"2026-02-24T23:59:20.943314979+06:00","close_reason":"1f09e81 Fix: Harden cookie flags in callback route — secure: true, sameSite: strict","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-6ge.6","depends_on_id":"hypercerts-6ge","type":"parent-child","created_at":"2026-02-24T23:58:20.300876155+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-6ge.7","title":"Encrypt DPoP private key in session cookie with AES-256-GCM","description":"## Files\n- lib/epds-session-cookie.ts (modify)\n\n## What to do\nCodeRabbit flagged: security issue at line 64 (comment r2848394158).\n\n**Original comment (remaining concern):**\n\u003e The dpopPrivateJwk (DPoP private key material) is stored unencrypted in the cookie. Cookie flags (secure, httpOnly, sameSite) were already fixed in hypercerts-6ge.1, but the payload itself is still plaintext base64url — any attacker who reads the cookie value (log leak, browser extension, XSS) gets the raw EC private key.\n\n**What to implement:**\nEncrypt the JSON payload inside `encodeEpdsSessionCookie` using AES-256-GCM before base64url encoding. Keep the outer HMAC signature as a second integrity layer.\n\n**Detailed spec:**\n\n1. Add a `deriveEncryptionKey(secret: string): Buffer` helper that derives a 32-byte AES key from `getSessionSecret()` using HKDF (`crypto.hkdfSync('sha256', secret, '', 'epds-session-cookie-enc', 32)`).\n\n2. In `encodeEpdsSessionCookie`:\n   - Serialize data to JSON string\n   - Generate a random 12-byte IV (`crypto.randomBytes(12)`)\n   - Encrypt JSON with AES-256-GCM using the derived key and IV\n   - Get the 16-byte auth tag from the cipher\n   - Concatenate: `iv (12) + ciphertext + authTag (16)`\n   - Base64url-encode the concatenated buffer → this is the `payload`\n   - Compute HMAC-SHA256 of the payload (existing logic) → `signature`\n   - Return `${payload}.${signature}` (same outer format)\n\n3. In `decodeEpdsSessionCookie`:\n   - Split on `.`, verify HMAC (existing logic)\n   - Base64url-decode the payload\n   - Extract IV (first 12 bytes), auth tag (last 16 bytes), ciphertext (middle)\n   - Decrypt with AES-256-GCM using derived key, IV, and auth tag\n   - Parse JSON → return `EpdsOAuthSessionData`\n   - If decryption fails (wrong key, tampered), throw 'Invalid session cookie'\n\n4. Do NOT change the `EpdsOAuthSessionData` interface.\n5. Do NOT change any cookie-setting code in other files — only `lib/epds-session-cookie.ts`.\n6. Do NOT add any npm dependencies — use only `node:crypto`.\n\n**Edge cases:**\n- Tampered ciphertext → GCM auth tag verification fails → throw\n- Tampered HMAC → outer check fails first → throw\n- Old unencrypted cookies → will fail to decrypt → throw (acceptable: cookie is short-lived, 10 min)\n\n## Don't\n- Don't change files other than lib/epds-session-cookie.ts\n- Don't change the EpdsOAuthSessionData interface\n- Don't add npm dependencies\n- Don't change cookie-setting code in login/route.ts or callback/route.ts\n- Don't remove the outer HMAC — keep it as a second integrity layer","acceptance_criteria":"1. encodeEpdsSessionCookie output cannot be decoded by simple base64url — the payload portion is AES-256-GCM encrypted. 2. Round-trip works: decodeEpdsSessionCookie(encodeEpdsSessionCookie(data)) returns the original EpdsOAuthSessionData for any valid input. 3. Flipping any byte in the payload portion of the cookie causes decodeEpdsSessionCookie to throw 'Invalid session cookie'. 4. No new npm dependencies — only node:crypto is used. 5. npm run build passes with zero errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-25T11:21:31.801461391+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T11:30:41.138344419+06:00","closed_at":"2026-02-25T11:30:41.138344419+06:00","close_reason":"37bf781 encrypt DPoP private key in session cookie with AES-256-GCM","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-6ge.7","depends_on_id":"hypercerts-6ge","type":"parent-child","created_at":"2026-02-25T11:21:31.803808317+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-6qx","title":"Fix: Loading skeleton sections have no aria-busy or role=status — screen readers get no loading feedback","description":"## Files\\n- components/detail-view-skeletons.tsx (all three skeleton components)\\n- components/hypercert-contributors-section.tsx lines 82-94\\n- components/hypercert-evaluations-section.tsx lines 75-79\\n- components/hypercert-evidence-section.tsx lines 80-85\\n- components/hypercert-measurements-section.tsx lines 84-89\\n\\n## Severity\\nMEDIUM — Accessibility\\n\\n## Problem\\nAll skeleton/loading states render purely visual placeholder elements with no ARIA attributes. Screen readers receive no indication that content is loading. WCAG 4.1.3 (Status Messages) requires that status messages (including loading states) be programmatically determinable.\\n\\n## Fix\\nAdd aria-busy and aria-label to loading containers:\\n```tsx\\n// In each section component, wrap the skeleton in:\\n{isLoading ? (\\n  \u003cdiv aria-busy=\"true\" aria-label=\"Loading...\" role=\"status\"\u003e\\n    \u003cEvidenceSkeleton /\u003e\\n  \u003c/div\u003e\\n) : null}\\n```\\n\\nFor the skeleton components themselves, add a visually-hidden text:\\n```tsx\\nexport function MeasurementsSectionSkeleton() {\\n  return (\\n    \u003cdiv role=\"status\" aria-label=\"Loading measurements\"\u003e\\n      \u003cspan className=\"sr-only\"\u003eLoading measurements...\u003c/span\u003e\\n      {/* existing skeleton markup */}\\n    \u003c/div\u003e\\n  );\\n}\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:18.969204306+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:18.969204306+06:00"}
{"id":"hypercerts-6r0","title":"Fix: user-did cookie missing sameSite attribute in ePDS callback (from hypercerts-dhz.6)","description":"Review of hypercerts-dhz found: In app/api/oauth/callback/route.ts (lines 140-145), the user-did cookie is set without a sameSite attribute:\n\n```typescript\nresponse.cookies.set('user-did', tokenData.sub, {\n  httpOnly: true,\n  secure: config.isProduction,\n  maxAge: 60 * 60 * 24 * 7,\n  path: '/',\n  // sameSite is MISSING\n});\n```\n\nThe existing handle-based callback (app/api/auth/callback/route.ts line 12) uses `cookies().set()` which defaults to 'lax', but the ePDS callback uses NextResponse.cookies.set() which defaults to 'strict' in some Next.js versions and undefined in others. This inconsistency means the user-did cookie may not be sent on top-level navigations from external sites (e.g. after the ePDS redirect back), causing the session to appear missing on the first page load after login.\n\nFix: add `sameSite: 'lax'` to match the existing auth callback behavior and the epds-oauth-session cookie (login/route.ts line 91).","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:25:55.83926122+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:44.139248904+06:00","closed_at":"2026-02-24T17:12:44.139248904+06:00","close_reason":"Already fixed in dhz.12 (commit c323ec7). user-did cookie now has sameSite: 'lax' (callback/route.ts line 141). Duplicate of hypercerts-5jd.","dependencies":[{"issue_id":"hypercerts-6r0","depends_on_id":"hypercerts-dhz.6","type":"discovered-from","created_at":"2026-02-24T16:26:01.725553226+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-6x5","title":"Fix: website field in profile update has no URL format validation","description":"## Problem\n`app/api/profile/update/route.ts` accepts a `website` field (line 18) and stores it in the user's profile without any URL format validation. A user can submit arbitrary strings like `javascript:alert(1)`, `not-a-url`, or excessively long strings as their website URL.\n\nWhile the profile update route does validate `pronouns` length (line 35-40), there is no equivalent validation for `website`.\n\n## Risk\n- If the website URL is rendered as an `\u003ca href\u003e` anywhere in the UI without sanitization, `javascript:` URIs could enable XSS\n- Malformed URLs stored in the ATProto record may cause downstream parsing errors\n\n## Fix\nAdd URL validation before storing the website field:\n```ts\nif (website) {\n  try {\n    const parsed = new URL(website);\n    if (!['http:', 'https:'].includes(parsed.protocol)) {\n      return NextResponse.json(\n        { error: 'Website must be an http or https URL' },\n        { status: 400 },\n      );\n    }\n  } catch {\n    return NextResponse.json(\n      { error: 'Website must be a valid URL' },\n      { status: 400 },\n    );\n  }\n}\n```\n\n## Evidence\n`app/api/profile/update/route.ts` lines 18, 35-40 — pronouns validated, website not validated","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:04.075507305+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:04.075507305+06:00"}
{"id":"hypercerts-73o","title":"Epic: Remove @hypercerts-org/sdk-core — replace all imports with @hypercerts-org/lexicon and local types","description":"## Why\nPhase 5 (final) of the SDK removal migration. Phases 1-4 replaced all runtime operations (auth, reads, simple writes, compound writes). Now we remove the SDK package entirely by replacing its type imports.\n\n## Goal\nAfter this epic:\n- Zero imports from @hypercerts-org/sdk-core anywhere in the codebase\n- @hypercerts-org/sdk-core removed from package.json\n- vendor/hypercerts-org-sdk-core-0.10.0-beta.8.tgz deleted\n- All types come from @hypercerts-org/lexicon (which exports the same namespaces) or local definitions\n- pnpm build succeeds\n\n## Type mapping strategy\nThe lexicon package (@hypercerts-org/lexicon) exports identical type namespaces:\n- OrgHypercertsClaimActivity, OrgHypercertsClaimAttachment, OrgHypercertsClaimContributionDetails\n- OrgHypercertsClaimEvaluation, OrgHypercertsClaimMeasurement, OrgHypercertsClaimRights\n- AppCertifiedLocation, OrgHypercertsDefs, ComAtprotoRepoStrongRef\n\nThree SDK-only types need local replacements:\n1. CreateHypercertResult — { hypercertUri, rightsUri, hypercertCid, rightsCid, locationUris? }\n2. CreateHypercertParams — complex interface used by forms (title, description, workScope, dates, rights, image, contributions, locations, etc.)\n3. UpdateResult — { uri, cid }\n\n## Scope\n- 6 files with direct @hypercerts-org/sdk-core imports\n- 1 file with inline import() from sdk-core\n- package.json dependency removal\n- vendor tarball deletion\n- pnpm install to update lockfile\n\n## Constraints\n- Must not change any runtime behavior — type-only changes\n- Must build (pnpm build) after all tasks\n- Keep @hypercerts-org/lexicon as the type source (already installed)","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-03T22:56:57.210629184+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:54:35.016067302+06:00","closed_at":"2026-03-04T11:54:35.016067302+06:00","close_reason":"81ad74b Phase 5 SDK removal complete — zero sdk-core references","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-73o.1","title":"Add local SDK-replacement types to lib/types.ts — CreateHypercertResult, CreateHypercertParams, UpdateResult","description":"## Files\n- lib/types.ts (modify)\n\n## What to do\nAdd three interfaces to lib/types.ts that replace SDK-only types not available in @hypercerts-org/lexicon. These are used by forms and API response types.\n\n### 1. UpdateResult\nSimple type returned by update/create operations:\n```ts\nexport interface UpdateResult {\n  uri: string;\n  cid: string;\n}\n```\n\n### 2. CreateHypercertResult\nReturned by POST /api/certs after creating a hypercert + rights:\n```ts\nexport interface CreateHypercertResult {\n  hypercertUri: string;\n  rightsUri: string;\n  hypercertCid: string;\n  rightsCid: string;\n  locationUris?: string[];\n}\n```\n\n### 3. CreateHypercertParams\nUsed by the create form to pass params to the API. This is the most complex one. Define it based on what `hypercerts-base-form.tsx` actually constructs (see getRecord() around line 154):\n```ts\nexport interface CreateHypercertParams {\n  title: string;\n  description: string;\n  shortDescription: string;\n  startDate: string;\n  endDate: string;\n  rights: {\n    name: string;\n    type: string;\n    description: string;\n  };\n  workScope?: string | { uri: string; cid: string };\n  image?: Blob;\n  contributions?: Array\u003c{\n    contributors: Array\u003cstring | { identity: string } | { uri: string; cid: string }\u003e;\n    contributionDetails: string | { role: string } | { uri: string; cid: string };\n    weight?: string;\n  }\u003e;\n  locations?: Array\u003cstring | Record\u003cstring, unknown\u003e\u003e;\n  shortDescriptionFacets?: unknown[];\n  descriptionFacets?: unknown[];\n}\n```\n\nAlso update the existing `import type { CreateHypercertResult } from \"@hypercerts-org/sdk-core\"` on line 7 — remove it since CreateHypercertResult is now defined locally in the same file.\n\n### Also update the existing SDK imports in this file\nReplace:\n```ts\nimport type { OrgHypercertsClaimActivity as HypercertRecord } from \"@hypercerts-org/sdk-core\";\nimport type { OrgHypercertsClaimContributionDetails as HypercertContribution } from \"@hypercerts-org/sdk-core\";\nimport type { OrgHypercertsClaimAttachment as HypercertEvidence } from \"@hypercerts-org/sdk-core\";\nimport type { OrgHypercertsClaimRights as HypercertRights } from \"@hypercerts-org/sdk-core\";\nimport type { AppCertifiedLocation as HypercertLocation } from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport type { OrgHypercertsClaimActivity as HypercertRecord } from \"@hypercerts-org/lexicon\";\nimport type { OrgHypercertsClaimContributionDetails as HypercertContribution } from \"@hypercerts-org/lexicon\";\nimport type { OrgHypercertsClaimAttachment as HypercertEvidence } from \"@hypercerts-org/lexicon\";\nimport type { OrgHypercertsClaimRights as HypercertRights } from \"@hypercerts-org/lexicon\";\nimport type { AppCertifiedLocation as HypercertLocation } from \"@hypercerts-org/lexicon\";\n```\n\n## Dont\n- Do NOT change any runtime code\n- Do NOT remove the Collections enum\n- Do NOT change the HypercertEvidenceData, HypercertRecordData, etc. type aliases — only change their import sources","acceptance_criteria":"1. lib/types.ts has zero imports from @hypercerts-org/sdk-core\n2. lib/types.ts exports CreateHypercertResult, CreateHypercertParams, and UpdateResult interfaces\n3. lib/types.ts imports record types from @hypercerts-org/lexicon instead\n4. pnpm tsc --noEmit passes (run from project root)","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-03T22:57:20.026902039+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:00:01.181829918+06:00","closed_at":"2026-03-04T11:00:01.181829918+06:00","close_reason":"622a477 Add local SDK-replacement types to lib/types.ts","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-73o.1","depends_on_id":"hypercerts-73o","type":"parent-child","created_at":"2026-03-03T22:57:20.028413488+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-73o.2","title":"Replace SDK imports in lib/api/types.ts with local types from lib/types.ts","description":"## Files\n- lib/api/types.ts (modify)\n\n## What to do\nReplace the SDK type imports with imports from lib/types.ts where the local replacements now live.\n\n### Current (lines 5-8):\n```ts\nimport type {\n  CreateHypercertResult,\n  UpdateResult,\n} from \"@hypercerts-org/sdk-core\";\n```\n\n### Replace with:\n```ts\nimport type {\n  CreateHypercertResult,\n  UpdateResult,\n} from \"@/lib/types\";\n```\n\nThe rest of the file stays exactly the same — CreateHypercertResponse and UpdateHypercertResponse type aliases will still work since they reference the same-shaped types.\n\n## Dont\n- Do NOT change any other types in this file\n- Do NOT change any runtime code\n- Do NOT add new types — only change the import source","acceptance_criteria":"1. lib/api/types.ts has zero imports from @hypercerts-org/sdk-core\n2. lib/api/types.ts imports CreateHypercertResult and UpdateResult from @/lib/types\n3. CreateHypercertResponse and UpdateHypercertResponse type aliases still exist unchanged\n4. pnpm tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-03T22:57:32.34130625+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:01:19.99283547+06:00","closed_at":"2026-03-04T11:01:19.99283547+06:00","close_reason":"55ea0e3 Replace sdk-core imports with local types in lib/api/types.ts","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-73o.2","depends_on_id":"hypercerts-73o","type":"parent-child","created_at":"2026-03-03T22:57:32.342533385+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.2","depends_on_id":"hypercerts-73o.1","type":"blocks","created_at":"2026-03-03T22:57:32.356849875+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-73o.3","title":"Replace SDK imports in lib/queries.ts and lib/utils.ts with @hypercerts-org/lexicon","description":"## Files\n- lib/queries.ts (modify)\n- lib/utils.ts (modify)\n\n## What to do\n\n### lib/queries.ts (lines 2-6)\nReplace:\n```ts\nimport { OrgHypercertsClaimActivity as HypercertClaim } from \"@hypercerts-org/sdk-core\";\nimport { OrgHypercertsClaimEvaluation as HypercertEvaluation } from \"@hypercerts-org/sdk-core\";\nimport { OrgHypercertsClaimContributionDetails as HypercertContribution } from \"@hypercerts-org/sdk-core\";\nimport { OrgHypercertsClaimAttachment as HypercertEvidence } from \"@hypercerts-org/sdk-core\";\nimport { AppCertifiedLocation as HypercertLocation } from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport { OrgHypercertsClaimActivity as HypercertClaim } from \"@hypercerts-org/lexicon\";\nimport { OrgHypercertsClaimEvaluation as HypercertEvaluation } from \"@hypercerts-org/lexicon\";\nimport { OrgHypercertsClaimContributionDetails as HypercertContribution } from \"@hypercerts-org/lexicon\";\nimport { OrgHypercertsClaimAttachment as HypercertEvidence } from \"@hypercerts-org/lexicon\";\nimport { AppCertifiedLocation as HypercertLocation } from \"@hypercerts-org/lexicon\";\n```\n\n### lib/utils.ts (lines 4-6)\nReplace:\n```ts\nimport { OrgHypercertsClaimActivity as Hypercert } from \"@hypercerts-org/sdk-core\";\nimport { OrgHypercertsClaimContributionDetails as Contribution } from \"@hypercerts-org/sdk-core\";\nimport { OrgHypercertsClaimEvaluation as Evaluation } from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport { OrgHypercertsClaimActivity as Hypercert } from \"@hypercerts-org/lexicon\";\nimport { OrgHypercertsClaimContributionDetails as Contribution } from \"@hypercerts-org/lexicon\";\nimport { OrgHypercertsClaimEvaluation as Evaluation } from \"@hypercerts-org/lexicon\";\n```\n\nThese are direct 1:1 replacements — the lexicon package exports identical type namespaces.\n\n## Dont\n- Do NOT change any runtime code\n- Do NOT change type aliases or how the types are used downstream\n- Do NOT touch any other files","acceptance_criteria":"1. lib/queries.ts has zero imports from @hypercerts-org/sdk-core\n2. lib/utils.ts has zero imports from @hypercerts-org/sdk-core\n3. Both files import from @hypercerts-org/lexicon instead\n4. pnpm tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-03T22:57:44.552162831+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:00:14.810373178+06:00","closed_at":"2026-03-04T11:00:14.810373178+06:00","close_reason":"a5d0e84 Replace sdk-core imports with lexicon in queries.ts and utils.ts","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-73o.3","depends_on_id":"hypercerts-73o","type":"parent-child","created_at":"2026-03-03T22:57:44.553686049+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-73o.4","title":"Replace SDK imports in lib/contributor-utils.ts with @hypercerts-org/lexicon","description":"## Files\n- lib/contributor-utils.ts (modify)\n\n## What to do\nReplace the SDK type imports with lexicon equivalents.\n\n### Current (lines 1-4):\n```ts\nimport type {\n  OrgHypercertsClaimActivity,\n  ComAtprotoRepoStrongRef,\n} from \"@hypercerts-org/sdk-core\";\n```\n\n### Replace with:\n```ts\nimport type {\n  OrgHypercertsClaimActivity,\n  ComAtprotoRepoStrongRef,\n} from \"@hypercerts-org/lexicon\";\n```\n\nThe lexicon package exports both `OrgHypercertsClaimActivity` and `ComAtprotoRepoStrongRef` namespaces with identical type shapes.\n\nThe rest of the file (type aliases on lines 6-9, type guards, parseContributor, parseContributors) stays exactly the same.\n\n## Dont\n- Do NOT change any runtime code or type guards\n- Do NOT change the Contributor, ContributorIdentity, ContributorRole, StrongRef type aliases\n- Do NOT change DisplayContributor interface","acceptance_criteria":"1. lib/contributor-utils.ts has zero imports from @hypercerts-org/sdk-core\n2. lib/contributor-utils.ts imports from @hypercerts-org/lexicon instead\n3. All type aliases (Contributor, ContributorIdentity, ContributorRole, StrongRef) still work\n4. pnpm tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-03T22:57:57.265704505+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T10:59:50.431636382+06:00","closed_at":"2026-03-04T10:59:50.431636382+06:00","close_reason":"ab0ad58 Replace sdk-core imports with lexicon in contributor-utils.ts","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-73o.4","depends_on_id":"hypercerts-73o","type":"parent-child","created_at":"2026-03-03T22:57:57.266989433+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-73o.5","title":"Replace SDK imports in component files — blob-display, hypercerts pages, form files","description":"## Files\n- components/blob-display.tsx (modify)\n- components/hypercerts-create-form.tsx (modify)\n- components/hypercerts-base-form.tsx (modify)\n- queries/hypercerts/use-create-hypercert-mutation.ts (modify)\n- lib/api/hypercerts.ts (modify)\n- app/hypercerts/page.tsx (modify)\n- app/hypercerts/[hypercertUri]/page.tsx (modify)\n- app/hypercerts/[hypercertUri]/edit/page.tsx (modify)\n\n## What to do\n\n### components/blob-display.tsx\nReplace line 3:\n```ts\nimport { OrgHypercertsDefs } from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport { OrgHypercertsDefs } from \"@hypercerts-org/lexicon\";\n```\n\n### components/hypercerts-create-form.tsx\nReplace lines 1-4:\n```ts\nimport type {\n  CreateHypercertParams,\n  CreateHypercertResult,\n} from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport type {\n  CreateHypercertParams,\n  CreateHypercertResult,\n} from \"@/lib/types\";\n```\n\n### components/hypercerts-base-form.tsx\nReplace line 12:\n```ts\nimport type { CreateHypercertParams } from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport type { CreateHypercertParams } from \"@/lib/types\";\n```\n\n### queries/hypercerts/use-create-hypercert-mutation.ts\nReplace line 6:\n```ts\nimport type { CreateHypercertParams } from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport type { CreateHypercertParams } from \"@/lib/types\";\n```\n\n### lib/api/hypercerts.ts\nReplace line 42 inline import:\n```ts\nparams: import(\"@hypercerts-org/sdk-core\").CreateHypercertParams,\n```\nWith:\n```ts\nparams: import(\"@/lib/types\").CreateHypercertParams,\n```\nNote: This is an inline import() type annotation, not a top-level import statement. The path alias @/ should work in type position.\n\n### app/hypercerts/page.tsx\nReplace line 18:\n```ts\nimport { OrgHypercertsDefs } from \"@hypercerts-org/sdk-core\";\n```\nWith:\n```ts\nimport { OrgHypercertsDefs } from \"@hypercerts-org/lexicon\";\n```\n\n### app/hypercerts/[hypercertUri]/page.tsx\nFind the import of OrgHypercertsDefs from sdk-core (around line 11) and replace with:\n```ts\nimport { OrgHypercertsDefs } from \"@hypercerts-org/lexicon\";\n```\n\n### app/hypercerts/[hypercertUri]/edit/page.tsx\nFind the import of OrgHypercertsDefs from sdk-core (around line 10) and replace with:\n```ts\nimport { OrgHypercertsDefs } from \"@hypercerts-org/lexicon\";\n```\n\n## Dont\n- Do NOT change any runtime code or component logic\n- Do NOT change how types are used — only change import sources\n- Do NOT modify the local lexicons/ directory types","acceptance_criteria":"1. Zero imports from @hypercerts-org/sdk-core in any of the 8 files listed\n2. All OrgHypercertsDefs imports come from @hypercerts-org/lexicon\n3. All CreateHypercertParams and CreateHypercertResult imports come from @/lib/types\n4. The inline import() in lib/api/hypercerts.ts references @/lib/types not sdk-core\n5. pnpm tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-03T22:58:14.029608942+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:02:58.136178536+06:00","closed_at":"2026-03-04T11:02:58.136178536+06:00","close_reason":"bece459 Replace SDK imports in component/page files","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-73o.5","depends_on_id":"hypercerts-73o","type":"parent-child","created_at":"2026-03-03T22:58:14.030883301+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.5","depends_on_id":"hypercerts-73o.1","type":"blocks","created_at":"2026-03-03T22:58:14.034141591+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-73o.6","title":"Remove @hypercerts-org/sdk-core from package.json, delete vendor tarball, run pnpm install","description":"## Files\n- package.json (modify)\n- vendor/hypercerts-org-sdk-core-0.10.0-beta.8.tgz (delete)\n- pnpm-lock.yaml (auto-updated by pnpm install)\n\n## What to do\n\n### Step 1: Remove from package.json\nRemove this line from the dependencies section (currently line 29):\n```json\n\"@hypercerts-org/sdk-core\": \"file:./vendor/hypercerts-org-sdk-core-0.10.0-beta.8.tgz\",\n```\n\n### Step 2: Delete the vendor tarball\n```bash\nrm vendor/hypercerts-org-sdk-core-0.10.0-beta.8.tgz\n```\n\nNote: Keep the vendor/ directory — it still contains `hypercerts-org-lexicon-0.10.0-beta.15.tgz`.\n\n### Step 3: Run pnpm install\n```bash\npnpm install\n```\nThis will update pnpm-lock.yaml to remove the sdk-core dependency tree.\n\n### Step 4: Verify no remaining references\nRun:\n```bash\ngrep -r \"@hypercerts-org/sdk-core\" --include=\"*.ts\" --include=\"*.tsx\" --include=\"*.json\" .\n```\nThis should return zero results (excluding node_modules).\n\n## Dont\n- Do NOT remove @hypercerts-org/lexicon from package.json — it is still needed\n- Do NOT delete vendor/hypercerts-org-lexicon-0.10.0-beta.15.tgz\n- Do NOT delete the vendor/ directory itself\n- Do NOT modify any .ts/.tsx files — those should already be migrated by prior tasks","acceptance_criteria":"1. @hypercerts-org/sdk-core is NOT in package.json dependencies\n2. vendor/hypercerts-org-sdk-core-0.10.0-beta.8.tgz does not exist\n3. vendor/hypercerts-org-lexicon-0.10.0-beta.15.tgz still exists\n4. pnpm install succeeds without errors\n5. grep -r \"@hypercerts-org/sdk-core\" --include=\"*.ts\" --include=\"*.tsx\" --include=\"*.json\" . (excluding node_modules) returns zero results\n6. pnpm build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-03T22:58:31.038494203+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:06:05.49887528+06:00","closed_at":"2026-03-04T11:06:05.49887528+06:00","close_reason":"cc8cf40 Remove @hypercerts-org/sdk-core from package.json, delete vendor tarball, run pnpm install","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-73o.6","depends_on_id":"hypercerts-73o","type":"parent-child","created_at":"2026-03-03T22:58:31.039788084+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.6","depends_on_id":"hypercerts-73o.1","type":"blocks","created_at":"2026-03-03T22:58:31.043319733+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.6","depends_on_id":"hypercerts-73o.2","type":"blocks","created_at":"2026-03-03T22:58:31.046544653+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.6","depends_on_id":"hypercerts-73o.3","type":"blocks","created_at":"2026-03-03T22:58:31.049838327+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.6","depends_on_id":"hypercerts-73o.4","type":"blocks","created_at":"2026-03-03T22:58:31.053170289+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.6","depends_on_id":"hypercerts-73o.5","type":"blocks","created_at":"2026-03-03T22:58:31.056550056+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-73o.7","title":"Verify full build passes after SDK removal — pnpm build with zero sdk-core references","description":"## Files\n- (no files to modify — verification only)\n\n## What to do\n\n### Step 1: Verify zero SDK references\nRun:\n```bash\ngrep -r \"@hypercerts-org/sdk-core\" --include=\"*.ts\" --include=\"*.tsx\" --include=\"*.json\" . | grep -v node_modules | grep -v .beads\n```\nMust return zero results.\n\n### Step 2: Run type check\n```bash\npnpm tsc --noEmit\n```\nMust pass with zero errors.\n\n### Step 3: Run build\n```bash\npnpm build\n```\nMust complete successfully.\n\n### Step 4: Verify vendor directory\n```bash\nls vendor/\n```\nShould show only: `hypercerts-org-lexicon-0.10.0-beta.15.tgz`\nThe sdk-core tarball should NOT be present.\n\n### Step 5: Verify package.json\n```bash\ngrep sdk-core package.json\n```\nMust return zero results.\n\n## Dont\n- Do NOT modify any files\n- This is a verification-only task","acceptance_criteria":"1. grep -r \"@hypercerts-org/sdk-core\" (excluding node_modules and .beads) returns zero results\n2. pnpm tsc --noEmit passes with zero errors\n3. pnpm build succeeds\n4. vendor/ contains only the lexicon tarball, not the sdk-core tarball\n5. package.json has no reference to sdk-core","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-03T22:58:43.490065938+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:07:22.148910897+06:00","closed_at":"2026-03-04T11:07:22.148910897+06:00","close_reason":"af3be5b verify: full build passes with zero sdk-core references","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-73o.7","depends_on_id":"hypercerts-73o","type":"parent-child","created_at":"2026-03-03T22:58:43.491557016+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-73o.7","depends_on_id":"hypercerts-73o.6","type":"blocks","created_at":"2026-03-03T22:58:43.49560981+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-73y","title":"Fix: ePDS OAuth state not deleted from Redis after successful callback — replay risk","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\nAfter a successful token exchange in the ePDS OAuth callback (line 138), the OAuth state (code_verifier + DPoP private JWK) is never deleted from Redis. The state was stored via `epdsStateStore.set(state, ...)` in the login route and retrieved via `epdsStateStore.get(state)` in the callback, but never cleaned up.\n\nThis means:\n1. Stale state entries accumulate in Redis forever\n2. Theoretically, if an attacker captures a state value, the stored code_verifier and DPoP key remain available (though the auth server would reject a replayed authorization code)\n\n### Fix:\nAfter successfully retrieving and using the state (after line 39), delete it:\n```ts\nconst { codeVerifier, dpopPrivateJwk } = oauthState;\n\n// Clean up: delete state from Redis to prevent replay and avoid stale entries\nawait epdsStateStore.del(state);\n```\n\nCheck if `RedisEpdsStateStore` has a `del` method. If not, add one following the same pattern as the existing `get`/`set` methods.\n\n## Don't\n- Change the login route state storage logic\n- Change the token exchange logic\n- Add TTL to the state store (that is a separate concern — see hypercerts-7vo)","acceptance_criteria":"1. After successful state retrieval in the callback, epdsStateStore.del(state) is called\n2. RedisEpdsStateStore has a del method (add if missing)\n3. The delete happens before the token exchange (so if token exchange fails, state is still consumed — prevents replay)\n4. npx tsc --noEmit passes","status":"open","priority":3,"issue_type":"bug","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-26T15:48:54.233248443+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T15:48:54.233248443+06:00","labels":["scope:trivial"]}
{"id":"hypercerts-795","title":"Fix: ePDS OAuth callback errors return JSON 502 instead of user-friendly redirect (from hypercerts-dhz.6)","description":"Review of hypercerts-dhz.6 found: app/api/oauth/callback/route.ts returns NextResponse.json({error: ...}, {status: 400/502}) for all error cases (missing params, invalid cookie, state mismatch, token exchange failure). This means users who encounter errors during the ePDS callback see a raw JSON error page in the browser — not a user-friendly message. By contrast, the existing handle-based auth callback (app/api/auth/callback/route.ts) also returns JSON on error, but the ePDS callback is a browser-facing redirect endpoint where users land after ePDS authentication. Fix: redirect to /?auth_error=\u003ccode\u003e (matching the pattern used by the existing jsu.1 fix for OAuth rejection) instead of returning JSON. The existing AuthErrorToast pattern (if present) would then display the error. At minimum, redirect to / with a query param so the user sees the login dialog again rather than a blank JSON page. Evidence: callback/route.ts lines 25-28, 33-37, 50-53, 58-62, 93-97 all return JSON.","status":"closed","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:27:23.649167986+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:13:11.99663979+06:00","closed_at":"2026-02-24T17:13:11.99663979+06:00","close_reason":"Already fixed in dhz.11 (commit b4f7ed9). All error paths in callback/route.ts now redirect to /?error=auth_failed instead of returning JSON. See lines 27, 34, 48, 54, 86, 102, 158.","dependencies":[{"issue_id":"hypercerts-795","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:27:33.546961694+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-7g9","title":"Fix: next.config.ts uses skipProxyUrlNormalize but proxy.ts is not wired as Next.js middleware","description":"Review of next.config.ts and proxy.ts found a naming/wiring inconsistency.\n\nEvidence:\n- next.config.ts line 8: 'skipProxyUrlNormalize: true'\n- proxy.ts exports a 'proxy()' function and a 'config' matcher — but Next.js middleware MUST be in a file named 'middleware.ts' at the project root\n- There is NO middleware.ts file in the project root (verified with find)\n- The .next/server/middleware-manifest.json shows: 'middleware': {} — NO middleware is actually running\n- The build output shows 'ƒ Proxy (Middleware)' label but this is just Next.js labeling the middleware runtime, not actual middleware code\n\nThis means the localhost→127.0.0.1 redirect in proxy.ts is NEVER executed. The proxy.ts file is dead code. The skipProxyUrlNormalize: true in next.config.ts is also unnecessary since there is no middleware.\n\nImpact: Users who access http://localhost:3000 will NOT be redirected to http://127.0.0.1:3000. The README and .env.example both say the app 'automatically redirects localhost to 127.0.0.1' — this is incorrect.\n\nSuggested fix:\nRename proxy.ts to middleware.ts at the project root. Next.js will automatically pick it up as middleware. The 'config' export with matcher '/:path*' is already correct middleware config format.\n\nOr alternatively: remove proxy.ts and the skipProxyUrlNormalize config, and update docs to say users must manually use 127.0.0.1 (no automatic redirect).","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:29.865765511+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:58.099828047+06:00"}
{"id":"hypercerts-7ke","title":"Add branded ePDS email template + client metadata branding fields","description":"## Files\n- public/email-template.html (create)\n- lib/config.ts (modify — buildClientMetadata function only)\n\n## What to do\n\n### 1. Create public/email-template.html\n\nCreate an HTML email template that ePDS will use for OTP emails. It must match the Hypercerts scaffold visual identity.\n\n**Design specs (derived from the app's globals.css and branding):**\n- Background: light warm gray (#f8f8fa — matches oklch(0.985 0.002 260))\n- Card: white (#ffffff) with subtle border (#e8e8ec — matches oklch(0.91 0.005 260))\n- Primary text: near-black (#1c1e21 — matches oklch(0.20 0.005 260) / create-accent)\n- Muted text: medium gray (#7c7c85 — matches oklch(0.50 0.01 260))\n- Accent/button: dark charcoal (#1c1e21 — the create-accent color)\n- Font stack: 'Outfit', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif (Outfit is the app's body font)\n- Logo: use {{logo_uri}} template variable (ePDS provides this from client metadata)\n- App name: use {{app_name}} template variable\n\n**Template structure:**\n```html\n\u003c!DOCTYPE html\u003e\n\u003chtml\u003e\n\u003chead\u003e\n  \u003cmeta charset=\"utf-8\"\u003e\n  \u003cmeta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\"\u003e\n  \u003ctitle\u003eYour verification code\u003c/title\u003e\n\u003c/head\u003e\n\u003cbody style=\"margin:0; padding:0; background-color:#f8f8fa; font-family:'Outfit',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;\"\u003e\n  \u003ctable width=\"100%\" cellpadding=\"0\" cellspacing=\"0\" style=\"background-color:#f8f8fa; padding:40px 20px;\"\u003e\n    \u003ctr\u003e\n      \u003ctd align=\"center\"\u003e\n        \u003ctable width=\"100%\" cellpadding=\"0\" cellspacing=\"0\" style=\"max-width:460px; background:#ffffff; border-radius:12px; border:1px solid #e8e8ec; overflow:hidden;\"\u003e\n          \u003c!-- Logo header --\u003e\n          \u003ctr\u003e\n            \u003ctd style=\"padding:32px 32px 0 32px; text-align:center;\"\u003e\n              \u003cimg src=\"{{logo_uri}}\" alt=\"{{app_name}}\" width=\"40\" height=\"44\" style=\"display:block; margin:0 auto;\"\u003e\n            \u003c/td\u003e\n          \u003c/tr\u003e\n          \u003c!-- Heading --\u003e\n          \u003ctr\u003e\n            \u003ctd style=\"padding:20px 32px 0 32px; text-align:center;\"\u003e\n              \u003ch1 style=\"margin:0; font-size:22px; font-weight:700; color:#1c1e21; letter-spacing:-0.02em;\"\u003eYour verification code\u003c/h1\u003e\n            \u003c/td\u003e\n          \u003c/tr\u003e\n          \u003c!-- Code --\u003e\n          \u003ctr\u003e\n            \u003ctd style=\"padding:24px 32px; text-align:center;\"\u003e\n              \u003cdiv style=\"display:inline-block; padding:16px 32px; background:#f8f8fa; border-radius:10px; border:1px solid #e8e8ec; letter-spacing:0.3em; font-size:32px; font-weight:700; color:#1c1e21; font-family:'Outfit',monospace;\"\u003e\n                {{code}}\n              \u003c/div\u003e\n            \u003c/td\u003e\n          \u003c/tr\u003e\n          \u003c!-- Instructions --\u003e\n          \u003ctr\u003e\n            \u003ctd style=\"padding:0 32px 32px 32px; text-align:center;\"\u003e\n              \u003cp style=\"margin:0 0 8px 0; font-size:14px; color:#7c7c85; line-height:1.5;\"\u003e\n                Enter this code to sign in to \u003cstrong style=\"color:#1c1e21;\"\u003e{{app_name}}\u003c/strong\u003e.\n              \u003c/p\u003e\n              \u003cp style=\"margin:0; font-size:13px; color:#7c7c85; line-height:1.5;\"\u003e\n                This code expires in 10 minutes. If you didn't request this, you can safely ignore this email.\n              \u003c/p\u003e\n            \u003c/td\u003e\n          \u003c/tr\u003e\n          {{#is_new_user}}\n          \u003c!-- New user welcome --\u003e\n          \u003ctr\u003e\n            \u003ctd style=\"padding:0 32px 32px 32px; text-align:center; border-top:1px solid #e8e8ec;\"\u003e\n              \u003cp style=\"margin:24px 0 0 0; font-size:14px; color:#7c7c85; line-height:1.5;\"\u003e\n                Welcome! A new account will be created for you automatically.\n              \u003c/p\u003e\n            \u003c/td\u003e\n          \u003c/tr\u003e\n          {{/is_new_user}}\n        \u003c/table\u003e\n        \u003c!-- Footer --\u003e\n        \u003ctable width=\"100%\" cellpadding=\"0\" cellspacing=\"0\" style=\"max-width:460px;\"\u003e\n          \u003ctr\u003e\n            \u003ctd style=\"padding:24px 32px; text-align:center;\"\u003e\n              \u003cp style=\"margin:0; font-size:12px; color:#a0a0a8; line-height:1.5;\"\u003e\n                Sent by {{app_name}} · Powered by Certified\n              \u003c/p\u003e\n            \u003c/td\u003e\n          \u003c/tr\u003e\n        \u003c/table\u003e\n      \u003c/td\u003e\n    \u003c/tr\u003e\n  \u003c/table\u003e\n\u003c/body\u003e\n\u003c/html\u003e\n```\n\n**Important email HTML constraints:**\n- Use only inline styles (no \u003cstyle\u003e blocks — many email clients strip them)\n- Use table-based layout (not flexbox/grid — email clients don't support them)\n- All images must use absolute URLs via template variables\n- Keep it simple — no background images, no CSS variables\n\n### 2. Modify buildClientMetadata() in lib/config.ts\n\nAdd these fields to BOTH the loopback and production return objects in buildClientMetadata():\n\n```typescript\nemail_template_uri: `${config.baseUrl}/email-template.html`,\nemail_subject_template: \"{{code}} — Your {{app_name}} verification code\",\nbrand_color: \"#1c1e21\",\nbackground_color: \"#f8f8fa\",\n```\n\nAdd them after the existing `logo_uri` field in both objects (loopback and production).\n\n## Don't\n- Don't modify any other files\n- Don't change existing fields in buildClientMetadata()\n- Don't use \u003cstyle\u003e blocks in the email HTML (inline styles only)\n- Don't use CSS variables, flexbox, or grid in the email HTML\n- Don't use external font imports (Google Fonts etc) in the email — the font-family fallback stack is sufficient","acceptance_criteria":"1. public/email-template.html exists and is valid HTML\n2. Template contains {{code}} placeholder (required by ePDS)\n3. Template contains {{app_name}} and {{logo_uri}} placeholders\n4. Template contains {{#is_new_user}}...{{/is_new_user}} conditional block\n5. Template uses only inline styles (no \u003cstyle\u003e blocks)\n6. Template uses table-based layout (no flexbox/grid)\n7. Colors match the Hypercerts scaffold theme: background #f8f8fa, primary text #1c1e21, muted text #7c7c85, border #e8e8ec\n8. buildClientMetadata() in lib/config.ts includes email_template_uri, email_subject_template, brand_color, background_color in BOTH loopback and production objects\n9. email_template_uri points to ${config.baseUrl}/email-template.html\n10. brand_color is \"#1c1e21\" and background_color is \"#f8f8fa\"\n11. No TypeScript errors: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-24T19:14:27.456232084+06:00","created_by":"kzoeps","updated_at":"2026-02-24T19:15:48.952224815+06:00","closed_at":"2026-02-24T19:15:48.952224815+06:00","close_reason":"9a8cb9d Created email template and added branding fields to client metadata","labels":["scope:small"]}
{"id":"hypercerts-7l58","title":"Epic: Fix review findings — type quality, data bugs, and cleanup","description":"## Why\nThree reviewers audited the Phase 2-5 migration. They found 1 data-loss bug, 2 behavioral bugs, and pervasive unsafe type casts. The root cause of most type issues is that getRecord() returns value: unknown and the codebase casts to Record\u003cstring, unknown\u003e instead of validating with lexicon isRecord() validators.\n\n## Goal\nAfter this epic:\n- Zero `as any` or `as unknown as T` double-casts\n- Zero `as Parameters\u003ctypeof fn\u003e[0]` casts\n- Proper typed profile interfaces replace `Record\u003cstring, unknown\u003e` for profile records\n- Lexicon validators used at data-fetching boundaries\n- Rights field name mismatch fixed (silent data loss bug)\n- Profile create/update condition fixed (409 bug)\n- Bsky profile page blob URLs go through CDN conversion\n- All exported functions have explicit return types\n- pnpm build passes\n\n## Constraints\n- Use lexicon isRecord() validators where available\n- Define local typed interfaces for profile records (certified + bsky)\n- Keep changes minimal — fix types, don't refactor logic\n- Must build after each task","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-04T11:22:36.376239486+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:54:34.957027855+06:00","closed_at":"2026-03-04T11:54:34.957027855+06:00","close_reason":"81ad74b All 9 review fix tasks completed — type quality, data bugs, cleanup","labels":["scope:medium"]}
{"id":"hypercerts-7l58.1","title":"Fix rights field name mismatch — form sends name/type/description but API expects rightsName/rightsType/rightsDescription","description":"## Files\n- lib/types.ts (modify)\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\n\n### Bug\nThe form sends `{ name, type, description }` in the rights JSON but the API route (app/api/certs/route.ts) reads `rightsName`, `rightsType`, `rightsDescription`. The lexicon (org.hypercerts.claim.rights) confirms the correct field names are `rightsName`, `rightsType`, `rightsDescription`. This means every hypercert created via the form has an empty rights record.\n\n### Fix lib/types.ts\nChange the `rights` field in `CreateHypercertParams` (lines 61-65) from:\n```ts\nrights: {\n  name: string;\n  type: string;\n  description: string;\n};\n```\nTo:\n```ts\nrights: {\n  rightsName: string;\n  rightsType: string;\n  rightsDescription: string;\n};\n```\n\n### Fix components/hypercerts-base-form.tsx\nThe form state uses `rights.name`, `rights.type`, `rights.description` internally (for the form inputs). The fix is at the serialization point — where the `getRecord()` function builds the `CreateHypercertParams` object (around line 189-196).\n\nChange:\n```ts\nrights: {\n  name: rights.name.trim(),\n  type: rights.type.trim(),\n  description: rights.description.trim(),\n},\n```\nTo:\n```ts\nrights: {\n  rightsName: rights.name.trim(),\n  rightsType: rights.type.trim(),\n  rightsDescription: rights.description.trim(),\n},\n```\n\nAlso update the validation check (around line 156-158) — it currently checks `rights.name`, `rights.type`, `rights.description` which are the internal form state fields, NOT the CreateHypercertParams fields. The validation should stay as-is since it validates the form state, not the params. Only the getRecord() output needs to change.\n\n## Dont\n- Do NOT change the form state variable names (rights.name, rights.type, rights.description are fine as internal state)\n- Do NOT change app/api/certs/route.ts — it already reads the correct field names\n- Do NOT change the lexicon types","acceptance_criteria":"1. CreateHypercertParams.rights uses rightsName, rightsType, rightsDescription\n2. hypercerts-base-form.tsx getRecord() outputs rightsName, rightsType, rightsDescription\n3. pnpm tsc --noEmit passes\n4. The form validation still checks the internal state fields (rights.name etc.)","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T11:23:41.930597888+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:27:17.998245234+06:00","closed_at":"2026-03-04T11:27:17.998245234+06:00","close_reason":"749bc57 fix rights field name mismatch in CreateHypercertParams and getRecord()","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7l58.1","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:23:41.932990175+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.2","title":"Fix profile create vs update condition — check existingProfile === null, not displayName","description":"## Files\n- app/api/profile/update/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n\n## What to do\n\n### Bug\nBoth profile update routes use `if (!existingProfile?.displayName)` to decide between `createRecord` and `putRecord`. A profile that exists but has no displayName will trigger `createRecord`, which fails with 409 Conflict because rkey \"self\" already exists.\n\n### Fix app/api/profile/update/route.ts\nAround line 56, change:\n```ts\nif (!existingProfile?.displayName) {\n```\nTo:\n```ts\nif (existingProfile === null) {\n```\n\n### Fix app/api/profile/bsky/update/route.ts\nAround line 47, change:\n```ts\nif (!existingProfile?.displayName) {\n```\nTo:\n```ts\nif (existingProfile === null) {\n```\n\n## Dont\n- Do NOT change any other logic in these routes\n- Do NOT change how existingProfile is fetched","acceptance_criteria":"1. Both routes use existingProfile === null to decide create vs update\n2. pnpm tsc --noEmit passes","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-04T11:23:52.659128068+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:27:11.594455317+06:00","closed_at":"2026-03-04T11:27:11.594455317+06:00","close_reason":"fb20608 Fix profile create vs update condition — check existingProfile === null","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7l58.2","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:23:52.660566114+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.3","title":"Fix bsky-profile page — add convertBlobUrlToCdn for avatar/banner URLs","description":"## Files\n- app/bsky-profile/page.tsx (modify)\n\n## What to do\nThe bsky profile page resolves BlobRef to URL via getBlobURL but does not pass the result through convertBlobUrlToCdn, unlike the certified profile page. For users on bsky.network PDSes, this means the raw XRPC getBlob URL is used instead of the CDN URL.\n\n### Fix\nImport convertBlobUrlToCdn and wrap the getBlobURL calls:\n\nChange lines ~36-42 from:\n```ts\nconst avatarUrl = getBlobURL(profile?.avatar as Parameters\u003ctypeof getBlobURL\u003e[0], repo.assertDid, pdsUrl) || \"\";\nconst bannerUrl = getBlobURL(profile?.banner as Parameters\u003ctypeof getBlobURL\u003e[0], repo.assertDid, pdsUrl) || \"\";\n```\nTo:\n```ts\nconst avatarUrl = convertBlobUrlToCdn(getBlobURL(profile?.avatar as Parameters\u003ctypeof getBlobURL\u003e[0], repo.assertDid, pdsUrl)) || \"\";\nconst bannerUrl = convertBlobUrlToCdn(getBlobURL(profile?.banner as Parameters\u003ctypeof getBlobURL\u003e[0], repo.assertDid, pdsUrl)) || \"\";\n```\n\nAdd `convertBlobUrlToCdn` to the import from `@/lib/utils`.\n\nNote: The `as Parameters\u003ctypeof getBlobURL\u003e[0]` cast will be cleaned up in a separate task. Just add the CDN conversion for now.\n\n## Dont\n- Do NOT change the type casts in this task — that is a separate task","acceptance_criteria":"1. avatarUrl and bannerUrl go through convertBlobUrlToCdn\n2. convertBlobUrlToCdn is imported from @/lib/utils\n3. pnpm tsc --noEmit passes","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-04T11:24:06.133724926+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:31:34.301488703+06:00","closed_at":"2026-03-04T11:31:34.301488703+06:00","close_reason":"a7ae431 Fix bsky-profile page: wrap getBlobURL with convertBlobUrlToCdn","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7l58.3","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:24:06.135442935+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-7l58.3","depends_on_id":"hypercerts-7l58.4","type":"blocks","created_at":"2026-03-04T11:26:15.948977228+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.4","title":"Define typed profile interfaces and eliminate unsafe casts on profile record fields","description":"## Files\n- lib/types.ts (modify)\n- providers/SignedInProvider.tsx (modify)\n- app/profile/page.tsx (modify)\n- app/bsky-profile/page.tsx (modify)\n- app/api/profile/update/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n- lib/create-actions.ts (modify — only getActiveProfileInfo function)\n\n## What to do\n\n### Root cause\nProfile records from getRecord() return `value: unknown`. The codebase casts to `Record\u003cstring, unknown\u003e` then casts individual fields with `as string | undefined` and `as Parameters\u003ctypeof getBlobURL\u003e[0]`. These are unsafe — no runtime validation.\n\n### Step 1: Define profile interfaces in lib/types.ts\nAdd these interfaces (the avatar/banner fields are BlobRef objects from the PDS, not strings):\n\n```ts\nimport { BlobRef } from \"@atproto/lexicon\";\n\n/** Shape of app.certified.actor.profile record value from getRecord() */\nexport interface CertifiedActorProfile {\n  displayName?: string;\n  description?: string;\n  pronouns?: string;\n  website?: string;\n  handle?: string;\n  avatar?: BlobRef;\n  banner?: BlobRef;\n  [k: string]: unknown;\n}\n\n/** Shape of app.bsky.actor.profile record value from getRecord() */\nexport interface BskyActorProfile {\n  displayName?: string;\n  description?: string;\n  avatar?: BlobRef;\n  banner?: BlobRef;\n  [k: string]: unknown;\n}\n```\n\nAdd a type guard:\n```ts\nexport function isRecordObject(v: unknown): v is Record\u003cstring, unknown\u003e {\n  return typeof v === \"object\" \u0026\u0026 v !== null \u0026\u0026 !Array.isArray(v);\n}\n```\n\n### Step 2: Replace casts in each file\n\n**providers/SignedInProvider.tsx:**\n- Import `CertifiedActorProfile` from `@/lib/types`\n- Replace `(profileResult?.data?.value as Record\u003cstring, unknown\u003e | null) ?? null` with:\n  ```ts\n  const profile = profileResult?.data?.value as CertifiedActorProfile | undefined;\n  ```\n- Replace `profile?.avatar as Parameters\u003ctypeof getBlobURL\u003e[0]` with just `profile?.avatar` (now typed as `BlobRef | undefined` which getBlobURL accepts)\n- Replace `profile?.handle as string | undefined` with just `profile?.handle`\n- Remove the `Parameters\u003ctypeof getBlobURL\u003e` import pattern\n\n**app/profile/page.tsx:**\n- Import `CertifiedActorProfile`\n- Same pattern — cast `value` to `CertifiedActorProfile | undefined` once, then access fields directly\n- Remove `as string | undefined` casts on displayName, description, pronouns, website\n- Remove `as Parameters\u003ctypeof getBlobURL\u003e[0]` casts on avatar, banner\n\n**app/bsky-profile/page.tsx:**\n- Import `BskyActorProfile`\n- Same pattern\n\n**app/api/profile/update/route.ts:**\n- Import `CertifiedActorProfile`\n- Cast `existingResult?.data?.value` to `CertifiedActorProfile | undefined` (around line 53)\n- Cast `updatedResult?.data?.value` to `CertifiedActorProfile | undefined` (around line 145)\n- Remove `as string | null | undefined` casts on displayName, description, pronouns, website\n- Remove `as Parameters\u003ctypeof getBlobURL\u003e[0]` casts on avatar, banner\n\n**app/api/profile/bsky/update/route.ts:**\n- Import `BskyActorProfile`\n- Same pattern\n\n**lib/create-actions.ts (getActiveProfileInfo only):**\n- Import `CertifiedActorProfile`\n- Cast `profileResult?.data?.value` to `CertifiedActorProfile | undefined` (around line 30)\n- Remove `as string | undefined` casts on displayName, handle (around lines 37-40)\n\n## Dont\n- Do NOT change any runtime logic — only type annotations\n- Do NOT use `as any` or `as unknown as T`\n- Do NOT change how getRecord() is called\n- Do NOT touch other functions in create-actions.ts besides getActiveProfileInfo","acceptance_criteria":"1. CertifiedActorProfile and BskyActorProfile interfaces exist in lib/types.ts\n2. Zero `as Parameters\u003ctypeof getBlobURL\u003e[0]` casts remain in the codebase\n3. Zero `as string | undefined` casts on profile fields (displayName, description, etc.) in the listed files\n4. Profile fields are accessed directly from typed interfaces without casts\n5. pnpm tsc --noEmit passes","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-03-04T11:24:30.591878787+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:30:12.730607331+06:00","closed_at":"2026-03-04T11:30:12.730607331+06:00","close_reason":"81df36b Define typed profile interfaces and eliminate unsafe casts","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-7l58.4","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:24:30.593324017+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.5","title":"Eliminate as any and as unknown as T double-casts — use lexicon validators for hypercert records","description":"## Files\n- app/hypercerts/page.tsx (modify)\n- app/hypercerts/[hypercertUri]/page.tsx (modify)\n- app/hypercerts/[hypercertUri]/edit/page.tsx (modify)\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\n\n### Root cause\nHypercert records from getRecord()/listRecords() return `value: unknown`. The codebase uses `as any` and `as unknown as HypercertClaim` to bypass the type system. The lexicon package provides `OrgHypercertsClaimActivity.isRecord()` validator that should be used instead.\n\n### Fix app/hypercerts/page.tsx\nAround lines 46-48, replace:\n```ts\nconst records = hypercertsResult.data.records.map((r) =\u003e ({\n  uri: r.uri,\n  // eslint-disable-next-line @typescript-eslint/no-explicit-any\n  record: r.value as any,\n}));\n```\nWith:\n```ts\nimport { OrgHypercertsClaimActivity } from \"@hypercerts-org/lexicon\";\n\nconst records = hypercertsResult.data.records\n  .filter((r) =\u003e OrgHypercertsClaimActivity.isRecord(r.value))\n  .map((r) =\u003e ({\n    uri: r.uri,\n    record: r.value as OrgHypercertsClaimActivity.Record,\n  }));\n```\nNote: After the `isRecord()` filter, the cast to `OrgHypercertsClaimActivity.Record` is safe because the validator confirmed the shape. TypeScript may not narrow through `.filter()` automatically, so the cast is acceptable here.\n\nAlso remove the eslint-disable comment.\n\n### Fix app/hypercerts/[hypercertUri]/page.tsx\nAround line 225, replace:\n```ts\nrecord={certWithoutImage as unknown as HypercertClaim}\n```\nWith a properly typed variable. The `certResult.data.value` should be validated with `OrgHypercertsClaimActivity.isRecord()` when first fetched (around line 46). Then `certWithoutImage` will already be typed correctly and no cast is needed.\n\nSpecifically:\n1. After fetching the record (around line 46), validate:\n```ts\nconst rawValue = certResult.data.value;\nif (!OrgHypercertsClaimActivity.isRecord(rawValue)) {\n  notFound();\n}\nconst certRecord = rawValue; // now typed as OrgHypercertsClaimActivity.Record\n```\n2. Then destructure image and pass the rest:\n```ts\nconst { image, ...certWithoutImage } = certRecord;\n```\n3. The component prop should accept `OrgHypercertsClaimActivity.Record` (or the existing HypercertClaim alias).\n\nAlso remove the `console.log(imageUri)` on line 198 — this is debug logging that leaks blob URLs in production.\n\n### Fix app/hypercerts/[hypercertUri]/edit/page.tsx\nSame pattern as the detail page — validate with isRecord() at fetch time, remove the double-cast around line 152.\n\n### Fix components/hypercert-detail-view.tsx\nAround lines 59-63, replace:\n```ts\n// eslint-disable-next-line @typescript-eslint/no-explicit-any -- contributors field exists on record but not in HypercertClaim type yet\nconst rawContributors = (record as any).contributors;\n```\nWith:\n```ts\nconst rawContributors = \"contributors\" in record ? (record as { contributors?: unknown[] }).contributors : undefined;\n```\nOr better — the lexicon type OrgHypercertsClaimActivity.Main already has `contributors` as an optional field. If the component prop type is `OrgHypercertsClaimActivity.Record`, then `record.contributors` should work directly. Check the lexicon type first.\n\nRemove the eslint-disable comment.\n\n## Dont\n- Do NOT use `as any` or `as unknown as T`\n- Do NOT change component interfaces unless needed for the type fix\n- Do NOT change how records are fetched — only how they are typed after fetching","acceptance_criteria":"1. Zero `as any` casts in the listed files\n2. Zero `as unknown as HypercertClaim` double-casts\n3. Zero eslint-disable comments for @typescript-eslint/no-explicit-any in the listed files\n4. OrgHypercertsClaimActivity.isRecord() used to validate records at fetch boundaries\n5. console.log(imageUri) removed from [hypercertUri]/page.tsx\n6. pnpm tsc --noEmit passes","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-03-04T11:24:56.414316651+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:30:20.341631396+06:00","closed_at":"2026-03-04T11:30:20.341631396+06:00","close_reason":"33dc160 Use isRecord() validators instead of as any / double-casts for hypercert records","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-7l58.5","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:24:56.415742335+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.6","title":"Fix uploadContentBlob return type and getBlobURL dead code branch","description":"## Files\n- lib/atproto-writes.ts (modify)\n- lib/utils.ts (modify)\n\n## What to do\n\n### Fix 1: uploadContentBlob return type (lib/atproto-writes.ts)\nAround lines 98-106, change the return type from `Promise\u003cunknown\u003e` to the actual type:\n\n```ts\nimport { BlobRef } from \"@atproto/lexicon\";\n\nexport async function uploadContentBlob(\n  agent: Agent,\n  file: File | Blob,\n): Promise\u003cBlobRef\u003e {\n  const blob =\n    file instanceof File ? new Blob([file], { type: file.type }) : file;\n  const result = await agent.com.atproto.repo.uploadBlob(blob);\n  return result.data.blob;\n}\n```\n\nCheck what `result.data.blob` actually returns from the ATProto SDK. If it is already typed as `BlobRef`, the return type annotation is straightforward. If it returns a different type, use that type instead.\n\n### Fix 2: Remove dead code branch in getBlobURL (lib/utils.ts)\nAround line 25, remove this dead-code branch:\n```ts\nif (blobRef \u0026\u0026 \"$type\" in blobRef \u0026\u0026 blobRef.$type === \"string\") {\n  return blobRef.$type;\n}\n```\n\nThis checks if `$type` equals the literal string `\"string\"` — no ATProto record uses `$type: \"string\"`. If hit, it would return the string `\"string\"` as a URL, which is broken. The first guard (`typeof blobRef === \"string\"`) already handles actual string inputs.\n\nAlso remove the `{ $type: string }` from the function signature since that branch is gone:\n```ts\nexport function getBlobURL(\n  blobRef: BlobRef | string | undefined,\n  did?: string,\n  pdsUrl?: string,\n): string | undefined {\n```\n\nWait — check if any callers pass `{ $type: string }` objects. If `blob-utils.ts` line 45 passes `obj as BlobRef | { $type: string }`, that caller needs to be updated too. Check all callers of getBlobURL before removing the union member.\n\n## Dont\n- Do NOT change any callers of uploadContentBlob (they should just work with the narrower return type)\n- Do NOT change the logic of getBlobURL — only remove the dead branch and tighten the signature if safe","acceptance_criteria":"1. uploadContentBlob has an explicit return type (not Promise\u003cunknown\u003e)\n2. The dead $type === \"string\" branch is removed from getBlobURL\n3. pnpm tsc --noEmit passes\n4. All callers of getBlobURL still compile","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T11:25:15.233471428+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:28:55.048404242+06:00","closed_at":"2026-03-04T11:28:55.048404242+06:00","close_reason":"6669797 fix: uploadContentBlob return type and getBlobURL dead code branch","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-7l58.6","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:25:15.234985728+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.7","title":"Add safe formData string extraction helper and fix formData.get() as string casts","description":"## Files\n- lib/utils.ts (modify) or a new lib/form-utils.ts (create)\n- app/api/certs/route.ts (modify)\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n\n## What to do\n\n### Problem\n`formData.get()` returns `FormDataEntryValue | null` where `FormDataEntryValue = string | File`. Casting to `string | null` is unsafe — if a File is submitted for a field expected to be a string, the cast succeeds silently.\n\n### Step 1: Add helper\nAdd a helper function (in lib/utils.ts or a new lib/form-utils.ts):\n```ts\n/** Safely extract a string field from FormData, returning null if the value is a File */\nexport function getStringField(data: FormData, key: string): string | null {\n  const value = data.get(key);\n  return typeof value === \"string\" ? value : null;\n}\n```\n\n### Step 2: Replace casts in API routes\nIn each route, replace patterns like:\n```ts\nconst title = formData.get(\"title\") as string | null;\n```\nWith:\n```ts\nconst title = getStringField(formData, \"title\");\n```\n\nLeave `formData.get(\"image\") as File | null` casts alone — those are intentionally getting File values.\n\nScan all three API route files for `formData.get(...) as string` patterns and replace them.\n\n## Dont\n- Do NOT change File-type field extractions (image, avatar, banner, content, location when File)\n- Do NOT change the validation logic after extraction","acceptance_criteria":"1. A getStringField helper exists and is used\n2. Zero `formData.get(...) as string | null` casts remain in the listed files (File casts are OK)\n3. pnpm tsc --noEmit passes","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T11:25:29.384536377+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:28:57.307047721+06:00","closed_at":"2026-03-04T11:28:57.307047721+06:00","close_reason":"6669797 Add getStringField helper and replace unsafe formData string casts","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-7l58.7","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:25:29.386691615+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.8","title":"Use lexicon types for ATProto record construction instead of Record\u003cstring, unknown\u003e","description":"## Files\n- app/api/certs/route.ts (modify)\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n- lib/create-actions.ts (modify)\n- lib/atproto-writes.ts (modify)\n\n## What to do\n\n### Problem\nWhen building ATProto records to pass to createRecord/putRecord, the codebase uses `Record\u003cstring, unknown\u003e` instead of the lexicon types. This means the compiler cannot catch typos in field names, missing required fields, or wrong field types.\n\n### Fix\nImport the lexicon record types and use them when constructing records. The lexicon exports typed interfaces for all collections:\n\n- `OrgHypercertsClaimActivity.Record` — hypercert claim\n- `OrgHypercertsClaimRights.Record` — rights\n- `OrgHypercertsClaimEvaluation.Record` — evaluation\n- `OrgHypercertsClaimMeasurement.Record` — measurement\n- `OrgHypercertsClaimContributionDetails.Record` — contribution details\n- `OrgHypercertsClaimAttachment.Record` — attachment/evidence\n- `AppCertifiedLocation.Record` — location\n\nFor each file, find `Record\u003cstring, unknown\u003e` objects that are passed to `createRecord({ record: ... })` or `putRecord({ record: ... })` and replace with the appropriate lexicon type.\n\nExample — in app/api/certs/route.ts, the rights record (lines 93-105):\n```ts\n// Before:\nconst rightsRecord: Record\u003cstring, unknown\u003e = {\n  $type: \"org.hypercerts.claim.rights\",\n  createdAt: new Date().toISOString(),\n};\n\n// After:\nimport { OrgHypercertsClaimRights } from \"@hypercerts-org/lexicon\";\n\nconst rightsRecord: OrgHypercertsClaimRights.Record = {\n  $type: \"org.hypercerts.claim.rights\",\n  rightsName: hypercertParams.rights?.rightsName ?? \"\",\n  rightsType: hypercertParams.rights?.rightsType ?? \"\",\n  rightsDescription: hypercertParams.rights?.rightsDescription ?? \"\",\n  createdAt: new Date().toISOString(),\n};\n```\n\nNote: The lexicon Record types have required fields. If the current code conditionally adds fields, you may need to provide defaults or make the construction conditional. Check each lexicon type to see which fields are required vs optional.\n\nIMPORTANT: The `createRecord` and `putRecord` methods accept `record: unknown` in their input types, so the typed record objects will be accepted without issue.\n\n## Dont\n- Do NOT change runtime behavior — the records should contain the same data\n- Do NOT change how records are passed to createRecord/putRecord\n- If a lexicon type has required fields that the current code does not always provide, add sensible defaults rather than making the type partial","acceptance_criteria":"1. Zero Record\u003cstring, unknown\u003e used for constructing ATProto records that are passed to createRecord/putRecord\n2. Lexicon types imported and used for record construction\n3. pnpm tsc --noEmit passes\n4. pnpm build passes","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-04T11:25:49.856465504+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:38:40.296614621+06:00","closed_at":"2026-03-04T11:38:40.296614621+06:00","close_reason":"87cccee Use lexicon types for ATProto record construction","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-7l58.8","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:25:49.858460052+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-7l58.8","depends_on_id":"hypercerts-7l58.1","type":"blocks","created_at":"2026-03-04T11:26:15.807937865+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7l58.9","title":"Add explicit return types to exported server actions and fix JSON.parse returning any","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\n\n### Problem 1: Missing return types\nAll exported server actions in lib/create-actions.ts lack explicit return type annotations. For server actions (used across the client/server boundary), the return type is part of the public API contract.\n\nAdd explicit return types to all exported functions:\n- `getActiveProfileInfo` — returns profile info or null\n- `switchActiveProfile` — returns void\n- `logout` — returns void\n- `addContribution` — returns the updated record data\n- `addEvaluation` — returns the created record data\n- `addMeasurement` — returns the created record data\n- `getMeasurementRecord` — returns record data or throws\n- `getEvaluationRecord` — returns record data or throws\n- `getEvidenceRecord` — returns record data or throws\n- `getContributorInformationRecord` — returns record data or throws\n- `deleteHypercert` — returns void\n- `updateMeasurement` — returns the updated record data\n- `deleteRecord` — returns void\n\nRead each function body to determine the actual return type. Define interfaces as needed.\n\n### Problem 2: JSON.parse(JSON.stringify(data)) returns any\nLines 313, 338, 363, 387 use `JSON.parse(JSON.stringify(data))` which returns `any`. This is used to serialize CID objects for Next.js server action serialization.\n\nAdd a type assertion after JSON.parse:\n```ts\nreturn JSON.parse(JSON.stringify(data)) as { uri: string; cid: string; value: Record\u003cstring, unknown\u003e };\n```\n\nOr define a `SerializedRecord` interface and use it consistently.\n\n## Dont\n- Do NOT change runtime behavior\n- Do NOT remove the JSON.parse(JSON.stringify()) calls — they are needed for Next.js serialization\n- Do NOT change function signatures (parameter types) — only add return types","acceptance_criteria":"1. All exported functions in lib/create-actions.ts have explicit return type annotations\n2. Zero inferred `any` return types from JSON.parse\n3. pnpm tsc --noEmit passes","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T11:26:05.24408192+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:43:14.171774572+06:00","closed_at":"2026-03-04T11:43:14.171774572+06:00","close_reason":"182cbe7 Add explicit return types to server actions and fix JSON.parse any","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-7l58.9","depends_on_id":"hypercerts-7l58","type":"parent-child","created_at":"2026-03-04T11:26:05.245368405+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-7l58.9","depends_on_id":"hypercerts-7l58.8","type":"blocks","created_at":"2026-03-04T11:26:15.877005472+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7q1","title":"Epic: Resolve CodeRabbit review for PR #12","description":"Resolve 4 open CodeRabbit inline review comments on PR #12. Covers: TTL on RedisSessionStore, handle validation in login route, UserAvatar empty-string guard, and stale comment/README path fixes. 4 tasks across 4 files.","status":"open","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-02-27T13:58:44.749076062+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T13:58:44.749076062+06:00","labels":["scope:small"]}
{"id":"hypercerts-7q1.1","title":"Add TTL to RedisSessionStore.set()","description":"## Files\n- lib/redis-state-store.ts (modify)\n\n## What to do\nIn `RedisSessionStore.set()` (line 44-46), the `redisClient.set()` call stores sessions without any expiration. Add a TTL using the `EX` option.\n\n- Add a new constant `SESSION_EXPIRATION_SECONDS` set to `86400` (24 hours) near the existing `STATE_EXPIRATION_SECONDS` constant on line 7.\n- Update the `redisClient.set()` call in `RedisSessionStore.set()` to include `{ EX: SESSION_EXPIRATION_SECONDS }` as the third argument, matching the pattern used by `RedisStateStore.set()` (line 22-24) and `RedisEpdsStateStore.set()` (line 68-70).\n- Remove the TODO comment in `app/api/oauth/epds/callback/route.ts` on line 136 that says `// TODO: RedisSessionStore.set() does not support TTL`. Replace it with a comment noting the TTL value, e.g. `// Session persists in Redis for 24h; SDK handles token refresh within that window`.\n\n## Don't\n- Change the key prefix or any other store class\n- Change the TTL of StateStore or EpdsStateStore\n- Use a TTL shorter than 1 hour (sessions need to survive token refreshes)","acceptance_criteria":"1. RedisSessionStore.set() passes EX option to redisClient.set(). 2. SESSION_EXPIRATION_SECONDS constant exists and is used. 3. TODO comment in epds/callback/route.ts line 136 is removed/updated. 4. TypeScript compiles without errors (npx tsc --noEmit).","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-27T13:58:59.116998514+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T14:02:41.272202147+06:00","closed_at":"2026-02-27T14:02:41.272202147+06:00","close_reason":"04361cf Add TTL to RedisSessionStore.set()","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7q1.1","depends_on_id":"hypercerts-7q1","type":"parent-child","created_at":"2026-02-27T13:58:59.11848638+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7q1.2","title":"Add handle input validation in login route","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n\n## What to do\nThe POST handler extracts `body.handle` and passes it directly to `sdk.authorize(handle)` without validation. If `handle` is undefined, empty, or not a string, `sdk.authorize()` will throw a cryptic error.\n\nAdd validation after line 6 (`const handle = body.handle;`) and before the try block (line 7):\n\n```ts\nif (!handle || typeof handle !== \"string\") {\n  return Response.json(\n    { error: \"Missing or invalid handle\" },\n    { status: 400 },\n  );\n}\n```\n\nAlso wrap the `await request.json()` in a try-catch to handle malformed JSON bodies — return a 400 with `{ error: \"Invalid request body\" }` if parsing fails.\n\n## Don't\n- Change the sdk.authorize() call or its return value\n- Add any new imports\n- Change the error response format for the existing catch block (lines 10-15)","acceptance_criteria":"1. Sending POST with empty body returns 400 with error message. 2. Sending POST with {handle: ''} returns 400. 3. Sending POST with {handle: 123} returns 400. 4. Sending malformed JSON returns 400. 5. Valid {handle: 'foo.bsky.social'} still reaches sdk.authorize(). 6. TypeScript compiles without errors (npx tsc --noEmit).","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T13:59:09.481559749+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T14:02:24.389144331+06:00","closed_at":"2026-02-27T14:02:24.389144331+06:00","close_reason":"4b979f2 Add handle input validation in login route","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7q1.2","depends_on_id":"hypercerts-7q1","type":"parent-child","created_at":"2026-02-27T13:59:09.484049158+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7q1.3","title":"Guard empty-string initials in UserAvatar","description":"## Files\n- components/user-avatar.tsx (modify)\n\n## What to do\nOn lines 25-26, the avatar fallback initials expression can throw when `displayName` is an empty string `\"\"`:\n\n```ts\n// Current (broken for displayName === \"\"):\n{user.displayName?.[0].toUpperCase() ||\n  user.handle?.[0].toUpperCase()}\n```\n\nWhen `displayName` is `\"\"`, `\"\"?.[0]` returns `undefined`, then `.toUpperCase()` throws `TypeError: Cannot read properties of undefined`.\n\nReplace lines 25-26 with:\n```ts\n{user.displayName?.[0]?.toUpperCase() ??\n  user.handle?.[0]?.toUpperCase() ??\n  \"?\"}\n```\n\nKey changes:\n1. Add `?.` after `[0]` on both `displayName` and `handle` to guard against empty strings\n2. Use `??` (nullish coalescing) instead of `||` so that the fallback chain works correctly with undefined\n3. Add final `\"?\"` fallback for the edge case where both are empty/undefined\n\n## Don't\n- Change any other part of the component\n- Change the className or layout\n- Add any imports","acceptance_criteria":"1. No runtime error when displayName is '' (empty string). 2. No runtime error when both displayName and handle are undefined. 3. Shows '?' when both are empty/undefined. 4. Shows first char of displayName when present. 5. Falls back to first char of handle when displayName is missing. 6. TypeScript compiles without errors (npx tsc --noEmit).","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T13:59:21.518371668+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T14:02:19.980393728+06:00","closed_at":"2026-02-27T14:02:19.980393728+06:00","close_reason":"50ce760 Guard empty-string initials in UserAvatar","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7q1.3","depends_on_id":"hypercerts-7q1","type":"parent-child","created_at":"2026-02-27T13:59:21.519750679+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7q1.4","title":"Fix stale comment in config.ts and README path inconsistency","description":"## Files\n- lib/config.ts (modify)\n- README.md (modify)\n\n## What to do\n\n### 1. lib/config.ts line 263\nThe comment says `// Loopback mode: no branding, application_type is \"native\"` but the returned object on line 281 sets `application_type: \"web\"`. The comment is wrong.\n\nChange line 263 from:\n```ts\n// Loopback mode: no branding, application_type is \"native\"\n```\nto:\n```ts\n// Loopback mode: minimal metadata, application_type is \"web\"\n```\n\nAlso the comment says \"no branding\" but the object includes logo_uri, email_template_uri, brand_color, background_color. Change to \"minimal metadata\" to be accurate.\n\n### 2. README.md lines 332-334\nThe project structure tree shows:\n```\n│   │   ├── auth/              # Standard ATProto OAuth (login, callback, logout)\n│   │   ├── oauth/             # ePDS email OAuth (login, callback)\n```\n\nBut the actual routes are all under `api/oauth/` now (the `api/auth/` directory was deleted in this PR). Replace those two lines with:\n```\n│   │   ├── oauth/             # ATProto OAuth (login, callback, logout)\n│   │   │   └── epds/          # ePDS email OAuth (login, callback)\n```\n\n## Don't\n- Change any code logic\n- Modify any other comments or docs sections\n- Reformat surrounding lines","acceptance_criteria":"1. lib/config.ts line 263 comment says 'web' not 'native'. 2. lib/config.ts comment no longer says 'no branding'. 3. README.md project structure no longer references 'auth/' directory. 4. README.md shows oauth/ with epds/ as a subdirectory. 5. No code logic changes in the diff.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T13:59:36.013092245+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T14:03:17.384670723+06:00","closed_at":"2026-02-27T14:03:17.384670723+06:00","close_reason":"0224a90 Fix stale comment in config.ts and README path inconsistency (changes already in 508bb34)","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7q1.4","depends_on_id":"hypercerts-7q1","type":"parent-child","created_at":"2026-02-27T13:59:36.014821974+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7v8","title":"Epic: Re-enable workScope in hypercert creation flow","description":"The workScope field was commented out across the hypercert creation pipeline. The form UI collects workScope tags, the client API sends them, but the server API route ignores them (commented out) and the form's getRecord() doesn't include workScope in the CreateHypercertParams object. Goal: wire workScope end-to-end so it flows from form → client API → server API route → SDK create call. The SDK's CreateHypercertParams.workScope accepts string | StrongRef (optional). For now we pass a comma-joined string of the tags.","notes":"Wrong scope — user meant OAuth scopes, not workScope field","status":"closed","priority":2,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-17T18:58:07.96248841+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:06:51.176403547+06:00","closed_at":"2026-02-17T19:06:51.176411943+06:00","labels":["scope:small"]}
{"id":"hypercerts-7v8.1","title":"Add workScope to getRecord() in hypercerts-base-form.tsx","description":"## Files\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\nIn the `getRecord()` function (around line 188), the returned `CreateHypercertParams` object is missing the `workScope` field. The `cleanedWorkScope` variable is already computed on line 153 (`const cleanedWorkScope = workScope.map((w) =\u003e w.trim()).filter(Boolean);`) but never used.\n\nAdd `workScope` to the record object. The SDK type `CreateHypercertParams.workScope` accepts `string | StrongRef | undefined`. Pass the cleaned work scope tags as a comma-joined string:\n\n```typescript\nconst record: CreateHypercertParams = {\n  title,\n  shortDescription,\n  rights: { ... },\n  description: shortDescription,\n  image: backgroundImage,\n  startDate: startDate.toISOString(),\n  endDate: endDate.toISOString(),\n  contributions,\n  workScope: cleanedWorkScope.length \u003e 0 ? cleanedWorkScope.join(\", \") : undefined,\n};\n```\n\n## Don't\n- Change the workScope UI inputs or state management — they already work\n- Change the type of workScope (keep it as a comma-joined string for now)\n- Remove or modify the cleanedWorkScope computation","acceptance_criteria":"1. The `getRecord()` function in `hypercerts-base-form.tsx` returns a `CreateHypercertParams` object that includes `workScope` when the user has entered work scope tags.\n2. When no work scope tags are entered, `workScope` is `undefined` (not an empty string).\n3. `npm run build` succeeds with no type errors.","notes":"Parent epic cancelled","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T18:58:21.95276996+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:06:51.230972771+06:00","closed_at":"2026-02-17T19:06:51.230979845+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7v8.1","depends_on_id":"hypercerts-7v8","type":"parent-child","created_at":"2026-02-17T18:58:21.95441263+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7v8.2","title":"Uncomment workScope parsing in API route app/api/certs/create/route.ts","description":"## Files\n- app/api/certs/create/route.ts (modify)\n\n## What to do\nLines 14-15 have the workScope parsing commented out:\n```typescript\n// TODO map to proper workscope\n// // const workScopeRaw = formData.get(\"workScope\") as string | null;\n```\n\nAnd line 36 has the workScope param commented out:\n```typescript\n// workScope,\n```\n\nUncomment and wire these back in:\n\n1. Uncomment the workScope extraction (remove the TODO comment too):\n```typescript\nconst workScopeRaw = formData.get(\"workScope\") as string | null;\n```\n\n2. Parse it — the client sends it as `JSON.stringify(params.workScope)` from `lib/api/hypercerts.ts`. Since `workScope` in `CreateHypercertParams` is `string | StrongRef | undefined`, and we're sending a simple string, parse it:\n```typescript\nconst workScope = workScopeRaw ? JSON.parse(workScopeRaw) : undefined;\n```\n\n3. Add `workScope` back to the `hypercertParams` object:\n```typescript\nconst hypercertParams: CreateHypercertParams = {\n  title,\n  shortDescription,\n  description: description ?? shortDescription,\n  workScope,\n  startDate,\n  endDate,\n  rights,\n  image: image || undefined,\n  contributions,\n};\n```\n\n## Don't\n- Change the FormData key name (must remain \"workScope\")\n- Add validation beyond what exists for other fields\n- Change how other fields are parsed","acceptance_criteria":"1. The `workScopeRaw` variable is extracted from formData (no longer commented out).\n2. The `workScope` field is included in the `hypercertParams` object passed to `ctx.scopedRepo.hypercerts.create()`.\n3. The TODO comment on line 14 is removed.\n4. `npm run build` succeeds with no type errors.","notes":"Parent epic cancelled","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T18:58:31.991816626+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:06:51.287562421+06:00","closed_at":"2026-02-17T19:06:51.287567741+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-7v8.2","depends_on_id":"hypercerts-7v8","type":"parent-child","created_at":"2026-02-17T18:58:31.993423348+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-7vo","title":"Fix: Redis session store has no TTL — ePDS sessions persist forever (from hypercerts-dhz.6)","description":"Review of hypercerts-dhz found: In lib/redis-state-store.ts, RedisSessionStore.set() (line 38-41) calls `redisClient.set(key, JSON.stringify(session))` with NO expiry option. This means ePDS sessions written by the OAuth callback route persist in Redis indefinitely.\n\nBy contrast, RedisStateStore.set() (line 14-18) correctly sets `EX: STATE_EXPIRATION_SECONDS` (600s).\n\nThe user-did cookie has a 1-week maxAge, so the effective session lifetime should be 1 week. The Redis entry should be set with a matching TTL (e.g. `EX: 60 * 60 * 24 * 7`) so that stale sessions are automatically evicted. Without this, Redis memory grows unboundedly as users log in.\n\nNote: this affects both the existing handle-based flow AND the new ePDS flow, but the ePDS flow introduced the first explicit call to sessionStore.set() from application code (the SDK previously managed this internally), making the missing TTL newly visible.\n\nFix: add `{ EX: 60 * 60 * 24 * 7 }` to RedisSessionStore.set(), or accept an optional TTL parameter.","status":"open","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:26:17.398694319+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:26:17.398694319+06:00","dependencies":[{"issue_id":"hypercerts-7vo","depends_on_id":"hypercerts-dhz.6","type":"discovered-from","created_at":"2026-02-24T16:26:25.21195365+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-84k","title":"Fix: unnecessary `position: relative` on Authenticate H1 in atproto-branding.ts (from hypercerts-nyk.15)","description":"Review of hypercerts-nyk.15 found an unnecessary CSS rule in lib/atproto-branding.ts.\n\nLine 232-234:\n```css\nmain.flex.flex-col.items-center \u003e h1.text-primary {\n  position: relative !important;\n}\n```\n\nThis `position: relative` is set to enable the `::before` pseudo-element logo. However, the `::before` pseudo-element (lines 236-246) uses `display: block` with `margin: 0 auto 24px auto` — it is in normal block flow, not absolutely positioned. A `position: relative` parent is only needed for `position: absolute` children. Since the `::before` is block-flow, the `position: relative` on the parent has no effect.\n\nEvidence: lib/atproto-branding.ts lines 232-246. The ::before pseudo-element has no `position: absolute` or `position: fixed`.\n\nFix: Remove the `position: relative !important` rule from `main.flex.flex-col.items-center \u003e h1.text-primary`. This also allows merging the two split rule blocks for this selector (lines 232-234 and 258-261) into one.","status":"open","priority":4,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-02-17T19:46:28.066268808+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:46:28.066268808+06:00","dependencies":[{"issue_id":"hypercerts-84k","depends_on_id":"hypercerts-nyk.15","type":"discovered-from","created_at":"2026-02-17T19:46:32.467819549+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-8du","title":"Fix: error detail still leaked to client in add-attachment and bsky/update routes (from hypercerts-dcb.5)","description":"Review of hypercerts-dcb.5 found: Two API routes still interpolate the raw error message directly into the client-facing JSON response, leaking internal error details.\n\n**app/api/certs/add-attachment/route.ts line 142:**\n```ts\nerror: `Failed to add attachment: ${e instanceof Error ? e.message : String(e)}`\n```\n\n**app/api/profile/bsky/update/route.ts line 107:**\n```ts\nerror: `Profile update failed: ${error instanceof Error ? error.message : String(error)}`\n```\n\nBoth were fixed by hypercerts-tlw.5 (commit 81cca2f) to use the safe instanceof check, but the fix only replaced the unsafe cast — it still interpolates the message into the client response. The dcb.5 spec requires that error details stay server-side only (in console.error) and the client receives only a generic message.\n\n**Required fix:**\n- add-attachment: return `{ error: \"Failed to add attachment\" }` (no interpolation)\n- bsky/update: return `{ error: \"Profile update failed\" }` (no interpolation)\n- Keep existing console.error calls (they already log the full error object)\n\n**Evidence:** git show 81cca2f -- app/api/certs/add-attachment/route.ts app/api/profile/bsky/update/route.ts","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-24T14:52:22.270561971+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:03:56.419693153+06:00","closed_at":"2026-02-24T15:03:56.419693153+06:00","close_reason":"2acaf31 fix: remove error detail leak from client responses","dependencies":[{"issue_id":"hypercerts-8du","depends_on_id":"hypercerts-dcb.5","type":"discovered-from","created_at":"2026-02-24T14:52:29.389217+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-8ld","title":"Fix: Collapsible Contributors section in hypercerts-base-form.tsx missing aria-expanded and aria-controls","description":"## File\\ncomponents/hypercerts-base-form.tsx lines 472-495\\n\\n## Severity\\nMEDIUM — Accessibility\\n\\n## Problem\\nThe Contributors collapsible section uses a plain \u003cbutton\u003e to toggle visibility, but has no aria-expanded to communicate the open/closed state to screen readers, and no aria-controls to link the button to the panel it controls. Screen reader users cannot determine whether the section is expanded or collapsed.\\n\\n## Evidence\\n```tsx\\n\u003cbutton\\n  type=\"button\"\\n  className=\"group flex w-full items-center justify-between\"\\n  onClick={() =\u003e setShowContributions(!showContributions)}\\n  // Missing: aria-expanded, aria-controls\\n\u003e\\n  ...\\n\u003c/button\u003e\\n{showContributions ? (\\n  \u003cdiv className=\"...\"\u003e {/* Missing: id for aria-controls */}\\n    ...\\n  \u003c/div\u003e\\n) : null}\\n```\\n\\n## Fix\\n```tsx\\n\u003cbutton\\n  type=\"button\"\\n  aria-expanded={showContributions}\\n  aria-controls=\"contributors-panel\"\\n  className=\"group flex w-full items-center justify-between\"\\n  onClick={() =\u003e setShowContributions(!showContributions)}\\n\u003e\\n  ...\\n\u003c/button\u003e\\n{showContributions ? (\\n  \u003cdiv id=\"contributors-panel\" className=\"...\"\u003e\\n    ...\\n  \u003c/div\u003e\\n) : null}\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:33.783513608+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:33.783513608+06:00"}
{"id":"hypercerts-8na","title":"Fix: parseAtUri used as filter predicate but returns object (truthy) even for valid URIs — filter logic is correct but fragile (use-contributor-profiles-query.ts)","description":"## File\\nqueries/hypercerts/use-contributor-profiles-query.ts line 60\\n\\n## Severity\\nLOW — Code clarity / fragility\\n\\n## Problem\\nparseAtUri is used as a filter predicate in the needsResolution filter:\\n```tsx\\nconst needsResolution = contributors.filter(\\n  (c) =\u003e c.needsResolution \u0026\u0026 c.identityRef \u0026\u0026 parseAtUri(c.identityRef.uri),\\n);\\n```\\n\\nparseAtUri returns undefined for invalid URIs and an object for valid ones. Using it as a truthy predicate works, but it is fragile: the function is called purely for its side-effect-free return value as a boolean, not for the parsed result. The actual parsing is done again on line 66 with parseAtUri(ref.uri)!. This means parseAtUri is called twice per contributor, and the non-null assertion on line 66 is only safe because of the filter on line 60.\\n\\n## Fix\\nParse once and store the result:\\n```tsx\\nconst needsResolution = contributors\\n  .filter((c) =\u003e c.needsResolution \u0026\u0026 c.identityRef)\\n  .map((c) =\u003e ({\\n    contributor: c,\\n    parsed: parseAtUri(c.identityRef!.uri),\\n  }))\\n  .filter((item): item is { contributor: DisplayContributor; parsed: NonNullable\u003cReturnType\u003ctypeof parseAtUri\u003e\u003e } =\u003e\\n    item.parsed !== undefined\\n  );\\n\\n// Then in useQueries:\\nqueries: needsResolution.map(({ contributor, parsed }) =\u003e ({\\n  queryKey: queryKeys.hypercerts.contributorInformation(parsed.did, parsed.rkey),\\n  queryFn: () =\u003e getContributorInformationRecord(parsed),\\n  ...\\n}))\\n```","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:33.513838454+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:33.513838454+06:00"}
{"id":"hypercerts-9be","title":"Fix: record cast to 'any' to access contributors field in hypercert-detail-view.tsx","description":"## File\\ncomponents/hypercert-detail-view.tsx lines 40-44\\n\\n## Severity\\nMEDIUM — Type safety\\n\\n## Problem\\nThe contributors field is accessed via an unsafe (record as any).contributors cast because the HypercertClaim type does not yet include the contributors field. This bypasses TypeScript's type checking entirely. If the field name changes or the type changes, there will be no compile-time error.\\n\\n## Evidence\\n```tsx\\n// eslint-disable-next-line @typescript-eslint/no-explicit-any -- contributors field exists on record but not in HypercertClaim type yet\\nconst rawContributors = (record as any).contributors;\\nconst contributors = Array.isArray(rawContributors)\\n  ? rawContributors\\n  : undefined;\\n```\\n\\n## Fix\\nCreate a typed extension of HypercertClaim that includes the contributors field, or use a type assertion with a proper interface:\\n```tsx\\ninterface HypercertClaimWithContributors extends HypercertClaim {\\n  contributors?: OrgHypercertsClaimActivity.Contributor[];\\n}\\n\\nconst typedRecord = record as HypercertClaimWithContributors;\\nconst contributors = Array.isArray(typedRecord.contributors)\\n  ? typedRecord.contributors\\n  : undefined;\\n```\\n\\nThis is a temporary workaround until the SDK type is updated. The interface should be removed once HypercertClaim includes contributors.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:11.367818634+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:11.367818634+06:00"}
{"id":"hypercerts-9n0","title":"Fix: issuer derived via fragile string replace on tokenEndpoint — breaks if path differs (from security review)","description":"## File\napp/api/oauth/epds/callback/route.ts, line 112\n\n## Problem\nThe OAuth issuer is derived by stripping the path from the token endpoint URL using a hardcoded string replace:\n```ts\nconst issuer = tokenEndpoint.replace('/oauth/token', '');\n```\n\nThis is fragile for two reasons:\n1. If the ePDS server uses a different token endpoint path (e.g. `/oauth/v2/token`, `/token`, or a path with a trailing slash), the replace silently fails and `issuer` becomes the full token endpoint URL — an incorrect value that will cause session restoration failures.\n2. `String.replace()` only replaces the first occurrence. If `/oauth/token` appears elsewhere in the URL (e.g. in a query parameter), the result is wrong.\n\n## Fix\nUse URL parsing to extract the origin instead of string manipulation:\n```ts\nconst issuer = new URL(tokenEndpoint).origin;\n```\n\n`URL.origin` returns `protocol + hostname + port` (e.g. `https://pds-eu-west4.test.certified.app`) regardless of the path, which is the correct issuer value for an ATProto PDS.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:11.791678872+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:11.791678872+06:00","dependencies":[{"issue_id":"hypercerts-9n0","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.765878766+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-9n8","title":"Fix: request_uri from PAR response not validated before use in redirect (from security review)","description":"## File\napp/api/oauth/epds/login/route.ts, lines 67-80\n\n## Problem\nThe `request_uri` value from the PAR response is used directly in the authorization redirect without validation:\n```ts\nconst { request_uri } = (await parResponse.json()) as { request_uri: string };\n// ...\nauthUrl.searchParams.set('request_uri', request_uri);\n```\n\nRFC 9126 specifies that `request_uri` values MUST use the `urn:ietf:params:oauth:request_uri:` prefix. If the ePDS server is compromised or returns a malformed response, an attacker-controlled `request_uri` could be injected into the authorization URL. While the redirect target (`authEndpoint`) is fixed and derived from the trusted `NEXT_PUBLIC_EPDS_URL`, the `request_uri` parameter value itself is unvalidated.\n\nAdditionally, if `request_uri` is missing from the response (e.g. server returns `{}`), `request_uri` will be `undefined`, and `authUrl.searchParams.set('request_uri', undefined)` will set the parameter to the string `'undefined'`, causing the authorization request to fail with a confusing error.\n\n## Fix\n1. Validate the `request_uri` is present and has the expected prefix:\n```ts\nconst { request_uri } = (await parResponse.json()) as { request_uri?: string };\nif (!request_uri || !request_uri.startsWith('urn:ietf:params:oauth:request_uri:')) {\n  console.error('[oauth/epds/login] Invalid or missing request_uri in PAR response:', request_uri);\n  return NextResponse.redirect(new URL('/?error=auth_failed', config.baseUrl));\n}\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:21.527000262+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:21.527000262+06:00","dependencies":[{"issue_id":"hypercerts-9n8","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.831337237+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-9ru3","title":"Fix: Collections enum has wrong NSIDs for contribution and evidence (from hypercerts-fpyy.2)","description":"Review of hypercerts-fpyy.2 found a namespace mismatch in lib/types.ts Collections enum and lib/queries.ts.\n\n## Problem\nThe Collections enum in lib/types.ts has two wrong values:\n```ts\ncontribution = \"org.hypercerts.claim.contribution\",  // WRONG — no such lexicon\nevidence = \"org.hypercerts.claim.evidence\",           // WRONG — no such lexicon\n```\n\nThe correct NSIDs from the @hypercerts-org/lexicon package are:\n- CONTRIBUTION_DETAILS_NSID = \"org.hypercerts.claim.contributionDetails\"\n- ATTACHMENT_NSID = \"org.hypercerts.claim.attachment\"\n\nIn lib/queries.ts, createContribution() writes to Collections.contribution (wrong NSID) but validates with OrgHypercertsClaimContributionDetails.validateRecord (correct schema). Similarly, createEvidence() writes to Collections.evidence (wrong NSID) but validates with OrgHypercertsClaimAttachment.validateRecord (correct schema).\n\nThe epic spec requires: 'Each validation uses the correct lexicon namespace's validateRecord function matching the collection.' The collection being written to does not match the lexicon being validated against.\n\n## Current Impact\nBoth createContribution() and createEvidence() in queries.ts are dead code (never called from anywhere in the codebase). So there is no runtime impact today. However, if either function is called in the future, records will be written to non-existent collections with mismatched  fields.\n\n## Fix\nUpdate lib/types.ts Collections enum:\n```ts\ncontribution = \"org.hypercerts.claim.contributionDetails\",\nevidence = \"org.hypercerts.claim.attachment\",\n```\nOr remove the dead createContribution/createEvidence functions from queries.ts entirely (the real implementations are in create-actions.ts and add-attachment/route.ts which use correct hardcoded strings).\n\n## Evidence\n- node_modules/@hypercerts-org/lexicon/dist/exports.d.ts: CONTRIBUTION_DETAILS_NSID = 'org.hypercerts.claim.contributionDetails', ATTACHMENT_NSID = 'org.hypercerts.claim.attachment'\n- No 'contribution' or 'evidence' JSON files exist in the lexicon package\n- lib/queries.ts lines 98, 124: Collections.contribution, Collections.evidence","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T13:08:34.275053641+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T13:56:38.369124409+06:00","closed_at":"2026-03-04T13:56:38.369124409+06:00","close_reason":"7c2daac fix: correct Collections enum NSIDs for contribution and evidence","dependencies":[{"issue_id":"hypercerts-9ru3","depends_on_id":"hypercerts-fpyy.2","type":"discovered-from","created_at":"2026-03-04T13:08:34.34093928+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-9ud","title":"Fix: Contributor card link has no aria-label — screen readers announce raw DID URL (hypercert-contributor-view.tsx)","description":"## File\\ncomponents/hypercert-contributor-view.tsx lines 86-93\\n\\n## Severity\\nMEDIUM — Accessibility\\n\\n## Problem\\nWhen a contributor has isDid=true, the card is wrapped in an \u003ca\u003e tag linking to their Bluesky profile. The link has no aria-label, so screen readers will announce the full URL (https://bsky.app/profile/did:plc:...) which is not meaningful. The visible content inside the card (name/handle) is not associated with the link.\\n\\nNote: hypercerts-mrb.10 tracks the missing aria-label on the contributor card link. This issue is distinct — it specifically tracks the fix needed: the aria-label should use the resolved display name or handle.\\n\\n## Evidence\\n```tsx\\n// Lines 86-93\\n\u003ca\\n  href={}\\n  target=\"_blank\"\\n  rel=\"noopener noreferrer\"\\n  className=\"block\"\\n\u003e\\n  {cardContent}\\n\u003c/a\u003e\\n```\\n\\n## Fix\\nAdd a descriptive aria-label:\\n```tsx\\n\u003ca\\n  href={}\\n  target=\"_blank\"\\n  rel=\"noopener noreferrer\"\\n  className=\"block\"\\n  aria-label={}\\n\u003e\\n  {cardContent}\\n\u003c/a\u003e\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:07.761372528+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:07.761372528+06:00"}
{"id":"hypercerts-a48","title":"Epic: Reorganize OAuth API routes under /api/oauth namespace","description":"## Why\nCurrently the API routes are split across two directories with confusing naming:\n- `api/auth/login|callback|logout` — ATProto handle-based OAuth flow (SDK-based)\n- `api/oauth/login|callback` — ePDS email-based OAuth flow (manual PAR/DPoP)\n\nThe naming is misleading: both are OAuth flows, but only the ePDS one lives under `/oauth`. The handle-based flow lives under `/auth` which doesn't convey that it's also OAuth.\n\n## Target structure\n```\napp/api/oauth/\n  login/route.ts          ← ATProto handle login (moved from auth/login)\n  callback/route.ts       ← ATProto handle callback (moved from auth/callback)\n  logout/route.ts         ← Logout (moved from auth/logout)\n  epds/\n    login/route.ts        ← ePDS email login (moved from oauth/login)\n    callback/route.ts     ← ePDS email callback (moved from oauth/callback)\n```\n\n## What success looks like\n- All OAuth routes live under `/api/oauth/`\n- ePDS-specific routes are namespaced under `/api/oauth/epds/`\n- All internal references (config, client API, login-dialog, README) updated\n- Client metadata redirect_uris updated\n- No behavioral changes — same logic, just new paths\n- Build passes, no broken imports\n\n## Key constraints\n- This is a pure file-move + reference-update refactor — NO logic changes\n- The `app/api/auth/` directory should be deleted entirely after the move\n- All redirect URIs in config.ts and client-metadata must be updated atomically\n- The ePDS config (epds-config.ts) redirect URI must also update","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-27T11:14:12.652428885+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:28:43.719206426+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-a48.1","title":"Move ePDS OAuth routes from api/oauth/ to api/oauth/epds/","description":"## Files\n- app/api/oauth/login/route.ts (move → app/api/oauth/epds/login/route.ts)\n- app/api/oauth/callback/route.ts (move → app/api/oauth/epds/callback/route.ts)\n\n## What to do\n1. Create directory `app/api/oauth/epds/login/` and `app/api/oauth/epds/callback/`\n2. Move `app/api/oauth/login/route.ts` → `app/api/oauth/epds/login/route.ts`\n3. Move `app/api/oauth/callback/route.ts` → `app/api/oauth/epds/callback/route.ts`\n4. The file contents stay IDENTICAL — no import changes needed since they all use `@/` aliases\n5. Delete the now-empty `app/api/oauth/login/` and `app/api/oauth/callback/` directories\n6. Use `git mv` for the moves to preserve history\n\n## Dont\n- Change any code inside the route files\n- Change any imports (they use @/ aliases which are path-independent)\n- Update any references yet — that is a separate task\n- Touch any files outside app/api/oauth/","acceptance_criteria":"1. app/api/oauth/epds/login/route.ts exists with identical content to the old app/api/oauth/login/route.ts\n2. app/api/oauth/epds/callback/route.ts exists with identical content to the old app/api/oauth/callback/route.ts\n3. app/api/oauth/login/ directory no longer exists\n4. app/api/oauth/callback/ directory no longer exists\n5. No code changes inside the moved files\n6. git status shows renames, not delete+create","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-27T11:14:27.322706332+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:21:44.417771594+06:00","closed_at":"2026-02-27T11:21:44.417771594+06:00","close_reason":"5e9807c Move ePDS OAuth routes from api/oauth/ to api/oauth/epds/","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-a48.1","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:14:27.324472531+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a48.2","title":"Move ATProto auth routes from api/auth/ to api/oauth/","description":"## Files\n- app/api/auth/login/route.ts (move → app/api/oauth/login/route.ts)\n- app/api/auth/callback/route.ts (move → app/api/oauth/callback/route.ts)\n- app/api/auth/logout/route.ts (move → app/api/oauth/logout/route.ts)\n\n## What to do\n1. Move `app/api/auth/login/route.ts` → `app/api/oauth/login/route.ts`\n2. Move `app/api/auth/callback/route.ts` → `app/api/oauth/callback/route.ts`\n3. Move `app/api/auth/logout/route.ts` → `app/api/oauth/logout/route.ts`\n4. Delete the now-empty `app/api/auth/` directory entirely\n5. Use `git mv` for the moves to preserve history\n6. The file contents stay IDENTICAL — no import changes needed since they all use `@/` aliases\n\n**IMPORTANT**: This task depends on a48.1 completing first. After a48.1, the `app/api/oauth/login/` and `app/api/oauth/callback/` directories will be empty (the ePDS routes moved to `epds/` subdirs). So these moves will place the ATProto routes into those now-empty directories.\n\n## Dont\n- Change any code inside the route files\n- Change any imports (they use @/ aliases which are path-independent)\n- Update any references yet — that is a separate task\n- Leave any files in app/api/auth/ — the directory must be fully deleted","acceptance_criteria":"1. app/api/oauth/login/route.ts exists with identical content to the old app/api/auth/login/route.ts\n2. app/api/oauth/callback/route.ts exists with identical content to the old app/api/auth/callback/route.ts\n3. app/api/oauth/logout/route.ts exists with identical content to the old app/api/auth/logout/route.ts\n4. app/api/auth/ directory no longer exists\n5. No code changes inside the moved files\n6. git status shows renames, not delete+create","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-27T11:14:38.650336914+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:23:17.540751953+06:00","closed_at":"2026-02-27T11:23:17.540751953+06:00","close_reason":"76e8e54 Move ATProto auth routes from api/auth/ to api/oauth/","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-a48.2","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:14:38.651826086+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.2","depends_on_id":"hypercerts-a48.1","type":"blocks","created_at":"2026-02-27T11:14:38.654498073+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a48.3","title":"Update config.ts redirect URIs for new route paths","description":"## Files\n- lib/config.ts (modify)\n\n## What to do\nUpdate the redirect URI constants to reflect the new route paths:\n\n### Line 196 — ATProto redirect URI\nChange:\n```typescript\nconst redirectUri = `${redirectBaseUrl}/api/auth/callback`;\n```\nTo:\n```typescript\nconst redirectUri = `${redirectBaseUrl}/api/oauth/callback`;\n```\n\n### Line 197 — ePDS redirect URI\nChange:\n```typescript\nconst epdsRedirectUri = `${redirectBaseUrl}/api/oauth/callback`;\n```\nTo:\n```typescript\nconst epdsRedirectUri = `${redirectBaseUrl}/api/oauth/epds/callback`;\n```\n\nThese are the ONLY two lines that change in this file. Everything downstream (clientId, epdsClientId, buildClientMetadata, etc.) derives from these constants automatically.\n\n## Dont\n- Change any other lines in config.ts\n- Change the buildClientMetadata function (it already uses config.redirectUri and config.epdsRedirectUri dynamically)\n- Change the clientId or epdsClientId construction (they derive from the redirect URIs)\n- Change any environment variable names","acceptance_criteria":"1. redirectUri is set to `${redirectBaseUrl}/api/oauth/callback`\n2. epdsRedirectUri is set to `${redirectBaseUrl}/api/oauth/epds/callback`\n3. No other lines in config.ts changed\n4. TypeScript compiles: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T11:14:50.021713651+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:25:27.036022908+06:00","closed_at":"2026-02-27T11:25:27.036022908+06:00","close_reason":"46186a0 Update config.ts redirect URIs for new route paths","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-a48.3","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:14:50.023655892+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.3","depends_on_id":"hypercerts-a48.2","type":"blocks","created_at":"2026-02-27T11:14:50.026448936+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a48.4","title":"Update client API auth functions to use new /api/oauth/ paths","description":"## Files\n- lib/api/auth.ts (modify)\n\n## What to do\nUpdate the API endpoint paths in the auth client functions:\n\n### Line 12 — login function\nChange:\n```typescript\nreturn apiClient\u003cLoginResponse\u003e(\"/api/auth/login\", {\n```\nTo:\n```typescript\nreturn apiClient\u003cLoginResponse\u003e(\"/api/oauth/login\", {\n```\n\n### Line 22 — logout function\nChange:\n```typescript\nawait apiClient\u003cvoid\u003e(\"/api/auth/logout\", {\n```\nTo:\n```typescript\nawait apiClient\u003cvoid\u003e(\"/api/oauth/logout\", {\n```\n\nThese are the ONLY two lines that change in this file.\n\n## Dont\n- Change the function signatures or return types\n- Change the HTTP methods (POST for login, GET for logout)\n- Change the request body shape\n- Rename the file (it stays as lib/api/auth.ts — it is still auth logic)","acceptance_criteria":"1. login() calls \"/api/oauth/login\" instead of \"/api/auth/login\"\n2. logout() calls \"/api/oauth/logout\" instead of \"/api/auth/logout\"\n3. No other changes in the file\n4. TypeScript compiles: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T11:15:01.103161746+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:26:06.961003557+06:00","closed_at":"2026-02-27T11:26:06.961003557+06:00","close_reason":"f5efd9c auth.ts already uses /api/oauth/ paths — changes were applied in 46186a0","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-a48.4","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:15:01.111838597+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.4","depends_on_id":"hypercerts-a48.2","type":"blocks","created_at":"2026-02-27T11:15:01.114998734+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a48.5","title":"Update login-dialog.tsx ePDS login URL to /api/oauth/epds/login","description":"## Files\n- components/login-dialog.tsx (modify)\n\n## What to do\nUpdate the ePDS login redirect URL in the EmailForm component:\n\n### Lines 123-125 — handleContinue function\nChange:\n```typescript\nconst url = email\n  ? `/api/oauth/login?email=${encodeURIComponent(email)}`\n  : `/api/oauth/login`;\n```\nTo:\n```typescript\nconst url = email\n  ? `/api/oauth/epds/login?email=${encodeURIComponent(email)}`\n  : `/api/oauth/epds/login`;\n```\n\nThese are the ONLY two lines that change in this file.\n\n## Dont\n- Change any other code in login-dialog.tsx\n- Change the HandleForm component (it uses the login mutation which calls lib/api/auth.ts — updated in a separate task)\n- Change any styling or component structure","acceptance_criteria":"1. EmailForm handleContinue navigates to /api/oauth/epds/login (with or without email param)\n2. No other changes in login-dialog.tsx\n3. TypeScript compiles: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T11:15:12.040478829+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:23:16.421370831+06:00","closed_at":"2026-02-27T11:23:16.421370831+06:00","close_reason":"76e8e54 Update ePDS login URL to /api/oauth/epds/login","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-a48.5","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:15:12.041978852+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.5","depends_on_id":"hypercerts-a48.1","type":"blocks","created_at":"2026-02-27T11:15:12.052035134+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a48.6","title":"Update ePDS route log prefixes from [oauth/] to [oauth/epds/]","description":"## Files\n- app/api/oauth/epds/login/route.ts (modify)\n- app/api/oauth/epds/callback/route.ts (modify)\n\n## What to do\nAfter the ePDS routes are moved to api/oauth/epds/, update the console.error log prefixes to reflect the new path. This helps with debugging — log messages should match the actual route path.\n\n### app/api/oauth/epds/login/route.ts\n- Line 56: Change `\"[oauth/login] PAR failed\"` → `\"[oauth/epds/login] PAR failed\"`\n- Line 87: Change `\"[oauth/login] Unexpected error:\"` → `\"[oauth/epds/login] Unexpected error:\"`\n\n### app/api/oauth/epds/callback/route.ts\n- Line 22: Change `\"[oauth/callback] Missing required query parameters...\"` → `\"[oauth/epds/callback] Missing required query parameters...\"`\n- Line 32: Change `\"[oauth/callback] No OAuth state found...\"` → `\"[oauth/epds/callback] No OAuth state found...\"`\n- Line 101: Change `\"[oauth/callback] Invalid sub...\"` → `\"[oauth/epds/callback] Invalid sub...\"`\n- Line 76: Change `\"[oauth/callback] Token exchange failed:\"` → `\"[oauth/epds/callback] Token exchange failed:\"`\n- Line 154: Change `\"[oauth/callback] Unexpected error:\"` → `\"[oauth/epds/callback] Unexpected error:\"`\n\n## Dont\n- Change any logic or behavior\n- Change any imports\n- Change anything other than the string prefixes in console.error calls","acceptance_criteria":"1. All console.error prefixes in both files use [oauth/epds/login] or [oauth/epds/callback] instead of [oauth/login] or [oauth/callback]\n2. No logic changes\n3. TypeScript compiles: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T11:15:23.317053976+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:23:47.470152383+06:00","closed_at":"2026-02-27T11:23:47.470152383+06:00","close_reason":"65460a2 Update ePDS route log prefixes to [oauth/epds/]","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-a48.6","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:15:23.319248158+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.6","depends_on_id":"hypercerts-a48.1","type":"blocks","created_at":"2026-02-27T11:15:23.322661238+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a48.7","title":"Update README.md route references for new OAuth paths","description":"## Files\n- README.md (modify)\n\n## What to do\nUpdate all route path references in the README to reflect the new structure.\n\n### Line 143 — Architecture diagram\nChange:\n```\n│  /api/auth/*        │    │  /api/oauth/*        │\n```\nTo:\n```\n│  /api/oauth/*       │    │  /api/oauth/epds/*   │\n```\n\n### Line 234 — ePDS flow step 7\nChange:\n```\n7. ePDS redirects back to `/api/oauth/callback` with an authorization code\n```\nTo:\n```\n7. ePDS redirects back to `/api/oauth/epds/callback` with an authorization code\n```\n\nAlso search for any other references to `/api/auth/` or the old `/api/oauth/callback` path in the README and update them. The README may have other references — check the full Authentication section (around lines 196-250).\n\n## Dont\n- Change any non-route-related content\n- Change the flow descriptions or explanations (just the paths)\n- Add new sections","acceptance_criteria":"1. No references to /api/auth/ remain in README.md\n2. ePDS callback reference updated to /api/oauth/epds/callback\n3. Architecture diagram updated to show /api/oauth/* and /api/oauth/epds/*\n4. All route paths in the README match the new directory structure","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-27T11:15:34.989268041+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:25:31.890351723+06:00","closed_at":"2026-02-27T11:25:31.890351723+06:00","close_reason":"e851441 Update README.md route references for new OAuth paths","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-a48.7","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:15:34.99119789+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.7","depends_on_id":"hypercerts-a48.2","type":"blocks","created_at":"2026-02-27T11:15:34.994650938+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a48.8","title":"Verify build passes and no stale route references remain","description":"## Files\n- (verification only — no file changes expected)\n\n## What to do\nRun verification checks to ensure the route reorganization is complete and correct:\n\n1. Run `pnpm build` — must pass with zero errors\n2. Run `grep -r \"api/auth/\" --include=\"*.ts\" --include=\"*.tsx\" --include=\"*.md\" .` (excluding .beads/ and node_modules/) — must return NO results. All references to the old /api/auth/ path should be gone.\n3. Run `grep -r \"\\\"\\/api\\/oauth\\/login\\\"\" --include=\"*.ts\" --include=\"*.tsx\" .` and `grep -r \"\\\"\\/api\\/oauth\\/callback\\\"\" --include=\"*.ts\" --include=\"*.tsx\" .` — these should ONLY match the ATProto routes (app/api/oauth/login/route.ts and app/api/oauth/callback/route.ts), NOT the ePDS routes (which should be at /api/oauth/epds/*)\n4. Verify the final directory structure matches:\n```\napp/api/oauth/\n  login/route.ts          ← ATProto handle login\n  callback/route.ts       ← ATProto handle callback\n  logout/route.ts         ← Logout\n  epds/\n    login/route.ts        ← ePDS email login\n    callback/route.ts     ← ePDS email callback\n```\n\nIf any issues are found, fix them in this task.\n\n## Dont\n- Skip the build check\n- Ignore grep results from .beads/ or node_modules/ (those are expected)","acceptance_criteria":"1. pnpm build exits 0\n2. No references to /api/auth/ in any .ts, .tsx, or .md files (excluding .beads/ and node_modules/)\n3. Directory structure matches the target layout\n4. All 5 route files exist in their new locations","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-27T11:15:50.823643637+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T11:28:18.469500414+06:00","closed_at":"2026-02-27T11:28:18.469500414+06:00","close_reason":"e12cd9a fix: update DEVELOPMENT.md stale api/auth/ route references; build passes, no stale references remain","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-a48.8","depends_on_id":"hypercerts-a48","type":"parent-child","created_at":"2026-02-27T11:15:50.825110136+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.8","depends_on_id":"hypercerts-a48.3","type":"blocks","created_at":"2026-02-27T11:15:50.828302148+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.8","depends_on_id":"hypercerts-a48.4","type":"blocks","created_at":"2026-02-27T11:15:50.831296987+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.8","depends_on_id":"hypercerts-a48.5","type":"blocks","created_at":"2026-02-27T11:15:50.839551174+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.8","depends_on_id":"hypercerts-a48.6","type":"blocks","created_at":"2026-02-27T11:15:50.843955762+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-a48.8","depends_on_id":"hypercerts-a48.7","type":"blocks","created_at":"2026-02-27T11:15:50.847068149+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-a7j","title":"Seam mismatch: buildClientMetadata() uses config.clientId but ePDS PAR uses config.epdsClientId — metadata document won't match ePDS client_id in loopback","description":"Review of hypercerts-i1m.1 (commit f880d7b) found a seam mismatch between the client metadata document and the ePDS login flow.\n\n## Problem\n\n`buildClientMetadata()` in `lib/config.ts` (lines 263, 285) always sets `client_id: config.clientId`. This is the ATProto client ID built from `redirectUri` (`/api/auth/callback`).\n\nThe ePDS login flow (`app/api/oauth/login/route.ts`) sends `client_id: config.epdsClientId` in the PAR request. `config.epdsClientId` is built from `epdsRedirectUri` (`/api/oauth/callback`).\n\nIn loopback mode, both are `http://localhost?scope=...\u0026redirect_uri=...` URLs — but with **different** `redirect_uri` query params embedded. They are different strings.\n\nIn production mode, both resolve to `https://yourdomain.com/client-metadata.json` — so they are the same string and there is no mismatch.\n\n## Risk\n\nIf the ePDS server (in loopback mode) tries to fetch the client metadata document using the `client_id` from the PAR request (`config.epdsClientId`), it will get a document whose `client_id` field is `config.clientId` — a different value. This violates RFC 7591 §3.2.1 which requires the `client_id` in the metadata to match the one used in the request.\n\nIn practice, loopback clients may not be fetched by the server (the client_id IS the metadata for loopback), so this may not cause a runtime failure today. But it is a correctness gap that should be documented or fixed.\n\n## Evidence\n\n```\nlib/config.ts:198  const epdsClientId = buildClientId(baseUrl, OAUTH_SCOPE, epdsRedirectUri);\nlib/config.ts:199  const clientId     = buildClientId(baseUrl, OAUTH_SCOPE, redirectUri);\nlib/config.ts:263  client_id: config.clientId,   // ← ATProto client ID, not ePDS client ID\nlib/config.ts:285  client_id: config.clientId,   // ← same issue in production branch\n```\n\n## Fix options (for Lead to decide)\n\n1. Add a separate `buildEpdsClientMetadata()` that uses `config.epdsClientId` and serve it at a separate endpoint (e.g. `/epds-client-metadata.json`).\n2. Merge both redirect URIs into a single client ID (single OAuth client for both flows).\n3. Document that this is intentional and explain why it is safe (e.g. ePDS server never fetches loopback client metadata).","status":"open","priority":2,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-02-26T15:57:33.611472741+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T15:57:33.611472741+06:00","dependencies":[{"issue_id":"hypercerts-a7j","depends_on_id":"hypercerts-i1m.1","type":"discovered-from","created_at":"2026-02-26T15:57:38.020864397+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu","title":"Epic: Replace hypercerts-sdk auth layer with native @atproto/oauth-client-node","description":"## Why\nThe @hypercerts-org/sdk-core is a vendored beta (0.10.0-beta.8) that wraps @atproto/oauth-client-node for OAuth and provides a Repository abstraction for data operations. The SDK is unstable and introduces its own bugs that conflate with application bugs. We already depend on @atproto/oauth-client-node, @atproto/api, and @atproto/lexicon directly.\n\n## Goal\nReplace all SDK auth usage (createATProtoSDK, sdk.authorize, sdk.callback, sdk.restoreSession, sdk.revokeSession, sdk.repository, sdk.resolveSessionPds) with direct @atproto/oauth-client-node NodeOAuthClient usage. After this epic, no file should import auth-related symbols from @hypercerts-org/sdk-core.\n\n## Scope\nPhase 1 of a 5-phase migration. This epic covers ONLY the auth/session layer:\n- Replace lib/hypercerts-sdk.ts with a NodeOAuthClient-based singleton\n- Update lib/config.ts to remove SDK constant imports (ATPROTO_SCOPE, HYPERCERT_COLLECTIONS)\n- Update lib/redis-state-store.ts to import interfaces from @atproto/oauth-client-node\n- Replace lib/atproto-session.ts to return OAuthSession + Agent instead of Repository\n- Replace lib/repo-context.ts to return Agent + DID instead of Repository/scopedRepo\n- Update all OAuth routes (login, callback, logout) to use new client\n- Update lib/server-utils.ts to resolve PDS without SDK\n- Update lib/create-actions.ts logout/revokeSession call\n\n## Constraints\n- Do NOT touch any data operations (reads/writes) in this epic — those are Phase 2-4\n- The app must build (pnpm build) after all tasks complete\n- Existing data operations that use ctx.scopedRepo.* will temporarily break at the type level — that is expected and will be fixed in Phase 2-4\n- Keep @hypercerts-org/sdk-core in package.json for now (other files still import types from it)\n\n## Success\n- All auth flows (login, callback, session restore, logout) work via NodeOAuthClient\n- No file imports createATProtoSDK, ATProtoSDKConfig, or Repository from sdk-core\n- pnpm build succeeds (with the understanding that runtime data operations are deferred to later phases)","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-03T15:56:40.254629524+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:58:54.202185656+06:00","closed_at":"2026-03-03T22:58:54.202185656+06:00","close_reason":"52ffa54 All Phase 1 auth layer tasks completed and verified","labels":["scope:medium"]}
{"id":"hypercerts-acu.1","title":"Inline ATPROTO_SCOPE and HYPERCERT_COLLECTIONS constants in lib/config.ts","description":"## Files\n- lib/config.ts (modify)\n\n## What to do\nRemove the import `import { ATPROTO_SCOPE, HYPERCERT_COLLECTIONS } from '@hypercerts-org/sdk-core'` from lib/config.ts and define these constants locally in the same file.\n\n### ATPROTO_SCOPE\nThis is just the string `'atproto'`. Define it as:\n```ts\nconst ATPROTO_SCOPE = 'atproto';\n```\n\n### HYPERCERT_COLLECTIONS\nDefine as a const object with these NSID string values (look at the current usage in lines 18-30 to confirm the keys used):\n```ts\nexport const HYPERCERT_COLLECTIONS = {\n  CLAIM: 'org.hypercerts.claim.activity',\n  RIGHTS: 'org.hypercerts.claim.rights',\n  LOCATION: 'app.certified.location',\n  CONTRIBUTION_DETAILS: 'org.hypercerts.claim.contributionDetails',\n  CONTRIBUTOR_INFORMATION: 'org.hypercerts.claim.contributorInformation',\n  MEASUREMENT: 'org.hypercerts.claim.measurement',\n  EVALUATION: 'org.hypercerts.claim.evaluation',\n  ATTACHMENT: 'org.hypercerts.claim.attachment',\n  COLLECTION: 'org.hypercerts.claim.collection',\n  FUNDING_RECEIPT: 'org.hypercerts.claim.fundingReceipt',\n  WORK_SCOPE_TAG: 'org.hypercerts.claim.workScopeTag',\n  CERTIFIED_PROFILE: 'app.certified.profile',\n  BSKY_PROFILE: 'app.bsky.actor.profile',\n} as const;\n```\n\n**IMPORTANT**: Verify the exact NSID strings by checking the SDK source or the lexicons/ directory. The values above are best guesses — cross-reference with `node_modules/@hypercerts-org/sdk-core` or the generated lexicon files in `lexicons/` to get the exact strings. Run `grep -r 'org.hypercerts' lexicons/ | head -20` and check the SDK source.\n\n### Also\n- The file also imports `OAuthClientMetadataInput` from `@atproto/oauth-client-node` — keep that import unchanged.\n- The file also imports `generateBrandingCss` from `./atproto-branding` — keep that unchanged.\n- Export HYPERCERT_COLLECTIONS so other files can import it from `@/lib/config` instead of from the SDK.\n- The existing `OAUTH_SCOPE` export (line 193) uses `GRANULAR_SCOPE` which references `ATPROTO_SCOPE` — this should continue to work with the local constant.\n\n## Don't\n- Don't change any other logic in config.ts\n- Don't modify the buildClientMetadata function\n- Don't change any other imports","acceptance_criteria":"1. lib/config.ts has zero imports from @hypercerts-org/sdk-core\n2. HYPERCERT_COLLECTIONS is exported from lib/config.ts with correct NSID strings\n3. OAUTH_SCOPE still resolves to the same granular scope string as before\n4. pnpm build passes (ignoring errors in OTHER files that still import from sdk-core)","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T15:57:04.216191279+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:10:52.855831507+06:00","closed_at":"2026-03-03T16:10:52.855831507+06:00","close_reason":"ba00e2e Inline ATPROTO_SCOPE and HYPERCERT_COLLECTIONS constants in lib/config.ts","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-acu.1","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:57:04.217813139+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.10","title":"Verify build passes after auth layer migration — fix type errors in consumer files","description":"## Files\n- Any file that fails `pnpm build` due to auth migration changes (likely multiple)\n\n## What to do\nAfter tasks .1 through .9 are complete, run `pnpm build` and fix any remaining type errors caused by the auth migration. These will be in files that CONSUME the changed modules but weren't directly modified.\n\n### Expected issues:\n\n1. **Files using `ctx.scopedRepo.*`**: After repo-context.ts changes (task .7), `ctx.scopedRepo` no longer exists. These files (lib/create-actions.ts data operations, app/api/certs/route.ts, app/hypercerts/*.tsx pages) will have type errors. For this task, add `// @ts-expect-error -- Phase 2-4 migration` comments above each broken line OR use type assertions to suppress errors temporarily. Do NOT rewrite the data operations.\n\n2. **Files using `getAuthenticatedRepo()`**: After atproto-session.ts changes (task .6), this function no longer exists. Search for any remaining callers and replace with `getAgent()` or `getSession()` as appropriate. If a caller was using `repo.repo(did)` from the result, it needs a Phase 2+ rewrite — add a `// @ts-expect-error` comment.\n\n3. **Import path issues**: If lib/hypercerts-sdk.ts changed from default export to named export (getOAuthClient), all importers need updating.\n\n### Process:\n1. Run `pnpm build`\n2. For each error:\n   - If it's an auth-related error (wrong import, missing function): fix it properly\n   - If it's a data-operation error (scopedRepo, Repository type): suppress with `// @ts-expect-error -- migrating to native atproto in Phase 2-4`\n3. Repeat until `pnpm build` succeeds\n\n### Files likely to need @ts-expect-error suppressions:\n- lib/create-actions.ts (lines 56+ use ctx.scopedRepo)\n- app/api/certs/route.ts (uses ctx.scopedRepo.hypercerts.create/update)\n- app/api/certs/add-location/route.ts (uses personalRepository.hypercerts.attachLocation)\n- app/api/certs/add-attachment/route.ts (if it exists)\n- app/hypercerts/page.tsx (uses ctx.scopedRepo.hypercerts.list)\n- app/hypercerts/[hypercertUri]/page.tsx (uses viewCtx.scopedRepo.hypercerts.get)\n- app/hypercerts/[hypercertUri]/edit/page.tsx (uses viewCtx.scopedRepo.hypercerts.get)\n\n## Don't\n- Don't rewrite data operations — just suppress type errors\n- Don't remove any functionality\n- Don't change any business logic\n- Don't add new features","acceptance_criteria":"1. pnpm build succeeds with exit code 0\n2. No imports of createATProtoSDK, ATProtoSDKConfig, or Repository from @hypercerts-org/sdk-core remain\n3. All @ts-expect-error comments include a note about Phase 2-4 migration\n4. Auth flows (login, callback, restore, logout) use NodeOAuthClient methods","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":60,"created_at":"2026-03-03T15:59:47.556472043+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:51:40.90822263+06:00","closed_at":"2026-03-03T16:51:40.90822263+06:00","close_reason":"01fd277 Fix type errors in consumer files after auth migration","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-acu.10","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:59:47.55780639+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.10","depends_on_id":"hypercerts-acu.4","type":"blocks","created_at":"2026-03-03T15:59:47.562441647+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.10","depends_on_id":"hypercerts-acu.5","type":"blocks","created_at":"2026-03-03T15:59:47.566465378+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.10","depends_on_id":"hypercerts-acu.6","type":"blocks","created_at":"2026-03-03T15:59:47.570487586+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.10","depends_on_id":"hypercerts-acu.7","type":"blocks","created_at":"2026-03-03T15:59:47.574401074+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.10","depends_on_id":"hypercerts-acu.8","type":"blocks","created_at":"2026-03-03T15:59:47.578671319+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.10","depends_on_id":"hypercerts-acu.9","type":"blocks","created_at":"2026-03-03T15:59:47.582827495+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.2","title":"Update redis-state-store.ts to import interfaces from @atproto/oauth-client-node","description":"## Files\n- lib/redis-state-store.ts (modify)\n\n## What to do\nChange line 1 from:\n```ts\nimport { SessionStore, StateStore } from '@hypercerts-org/sdk-core';\n```\nto:\n```ts\nimport type { NodeSavedSessionStore as SessionStore, NodeSavedStateStore as StateStore } from '@atproto/oauth-client-node';\n```\n\nThe SDK's `SessionStore` and `StateStore` are just re-exports of `NodeSavedSessionStore` and `NodeSavedStateStore` from `@atproto/oauth-client-node`. These are both `SimpleStore\u003cstring, NodeSavedSession\u003e` and `SimpleStore\u003cstring, NodeSavedState\u003e` respectively.\n\nThe classes `RedisStateStore`, `RedisSessionStore`, and `RedisEpdsStateStore` should continue to implement the same interfaces — the type signatures are identical.\n\nKeep the existing import of `NodeSavedSession` and `NodeSavedState` from `@atproto/oauth-client-node` on line 3 — just merge them into one import statement.\n\n## Don't\n- Don't change any class implementations\n- Don't change the Redis key prefixes or expiration values\n- Don't change the EpdsOAuthState interface","acceptance_criteria":"1. lib/redis-state-store.ts has zero imports from @hypercerts-org/sdk-core\n2. RedisStateStore implements StateStore (NodeSavedStateStore)\n3. RedisSessionStore implements SessionStore (NodeSavedSessionStore)\n4. TypeScript compiles this file without errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-03T15:57:15.802900599+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:10:26.286926034+06:00","closed_at":"2026-03-03T16:10:26.286926034+06:00","close_reason":"70ae0e3 Update redis-state-store.ts to import interfaces from @atproto/oauth-client-node","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-acu.2","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:57:15.804178808+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.3","title":"Replace lib/hypercerts-sdk.ts with NodeOAuthClient singleton","description":"## Files\n- lib/hypercerts-sdk.ts (modify — rewrite entirely)\n\n## What to do\nReplace the entire file. Currently it creates an SDK via `createATProtoSDK()`. Replace with a `NodeOAuthClient` instance from `@atproto/oauth-client-node`.\n\n### New implementation:\n```ts\nimport { NodeOAuthClient, JoseKey } from '@atproto/oauth-client-node';\nimport { buildClientMetadata, config, OAUTH_SCOPE } from './config';\nimport {\n  RedisSessionStore,\n  RedisStateStore,\n  RedisEpdsStateStore,\n} from './redis-state-store';\n\nexport const sessionStore = new RedisSessionStore();\nexport const stateStore = new RedisStateStore();\nexport const epdsStateStore = new RedisEpdsStateStore();\n\nexport { OAUTH_SCOPE };\n\nconst clientMetadata = buildClientMetadata();\n\nconst oauthClient = new NodeOAuthClient({\n  clientMetadata,\n  stateStore,\n  sessionStore,\n  handleResolver: config.handleResolver,\n  // keyset is needed for non-loopback (production) clients that use private_key_jwt\n  // For loopback, token_endpoint_auth_method is 'none' so keyset is optional\n  ...(config.jwkPrivate ? {\n    keyset: [await JoseKey.fromJWK(JSON.parse(config.jwkPrivate))],\n  } : {}),\n});\n\nexport default oauthClient;\n```\n\n### Key differences from the SDK wrapper:\n- `NodeOAuthClient` has `.authorize(handle)` → returns `URL` (not string). The login route will need to call `.toString()` or use the URL directly.\n- `NodeOAuthClient` has `.callback(params)` → returns `{ session: OAuthSession, state: string | null }`. The callback route currently expects just the session — it will need to destructure.\n- `NodeOAuthClient` has `.restore(did)` → returns `OAuthSession` (same as SDK's `restoreSession`).\n- `NodeOAuthClient` has `.revoke(did)` → same as SDK's `revokeSession`.\n- There is NO `.repository()` method — that was SDK-specific and will be handled in later phases.\n- There is NO `.resolveSessionPds()` — that will be handled in a separate task.\n\n### Top-level await\nThe `JoseKey.fromJWK()` call is async. Since this is a module-level singleton, use top-level await. Next.js supports this in server modules. If there are issues, wrap in a lazy initialization pattern:\n```ts\nlet _client: NodeOAuthClient | null = null;\nexport async function getOAuthClient(): Promise\u003cNodeOAuthClient\u003e {\n  if (!_client) {\n    _client = new NodeOAuthClient({ ... });\n  }\n  return _client;\n}\n```\nPrefer the top-level await approach first. Only fall back to lazy init if build fails.\n\n### Important: JoseKey import\n`JoseKey` is re-exported from `@atproto/oauth-client-node` (it re-exports from `@atproto/jwk-jose`). Check the actual export name — it might be `JoseKey` or might need to be imported differently. Run: `grep -r 'JoseKey\\|fromJWK' node_modules/@atproto/oauth-client-node/dist/` to confirm.\n\n## Don't\n- Don't import anything from @hypercerts-org/sdk-core\n- Don't add a .repository() method — that's Phase 2+\n- Don't change the exported names (sessionStore, stateStore, epdsStateStore, OAUTH_SCOPE must stay)","acceptance_criteria":"1. lib/hypercerts-sdk.ts has zero imports from @hypercerts-org/sdk-core\n2. Default export is a NodeOAuthClient instance (or async getter that returns one)\n3. oauthClient.authorize, oauthClient.callback, oauthClient.restore, oauthClient.revoke are available\n4. sessionStore, stateStore, epdsStateStore, OAUTH_SCOPE are still exported\n5. The file compiles without TypeScript errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-03T15:57:39.934899407+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:12:35.691409257+06:00","closed_at":"2026-03-03T16:12:35.691409257+06:00","close_reason":"9957157 Replace lib/hypercerts-sdk.ts with NodeOAuthClient singleton","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-acu.3","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:57:39.936516488+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.3","depends_on_id":"hypercerts-acu.1","type":"blocks","created_at":"2026-03-03T15:57:39.939609908+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.3","depends_on_id":"hypercerts-acu.2","type":"blocks","created_at":"2026-03-03T15:57:39.94279206+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.4","title":"Update OAuth login route to use NodeOAuthClient","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n\n## What to do\nUpdate the login route to use the new NodeOAuthClient instead of the SDK.\n\n### Current code:\n```ts\nimport sdk from '@/lib/hypercerts-sdk';\n// ...\nconst authUrl = await sdk.authorize(handle);\nreturn NextResponse.json({ authUrl });\n```\n\n### New code:\n```ts\nimport oauthClient from '@/lib/hypercerts-sdk';\n// ...\nconst authUrl = await oauthClient.authorize(handle);\nreturn NextResponse.json({ authUrl: authUrl.toString() });\n```\n\nKey difference: `NodeOAuthClient.authorize()` returns a `URL` object, not a string. The frontend expects a string in the JSON response, so call `.toString()` on it.\n\nIf lib/hypercerts-sdk.ts exports an async getter instead of a direct default export, adjust accordingly:\n```ts\nimport { getOAuthClient } from '@/lib/hypercerts-sdk';\n// ...\nconst oauthClient = await getOAuthClient();\nconst authUrl = await oauthClient.authorize(handle);\n```\n\n## Don't\n- Don't change error handling logic\n- Don't change request body parsing\n- Don't change the response shape (must still return { authUrl: string })","acceptance_criteria":"1. app/api/oauth/login/route.ts has zero imports from @hypercerts-org/sdk-core\n2. The route calls oauthClient.authorize(handle) and returns { authUrl: string }\n3. File compiles without TypeScript errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-03T15:57:49.93786361+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:13:58.546359944+06:00","closed_at":"2026-03-03T16:13:58.546359944+06:00","close_reason":"d283144 Use NodeOAuthClient in login route, call .toString() on URL","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-acu.4","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:57:49.939379315+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.4","depends_on_id":"hypercerts-acu.3","type":"blocks","created_at":"2026-03-03T15:57:49.942725332+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.5","title":"Update OAuth callback route to use NodeOAuthClient","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\nUpdate the callback route to use the new NodeOAuthClient instead of the SDK.\n\n### Current code:\n```ts\nimport sdk from '@/lib/hypercerts-sdk';\n// ...\nconst [session, cookieStore] = await Promise.all([\n  sdk.callback(searchParams),\n  cookies(),\n]);\ncookieStore.set('user-did', session.did, { ... });\n```\n\n### New code:\n```ts\nimport oauthClient from '@/lib/hypercerts-sdk';\n// ...\nconst [result, cookieStore] = await Promise.all([\n  oauthClient.callback(searchParams),\n  cookies(),\n]);\nconst { session } = result;\ncookieStore.set('user-did', session.did, { ... });\n```\n\nKey difference: `NodeOAuthClient.callback()` returns `{ session: OAuthSession, state: string | null }` instead of just the session. Destructure to get the session.\n\nThe `session.did` property should still be available on `OAuthSession` — it's the same type the SDK was wrapping.\n\nIf lib/hypercerts-sdk.ts exports an async getter, adjust the import accordingly.\n\n## Don't\n- Don't change error handling or redirect logic\n- Don't change cookie settings\n- Don't change the return_to / open redirect protection logic","acceptance_criteria":"1. app/api/oauth/callback/route.ts has zero imports from @hypercerts-org/sdk-core\n2. The route destructures { session } from oauthClient.callback() result\n3. Cookie is set with session.did\n4. File compiles without TypeScript errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-03T15:57:59.703657133+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:21:41.578988809+06:00","closed_at":"2026-03-03T16:21:41.578988809+06:00","close_reason":"4f56100 Use NodeOAuthClient in OAuth callback route","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-acu.5","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:57:59.705507983+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.5","depends_on_id":"hypercerts-acu.3","type":"blocks","created_at":"2026-03-03T15:57:59.708794896+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.6","title":"Rewrite lib/atproto-session.ts to return OAuthSession + Agent instead of Repository","description":"## Files\n- lib/atproto-session.ts (modify — rewrite)\n\n## What to do\nReplace the current implementation that returns `Repository` (SDK type) with one that returns `OAuthSession` and an `Agent` from `@atproto/api`.\n\n### Current exports:\n- `getAuthenticatedRepo()` → `Promise\u003cRepository | null\u003e` — calls `sdk.restoreSession(did)` then `sdk.repository(session, { server: 'pds' })`\n- `getSession()` → `Promise\u003cOAuthSession | null\u003e` — calls `sdk.restoreSession(did)`\n\n### New exports:\n- `getSession()` → `Promise\u003cOAuthSession | null\u003e` — calls `oauthClient.restore(did)` (note: method is `restore`, not `restoreSession`)\n- `getAgent()` → `Promise\u003cAgent | null\u003e` — restores session, then creates an Agent. The OAuthSession from NodeOAuthClient has a `fetchHandler` that can be used with Agent. Specifically, `OAuthSession` extends `CredentialSession` which has a `fetchHandler`. You can create an Agent like: `new Agent(session)` where session is the OAuthSession.\n- Remove `getAuthenticatedRepo()` entirely — it returns a Repository which is an SDK concept.\n\n### New implementation:\n```ts\nimport 'server-only';\nimport { cache } from 'react';\nimport { cookies } from 'next/headers';\nimport { Agent } from '@atproto/api';\nimport type { OAuthSession } from '@atproto/oauth-client-node';\nimport oauthClient from '@/lib/hypercerts-sdk';\n\nexport type { OAuthSession };\n\nexport const getSession = cache(async function getSession(): Promise\u003cOAuthSession | null\u003e {\n  const cookieStore = await cookies();\n  const userDid = cookieStore.get('user-did')?.value;\n  if (!userDid) return null;\n\n  try {\n    return await oauthClient.restore(userDid);\n  } catch (error) {\n    console.error(`Failed to restore session for DID ${userDid}:`, error);\n    return null;\n  }\n});\n\nexport const getAgent = cache(async function getAgent(): Promise\u003cAgent | null\u003e {\n  const session = await getSession();\n  if (!session) return null;\n  return new Agent(session);\n});\n```\n\n### Callers that need updating (in OTHER tasks):\n- `lib/repo-context.ts` uses `getAuthenticatedRepo()` — will be updated in task .7\n- `lib/create-actions.ts` uses `getSession()` — signature unchanged\n- `app/api/oauth/logout/route.ts` uses `getSession()` — signature unchanged\n\n## Don't\n- Don't import anything from @hypercerts-org/sdk-core\n- Don't keep getAuthenticatedRepo() — remove it entirely\n- Don't add any data operation methods","acceptance_criteria":"1. lib/atproto-session.ts has zero imports from @hypercerts-org/sdk-core\n2. Exports getSession() returning Promise\u003cOAuthSession | null\u003e\n3. Exports getAgent() returning Promise\u003cAgent | null\u003e\n4. getAuthenticatedRepo is removed\n5. File compiles without TypeScript errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T15:58:17.905751858+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:14:01.964737641+06:00","closed_at":"2026-03-03T16:14:01.964737641+06:00","close_reason":"36b8915 Rewrite lib/atproto-session.ts to return OAuthSession + Agent instead of Repository","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-acu.6","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:58:17.907227406+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.6","depends_on_id":"hypercerts-acu.3","type":"blocks","created_at":"2026-03-03T15:58:17.910828624+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.7","title":"Rewrite lib/repo-context.ts to return Agent + DIDs instead of Repository/scopedRepo","description":"## Files\n- lib/repo-context.ts (modify — rewrite)\n\n## What to do\nReplace the Repository/scopedRepo-based context with a simpler Agent + DID context. The `scopedRepo` concept was SDK-specific — in the native atproto world, you just pass the target DID to each `com.atproto.repo.*` call.\n\n### Current interface:\n```ts\ninterface RepoContext {\n  userDid: string;\n  activeDid: string;\n  targetDid: string;\n  server: RepoServer;\n  repository: Repository;        // SDK type\n  scopedRepo: ReturnType\u003cRepository['repo']\u003e;  // SDK type\n}\n```\n\n### New interface:\n```ts\nexport interface RepoContext {\n  userDid: string;\n  activeDid: string;\n  targetDid: string;\n  agent: Agent;\n}\n```\n\nRemove the `RepoServer` type and `server` field — there's only one server (PDS), and the Agent is already configured to talk to it via the session.\n\n### New implementation:\n```ts\nimport 'server-only';\nimport { cache } from 'react';\nimport { cookies } from 'next/headers';\nimport { Agent } from '@atproto/api';\nimport oauthClient from '@/lib/hypercerts-sdk';\n\nexport interface RepoContextOptions {\n  targetDid?: string;\n}\n\nexport interface RepoContext {\n  userDid: string;\n  activeDid: string;\n  targetDid: string;\n  agent: Agent;\n}\n\nexport const getRepoContext = cache(async function getRepoContext(\n  options: RepoContextOptions = {},\n): Promise\u003cRepoContext | null\u003e {\n  const cookieStore = await cookies();\n\n  const userDid = cookieStore.get('user-did')?.value;\n  if (!userDid) {\n    console.warn('[getRepoContext] No user-did cookie found');\n    return null;\n  }\n\n  const activeDid = cookieStore.get('active-did')?.value || userDid;\n  const targetDid = options.targetDid || activeDid;\n\n  try {\n    const session = await oauthClient.restore(userDid);\n    if (!session) return null;\n    const agent = new Agent(session);\n    return { userDid, activeDid, targetDid, agent };\n  } catch (error) {\n    console.error(\n      `Failed to build repo context (userDid=${userDid}, targetDid=${targetDid}):`,\n      error,\n    );\n    return null;\n  }\n});\n```\n\n### Impact on callers (handled in later phases):\nMany files use `ctx.scopedRepo.hypercerts.*` and `ctx.scopedRepo.records.*` — these will break at the type level. That's expected. They will be migrated in Phase 2-4 to use `ctx.agent.com.atproto.repo.*` with `ctx.targetDid`.\n\n## Don't\n- Don't import anything from @hypercerts-org/sdk-core\n- Don't keep the Repository type or scopedRepo\n- Don't keep the RepoServer type or server field\n- Don't try to fix callers of ctx.scopedRepo — that's Phase 2-4","acceptance_criteria":"1. lib/repo-context.ts has zero imports from @hypercerts-org/sdk-core\n2. RepoContext interface has agent: Agent, userDid, activeDid, targetDid (no repository, no scopedRepo, no server)\n3. getRepoContext creates an Agent from the restored session\n4. File compiles without TypeScript errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T15:58:36.278452808+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:13:48.42039631+06:00","closed_at":"2026-03-03T16:13:48.42039631+06:00","close_reason":"1f88a32 Rewrite lib/repo-context.ts to return Agent + DIDs instead of Repository/scopedRepo","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-acu.7","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:58:36.280419521+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.7","depends_on_id":"hypercerts-acu.3","type":"blocks","created_at":"2026-03-03T15:58:36.285527948+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.8","title":"Update lib/server-utils.ts to resolve PDS URL without SDK","description":"## Files\n- lib/server-utils.ts (modify)\n\n## What to do\nReplace the SDK's `sdk.resolveSessionPds(session)` with reading the PDS URL from the OAuthSession's token info.\n\n### Current code:\n```ts\nimport sdk from './hypercerts-sdk';\nimport type { OAuthSession } from '@atproto/oauth-client-node';\n\nexport async function resolveSessionPds(session: OAuthSession): Promise\u003cstring\u003e {\n  try {\n    const pdsUrl = await sdk.resolveSessionPds(session);\n    return pdsUrl.endsWith('/') ? pdsUrl.slice(0, -1) : pdsUrl;\n  } catch (error) {\n    console.warn('Failed to resolve PDS URL from session, falling back to CDN:', error);\n    return 'https://cdn.bsky.app';\n  }\n}\n```\n\n### Why we need per-session PDS resolution:\nUsers can log in from any PDS (e.g., bsky.social, climateai.org). Their data lives on their PDS, not necessarily on NEXT_PUBLIC_PDS_URL. The `issuer` is the authorization server — the `aud` (audience) is the resource server (PDS) where the user's repo actually lives.\n\n### How to get it:\n`OAuthSession` has `getTokenInfo()` which returns `TokenInfo` containing `aud: string` — this is the PDS URL (resource server).\n\n```ts\ntype TokenInfo = {\n  expiresAt?: Date;\n  expired?: boolean;\n  scope: AtprotoOAuthScope;\n  iss: string;   // authorization server\n  aud: string;   // resource server (PDS) ← this is what we want\n  sub: AtprotoDid;\n};\n```\n\n### New implementation:\n```ts\nimport 'server-only';\nimport type { OAuthSession } from '@atproto/oauth-client-node';\n\nexport async function resolveSessionPds(session: OAuthSession): Promise\u003cstring\u003e {\n  try {\n    const tokenInfo = await session.getTokenInfo();\n    const pdsUrl = tokenInfo.aud;\n    return pdsUrl.endsWith('/') ? pdsUrl.slice(0, -1) : pdsUrl;\n  } catch (error) {\n    console.warn('Failed to resolve PDS URL from session, falling back to CDN:', error);\n    return 'https://cdn.bsky.app';\n  }\n}\n```\n\n## Don't\n- Don't import from @hypercerts-org/sdk-core\n- Don't import from ./hypercerts-sdk (no longer needed)\n- Don't use session.serverMetadata.issuer — that's the auth server, not the PDS\n- Don't use process.env.NEXT_PUBLIC_PDS_URL — users may be on different PDSes\n- Don't change the function signature or return type\n- Don't remove the 'server-only' import","acceptance_criteria":"1. lib/server-utils.ts has zero imports from @hypercerts-org/sdk-core or ./hypercerts-sdk\n2. resolveSessionPds still accepts OAuthSession and returns Promise\u003cstring\u003e\n3. Returns a valid PDS URL (from env var or fallback)\n4. File compiles without TypeScript errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-03T15:58:59.093972796+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:14:02.49698953+06:00","closed_at":"2026-03-03T16:14:02.49698953+06:00","close_reason":"36b8915 resolve PDS URL via session.getTokenInfo().aud","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-acu.8","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:58:59.09576308+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.8","depends_on_id":"hypercerts-acu.3","type":"blocks","created_at":"2026-03-03T15:58:59.100603833+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-acu.9","title":"Update lib/create-actions.ts auth imports — replace sdk.revokeSession and RepositoryRole","description":"## Files\n- lib/create-actions.ts (modify — partial, auth-only changes)\n\n## What to do\nThis file has two SDK auth-related imports that need updating. Do NOT touch the data operation code (addContribution, addEvaluation, etc.) — those are Phase 2-4.\n\n### Change 1: Replace sdk.revokeSession\nCurrent (line 9, 53):\n```ts\nimport sdk from './hypercerts-sdk';\n// ...\nexport const logout = async () =\u003e {\n  const session = await getSession();\n  if (!session) return;\n  sdk.revokeSession(session.sub);\n};\n```\n\nNew:\n```ts\nimport oauthClient from './hypercerts-sdk';\n// ...\nexport const logout = async () =\u003e {\n  const session = await getSession();\n  if (!session) return;\n  oauthClient.revoke(session.sub);\n};\n```\n\nNote: The method is `revoke` not `revokeSession` on NodeOAuthClient. And `session.sub` should work — OAuthSession has a `sub` property (the DID). If the import is async (getOAuthClient), adjust accordingly.\n\n### Change 2: Replace RepositoryRole import\nCurrent (line 6):\n```ts\nimport { RepositoryRole } from '@hypercerts-org/sdk-core';\n```\n\nThis is used in the `GrantAccessParams` interface (line 14):\n```ts\nexport interface GrantAccessParams {\n  repoDid: string;\n  userDid: string;\n  role: RepositoryRole;\n}\n```\n\nFor now, replace `RepositoryRole` with a simple string union type:\n```ts\nexport type RepositoryRole = 'admin' | 'writer' | 'reader';\n```\n\nThis is a temporary placeholder — the GrantAccessParams interface may be removed or reworked in later phases.\n\n### What NOT to change:\n- Lines 56+ contain data operations (addContribution, addEvaluation, createHypercert, etc.) that use `ctx.scopedRepo.*`. These will have type errors after repo-context.ts is updated (task .7). That's expected — do NOT fix them in this task.\n- The `getRepoContext` import stays.\n- The `getSession` import stays.\n\n## Don't\n- Don't touch any function after line 55 (the data operations)\n- Don't try to fix scopedRepo type errors — those are Phase 2-4\n- Don't remove the getRepoContext import","acceptance_criteria":"1. lib/create-actions.ts does not import sdk from ./hypercerts-sdk for revokeSession (uses oauthClient.revoke instead)\n2. lib/create-actions.ts does not import RepositoryRole from @hypercerts-org/sdk-core\n3. logout() calls oauthClient.revoke(session.sub)\n4. GrantAccessParams.role is typed as a local RepositoryRole string union\n5. No changes to data operation functions (addContribution, etc.)","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-03T15:59:19.436716449+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T16:21:26.055252101+06:00","closed_at":"2026-03-03T16:21:26.055252101+06:00","close_reason":"25d7dc4 Replace sdk.revokeSession with oauthClient.revoke and local RepositoryRole type","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-acu.9","depends_on_id":"hypercerts-acu","type":"parent-child","created_at":"2026-03-03T15:59:19.438450014+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.9","depends_on_id":"hypercerts-acu.3","type":"blocks","created_at":"2026-03-03T15:59:19.443348889+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-acu.9","depends_on_id":"hypercerts-acu.6","type":"blocks","created_at":"2026-03-03T15:59:19.448566406+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-akx","title":"Fix: Unguarded JSON.parse in /api/certs/create can crash server on malformed input","description":"## Problem\nIn `app/api/certs/create/route.ts` lines 28-31, `JSON.parse()` is called on user-supplied form fields without a try/catch:\n\n```ts\nconst rights = rightsRaw ? JSON.parse(rightsRaw) : undefined;\nconst contributions = contributionsRaw ? JSON.parse(contributionsRaw) : undefined;\n```\n\nIf a client sends malformed JSON (e.g. `rights=not-json`), `JSON.parse` throws a `SyntaxError` that propagates to the outer catch block and returns a 500 with the raw error message. This leaks internal stack details and returns the wrong status code — it should be 400 (Bad Request), not 500 (Server Error).\n\n## Fix\nWrap each parse in a try/catch and return 400 on failure:\n```ts\nlet rights: unknown;\ntry {\n  rights = rightsRaw ? JSON.parse(rightsRaw) : undefined;\n} catch {\n  return NextResponse.json({ error: 'Invalid JSON in rights field' }, { status: 400 });\n}\n```\nApply the same pattern to `contributionsRaw`.\n\n## Evidence\n`app/api/certs/create/route.ts` lines 28-31 — no try/catch around JSON.parse on user input.","status":"open","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:07.027183117+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:07.027183117+06:00"}
{"id":"hypercerts-amn","title":"Remove build step from CI workflow, keep only lint and format check","description":"## Files\n- .github/workflows/ci.yml (modify)\n\n## What to do\nThe CI workflow currently runs `pnpm build` which fails because it requires environment variables (ATPROTO_JWK_PRIVATE, REDIS_HOST, etc.) that are not available in CI.\n\nRemove the build step entirely. Keep only lint and format check.\n\n### Current steps to keep:\n1. Checkout\n2. Install pnpm (via pnpm/action-setup@v4)\n3. Setup Node.js 20 with pnpm cache\n4. pnpm install --frozen-lockfile\n5. pnpm lint\n6. pnpm format:check\n\n### Step to REMOVE:\n- The \"Build\" step (`pnpm build`) — delete it entirely\n\n### Also update the job name:\n- Change `name: Build, Lint \u0026 Format Check` to `name: Lint \u0026 Format Check`\n\n## Don't\n- Change the trigger conditions (push to main, PR to main)\n- Change the concurrency settings\n- Change the Node.js version or pnpm setup\n- Add any new steps\n- Touch any other files","acceptance_criteria":"1. The CI workflow file at .github/workflows/ci.yml has NO build step (no `pnpm build` or `next build`)\n2. The workflow still runs `pnpm lint` and `pnpm format:check`\n3. The job name says \"Lint \u0026 Format Check\" (no \"Build\")\n4. The workflow YAML is valid (proper indentation, no syntax errors)\n5. All other workflow settings (triggers, concurrency, checkout, pnpm setup, node setup, install) are unchanged","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","estimated_minutes":10,"created_at":"2026-02-17T20:35:51.395690311+06:00","created_by":"kzoeps","updated_at":"2026-02-17T20:36:51.469815209+06:00","closed_at":"2026-02-17T20:36:51.469815209+06:00","close_reason":"d90661f remove build step from CI workflow","labels":["scope:trivial"]}
{"id":"hypercerts-ars","title":"Epic: Resolve CodeRabbit review for PR #11","description":"Resolve all open CodeRabbit inline review comments on PR #11 (feat: Add hypercert editing, deletion, and management capabilities). 6 tasks from 9 comments across 8 files. 3 comments skipped (2 already resolved, 1 duplicate).","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-02T10:54:34.935441725+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T16:21:03.943904305+06:00","closed_at":"2026-03-02T16:21:03.943904305+06:00","close_reason":"174e8de all CodeRabbit review comments for PR #11 resolved","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-ars.1","title":"Fix date handling bugs in hypercerts edit form","description":"## Files\n- components/hypercerts-edit-form.tsx (modify)\n\n## What to do\nTwo bugs in date handling:\n\n### Bug 1: UTC conversion shifts calendar day\n`toDateString` on line 82 uses `d.toISOString().slice(0, 10)` which converts to UTC before extracting the date portion. A date like `2026-01-15T00:00:00+14:00` becomes `2026-01-14` in UTC.\n\nFix: Replace `toDateString` (line 82) to use local date extraction:\n```ts\nconst toDateString = (d: Date): string =\u003e {\n  const y = d.getFullYear();\n  const m = String(d.getMonth() + 1).padStart(2, \"0\");\n  const day = String(d.getDate()).padStart(2, \"0\");\n  return `${y}-${m}-${day}`;\n};\n```\n\nAlso fix `recordDateString` (line 86-92) — when it falls through to `new Date(raw).toISOString().slice(0, 10)`, it has the same UTC shift bug. Replace with local extraction:\n```ts\nconst recordDateString = (raw: string | undefined | null): string =\u003e {\n  if (!raw) return \"\";\n  if (/^\\d{4}-\\d{2}-\\d{2}$/.test(raw)) return raw;\n  const d = new Date(raw);\n  const y = d.getFullYear();\n  const m = String(d.getMonth() + 1).padStart(2, \"0\");\n  const day = String(d.getDate()).padStart(2, \"0\");\n  return `${y}-${m}-${day}`;\n};\n```\n\n### Bug 2: Clearing dates is not persisted\nLines 94-99 (`isPristine`) treat falsy dates as \"pristine\" via `(!startDate || ...)`. If a user clears a date, the form thinks nothing changed.\n\nFix the `isPristine` check (lines 94-101):\n```ts\nconst isPristine =\n  title === (record.title ?? \"\") \u0026\u0026\n  shortDescription === (record.shortDescription ?? \"\") \u0026\u0026\n  (startDate ? toDateString(startDate) : \"\") === recordDateString(record.startDate) \u0026\u0026\n  (endDate ? toDateString(endDate) : \"\") === recordDateString(record.endDate) \u0026\u0026\n  !newImage \u0026\u0026\n  !removeImage;\n```\n\nFix the update payload builder (lines 120-126) to allow cleared dates:\n```ts\nconst nextStart = startDate ? toDateString(startDate) : \"\";\nconst prevStart = recordDateString(record.startDate);\nif (nextStart !== prevStart) {\n  updates.startDate = nextStart || null;\n}\n\nconst nextEnd = endDate ? toDateString(endDate) : \"\";\nconst prevEnd = recordDateString(record.endDate);\nif (nextEnd !== prevEnd) {\n  updates.endDate = nextEnd || null;\n}\n```\n\n## Dont\n- Do not change any other form fields or logic outside date handling\n- Do not modify the DatePicker component itself\n- Do not change the form submission flow beyond the date update payload\n- Do not add new dependencies","acceptance_criteria":"1. `toDateString(new Date(\"2026-01-15T00:00:00+14:00\"))` returns `\"2026-01-15\"` (not `\"2026-01-14\"`)\n2. `recordDateString(\"2026-01-15T00:00:00+14:00\")` returns `\"2026-01-15\"` (not `\"2026-01-14\"`)\n3. When a user clears startDate (sets to null) that previously had a value, `isPristine` is `false`\n4. When a user clears startDate, the update payload includes `startDate: null`\n5. TypeScript compiles with no errors: `npx tsc --noEmit`\n6. Lint passes: `npx eslint components/hypercerts-edit-form.tsx`","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-02T10:55:00.992446591+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T11:15:40.55265622+06:00","closed_at":"2026-03-02T11:15:40.55265622+06:00","close_reason":"722067d Fix date handling bugs in hypercerts edit form","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.1","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T10:55:00.995508566+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.10","title":"Fix: recordDateString still broken for UTC ISO strings in negative-offset timezones (from hypercerts-ars.1)","description":"## Problem\n\nReview of hypercerts-ars.1 (commit 722067d) found that `recordDateString` is still timezone-dependent for full UTC ISO strings.\n\nThe fix added a regex guard for date-only strings (`/^\\d{4}-\\d{2}-\\d{2}$/`) which correctly returns them as-is. However, when `record.startDate` is a full UTC ISO string like `\"2024-01-01T00:00:00Z\"` (a format the SDK uses — see SDK docs examples), the fallback path does:\n\n```ts\nconst d = new Date(\"2024-01-01T00:00:00Z\"); // UTC midnight\nd.getDate(); // returns 31 in UTC-5 timezone → \"2023-12-31\" ✗\n```\n\nThis is the same class of bug as the original: local time extraction from a UTC-anchored Date is still timezone-dependent. In any UTC-N timezone, UTC midnight becomes the previous calendar day locally.\n\n## Evidence\n\nThe `@hypercerts-org/sdk-core` type docs show both formats in use:\n- `startDate: \"2024-01-01T00:00:00Z\"` (full UTC ISO — used in older records)\n- `startDate: \"2024-01-01\"` (date-only — handled correctly by the regex guard)\n\nThe acceptance criterion `recordDateString(\"2026-01-15T00:00:00+14:00\") returns \"2026-01-15\"` only passes in UTC+14 timezone. In UTC+0 or any UTC-N timezone it returns `\"2026-01-14\"`.\n\n## Fix\n\nExtend the regex guard to also handle ISO strings by extracting the date portion directly from the string (no `new Date()` parsing needed):\n\n```ts\nconst recordDateString = (raw: string | undefined | null): string =\u003e {\n  if (!raw) return \"\";\n  // Date-only string — return as-is\n  if (/^\\d{4}-\\d{2}-\\d{2}$/.test(raw)) return raw;\n  // ISO string with time component — extract date portion from the string directly\n  // (avoids timezone-dependent local extraction)\n  const isoMatch = raw.match(/^(\\d{4}-\\d{2}-\\d{2})T/);\n  if (isoMatch) return isoMatch[1];\n  // Fallback for other formats (unusual) — local extraction\n  const d = new Date(raw);\n  const y = d.getFullYear();\n  const m = String(d.getMonth() + 1).padStart(2, \"0\");\n  const day = String(d.getDate()).padStart(2, \"0\");\n  return `${y}-${m}-${day}`;\n};\n```\n\n## File\n- `components/hypercerts-edit-form.tsx`","acceptance_criteria":"1. `recordDateString(\"2024-01-01T00:00:00Z\")` returns `\"2024-01-01\"` regardless of the browser/server timezone\n2. `recordDateString(\"2026-01-15T00:00:00+14:00\")` returns `\"2026-01-15\"` regardless of timezone\n3. `recordDateString(\"2024-01-01\")` still returns `\"2024-01-01\"` (date-only path unchanged)\n4. `recordDateString(null)` returns `\"\"`\n5. TypeScript compiles: `npx tsc --noEmit`\n6. Lint passes: `npx eslint components/hypercerts-edit-form.tsx`","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-02T15:37:42.44321992+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T16:05:30.221984978+06:00","closed_at":"2026-03-02T16:05:30.221984978+06:00","close_reason":"6d65a4d fix: extract ISO date portion from string to avoid timezone shift","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.10","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T15:37:42.444874723+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-ars.10","depends_on_id":"hypercerts-ars.1","type":"discovered-from","created_at":"2026-03-02T15:37:45.961297518+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.11","title":"Fix: DatePicker has no clear mechanism — isPristine date-clearing fix is dead code (from hypercerts-ars.1)","description":"## Problem\n\nReview of hypercerts-ars.1 (commit 722067d) found that the `isPristine` fix and null-sending update payload for cleared dates are logically correct but unreachable via the UI.\n\nThe `DatePicker` component (`components/date-range-picker.tsx`) has `onChange: (date: Date) =\u003e void` — it can only call `onChange` with a `Date` object, never with `null` or `undefined`. The Calendar's `onSelect` uses a non-null assertion (`onChange(date!)`), so even clicking an already-selected date to deselect it would pass `undefined` (not `null`) to `setStartDate`, causing a type mismatch.\n\nThere is no \"Clear\" button or reset mechanism in the DatePicker. This means:\n- A user who has a start/end date set cannot clear it via the edit form\n- The `isPristine` check for cleared dates (`(startDate ? toDateString(startDate) : \"\") === recordDateString(record.startDate)`) is correct but the null branch is never reached\n- The update payload `updates.startDate = nextStart || null` is correct but the `null` path is dead code\n\n## Evidence\n\n`components/date-range-picker.tsx` line 18: `onChange: (date: Date) =\u003e void`\n`components/date-range-picker.tsx` line 46: `onChange(date!)` — non-null assertion, no clear path\n\n`components/hypercerts-edit-form.tsx` line 225: `onChange={setStartDate}` — `setStartDate` is `Dispatch\u003cSetStateAction\u003cDate | null\u003e\u003e`, but DatePicker will never call it with `null`\n\n## Fix\n\nAdd a \"Clear\" button to the DatePicker (or alongside it in the edit form) that calls `setStartDate(null)`. The simplest approach is to add an optional `onClear` prop to `DatePicker` and render a clear button when a date is selected:\n\n```ts\n// In date-range-picker.tsx\nexport interface DatePickerProps {\n  label: string;\n  initDate?: Date | undefined;\n  onChange: (date: Date | null) =\u003e void; // widen to allow null\n}\n// Add a \"Clear\" button in the popover or next to the trigger\n```\n\nOr alternatively, add a clear button directly in the edit form next to each DatePicker.\n\n## Files\n- `components/date-range-picker.tsx` (add clear mechanism)\n- `components/hypercerts-edit-form.tsx` (wire up clear to setStartDate(null) / setEndDate(null))","acceptance_criteria":"1. The edit form has a visible way to clear a previously-set start date (e.g. a clear button)\n2. Clicking clear sets `startDate` state to `null`\n3. When a date is cleared, `isPristine` is `false` (the existing logic already handles this correctly once null is reachable)\n4. When a date is cleared and saved, the update payload includes `startDate: null` (the existing logic already handles this correctly)\n5. TypeScript compiles: `npx tsc --noEmit`\n6. Lint passes: `npx eslint components/date-range-picker.tsx components/hypercerts-edit-form.tsx`","status":"closed","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-02T15:38:06.81954868+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T16:04:03.21072942+06:00","closed_at":"2026-03-02T16:04:03.21072942+06:00","close_reason":"0000000 skip: feature gap out of epic scope","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.11","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T15:38:06.821145615+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-ars.11","depends_on_id":"hypercerts-ars.1","type":"discovered-from","created_at":"2026-03-02T15:38:10.244540687+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.2","title":"Add DID ownership guard to deleteRecord","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\nThe `deleteRecord` function (lines 276-292) parses an AT-URI and deletes by `{ collection, rkey }` without verifying that `parsed.did` matches the active session DID. This means a crafted URI could target records in the wrong repo.\n\nNote: `deleteHypercert` (line 224-242) already has this guard — mirror the same pattern.\n\nAdd a DID ownership check after the existing URI validation (line 284-286), before the delete call (line 287):\n\n```ts\nif (parsed.did !== ctx.activeDid) {\n  throw new Error(\n    \"deleteRecord failed: Forbidden — URI DID does not match active session DID.\",\n  );\n}\n```\n\nThe full function should look like:\n```ts\nexport const deleteRecord = async (params: { recordUri: string }) =\u003e {\n  const ctx = await getRepoContext();\n  if (!ctx) {\n    throw new Error(\"deleteRecord failed: could not establish repository context.\");\n  }\n  const parsed = parseAtUri(params.recordUri);\n  if (!parsed || !parsed.collection || !parsed.rkey) {\n    throw new Error(\"deleteRecord failed: invalid AT-URI format\");\n  }\n  if (parsed.did !== ctx.activeDid) {\n    throw new Error(\n      \"deleteRecord failed: Forbidden — URI DID does not match active session DID.\",\n    );\n  }\n  await ctx.scopedRepo.records.delete({\n    collection: parsed.collection,\n    rkey: parsed.rkey,\n  });\n  return { success: true };\n};\n```\n\n## Dont\n- Do not modify any other function in create-actions.ts\n- Do not change the deleteHypercert function\n- Do not add new imports or dependencies\n- Do not modify the error message format of existing checks","acceptance_criteria":"1. `deleteRecord({ recordUri: \"at://did:plc:other/collection/rkey\" })` throws \"Forbidden — URI DID does not match active session DID\" when ctx.activeDid is different\n2. `deleteRecord({ recordUri: \"at://did:plc:self/collection/rkey\" })` proceeds to delete when ctx.activeDid matches\n3. TypeScript compiles with no errors: `npx tsc --noEmit`\n4. Lint passes: `npx eslint lib/create-actions.ts`","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-02T10:55:05.704996298+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T10:57:14.813996099+06:00","closed_at":"2026-03-02T10:57:14.813996099+06:00","close_reason":"ee961d5 Add DID ownership guard to deleteRecord","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.2","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T10:55:05.708138107+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.3","title":"Return 400 for malformed JSON in certs POST route","description":"## Files\n- app/api/certs/route.ts (modify)\n\n## What to do\nLines 29-32 in the POST handler parse `rightsRaw` and `contributionsRaw` with `JSON.parse()` directly. If the JSON is malformed, it throws and falls into the global catch on line 56, returning a 500. This should be a 400 (client error).\n\nAdd a safe JSON parse helper at the top of the POST function body (inside the try block, before line 29):\n\n```ts\nconst parseOptionalJson = \u003cT\u003e(raw: string | null, field: string): T | undefined =\u003e {\n  if (!raw) return undefined;\n  try {\n    return JSON.parse(raw) as T;\n  } catch {\n    throw new Error(`INVALID_JSON:${field}`);\n  }\n};\n```\n\nReplace lines 29-32:\n```ts\n// Before:\nconst rights = rightsRaw ? JSON.parse(rightsRaw) : undefined;\nconst contributions = contributionsRaw ? JSON.parse(contributionsRaw) : undefined;\n\n// After:\nconst rights = parseOptionalJson(rightsRaw, \"rights\");\nconst contributions = parseOptionalJson(contributionsRaw, \"contributions\");\n```\n\nUpdate the catch block (line 56-61) to detect the sentinel and return 400:\n```ts\n} catch (e) {\n  if (e instanceof Error \u0026\u0026 e.message.startsWith(\"INVALID_JSON:\")) {\n    const field = e.message.split(\":\")[1];\n    return NextResponse.json(\n      { error: `Invalid JSON in ${field}` },\n      { status: 400 },\n    );\n  }\n  console.error(\"Error creating hypercert:\", e);\n  return NextResponse.json(\n    { error: \"Failed to create hypercert\" },\n    { status: 500 },\n  );\n}\n```\n\n## Dont\n- Do not modify the PUT handler\n- Do not change the existing 400 validation for required fields (lines 22-27)\n- Do not add new imports\n- Do not change the response shape for non-JSON errors","acceptance_criteria":"1. POST with `rights=\"{invalid\"` returns HTTP 400 with body `{ \"error\": \"Invalid JSON in rights\" }`\n2. POST with `contributions=\"{invalid\"` returns HTTP 400 with body `{ \"error\": \"Invalid JSON in contributions\" }`\n3. POST with valid JSON in rights/contributions still works as before (200)\n4. POST with other errors still returns 500\n5. TypeScript compiles: `npx tsc --noEmit`\n6. Lint passes: `npx eslint app/api/certs/route.ts`","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-02T10:55:15.687101962+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T15:27:43.884176256+06:00","closed_at":"2026-03-02T15:27:43.884176256+06:00","close_reason":"dc5cac4 Return 400 for malformed JSON in certs POST route","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.3","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T10:55:15.688946361+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.4","title":"Add image upload validation in edit form","description":"## Files\n- components/hypercerts-edit-form.tsx (modify)\n\n## What to do\nThe `handleImageChange` function (lines 56-69) accepts any `image/*` file without checking size or type, even though the UI states \"PNG, JPG, WebP up to 5MB\" (line 251-252). Invalid files cause avoidable failures at submit time.\n\nAdd validation at the top of `handleImageChange`, after getting the file (line 57), before `setNewImage` (line 58):\n\n```ts\nconst handleImageChange = (e: React.ChangeEvent\u003cHTMLInputElement\u003e) =\u003e {\n  const file = e.target.files?.[0];\n  if (file) {\n    const allowed = new Set([\"image/png\", \"image/jpeg\", \"image/webp\"]);\n    const maxBytes = 5 * 1024 * 1024;\n    if (!allowed.has(file.type)) {\n      toast.error(\"Please upload a PNG, JPG, or WebP image.\");\n      e.currentTarget.value = \"\";\n      return;\n    }\n    if (file.size \u003e maxBytes) {\n      toast.error(\"Image must be 5 MB or smaller.\");\n      e.currentTarget.value = \"\";\n      return;\n    }\n  }\n  setNewImage(file);\n  setRemoveImage(false);\n  if (file) {\n    const reader = new FileReader();\n    reader.onloadend = () =\u003e {\n      setImagePreview(reader.result as string);\n    };\n    reader.readAsDataURL(file);\n  } else {\n    setImagePreview(imageUri ?? null);\n  }\n};\n```\n\n`toast` is already imported from \"sonner\" on line 7.\n\n## Dont\n- Do not change the file input accept attribute (keep `image/*` for broad browser support)\n- Do not modify the image preview logic\n- Do not add new state variables\n- Do not change the handleRemoveImage function\n- Do not modify the base form (hypercerts-base-form.tsx) — that is a separate concern","acceptance_criteria":"1. Selecting a .gif file triggers `toast.error(\"Please upload a PNG, JPG, or WebP image.\")` and does not set newImage\n2. Selecting a 6MB PNG triggers `toast.error(\"Image must be 5 MB or smaller.\")` and does not set newImage\n3. Selecting a valid 2MB JPEG sets newImage and shows preview as before\n4. `e.currentTarget.value` is cleared on rejection (so re-selecting the same file triggers onChange again)\n5. TypeScript compiles: `npx tsc --noEmit`\n6. Lint passes: `npx eslint components/hypercerts-edit-form.tsx`","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-02T10:55:27.439789694+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T15:26:43.373178103+06:00","closed_at":"2026-03-02T15:26:43.373178103+06:00","close_reason":"151d532 Add image upload validation in edit form","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.4","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T10:55:27.442124615+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-ars.4","depends_on_id":"hypercerts-ars.1","type":"blocks","created_at":"2026-03-02T10:55:47.72241498+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.5","title":"Guard indexed measurementLinks access","description":"## Files\n- components/hypercert-measurements-section.tsx (modify)\n\n## What to do\nOn line 71, the condition checks `measurementLinks` exists but does not verify `measurementLinks[idx]` is defined. If the array is shorter than expected, line 75 crashes reading `link.did`.\n\nChange line 71 from:\n```ts\nif (q.isSuccess \u0026\u0026 q.data \u0026\u0026 measurementLinks) {\n```\nto:\n```ts\nif (q.isSuccess \u0026\u0026 q.data \u0026\u0026 measurementLinks?.[idx]) {\n```\n\nThis is a one-line change. The optional chaining on `measurementLinks?.[idx]` ensures the element at index `idx` exists before accessing its properties.\n\n## Dont\n- Do not change any other logic in this file\n- Do not modify the useMemo dependencies\n- Do not change the MeasurementWithUri type\n- Do not refactor the loop structure","acceptance_criteria":"1. The condition on the line that previously read `if (q.isSuccess \u0026\u0026 q.data \u0026\u0026 measurementLinks)` now reads `if (q.isSuccess \u0026\u0026 q.data \u0026\u0026 measurementLinks?.[idx])`\n2. TypeScript compiles: `npx tsc --noEmit`\n3. Lint passes: `npx eslint components/hypercert-measurements-section.tsx`","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-02T10:55:34.683061826+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T15:26:34.662275309+06:00","closed_at":"2026-03-02T15:26:34.662275309+06:00","close_reason":"c5b4a21 Guard indexed measurementLinks access","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.5","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T10:55:34.684972303+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.6","title":"Restrict trigger prop type to React.ReactElement","description":"## Files\n- components/delete-confirm-dialog.tsx (modify)\n\n## What to do\nOn line 28, the `trigger` prop is typed as `React.ReactNode`, but line 40 uses `\u003cAlertDialogTrigger asChild\u003e` which requires exactly one React element child. Passing non-element nodes (strings, numbers, fragments) will cause a Radix UI runtime error.\n\nChange line 28 in the `DeleteConfirmDialogProps` interface from:\n```ts\ntrigger?: React.ReactNode;\n```\nto:\n```ts\ntrigger?: React.ReactElement;\n```\n\nThis is a one-line type change. No runtime behavior changes.\n\n## Dont\n- Do not change the component logic or JSX\n- Do not modify the fallback button (lines 42-50)\n- Do not add runtime validation — the type constraint is sufficient\n- Do not change any other props in the interface","acceptance_criteria":"1. The `trigger` prop in `DeleteConfirmDialogProps` is typed as `React.ReactElement` (not `React.ReactNode`)\n2. All existing callers still compile (they pass JSX elements, which satisfy `React.ReactElement`)\n3. TypeScript compiles: `npx tsc --noEmit`\n4. Lint passes: `npx eslint components/delete-confirm-dialog.tsx`","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-02T10:55:41.249447347+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T15:26:33.976419228+06:00","closed_at":"2026-03-02T15:26:33.976419228+06:00","close_reason":"c5b4a21 Restrict trigger prop type to React.ReactElement","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.6","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T10:55:41.250934692+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.7","title":"Fix: null startDate/endDate serialized as string \"null\" through FormData in updateHypercert","description":"## Problem\n\n`UpdateHypercertRequest` (lib/api/types.ts lines 46-47) correctly types `startDate` and `endDate` as `string | null | undefined` to support clearing dates. However, the serialization path silently corrupts `null` into the string `\"null\"`.\n\n### Trace\n\n1. Edit form (components/hypercerts-edit-form.tsx line 147): sets `updates.startDate = null` when user clears a date.\n2. `updateHypercert()` (lib/api/hypercerts.ts line 185-186): `if (params.startDate !== undefined) formData.append(\"startDate\", params.startDate)` — when `params.startDate` is `null`, this calls `formData.append(\"startDate\", null)`.\n3. FormData coerces `null` to the string `\"null\"` (confirmed: `new FormData(); fd.append('x', null); fd.get('x') === 'null'`).\n4. PUT handler (app/api/certs/route.ts line 131): `if (startDate !== null) updates.startDate = startDate` — `startDate` is the string `\"null\"`, which is not `null`, so it sets `updates.startDate = \"null\"`.\n5. The SDK receives the string `\"null\"` instead of `null`, so the date is not cleared.\n\n### Fix\n\nIn `updateHypercert()` (lib/api/hypercerts.ts), handle `null` dates the same way `null` image is handled — use a sentinel string:\n```ts\nif (params.startDate === null) {\n  formData.append(\"startDate\", \"null\");\n} else if (params.startDate !== undefined) {\n  formData.append(\"startDate\", params.startDate);\n}\n```\n\nAnd in the PUT handler (app/api/certs/route.ts), decode the sentinel:\n```ts\nif (startDate === \"null\") updates.startDate = null;\nelse if (startDate !== null) updates.startDate = startDate;\n```\n\nApply the same fix to `endDate`.","acceptance_criteria":"1. When user clears startDate in the edit form and saves, the PUT request body contains startDate=null (sentinel), and the SDK update receives null for startDate (not the string \"null\")\n2. When user clears endDate, same behavior\n3. When user sets a valid date string, it is passed through unchanged\n4. When user does not touch a date (undefined), no startDate field is appended to FormData\n5. TypeScript compiles: npx tsc --noEmit\n6. Lint passes: npx eslint lib/api/hypercerts.ts app/api/certs/route.ts","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-02T15:35:30.225862855+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T16:05:43.907402531+06:00","closed_at":"2026-03-02T16:05:43.907402531+06:00","close_reason":"0989cda fix: use sentinel 'null' for null startDate/endDate in FormData serialization","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.7","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T15:35:30.227555286+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.8","title":"Fix: updateMeasurement server action lacks DID ownership guard","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\nAdd a DID ownership guard to `updateMeasurement` (lines 244-274), mirroring the pattern already used in `deleteRecord` (lines 276-296) and `deleteHypercert` (lines 224-242).\n\nAfter the `getRepoContext()` check and before the SDK call, add:\n\n```ts\nconst parsed = parseAtUri(params.measurementUri);\nif (!parsed || !parsed.collection || !parsed.rkey) {\n  throw new Error(\"updateMeasurement failed: invalid AT-URI format\");\n}\nif (parsed.did !== ctx.activeDid) {\n  throw new Error(\n    \"updateMeasurement failed: Forbidden — URI DID does not match active session DID.\",\n  );\n}\n```\n\n`parseAtUri` is already imported at the top of the file.\n\n**Important context:** This is a defense-in-depth / consistency fix. The ATProto protocol itself would not allow cross-repo writes since `ctx.scopedRepo` is bound to the authenticated session DID. However, adding the guard keeps `updateMeasurement` consistent with `deleteRecord` and `deleteHypercert`, and prevents confusing behavior (e.g., silently operating on the wrong rkey in your own repo). Add a code comment above the guard noting this:\n\n```ts\n// Defense-in-depth: ATProto scoped repos already prevent cross-repo writes,\n// but we validate the DID for consistency with deleteRecord/deleteHypercert\n// and to surface clear errors on mismatched URIs.\n```\n\n## Dont\n- Do not modify any other function in create-actions.ts\n- Do not change deleteRecord or deleteHypercert\n- Do not add new imports","acceptance_criteria":"1. updateMeasurement({ measurementUri: 'at://did:plc:other/collection/rkey', updates: {} }) throws 'Forbidden — URI DID does not match active session DID' when ctx.activeDid is different\n2. updateMeasurement with a matching DID proceeds to call updateMeasurement on the SDK\n3. updateMeasurement with an invalid AT-URI throws 'invalid AT-URI format'\n4. TypeScript compiles: npx tsc --noEmit\n5. Lint passes: npx eslint lib/create-actions.ts","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-02T15:35:48.904109269+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T16:05:21.317325751+06:00","closed_at":"2026-03-02T16:05:21.317325751+06:00","close_reason":"712f155 fix: add DID ownership guard to updateMeasurement","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.8","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T15:35:48.90599511+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ars.9","title":"Fix: parseOptionalJson sentinel split on \":\" is fragile — use indexOf instead of split","description":"## Problem\n\nIn `app/api/certs/route.ts` (POST handler, catch block, line 73), the sentinel field name is extracted with:\n\n```ts\nconst field = e.message.split(\":\")[1];\n```\n\nThe sentinel format is `INVALID_JSON:${field}`. If `field` ever contains a colon (e.g., a future field named `foo:bar` or a namespaced field), `split(\":\")[1]` returns only the segment between the first and second colon, silently truncating the field name. The error message would then read `Invalid JSON in foo` instead of `Invalid JSON in foo:bar`.\n\nThe current fields (`rights`, `contributions`) don't contain colons, so this is not a live bug today — but it is a latent fragility that will silently produce wrong error messages if new fields are added.\n\n### Fix\n\nUse `indexOf` to split only on the first colon:\n\n```ts\nconst colonIdx = e.message.indexOf(\":\");\nconst field = colonIdx !== -1 ? e.message.slice(colonIdx + 1) : \"unknown\";\n```\n\nThis correctly handles field names containing colons.","acceptance_criteria":"1. A sentinel error with message 'INVALID_JSON:foo:bar' produces field = 'foo:bar' (not 'foo')\n2. A sentinel error with message 'INVALID_JSON:rights' still produces field = 'rights'\n3. TypeScript compiles: npx tsc --noEmit\n4. Lint passes: npx eslint app/api/certs/route.ts","status":"closed","priority":3,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-02T15:36:01.135224227+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T16:04:02.491690512+06:00","closed_at":"2026-03-02T16:04:02.491690512+06:00","close_reason":"0000000 skip: theoretical fragility not worth fixing","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ars.9","depends_on_id":"hypercerts-ars","type":"parent-child","created_at":"2026-03-02T15:36:01.136537078+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-b08","title":"Epic: Fix creation flow step numbering and profile picture blank after update","description":"Two UI bugs need fixing: (1) The creation flow shows 'Step X of 6' in form components but only 5 steps exist (Contributions step was commented out). The stepper sidebar is correct at 5 steps but the hardcoded stepLabel props in each form and skeleton are stale. (2) After updating a profile picture, the image goes blank because the API returns raw blob URLs instead of CDN-converted URLs. On page refresh it works because the server component applies convertBlobUrlToCdn(). Success looks like: step labels all say 'of 5' consistently, and profile picture remains visible immediately after save without requiring a refresh.","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-17T16:30:02.626885397+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:34:30.991521307+06:00","closed_at":"2026-02-17T16:34:30.942229502+06:00","close_reason":"b9895a8 Both child tasks completed — step labels and profile picture CDN fixes","labels":["needs-integration-review"]}
{"id":"hypercerts-b08.1","title":"Fix hardcoded step labels in creation flow forms and skeletons","description":"## Files\n- components/evidence-form.tsx (modify)\n- components/locations-form.tsx (modify)\n- components/measurement-form.tsx (modify)\n- components/evaluation-form.tsx (modify)\n- components/contributions-form.tsx (modify)\n- components/form-skeletons.tsx (modify)\n\n## What to do\nThe Contributions step (originally step 2) was commented out in app/hypercerts/create/page.tsx while the SDK stabilizes. The stepper sidebar in edit-cert-stepper.tsx correctly defines 5 steps, but the individual form components still have hardcoded stepLabel props saying \"of 6\" with wrong step numbers.\n\nUpdate the stepLabel prop in each form component to match the current 5-step flow:\n\n1. **evidence-form.tsx** line ~211: change `stepLabel=\"Step 3 of 6\"` → `stepLabel=\"Step 2 of 5\"`\n2. **locations-form.tsx** line ~113: change `stepLabel=\"Step 4 of 6\"` → `stepLabel=\"Step 3 of 5\"`\n3. **measurement-form.tsx** line ~291: change `stepLabel=\"Step 5 of 6\"` → `stepLabel=\"Step 4 of 5\"`\n4. **evaluation-form.tsx** line ~188: change `stepLabel=\"Step 6 of 6\"` → `stepLabel=\"Step 5 of 5\"`\n5. **contributions-form.tsx** line ~136: change `stepLabel=\"Step 2 of 6\"` → `stepLabel=\"Step 2 of 6\"` (leave as-is since it is commented out, but add a TODO comment: `// TODO: update stepLabel when contributions step is re-enabled`)\n6. **form-skeletons.tsx**: Update all skeleton stepLabels to match:\n   - ContributionFormSkeleton (~line 119): leave as-is (commented out step) but add TODO\n   - EvidenceFormSkeleton (~line 153): ensure says \"Step 2 of 5\"\n   - LocationFormSkeleton (~line 205): ensure says \"Step 3 of 5\"\n   - MeasurementFormSkeleton (~line 252): ensure says \"Step 4 of 5\"\n   - EvaluationFormSkeleton (~line 302): ensure says \"Step 5 of 5\"\n\n## Dont\n- Do NOT change the STEPS array in edit-cert-stepper.tsx (it is already correct)\n- Do NOT uncomment or re-enable the contributions step\n- Do NOT change the step state logic in app/hypercerts/create/page.tsx\n- Do NOT refactor stepLabel to be dynamic — just fix the hardcoded strings for now","acceptance_criteria":"All 4 active form components show correct step labels: \"Step 2 of 5\", \"Step 3 of 5\", \"Step 4 of 5\", \"Step 5 of 5\". All 4 active skeleton components match. No form says \"of 6\". The app builds without errors (npm run build or next build succeeds).","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T16:30:19.833303835+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:33:53.907122747+06:00","closed_at":"2026-02-17T16:33:53.907122747+06:00","close_reason":"391e426 Fix hardcoded step labels to match 5-step flow","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-b08.1","depends_on_id":"hypercerts-b08","type":"parent-child","created_at":"2026-02-17T16:30:19.835388129+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-b08.2","title":"Fix profile picture going blank after update by applying CDN URL conversion","description":"## Files\n- app/api/profile/update/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n- providers/SignedInProvider.tsx (modify)\n\n## What to do\nAfter a profile picture update, the image goes blank because the API response returns raw blob URLs (e.g. XRPC getBlob URLs from PDS hosts) that are not in Next.js allowed remotePatterns. The server-side profile page (app/profile/page.tsx) correctly applies `convertBlobUrlToCdn()` from `lib/utils.ts` before passing URLs as props, but the API routes do not.\n\n### Fix 1: app/api/profile/update/route.ts\nAround lines 99-115, the route fetches the updated profile and returns avatar/banner URLs. Currently:\n```ts\nconst avatarUrl = updated?.avatar || \"\";\nconst bannerUrl = updated?.banner || \"\";\n```\nChange to:\n```ts\nimport { convertBlobUrlToCdn } from \"@/lib/utils\";\n// ...\nconst avatarUrl = convertBlobUrlToCdn(updated?.avatar) || \"\";\nconst bannerUrl = convertBlobUrlToCdn(updated?.banner) || \"\";\n```\n\n### Fix 2: app/api/profile/bsky/update/route.ts\nSame pattern — find where avatar and banner URLs are returned (around lines 86-87) and apply `convertBlobUrlToCdn()` to both.\n\n### Fix 3: providers/SignedInProvider.tsx\nAround line 30, the Navbar avatar URL is set from the raw profile:\n```ts\navatarUrl = profile?.avatar;\n```\nChange to:\n```ts\nimport { convertBlobUrlToCdn } from \"@/lib/utils\";\n// ...\navatarUrl = convertBlobUrlToCdn(profile?.avatar) || \"\";\n```\n\n## Dont\n- Do NOT change the convertBlobUrlToCdn function itself\n- Do NOT modify the profile-form.tsx onSuccess handler (the fix should come from the API returning correct URLs)\n- Do NOT modify app/profile/page.tsx (it already works correctly)\n- Do NOT add new entries to next.config.ts remotePatterns — the CDN URL conversion is the correct approach\n- Do NOT change the ImageUploader component","acceptance_criteria":"After updating a profile picture on the /profile page, the new image remains visible immediately without requiring a page refresh. The Navbar avatar also updates correctly. The app builds without errors (npm run build or next build succeeds). Verify that convertBlobUrlToCdn is imported and applied in all three files.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T16:30:30.574689694+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:33:58.96474815+06:00","closed_at":"2026-02-17T16:33:58.96474815+06:00","close_reason":"772956d Apply CDN URL conversion to profile picture API routes and Navbar","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-b08.2","depends_on_id":"hypercerts-b08","type":"parent-child","created_at":"2026-02-17T16:30:30.577364056+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-b08.3","title":"Fix avatar camera icon getting clipped on profile page","description":"## Files\n- components/image-uploader.tsx (modify)\n\n## What to do\nWhen the avatar has an image, the camera icon overlay button (the edit/replace button) is positioned at `bottom-0 right-0` inside a container that has `overflow-hidden` and `rounded-full`. Because the circle clips the corners, the camera button gets cut off — it sits in the corner of the bounding box but the circle masks it.\n\nThe fix: move the camera icon button **outside** the overflow-hidden container so it is not clipped, but still visually anchored to the bottom-right of the avatar circle.\n\n### Approach\nIn `image-uploader.tsx`, for the `aspect=\"square\"` (avatar) case only:\n\n1. The camera button (lines 65-74) needs to sit outside the `overflow-hidden` container but still be positioned relative to it. The simplest approach: wrap the entire component in a `relative` container and move the camera button to be a sibling of the overflow-hidden div rather than a child of it. \n\n2. Specifically for the avatar case (`!isBanner`), the camera button should be positioned at the bottom-right edge of the circle. A good position is `bottom-0 right-0` on the outer relative wrapper (which does NOT have overflow-hidden).\n\n3. For the banner case (`isBanner`), keep the current behavior (`bottom-2 right-2` inside the overflow container) since banners are rectangular and dont clip.\n\n4. Make sure the camera button circle is fully visible — the entire round button with the camera icon should be shown, not cut off.\n\n### Current structure (simplified):\n```\n\u003cdiv class=\"overflow-hidden rounded-full\"\u003e \u003c!-- clips everything --\u003e\n  \u003cImage /\u003e\n  \u003cbutton class=\"bottom-0 right-0\"\u003e \u003c!-- gets clipped --\u003e\n    \u003cCamera /\u003e\n  \u003c/button\u003e\n\u003c/div\u003e\n```\n\n### Target structure for avatar:\n```\n\u003cdiv class=\"relative\"\u003e \u003c!-- no overflow-hidden, positions the button --\u003e\n  \u003cdiv class=\"overflow-hidden rounded-full\"\u003e \u003c!-- clips only the image --\u003e\n    \u003cImage /\u003e\n  \u003c/div\u003e\n  \u003cbutton class=\"absolute bottom-0 right-0 z-10\"\u003e \u003c!-- not clipped --\u003e\n    \u003cCamera /\u003e\n  \u003c/button\u003e\n\u003c/div\u003e\n```\n\nFor banner, keep the button inside the overflow container as it is today.\n\n## Dont\n- Do NOT change the avatar size (w-24 h-24)\n- Do NOT change the banner layout or positioning\n- Do NOT change the profile-form.tsx file\n- Do NOT remove the ring-4 ring-background styling (that comes from profile-form.tsx parent)\n- Do NOT change the camera icon size or styling (bg-black/40, backdrop-blur, etc.) — only change its positioning/nesting","acceptance_criteria":"The camera icon button on the avatar is fully visible as a complete circle — not clipped by the rounded avatar container. The button remains clickable and triggers file selection. The banner camera icon still works correctly (bottom-right corner inside the banner). The app builds without errors.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-17T16:57:29.35499163+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:59:15.455978468+06:00","closed_at":"2026-02-17T16:59:15.455978468+06:00","close_reason":"3eb6e49 Fix avatar camera icon clipped by overflow-hidden container","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-b08.3","depends_on_id":"hypercerts-b08","type":"parent-child","created_at":"2026-02-17T16:57:29.357597993+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-b25","title":"Fix: useResolveContributorIdentities uses non-null assertion on parseAtUri result that could be undefined","description":"## Problem\nIn `queries/hypercerts/use-contributor-profiles-query.ts` lines 62-63:\n\n```ts\nconst ref = c.identityRef!;\nconst { did, collection, rkey } = parseAtUri(ref.uri)!;\n```\n\nThe `parseAtUri` function (lib/utils.ts line 126) returns `undefined` if the URI is missing, doesn't start with `at://`, or is missing any of the three path segments. The non-null assertion `!` on line 63 will throw a runtime TypeError if `parseAtUri` returns `undefined`.\n\nThe filter on line 55 does check `parseAtUri(c.identityRef.uri)` to guard the `needsResolution` array, but the filter result is not stored — the `parseAtUri` call is repeated inside the `queries.map` without the guard. This is a TOCTOU pattern: the filter and the map call `parseAtUri` separately, so if the URI is somehow invalid, the assertion will throw.\n\n## Fix\nStore the parsed result in the filter and reuse it:\n```ts\nconst needsResolution = contributors\n  .filter((c) =\u003e c.needsResolution \u0026\u0026 c.identityRef)\n  .map((c) =\u003e ({ contributor: c, parsed: parseAtUri(c.identityRef!.uri) }))\n  .filter((x): x is { contributor: DisplayContributor; parsed: NonNullable\u003cReturnType\u003ctypeof parseAtUri\u003e\u003e } =\u003e x.parsed !== undefined);\n\nconst queries = useQueries({\n  queries: needsResolution.map(({ contributor, parsed }) =\u003e ({\n    queryKey: queryKeys.hypercerts.contributorInformation(parsed.did, parsed.rkey),\n    queryFn: () =\u003e getContributorInformationRecord(parsed),\n    retry: 1,\n    staleTime: 10 * 60 * 1000,\n  })),\n});\n```\n\n## Evidence\n`queries/hypercerts/use-contributor-profiles-query.ts` lines 55-63","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:01.48662368+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:01.48662368+06:00"}
{"id":"hypercerts-b6u","title":"Fix: redirectToAccountCreation throws unhandled Error in render context (login-dialog.tsx)","description":"## File\\ncomponents/login-dialog.tsx lines 65-70\\n\\n## Severity\\nHIGH — Runtime crash\\n\\n## Problem\\nThe redirectToAccountCreation function throws a raw Error when NEXT_PUBLIC_PDS_URL is not defined. This throw happens inside a click handler, which means it will be an unhandled exception — React will not catch it via an error boundary (click handlers are not in the render path). The user sees a white screen or uncaught error in the console with no recovery path.\\n\\n## Evidence\\n```tsx\\nconst redirectToAccountCreation = () =\u003e {\\n  if (!process.env.NEXT_PUBLIC_PDS_URL) {\\n    throw new Error('NEXT_PUBLIC_PDS_URL is not defined'); // unhandled!\\n  }\\n  loginMutation.mutate(process.env.NEXT_PUBLIC_PDS_URL);\\n};\\n```\\n\\n## Fix\\nHandle the missing env var gracefully — either disable the button when the env var is absent, or show a toast error:\\n\\n```tsx\\nconst redirectToAccountCreation = () =\u003e {\\n  const pdsUrl = process.env.NEXT_PUBLIC_PDS_URL;\\n  if (!pdsUrl) {\\n    toast.error('Account creation is not configured.');\\n    return;\\n  }\\n  loginMutation.mutate(pdsUrl);\\n};\\n```\\n\\nAlternatively, disable the 'Create an account' button when !process.env.NEXT_PUBLIC_PDS_URL.","status":"open","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:17.902743693+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:17.902743693+06:00"}
{"id":"hypercerts-bd0","title":"Fix: Contributors section loading state only shows skeletons when DID contributors exist — non-DID contributors with needsResolution get no loading UI (hypercert-contributors-section.tsx)","description":"## File\\ncomponents/hypercert-contributors-section.tsx lines 83-94\\n\\n## Severity\\nMEDIUM — UX / Loading state\\n\\n## Problem\\nThe loading skeleton is only shown when (didContributors.length \u003e 0 || displayContributors.some(c =\u003e c.needsResolution)). However, during Phase 1 (resolving StrongRef identities), the resolved contributors are not yet known to be DIDs. The skeleton count is based on didContributors.length (which may be 0 during resolution), so the skeleton may show 0 items even though contributors are loading.\\n\\nAdditionally, when isLoading is true but the content block (!isLoading) is hidden, the user sees a blank space if no skeletons are rendered (e.g., all contributors are non-DID and none need resolution).\\n\\n## Evidence\\n```tsx\\n{isLoading \u0026\u0026\\n  (didContributors.length \u003e 0 ||\\n    displayContributors.some((c) =\u003e c.needsResolution)) \u0026\u0026 (\\n    \u003cdiv className=\"space-y-3\"\u003e\\n      {(didContributors.length \u003e 0\\n        ? didContributors\\n        : displayContributors\\n      ).map((_, index) =\u003e (\\n        \u003cContributorSkeleton key={index} /\u003e\\n      ))}\\n    \u003c/div\u003e\\n  )}\\n```\\n\\n## Fix\\nSimplify: always show skeletons when isLoading is true, using displayContributors.length as the count (or a minimum of 1):\\n```tsx\\n{isLoading \u0026\u0026 (\\n  \u003cdiv className=\"space-y-3\"\u003e\\n    {Array.from({ length: Math.max(displayContributors.length, 1) }).map((_, i) =\u003e (\\n      \u003cContributorSkeleton key={i} /\u003e\\n    ))}\\n  \u003c/div\u003e\\n)}\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:45.333231355+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:45.333231355+06:00"}
{"id":"hypercerts-bkw","title":"Add RPC_SCOPE back to granular OAuth scope in lib/config.ts","description":"## Files\n- lib/config.ts (modify)\n\n## Context\nRPC_SCOPE was removed from the granular OAuth scope in task hypercerts-02l.4. We are now reverting that decision and adding it back. This task is a child of epic hypercerts-02l.\n\n## What to do\n1. Add the RPC_SCOPE constant definition after BLOB_SCOPE (currently line 36) and before GRANULAR_SCOPE (currently line 38):\n```typescript\nconst RPC_SCOPE =\n  \"rpc:app.bsky.actor.getProfile?aud=did:web:api.bsky.app%23bsky_appview\";\n```\n\n2. Add RPC_SCOPE to the GRANULAR_SCOPE array. Change from:\n```typescript\nconst GRANULAR_SCOPE = [\n  ATPROTO_SCOPE,\n  HYPERCERT_REPO_SCOPE,\n  BLOB_SCOPE,\n].join(\" \");\n```\nTo:\n```typescript\nconst GRANULAR_SCOPE = [\n  ATPROTO_SCOPE,\n  HYPERCERT_REPO_SCOPE,\n  RPC_SCOPE,\n  BLOB_SCOPE,\n].join(\" \");\n```\n\n3. Verify the JSDoc comment on getOAuthScope (around line 76) says \"(repo, rpc, blob)\". If it says \"(repo, blob)\" update it to \"(repo, rpc, blob)\".\n\n4. Check for any other JSDoc/comments in the file that list the granular scopes and ensure they include rpc. There is a second comment around line 201 that may also need updating.\n\n## Don't\n- Change HYPERCERT_REPO_SCOPE, BLOB_SCOPE, or LOOPBACK_SCOPE\n- Change any other file besides lib/config.ts\n- Remove or modify REPO_COLLECTIONS","acceptance_criteria":"1. RPC_SCOPE constant is defined in lib/config.ts as: \"rpc:app.bsky.actor.getProfile?aud=did:web:api.bsky.app%23bsky_appview\"\n2. GRANULAR_SCOPE includes RPC_SCOPE (the joined string contains an rpc: scope)\n3. LOOPBACK_SCOPE is unchanged (still \"atproto transition:generic\")\n4. All JSDoc comments mentioning granular scopes include \"rpc\" in the list\n5. npm run build succeeds with no type errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-18T00:12:21.958580946+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T00:13:45.861285836+06:00","closed_at":"2026-02-18T00:13:45.861285836+06:00","close_reason":"646af53 Add RPC_SCOPE back to granular OAuth scope","labels":["scope:trivial"]}
{"id":"hypercerts-bm5x","title":"Fix: profile update uses wrong condition to decide create vs putRecord (from hypercerts-c8i.2 hypercerts-c8i.3)","description":"Review of hypercerts-c8i.2 and hypercerts-c8i.3 found: Both app/api/profile/update/route.ts (line 56) and app/api/profile/bsky/update/route.ts (line 47) use the condition 'if (!existingProfile?.displayName)' to decide whether to call createRecord or putRecord. This is wrong: a profile record that exists but has no displayName set will trigger createRecord, which will fail with a 409 Conflict error from the PDS because the rkey 'self' already exists. The correct condition is 'if (existingProfile === null)'. Fix: change the condition in both files from '!existingProfile?.displayName' to 'existingProfile === null'.","status":"open","priority":2,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:22.62350893+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:22.62350893+06:00","dependencies":[{"issue_id":"hypercerts-bm5x","depends_on_id":"hypercerts-c8i.2","type":"discovered-from","created_at":"2026-03-04T11:17:22.69072296+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-bm5x","depends_on_id":"hypercerts-c8i.3","type":"discovered-from","created_at":"2026-03-04T11:17:22.758440116+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-bpk","title":"Fix: Email form — add Enter-to-submit and loading spinner on Continue button","description":"## Files\n- components/login-dialog.tsx (modify)\n\n## What to do\nThe EmailForm component (line 110-149) has two UX issues:\n\n1. **No Enter-to-submit**: The email input is inside a plain div, not a form. Pressing Enter does nothing. Wrap the EmailForm content in a \u003cform\u003e with onSubmit that calls handleContinue (with e.preventDefault()).\n\n2. **No loading state on Continue button**: When the user clicks Continue, window.location.href is set but the button stays enabled with no feedback. The user can click multiple times or think nothing happened. Add an isRedirecting state:\n   - Set isRedirecting = true in handleContinue before setting window.location.href\n   - While isRedirecting: show \u003cSpinner /\u003e inside the button, set disabled={true}\n   - Import Spinner from ./ui/spinner (already imported in the file for HandleForm)\n\nSpecific changes to EmailForm:\n\n```tsx\nfunction EmailForm() {\n  const [email, setEmail] = useState(\"\");\n  const [isRedirecting, setIsRedirecting] = useState(false);\n\n  const handleContinue = () =\u003e {\n    setIsRedirecting(true);\n    const url = email\n      ? `/api/oauth/login?email=${encodeURIComponent(email)}`\n      : `/api/oauth/login`;\n    window.location.href = url;\n  };\n\n  return (\n    \u003cform\n      onSubmit={(e) =\u003e {\n        e.preventDefault();\n        handleContinue();\n      }}\n      className=\"w-full space-y-3 animate-fade-in\"\n    \u003e\n      {/* ... InputGroup stays the same ... */}\n\n      \u003cButton\n        type=\"submit\"\n        disabled={isRedirecting}\n        className=\"...\"\n      \u003e\n        {isRedirecting \u0026\u0026 \u003cSpinner /\u003e}\n        Continue\n      \u003c/Button\u003e\n    \u003c/form\u003e\n  );\n}\n```\n\n## Don't\n- Don't change HandleForm — it already has loading state via loginMutation.isPending\n- Don't change PillToggle or the main LoginDialog component\n- Don't change navbar.tsx or any other file","acceptance_criteria":"1. Pressing Enter in the email input triggers the email login redirect (same as clicking Continue)\n2. After clicking Continue (or pressing Enter), the button shows a Spinner and becomes disabled\n3. HandleForm is unchanged — still works as before\n4. No TypeScript errors: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","estimated_minutes":15,"created_at":"2026-02-24T18:58:45.924880799+06:00","created_by":"kzoeps","updated_at":"2026-02-24T18:59:32.63719348+06:00","closed_at":"2026-02-24T18:59:32.63719348+06:00","close_reason":"Added form wrapper for Enter key + isRedirecting state with Spinner","labels":["scope:trivial"]}
{"id":"hypercerts-bq7z","title":"Refactor: use app.bsky.actor.getProfile instead of raw getRecord for profile reads","description":"## Files\n- app/bsky-profile/page.tsx (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n- lib/types.ts (modify — remove BskyActorProfile if no longer used)\n\n## Context\nCurrently, both files fetch the Bluesky profile using the low-level \\`com.atproto.repo.getRecord\\` API and then cast the result with \\`as BskyActorProfile\\`. The \\`@atproto/api\\` Agent has a typed \\`getProfile\\` method (\\`agent.getProfile({ actor: did })\\`) that returns \\`AppBskyActorDefs.ProfileViewDetailed\\` — a fully typed response where \\`avatar\\` and \\`banner\\` are already resolved to URL strings (not BlobRef objects).\n\n## What to do\n\n### 1. app/bsky-profile/page.tsx (lines 23-30)\nReplace:\n\\`\\`\\`ts\nconst profileResult = await repo.com.atproto.repo\n  .getRecord({ repo: repo.assertDid, collection: \"app.bsky.actor.profile\", rkey: \"self\" })\n  .catch(() =\u003e null);\nconst profile = profileResult?.data?.value as BskyActorProfile | undefined;\n\\`\\`\\`\nWith:\n\\`\\`\\`ts\nconst profileResult = await repo.getProfile({ actor: repo.assertDid }).catch(() =\u003e null);\nconst profile = profileResult?.data;\n\\`\\`\\`\n\nSince \\`ProfileViewDetailed.avatar\\` and \\`ProfileViewDetailed.banner\\` are already URL strings, you can also remove the \\`getBlobURL\\`/\\`convertBlobUrlToCdn\\` calls (lines 34-39) and the \\`resolveSessionPds\\` call (lines 32-33). Just use \\`profile?.avatar ?? \"\"\\` and \\`profile?.banner ?? \"\"\\` directly.\n\nRemove the \\`import type { BskyActorProfile }\\` and the \\`import { getBlobURL, convertBlobUrlToCdn }\\` and \\`import { resolveSessionPds }\\` and \\`import { getSession }\\` (only \\`getAgent\\` is needed). Remove the \\`import { getSession }\\` only if it's not used elsewhere in the file — currently it's only used for \\`resolveSessionPds\\` which is being removed.\n\n### 2. app/api/profile/bsky/update/route.ts\n\n**First getRecord (lines 38-46):** This one is used to check profile existence AND get the raw record for merging in the putRecord call. The raw record (with BlobRef objects) is needed for the merge. Keep this getRecord call BUT improve the typing. Instead of casting to \\`BskyActorProfile\\`, use the SDK's type:\n\\`\\`\\`ts\nimport type { AppBskyActorProfile } from \"@atproto/api\";\n// ...\nconst existingProfile = existingResult?.data?.value as AppBskyActorProfile.Record | undefined ?? null;\n\\`\\`\\`\nCheck if \\`AppBskyActorProfile\\` is exported from \\`@atproto/api\\`. If not, check \\`@atproto/api\\`'s exports for the right namespace. The type lives at \\`node_modules/@atproto/api/dist/client/types/app/bsky/actor/profile.d.ts\\`. Look at the actual exports from \\`@atproto/api\\` index to find the correct import path. If no typed export exists for the profile record type, keep the existing \\`BskyActorProfile\\` type from \\`lib/types.ts\\` for this one usage.\n\n**Second getRecord (lines 116-123):** This is only used to return the updated profile to the client. Replace with \\`getProfile\\`:\n\\`\\`\\`ts\nconst updatedResult = await repo.getProfile({ actor: repo.assertDid }).catch(() =\u003e null);\nconst updated = updatedResult?.data;\n\\`\\`\\`\nSince \\`ProfileViewDetailed.avatar\\` and \\`ProfileViewDetailed.banner\\` are already URL strings, remove the \\`getBlobURL\\`/\\`convertBlobUrlToCdn\\`/\\`resolveSessionPds\\`/\\`getSession\\` calls (lines 126-135) and simplify the response:\n\\`\\`\\`ts\nreturn NextResponse.json({\n  ok: true,\n  profile: {\n    displayName: updated?.displayName || \"\",\n    description: updated?.description || \"\",\n    avatar: updated?.avatar || \"\",\n    banner: updated?.banner || \"\",\n  },\n});\n\\`\\`\\`\n\nRemove unused imports: \\`getBlobURL\\`, \\`convertBlobUrlToCdn\\`, \\`getSession\\`, \\`resolveSessionPds\\`. Keep \\`revalidatePath\\` (still used on line 114).\n\n### 3. lib/types.ts\nIf \\`BskyActorProfile\\` is no longer imported anywhere after the above changes, remove the interface (lines 114-121). If the first getRecord in the update route still uses it, keep it.\n\n## Don't\n- Don't change the create/putRecord logic in the update route — that still needs raw record manipulation\n- Don't change how blob uploads work\n- Don't change the profile form component or any client-side code\n- Don't add new dependencies\n- Don't change the API response shape (the profile object returned must still have displayName, description, avatar, banner as strings)","acceptance_criteria":"1. app/bsky-profile/page.tsx uses agent.getProfile() instead of com.atproto.repo.getRecord to fetch the profile\n2. app/bsky-profile/page.tsx has no 'as BskyActorProfile' type cast\n3. app/api/profile/bsky/update/route.ts post-update fetch (previously lines 116-123) uses getProfile() instead of getRecord\n4. app/api/profile/bsky/update/route.ts post-update response no longer needs getBlobURL/convertBlobUrlToCdn since getProfile returns URL strings\n5. No unused imports remain in either file\n6. The API response shape is unchanged: { ok: true, profile: { displayName: string, description: string, avatar: string, banner: string } }\n7. npm run build succeeds with no type errors\n8. BskyActorProfile is removed from lib/types.ts if no longer referenced, or kept if still needed by the first getRecord in the update route","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T12:05:07.634797415+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T12:09:19.638502473+06:00","closed_at":"2026-03-04T12:09:19.638502473+06:00","close_reason":"2af74cd refactor: use agent.getProfile() instead of getRecord for bsky profile reads","labels":["scope:small"]}
{"id":"hypercerts-bu9","title":"Fix: duplicate inputRef causes upload label broken in no-image state (from hypercerts-b08.3)","description":"Review of hypercerts-b08.3 found: two \u003cinput ref={inputRef}\u003e elements are rendered simultaneously when imageUrl is falsy (no-image state). Lines 89-95 (inside the \u003clabel\u003e) and lines 100-106 (always-rendered hidden input) both use ref={inputRef}. React assigns the ref to the last-rendered element (lines 100-106), which is outside the \u003clabel\u003e. The label wraps the first input but inputRef points to the second, so clicking the upload label in the no-image state may not trigger file selection correctly. Evidence: components/image-uploader.tsx lines 89-106 in commit 3eb6e49. Fix: remove the always-rendered hidden input (lines 100-106) since the label-wrapped input (lines 89-95) already handles the no-image case, and the avatar camera button (lines 110-118) calls inputRef.current?.click() directly which works fine without a second input.","acceptance_criteria":"Only one \u003cinput ref={inputRef}\u003e element exists in image-uploader.tsx at any time. File upload works in both states: (1) clicking the dashed upload area when no image is set triggers file selection, (2) clicking the camera icon button when an image exists triggers file selection. The app builds without errors.","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoeps","created_at":"2026-02-17T17:04:19.608809781+06:00","created_by":"kzoeps","updated_at":"2026-02-17T17:08:46.23134294+06:00","closed_at":"2026-02-17T17:08:46.23134294+06:00","close_reason":"c54bd52 fix: remove duplicate inputRef — single hidden input always rendered","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-bu9","depends_on_id":"hypercerts-b08.3","type":"discovered-from","created_at":"2026-02-17T17:04:23.638372534+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-bvs","title":"Fix: README.md Project Structure table shows stale api/auth/ path — should be api/oauth/","description":"Review of README.md Project Structure section found a stale route path in the directory tree.\n\nEvidence:\n- README.md lines 332-334 (Project Structure tree):\n  │   ├── api/\n  │   │   ├── auth/              # Standard ATProto OAuth (login, callback, logout)\n  │   │   ├── oauth/             # ePDS email OAuth (login, callback)\n\n- Actual directory structure (verified with ls app/api/):\n  app/api/oauth/     (standard ATProto OAuth — login, callback, logout)\n  app/api/oauth/epds/ (ePDS email OAuth — login, callback)\n  app/api/certs/\n  app/api/profile/\n\nThe api/auth/ path was renamed to api/oauth/ in the hypercerts-a48 epic. The README tree still shows the old structure. The hypercerts-a48.7 task updated some README references but missed the Project Structure directory tree.\n\nSuggested fix:\nUpdate README.md lines 332-334 to:\n  │   ├── api/\n  │   │   ├── oauth/             # Standard ATProto OAuth (login, callback, logout)\n  │   │   ├── oauth/epds/        # ePDS email OAuth (login, callback)\n  │   │   ├── certs/             # Hypercert operations (create, add-attachment, add-location)\n  │   │   └── profile/           # Profile management (certified + bluesky)","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:49:01.766145156+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:49:01.766145156+06:00","dependencies":[{"issue_id":"hypercerts-bvs","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:49:06.071387616+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-c28","title":"Epic: Fix all pre-existing ESLint errors and warnings","description":"The codebase has 84 ESLint problems (6 errors, 78 warnings) after autofix. Most warnings (50+) come from auto-generated lexicons/ files that should be excluded from linting. The remaining ~30 issues are unused imports/vars and no-explicit-any errors in source files. Goal: zero ESLint errors, minimal warnings. Success: npm run lint exits with 0 errors and 0 warnings. Scope: ESLint config ignore for lexicons, remove unused imports, add proper types for any. Out of scope: changing ESLint rules, adding new plugins.","status":"closed","priority":2,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-02-17T18:57:45.568710354+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:08:04.893916104+06:00","closed_at":"2026-02-17T19:08:04.893916104+06:00","close_reason":"0476c57 All ESLint errors and warnings resolved — 0 problems","labels":["scope:small"]}
{"id":"hypercerts-c28.1","title":"Add lexicons/ to ESLint globalIgnores","description":"## Files\n- eslint.config.mjs (modify)\n\n## What to do\n\nAdd `\"lexicons/**\"` to the `globalIgnores` array in `eslint.config.mjs`. The lexicons/ directory contains auto-generated TypeScript types from ATProto lexicon definitions. These files have many unused imports by design (they export types that consumers may or may not use). They should not be linted.\n\nThe globalIgnores array currently has:\n```js\nglobalIgnores([\n  \".next/**\",\n  \"out/**\",\n  \"build/**\",\n  \"next-env.d.ts\",\n])\n```\n\nAdd `\"lexicons/**\"` to this array.\n\n## Don't\n- Do NOT modify any files in lexicons/\n- Do NOT change any ESLint rules\n- Do NOT touch any other config","acceptance_criteria":"1. eslint.config.mjs has \"lexicons/**\" in the globalIgnores array\n2. npm run lint no longer reports any warnings from lexicons/ files\n3. npm run lint still reports issues in non-lexicon files (i.e. we did not accidentally ignore too much)\n4. npx tsc --noEmit passes","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":5,"created_at":"2026-02-17T18:58:12.848981311+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:01:10.848866351+06:00","closed_at":"2026-02-17T19:01:10.848866351+06:00","close_reason":"4155910 Add lexicons/** to ESLint globalIgnores","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-c28.1","depends_on_id":"hypercerts-c28","type":"parent-child","created_at":"2026-02-17T18:58:12.850921725+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c28.2","title":"Fix no-explicit-any errors across 3 files","description":"## Files\n- lib/blob-utils.ts (modify)\n- app/api/certs/add-attachment/route.ts (modify)\n- components/evidence-form.tsx (modify)\n\n## What to do\n\n### 1. lib/blob-utils.ts — 3 `any` usages (lines 31, 33, 51)\n\nThe function `resolveRecordBlobs` recursively walks an object tree resolving BlobRefs to URLs. It genuinely handles unknown shapes, so use `unknown` for the input and `Record\u003cstring, unknown\u003e` for the result object.\n\n```ts\n// Line 30-31: Change parameter type\nexport async function resolveRecordBlobs(\n  value: unknown,\n  ownerDid: string,\n): Promise\u003cunknown\u003e {\n```\n\nLine 34: the `typeof value !== \"object\"` check already narrows. But you need to handle the object access. After the null/typeof check, cast to `Record\u003cstring, unknown\u003e`:\n\n```ts\nif (!value || typeof value !== \"object\") return value;\n\nconst obj = value as Record\u003cstring, unknown\u003e;\n\n// Line 42: use obj instead of value\nif (obj.$type === \"blob\" || (obj.ref \u0026\u0026 obj.mimeType)) {\n  return await resolveBlobToUrl(obj as BlobRef | { $type: string }, ownerDid);\n}\n\n// Line 47-54: use obj\nconst entries = Object.entries(obj);\nconst resolved = await Promise.all(\n  entries.map(([, val]) =\u003e resolveRecordBlobs(val, ownerDid)),\n);\nconst result: Record\u003cstring, unknown\u003e = {};\nentries.forEach(([key], i) =\u003e {\n  result[key] = resolved[i];\n});\nreturn result;\n```\n\n### 2. app/api/certs/add-attachment/route.ts — 2 `any` usages (lines 72, 122)\n\nLine 72: `let location: any = undefined;`\n→ Change to a proper type. The location can be either a string or an object with specific fields:\n```ts\nlet location:\n  | string\n  | {\n      lpVersion: string;\n      srs: string;\n      locationType: string;\n      location: string | File;\n      name?: string;\n      description?: string;\n    }\n  | undefined = undefined;\n```\n\nLine 122: `contentType: contentType as any`\n→ The contentType comes from form data as a string. Check what the SDK method expects. If it is a union of string literals, cast to that type. If the type is not easily importable, use a type assertion through `unknown`:\n```ts\ncontentType: contentType as Parameters\u003ctypeof ctx.scopedRepo.hypercerts.addAttachment\u003e[0][\"contentType\"],\n```\nOr if that is too complex, prefix with an eslint-disable comment for just that line:\n```ts\n// eslint-disable-next-line @typescript-eslint/no-explicit-any\ncontentType: contentType as any,\n```\nUse the eslint-disable ONLY as a last resort if the SDK type is not accessible.\n\n### 3. components/evidence-form.tsx — 1 `any` usage (line 159)\n\nLine 159: `!CONTENT_TYPES.includes(contentType as any)`\n→ This is a type narrowing issue. `CONTENT_TYPES` is likely a `readonly` array and `contentType` is `string`. Fix by checking membership differently:\n```ts\nif (contentType \u0026\u0026 !(CONTENT_TYPES as readonly string[]).includes(contentType)) {\n```\n\n## Don't\n- Do NOT change any logic or behavior\n- Do NOT add eslint-disable comments unless absolutely necessary (SDK types not accessible)\n- Do NOT modify function signatures that are part of the public API unless the change is backward-compatible","acceptance_criteria":"1. npm run lint reports 0 errors for no-explicit-any in these 3 files\n2. npx tsc --noEmit passes\n3. No logic changes — only type annotations changed\n4. No eslint-disable comments added (unless the SDK type for contentType is truly inaccessible, in which case document why)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-02-17T18:58:37.219922107+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:02:45.658012941+06:00","closed_at":"2026-02-17T19:02:45.658012941+06:00","close_reason":"6c729a4 fix: replace no-explicit-any with proper types in 3 files","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-c28.2","depends_on_id":"hypercerts-c28","type":"parent-child","created_at":"2026-02-17T18:58:37.221662402+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c28.3","title":"Remove all unused imports and variables from source files","description":"## Files (all modify)\n- app/api/profile/bsky/update/route.ts\n- app/api/profile/update/route.ts\n- app/hypercerts/page.tsx\n- app/jwks.json/route.ts\n- app/page.tsx\n- app/profile/page.tsx\n- components/edit-cert-stepper.tsx\n- components/hypercert-detail-view.tsx\n- components/hypercert-evaluations-section.tsx\n- components/hypercert-evidence-section.tsx\n- components/hypercert-evidence-view.tsx\n- components/hypercert-measurements-section.tsx\n- components/hypercerts-base-form.tsx\n- components/login-dialog.tsx\n- components/navbar.tsx\n- lexicons/index.ts (only if not already ignored by ESLint after hypercerts-c28.1)\n- lib/api/external/bluesky.ts\n- lib/create-actions.ts\n- lib/utils.ts\n- providers/SignedInProvider.tsx\n\n## What to do\n\nRemove every unused import and unused variable flagged by ESLint `@typescript-eslint/no-unused-vars`. Also fix the one `@next/next/no-img-element` warning. Here is the exact list per file:\n\n### Simple unused imports (just delete the import or remove from destructure):\n- `app/hypercerts/page.tsx:5` — remove `CardContent` from import\n- `app/page.tsx:4` — remove `Badge` from import\n- `app/profile/page.tsx:3` — remove `getSession` from import\n- `app/profile/page.tsx:5` — remove `getBlobURL` from import\n- `components/edit-cert-stepper.tsx:5` — remove `Users` from import\n- `components/hypercert-detail-view.tsx:3` — remove `Separator` from import\n- `components/hypercert-detail-view.tsx:11` — remove `Award` from import\n- `components/hypercert-evaluations-section.tsx:8` — remove `Separator` from import\n- `components/hypercert-evidence-section.tsx:6` — remove `Separator` from import\n- `components/hypercert-evidence-view.tsx:16` — remove `OrgHypercertsDefs` from import\n- `components/hypercert-measurements-section.tsx:8` — remove `Separator` from import\n- `lib/api/external/bluesky.ts:5` — remove `APIError` from import\n- `lib/create-actions.ts:6` — remove `revalidatePath` import entirely\n- `lib/create-actions.ts:8` — remove `after` import entirely\n- `lib/utils.ts:7` — remove `sdk` import\n- `lib/utils.ts:8` — remove `OAuthSession` from import\n- `providers/SignedInProvider.tsx:4` — remove `getBlobURL` from import\n\n### Unused destructured variables (prefix with underscore or remove):\n- `app/api/profile/bsky/update/route.ts:38` — `catch (error)` → `catch`  (remove the binding since error is unused)\n- `app/api/profile/update/route.ts:46` — same pattern, `catch (error)` → `catch`\n- `app/jwks.json/route.ts:39-41` — `d`, `use`, `key_ops` are intentionally destructured to EXCLUDE them from the spread. Prefix with underscores: `_d`, `_use`, `_key_ops`\n- `components/hypercert-evidence-view.tsx:30` — `type` param in `getContentTypeColor` is unused. Prefix with underscore: `_type`\n- `lib/create-actions.ts:57` — `params` in `addContribution` is unused. Prefix with underscore: `_params`\n\n### Unused assigned variables (remove the assignment or the variable):\n- `components/edit-cert-stepper.tsx:32` — `isUpcoming` is assigned but never used. Delete the line `const isUpcoming = step \u003c s.id;`\n- `components/hypercerts-base-form.tsx:153` — `cleanedWorkScope` is assigned but never used. Delete the line `const cleanedWorkScope = workScope.map((w) =\u003e w.trim()).filter(Boolean);`\n- `components/login-dialog.tsx:22` — `hostname` is assigned but never used. Delete the line and its usage\n- `components/navbar.tsx:44-45` — `userDid` and `activeDid` are destructured from props but never used. Remove them from the destructure. Check if NavbarProps interface still needs them (they may be passed from parent but unused in this component — if so, also remove from the interface, but ONLY if no other component uses NavbarProps)\n\n### no-img-element (1 instance):\n- `components/hypercerts-base-form.tsx:352` — Replace `\u003cimg\u003e` with `\u003cImage\u003e` from `next/image`. Add `import Image from \"next/image\"` if not already imported. The `\u003cimg\u003e` tag needs `width` and `height` props for `\u003cImage\u003e`. Check the context to determine appropriate values.\n\n### lexicons/ files\n- If `lexicons/` is already ignored by ESLint (from hypercerts-c28.1), skip all lexicons/ files\n- If not yet ignored, skip them anyway — they are auto-generated\n\n## Don't\n- Do NOT change any logic or behavior\n- Do NOT remove variables/imports that ARE used (double-check before removing)\n- Do NOT modify lexicons/ files (they are auto-generated)\n- Do NOT change ESLint config\n- Do NOT remove props from component interfaces if they are passed by callers (even if unused in the component — they may be needed later). Instead prefix with underscore.","acceptance_criteria":"1. npm run lint reports 0 warnings for no-unused-vars in non-lexicon files\n2. npm run lint reports 0 warnings for no-img-element\n3. npx tsc --noEmit passes\n4. No logic or behavior changes — only dead code removed\n5. All removed imports/variables were genuinely unused (verify each one)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T18:59:58.894079326+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:07:00.866322756+06:00","closed_at":"2026-02-17T19:07:00.866322756+06:00","close_reason":"6286af9 Remove all unused imports and variables from source files","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-c28.3","depends_on_id":"hypercerts-c28","type":"parent-child","created_at":"2026-02-17T18:59:58.896233832+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c3t","title":"Refactor: consolidate 5 scattered `@media (prefers-color-scheme: dark)` blocks in atproto-branding.ts (from hypercerts-nyk.15)","description":"Review of hypercerts-nyk.15 found that lib/atproto-branding.ts now has 5 separate `@media (prefers-color-scheme: dark)` blocks scattered across the file (lines 119-127, 171-175, 207-226, 264-268, 337-348). This is a maintainability concern — a developer reading the file must scan all 437 lines to understand the full dark mode behavior.\n\nAdditionally, the `main.flex.flex-col.items-center \u003e h1.text-primary` selector appears in two separate unconditional rule blocks (lines 232-234 and 258-261) that could be merged into one.\n\nEvidence: lib/atproto-branding.ts — count of dark media queries:\n- Lines 119-127: logo invert\n- Lines 171-175: H1 signin SVG swap\n- Lines 207-226: left panel background + text colors\n- Lines 264-268: Authenticate H1 color\n- Lines 337-348: button visibility\n\nRecommendation: Consolidate into 1-2 dark mode blocks (one for structural/layout rules, one for color overrides), or at minimum add a table-of-contents comment at the top listing all dark mode overrides. Also merge the split `main.flex.flex-col.items-center \u003e h1.text-primary` rules at lines 232-234 and 258-261 into a single block.\n\nThis is a refactor/cleanup task, not a functional bug.","status":"open","priority":4,"issue_type":"task","owner":"karma.gainforest.id","created_at":"2026-02-17T19:46:13.986219082+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:46:13.986219082+06:00","dependencies":[{"issue_id":"hypercerts-c3t","depends_on_id":"hypercerts-nyk.15","type":"discovered-from","created_at":"2026-02-17T19:46:18.157191988+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c7v","title":"Fix: redundant `background-position: left center` in mobile logo override in atproto-branding.ts (from hypercerts-nyk.15)","description":"Review of hypercerts-nyk.15 found a redundant CSS declaration in lib/atproto-branding.ts.\n\nLine 112 in the mobile media query:\n```css\n@media (max-width: 767px) {\n  img[alt=\"Hypercerts Logo\"],\n  img[alt*=\"Logo\"] {\n    width: 150px !important;\n    height: 30px !important;\n    background-position: left center !important;  /* REDUNDANT */\n    margin-top: 8px !important;\n    margin-bottom: 8px !important;\n  }\n}\n```\n\nThe base rule (lines 94-104) already sets `background-position: left center !important`. The mobile override repeats the same value — it doesn't change anything. This is dead CSS that adds noise.\n\nEvidence: lib/atproto-branding.ts line 103 (base rule) vs line 112 (mobile override).\n\nFix: Remove `background-position: left center !important` from the mobile override block. Only the properties that actually differ from the base rule (width, height, margin-top, margin-bottom) need to be in the override.","status":"open","priority":4,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-02-17T19:47:18.882310696+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:47:18.882310696+06:00","dependencies":[{"issue_id":"hypercerts-c7v","depends_on_id":"hypercerts-nyk.15","type":"discovered-from","created_at":"2026-02-17T19:47:23.074664826+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c8i","title":"Epic: Replace SDK simple write operations with native atproto calls","description":"## Why\nPhase 3 of the SDK removal migration. Phases 1-2 replaced auth and reads. Now we replace the simple write operations: deletes and profile creates/updates.\n\n## Goal\nAfter this epic, deleteHypercert, deleteRecord, and all profile create/update operations use native atproto calls. No scopedRepo write calls remain for these operations.\n\n## Scope\n6 write operations:\n- deleteHypercert (lib/create-actions.ts) — scopedRepo.hypercerts.delete → agent.com.atproto.repo.deleteRecord\n- deleteRecord (lib/create-actions.ts) — scopedRepo.records.delete → agent.com.atproto.repo.deleteRecord\n- createCertifiedProfile (app/api/profile/update/route.ts) — uploadBlob for avatar/banner + createRecord\n- updateCertifiedProfile (app/api/profile/update/route.ts) — uploadBlob for avatar/banner + putRecord (merge with existing)\n- createBskyProfile (app/api/profile/bsky/update/route.ts) — same pattern\n- updateBskyProfile (app/api/profile/bsky/update/route.ts) — same pattern\n\n## Constraints\n- Do NOT touch complex compound writes (hypercert create/update, addContribution, addEvaluation, addMeasurement, addAttachment, attachLocation, updateMeasurement) — those are Phase 4\n- Profile updates must handle: File → uploadBlob → BlobRef, null → remove field, undefined → preserve existing\n- The app must build (pnpm build) after all tasks complete\n\n## Success\n- All 6 simple write operations use native atproto calls\n- No @ts-expect-error comments on these operations\n- pnpm build succeeds","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-03T20:11:36.485852751+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:58:54.340316067+06:00","closed_at":"2026-03-03T22:58:54.340316067+06:00","close_reason":"52ffa54 All Phase 3 simple write tasks completed and verified","labels":["scope:medium"]}
{"id":"hypercerts-c8i.1","title":"Migrate deleteHypercert and deleteRecord to native atproto deleteRecord","description":"## Files\n- lib/create-actions.ts (modify — 2 functions only)\n\n## What to do\nReplace the 2 delete operations with direct `agent.com.atproto.repo.deleteRecord()` calls.\n\n### deleteHypercert (lines 255-274):\nCurrent:\n```ts\n// @ts-expect-error\nawait ctx.scopedRepo.hypercerts.delete(params.hypercertUri);\n```\n\nThe SDK's `hypercerts.delete(uri)` parses the AT-URI and calls deleteRecord. We already have the parsed URI from `parseAtUri` on line 262.\n\nNew:\n```ts\nawait ctx.agent.com.atproto.repo.deleteRecord({\n  repo: ctx.activeDid,\n  collection: parsed.collection || 'org.hypercerts.claim.activity',\n  rkey: parsed.rkey,\n});\n```\n\nRemove the `@ts-expect-error` comment.\n\n### deleteRecord (lines 321-343):\nCurrent:\n```ts\n// @ts-expect-error\nawait ctx.scopedRepo.records.delete({\n  collection: parsed.collection,\n  rkey: parsed.rkey,\n});\n```\n\nNew:\n```ts\nawait ctx.agent.com.atproto.repo.deleteRecord({\n  repo: ctx.activeDid,\n  collection: parsed.collection,\n  rkey: parsed.rkey,\n});\n```\n\nRemove the `@ts-expect-error` comment.\n\n### Both functions already have:\n- DID ownership validation (parsed.did !== ctx.activeDid check)\n- AT-URI parsing via parseAtUri\n- Error handling\n\n## Don't\n- Don't touch any other functions in create-actions.ts\n- Don't change the ownership validation logic\n- Don't change the return values ({ success: true })","acceptance_criteria":"1. deleteHypercert uses ctx.agent.com.atproto.repo.deleteRecord\n2. deleteRecord uses ctx.agent.com.atproto.repo.deleteRecord\n3. Both @ts-expect-error comments removed\n4. Ownership validation preserved\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-03T20:11:50.43746983+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T20:17:24.526275285+06:00","closed_at":"2026-03-03T20:17:24.526275285+06:00","close_reason":"66d397b Migrate deleteHypercert and deleteRecord to native atproto deleteRecord","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-c8i.1","depends_on_id":"hypercerts-c8i","type":"parent-child","created_at":"2026-03-03T20:11:50.438866735+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c8i.2","title":"Migrate certified profile create/update to native atproto createRecord/putRecord with blob uploads","description":"## Files\n- app/api/profile/update/route.ts (modify — write operations only)\n\n## What to do\nReplace `repo.profile.createCertifiedProfile(params)` and `repo.profile.updateCertifiedProfile(params)` with native atproto calls.\n\n### What the SDK did under the hood:\n1. If avatar/banner is a File → `uploadBlob(file)` → get BlobRef\n2. Build the record object with string fields + BlobRefs\n3. For create: `createRecord({ repo, collection: 'app.certified.actor.profile', rkey: 'self', record })`\n4. For update: fetch existing record, merge updates (null = remove, undefined = preserve), `putRecord({ repo, collection, rkey: 'self', record })`\n\n### New implementation for CREATE (line 72-73):\n\nReplace:\n```ts\n// @ts-expect-error\nawait repo.profile.createCertifiedProfile(createParams);\n```\n\nWith:\n```ts\n// Upload blobs if provided\nlet avatarBlob, bannerBlob;\nif (avatar) {\n  const avatarData = new Blob([avatar], { type: avatar.type });\n  const uploadResult = await repo.com.atproto.repo.uploadBlob(avatarData);\n  avatarBlob = uploadResult.data.blob;\n}\nif (banner) {\n  const bannerData = new Blob([banner], { type: banner.type });\n  const uploadResult = await repo.com.atproto.repo.uploadBlob(bannerData);\n  bannerBlob = uploadResult.data.blob;\n}\n\nconst record: Record\u003cstring, unknown\u003e = {\n  $type: 'app.certified.actor.profile',\n};\nif (displayName) record.displayName = displayName;\nif (description) record.description = description;\nif (pronouns) record.pronouns = pronouns;\nif (website) record.website = website;\nif (avatarBlob) record.avatar = avatarBlob;\nif (bannerBlob) record.banner = bannerBlob;\n\nawait repo.com.atproto.repo.createRecord({\n  repo: repo.assertDid,\n  collection: 'app.certified.actor.profile',\n  rkey: 'self',\n  record,\n});\n```\n\n### New implementation for UPDATE (line 99-100):\n\nReplace:\n```ts\n// @ts-expect-error\nawait repo.profile.updateCertifiedProfile(updateParams);\n```\n\nWith:\n```ts\n// Fetch existing profile to merge\nconst existingResult = await repo.com.atproto.repo.getRecord({\n  repo: repo.assertDid,\n  collection: 'app.certified.actor.profile',\n  rkey: 'self',\n}).catch(() =\u003e null);\nconst existing = (existingResult?.data?.value as Record\u003cstring, unknown\u003e) || {};\n\n// Upload new blobs if provided\nlet avatarBlob, bannerBlob;\nif (avatar) {\n  const avatarData = new Blob([avatar], { type: avatar.type });\n  const uploadResult = await repo.com.atproto.repo.uploadBlob(avatarData);\n  avatarBlob = uploadResult.data.blob;\n}\nif (banner) {\n  const bannerData = new Blob([banner], { type: banner.type });\n  const uploadResult = await repo.com.atproto.repo.uploadBlob(bannerData);\n  bannerBlob = uploadResult.data.blob;\n}\n\n// Merge: null = remove, undefined = preserve existing, value = set\nconst record: Record\u003cstring, unknown\u003e = {\n  ...existing,\n  $type: 'app.certified.actor.profile',\n};\n\n// Handle string fields: null removes, truthy sets, falsy (empty string) removes\nif (displayName !== undefined) {\n  if (displayName) record.displayName = displayName;\n  else delete record.displayName;\n}\nif (description !== undefined) {\n  if (description) record.description = description;\n  else delete record.description;\n}\nif (pronouns !== undefined) {\n  if (pronouns) record.pronouns = pronouns;\n  else delete record.pronouns;\n}\nif (website !== undefined) {\n  if (website) record.website = website;\n  else delete record.website;\n}\n\n// Handle blobs: new File = upload and set, null would remove (but current code only sends File or undefined)\nif (avatarBlob) record.avatar = avatarBlob;\nif (bannerBlob) record.banner = bannerBlob;\n\nawait repo.com.atproto.repo.putRecord({\n  repo: repo.assertDid,\n  collection: 'app.certified.actor.profile',\n  rkey: 'self',\n  record,\n});\n```\n\n### Important notes:\n- `putRecord` with rkey `'self'` will overwrite the existing record entirely — that's why we merge with existing first\n- The `$type` field must be set on the record for ATProto validation\n- `uploadBlob` returns `{ data: { blob: BlobRef } }` — the BlobRef is what goes in the record\n- The variable `repo` in this file is the Agent from `getAgent()`\n- The blob upload pattern is already used in `lib/queries.ts` (`uploadFile` function) — reference that\n\n## Don't\n- Don't touch the read operations (already migrated in Phase 2)\n- Don't change form data parsing or validation\n- Don't change the response shape\n- Don't touch the bsky profile route (that's task .3)","acceptance_criteria":"1. createCertifiedProfile replaced with uploadBlob + createRecord\n2. updateCertifiedProfile replaced with getRecord + uploadBlob + putRecord (merge)\n3. Both @ts-expect-error comments on writes removed\n4. Avatar/banner File objects are uploaded via uploadBlob before record creation\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-03T20:12:19.744075606+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T20:20:01.152440699+06:00","closed_at":"2026-03-03T20:20:01.152440699+06:00","close_reason":"55f1c55 Migrate certified profile create/update to native atproto calls","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-c8i.2","depends_on_id":"hypercerts-c8i","type":"parent-child","created_at":"2026-03-03T20:12:19.745812404+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c8i.3","title":"Migrate bsky profile create/update to native atproto createRecord/putRecord with blob uploads","description":"## Files\n- app/api/profile/bsky/update/route.ts (modify — write operations only)\n\n## What to do\nExact same pattern as task .2 but for the Bluesky profile. Replace `repo.profile.createBskyProfile(params)` and `repo.profile.updateBskyProfile(params)` with native atproto calls.\n\n### Collection: `app.bsky.actor.profile` (not `app.certified.actor.profile`)\n### Rkey: `'self'`\n\n### CREATE (line 63-64):\nReplace:\n```ts\n// @ts-expect-error\nawait repo.profile.createBskyProfile(createParams);\n```\n\nWith the same uploadBlob + createRecord pattern as task .2, but using collection `'app.bsky.actor.profile'`. The bsky profile has fewer fields — just displayName, description, avatar, banner (no pronouns, no website).\n\n### UPDATE (line 86-87):\nReplace:\n```ts\n// @ts-expect-error\nawait repo.profile.updateBskyProfile(updateParams);\n```\n\nWith the same getRecord + uploadBlob + putRecord merge pattern as task .2, using collection `'app.bsky.actor.profile'`.\n\n### Record fields for bsky profile:\n```ts\nconst record: Record\u003cstring, unknown\u003e = {\n  $type: 'app.bsky.actor.profile',\n  // displayName, description, avatar (BlobRef), banner (BlobRef)\n};\n```\n\n## Don't\n- Don't touch the read operations (already migrated in Phase 2)\n- Don't change form data parsing or validation\n- Don't change the response shape\n- Don't touch the certified profile route (that's task .2)","acceptance_criteria":"1. createBskyProfile replaced with uploadBlob + createRecord\n2. updateBskyProfile replaced with getRecord + uploadBlob + putRecord (merge)\n3. Both @ts-expect-error comments on writes removed\n4. Collection is 'app.bsky.actor.profile'\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-03-03T20:12:30.537612991+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T20:21:45.791618977+06:00","closed_at":"2026-03-03T20:21:45.791618977+06:00","close_reason":"b73a39c migrate bsky profile create/update to native atproto calls","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-c8i.3","depends_on_id":"hypercerts-c8i","type":"parent-child","created_at":"2026-03-03T20:12:30.539264889+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-c8i.4","title":"Verify build passes after Phase 3 simple write migrations","description":"## Files\n- Any file that fails pnpm build due to Phase 3 changes\n\n## What to do\nAfter tasks .1 through .3 are complete, run `pnpm build` and fix any remaining type errors.\n\n### Verify:\n1. `pnpm build` succeeds\n2. Run: `grep -rn '@ts-expect-error' --include='*.ts' --include='*.tsx' app/ lib/ providers/`\n   - The remaining @ts-expect-error should ONLY be on Phase 4 complex writes:\n     - addContribution (create-actions.ts)\n     - addEvaluation (create-actions.ts)\n     - addMeasurement (create-actions.ts)\n     - updateMeasurement (create-actions.ts)\n     - scopedRepo.hypercerts.create (app/api/certs/route.ts POST)\n     - scopedRepo.hypercerts.update (app/api/certs/route.ts PUT)\n     - scopedRepo.hypercerts.addAttachment (app/api/certs/add-attachment/route.ts)\n     - personalRepository.hypercerts.attachLocation (app/api/certs/add-location/route.ts)\n   - That's 8 remaining. If you see fewer or more, investigate.\n\n## Don't\n- Don't rewrite complex compound writes\n- Don't change business logic","acceptance_criteria":"1. pnpm build succeeds with exit code 0\n2. Only 8 @ts-expect-error comments remain (all on Phase 4 complex writes)\n3. No @ts-expect-error on delete or profile operations","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T20:12:43.363241875+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T20:24:21.233398994+06:00","closed_at":"2026-03-03T20:24:21.233398994+06:00","close_reason":"e4ae1dc Verify build passes after Phase 3 migrations","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-c8i.4","depends_on_id":"hypercerts-c8i","type":"parent-child","created_at":"2026-03-03T20:12:43.364583445+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-c8i.4","depends_on_id":"hypercerts-c8i.1","type":"blocks","created_at":"2026-03-03T20:12:43.367655833+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-c8i.4","depends_on_id":"hypercerts-c8i.2","type":"blocks","created_at":"2026-03-03T20:12:43.370645253+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-c8i.4","depends_on_id":"hypercerts-c8i.3","type":"blocks","created_at":"2026-03-03T20:12:43.373541457+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ccb","title":"Fix: getBlobURL in lib/utils.ts always returns JPEG format for CDN URLs regardless of actual MIME type","description":"## Problem\n`lib/utils.ts` lines 39-41 and `lib/utils.ts` lines 79-81 hardcode `@jpeg` in CDN blob URLs:\n\n```ts\nreturn `https://cdn.bsky.app/img/feed_thumbnail/plain/${did}/${cid.toString()}@jpeg`;\n```\n\nThis is also repeated in `convertBlobUrlToCdn` (line 80). The `@jpeg` suffix tells the Bluesky CDN to transcode the image to JPEG. For non-image blobs (e.g. GeoJSON location files, PDF evidence files) this will either fail or return corrupted data. Even for images, PNG files with transparency will lose their alpha channel.\n\nThe MIME type is available on the `BlobRef` object as `blobRef.mimeType` but is not used.\n\n## Fix\nUse the actual MIME type from the blob reference to select the correct CDN format, or use `@png` as a safer default for images. For non-image blobs, use the PDS direct URL instead of the CDN:\n\n```ts\n// Only use CDN for image blobs\nif (blobRef.mimeType?.startsWith('image/')) {\n  const format = blobRef.mimeType === 'image/png' ? '@png' : '@jpeg';\n  return `https://cdn.bsky.app/img/feed_thumbnail/plain/${did}/${cid}${format}`;\n}\n// Fall back to PDS URL for non-image blobs\nreturn `${resolvedPdsUrl}/xrpc/com.atproto.sync.getBlob?did=...`;\n```\n\n## Evidence\n- `lib/utils.ts` lines 39-41: hardcoded `@jpeg`\n- `lib/utils.ts` lines 79-81 (`convertBlobUrlToCdn`): hardcoded `@jpeg`","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:36.385856565+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:36.385856565+06:00"}
{"id":"hypercerts-cev","title":"Fix: ePDS email login button has no loading/disabled state during redirect (from hypercerts-dhz.8/dhz.10)","description":"Review of hypercerts-dhz.8 and dhz.10 found: The 'Sign in with email' button (login-dialog.tsx lines 105-116) has no loading state and no disabled state. When clicked, window.location.href is set and the browser navigates away — but there is a brief moment before navigation where the user could click again. More importantly, the handle-based 'Sign In' button (line 71-78) shows a Spinner and is disabled while loginMutation.isPending, but the email button has no equivalent feedback. The spec (dhz.10 acceptance criteria) does not require a loading state, but the UX is inconsistent: one button shows loading, the other does not. Additionally, if the PAR request to ePDS takes time (it involves a network round-trip + DPoP nonce retry), the user sees no feedback at all — the button just sits there. Fix: add a local isNavigating state that is set to true on click, show a Spinner and disable the button while isNavigating is true. Evidence: login-dialog.tsx line 105-116 vs lines 71-78.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:26:42.404911641+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:26:42.404911641+06:00","dependencies":[{"issue_id":"hypercerts-cev","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:26:50.571975934+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-cl4","title":"Fix: Evaluation and Measurement data cast to local interfaces without validation (evaluations-section.tsx, measurements-section.tsx)","description":"## Files\\n- components/hypercert-evaluations-section.tsx line 42\\n- components/hypercert-measurements-section.tsx line 51\\n\\n## Severity\\nMEDIUM — Type safety\\n\\n## Problem\\nFetched record values are cast directly to local Evaluation and Measurement interfaces without any runtime validation. If the server returns unexpected data (missing fields, wrong types), the cast silently succeeds and the component will render with undefined values or crash at runtime.\\n\\n## Evidence\\n```tsx\\n// evaluations-section.tsx line 42\\nif (q.isSuccess \u0026\u0026 q.data) items.push(q.data.value as Evaluation);\\n\\n// measurements-section.tsx line 51\\nif (q.isSuccess \u0026\u0026 q.data) items.push(q.data.value as Measurement);\\n```\\n\\n## Fix\\nAdd a type guard or use zod/valibot to validate the shape before casting:\\n```tsx\\nfunction isEvaluation(v: unknown): v is Evaluation {\\n  return (\\n    typeof v === 'object' \u0026\u0026 v !== null \u0026\u0026\\n    typeof (v as Evaluation).summary === 'string' \u0026\u0026\\n    typeof (v as Evaluation).createdAt === 'string' \u0026\u0026\\n    Array.isArray((v as Evaluation).evaluators)\\n  );\\n}\\n\\n// Then:\\nif (q.isSuccess \u0026\u0026 q.data \u0026\u0026 isEvaluation(q.data.value)) {\\n  items.push(q.data.value);\\n}\\n```\\n\\nAlternatively, use optional chaining in the view components to handle missing fields gracefully.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:21.244386326+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:21.244386326+06:00"}
{"id":"hypercerts-cs2","title":"Fix: Trash icon buttons in evaluation-form.tsx and measurement-form.tsx missing aria-label","description":"## Files\\n- components/evaluation-form.tsx lines 278-286, 447-457, 513-526\\n- components/measurement-form.tsx lines 375-383, 630-639\\n\\n## Severity\\nMEDIUM — Accessibility\\n\\n## Problem\\nSeveral icon-only Trash buttons in evaluation-form.tsx and measurement-form.tsx are missing aria-label. Screen readers will announce these as unlabeled buttons. The contributions-form.tsx has the same issue (line 230-238).\\n\\nNote: Some buttons DO have aria-label (e.g., evaluation-form.tsx line 255 'Remove evaluator', measurement-form.tsx line 352 'Remove measurer') but the pattern is inconsistent — the manual DID/URI remove buttons do not.\\n\\n## Evidence\\n```tsx\\n// evaluation-form.tsx line 278 — no aria-label\\n\u003cButton\\n  variant=\"ghost\"\\n  size=\"icon\"\\n  onClick={() =\u003e removeManualDid(index)}\\n  disabled={manualDids.length === 1 || mutation.isPending}\\n  className=\"text-muted-foreground hover:text-destructive\"\\n\u003e\\n  \u003cTrash className=\"h-4 w-4\" /\u003e\\n\u003c/Button\u003e\\n```\\n\\n## Fix\\nAdd aria-label to all icon-only buttons:\\n```tsx\\n\u003cButton\\n  type=\"button\"\\n  variant=\"ghost\"\\n  size=\"icon\"\\n  aria-label=\"Remove DID\"\\n  onClick={() =\u003e removeManualDid(index)}\\n  ...\\n\u003e\\n  \u003cTrash className=\"h-4 w-4\" /\u003e\\n\u003c/Button\u003e\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:37.454172259+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:37.454172259+06:00"}
{"id":"hypercerts-czy","title":"Epic: Replace SDK complex compound write operations with native atproto calls","description":"## Why\nPhase 4 (final) of the SDK removal migration. Phases 1-3 replaced auth, reads, and simple writes. Now we replace the 8 remaining complex compound write operations.\n\n## Goal\nAfter this epic, zero @ts-expect-error comments remain. Zero imports from @hypercerts-org/sdk-core for runtime operations. The SDK can be removed in Phase 5 (cleanup).\n\n## Scope — 8 operations:\n1. addEvaluation — single createRecord (simplest)\n2. addMeasurement — createRecord + optional location creation\n3. updateMeasurement — getRecord + merge + putRecord + optional location creation\n4. addContribution — creates contributorInformation + contributionDetails records, then updates hypercert\n5. hypercerts.create — compound: upload image, create rights, process contributors, create claim (with content-hash rkey), create attachments\n6. hypercerts.update — getRecord + merge + optional image upload + putRecord\n7. addAttachment — resolve subjects to StrongRefs, upload content blobs, optional location, createRecord\n8. attachLocation — create location record, then update hypercert's locations array\n\n## Key patterns needed:\n- StrongRef resolution: given an AT-URI, fetch the record to get its CID, return { uri, cid }\n- Location creation: create app.certified.location record, handle string/File/URL content\n- Blob upload: agent.com.atproto.repo.uploadBlob(blob) → BlobRef\n- Content-hash rkey: hypercert claim uses rkey format 'hc2:{sha256}' based on record content\n\n## Constraints\n- Must build (pnpm build) after all tasks\n- Keep @hypercerts-org/sdk-core in package.json for now (type imports remain — Phase 5)\n\n## Success\n- Zero @ts-expect-error comments on scopedRepo calls\n- All 8 operations use native atproto calls\n- pnpm build succeeds","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-03T22:39:00.381609278+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:58:54.409393704+06:00","closed_at":"2026-03-03T22:58:54.409393704+06:00","close_reason":"52ffa54 All Phase 4 compound write tasks completed and verified","labels":["scope:medium"]}
{"id":"hypercerts-czy.1","title":"Create shared helper functions for compound writes — resolveStrongRef, createLocationRecord, uploadBlob wrapper","description":"## Files\n- lib/atproto-writes.ts (create)\n\n## What to do\nCreate a new helper module with shared functions needed by multiple compound write operations. These replace internal SDK utilities.\n\n### Function 1: resolveStrongRef\nMany SDK methods need a StrongRef ({ uri: string, cid: string }) for linking records. Given an AT-URI, fetch the record to get its CID.\n\n```ts\nimport { Agent } from '@atproto/api';\nimport { parseAtUri } from './utils';\n\nexport interface StrongRef {\n  uri: string;\n  cid: string;\n}\n\nexport async function resolveStrongRef(\n  agent: Agent,\n  uri: string,\n  fallbackCollection?: string,\n): Promise\u003cStrongRef\u003e {\n  const parsed = parseAtUri(uri);\n  if (!parsed) throw new Error(`Invalid AT-URI: ${uri}`);\n  \n  const result = await agent.com.atproto.repo.getRecord({\n    repo: parsed.did,\n    collection: parsed.collection || fallbackCollection || '',\n    rkey: parsed.rkey,\n  });\n  \n  return { uri: result.data.uri, cid: result.data.cid };\n}\n```\n\n### Function 2: createLocationRecord\nCreates an `app.certified.location` record. Used by addMeasurement, addAttachment, attachLocation, and hypercerts.create.\n\n```ts\nexport interface LocationCreateParams {\n  lpVersion: string;\n  srs: string;\n  locationType: string;\n  location: string | File;  // string URL or File (GeoJSON blob)\n  name?: string;\n  description?: string;\n}\n\nexport async function createLocationRecord(\n  agent: Agent,\n  did: string,\n  params: LocationCreateParams,\n): Promise\u003cStrongRef\u003e {\n  // Resolve location content\n  let locationContent: Record\u003cstring, unknown\u003e;\n  if (typeof params.location === 'string') {\n    // URL string\n    locationContent = { $type: 'org.hypercerts.defs#uri', uri: params.location };\n  } else {\n    // File — upload as blob\n    const blob = new Blob([params.location], { type: params.location.type });\n    const uploadResult = await agent.com.atproto.repo.uploadBlob(blob);\n    locationContent = { $type: 'org.hypercerts.defs#smallBlob', blob: uploadResult.data.blob };\n  }\n\n  const record: Record\u003cstring, unknown\u003e = {\n    $type: 'app.certified.location',\n    lpVersion: params.lpVersion,\n    srs: params.srs,\n    locationType: params.locationType,\n    location: locationContent,\n    createdAt: new Date().toISOString(),\n  };\n  if (params.name) record.name = params.name;\n  if (params.description) record.description = params.description;\n\n  const result = await agent.com.atproto.repo.createRecord({\n    repo: did,\n    collection: 'app.certified.location',\n    record,\n  });\n\n  return { uri: result.data.uri, cid: result.data.cid };\n}\n```\n\n### Function 3: processLocations\nResolves an array of location params — either existing AT-URI strings or new location objects to create.\n\n```ts\nexport async function processLocations(\n  agent: Agent,\n  did: string,\n  locations: (string | LocationCreateParams)[],\n): Promise\u003cStrongRef[]\u003e {\n  return Promise.all(\n    locations.map(async (loc) =\u003e {\n      if (typeof loc === 'string') {\n        // Existing location AT-URI — resolve to StrongRef\n        return resolveStrongRef(agent, loc, 'app.certified.location');\n      }\n      // New location — create record\n      return createLocationRecord(agent, did, loc);\n    }),\n  );\n}\n```\n\n### Function 4: uploadContentBlob\nUploads a File/Blob and returns a BlobRef.\n\n```ts\nexport async function uploadContentBlob(\n  agent: Agent,\n  file: File | Blob,\n): Promise\u003cunknown\u003e {\n  const blob = file instanceof File ? new Blob([file], { type: file.type }) : file;\n  const result = await agent.com.atproto.repo.uploadBlob(blob);\n  return result.data.blob;\n}\n```\n\n### Also add 'server-only' import at the top:\n```ts\nimport 'server-only';\n```\n\n## Don't\n- Don't import from @hypercerts-org/sdk-core\n- Don't add any route-specific logic — keep these generic\n- Don't add write operations for specific record types (those go in other tasks)","acceptance_criteria":"1. lib/atproto-writes.ts exists with resolveStrongRef, createLocationRecord, processLocations, uploadContentBlob\n2. StrongRef interface exported\n3. LocationCreateParams interface exported\n4. No imports from @hypercerts-org/sdk-core\n5. pnpm build passes (file compiles)","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-03T22:39:27.291159439+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:44:46.218668068+06:00","closed_at":"2026-03-03T22:44:46.218668068+06:00","close_reason":"7079a91 Add shared helper functions for compound writes","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-czy.1","depends_on_id":"hypercerts-czy","type":"parent-child","created_at":"2026-03-03T22:39:27.299109568+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-czy.2","title":"Migrate addEvaluation and addMeasurement to native atproto createRecord","description":"## Files\n- lib/create-actions.ts (modify — addEvaluation and addMeasurement functions)\n\n## What to do\n\n### addEvaluation (lines 89-112):\nThe SDK's addEvaluation does: resolve subject to StrongRef, build evaluation record, createRecord.\n\nReplace:\n```ts\n// @ts-expect-error\nreturn ctx.scopedRepo.hypercerts.addEvaluation({\n  ...evaluationData,\n  subjectUri: hypercertUri,\n});\n```\n\nWith:\n```ts\nimport { resolveStrongRef } from './atproto-writes';\n\n// Resolve subject hypercert to StrongRef\nconst subject = await resolveStrongRef(ctx.agent, hypercertUri, 'org.hypercerts.claim.activity');\n\nconst record: Record\u003cstring, unknown\u003e = {\n  $type: 'org.hypercerts.claim.evaluation',\n  subject,\n  evaluators: evaluationData.evaluators.map(did =\u003e ({ did })),\n  summary: evaluationData.summary,\n  createdAt: new Date().toISOString(),\n};\nif (evaluationData.score) record.score = evaluationData.score;\nif (evaluationData.content) record.content = evaluationData.content;\nif (evaluationData.measurements) record.measurements = evaluationData.measurements;\nif (evaluationData.location) record.location = evaluationData.location;\n\nconst result = await ctx.agent.com.atproto.repo.createRecord({\n  repo: ctx.activeDid,\n  collection: 'org.hypercerts.claim.evaluation',\n  record,\n});\n\nreturn { uri: result.data.uri, cid: result.data.cid };\n```\n\n### addMeasurement (lines 126-154):\nThe SDK's addMeasurement does: resolve subject to StrongRef, optionally create location records, build measurement record, createRecord.\n\nReplace:\n```ts\n// @ts-expect-error\nreturn ctx.scopedRepo.hypercerts.addMeasurement({\n  ...params,\n  measurers: (params.measurers || []).map((measurer) =\u003e ({ did: measurer })),\n});\n```\n\nWith:\n```ts\nimport { resolveStrongRef, processLocations } from './atproto-writes';\n\n// Resolve subject to StrongRef\nconst subject = await resolveStrongRef(ctx.agent, params.subject, 'org.hypercerts.claim.activity');\n\n// Process locations if provided\nlet locationRefs: StrongRef[] | undefined;\nif (params.locations \u0026\u0026 params.locations.length \u003e 0) {\n  locationRefs = await processLocations(ctx.agent, ctx.activeDid, params.locations);\n}\n\nconst record: Record\u003cstring, unknown\u003e = {\n  $type: 'org.hypercerts.claim.measurement',\n  subject,\n  metric: params.metric,\n  value: params.value,\n  unit: params.unit,\n  createdAt: new Date().toISOString(),\n};\nif (params.measurers?.length) record.measurers = params.measurers.map(did =\u003e ({ did }));\nif (params.startDate) record.startDate = params.startDate;\nif (params.endDate) record.endDate = params.endDate;\nif (params.methodType) record.methodType = params.methodType;\nif (params.methodURI) record.methodURI = params.methodURI;\nif (params.evidenceURI?.length) record.evidenceURI = params.evidenceURI;\nif (params.comment) record.comment = params.comment;\nif (locationRefs?.length) record.locations = locationRefs;\n\nconst result = await ctx.agent.com.atproto.repo.createRecord({\n  repo: ctx.activeDid,\n  collection: 'org.hypercerts.claim.measurement',\n  record,\n});\n\nreturn { uri: result.data.uri, cid: result.data.cid };\n```\n\n## Don't\n- Don't touch other functions\n- Don't change function signatures or parameter types\n- Don't change the MeasurementLocationParam type","acceptance_criteria":"1. addEvaluation uses resolveStrongRef + createRecord on org.hypercerts.claim.evaluation\n2. addMeasurement uses resolveStrongRef + processLocations + createRecord on org.hypercerts.claim.measurement\n3. Both @ts-expect-error comments removed\n4. Both return { uri, cid }\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-03-03T22:39:48.027449463+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:48:40.41679378+06:00","closed_at":"2026-03-03T22:48:40.41679378+06:00","close_reason":"5482ca8 Migrate addEvaluation and addMeasurement to native atproto createRecord","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-czy.2","depends_on_id":"hypercerts-czy","type":"parent-child","created_at":"2026-03-03T22:39:48.037237573+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.2","depends_on_id":"hypercerts-czy.1","type":"blocks","created_at":"2026-03-03T22:39:48.055155619+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-czy.3","title":"Migrate updateMeasurement and addContribution to native atproto calls","description":"## Files\n- lib/create-actions.ts (modify — updateMeasurement and addContribution functions)\n\n## What to do\n\n### updateMeasurement (lines 279-322):\nThe SDK does: fetch existing measurement, merge updates (preserving $type, subject, createdAt), optionally create locations, putRecord.\n\nReplace:\n```ts\n// @ts-expect-error\nreturn ctx.scopedRepo.hypercerts.updateMeasurement(params.measurementUri, sdkUpdates);\n```\n\nWith:\n```ts\nimport { processLocations } from './atproto-writes';\n\n// Fetch existing measurement\nconst existingResult = await ctx.agent.com.atproto.repo.getRecord({\n  repo: parsed.did,\n  collection: parsed.collection || 'org.hypercerts.claim.measurement',\n  rkey: parsed.rkey,\n});\nconst existing = existingResult.data.value as Record\u003cstring, unknown\u003e;\n\n// Merge updates, preserving immutable fields\nconst record: Record\u003cstring, unknown\u003e = {\n  ...existing,\n  $type: 'org.hypercerts.claim.measurement',\n};\n\n// Apply individual updates\nconst updates = params.updates;\nif (updates.metric !== undefined) record.metric = updates.metric;\nif (updates.value !== undefined) record.value = updates.value;\nif (updates.unit !== undefined) record.unit = updates.unit;\nif (updates.startDate !== undefined) record.startDate = updates.startDate;\nif (updates.endDate !== undefined) record.endDate = updates.endDate;\nif (updates.methodType !== undefined) record.methodType = updates.methodType;\nif (updates.methodURI !== undefined) record.methodURI = updates.methodURI;\nif (updates.evidenceURI !== undefined) record.evidenceURI = updates.evidenceURI;\nif (updates.comment !== undefined) record.comment = updates.comment;\nif (updates.measurers !== undefined) {\n  record.measurers = updates.measurers.map(did =\u003e ({ did }));\n}\n\nconst result = await ctx.agent.com.atproto.repo.putRecord({\n  repo: ctx.activeDid,\n  collection: parsed.collection || 'org.hypercerts.claim.measurement',\n  rkey: parsed.rkey,\n  record,\n});\n\nreturn { uri: result.data.uri, cid: result.data.cid };\n```\n\nAlso remove the `sdkUpdates` variable and the measurers mapping above it — that's now handled inline.\n\n### addContribution (lines 64-87):\nThe SDK does: for each contributor, create contributorInformation record; create contributionDetails record; then fetch hypercert, append contributors, update hypercert.\n\nReplace:\n```ts\n// @ts-expect-error\nreturn ctx.scopedRepo.hypercerts.addContribution({...});\n```\n\nWith:\n```ts\nimport { resolveStrongRef } from './atproto-writes';\n\n// 1. Create contributionDetails record\nconst detailsRecord: Record\u003cstring, unknown\u003e = {\n  $type: 'org.hypercerts.claim.contributionDetails',\n  role: params.contributionDetails.role,\n  createdAt: new Date().toISOString(),\n};\nif (params.contributionDetails.contributionDescription) {\n  detailsRecord.contributionDescription = params.contributionDetails.contributionDescription;\n}\nif (params.contributionDetails.startDate) detailsRecord.startDate = params.contributionDetails.startDate;\nif (params.contributionDetails.endDate) detailsRecord.endDate = params.contributionDetails.endDate;\n\nconst detailsResult = await ctx.agent.com.atproto.repo.createRecord({\n  repo: ctx.activeDid,\n  collection: 'org.hypercerts.claim.contributionDetails',\n  record: detailsRecord,\n});\nconst detailsRef = { uri: detailsResult.data.uri, cid: detailsResult.data.cid };\n\n// 2. Create contributorInformation records for each contributor\nconst contributorRefs = await Promise.all(\n  params.contributors.map(async (identifier) =\u003e {\n    const infoRecord: Record\u003cstring, unknown\u003e = {\n      $type: 'org.hypercerts.claim.contributorInformation',\n      identifier,\n      createdAt: new Date().toISOString(),\n    };\n    const infoResult = await ctx.agent.com.atproto.repo.createRecord({\n      repo: ctx.activeDid,\n      collection: 'org.hypercerts.claim.contributorInformation',\n      record: infoRecord,\n    });\n    return { uri: infoResult.data.uri, cid: infoResult.data.cid };\n  }),\n);\n\n// 3. Fetch existing hypercert and append contributors\nconst hypercertRef = await resolveStrongRef(ctx.agent, params.hypercertUri, 'org.hypercerts.claim.activity');\nconst hypercertParsed = parseAtUri(params.hypercertUri);\nif (!hypercertParsed) throw new Error('Invalid hypercertUri');\n\nconst existingResult = await ctx.agent.com.atproto.repo.getRecord({\n  repo: hypercertParsed.did,\n  collection: hypercertParsed.collection || 'org.hypercerts.claim.activity',\n  rkey: hypercertParsed.rkey,\n});\nconst existingRecord = existingResult.data.value as Record\u003cstring, unknown\u003e;\n\n// Build new contributor entries\nconst newContributors = contributorRefs.map(ref =\u003e ({\n  contributorIdentity: ref,\n  contributionDetails: detailsRef,\n}));\n\nconst existingContributors = (existingRecord.contributors as unknown[]) || [];\nexistingRecord.contributors = [...existingContributors, ...newContributors];\n\n// 4. Update hypercert with appended contributors\nconst updateResult = await ctx.agent.com.atproto.repo.putRecord({\n  repo: ctx.activeDid,\n  collection: hypercertParsed.collection || 'org.hypercerts.claim.activity',\n  rkey: hypercertParsed.rkey,\n  record: existingRecord,\n});\n\nreturn { uri: updateResult.data.uri, cid: updateResult.data.cid };\n```\n\n## Don't\n- Don't touch other functions\n- Don't change function signatures","acceptance_criteria":"1. updateMeasurement uses getRecord + merge + putRecord on org.hypercerts.claim.measurement\n2. addContribution creates contributionDetails + contributorInformation records, then updates hypercert\n3. Both @ts-expect-error comments removed\n4. Both return { uri, cid }\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":50,"created_at":"2026-03-03T22:40:14.474780367+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:50:07.098872219+06:00","closed_at":"2026-03-03T22:50:07.098872219+06:00","close_reason":"c77e90a Migrate updateMeasurement and addContribution to native atproto calls","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-czy.3","depends_on_id":"hypercerts-czy","type":"parent-child","created_at":"2026-03-03T22:40:14.47883239+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.3","depends_on_id":"hypercerts-czy.1","type":"blocks","created_at":"2026-03-03T22:40:14.497121011+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-czy.4","title":"Migrate hypercerts.create (POST /api/certs) to native atproto compound write","description":"## Files\n- app/api/certs/route.ts (modify — POST handler)\n\n## What to do\nReplace `ctx.scopedRepo.hypercerts.create(hypercertParams)` with native atproto calls. This is the most complex compound write — it creates multiple records in sequence.\n\n### What the SDK does:\n1. Upload image blob (if provided)\n2. Create rights record (`org.hypercerts.claim.rights`)\n3. Process contributors (create contributorInformation + contributionDetails records)\n4. Create the hypercert claim record (`org.hypercerts.claim.activity`) with a content-hash rkey\n\n### New implementation:\n\nReplace lines 69-71:\n```ts\n// @ts-expect-error\nconst data = await ctx.scopedRepo.hypercerts.create(hypercertParams);\nreturn NextResponse.json(data);\n```\n\nWith:\n```ts\nimport { uploadContentBlob, resolveStrongRef } from '@/lib/atproto-writes';\n\n// 1. Upload image if provided\nlet imageField: Record\u003cstring, unknown\u003e | undefined;\nif (hypercertParams.image) {\n  const blobRef = await uploadContentBlob(ctx.agent, hypercertParams.image as File);\n  imageField = { $type: 'org.hypercerts.defs#smallImage', image: blobRef };\n}\n\n// 2. Create rights record\nconst rightsRecord: Record\u003cstring, unknown\u003e = {\n  $type: 'org.hypercerts.claim.rights',\n  createdAt: new Date().toISOString(),\n};\nif (hypercertParams.rights) {\n  if (hypercertParams.rights.rightsName) rightsRecord.rightsName = hypercertParams.rights.rightsName;\n  if (hypercertParams.rights.rightsType) rightsRecord.rightsType = hypercertParams.rights.rightsType;\n  if (hypercertParams.rights.rightsDescription) rightsRecord.rightsDescription = hypercertParams.rights.rightsDescription;\n}\nconst rightsResult = await ctx.agent.com.atproto.repo.createRecord({\n  repo: ctx.activeDid,\n  collection: 'org.hypercerts.claim.rights',\n  record: rightsRecord,\n});\nconst rightsRef = { uri: rightsResult.data.uri, cid: rightsResult.data.cid };\n\n// 3. Build the claim record\nconst claimRecord: Record\u003cstring, unknown\u003e = {\n  $type: 'org.hypercerts.claim.activity',\n  title: hypercertParams.title,\n  shortDescription: hypercertParams.shortDescription,\n  description: hypercertParams.description || hypercertParams.shortDescription,\n  startDate: hypercertParams.startDate,\n  endDate: hypercertParams.endDate,\n  rights: rightsRef,\n  workScope: hypercertParams.workScope || [],\n  createdAt: new Date().toISOString(),\n};\nif (imageField) claimRecord.image = imageField;\n\n// 4. Create the claim record\n// Note: The SDK used a content-hash rkey (hc2:{sha256}). For simplicity, let the PDS\n// generate a TID rkey (omit rkey param). If content-hash rkeys are required for\n// deduplication, implement later.\nconst claimResult = await ctx.agent.com.atproto.repo.createRecord({\n  repo: ctx.activeDid,\n  collection: 'org.hypercerts.claim.activity',\n  record: claimRecord,\n});\n\nconst data = {\n  hypercertUri: claimResult.data.uri,\n  hypercertCid: claimResult.data.cid,\n  rightsUri: rightsResult.data.uri,\n  rightsCid: rightsResult.data.cid,\n};\nreturn NextResponse.json(data);\n```\n\n### Also:\n- Remove the `CreateHypercertParams` import from `@hypercerts-org/sdk-core` (line 3). Define a local type or use inline typing for the params.\n- The `parseOptionalJson` calls for rights and contributions can stay — just adjust the types.\n- For now, skip contributor processing during create (the SDK did it but it's complex). Contributors can be added after creation via addContribution. If the form currently sends contributions, just ignore them for now and add a TODO.\n\n## Don't\n- Don't change the PUT handler (that's task .5)\n- Don't change form data parsing\n- Don't change error handling patterns","acceptance_criteria":"1. POST handler creates rights record + claim record using native atproto calls\n2. Image upload handled via uploadContentBlob\n3. @ts-expect-error removed\n4. Returns { hypercertUri, hypercertCid, rightsUri, rightsCid }\n5. CreateHypercertParams no longer imported from sdk-core\n6. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":50,"created_at":"2026-03-03T22:40:39.840616313+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:49:39.564489058+06:00","closed_at":"2026-03-03T22:49:39.564489058+06:00","close_reason":"79006fb Migrate hypercerts.create POST to native atproto compound write","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-czy.4","depends_on_id":"hypercerts-czy","type":"parent-child","created_at":"2026-03-03T22:40:39.844862782+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.4","depends_on_id":"hypercerts-czy.1","type":"blocks","created_at":"2026-03-03T22:40:39.858671361+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-czy.5","title":"Migrate hypercerts.update (PUT /api/certs) to native atproto getRecord + merge + putRecord","description":"## Files\n- app/api/certs/route.ts (modify — PUT handler only)\n\n## What to do\nReplace `ctx.scopedRepo.hypercerts.update({ uri, updates, image })` with native atproto calls.\n\n### What the SDK does:\n1. Fetch existing record via getRecord\n2. Shallow merge updates into existing (preserving createdAt, rights as immutable)\n3. Handle image: null = remove, Blob = upload + wrap, undefined = preserve\n4. putRecord with merged record\n\n### Replace lines 145-151:\n```ts\n// @ts-expect-error\nconst data = await ctx.scopedRepo.hypercerts.update({ uri: hypercertUri, updates, image });\nreturn NextResponse.json(data);\n```\n\nWith:\n```ts\nimport { uploadContentBlob } from '@/lib/atproto-writes';\n\n// Fetch existing record\nconst existingResult = await ctx.agent.com.atproto.repo.getRecord({\n  repo: parsed.did,\n  collection: parsed.collection || 'org.hypercerts.claim.activity',\n  rkey: parsed.rkey,\n});\nconst existing = existingResult.data.value as Record\u003cstring, unknown\u003e;\n\n// Merge updates into existing, preserving immutable fields\nconst record: Record\u003cstring, unknown\u003e = {\n  ...existing,\n  ...updates,\n  $type: 'org.hypercerts.claim.activity',\n  createdAt: existing.createdAt,  // immutable\n  rights: existing.rights,         // immutable\n};\n\n// Handle image\nif (image === null) {\n  // Remove image\n  delete record.image;\n} else if (image instanceof Blob) {\n  // Upload new image\n  const blobRef = await uploadContentBlob(ctx.agent, image);\n  record.image = { $type: 'org.hypercerts.defs#smallImage', image: blobRef };\n}\n// else image === undefined → preserve existing (already in spread)\n\nconst result = await ctx.agent.com.atproto.repo.putRecord({\n  repo: ctx.activeDid,\n  collection: parsed.collection || 'org.hypercerts.claim.activity',\n  rkey: parsed.rkey,\n  record,\n});\n\nreturn NextResponse.json({ uri: result.data.uri, cid: result.data.cid });\n```\n\nThe `parsed` variable already exists from line 116.\n\n## Don't\n- Don't change the POST handler (that's task .4)\n- Don't change form data parsing or validation\n- Don't change the ownership check","acceptance_criteria":"1. PUT handler uses getRecord + merge + putRecord\n2. Image handling: null removes, Blob uploads, undefined preserves\n3. @ts-expect-error removed\n4. createdAt and rights preserved as immutable\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":35,"created_at":"2026-03-03T22:40:55.612136797+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:50:22.72782433+06:00","closed_at":"2026-03-03T22:50:22.72782433+06:00","close_reason":"79006fb Migrate hypercerts.update PUT to native atproto getRecord + merge + putRecord","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-czy.5","depends_on_id":"hypercerts-czy","type":"parent-child","created_at":"2026-03-03T22:40:55.620396653+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.5","depends_on_id":"hypercerts-czy.1","type":"blocks","created_at":"2026-03-03T22:40:55.635541169+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-czy.6","title":"Migrate addAttachment and attachLocation to native atproto calls","description":"## Files\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n\n## What to do\n\n### addAttachment (app/api/certs/add-attachment/route.ts, line 127-136):\nThe SDK does: resolve subjects to StrongRefs, resolve content (string URL → uri ref, Blob → upload + smallBlob ref), optionally create location, createRecord.\n\nReplace:\n```ts\n// @ts-expect-error\nconst response = await ctx.scopedRepo.hypercerts.addAttachment({\n  subjects: hypercertUri,\n  content,\n  title,\n  shortDescription,\n  description,\n  contentType,\n  ...(location \u0026\u0026 { location }),\n});\n```\n\nWith:\n```ts\nimport { resolveStrongRef, createLocationRecord, uploadContentBlob, type LocationCreateParams } from '@/lib/atproto-writes';\n\n// 1. Resolve subject to StrongRef\nconst subjectRef = await resolveStrongRef(ctx.agent, hypercertUri, 'org.hypercerts.claim.activity');\n\n// 2. Resolve content — string URL or uploaded blob\nlet contentField: Record\u003cstring, unknown\u003e;\nif (typeof content === 'string') {\n  contentField = { $type: 'org.hypercerts.defs#uri', uri: content };\n} else {\n  // content is a Blob/File\n  const blobRef = await uploadContentBlob(ctx.agent, content);\n  contentField = { $type: 'org.hypercerts.defs#smallBlob', blob: blobRef };\n}\n\n// 3. Optionally create location record\nlet locationRef: { uri: string; cid: string } | undefined;\nif (location) {\n  if (typeof location === 'string') {\n    locationRef = await resolveStrongRef(ctx.agent, location, 'app.certified.location');\n  } else {\n    locationRef = await createLocationRecord(ctx.agent, ctx.activeDid, location as LocationCreateParams);\n  }\n}\n\n// 4. Build attachment record\nconst record: Record\u003cstring, unknown\u003e = {\n  $type: 'org.hypercerts.claim.attachment',\n  subjects: [subjectRef],\n  content: [contentField],\n  title,\n  createdAt: new Date().toISOString(),\n};\nif (shortDescription) record.shortDescription = shortDescription;\nif (description) record.description = description;\nif (contentType) record.contentType = contentType;\nif (locationRef) record.location = locationRef;\n\nconst result = await ctx.agent.com.atproto.repo.createRecord({\n  repo: ctx.activeDid,\n  collection: 'org.hypercerts.claim.attachment',\n  record,\n});\n\nreturn NextResponse.json({ uri: result.data.uri, cid: result.data.cid });\n```\n\n### attachLocation (app/api/certs/add-location/route.ts, line 88-92):\nThe SDK does: create location record, then fetch hypercert, append location to locations array, update hypercert.\n\nReplace:\n```ts\n// @ts-expect-error\nconst result = await personalRepository.hypercerts.attachLocation(hypercertUri, locationPayload);\n```\n\nWith:\n```ts\nimport { createLocationRecord, resolveStrongRef, type LocationCreateParams } from '@/lib/atproto-writes';\nimport { parseAtUri } from '@/lib/utils';\n\n// 1. Create location record\nconst locationRef = await createLocationRecord(\n  personalRepository,\n  personalRepository.assertDid,\n  locationPayload as LocationCreateParams,\n);\n\n// 2. Fetch existing hypercert and append location\nconst hypercertParsed = parseAtUri(hypercertUri);\nif (!hypercertParsed) throw new Error('Invalid hypercertUri');\n\nconst existingResult = await personalRepository.com.atproto.repo.getRecord({\n  repo: hypercertParsed.did,\n  collection: hypercertParsed.collection || 'org.hypercerts.claim.activity',\n  rkey: hypercertParsed.rkey,\n});\nconst existingRecord = existingResult.data.value as Record\u003cstring, unknown\u003e;\n\nconst existingLocations = (existingRecord.locations as unknown[]) || [];\nexistingRecord.locations = [...existingLocations, locationRef];\n\n// 3. Update hypercert\nawait personalRepository.com.atproto.repo.putRecord({\n  repo: personalRepository.assertDid,\n  collection: hypercertParsed.collection || 'org.hypercerts.claim.activity',\n  rkey: hypercertParsed.rkey,\n  record: existingRecord,\n});\n\nreturn NextResponse.json(locationRef);\n```\n\n### Also for add-location/route.ts:\n- Remove the `LocationParams` import from `@hypercerts-org/sdk-core` (line 2)\n- The `locationPayload` variable is already typed inline — just cast it to `LocationCreateParams` from atproto-writes\n\n## Don't\n- Don't change form data parsing or validation\n- Don't change error handling","acceptance_criteria":"1. addAttachment creates attachment record with resolved subject StrongRef and content (URI or blob)\n2. attachLocation creates location record then updates hypercert's locations array\n3. Both @ts-expect-error comments removed\n4. LocationParams import from sdk-core removed\n5. pnpm build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":50,"created_at":"2026-03-03T22:41:20.965690269+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:51:22.068145158+06:00","closed_at":"2026-03-03T22:51:22.068145158+06:00","close_reason":"69b6b4e Migrate addAttachment and attachLocation to native atproto calls","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-czy.6","depends_on_id":"hypercerts-czy","type":"parent-child","created_at":"2026-03-03T22:41:20.974109953+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.6","depends_on_id":"hypercerts-czy.1","type":"blocks","created_at":"2026-03-03T22:41:20.983058462+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-czy.7","title":"Verify build passes after Phase 4 — zero @ts-expect-error on scopedRepo","description":"## Files\n- Any file that fails pnpm build\n\n## What to do\nAfter tasks .1 through .6 are complete:\n1. Run `pnpm build` and fix any errors\n2. Run `grep -rn '@ts-expect-error' --include='*.ts' --include='*.tsx' app/ lib/ providers/` — should return ZERO results related to scopedRepo\n3. Run `grep -rn 'scopedRepo' --include='*.ts' --include='*.tsx' app/ lib/ providers/` — should return ZERO results\n4. Run `grep -rn 'sdk-core' --include='*.ts' --include='*.tsx' app/ lib/ providers/` — note remaining type-only imports (these are Phase 5)\n\n## Don't\n- Don't remove sdk-core type imports (Phase 5)\n- Don't change business logic","acceptance_criteria":"1. pnpm build succeeds\n2. Zero @ts-expect-error comments on scopedRepo operations\n3. Zero scopedRepo references in app/, lib/, providers/\n4. All 8 compound writes use native atproto calls","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-03T22:41:32.314955547+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-03T22:52:47.924183146+06:00","closed_at":"2026-03-03T22:52:47.924183146+06:00","close_reason":"2774096 verify Phase 4 build passes, zero scopedRepo refs","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-czy.7","depends_on_id":"hypercerts-czy","type":"parent-child","created_at":"2026-03-03T22:41:32.320207123+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.7","depends_on_id":"hypercerts-czy.2","type":"blocks","created_at":"2026-03-03T22:41:32.330942814+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.7","depends_on_id":"hypercerts-czy.3","type":"blocks","created_at":"2026-03-03T22:41:32.343310528+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.7","depends_on_id":"hypercerts-czy.4","type":"blocks","created_at":"2026-03-03T22:41:32.356360153+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.7","depends_on_id":"hypercerts-czy.5","type":"blocks","created_at":"2026-03-03T22:41:32.373496586+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-czy.7","depends_on_id":"hypercerts-czy.6","type":"blocks","created_at":"2026-03-03T22:41:32.38511632+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-d26","title":"Fix: Duplicate LocationEntry interface declaration in measurement-form.tsx","description":"## File\\ncomponents/measurement-form.tsx lines 38-65\\n\\n## Severity\\nMEDIUM — Type safety / code quality\\n\\n## Problem\\nThe LocationEntry interface is declared twice identically (lines 38-51 and lines 52-65). TypeScript allows duplicate interface declarations (they are merged), but this is a copy-paste error that creates confusion and maintenance risk — if one copy is updated but not the other, the merged type will have unexpected shape.\\n\\n## Evidence\\n```tsx\\n// Lines 38-51\\ninterface LocationEntry {\\n  id: string;\\n  mode: LocationEntryMode;\\n  stringValue: string;\\n  lpVersion: string;\\n  srs: string;\\n  locationType: LocationTypePreset;\\n  locationTypeCustom: string;\\n  name: string;\\n  description: string;\\n  contentMode: LocationContentMode;\\n  locationUrl: string;\\n  locationFile: File | null;\\n}\\n// Lines 52-65 — EXACT DUPLICATE\\ninterface LocationEntry {\\n  id: string;\\n  ...\\n}\\n```\\n\\n## Fix\\nRemove the duplicate declaration (lines 52-65). Keep only one.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:36.813175354+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:36.813175354+06:00"}
{"id":"hypercerts-dcb","title":"Epic: Resolve CodeRabbit review for PR #7","description":"Resolve all open CodeRabbit inline review comments on PR #7 (quick-fixes branch). 10 comments across 10 files, grouped into 8 tasks covering security (JWK allowlist), bugs (undefined profile vars, simultaneous loading/error states, loose URL check), error handling (3 API routes), and style/nit fixes.","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-24T14:41:48.496025906+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:45:22.525512587+06:00","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-dcb.1","title":"Fix: JWK private key stripping — switch denylist to allowlist","description":"## Files\n- app/jwks.json/route.ts (modify)\n\n## What to do\nCodeRabbit flagged: security issue at line 68.\n\n**Original comment:**\n\u003e Private key stripping may be incomplete for RSA JWKs. The filter only removes d, use, and key_ops. For EC keys this is sufficient. However, if RSA keys are ever used, the fields p, q, dp, dq, qi, and oth are also private and would be leaked through this endpoint. Consider using an allowlist of public fields instead of a denylist.\n\n**Suggested fix:**\nReplace the PRIVATE_KEY_FIELDS denylist with a PUBLIC_JWK_FIELDS allowlist. Define:\n```ts\nconst PUBLIC_JWK_FIELDS = [\"kty\", \"crv\", \"x\", \"y\", \"n\", \"e\", \"kid\", \"alg\"] as const;\n```\nThen filter entries to keep only keys in PUBLIC_JWK_FIELDS, and append `key_ops: [\"verify\"]` to each result.\n\n**Context:**\nThis is the JWKS endpoint that exposes public keys for OAuth signature verification. Leaking private key material would be a critical security issue. The current denylist approach is fragile — an allowlist is safer.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify the key generation or storage logic\n- Don't change the route handler signature or response structure","acceptance_criteria":"The keys mapping in app/jwks.json/route.ts uses an allowlist (PUBLIC_JWK_FIELDS or equivalent) containing only public JWK fields (kty, crv, x, y, n, e, kid, alg) instead of a denylist. The old PRIVATE_KEY_FIELDS array is removed. Each key in the response still includes key_ops: [\"verify\"]. No RSA private fields (d, p, q, dp, dq, qi, oth) can appear in the JSON response regardless of key type. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T14:42:05.114265845+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:35.870208756+06:00","closed_at":"2026-02-24T14:44:35.870208756+06:00","close_reason":"9555b4f fix: replace PRIVATE_KEY_FIELDS denylist with PUBLIC_JWK_FIELDS allowlist in jwks.json route — already implemented as part of hypercerts-1hr.3","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.1","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:42:05.115933853+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dcb.2","title":"Fix: activeProfileName/Handle always undefined in SignedInProvider","description":"## Files\n- providers/SignedInProvider.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug at line 20.\n\n**Original comment:**\n\u003e activeProfileName and activeProfileHandle are declared as const and never assigned, so they are always undefined. Unlike avatarUrl and handle (which are let and populated from the profile), these remain unused yet are passed to Navbar.\n\n**Suggested fix:**\n1. Change `const activeProfileName: string | undefined = undefined;` to `let activeProfileName: string | undefined = undefined;`\n2. Change `const activeProfileHandle: string | undefined = undefined;` to `let activeProfileHandle: string | undefined = undefined;`\n3. Inside the `if (repo)` block, after the profile is fetched, add:\n   ```ts\n   activeProfileName = profile?.displayName;\n   activeProfileHandle = profile?.handle;\n   ```\n\n**Context:**\nThese variables are passed as props to the Navbar component. Without this fix, the Navbar never receives the user's display name or handle, even when the profile is successfully loaded.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify the Navbar component itself\n- Don't change how avatarUrl or handle are assigned","acceptance_criteria":"In providers/SignedInProvider.tsx, activeProfileName and activeProfileHandle are declared with let (not const). Both are assigned from the profile data inside the if (repo) block: activeProfileName = profile?.displayName and activeProfileHandle = profile?.handle. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T14:42:11.795199379+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:16.844939698+06:00","closed_at":"2026-02-24T14:44:16.844939698+06:00","close_reason":"092e10e fix: populate activeProfileName and activeProfileHandle in SignedInProvider - already implemented, TypeScript compiles cleanly","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.2","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:42:11.797185182+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dcb.3","title":"Fix: loading and error states render simultaneously in evaluations section","description":"## Files\n- components/hypercert-evaluations-section.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug at line 87.\n\n**Original comment:**\n\u003e If isLoadingLinks is false but isLoadingDetails is true while isErrorLinks is also true, both the skeleton and error panel will render at the same time. Consider using an if/else-if chain or guarding error rendering with !isLoading.\n\n**Suggested fix:**\nReplace the three separate conditional renders with a single if/else-if chain:\n```tsx\n{isLoading ? (\n  ...skeleton...\n) : isError ? (\n  ...error panel...\n) : (\n  ...content...\n)}\n```\nThis ensures only one of skeleton, error, or content renders at any time. Use a combined loading flag if needed: `const isLoading = isLoadingLinks || isLoadingDetails;`\n\n**Context:**\nThe component currently has three independent conditional blocks that can overlap. The skeleton shows during loading, the error panel shows on error, and content shows when neither — but the guards are not mutually exclusive.\n\n## Don't\n- Don't change the skeleton component itself\n- Don't change the error panel content or styling\n- Don't change the data fetching hooks\n- Don't change unrelated code in the same file","acceptance_criteria":"In components/hypercert-evaluations-section.tsx, the loading skeleton, error panel, and content are rendered in a mutually exclusive if/else-if chain (or equivalent ternary). It is impossible for both the skeleton and error panel to be visible at the same time. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T14:42:20.27412414+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:13.658180326+06:00","closed_at":"2026-02-24T14:44:13.658180326+06:00","close_reason":"9487246 Fix already implemented: loading/error/content rendered in mutually exclusive if/else-if chain","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.3","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:42:20.276160649+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dcb.4","title":"Fix: loose includes(\"https\") URL check in measurement view","description":"## Files\n- components/hypercert-measurement-view.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug at line 118.\n\n**Original comment:**\n\u003e uri?.includes(\"https\") is a loose URL protocol check. It would match any URI containing \"https\" anywhere in the string (e.g., a path segment). Use uri?.startsWith(\"https\") for a more precise protocol check.\n\n**Suggested fix:**\nChange:\n```tsx\nuri={uri?.includes(\"https\") ? uri : getPDSlsURI(uri)}\n```\nTo:\n```tsx\nuri={uri?.startsWith(\"https\") ? uri : getPDSlsURI(uri)}\n```\n\n**Context:**\nThe URILink component receives a uri prop. If the URI is already an https URL, it's used directly; otherwise it's converted via getPDSlsURI(). The current includes() check could incorrectly match URIs like `at://user.example/https-path`.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify the URILink or getPDSlsURI implementations\n- Don't change other URI checks elsewhere in the file","acceptance_criteria":"In components/hypercert-measurement-view.tsx, the URI check uses uri?.startsWith(\"https\") instead of uri?.includes(\"https\"). The includes(\"https\") pattern does not appear anywhere in this file. TypeScript compiles without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T14:42:27.438715582+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:26.43425989+06:00","closed_at":"2026-02-24T14:44:26.43425989+06:00","close_reason":"87309cc Fix loose includes(https) URL check — already applied","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.4","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:42:27.440338291+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dcb.5","title":"Fix: safe error handling across 3 API routes (add-location, profile/update, auth/logout)","description":"## Files\n- app/api/certs/add-location/route.ts (modify)\n- app/api/profile/update/route.ts (modify)\n- app/api/auth/logout/route.ts (modify)\n\n## What to do\nCodeRabbit flagged 3 related error-handling issues:\n\n1. [error-handling] Line 102 in add-location/route.ts: `(e as Error).message` leaks internal error details to the client and is undefined if e is not an Error instance. Replace with safe extraction: `const detail = e instanceof Error ? e.message : \"Unknown error\";` and use a generic message in the response (do NOT include the raw error message in the client response — keep it server-side only via console.error which is already present).\n\n2. [error-handling] Line 128 in profile/update/route.ts: Same `(error as Error).message` unsafe cast pattern. Replace with `error instanceof Error ? error.message : String(error)` for the server-side log, and return only a generic error message to the client.\n\n3. [error-handling] Line 21 in auth/logout/route.ts: Raw error message leaked via template literal `Logout failed: ${(e as Error).message}`. Change to return only `{ error: \"Logout failed\" }` without the raw message. Keep the existing server-side logging.\n\n**Suggested fixes:**\nFor all 3 routes:\n- Use `e instanceof Error ? e.message : String(e)` for server-side logging\n- Return only generic error messages to the client (no `.message` interpolation)\n- Keep existing console.error/logging calls intact\n\n**Context:**\nThese are API routes that catch errors and return JSON responses. Leaking internal error messages (Redis connection errors, SDK internals, file paths) to API consumers is a security concern. The server-side logging already captures the full error.\n\n## Don't\n- Don't change unrelated code in these files\n- Don't remove the existing console.error/logging calls\n- Don't change the HTTP status codes\n- Don't introduce new dependencies","acceptance_criteria":"In all 3 files: (1) The pattern (e as Error).message or (error as Error).message does not appear in any client-facing NextResponse.json() call. (2) Each catch block uses instanceof Error check for safe error extraction in server-side logging. (3) Client responses contain only generic error messages without interpolated error details. (4) Existing console.error calls are preserved. (5) TypeScript compiles without errors.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-24T14:42:40.470306151+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:38.713096514+06:00","closed_at":"2026-02-24T14:44:38.713096514+06:00","close_reason":"f5fbb5d fix: safe error handling in add-location and profile/update routes","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.5","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:42:40.471988453+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dcb.6","title":"Fix: stale step label \"Step 2 of 6\" in form skeleton","description":"## Files\n- components/form-skeletons.tsx (modify)\n\n## What to do\nCodeRabbit flagged: nit at line 116.\n\n**Original comment:**\n\u003e The PR objectives mention correcting hardcoded step labels for a 5-step flow, but ContributionFormSkeleton still shows \"Step 2 of 6\". The TODO comment suggests this step is currently disabled — if so, this is a low-risk inconsistency, but it may confuse future contributors re-enabling this step.\n\n**Suggested fix:**\nIn the ContributionFormSkeleton component, change the stepLabel prop of FormInfoHeaderSkeleton from \"Step 2 of 6\" to \"Step 2 of 5\". Also update or remove the TODO comment about the disabled contributions step to reflect the current state.\n\n**Context:**\nThe form flow was reduced from 6 steps to 5. Other step labels were updated but this skeleton was missed.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify other step labels that are already correct\n- Don't change the FormInfoHeaderSkeleton component itself","acceptance_criteria":"In components/form-skeletons.tsx, the ContributionFormSkeleton uses stepLabel \"Step 2 of 5\" (not \"Step 2 of 6\"). The string \"of 6\" does not appear in this file. TypeScript compiles without errors.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T14:42:47.547685613+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:27.133489389+06:00","closed_at":"2026-02-24T14:44:27.133489389+06:00","close_reason":"c6331a8 fix already applied: ContributionFormSkeleton uses Step 2 of 5, no 'of 6' in file, TypeScript compiles","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.6","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:42:47.549813582+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dcb.7","title":"Fix: make image upload trigger keyboard-accessible","description":"## Files\n- components/image-uploader.tsx (modify)\n\n## What to do\nCodeRabbit flagged: style/accessibility issue at line 98.\n\n**Original comment:**\n\u003e The \u003clabel\u003e used as the upload trigger isn't linked to any input (no htmlFor, no nested control) and is only clickable via the onClick handler. Unlike \u003cbutton\u003e, a \u003clabel\u003e is not keyboard-focusable by default, so keyboard-only users cannot trigger the file picker.\n\n**Suggested fix:**\nReplace the \u003clabel\u003e element with a \u003cbutton type=\"button\"\u003e element. Keep the same:\n- onClick={() =\u003e inputRef.current?.click()) handler\n- className with cn(...) including the isBanner conditional\n- Children (Camera icon and Upload span)\n\nAdd aria-label=\"Upload image\" for screen reader support.\n\n**Context:**\nThis is the upload trigger shown when no image is currently set. It needs to be keyboard-focusable for accessibility compliance.\n\n## Don't\n- Don't change unrelated code in the same file\n- Don't modify the hidden file input element\n- Don't change the image preview/display logic\n- Don't introduce new dependencies","acceptance_criteria":"In components/image-uploader.tsx, the upload trigger uses a \u003cbutton type=\"button\"\u003e element instead of a \u003clabel\u003e. The button has an aria-label attribute. The onClick handler, className, and children (Camera icon + Upload span) are preserved. No \u003clabel\u003e element without an associated input remains in the upload trigger area. TypeScript compiles without errors.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T14:42:56.160355377+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:24.555090534+06:00","closed_at":"2026-02-24T14:44:24.555090534+06:00","close_reason":"c6331a8 fix already implemented: image-uploader.tsx uses button type=button with aria-label","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.7","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:42:56.163719937+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dcb.8","title":"Fix: add lexicons/** to .prettierignore","description":"## Files\n- .prettierignore (modify)\n\n## What to do\nCodeRabbit flagged: style issue on lexicons/types/app/certified/location.ts line 53.\n\n**Original comment:**\n\u003e This file is marked GENERATED CODE - DO NOT MODIFY but has been formatted with Prettier. The .prettierignore file does not currently exclude the lexicons/ directory, so re-running the code generator will produce unformatted output and create diff noise. Add lexicons/** to .prettierignore to align with the ESLint globalIgnores already added in this PR.\n\n**Suggested fix:**\nAdd a line `lexicons/**` to the .prettierignore file. Place it near any existing ignore patterns for generated code.\n\n**Context:**\nThe lexicons/ directory contains auto-generated TypeScript types from ATProto lexicon schemas. ESLint already ignores this directory via globalIgnores in the eslint config. Prettier should also ignore it to prevent formatting churn when types are regenerated.\n\n## Don't\n- Don't modify any files in the lexicons/ directory\n- Don't change existing .prettierignore entries\n- Don't modify the ESLint config","acceptance_criteria":"The .prettierignore file contains a line with lexicons/** (or lexicons/). Running npx prettier --check lexicons/ does not format or flag any files in the lexicons directory.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T14:43:04.051374485+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T14:44:03.848838702+06:00","closed_at":"2026-02-24T14:44:03.848838702+06:00","close_reason":"2cf834a lexicons/ already present in .prettierignore; prettier --check lexicons/ passes","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dcb.8","depends_on_id":"hypercerts-dcb","type":"parent-child","created_at":"2026-02-24T14:43:04.053116888+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dhz","title":"Epic: Add ePDS email/OTP login (scrappy e2e)","description":"Add ePDS email/OTP login as a second login option alongside the existing handle-based SDK login. The goal is to validate the full flow end-to-end as fast as possible — especially that manually constructing a NodeSavedSession and writing it to Redis works with sdk.restoreSession(). Polish comes later.\n\nSuccess looks like: a user can click 'Sign in with email', enter their email, get redirected to ePDS, enter the OTP, get redirected back, and the app recognizes them as logged in (getSession/getAuthenticatedRepo work).\n\nKey constraints:\n- ePDS uses a separate PDS URL (NEXT_PUBLIC_EPDS_URL env var)\n- sdk.authorize() cannot be used because it requires a handle, not an email\n- We must do PAR + PKCE + DPoP manually for the login handler\n- sdk.callback() cannot be used because it expects state in Redis (written by sdk.authorize)\n- After manual token exchange, we construct a NodeSavedSession and write it to RedisSessionStore so sdk.restoreSession() works transparently\n- The user-did cookie is set identically to the existing flow\n- UX is scrappy — just needs to work, not look good","status":"closed","priority":1,"issue_type":"epic","owner":"kzoepa@gmail.com","created_at":"2026-02-24T14:35:50.225039451+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:13:25.162217925+06:00","closed_at":"2026-02-24T17:13:25.162217925+06:00","close_reason":"All 12 children closed. All P1 discovered bugs closed. Most P2 discovered bugs closed (already fixed in dhz.11-16). E2E flow validated working with both Flow 1 (email→direct OTP) and Flow 2 (no email→ePDS collects). Remaining P2/P3 discovered issues (lv8, 7vo, 6a6, cev, x2b) are polish items for a follow-up epic.","labels":["scope:medium"]}
{"id":"hypercerts-dhz.1","title":"Add NEXT_PUBLIC_EPDS_URL and OAUTH_SESSION_SECRET to config","description":"## Files\n- lib/config.ts (modify)\n- .env.example (modify)\n\n## What to do\nAdd two new config values:\n\n1. `epdsUrl` — read from `process.env.NEXT_PUBLIC_EPDS_URL`. This is the ePDS PDS base URL (e.g. `https://pds-eu-west4.test.certified.app`). Add it to the `config` object. Do NOT add it to the required env vars validation — it should be optional (only needed if ePDS login is used).\n\n2. `oauthSessionSecret` — read from `process.env.OAUTH_SESSION_SECRET`. This is a server-only secret (32+ chars) used to HMAC-sign the transient OAuth session cookie. Add it to the `config` object. Do NOT add to required env vars — it should be optional.\n\n3. Add `epdsRedirectUri` — `${redirectBaseUrl}/api/oauth/callback` to the config object.\n\n4. In `.env.example`, add a new section documenting both vars with comments explaining their purpose.\n\n## Don't\n- Add these to the required env vars array (they're optional)\n- Change any existing config values\n- Add any NEXT_PUBLIC_ prefix to OAUTH_SESSION_SECRET (it's server-only)","acceptance_criteria":"1. config.epdsUrl returns the value of NEXT_PUBLIC_EPDS_URL when set\n2. config.oauthSessionSecret returns the value of OAUTH_SESSION_SECRET when set  \n3. config.epdsRedirectUri returns ${redirectBaseUrl}/api/oauth/callback\n4. .env.example documents both new vars with examples\n5. App still starts without these vars set (no validation errors)\n6. pnpm build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":20,"created_at":"2026-02-24T14:36:04.477615026+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:06:09.054309427+06:00","closed_at":"2026-02-24T15:06:09.054309427+06:00","close_reason":"9f485b0 Add epdsUrl, oauthSessionSecret, epdsRedirectUri to config","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dhz.1","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T14:36:04.48095545+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.10","title":"Support Flow 1 — pass email as login_hint on auth redirect URL only (not PAR body)","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n- components/login-dialog.tsx (modify)\n\n## Context\nWe discovered from the ePDS demo source (packages/demo/src/app/api/oauth/login/route.ts) that:\n- The PAR body must NEVER include login_hint (PDS core validates it as ATProto identity, rejects emails)\n- The login_hint with an email goes ONLY on the auth redirect URL query param\n- The auth service (login-page.ts) reads login_hint from the redirect URL, checks for '@', and if present skips the email form and goes straight to OTP entry\n\nThis means one route can handle both Flow 1 and Flow 2 — the only difference is whether login_hint is appended to the auth redirect URL.\n\n## What to do\n\n### app/api/oauth/login/route.ts\n1. Read optional `email` from query params: `const email = req.nextUrl.searchParams.get('email')`\n2. PAR body stays exactly as-is — NO login_hint (this is already correct after dhz.9)\n3. When building the auth redirect URL, conditionally append login_hint:\n```typescript\nconst authUrl = new URL(authEndpoint)\nauthUrl.searchParams.set('client_id', clientId)\nauthUrl.searchParams.set('request_uri', request_uri)\nif (email) {\n  authUrl.searchParams.set('login_hint', email)\n}\nconst response = NextResponse.redirect(authUrl.toString())\n```\n4. Keep all other logic identical (DPoP, PKCE, PAR, cookie, etc.)\n\n### components/login-dialog.tsx\nThe login dialog should have 3 login methods:\n1. **Handle login** (existing, unchanged) — handle input + 'Sign In' button\n2. **Email login** (Flow 1) — email input + 'Sign in with email' button\n3. **ePDS login** (Flow 2) — 'Sign in with email' button works even when email is empty\n\nImplementation:\n1. Add back the email state: `const [email, setEmail] = useState('')`\n2. Add back the MailIcon import from lucide-react\n3. After the existing handle login section, add an 'or' divider\n4. Add an email input field (type='email', placeholder='user@example.com') using the same InputGroup pattern\n5. Add a 'Sign in with email' button that:\n   - If email is non-empty: navigates to `/api/oauth/login?email=${encodeURIComponent(email)}` (Flow 1)\n   - If email is empty: navigates to `/api/oauth/login` (Flow 2)\n   - The button is ALWAYS enabled (works for both flows)\n   - Uses `window.location.href` for navigation (GET redirect, not fetch)\n\n## Don't\n- Add login_hint to the PAR body — this is what caused the original 400 error\n- Remove or change the existing handle-based login\n- Change the callback route\n- Add any new dependencies or mutation hooks","acceptance_criteria":"1. GET /api/oauth/login?email=test@example.com redirects to auth endpoint with login_hint=test@example.com in the URL\n2. GET /api/oauth/login (no email) redirects to auth endpoint WITHOUT login_hint in the URL\n3. Neither request includes login_hint in the PAR body\n4. LoginDialog shows: handle input + Sign In, then 'or' divider, then email input + 'Sign in with email' button\n5. 'Sign in with email' button works with or without email entered\n6. Existing handle-based login still works\n7. pnpm tsc --noEmit succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":25,"created_at":"2026-02-24T16:00:28.619794325+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:02:08.357121127+06:00","closed_at":"2026-02-24T16:02:08.357121127+06:00","close_reason":"82d32d0 feat: support Flow 1 email login_hint on auth redirect URL","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.10","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T16:00:28.621885174+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.11","title":"Fix: add try/catch to both OAuth routes + DPoP nonce retry on 401","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n- app/api/oauth/callback/route.ts (modify)\n- lib/epds-helpers.ts (modify)\n\n## What to do\n\n### 1. Add top-level try/catch to both routes (fixes hypercerts-ycl)\n\nBoth routes currently have no top-level error handling. Network errors, JSON parse failures, Redis errors, and missing env vars all crash with unhandled 500.\n\nWrap the entire body of both GET handlers in try/catch:\n\n```typescript\n// app/api/oauth/login/route.ts\nexport async function GET(req: NextRequest): Promise\u003cNextResponse\u003e {\n  try {\n    // ... existing code ...\n  } catch (error) {\n    console.error('[oauth/login] Unexpected error:', error)\n    // Redirect to home with error param instead of raw 500\n    return NextResponse.redirect(new URL('/?error=auth_failed', config.baseUrl))\n  }\n}\n```\n\n```typescript\n// app/api/oauth/callback/route.ts\nexport async function GET(req: NextRequest): Promise\u003cNextResponse\u003e {\n  try {\n    // ... existing code ...\n  } catch (error) {\n    console.error('[oauth/callback] Unexpected error:', error)\n    return NextResponse.redirect(new URL('/?error=auth_failed', config.baseUrl))\n  }\n}\n```\n\nAlso convert the existing inline error returns (400s, 502s) in the callback route to redirects with error params instead of raw JSON, since these are browser-navigated routes:\n- 400 missing params → redirect to `/?error=auth_failed`\n- 400 no session cookie → redirect to `/?error=auth_failed`\n- 400 state mismatch → redirect to `/?error=auth_failed`\n- 502 token exchange failed → redirect to `/?error=auth_failed`\n\nKeep the console.error logging for each case so we can debug server-side.\n\nFor the login route, the PAR error can stay as JSON (or redirect) — either is fine since the user sees it briefly before redirect.\n\n### 2. Fix DPoP nonce retry to also handle 401 (fixes hypercerts-0xi)\n\nIn `lib/epds-helpers.ts`, the `fetchWithDpopRetry` function currently only retries on status 400:\n\n```typescript\nif (!firstRes.ok \u0026\u0026 firstRes.status === 400) {\n```\n\nThe AT Protocol spec allows servers to return 401 with a `dpop-nonce` header for the `use_dpop_nonce` error. Change the condition to:\n\n```typescript\nif (!firstRes.ok \u0026\u0026 (firstRes.status === 400 || firstRes.status === 401)) {\n```\n\nThe rest of the retry logic stays the same — check for `dpop-nonce` header, retry with nonce.\n\n## Don't\n- Change the happy path logic (PAR, token exchange, session construction)\n- Change the cookie handling\n- Add any new dependencies\n- Change the UI","acceptance_criteria":"1. Both routes have top-level try/catch that logs errors and redirects to /?error=auth_failed\n2. Callback route returns redirects (not JSON) for all error cases\n3. fetchWithDpopRetry retries on both 400 and 401 when dpop-nonce header is present\n4. All error cases log to console.error with descriptive messages\n5. pnpm tsc --noEmit succeeds\n6. Happy path is unchanged","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-24T16:36:23.211737377+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:39:38.876650212+06:00","closed_at":"2026-02-24T16:39:38.876650212+06:00","close_reason":"b4f7ed9 fix: add try/catch to OAuth routes + DPoP nonce retry on 401","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.11","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T16:36:23.213507157+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.12","title":"Fix: cookie security flags — sameSite on user-did, proper deletion of epds-oauth-session","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\n\n### 1. Add sameSite: 'lax' to user-did cookie (fixes hypercerts-5jd / hypercerts-6r0)\n\nThe user-did cookie is set without sameSite. Since the callback is a cross-site redirect from `auth.\u003cpds-hostname\u003e` back to our app, some browsers (especially Safari) may block the cookie without an explicit `sameSite: 'lax'`.\n\nChange the user-did cookie setting from:\n```typescript\nresponse.cookies.set('user-did', tokenData.sub, {\n  httpOnly: true,\n  secure: config.isProduction,\n  maxAge: 60 * 60 * 24 * 7,\n  path: '/',\n})\n```\n\nTo:\n```typescript\nresponse.cookies.set('user-did', tokenData.sub, {\n  httpOnly: true,\n  secure: config.isProduction,\n  sameSite: 'lax',\n  maxAge: 60 * 60 * 24 * 7,\n  path: '/',\n})\n```\n\n### 2. Fix epds-oauth-session cookie deletion (fixes hypercerts-0p8)\n\nThe cookie deletion currently only sets maxAge=0 and path, but Safari requires matching httpOnly/secure/sameSite flags to properly delete a cookie.\n\nChange from:\n```typescript\nresponse.cookies.set(EPDS_SESSION_COOKIE_NAME, '', {\n  maxAge: 0,\n  path: '/',\n})\n```\n\nTo:\n```typescript\nresponse.cookies.set(EPDS_SESSION_COOKIE_NAME, '', {\n  httpOnly: true,\n  secure: config.isProduction,\n  sameSite: 'lax',\n  maxAge: 0,\n  path: '/',\n})\n```\n\n## Don't\n- Change any other cookie settings\n- Change the session construction logic\n- Change the login route","acceptance_criteria":"1. user-did cookie includes sameSite: 'lax'\n2. epds-oauth-session deletion includes httpOnly, secure, sameSite flags matching the original set\n3. pnpm tsc --noEmit succeeds","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":10,"created_at":"2026-02-24T16:36:39.21136517+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:38:19.363740567+06:00","closed_at":"2026-02-24T16:38:19.363740567+06:00","close_reason":"c323ec7 fix: cookie security flags — sameSite lax on user-did, proper deletion of epds-oauth-session","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dhz.12","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T16:36:39.213621463+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.13","title":"Fix: validate tokenData.sub as DID + add TTL to Redis session","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\n\n### 1. Validate tokenData.sub as a DID before using it (fixes hypercerts-xp2)\n\nAfter parsing the token response, validate that `sub` looks like a DID before writing it to Redis and setting it as a cookie. An empty or malformed sub would poison the session store.\n\nAdd after line ~100 (after parsing tokenData):\n```typescript\n// Validate sub is a DID\nif (!tokenData.sub || !(tokenData.sub.startsWith('did:plc:') || tokenData.sub.startsWith('did:web:'))) {\n  console.error('[oauth/callback] Invalid sub in token response:', tokenData.sub)\n  return NextResponse.redirect(new URL('/?error=auth_failed', config.baseUrl))\n}\n```\n\n### 2. Add TTL to Redis session (fixes hypercerts-7vo)\n\nCurrently `sessionStore.set(did, session)` writes with no expiry. ePDS sessions would persist in Redis forever.\n\nCheck if `sessionStore.set` accepts a TTL parameter. If it does, pass a reasonable TTL (e.g. 7 days = 604800 seconds, matching the user-did cookie maxAge).\n\nIf `sessionStore.set` does NOT accept a TTL (check the RedisSessionStore implementation in lib/redis-session-store.ts), then add a comment noting this limitation:\n```typescript\n// TODO: RedisSessionStore.set() does not support TTL — sessions persist until manually deleted\n// The SDK handles token refresh internally, so stale sessions get refreshed or error out\n```\n\n## Don't\n- Change the NodeSavedSession shape\n- Change the DPoP or PKCE logic\n- Add new dependencies","acceptance_criteria":"1. Token response sub is validated as did:plc: or did:web: before use\n2. Invalid sub results in redirect to /?error=auth_failed with console.error log\n3. Redis TTL is set if sessionStore supports it, or a TODO comment is added\n4. pnpm tsc --noEmit succeeds","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":15,"created_at":"2026-02-24T16:36:56.043111252+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:38:46.783050803+06:00","closed_at":"2026-02-24T16:38:46.783050803+06:00","close_reason":"6da38d4 fix: validate tokenData.sub as DID + add TTL TODO to Redis session","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dhz.13","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T16:36:56.045067632+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.14","title":"Fix: use config.oauthSessionSecret instead of direct process.env + fix auth endpoint port handling","description":"## Files\n- lib/epds-session-cookie.ts (modify)\n- lib/epds-config.ts (modify)\n\n## What to do\n\n### 1. Use config.oauthSessionSecret instead of process.env (fixes hypercerts-2wn)\n\nIn `lib/epds-session-cookie.ts`, the HMAC secret is read directly from `process.env.OAUTH_SESSION_SECRET` instead of using the centralized `config.oauthSessionSecret` that was added in dhz.1.\n\nChange the import and secret reading:\n```typescript\nimport { config } from '@/lib/config'\n\n// In encodeEpdsSessionCookie:\nconst secret = config.oauthSessionSecret\nif (!secret) {\n  throw new Error('OAUTH_SESSION_SECRET is not configured')\n}\n\n// In decodeEpdsSessionCookie:\nconst secret = config.oauthSessionSecret\nif (!secret) {\n  throw new Error('OAUTH_SESSION_SECRET is not configured')\n}\n```\n\n### 2. Fix auth endpoint to preserve port from NEXT_PUBLIC_EPDS_URL (fixes hypercerts-vmn)\n\nIn `lib/epds-config.ts`, the auth endpoint is constructed using only `hostname`:\n```typescript\nauthEndpoint: `https://auth.${new URL(epdsUrl).hostname}/oauth/authorize`\n```\n\nThis drops the port if `NEXT_PUBLIC_EPDS_URL` includes one (e.g. `https://localhost:3001`). Use `host` instead of `hostname` to preserve the port:\n```typescript\nconst url = new URL(epdsUrl)\nconst authHost = url.port ? `auth.${url.hostname}:${url.port}` : `auth.${url.hostname}`\n// authEndpoint: `${url.protocol}//${authHost}/oauth/authorize`\n```\n\nAlso use `url.protocol` instead of hardcoding `https://` — this allows local development with http.\n\n## Don't\n- Change the HMAC algorithm or cookie format\n- Change the endpoint paths (/oauth/par, /oauth/token, /oauth/authorize)\n- Add new dependencies","acceptance_criteria":"1. epds-session-cookie.ts reads secret from config.oauthSessionSecret, not process.env\n2. Auth endpoint preserves port when NEXT_PUBLIC_EPDS_URL includes one\n3. Auth endpoint uses the protocol from the URL (not hardcoded https)\n4. pnpm tsc --noEmit succeeds","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":15,"created_at":"2026-02-24T16:37:12.758946647+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:38:32.997832196+06:00","closed_at":"2026-02-24T16:38:32.997832196+06:00","close_reason":"98de1d5 fix: use config.oauthSessionSecret + preserve port in auth endpoint","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dhz.14","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T16:37:12.761513973+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.15","title":"Redesign login UX: two-door dialog with distinct handle vs email paths","description":"## Files\n- components/login-dialog.tsx (rewrite)\n- components/navbar.tsx (modify)\n- app/globals.css (modify — add new animations/styles)\n\n## Design Concept: Two Doors\n\nThe login dialog presents two distinct visual \"doors\" — side-by-side panels that represent two different identity paradigms:\n\n**Left door: Handle** — technical, precise, monospace-flavored. For users who already have an ATProto identity.\n**Right door: Email** — warm, approachable, familiar. For users who just want to sign in with their email.\n\nEach door is a clickable card. Clicking one expands it into the active form while the other recedes. This is progressive disclosure — you choose your path first, then the relevant form appears.\n\n### Visual Design\n\n**Initial state (two doors side by side):**\n- Dialog opens showing two equal-width cards/panels side by side\n- Left card: dark/charcoal background (`create-accent`), white text. Icon: `AtSignIcon`. Title: \"Handle\". Subtitle: \"Sign in with your ATProto identity\"\n- Right card: warm/light background (subtle warm tint or glass-panel). Icon: `MailIcon`. Title: \"Email\". Subtitle: \"Sign in with a one-time code\"\n- Both cards have a subtle hover effect (slight scale, border glow)\n- Cards should feel like actual doors/portals — generous padding, centered content, maybe a subtle gradient or texture difference between them\n\n**Selected state (one door expanded):**\n- Clicking a door transitions to showing only that door's form\n- The selected door expands to fill the dialog width\n- The other door shrinks to a small pill/chip at the top (so you can switch back)\n- Transition should be smooth — use CSS transitions on width/opacity/transform\n- The form content fades in with staggered animation (use existing `stagger-children` class)\n\n**Handle form (when handle door is selected):**\n- Same as existing: handle InputGroup with AtSignIcon, Sign In button, Create account link\n- Uses `useLoginMutation` for the submit\n- Shows Spinner when pending\n\n**Email form (when email door is selected):**\n- Email InputGroup with MailIcon (optional — user can leave empty for Flow 2)\n- \"Continue\" button — navigates to `/api/oauth/login?email=...` or `/api/oauth/login`\n- Small helper text: \"Enter your email to skip straight to the code, or leave blank\"\n- Uses `window.location.href` for navigation\n\n**Only show email door when `process.env.NEXT_PUBLIC_EPDS_URL` is set.** If not set, show only the handle form (no doors, just the form directly).\n\n### Navbar Changes\n\nReplace the Popover login in navbar.tsx with a Dialog:\n1. Remove the inline form state (`handle`, `handleSubmit`) from navbar\n2. Import `Dialog`, `DialogContent`, `DialogTrigger` from `@/components/ui/dialog`\n3. Import `LoginDialog` from `@/components/login-dialog`\n4. The \"Sign In\" button becomes a `DialogTrigger`\n5. `DialogContent` wraps `\u003cLoginDialog /\u003e` with `sm:max-w-xl` or `sm:max-w-2xl` (wider than default to fit two doors)\n6. Add `DialogTitle` with sr-only text for accessibility\n7. Keep all signed-in state (dropdown, links, avatar) completely unchanged\n\n### Component Structure\n\n```tsx\n// login-dialog.tsx\nexport default function LoginDialog() {\n  const [selectedDoor, setSelectedDoor] = useState\u003c'handle' | 'email' | null\u003e(null)\n  const hasEpds = !!process.env.NEXT_PUBLIC_EPDS_URL\n  \n  // If no ePDS, skip doors and show handle form directly\n  if (!hasEpds) {\n    return \u003cHandleLoginForm /\u003e\n  }\n  \n  return (\n    \u003cdiv\u003e\n      {selectedDoor === null ? (\n        // Two doors side by side\n        \u003cDoorSelection onSelect={setSelectedDoor} /\u003e\n      ) : (\n        // Selected door expanded with form\n        \u003cExpandedDoor \n          selected={selectedDoor} \n          onBack={() =\u003e setSelectedDoor(null)} \n        /\u003e\n      )}\n    \u003c/div\u003e\n  )\n}\n```\n\n### Aesthetic Details\n\n- Use Syne for door titles, Outfit for subtitles and form labels\n- The handle door should feel \"technical\" — darker, maybe a subtle monospace hint in the subtitle\n- The email door should feel \"friendly\" — lighter, warmer\n- Transition between doors and expanded state: 300ms ease-out, opacity + transform\n- The \"back\" action (clicking the inactive pill) should reverse the animation\n- Use the existing `glass-panel` class for the email door background\n- Use `create-accent` bg for the handle door\n- Add a subtle noise texture (`noise-bg`) to the handle door for depth\n- Hover states: slight translateY(-2px) + shadow increase\n\n### Animations to add to globals.css\n\n```css\n@keyframes expandDoor {\n  from { opacity: 0.8; transform: scale(0.95); }\n  to { opacity: 1; transform: scale(1); }\n}\n\n.animate-expand-door {\n  animation: expandDoor 0.3s ease-out both;\n}\n```\n\n## Don't\n- Add framer-motion or any new npm dependencies — CSS animations only\n- Change any backend routes or auth logic\n- Change the signed-in navbar state (dropdown, avatar, links)\n- Break the existing handle login flow (useLoginMutation must still work)\n- Break the ePDS email flow (window.location.href to /api/oauth/login must still work)\n- Use generic tab components — this should feel custom and intentional\n- Make the doors look like boring cards — they should have distinct visual character","acceptance_criteria":"1. Navbar 'Sign In' button opens a Dialog (not a Popover) containing LoginDialog\n2. LoginDialog shows two distinct door panels when NEXT_PUBLIC_EPDS_URL is set\n3. Clicking a door expands it to show the relevant form, other door becomes a switchable pill\n4. Handle door form: handle input + Sign In + Create account — uses useLoginMutation\n5. Email door form: optional email input + Continue — navigates to /api/oauth/login\n6. When NEXT_PUBLIC_EPDS_URL is NOT set, LoginDialog shows handle form directly (no doors)\n7. Transitions between states are smooth (CSS, 300ms)\n8. Handle door has dark/charcoal aesthetic, email door has light/warm aesthetic\n9. Navbar no longer has inline login form state — all login logic is in LoginDialog\n10. pnpm tsc --noEmit succeeds\n11. Existing handle login still works end-to-end\n12. Existing email login still works end-to-end","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":60,"created_at":"2026-02-24T16:58:24.93044585+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:01:36.108017033+06:00","closed_at":"2026-02-24T17:01:36.108017033+06:00","close_reason":"7ad9152 feat: two-door login dialog with handle/email paths","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-dhz.15","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T16:58:24.932781244+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.16","title":"Redesign login dialog: pill toggle tabs, same width as original popover","description":"## Files\n- components/login-dialog.tsx (rewrite)\n- components/navbar.tsx (modify)\n\n## Context\nThe two-door design (dhz.15) was too wide and extended outside the navbar. We need a compact pill toggle design that fits within the same width as the original popover (w-80 / sm:max-w-sm).\n\n## What to do\n\n### components/login-dialog.tsx — Full rewrite\n\nReplace the two-door design with a pill toggle (segmented control) at the top that switches between Handle and Email forms.\n\n**Layout:**\n1. A heading: 'Sign In' (Syne, bold)\n2. A pill toggle with two segments: 'Handle' | 'Email'\n3. Below the toggle, the active form (handle or email) with a smooth crossfade\n\n**Pill toggle design:**\n- A rounded-full container with a muted background (`bg-muted`)\n- Two buttons inside, each taking 50% width\n- The active button has `bg-background` (white in light mode) with a subtle shadow — looks like it's raised/selected\n- Inactive button is transparent with muted text\n- Transition the active indicator smoothly (use CSS transition on the buttons themselves — no sliding indicator needed, just swap bg/text colors with transition-all duration-200)\n- Use Outfit font, text-sm, font-medium\n\n**Handle form (when Handle pill is active):**\n- Handle InputGroup with AtSignIcon (same as existing)\n- Sign In button (uses `useLoginMutation`)\n- Create an account ghost button\n- Shows Spinner when pending\n\n**Email form (when Email pill is active):**\n- Email InputGroup with MailIcon (optional input)\n- Continue button — navigates to `/api/oauth/login?email=...` or `/api/oauth/login`\n- Small helper text: 'Enter your email for a direct code, or continue without'\n- Uses `window.location.href`\n\n**Conditional rendering:**\n- Only show the pill toggle when `process.env.NEXT_PUBLIC_EPDS_URL` is set\n- If not set, show only the handle form directly (no toggle)\n\n**Component structure:**\n```tsx\nexport default function LoginDialog() {\n  const [activeTab, setActiveTab] = useState\u003c'handle' | 'email'\u003e('handle')\n  const hasEpds = !!process.env.NEXT_PUBLIC_EPDS_URL\n\n  return (\n    \u003cdiv className='w-full space-y-5'\u003e\n      \u003cdiv\u003e\n        \u003ch2\u003eSign In\u003c/h2\u003e\n        \u003cp\u003eChoose how to continue\u003c/p\u003e\n      \u003c/div\u003e\n      \n      {hasEpds \u0026\u0026 (\n        \u003cPillToggle active={activeTab} onChange={setActiveTab} /\u003e\n      )}\n      \n      {activeTab === 'handle' || !hasEpds ? (\n        \u003cHandleForm /\u003e\n      ) : (\n        \u003cEmailForm /\u003e\n      )}\n    \u003c/div\u003e\n  )\n}\n```\n\n### components/navbar.tsx — Keep Dialog, fix width\n\nKeep the Dialog approach from dhz.15 (not Popover), but change the DialogContent width:\n- Change `sm:max-w-xl` to `sm:max-w-sm` (same width as the original popover was w-80)\n- Keep DialogTitle sr-only for accessibility\n- Keep all signed-in state unchanged\n\n### Styling details\n- Pill toggle: `rounded-full bg-muted p-1` container, buttons inside with `rounded-full px-4 py-1.5`\n- Active button: `bg-background text-foreground shadow-sm`\n- Inactive button: `text-muted-foreground hover:text-foreground`\n- Form transition: use `animate-fade-in` on the form content when switching tabs\n- Keep existing font conventions: Syne for headings, Outfit for body/labels\n- Keep existing color conventions: `create-accent` for primary actions\n\n## Don't\n- Use the two-door design — it's too wide\n- Add framer-motion or any new dependencies\n- Change any backend routes\n- Change the signed-in navbar state\n- Make the dialog wider than sm:max-w-sm\n- Remove the DoorSelection, ExpandedDoor, etc. components from dhz.15 — just replace them entirely with the new pill toggle approach","acceptance_criteria":"1. LoginDialog shows a pill toggle with Handle | Email segments when NEXT_PUBLIC_EPDS_URL is set\n2. Clicking a pill segment switches the form below it\n3. Active pill has white/raised background, inactive is transparent\n4. Dialog width is sm:max-w-sm (same as original popover width)\n5. Handle form: input + Sign In + Create account — uses useLoginMutation\n6. Email form: optional input + Continue — navigates to /api/oauth/login\n7. When NEXT_PUBLIC_EPDS_URL is NOT set, shows handle form only (no toggle)\n8. Dialog is centered and doesn't extend beyond the navbar\n9. pnpm tsc --noEmit succeeds\n10. Both login flows still work end-to-end","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":40,"created_at":"2026-02-24T17:08:57.361311945+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:10:30.864119718+06:00","closed_at":"2026-02-24T17:10:30.864119718+06:00","close_reason":"dcac8b3 redesign login dialog: pill toggle tabs, sm:max-w-sm width","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.16","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T17:08:57.363603239+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.2","title":"Create lib/epds-helpers.ts — PKCE + DPoP utilities","description":"## Files\n- lib/epds-helpers.ts (create)\n\n## What to do\nCreate a new file with the following exports, using only `node:crypto` (no new dependencies):\n\n1. `generateCodeVerifier()` — returns `crypto.randomBytes(32).toString('base64url')`\n2. `generateCodeChallenge(verifier: string)` — returns SHA-256 hash of verifier as base64url\n3. `generateState()` — returns `crypto.randomBytes(16).toString('base64url')`\n4. `generateDpopKeyPair()` — generates EC P-256 key pair, returns `{ privateKey: KeyObject, publicJwk: JsonWebKey, privateJwk: JsonWebKey }`\n5. `restoreDpopKeyPair(privateJwk: JsonWebKey)` — recreates KeyObject from serialized JWK, returns `{ privateKey: KeyObject, publicJwk: JsonWebKey }`\n6. `createDpopProof(opts: { privateKey: KeyObject, jwk: object, method: string, url: string, nonce?: string, accessToken?: string })` — creates a DPoP proof JWT (ES256, typ: dpop+jwt). If nonce provided, include in payload. If accessToken provided, include `ath` (SHA-256 hash of token, base64url).\n7. Internal `derToRaw(der: Buffer)` — converts DER-encoded ECDSA signature to raw r||s format (64 bytes). Not exported.\n\nThe exact implementation is in the ePDS tutorial: https://github.com/hypercerts-org/ePDS/blob/main/docs/tutorial.md — copy it verbatim with proper TypeScript types.\n\nAlso export a convenience function:\n8. `fetchWithDpopRetry(url: string, body: URLSearchParams, privateKey: KeyObject, publicJwk: object): Promise\u003cResponse\u003e` — wraps the nonce retry pattern (first request without nonce, if 400 + dpop-nonce header, retry with nonce).\n\n## Don't\n- Add any npm dependencies\n- Use jose or any other JWT library — use raw node:crypto only\n- Export derToRaw","acceptance_criteria":"1. File exists at lib/epds-helpers.ts\n2. All 8 functions listed above are exported (except derToRaw which is internal)\n3. No imports other than node:crypto\n4. pnpm build succeeds\n5. TypeScript compiles without errors","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-24T14:36:21.276258258+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:07:40.532818839+06:00","closed_at":"2026-02-24T15:07:40.532818839+06:00","close_reason":"3ce9e52 Create lib/epds-helpers.ts — PKCE + DPoP utilities","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.2","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T14:36:21.278962545+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.3","title":"Create lib/epds-config.ts — ePDS endpoint derivation","description":"## Files\n- lib/epds-config.ts (create)\n\n## What to do\nCreate a module that derives ePDS OAuth endpoints from the config.\n\nExport a function `getEpdsEndpoints()` that reads `config.epdsUrl` and returns:\n```typescript\n{\n  parEndpoint: string    // ${epdsUrl}/oauth/par\n  authEndpoint: string   // https://auth.${hostname}/oauth/authorize\n  tokenEndpoint: string  // ${epdsUrl}/oauth/token\n}\n```\n\nWhere `hostname` is extracted from `epdsUrl` using `new URL(epdsUrl).hostname`.\n\nExample: if `NEXT_PUBLIC_EPDS_URL=https://pds-eu-west4.test.certified.app`:\n- PAR: `https://pds-eu-west4.test.certified.app/oauth/par`\n- Auth: `https://auth.pds-eu-west4.test.certified.app/oauth/authorize`\n- Token: `https://pds-eu-west4.test.certified.app/oauth/token`\n\nAlso export `getEpdsClientId()` which returns `config.clientId` (same client ID as the existing flow — the app is the same OAuth client regardless of login method).\n\nAnd `getEpdsRedirectUri()` which returns `config.epdsRedirectUri`.\n\nThrow a clear error if `config.epdsUrl` is not set when these functions are called.\n\n## Don't\n- Import anything other than `@/lib/config`\n- Hardcode any URLs\n- Make this a class — plain exported functions are fine","acceptance_criteria":"1. File exists at lib/epds-config.ts\n2. getEpdsEndpoints() returns correct PAR, Auth, Token URLs derived from NEXT_PUBLIC_EPDS_URL\n3. Auth endpoint uses 'auth.' subdomain prefix\n4. Throws descriptive error if NEXT_PUBLIC_EPDS_URL is not set\n5. pnpm build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":15,"created_at":"2026-02-24T14:36:35.955005064+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:09:35.820040686+06:00","closed_at":"2026-02-24T15:09:35.820040686+06:00","close_reason":"cc7df81 Create lib/epds-config.ts — ePDS endpoint derivation","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dhz.3","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T14:36:35.956959902+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.4","title":"Create lib/epds-session-cookie.ts — signed transient OAuth cookie","description":"## Files\n- lib/epds-session-cookie.ts (create)\n\n## What to do\nCreate a module for encoding/decoding a signed HttpOnly cookie that stores transient OAuth state during the ePDS login flow. This cookie lives for 10 minutes (matching PAR request_uri lifetime) and is deleted after the callback.\n\n### Types\n```typescript\ninterface EpdsOAuthSessionData {\n  state: string\n  codeVerifier: string\n  dpopPrivateJwk: JsonWebKey  // from node:crypto\n}\n```\n\n### Functions\n\n1. `encodeEpdsSessionCookie(data: EpdsOAuthSessionData): string`\n   - JSON.stringify the data\n   - Compute HMAC-SHA256 of the JSON using `config.oauthSessionSecret`\n   - Return `${base64url(json)}.${base64url(hmac)}`\n   - Throw if `config.oauthSessionSecret` is not set\n\n2. `decodeEpdsSessionCookie(cookie: string): EpdsOAuthSessionData`\n   - Split on '.'\n   - Verify HMAC matches\n   - Parse and return the data\n   - Throw 'Invalid session cookie' if HMAC doesn't match or parsing fails\n\n### Cookie name\nExport `EPDS_SESSION_COOKIE_NAME = 'epds-oauth-session'`\n\n## Don't\n- Add any npm dependencies (use node:crypto only)\n- Store the cookie yourself — just encode/decode the value. The route handlers will set/delete the cookie.\n- Use JWT format — the simple payload.signature format is sufficient","acceptance_criteria":"1. File exists at lib/epds-session-cookie.ts\n2. encodeEpdsSessionCookie produces a string in format 'base64url.base64url'\n3. decodeEpdsSessionCookie correctly round-trips data encoded by encodeEpdsSessionCookie\n4. decodeEpdsSessionCookie throws on tampered data (modified payload or signature)\n5. Throws if OAUTH_SESSION_SECRET is not configured\n6. pnpm build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":25,"created_at":"2026-02-24T14:36:50.485871902+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:08:10.955713286+06:00","closed_at":"2026-02-24T15:08:10.955713286+06:00","close_reason":"f089177 Add lib/epds-session-cookie.ts — signed transient OAuth cookie","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.4","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T14:36:50.48830895+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.5","title":"Create app/api/oauth/login/route.ts — ePDS PAR + redirect","description":"## Files\n- app/api/oauth/login/route.ts (create)\n\n## What to do\nCreate a GET route handler: `GET /api/oauth/login?email=user@example.com`\n\n### Flow\n1. Read `email` from `req.nextUrl.searchParams.get('email')`. If missing, return 400 JSON error.\n2. Call `generateDpopKeyPair()`, `generateCodeVerifier()`, `generateCodeChallenge(verifier)`, `generateState()` from `@/lib/epds-helpers`.\n3. Get endpoints from `getEpdsEndpoints()`, client ID from `getEpdsClientId()`, redirect URI from `getEpdsRedirectUri()` — all from `@/lib/epds-config`.\n4. Build PAR body as URLSearchParams:\n   - client_id, redirect_uri, response_type: 'code', scope: 'atproto transition:generic', state, code_challenge, code_challenge_method: 'S256', login_hint: email\n5. Call `fetchWithDpopRetry(parEndpoint, parBody, privateKey, publicJwk)` from `@/lib/epds-helpers`.\n6. If PAR response is not ok, return 502 JSON error with status text.\n7. Parse `{ request_uri }` from PAR response JSON.\n8. Encode session cookie: `encodeEpdsSessionCookie({ state, codeVerifier, dpopPrivateJwk: privateJwk })`\n9. Build auth URL: `${authEndpoint}?client_id=${clientId}\u0026request_uri=${request_uri}\u0026login_hint=${email}` (all URI-encoded)\n10. Create `NextResponse.redirect(authUrl)`\n11. Set cookie on the response: name=EPDS_SESSION_COOKIE_NAME, value=encoded cookie, httpOnly=true, secure=config.isProduction, sameSite='lax', maxAge=600, path='/'\n12. Return the redirect response.\n\n### Imports\n- `NextRequest`, `NextResponse` from 'next/server'\n- helpers from `@/lib/epds-helpers`\n- config functions from `@/lib/epds-config`\n- cookie functions from `@/lib/epds-session-cookie`\n- `config` from `@/lib/config` (for isProduction)\n\n## Don't\n- Accept POST — this is GET only (browser redirect)\n- Use sdk.authorize() — that's the whole point\n- Forget login_hint in BOTH the PAR body AND the auth redirect URL (prevents flash of email form)","acceptance_criteria":"1. GET /api/oauth/login?email=test@example.com returns a 307 redirect\n2. Redirect URL points to auth.{epds-hostname}/oauth/authorize with client_id, request_uri, and login_hint params\n3. Response sets epds-oauth-session HttpOnly cookie with maxAge=600\n4. GET /api/oauth/login without email param returns 400 JSON error\n5. pnpm build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":45,"created_at":"2026-02-24T14:37:08.81948204+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:11:51.884617702+06:00","closed_at":"2026-02-24T15:11:51.884617702+06:00","close_reason":"306dbc3 Add ePDS PAR + redirect login route","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.5","depends_on_id":"hypercerts-dhz.1","type":"blocks","created_at":"2026-02-24T14:37:08.823759112+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-dhz.5","depends_on_id":"hypercerts-dhz.2","type":"blocks","created_at":"2026-02-24T14:37:08.825968926+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-dhz.5","depends_on_id":"hypercerts-dhz.3","type":"blocks","created_at":"2026-02-24T14:37:08.827962566+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-dhz.5","depends_on_id":"hypercerts-dhz.4","type":"blocks","created_at":"2026-02-24T14:37:08.829853576+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.6","title":"Create app/api/oauth/callback/route.ts — token exchange + Redis session + user-did cookie","description":"## Files\n- app/api/oauth/callback/route.ts (create)\n\n## What to do\nCreate a GET route handler: `GET /api/oauth/callback?code=...\u0026state=...`\n\nThis is the critical task — it does the manual token exchange and constructs a NodeSavedSession that the SDK can restore.\n\n### Flow\n1. Read `code` and `state` from query params. If either missing, return 400.\n2. Read the `epds-oauth-session` cookie. If missing, return 400 ('No OAuth session found').\n3. Call `decodeEpdsSessionCookie(cookieValue)` to get `{ state: savedState, codeVerifier, dpopPrivateJwk }`.\n4. Verify `state === savedState`. If mismatch, return 400 ('State mismatch').\n5. Call `restoreDpopKeyPair(dpopPrivateJwk)` to get `{ privateKey, publicJwk }`.\n6. Build token body as URLSearchParams: grant_type='authorization_code', code, redirect_uri=getEpdsRedirectUri(), client_id=getEpdsClientId(), code_verifier=codeVerifier.\n7. Call `fetchWithDpopRetry(tokenEndpoint, tokenBody, privateKey, publicJwk)`.\n8. If token response is not ok, return 502 with error details.\n9. Parse token response JSON. Expected shape: `{ access_token, token_type, scope, expires_in, sub, refresh_token? }`.\n10. **Construct NodeSavedSession** and write to Redis:\n```typescript\nimport { sessionStore } from '@/lib/hypercerts-sdk'\n\nconst nodeSavedSession = {\n  dpopJwk: dpopPrivateJwk,  // the original private JWK from the cookie\n  tokenSet: {\n    iss: getEpdsEndpoints().tokenEndpoint.replace('/oauth/token', ''),  // issuer is the PDS origin\n    sub: tokenResponse.sub,  // DID e.g. 'did:plc:abc123'\n    aud: getEpdsEndpoints().tokenEndpoint.replace('/oauth/token', ''),  // audience = PDS\n    scope: tokenResponse.scope || 'atproto transition:generic',\n    access_token: tokenResponse.access_token,\n    token_type: 'DPoP' as const,\n    refresh_token: tokenResponse.refresh_token,\n    expires_at: tokenResponse.expires_in\n      ? new Date(Date.now() + tokenResponse.expires_in * 1000).toISOString()\n      : undefined,\n  },\n}\nawait sessionStore.set(tokenResponse.sub, nodeSavedSession)\n```\n11. Create redirect response to `/`.\n12. Set `user-did` cookie on the response (same as existing auth callback): httpOnly=true, secure=config.isProduction, maxAge=60*60*24*7, path='/'.\n13. Delete the `epds-oauth-session` cookie (set maxAge=0).\n14. Return the redirect.\n\n### Critical notes on NodeSavedSession shape\n- `dpopJwk` must be the **private** JWK (includes 'd' parameter) — this is what the SDK uses to create DPoP proofs for subsequent API calls\n- `iss` (issuer) should be the PDS origin URL (e.g. 'https://pds-eu-west4.test.certified.app') — the SDK uses this to find the server metadata for token refresh\n- `aud` should match the PDS URL that the DID resolves to — for ePDS users this is the same as iss\n- `token_type` must be literally 'DPoP' (capital D, capital P)\n- The `authMethod` field can be omitted (it's optional, defaults to legacy handling)\n\n### Imports\n- NextRequest, NextResponse from 'next/server'\n- restoreDpopKeyPair, fetchWithDpopRetry from '@/lib/epds-helpers'\n- getEpdsEndpoints, getEpdsClientId, getEpdsRedirectUri from '@/lib/epds-config'\n- decodeEpdsSessionCookie, EPDS_SESSION_COOKIE_NAME from '@/lib/epds-session-cookie'\n- sessionStore from '@/lib/hypercerts-sdk'\n- config from '@/lib/config'\n\n## Don't\n- Use sdk.callback() — it won't work (state not in Redis state store)\n- Generate a new DPoP key pair — MUST restore from the cookie (same key used in PAR)\n- Forget to delete the epds-oauth-session cookie after use\n- Forget the 'DPoP' token_type in the session (SDK will reject it otherwise)","acceptance_criteria":"1. GET /api/oauth/callback?code=X\u0026state=Y with valid epds-oauth-session cookie performs token exchange\n2. On success, writes a NodeSavedSession to Redis via sessionStore.set(did, session)\n3. Sets user-did cookie with the DID from token response\n4. Deletes epds-oauth-session cookie\n5. Redirects to /\n6. Returns 400 if state param doesn't match cookie state\n7. Returns 400 if epds-oauth-session cookie is missing\n8. pnpm build succeeds\n9. CRITICAL: after this callback, sdk.restoreSession(did) must succeed — this is the whole point of the scrappy e2e","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":60,"created_at":"2026-02-24T14:37:35.892758637+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:15:00.705662664+06:00","closed_at":"2026-02-24T15:15:00.705662664+06:00","close_reason":"5ced804 add ePDS OAuth callback route — token exchange + Redis session + user-did cookie","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.6","depends_on_id":"hypercerts-dhz.2","type":"blocks","created_at":"2026-02-24T14:37:35.899186934+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-dhz.6","depends_on_id":"hypercerts-dhz.3","type":"blocks","created_at":"2026-02-24T14:37:35.901985917+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-dhz.6","depends_on_id":"hypercerts-dhz.4","type":"blocks","created_at":"2026-02-24T14:37:35.904238611+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.7","title":"Add /api/oauth/callback to redirect_uris in client-metadata.json","description":"## Files\n- app/client-metadata.json/route.ts (modify)\n- lib/config.ts (modify — buildClientMetadata function)\n\n## What to do\nThe ePDS callback goes to `/api/oauth/callback` which is different from the existing `/api/auth/callback`. The client metadata's `redirect_uris` array must include both.\n\n1. In `lib/config.ts`, update `buildClientMetadata()` to include both redirect URIs in the `redirect_uris` array:\n   - `config.redirectUri` (existing: `${redirectBaseUrl}/api/auth/callback`)\n   - `config.epdsRedirectUri` (new: `${redirectBaseUrl}/api/oauth/callback`) — but ONLY if `config.epdsUrl` is set\n\n2. In the loopback mode block AND the production mode block, change:\n   ```typescript\n   // Before\n   redirect_uris: [config.redirectUri],\n   // After\n   redirect_uris: config.epdsUrl \n     ? [config.redirectUri, config.epdsRedirectUri]\n     : [config.redirectUri],\n   ```\n\n## Don't\n- Change the client_id\n- Change any other metadata fields\n- Always include the ePDS redirect URI even when NEXT_PUBLIC_EPDS_URL is not set","acceptance_criteria":"1. When NEXT_PUBLIC_EPDS_URL is set, GET /client-metadata.json returns redirect_uris with both /api/auth/callback and /api/oauth/callback\n2. When NEXT_PUBLIC_EPDS_URL is NOT set, redirect_uris only contains /api/auth/callback (no change from current behavior)\n3. pnpm build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":15,"created_at":"2026-02-24T14:37:48.400788428+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:11:43.513128217+06:00","closed_at":"2026-02-24T15:11:43.513128217+06:00","close_reason":"7cc6302 Add /api/oauth/callback to redirect_uris when NEXT_PUBLIC_EPDS_URL is set","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dhz.7","depends_on_id":"hypercerts-dhz.1","type":"blocks","created_at":"2026-02-24T14:37:48.407502308+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dhz.8","title":"Add scrappy email login UI to LoginDialog","description":"## Files\n- components/login-dialog.tsx (modify)\n\n## What to do\nAdd a minimal email login option to the existing LoginDialog. This is scrappy — just needs to work, not look polished.\n\n### Changes\n1. Add a second state variable: `const [email, setEmail] = useState('')`\n2. Add a simple divider after the existing handle login form: `\u003cdiv className='text-center text-sm text-muted-foreground my-4'\u003eor\u003c/div\u003e`\n3. Below the divider, add:\n   - An email input field (`\u003cinput type='email' placeholder='user@example.com' /\u003e`) — can use the same InputGroup pattern as the handle input\n   - A 'Sign in with email' button\n4. On submit, navigate the browser to `/api/oauth/login?email=${encodeURIComponent(email)}` — this is a GET redirect, not a fetch. Use `window.location.href = ...` directly.\n\n### Why window.location.href instead of a mutation\nThe `/api/oauth/login` route returns a 307 redirect to the ePDS auth page. The browser needs to follow this redirect natively (not via fetch), so we just set window.location.href.\n\n### Disable state\nDisable the email submit button when email is empty. No other validation needed for the scrappy version.\n\n## Don't\n- Remove or change the existing handle-based login\n- Add a new mutation hook — just use window.location.href\n- Add email validation beyond the HTML type='email' attribute\n- Spend time on styling — functional is enough","acceptance_criteria":"1. LoginDialog shows both handle login (existing) and email login (new) separated by 'or'\n2. Entering an email and clicking 'Sign in with email' navigates to /api/oauth/login?email=\u003cencoded-email\u003e\n3. Button is disabled when email input is empty\n4. Existing handle-based login still works unchanged\n5. pnpm build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":20,"created_at":"2026-02-24T14:38:04.527704202+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:05:32.157908801+06:00","closed_at":"2026-02-24T15:05:32.157908801+06:00","close_reason":"85505ad Add email login UI to LoginDialog","labels":["scope:small"]}
{"id":"hypercerts-dhz.9","title":"Switch ePDS login to Flow 2 — remove login_hint from PAR body","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n- components/login-dialog.tsx (modify)\n\n## What to do\n\n### Problem\nThe ePDS PDS core validates `login_hint` as an ATProto identity (handle/DID), not an email address. Passing `login_hint=user@example.com` in the PAR body causes a 400 error: `Invalid login_hint`. We need to switch from Flow 1 (app collects email) to Flow 2 (ePDS collects email).\n\n### Changes to app/api/oauth/login/route.ts\n1. Change the route from requiring an `email` query param to not requiring it. The route should work as `GET /api/oauth/login` with no params.\n2. Remove `login_hint: email` from the `parBody` URLSearchParams (line 50).\n3. Remove `\u0026login_hint=${encodeURIComponent(email)}` from the auth URL construction (line 82).\n4. Remove the email validation check at the top (lines 22-28 that return 400 if email is missing).\n5. Keep everything else identical — DPoP, PKCE, PAR request, cookie, redirect all stay the same.\n\n### Changes to components/login-dialog.tsx\n1. Remove the email input field and its state variable.\n2. Change the 'Sign in with email' button to just navigate to `/api/oauth/login` (no query params).\n3. The button label can be 'Sign in with ePDS' or 'Sign in with email' — doesn't matter for the scrappy version.\n4. The button should always be enabled (no disabled state since there's no email to validate).\n5. Keep the existing handle-based login completely unchanged.\n\n## Don't\n- Remove the DPoP nonce retry logic\n- Change the callback route\n- Change any other files\n- Add login_hint anywhere — that's what's broken","acceptance_criteria":"1. GET /api/oauth/login (no query params) returns a 307 redirect to the ePDS auth page\n2. The PAR body does NOT contain login_hint\n3. The auth redirect URL does NOT contain login_hint\n4. The LoginDialog has a button that navigates to /api/oauth/login without any query params\n5. Existing handle-based login still works\n6. pnpm tsc --noEmit succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":20,"created_at":"2026-02-24T15:39:33.139718789+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:41:33.347274367+06:00","closed_at":"2026-02-24T15:41:33.347274367+06:00","close_reason":"395fe55 Switch ePDS login to Flow 2 — remove login_hint","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dhz.9","depends_on_id":"hypercerts-dhz","type":"parent-child","created_at":"2026-02-24T15:39:33.1418301+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-dlm","title":"Fix: stale 'Step 7' comment in CompletionStepSkeleton (from hypercerts-dcb)","description":"Review of hypercerts-dcb found: The comment on line 343 of components/form-skeletons.tsx reads '// Step 7: Completion Step Skeleton' but the flow is now a 5-step flow (contributions step disabled). The comment should read '// Completion Step Skeleton' or '// Step 6: Completion Step Skeleton' if the completion step is considered step 6 in the old numbering. All other step labels in the file correctly say 'of 5'. This is a cosmetic/documentation issue — no functional impact. Evidence: form-skeletons.tsx:343 still has the old step number from before the 6→5 step reduction.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-24T14:51:55.904206739+06:00","created_by":"kzoeps","updated_at":"2026-02-24T14:51:55.904206739+06:00"}
{"id":"hypercerts-dqf","title":"Epic: Apply Vercel React best practices to changed files on quick-fixes-test branch","description":"## Why\nAudit of all files changed on quick-fixes-test vs main revealed 25+ violations of Vercel React Best Practices across 5 categories. These impact page load time, bundle size, and rendering correctness.\n\n## What success looks like\nAll identified violations are fixed. No new violations introduced. Build and lint pass. The codebase follows Vercel's recommended patterns for async operations, bundle optimization, server-side performance, and conditional rendering.\n\n## Key constraints\n- Only modify files already changed on quick-fixes-test branch (no scope creep)\n- Do NOT change behavior — only performance/correctness patterns\n- Build and lint must pass after each task\n- Preserve existing Prettier formatting\n\n## Scope\n- 6 async waterfall fixes in API routes (CRITICAL)\n- 1 async parallel fix in server page (CRITICAL)  \n- 2 bundle size fixes (HIGH/MEDIUM)\n- 2 server parallel fetching fixes (HIGH)\n- 15 conditional rendering fixes across components (MEDIUM)\n- 5 combined iteration fixes (LOW)","status":"open","priority":2,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-17T23:01:00.930739778+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:36:20.848204634+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-dqf.1","title":"Fix async waterfalls in API routes: start auth promises early, await after validation","description":"## Files\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n- app/api/certs/create/route.ts (modify)\n\n## What to do\nApply the `async-api-routes` pattern: start expensive auth promises early, await them after synchronous validation.\n\n### add-attachment/route.ts\nCurrent: `getRepoContext()` is called on ~line 63 AFTER all form parsing and validation (lines 6-61).\nFix: Start `const ctxPromise = getRepoContext();` immediately after `const data = await req.formData();` (no await). Then `const ctx = await ctxPromise;` on the line where `getRepoContext()` is currently called. This overlaps auth network latency with CPU-bound parsing.\n\n### add-location/route.ts\nCurrent: `getAuthenticatedRepo()` is called on ~line 79 AFTER all form parsing and validation (lines 7-77).\nFix: Start `const repoPromise = getAuthenticatedRepo();` immediately after `const data = await req.formData();`. Then `const personalRepository = await repoPromise;` where the current await is.\n\n### create/route.ts\nCurrent: `getRepoContext()` is called on ~line 44 AFTER field parsing and validation (lines 8-42).\nFix: Start `const ctxPromise = getRepoContext();` immediately after `const formData = await req.formData();`. Then `const ctx = await ctxPromise;` where the current await is.\n\n## Don't\n- Change any error handling behavior\n- Change any validation logic or order\n- Move the `req.formData()` call — it must remain first since everything depends on it\n- Add any new imports","acceptance_criteria":"1. In each of the 3 files, the auth function (getRepoContext or getAuthenticatedRepo) is started as a promise (no await) immediately after req.formData()\n2. The promise is awaited later, after all synchronous validation\n3. Build passes: `pnpm build` exits 0\n4. Lint passes: `pnpm lint` exits 0\n5. No behavioral changes — same error responses, same status codes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T23:01:17.5891646+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:23:36.98616082+06:00","closed_at":"2026-02-17T23:23:36.98616082+06:00","close_reason":"e036e44 fix: start auth promises early to overlap with form validation","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dqf.1","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:01:17.590743968+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.10","title":"Fix js-combine-iterations: combine map+filter into single loops in form components","description":"## Files\n- components/contributions-form.tsx (modify)\n- components/evaluation-form.tsx (modify)\n- components/measurement-form.tsx (modify)\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\nReplace chained `.map()` + `.filter()` patterns with single-pass loops to avoid iterating arrays twice.\n\n### contributions-form.tsx (lines ~66-69):\nCurrent:\n```ts\nconst mappedContributors = [\n  ...contributors.map(({ did }) =\u003e did),\n  ...manualContributors.filter((uri) =\u003e uri.trim() !== \"\"),\n];\n```\nFix:\n```ts\nconst mappedContributors: string[] = [];\nfor (const c of contributors) mappedContributors.push(c.did);\nfor (const uri of manualContributors) {\n  if (uri.trim() !== \"\") mappedContributors.push(uri);\n}\n```\n\n### evaluation-form.tsx (lines ~173-176):\nCurrent:\n```ts\nconst allEvaluatorDids = [\n  ...evaluators.map((e) =\u003e e.did),\n  ...manualDids.filter((did) =\u003e did.trim() !== \"\"),\n];\n```\nFix: Same single-loop pattern.\n\n### measurement-form.tsx (lines ~273-276):\nCurrent:\n```ts\nconst allMeasurerDids = [\n  ...measurers.map((m) =\u003e m.did),\n  ...manualDids.filter((did) =\u003e did.trim() !== \"\"),\n];\n```\nFix: Same single-loop pattern.\n\n### measurement-form.tsx `buildLocationParams` (lines ~216-250):\nCurrent:\n```ts\nreturn locationEntries\n  .map((entry) =\u003e { ... })\n  .filter((loc): loc is MeasurementLocationParam =\u003e { ... });\n```\nFix: Use a single `for...of` loop that transforms and filters in one pass, pushing valid results into a result array.\n\n### hypercerts-base-form.tsx (lines ~169-172):\nCurrent:\n```ts\nconst mappedContributors = [\n  ...contributors.map(({ did }) =\u003e did),\n  ...manualContributors.filter((uri) =\u003e uri.trim() !== \"\"),\n];\n```\nFix: Same single-loop pattern.\n\n## Don't\n- Change the resulting array contents or order\n- Change any variable names used downstream\n- Change any types\n- Introduce any new dependencies","acceptance_criteria":"1. All 5 chained map+filter patterns are replaced with single-pass for...of loops\n2. The resulting arrays contain the same elements in the same order\n3. Build passes: `pnpm build` exits 0\n4. Lint passes: `pnpm lint` exits 0\n5. No behavioral changes","status":"closed","priority":4,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T23:03:17.766316916+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:22:06.665087419+06:00","closed_at":"2026-02-17T23:22:06.665087419+06:00","close_reason":"6bb1a72 fix: replace chained map+filter with single-pass for...of loops","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dqf.10","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:03:17.768139061+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.2","title":"Fix async waterfalls in profile update API routes: parallelize auth and formData","description":"## Files\n- app/api/profile/bsky/update/route.ts (modify)\n- app/api/profile/update/route.ts (modify)\n\n## What to do\nApply the `async-parallel` pattern: `getAuthenticatedRepo()` and `req.formData()` are independent async operations currently awaited sequentially.\n\n### bsky/update/route.ts\nCurrent (lines 8, 13):\n```ts\nconst repo = await getAuthenticatedRepo();  // line 8\nif (!repo) { return 401; }\nconst formData = await req.formData();      // line 13\n```\nFix: Use start-early-await-late pattern to avoid parsing body for unauthenticated requests:\n```ts\nconst repoPromise = getAuthenticatedRepo();\nconst formData = await req.formData();\nconst repo = await repoPromise;\nif (!repo) {\n  return NextResponse.json({ error: \"Not authenticated\" }, { status: 401 });\n}\n```\n\n### update/route.ts\nCurrent (lines 8, 13): Same sequential pattern.\nFix: Same start-early-await-late pattern as above.\n\n## Don't\n- Use Promise.all (we want to keep the early-exit guard for unauthenticated requests — avoid parsing body unnecessarily if auth fails fast)\n- Change any error messages or status codes\n- Change any validation logic after formData parsing\n- Modify the rest of the function after the auth check","acceptance_criteria":"1. In both files, getAuthenticatedRepo() is started as a promise (no await) BEFORE req.formData()\n2. req.formData() is awaited first, then the repo promise is awaited\n3. The !repo guard still returns 401 with the same error message\n4. Build passes: `pnpm build` exits 0\n5. Lint passes: `pnpm lint` exits 0","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-17T23:01:29.517013523+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:25:53.527275191+06:00","closed_at":"2026-02-17T23:25:53.527275191+06:00","close_reason":"d26d1ef parallelize auth and formData in profile update routes","labels":["scope:small","test-suspect"],"dependencies":[{"issue_id":"hypercerts-dqf.2","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:01:29.518730491+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.3","title":"Fix async parallel in auth callback and create-actions","description":"## Files\n- app/api/auth/callback/route.ts (modify)\n- lib/create-actions.ts (modify)\n\n## What to do\nApply the `async-parallel` pattern for independent async operations.\n\n### callback/route.ts (lines 21-22 in try block)\nCurrent:\n```ts\nconst session = await sdk.callback(searchParams);\nconst cookieStore = await cookies();\n```\nThese are independent — `cookies()` does not depend on `session`.\nFix:\n```ts\nconst [session, cookieStore] = await Promise.all([\n  sdk.callback(searchParams),\n  cookies(),\n]);\n```\n\n### create-actions.ts — `switchActiveProfile` function (lines ~30, ~37)\nCurrent:\n```ts\nconst session = await getSession();\nif (!session) { throw ... }\nconst cookieStore = await cookies();\n```\nFix: Use start-early-await-late since we want the guard:\n```ts\nconst cookiePromise = cookies();\nconst session = await getSession();\nif (!session) {\n  throw new Error(\"Not signed in\");\n}\nconst cookieStore = await cookiePromise;\n```\n\n## Don't\n- Change any error handling behavior or messages\n- Change the session guard logic in switchActiveProfile\n- Modify any other functions in create-actions.ts\n- Change the cookie operations after the await","acceptance_criteria":"1. callback/route.ts uses Promise.all for sdk.callback and cookies()\n2. create-actions.ts switchActiveProfile starts cookies() promise before awaiting getSession()\n3. Build passes: `pnpm build` exits 0\n4. Lint passes: `pnpm lint` exits 0\n5. No behavioral changes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-17T23:01:41.411527505+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:29:42.051482867+06:00","closed_at":"2026-02-17T23:29:42.051482867+06:00","close_reason":"0e491ba apply async-parallel pattern in auth callback and create-actions","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dqf.3","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:01:41.41348186+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.4","title":"Fix async parallel in hypercerts page and SignedInProvider","description":"## Files\n- app/hypercerts/page.tsx (modify)\n- providers/SignedInProvider.tsx (modify)\n\n## What to do\nApply the `async-parallel` and `server-parallel-fetching` patterns.\n\n### app/hypercerts/page.tsx (lines 34-35)\nCurrent:\n```ts\nconst { records } = await ctx.scopedRepo.hypercerts.list({ limit: 100 });\nconst pdsUrl = await resolveSessionPds(session);\n```\nThese are independent — `records` does not depend on `pdsUrl` and vice versa.\nFix:\n```ts\nconst [{ records }, pdsUrl] = await Promise.all([\n  ctx.scopedRepo.hypercerts.list({ limit: 100 }),\n  resolveSessionPds(session),\n]);\n```\n\n### providers/SignedInProvider.tsx (lines ~14-27)\nCurrent:\n```ts\nconst [session, cookieStore] = await Promise.all([getSession(), cookies()]);\n// ... guard checks ...\nif (session) {\n  const repo = await getAuthenticatedRepo();\n  // ...\n}\n```\n`getAuthenticatedRepo()` is independent of the session/cookies Promise.all result — it reads cookies internally. Include it in the initial Promise.all.\nFix:\n```ts\nconst [session, cookieStore, repo] = await Promise.all([\n  getSession(),\n  cookies(),\n  getAuthenticatedRepo(),\n]);\n// ... guard checks ...\nif (session \u0026\u0026 repo) {\n  const profile = await repo.profile.getCertifiedProfile().catch(() =\u003e null);\n  // ...\n}\n```\n\n## Don't\n- Change the redirect logic in hypercerts/page.tsx\n- Change the cookie operations in SignedInProvider\n- Change how profile is fetched (it depends on repo, so must stay sequential)\n- Change any JSX rendering logic","acceptance_criteria":"1. app/hypercerts/page.tsx uses Promise.all for hypercerts.list() and resolveSessionPds()\n2. providers/SignedInProvider.tsx includes getAuthenticatedRepo() in the initial Promise.all\n3. The repo-dependent profile fetch remains sequential (after repo is resolved)\n4. Build passes: `pnpm build` exits 0\n5. Lint passes: `pnpm lint` exits 0\n6. No behavioral changes — same redirects, same rendering","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-02-17T23:01:54.244393186+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:30:21.125337821+06:00","closed_at":"2026-02-17T23:30:21.125337821+06:00","close_reason":"5b74892 parallelize getAuthenticatedRepo in SignedInProvider","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dqf.4","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:01:54.246753146+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.5","title":"Fix bundle-barrel-imports: use import type for lexicon namespace import","description":"## Files\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\nFix the `bundle-barrel-imports` violation. The file uses `import * as Hypercert` which defeats tree-shaking and pulls runtime validators + multiformats/cid into the client bundle, even though only the TypeScript type `Hypercert.Record` is used.\n\n### Current (line 11):\n```ts\nimport * as Hypercert from \"@/lexicons/types/org/hypercerts/claim/activity\";\n```\n\n### Fix:\n```ts\nimport type { Record as HypercertRecord } from \"@/lexicons/types/org/hypercerts/claim/activity\";\n```\n\nThen find all usages of `Hypercert.Record` in the file and replace with `HypercertRecord`. There should be approximately 1 usage around line 39 in the component props/type definition.\n\n## Don't\n- Change any runtime behavior\n- Add any new runtime imports\n- Modify any other imports in the file\n- Change the component logic","acceptance_criteria":"1. The `import * as Hypercert` is replaced with `import type { Record as HypercertRecord }`\n2. All usages of `Hypercert.Record` are replaced with `HypercertRecord`\n3. No runtime import from the lexicon module exists (only `import type`)\n4. Build passes: `pnpm build` exits 0\n5. Lint passes: `pnpm lint` exits 0","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T23:02:06.071425516+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:35:48.235089218+06:00","closed_at":"2026-02-17T23:35:48.235089218+06:00","close_reason":"6bb1a72 fix: use import type for lexicon namespace import (already applied as part of hypercerts-dqf.10)","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dqf.5","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:02:06.073821514+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.6","title":"Fix bundle-dynamic-imports: lazy-load below-the-fold sections in hypercert-detail-view","description":"## Files\n- components/detail-view-skeletons.tsx (create)\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\nTwo parts: (1) create skeleton loaders that match the exact layout of each section, (2) use next/dynamic with those skeletons as loading fallbacks.\n\n### Part 1: Create components/detail-view-skeletons.tsx\n\nCreate three exported skeleton components that match the pixel-perfect layout of each section. Each section follows the same pattern:\n\n**Section header** (all three sections share this layout):\n```\n\u003cdiv className=\"space-y-4\"\u003e\n  \u003cdiv className=\"flex items-center gap-3\"\u003e\n    \u003cdiv className=\"size-10 rounded-full bg-create-accent/10 flex items-center justify-center\"\u003e\n      \u003c!-- icon placeholder --\u003e\n    \u003c/div\u003e\n    \u003cdiv className=\"flex-1\"\u003e\n      \u003cSkeleton for title (h-6 w-40) /\u003e\n      \u003cSkeleton for count subtitle (h-3 w-24) /\u003e\n    \u003c/div\u003e\n  \u003c/div\u003e\n  \u003c!-- content cards below --\u003e\n\u003c/div\u003e\n```\n\n**MeasurementsSectionSkeleton**: Section header + 2 measurement card skeletons. Each card matches the existing `MeasurementSkeleton` layout inside the section (glass-panel p-6 border border-border/50 rounded-xl):\n- Skeleton h-6 w-3/4 (title)\n- Skeleton h-10 w-1/2 (value)\n- 2-col grid: 2x (Skeleton h-3 w-1/4 + Skeleton h-4 w-3/4)\n\n**EvidenceSectionSkeleton**: Section header + 2 evidence card skeletons. Each card:\n- flex justify-between: Skeleton h-6 w-1/3 + Skeleton h-6 w-20 rounded-full\n- Skeleton h-4 w-3/4\n- Skeleton h-4 w-1/2\n\n**EvaluationsSectionSkeleton**: Section header + 1 evaluation card skeleton. Each card:\n- Skeleton h-6 w-1/4\n- Skeleton h-4 w-3/4\n- Skeleton h-16 w-32 rounded-lg\n\nUse the Skeleton component from \"@/components/ui/skeleton\". Use the BarChart3, FileCheck, ClipboardCheck icons from lucide-react for the section header icons (matching the real sections). Import pattern: follow the existing form-skeletons.tsx style.\n\n### Part 2: Modify components/hypercert-detail-view.tsx\n\nReplace the three static imports with next/dynamic, using the skeleton loaders:\n\n```tsx\nimport dynamic from \"next/dynamic\";\nimport {\n  MeasurementsSectionSkeleton,\n  EvidenceSectionSkeleton,\n  EvaluationsSectionSkeleton,\n} from \"./detail-view-skeletons\";\n\nconst HypercertMeasurementsSection = dynamic(\n  () =\u003e import(\"./hypercert-measurements-section\"),\n  { loading: () =\u003e \u003cMeasurementsSectionSkeleton /\u003e },\n);\nconst HypercertEvidenceSection = dynamic(\n  () =\u003e import(\"./hypercert-evidence-section\"),\n  { loading: () =\u003e \u003cEvidenceSectionSkeleton /\u003e },\n);\nconst HypercertEvaluationsSection = dynamic(\n  () =\u003e import(\"./hypercert-evaluations-section\"),\n  { loading: () =\u003e \u003cEvaluationsSectionSkeleton /\u003e },\n);\n```\n\nRemove the old static imports of these three components.\n\n## Don't\n- Change any props passed to the section components\n- Change the JSX structure where sections are rendered (lines 152-157)\n- Add \"use client\" to detail-view-skeletons.tsx (it only uses Skeleton + icons, no client hooks)\n- Change any other imports in hypercert-detail-view.tsx\n- Modify the section components themselves","acceptance_criteria":"1. components/detail-view-skeletons.tsx exists and exports MeasurementsSectionSkeleton, EvidenceSectionSkeleton, EvaluationsSectionSkeleton\n2. Each skeleton matches the section header layout: size-10 rounded-full icon container + title skeleton + count skeleton\n3. MeasurementsSectionSkeleton renders 2 card skeletons matching MeasurementSkeleton layout\n4. EvidenceSectionSkeleton renders 2 card skeletons matching EvidenceSkeleton layout\n5. EvaluationsSectionSkeleton renders 1 card skeleton matching EvaluationSkeleton layout\n6. hypercert-detail-view.tsx uses next/dynamic with { loading: () =\u003e \u003cXSkeleton /\u003e } for all 3 sections\n7. No static imports of the three section components remain\n8. Build passes: pnpm build exits 0\n9. Lint passes: pnpm lint exits 0","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T23:02:17.824416773+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:22:18.791417219+06:00","closed_at":"2026-02-17T23:22:18.791417219+06:00","close_reason":"ef96b27 lazy-load below-the-fold sections with next/dynamic","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-dqf.6","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:02:17.826531781+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.7","title":"Fix rendering-conditional-render: replace \u0026\u0026 with ternary in view components","description":"## Files\n- components/hypercert-detail-view.tsx (modify)\n- components/hypercert-evaluation-view.tsx (modify)\n- components/hypercert-evidence-view.tsx (modify)\n- components/hypercert-measurement-view.tsx (modify)\n- components/hypercert-completion-step.tsx (modify)\n\n## What to do\nReplace `condition \u0026\u0026 (\u003cJSX\u003e)` patterns with `condition ? (\u003cJSX\u003e) : null` in all conditional renders where the condition could be a string or number (not already a boolean).\n\nThe `\u0026\u0026` operator can accidentally render `0` or `\"\"` to the DOM when the left operand is falsy but not `false`/`null`/`undefined`.\n\n### Rules:\n1. If the condition is ALREADY a boolean expression (e.g., `isLoading \u0026\u0026 ...`, `step === 2 \u0026\u0026 ...`, `arr.length \u003e 0 \u0026\u0026 ...`), convert to ternary for consistency but these are lower priority.\n2. If the condition is a STRING variable (e.g., `description \u0026\u0026 ...`, `location \u0026\u0026 ...`, `imageUri \u0026\u0026 ...`), these MUST be converted because `\"\"` would render to DOM.\n3. If the condition is an OBJECT/ARRAY check (e.g., `evaluation.score != null \u0026\u0026 ...`), convert for consistency.\n\n### Specific locations:\n\n**hypercert-detail-view.tsx**: lines ~29, ~47, ~139\n- `{imageUri \u0026\u0026 (` → `{imageUri ? (` ... `) : null}`\n- `{record.shortDescription \u0026\u0026 (` → ternary\n- `{record.description \u0026\u0026 (` → ternary\n\n**hypercert-evaluation-view.tsx**: lines ~57, ~101, ~126, ~151\n- `{evaluation.score != null \u0026\u0026 (` → ternary\n- `{evaluation.content \u0026\u0026 evaluation.content.length \u003e 0 \u0026\u0026 (` → ternary\n- `{evaluation.measurements \u0026\u0026 evaluation.measurements.length \u003e 0 \u0026\u0026 (` → ternary\n- `{evaluation.location \u0026\u0026 (` → ternary\n\n**hypercert-evidence-view.tsx**: lines ~76, ~83\n- `{evidence.description \u0026\u0026 (` → ternary\n- `{evidence.content \u0026\u0026 evidence.content.length \u003e 0 \u0026\u0026 (` → ternary\n\n**hypercert-measurement-view.tsx**: lines ~59, ~104\n- `{measurement.measurementMethodURI \u0026\u0026 (` → ternary\n- `{measurement.evidenceURI \u0026\u0026 measurement.evidenceURI.length \u003e 0 \u0026\u0026 (` → ternary\n\n**hypercert-completion-step.tsx**: lines ~62, ~76, ~91, ~103, ~112\n- `{hypercertInfo?.hypercertCid \u0026\u0026 (` → ternary\n- `{!hypercertInfo?.hypercertCid \u0026\u0026 (` → ternary\n- `{onBack \u0026\u0026 (` → ternary\n- `{onCreateAnother \u0026\u0026 (` → ternary\n\n## Don't\n- Change any component logic or behavior\n- Change any CSS classes or styling\n- Add or remove any JSX elements\n- Change any prop types or interfaces","acceptance_criteria":"1. All `condition \u0026\u0026 (\u003cJSX\u003e)` patterns in the 5 listed files are replaced with `condition ? (\u003cJSX\u003e) : null`\n2. No `\u0026\u0026` used for conditional rendering remains in these files (search for pattern: JSX element preceded by \u0026\u0026)\n3. Build passes: `pnpm build` exits 0\n4. Lint passes: `pnpm lint` exits 0\n5. No visual or behavioral changes","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T23:02:38.622081243+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:29:29.823351509+06:00","closed_at":"2026-02-17T23:29:29.823351509+06:00","close_reason":"720a96a fix: replace \u0026\u0026 with ternary in view components","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dqf.7","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:02:38.623965325+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.8","title":"Fix rendering-conditional-render: replace \u0026\u0026 with ternary in section components and navbar","description":"## Files\n- components/hypercert-evaluations-section.tsx (modify)\n- components/hypercert-evidence-section.tsx (modify)\n- components/hypercert-measurements-section.tsx (modify)\n- components/navbar.tsx (modify)\n- components/image-uploader.tsx (modify)\n- components/user-selection.tsx (modify)\n\n## What to do\nReplace `condition \u0026\u0026 (\u003cJSX\u003e)` patterns with `condition ? (\u003cJSX\u003e) : null` in all conditional renders.\n\n### Specific locations:\n\n**hypercert-evaluations-section.tsx**: lines ~65, ~75, ~81\n- `{evaluations \u0026\u0026 evaluations.length \u003e 0 \u0026\u0026 (` → `{evaluations.length \u003e 0 ? (` ... `) : null}`\n- `{isLoading \u0026\u0026 (` → ternary\n- `{isError \u0026\u0026 (` → ternary\n\n**hypercert-evidence-section.tsx**: lines ~70, ~80, ~87\n- Same patterns as evaluations section\n\n**hypercert-measurements-section.tsx**: lines ~74, ~84, ~91\n- Same patterns as evaluations section\n\n**navbar.tsx**: lines ~139, ~146, ~156\n- `{userHandle \u0026\u0026 (` → ternary (STRING — could render \"\")\n- `{activeProfileName \u0026\u0026 (` → ternary (STRING)\n- `{activeProfileHandle \u0026\u0026 (` → ternary (STRING)\n\n**image-uploader.tsx**: lines ~36, ~75, ~103\n- `{label \u0026\u0026 (` → ternary (STRING — could render \"\")\n- `{isBanner \u0026\u0026 (` → ternary (boolean, for consistency)\n- `{!isBanner \u0026\u0026 imageUrl \u0026\u0026 (` → ternary\n\n**user-selection.tsx**: lines ~44, ~45, ~48\n- `{isLoading \u0026\u0026 ...}` → ternary\n- `{!isLoading \u0026\u0026 userSuggestions.length === 0 \u0026\u0026 !!search \u0026\u0026 (` → ternary\n- `{!isLoading \u0026\u0026 !!userSuggestions.length \u0026\u0026 (` → ternary\n\n## Don't\n- Change any component logic or behavior\n- Change any CSS classes or styling\n- Add or remove any JSX elements\n- Change any prop types or interfaces","acceptance_criteria":"1. All `condition \u0026\u0026 (\u003cJSX\u003e)` patterns in the 6 listed files are replaced with `condition ? (\u003cJSX\u003e) : null`\n2. Build passes: `pnpm build` exits 0\n3. Lint passes: `pnpm lint` exits 0\n4. No visual or behavioral changes","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T23:02:50.951743919+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:30:46.902825732+06:00","closed_at":"2026-02-17T23:30:46.902825732+06:00","close_reason":"3ee4863 Fix conditional renders: replace \u0026\u0026 with ternary in 6 components","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dqf.8","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:02:50.954011826+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dqf.9","title":"Fix rendering-conditional-render: replace \u0026\u0026 with ternary in pages and form components","description":"## Files\n- app/page.tsx (modify)\n- app/hypercerts/page.tsx (modify)\n- app/hypercerts/create/page.tsx (modify)\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\nReplace `condition \u0026\u0026 (\u003cJSX\u003e)` patterns with `condition ? (\u003cJSX\u003e) : null` in all conditional renders.\n\n### Specific locations:\n\n**app/page.tsx**: lines ~169, ~197, ~205\n- `{isSignedIn \u0026\u0026 session \u0026\u0026 (` → `{isSignedIn \u0026\u0026 session ? (` ... `) : null}`\n- `{profile?.displayName \u0026\u0026 (` → ternary (STRING — could render \"\")\n- `{profile?.handle \u0026\u0026 (` → ternary (STRING)\n\n**app/hypercerts/page.tsx**: lines ~139, ~146, ~156\n- `{createdDate \u0026\u0026 (` → ternary\n- `{workScope.length \u003e 0 \u0026\u0026 (` → ternary\n- `{workScope.length \u003e 3 \u0026\u0026 (` → ternary\n\n**app/hypercerts/create/page.tsx**: lines ~81, ~96, ~103, ~110, ~117, ~124\n- All step conditionals: `{step === N \u0026\u0026 hypercertInfo \u0026\u0026 (` → `{step === N \u0026\u0026 hypercertInfo ? (` ... `) : null}`\n- `{step === 1 \u0026\u0026 (` → ternary\n\n**components/hypercerts-base-form.tsx**: lines ~410, ~422, ~495, ~621, ~631, ~664\n- `{workScope.length \u003e 1 \u0026\u0026 index !== 0 \u0026\u0026 (` → ternary\n- `{!!workScope[index] \u0026\u0026 index === workScope.length - 1 \u0026\u0026 (` → ternary\n- `{showContributions \u0026\u0026 (` → ternary\n- `{hasContributors \u0026\u0026 !contributionRole.trim() \u0026\u0026 (` → ternary\n- `{!!updateActions \u0026\u0026 (` → ternary\n- `{!updateActions \u0026\u0026 (` → ternary\n\n## Don't\n- Change any component logic or behavior\n- Change any CSS classes or styling\n- Add or remove any JSX elements\n- Change any prop types or interfaces","acceptance_criteria":"1. All `condition \u0026\u0026 (\u003cJSX\u003e)` patterns in the 4 listed files are replaced with `condition ? (\u003cJSX\u003e) : null`\n2. Build passes: `pnpm build` exits 0\n3. Lint passes: `pnpm lint` exits 0\n4. No visual or behavioral changes","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T23:03:02.957311173+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:23:20.570293532+06:00","closed_at":"2026-02-17T23:23:20.570293532+06:00","close_reason":"2a4c07d fix: replace \u0026\u0026 with ternary in conditional renders","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-dqf.9","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:03:02.95898536+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-dyp","title":"Fix: return_to open redirect bypass — //evil.com passes startsWith('/') check (from security review)","description":"## File\napp/api/oauth/callback/route.ts, line 45\n\n## Problem\nThe return_to parameter is validated with:\n```ts\nconst redirectPath = returnTo.startsWith('/') ? returnTo : '/';\n```\n\nThis check is bypassed by protocol-relative URLs like `//evil.com/path`. The string `//evil.com` starts with `/`, so it passes the guard. When passed to `new URL(redirectPath, config.baseUrl)`, Node's URL constructor treats `//evil.com` as a protocol-relative URL and resolves it to `https://evil.com` — an open redirect to an attacker-controlled domain.\n\n## Reproduction\n1. Attacker crafts: `/api/oauth/callback?code=...\u0026state=...\u0026return_to=//evil.com`\n2. `returnTo.startsWith('/')` → true\n3. `new URL('//evil.com', 'https://myapp.com')` → `https://evil.com`\n4. User is redirected to attacker's site after login\n\n## Fix\nReplace the startsWith check with a URL-parse-based validation that ensures the resolved URL stays on the same origin:\n```ts\nfunction safeReturnTo(returnTo: string, baseUrl: string): string {\n  try {\n    const resolved = new URL(returnTo, baseUrl);\n    const base = new URL(baseUrl);\n    return resolved.origin === base.origin ? resolved.pathname + resolved.search : '/';\n  } catch {\n    return '/';\n  }\n}\nconst redirectPath = safeReturnTo(returnTo, config.baseUrl);\n```\n\nThis rejects any return_to that resolves to a different origin, including protocol-relative URLs, absolute URLs, and data: URIs.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:43:25.615441825+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:43:25.615441825+06:00","dependencies":[{"issue_id":"hypercerts-dyp","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.449470483+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-e7f","title":"Fix: .env.example missing OAUTH_SESSION_SECRET documentation — required for ePDS flow","description":"Review of .env.example found that OAUTH_SESSION_SECRET is documented as commented-out (line 72) with no indication of when it is required vs optional.\n\nEvidence:\n- .env.example line 72: '# OAUTH_SESSION_SECRET=your-32-plus-char-secret-here' (commented out)\n- lib/config.ts line 234: 'oauthSessionSecret: process.env.OAUTH_SESSION_SECRET' — it is read but the value is only used when NEXT_PUBLIC_EPDS_URL is set\n- The README optional variables table (line 77-82) does NOT list OAUTH_SESSION_SECRET at all — it is completely absent from the README env var documentation\n- The .env.example comment says 'Server-only secret used to HMAC-sign the transient OAuth session cookie' but the ePDS flow now uses Redis (not cookies) for state storage\n\nIssues:\n1. OAUTH_SESSION_SECRET is missing from README.md environment variables table entirely\n2. The .env.example comment says it signs a 'session cookie' but the ePDS state is now stored in Redis (hypercerts-0lg epic moved it to Redis) — the comment is stale\n3. It is unclear whether this variable is still used at all after the Redis migration\n\nSuggested fix:\n1. Audit whether OAUTH_SESSION_SECRET is still used after the ePDS cookie→Redis migration (hypercerts-0lg)\n2. If still used: add it to README.md optional variables table with accurate description\n3. If no longer used: remove it from .env.example and lib/config.ts\n4. Update the .env.example comment to accurately describe current usage","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:16.835778253+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:16.835778253+06:00"}
{"id":"hypercerts-e8tq","title":"Fix: unnecessary 'as any' cast on contributors in hypercert-detail-view.tsx — field exists on lexicon type (from hypercerts-73o)","description":"Review of hypercerts-73o found: components/hypercert-detail-view.tsx line 60 casts record as any to access .contributors with a comment saying the field is not in HypercertClaim type yet. But OrgHypercertsClaimActivity.Main (the lexicon type, activity.d.ts line 32) DOES have contributors?: Contributor[]. The record prop is typed as HypercertClaim.Record which aliases to Main (activity.d.ts line 42). The as any cast and the eslint-disable comment are both unnecessary and suppress type checking on a now-properly-typed field. Fix: remove the cast and access record.contributors directly. Evidence: components/hypercert-detail-view.tsx:59-63 vs node_modules/@hypercerts-org/lexicon/dist/types/org/hypercerts/claim/activity.d.ts:32,42.","status":"open","priority":2,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:18:45.448140607+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:18:45.448140607+06:00","dependencies":[{"issue_id":"hypercerts-e8tq","depends_on_id":"hypercerts-73o","type":"discovered-from","created_at":"2026-03-04T11:18:45.520164594+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-ecf","title":"Epic: Improve DX lint and formatting toolchain","description":"## Why\nThe scaffold has a solid ESLint + Prettier baseline (eslint-config-next bundles react-hooks + jsx-a11y), but is missing lint coverage for TanStack Query patterns (16 files use it), React Compiler compatibility (React 19 + Next 16), and Tailwind class sorting. Adding these catches real bugs earlier and reduces review friction.\n\n## What success looks like\n- `@tanstack/eslint-plugin-query` catches bad query key patterns and missing options across 16+ query files\n- `eslint-plugin-react-compiler` warns on patterns that break auto-memoization (e.g. the profileMap issue in hypercerts-mrb.15)\n- `prettier-plugin-tailwindcss` auto-sorts Tailwind classes on format\n- Existing 3 lint warnings are fixed and `no-unused-vars` is promoted to error so warnings don't accumulate\n- `pnpm lint` and `pnpm build` pass cleanly after all changes\n\n## Key constraints\n- Do NOT add eslint-plugin-import (TypeScript covers this already)\n- Do NOT add eslint-plugin-tailwindcss (v4 compat uncertain; use Prettier plugin instead)\n- React compiler plugin runs in WARN mode only (not error) — it's forward-looking, not blocking\n- All changes must be compatible with ESLint 9 flat config format\n- Preserve existing eslint-config-prettier as the last config (must stay last to disable conflicting rules)","status":"closed","priority":2,"issue_type":"epic","owner":"kzoepa@gmail.com","created_at":"2026-02-26T22:19:52.225933305+06:00","created_by":"kzoeps","updated_at":"2026-02-26T22:32:26.589763626+06:00","closed_at":"2026-02-26T22:32:26.589763626+06:00","close_reason":"All 4 tasks completed: TanStack Query ESLint plugin, React Compiler ESLint plugin (warn), Prettier Tailwind class sorting, lint warnings fixed + no-unused-vars promoted to error","labels":["scope:small"]}
{"id":"hypercerts-ecf.1","title":"Install and configure @tanstack/eslint-plugin-query","description":"## Files\n- package.json (modify)\n- eslint.config.mjs (modify)\n\n## What to do\n1. Run `pnpm add -D @tanstack/eslint-plugin-query`\n2. In `eslint.config.mjs`, import the plugin's flat config and add it to the config array:\n   ```js\n   import pluginQuery from \"@tanstack/eslint-plugin-query\";\n   // ...\n   const eslintConfig = defineConfig([\n     ...nextVitals,\n     ...nextTs,\n     ...pluginQuery.configs[\"flat/recommended\"],\n     prettierConfig, // MUST stay last\n     globalIgnores([...]),\n   ]);\n   ```\n3. The plugin's `flat/recommended` config enables these rules at warn/error:\n   - `@tanstack/query/exhaustive-deps` (error)\n   - `@tanstack/query/no-rest-destructuring` (warn)\n   - `@tanstack/query/stable-query-client` (error)\n4. Run `pnpm lint` — if any new errors appear in the 16 query files, fix them in this same task (they are likely missing queryKey deps or unstable query client patterns)\n\n## Don't\n- Add any other ESLint plugins in this task\n- Change the order of prettierConfig (must remain last before globalIgnores)\n- Suppress errors with eslint-disable comments — fix the underlying issue","acceptance_criteria":"1. `pnpm ls @tanstack/eslint-plugin-query` shows the package installed as a devDependency\n2. `eslint.config.mjs` includes the plugin's flat/recommended config\n3. prettierConfig is still the last config entry before globalIgnores\n4. `pnpm lint` passes with zero errors (warnings from other plugins are acceptable)\n5. `pnpm build` passes","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-26T22:20:08.058314721+06:00","created_by":"kzoeps","updated_at":"2026-02-26T22:26:24.000912899+06:00","closed_at":"2026-02-26T22:26:24.000912899+06:00","close_reason":"97eb29b install and configure @tanstack/eslint-plugin-query","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ecf.1","depends_on_id":"hypercerts-ecf","type":"parent-child","created_at":"2026-02-26T22:20:08.05998802+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-ecf.2","title":"Install and configure eslint-plugin-react-compiler in warn mode","description":"## Files\n- package.json (modify)\n- eslint.config.mjs (modify)\n\n## What to do\n1. Run `pnpm add -D eslint-plugin-react-compiler`\n2. In `eslint.config.mjs`, import and add the plugin. The react-compiler plugin uses ESLint 9 flat config format:\n   ```js\n   import reactCompiler from \"eslint-plugin-react-compiler\";\n   // ...\n   const eslintConfig = defineConfig([\n     ...nextVitals,\n     ...nextTs,\n     ...pluginQuery.configs[\"flat/recommended\"],  // from ecf.1\n     reactCompiler.configs.recommended,\n     prettierConfig, // MUST stay last\n     globalIgnores([...]),\n   ]);\n   ```\n3. The recommended config sets `react-compiler/react-compiler` to `\"error\"`. Override it to `\"warn\"` so it's informational, not blocking:\n   ```js\n   {\n     rules: {\n       \"react-compiler/react-compiler\": \"warn\",\n     },\n   },\n   ```\n   Place this override object AFTER the recommended config but BEFORE prettierConfig.\n4. Run `pnpm lint` — note any new warnings (expected, not errors). Do NOT fix them in this task — they are informational.\n\n## Don't\n- Set the rule to \"error\" — it must be \"warn\" only\n- Fix react-compiler warnings in this task (they are informational for now)\n- Change the order of prettierConfig (must remain last before globalIgnores)\n- Install babel-plugin-react-compiler (that's the build-time compiler, not the linter)","acceptance_criteria":"1. `pnpm ls eslint-plugin-react-compiler` shows the package installed as a devDependency\n2. `eslint.config.mjs` includes the react-compiler recommended config\n3. The `react-compiler/react-compiler` rule is overridden to \"warn\" (not \"error\")\n4. prettierConfig is still the last config entry before globalIgnores\n5. `pnpm lint` passes with zero errors (new warnings from react-compiler are acceptable)\n6. `pnpm build` passes","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":20,"created_at":"2026-02-26T22:20:20.238144422+06:00","created_by":"kzoeps","updated_at":"2026-02-26T22:28:24.003444596+06:00","closed_at":"2026-02-26T22:28:24.003444596+06:00","close_reason":"0e3ab19 Add eslint-plugin-react-compiler in warn mode","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ecf.2","depends_on_id":"hypercerts-ecf","type":"parent-child","created_at":"2026-02-26T22:20:20.239460582+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-ecf.2","depends_on_id":"hypercerts-ecf.1","type":"blocks","created_at":"2026-02-26T22:20:54.441107642+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-ecf.3","title":"Install and configure prettier-plugin-tailwindcss for class sorting","description":"## Files\n- package.json (modify)\n- .prettierrc.json (modify)\n\n## What to do\n1. Run `pnpm add -D prettier-plugin-tailwindcss`\n2. In `.prettierrc.json`, add the plugin to the existing empty plugins array:\n   ```json\n   {\n     \"semi\": true,\n     \"singleQuote\": false,\n     \"tabWidth\": 2,\n     \"trailingComma\": \"all\",\n     \"printWidth\": 80,\n     \"plugins\": [\"prettier-plugin-tailwindcss\"],\n     \"endOfLine\": \"lf\"\n   }\n   ```\n3. The plugin auto-detects Tailwind v4 config (CSS-based). No `tailwindConfig` option needed.\n4. Run `pnpm format:check` to see how many files would change. Then run `pnpm format` to apply class sorting.\n5. Stage and commit the formatting changes separately from the config change so the diff is reviewable:\n   - First commit: package.json + .prettierrc.json (config change)\n   - Second commit: all reformatted files (class sorting applied)\n\n## Don't\n- Add any other Prettier plugins\n- Change any Prettier options other than the plugins array\n- Manually reorder Tailwind classes — let the plugin do it\n- Skip running `pnpm format` — the pre-commit hook will catch unsorted classes otherwise, creating noisy diffs in unrelated PRs","acceptance_criteria":"1. `pnpm ls prettier-plugin-tailwindcss` shows the package installed as a devDependency\n2. `.prettierrc.json` has `\"prettier-plugin-tailwindcss\"` in the plugins array\n3. `pnpm format:check` exits 0 (all files formatted, including Tailwind class sorting)\n4. `pnpm lint` still passes\n5. `pnpm build` still passes","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-26T22:20:32.490848205+06:00","created_by":"kzoeps","updated_at":"2026-02-26T22:26:21.426732929+06:00","closed_at":"2026-02-26T22:26:21.426732929+06:00","close_reason":"97eb29b apply prettier-plugin-tailwindcss class sorting","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-ecf.3","depends_on_id":"hypercerts-ecf","type":"parent-child","created_at":"2026-02-26T22:20:32.492845728+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-ecf.4","title":"Fix 3 existing lint warnings and promote no-unused-vars to error","description":"## Files\n- app/api/certs/add-attachment/route.ts (modify)\n- lib/contributor-utils.ts (modify)\n- providers/SignedInProvider.tsx (modify)\n- eslint.config.mjs (modify)\n\n## What to do\n\n### Part 1: Fix the 3 existing warnings\nCurrent `pnpm lint` output shows:\n\n1. **app/api/certs/add-attachment/route.ts:140:11** — `'message' is assigned a value but never used`\n   - Remove the unused `message` variable or use it (check context — it's likely a destructured error field that should be logged or returned)\n\n2. **lib/contributor-utils.ts:59:10** — `'extractDidFromAtUri' is defined but never used`\n   - Remove the unused function, OR if it's exported and used elsewhere, check imports. If it's truly dead code, delete it.\n\n3. **providers/SignedInProvider.tsx:5:10** — `'cookies' is defined but never used`\n   - Remove the unused `cookies` import\n\n### Part 2: Promote no-unused-vars to error\nIn `eslint.config.mjs`, add a rule override AFTER the nextTs config but BEFORE prettierConfig:\n```js\n{\n  rules: {\n    \"@typescript-eslint/no-unused-vars\": \"error\",\n  },\n},\n```\n\nThis ensures future unused vars are caught as errors (blocking) rather than warnings (ignored).\n\n## Don't\n- Add eslint-disable comments to suppress the warnings\n- Change the logic/behavior of any function — only remove truly unused code\n- Remove `extractDidFromAtUri` if it's exported and imported elsewhere (check with grep first)","acceptance_criteria":"1. `pnpm lint` exits with 0 errors AND 0 warnings (completely clean)\n2. The `@typescript-eslint/no-unused-vars` rule is set to \"error\" in eslint.config.mjs\n3. No eslint-disable comments were added\n4. `pnpm build` passes\n5. No behavioral changes to any function","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoepa@gmail.com","estimated_minutes":20,"created_at":"2026-02-26T22:20:46.81166566+06:00","created_by":"kzoeps","updated_at":"2026-02-26T22:31:33.87705697+06:00","closed_at":"2026-02-26T22:31:33.87705697+06:00","close_reason":"ea5783c fix: remove 3 lint warnings and promote no-unused-vars to error","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-ecf.4","depends_on_id":"hypercerts-ecf","type":"parent-child","created_at":"2026-02-26T22:20:46.813386809+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-ecf.4","depends_on_id":"hypercerts-ecf.2","type":"blocks","created_at":"2026-02-26T22:20:55.0695985+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-ei70","title":"Fix: BlobRef instance fails AppCertifiedActorProfile.validateRecord — avatar/banner upload always returns 400 (from hypercerts-fpyy.5)","description":"Review of hypercerts-fpyy.5 found a critical runtime bug in app/api/profile/update/route.ts.\n\n## Problem\n\nThe code uploads a blob and assigns the raw `BlobRef` instance to `record.avatar` / `record.banner` (lines 82–86 and 147–151), then immediately calls `assertValidRecord('certifiedProfile', record, AppCertifiedActorProfile.validateRecord)`.\n\n`AppCertifiedActorProfile.Record.avatar` is typed as `$Typed\u003cUri\u003e | $Typed\u003cSmallImage\u003e | { $type: string }` — **not** `BlobRef`. The lexicon validator rejects a `BlobRef` instance (it only accepts plain objects). Confirmed with:\n\n```\nAppCertifiedActorProfile.validateRecord({ $type: 'app.certified.actor.profile', avatar: blobRef })\n// → { success: false, error: {} }\n```\n\nThis means **any request that uploads an avatar or banner to the certified profile endpoint returns HTTP 400** — the feature is completely broken.\n\n## Evidence\n\n- `app/api/profile/update/route.ts` lines 81–86 (create path):\n  ```ts\n  if (avatarBlob)\n    record.avatar = avatarBlob as unknown as AppCertifiedActorProfile.Record[\"avatar\"];\n  ```\n- `app/api/profile/update/route.ts` lines 146–151 (update path): same pattern.\n- The `as unknown as` cast silences TypeScript but the validator still rejects the value at runtime.\n\n## Fix\n\nUse `avatarBlob.original` (the plain serialisable object) instead of the `BlobRef` instance:\n\n```ts\nif (avatarBlob) record.avatar = avatarBlob.original as AppCertifiedActorProfile.Record[\"avatar\"];\nif (bannerBlob) record.banner = bannerBlob.original as AppCertifiedActorProfile.Record[\"banner\"];\n```\n\n`BlobRef.original` is `{ $type: 'blob', ref: {...}, mimeType: string, size: number }` which satisfies the `{ $type: string }` branch of the union and passes validation.","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T13:06:14.504483128+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T13:58:44.582848071+06:00","closed_at":"2026-03-04T13:58:44.582848071+06:00","close_reason":"8cb56d0 fix: use BlobRef.original for avatar/banner — already implemented as part of hypercerts-0j8f","dependencies":[{"issue_id":"hypercerts-ei70","depends_on_id":"hypercerts-fpyy.5","type":"discovered-from","created_at":"2026-03-04T13:06:18.117993099+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-eig","title":"Fix: Internal error messages exposed in URL via auth_error_description (from hypercerts-jsu.1)","description":"Review of hypercerts-jsu.1 found: When sdk.callback() throws, the raw error message is placed in the auth_error_description URL query param (app/api/auth/callback/route.ts line 40: e instanceof Error ? e.message : 'An unexpected error occurred'). This can expose internal error details (OAuth state, internal URLs, library internals) in the browser address bar and server logs. Evidence: git show 1f18629 -- app/api/auth/callback/route.ts shows the catch block sets auth_error_description to e.message directly. Fix: use a generic user-facing message for callback_failed errors (e.g. 'Authentication failed. Please try again.') and log the full error server-side only. The error_description from the PDS (access_denied path) is fine to pass through as it is a standard OAuth param.","acceptance_criteria":"1. In the catch block of app/api/auth/callback/route.ts, auth_error_description is set to a generic string like \"Authentication failed. Please try again.\" — NOT e.message\n2. The full error is still logged server-side via console.error (already exists, just verify it stays)\n3. The access_denied early-return path (lines 9-17) is UNCHANGED — PDS-provided error_description is fine to pass through\n4. TypeScript compiles: npx tsc --noEmit passes\n5. No other files modified","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoeps","estimated_minutes":10,"created_at":"2026-02-17T18:26:46.108263718+06:00","created_by":"kzoeps","updated_at":"2026-02-17T18:29:29.358588231+06:00","closed_at":"2026-02-17T18:29:29.358588231+06:00","close_reason":"7026ba9 fix: use generic message for callback_failed auth_error_description","dependencies":[{"issue_id":"hypercerts-eig","depends_on_id":"hypercerts-jsu.1","type":"discovered-from","created_at":"2026-02-17T18:26:48.972287885+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-ejh","title":"Change logo_uri in client metadata from certified-logo.svg to certified.png","description":"## Files\n- lib/config.ts (modify — buildClientMetadata function only)\n\n## What to do\nChange logo_uri in buildClientMetadata() from certified-logo.svg to certified.png in BOTH the loopback and production return objects.\n\nLine 236: change \\`\\${config.baseUrl}/certified-logo.svg\\` to \\`\\${config.baseUrl}/certified.png\\`\nLine 258 (approx): same change for the production block.\n\nThe file public/certified.png already exists.\n\nThis does NOT affect the normal Bsky login because the branding CSS (atproto-branding.ts) overrides the logo image entirely. The logo_uri is primarily consumed by ePDS for the {{logo_uri}} template variable in OTP emails.\n\nDo NOT change lib/atproto-branding.ts — it correctly uses certified-logo.svg for the CSS background-image override on PDS OAuth pages.\n\n## Don't\n- Don't change atproto-branding.ts\n- Don't change any other fields in buildClientMetadata()\n- Don't change any other files","acceptance_criteria":"1. buildClientMetadata() returns logo_uri ending in /certified.png in BOTH loopback and production modes\n2. No reference to certified-logo.svg remains in config.ts logo_uri fields\n3. lib/atproto-branding.ts is unchanged (still uses certified-logo.svg for CSS)\n4. No TypeScript errors: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","owner":"kzoepa@gmail.com","estimated_minutes":10,"created_at":"2026-02-24T19:33:37.180201693+06:00","created_by":"kzoeps","updated_at":"2026-02-24T19:34:28.950551649+06:00","closed_at":"2026-02-24T19:34:28.950551649+06:00","close_reason":"Changed logo_uri from certified-logo.svg to certified.png in both loopback and production metadata","labels":["scope:trivial"]}
{"id":"hypercerts-fh1","title":"Epic: Update README to document ePDS email login flow and fill documentation gaps","description":"## Why\nThe README is significantly outdated after the ePDS email login flow was implemented. The codebase now has two distinct auth flows (standard ATProto handle-based and ePDS email-based with DPoP/PKCE/OTP), but the README only documents the standard flow. Multiple sections are incomplete or missing.\n\n## What success looks like\n- README accurately documents both authentication flows (standard ATProto + ePDS email)\n- All environment variables are documented (including optional ones like NEXT_PUBLIC_EPDS_URL)\n- Architecture diagram reflects the dual auth pathways and Redis stores\n- Project structure matches the actual codebase\n- A developer can clone the repo, read the README, and set up either auth flow without asking questions\n\n## Key constraints\n- Do NOT change any application code — this is docs-only\n- Keep the existing README structure where possible, extend rather than rewrite\n- Keep it concise — link to DEVELOPMENT.md for deep dives\n- The ePDS flow is optional (only enabled when NEXT_PUBLIC_EPDS_URL is set)","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-26T18:59:51.719295092+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T19:08:12.811379734+06:00","closed_at":"2026-02-26T19:08:12.81138297+06:00","labels":["scope:medium"]}
{"id":"hypercerts-fh1.1","title":"Update Environment Configuration section with all env vars","description":"## Files\n- README.md (modify)\n\n## What to do\nReplace the \"Environment Configuration \u003e Required Variables\" table (lines 64-73) with a complete table that includes ALL environment variables used by the codebase. Split into \"Required\" and \"Optional\" subsections.\n\n### Required Variables (keep existing 6, update descriptions):\n| Variable | Description |\n|---|---|\n| `NEXT_PUBLIC_BASE_URL` | App base URL. Use `http://127.0.0.1:3000` for local dev. Falls back to `VERCEL_URL` on Vercel. |\n| `ATPROTO_JWK_PRIVATE` | Private JWK (JWKS format) for OAuth client assertion. Generate with `pnpm run generate-jwk`. |\n| `REDIS_HOST` | Redis server hostname (e.g., `localhost` for Docker, or cloud Redis host) |\n| `REDIS_PORT` | Redis server port (default: `6379`) |\n| `REDIS_PASSWORD` | Redis password (empty string for local Docker) |\n| `NEXT_PUBLIC_PDS_URL` | Personal Data Server URL (e.g., `https://pds-eu-west4.test.certified.app`) |\n\n### Optional Variables (NEW — add after Required):\n| Variable | Description |\n|---|---|\n| `NEXT_PUBLIC_EPDS_URL` | ePDS URL for email-based login. When set, enables the Email login tab in the UI. Example: `https://epds1.test.certified.app` |\n| `REDIS_USERNAME` | Redis username. Defaults to `default` when `REDIS_PASSWORD` is set. |\n| `NEXT_PUBLIC_HANDLE_RESOLVER` | Handle resolver URL. Defaults to `https://bsky.social`. |\n\n### Test Server URLs subsection (update existing, lines 86-91):\nAdd the ePDS test URL after the PDS URL:\n```env\nNEXT_PUBLIC_PDS_URL=https://pds-eu-west4.test.certified.app\n# Optional: Enable email login via ePDS\nNEXT_PUBLIC_EPDS_URL=https://epds1.test.certified.app\n```\n\n## Dont\n- Do NOT add OAUTH_SESSION_SECRET — it is dead config (being removed in hypercerts-0lg.7)\n- Do NOT add NEXT_PUBLIC_VERCEL_TARGET_ENV — internal Vercel detail\n- Do NOT change any code files","acceptance_criteria":"1. README has a \"Required Variables\" table with 6 vars and an \"Optional Variables\" table with 3 vars\n2. NEXT_PUBLIC_EPDS_URL is documented with its purpose (enables email login tab)\n3. Test Server URLs section includes both PDS and ePDS URLs\n4. OAUTH_SESSION_SECRET is NOT mentioned\n5. All variable names match exactly what the code uses (check lib/config.ts, lib/redis.ts)","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-26T19:00:10.49771358+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T19:05:46.188845421+06:00","closed_at":"2026-02-26T19:05:46.188845421+06:00","close_reason":"b997d68 docs: update env vars table with required and optional sections","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fh1.1","depends_on_id":"hypercerts-fh1","type":"parent-child","created_at":"2026-02-26T19:00:10.500224469+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fh1.2","title":"Add ePDS Email Authentication section to README","description":"## Files\n- README.md (modify)\n\n## What to do\nThe current \"Authentication \u003e How It Works\" section (lines 196-209) only documents the standard ATProto handle-based flow. Restructure this section to document BOTH flows.\n\n### Changes to make:\n\n1. **Rename the existing \"How It Works\" subsection** to \"### Flow 1: Handle Login (Standard ATProto)\"\n   - Keep the existing 6-step flow description (lines 200-209) as-is\n\n2. **Add a new subsection after it**: \"### Flow 2: Email Login (ePDS)\"\n   - Add a note: \"\u003e Requires `NEXT_PUBLIC_EPDS_URL` to be set. When configured, an Email tab appears in the login dialog.\"\n   - Document the flow step-by-step:\n     1. User enters their email address (or leaves blank for the ePDS to collect it)\n     2. App sends a Pushed Authorization Request (PAR) to the ePDS with PKCE challenge and DPoP proof\n     3. App stores OAuth state (code verifier + DPoP private key) in Redis\n     4. User is redirected to the ePDS authorization page\n     5. ePDS sends a one-time password (OTP) to the users email\n     6. User enters the OTP code on the ePDS page\n     7. ePDS redirects back to `/api/oauth/callback` with an authorization code\n     8. App exchanges the code for tokens using DPoP, creates a session in Redis\n     9. User is authenticated — same `user-did` cookie as the standard flow\n\n3. **Add a brief \"### How the Login UI Works\" subsection** after both flows:\n   - When only `NEXT_PUBLIC_PDS_URL` is set: login dialog shows only the Handle tab\n   - When `NEXT_PUBLIC_EPDS_URL` is also set: login dialog shows a pill toggle with Handle and Email tabs\n   - Both flows result in the same session format — downstream code is agnostic to the login method\n\n### Key technical details to mention:\n- The ePDS flow uses DPoP (Demonstrating Proof-of-Possession) with EC P-256 keys\n- PKCE with S256 challenge method prevents authorization code interception\n- OAuth state is stored in Redis (not cookies) to avoid cross-site redirect issues\n- The ePDS flow supports custom branding (logo, colors) and a custom email template for OTP codes\n\n## Dont\n- Do NOT include code examples in this section (the existing server-side auth examples below already cover usage)\n- Do NOT document internal implementation details of lib/epds-helpers.ts\n- Do NOT mention the old cookie-based approach (it was replaced by Redis)\n- Keep each flow description to ~10 lines max — this is a README, not a spec","acceptance_criteria":"1. README has three subsections under Authentication: \"Flow 1: Handle Login\", \"Flow 2: Email Login (ePDS)\", and \"How the Login UI Works\"\n2. The ePDS flow is documented as a numbered list with 8-10 steps\n3. The note about NEXT_PUBLIC_EPDS_URL requirement is present\n4. DPoP, PKCE, and OTP are mentioned\n5. It is clear that both flows produce the same session format\n6. No code examples in this section (those are in the next section)\n7. The old cookie-based approach is NOT mentioned","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-02-26T19:00:32.850997836+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T19:06:15.221113214+06:00","closed_at":"2026-02-26T19:06:15.221113214+06:00","close_reason":"d1396f6 docs: add ePDS email auth section to README","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fh1.2","depends_on_id":"hypercerts-fh1","type":"parent-child","created_at":"2026-02-26T19:00:32.852888957+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fh1.3","title":"Update Architecture diagram to include ePDS flow and Redis stores","description":"## Files\n- README.md (modify)\n\n## What to do\nReplace the ASCII architecture diagram (lines 126-149) with an updated version that reflects the dual auth pathways and the three Redis stores.\n\n### New diagram should show:\n1. **Client (Browser)** — React Query, Login Dialog (Handle tab + Email tab)\n2. **Next.js API Routes** — split into three columns:\n   - `/api/auth/*` (standard ATProto OAuth — SDK-managed)\n   - `/api/oauth/*` (ePDS email OAuth — hand-rolled DPoP/PKCE)\n   - `/api/certs/*` + `/api/profile/*` (business logic)\n3. **Hypercerts SDK** — used by standard flow and business logic\n4. **Redis** — three stores:\n   - `oauth-state:*` (standard ATProto state, 10min TTL)\n   - `epds-oauth-state:*` (ePDS state: code verifier + DPoP key, 10min TTL)\n   - `session:*` (persisted sessions for both flows, no TTL)\n5. **PDS** — standard ATProto PDS\n6. **ePDS** — email-based PDS (Certified Auth)\n7. **OAuth metadata endpoints** — `/client-metadata.json`, `/jwks.json`\n\n### Also update the \"Personal Data Server (PDS)\" paragraph (lines 151-153):\nAdd a brief explanation of ePDS:\n- **PDS** stores user data (profiles, hypercerts). Standard ATProto login authenticates against the users PDS.\n- **ePDS (Email PDS)** is a Certified Auth PDS that supports email-based login with OTP verification. It serves the same role as a standard PDS but uses email instead of a handle for authentication.\n\n### Use this approximate layout (ASCII art):\n```\n┌─────────────────────────────────────────────────────────────────┐\n│                       Client (Browser)                          │\n│    Login Dialog (Handle │ Email)  │  React Query  │  Forms      │\n└──────────────┬──────────────────────────┬───────────────────────┘\n               │                          │\n    ┌──────────▼──────────┐    ┌──────────▼──────────┐\n    │  /api/auth/*        │    │  /api/oauth/*        │\n    │  (Standard ATProto) │    │  (ePDS Email Login)  │\n    │  SDK-managed OAuth  │    │  Manual DPoP + PKCE  │\n    └──────────┬──────────┘    └──────────┬───────────┘\n               │                          │\n               ▼                          ▼\n    ┌─────────────────────────────────────────────────┐\n    │              Hypercerts SDK (sdk-core)           │\n    │    Session Restore  │  Repository Ops  │  CRUD  │\n    └──────────┬──────────────────────┬───────────────┘\n               │                      │\n        ┌──────▼──────┐        ┌──────▼──────┐\n        │    Redis     │        │  PDS / ePDS │\n        │  Sessions    │        │  User Data  │\n        │  OAuth State │        │             │\n        └─────────────┘        └─────────────┘\n```\n\nFeel free to adjust the exact ASCII art to look clean, but it must show the two auth pathways and Redis.\n\n## Dont\n- Do NOT make the diagram overly complex — keep it readable in a terminal at 80 chars wide\n- Do NOT remove the PDS explanation paragraph, just extend it\n- Do NOT add the metadata endpoints to the diagram (mention them in text if needed)","acceptance_criteria":"1. Architecture diagram shows two distinct auth pathways: /api/auth/* (standard) and /api/oauth/* (ePDS)\n2. Redis box mentions sessions and OAuth state\n3. Both PDS and ePDS are shown or mentioned\n4. The PDS explanation paragraph mentions ePDS and email-based login\n5. Diagram renders correctly in a monospace font at ~80 char width\n6. The diagram is valid ASCII art (no broken box characters)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-26T19:00:56.610875146+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T19:06:42.43543176+06:00","closed_at":"2026-02-26T19:06:42.43543176+06:00","close_reason":"4b2376c Update architecture diagram with dual auth pathways and ePDS","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fh1.3","depends_on_id":"hypercerts-fh1","type":"parent-child","created_at":"2026-02-26T19:00:56.612699601+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fh1.4","title":"Update Project Structure and Key Files sections in README","description":"## Files\n- README.md (modify)\n\n## What to do\nReplace the \"Project Structure\" tree (lines 289-306) and \"Key Files\" table (lines 309-315) with accurate, complete versions.\n\n### New Project Structure tree:\n```\n├── app/\n│   ├── api/\n│   │   ├── auth/              # Standard ATProto OAuth (login, callback, logout)\n│   │   ├── oauth/             # ePDS email OAuth (login, callback)\n│   │   ├── certs/             # Hypercert operations (create, add-attachment, add-location)\n│   │   └── profile/           # Profile management (certified + bluesky)\n│   ├── client-metadata.json/  # OAuth client metadata endpoint\n│   ├── jwks.json/             # Public JWKS endpoint\n│   ├── hypercerts/            # Hypercert pages (list, create, [detail])\n│   ├── profile/               # Certified profile page\n│   └── bsky-profile/          # Bluesky profile page\n├── components/                # React components (login dialog, forms, detail views)\n│   └── ui/                    # shadcn/ui primitives (button, dialog, input, etc.)\n├── lib/\n│   ├── api/                   # Client-side API functions and types\n│   ├── config.ts              # Centralized app configuration\n│   ├── hypercerts-sdk.ts      # SDK initialization and singleton\n│   ├── redis.ts               # Redis client setup\n│   ├── redis-state-store.ts   # Redis stores (sessions, OAuth state, ePDS state)\n│   ├── atproto-session.ts     # Server-side session helpers\n│   ├── repo-context.ts        # Repository context helper\n│   ├── create-actions.ts      # Server actions for CRUD operations\n│   ├── epds-config.ts         # ePDS OAuth endpoint configuration\n│   ├── epds-helpers.ts        # ePDS PKCE + DPoP utilities\n│   └── atproto-branding.ts    # OAuth page branding (CSS, logos)\n├── providers/                 # React providers (QueryClient, auth gating)\n├── queries/                   # TanStack Query hooks (auth, hypercerts, profile)\n├── public/                    # Static assets (logos, email template)\n├── scripts/                   # Utility scripts (JWK generation)\n├── vendor/                    # Packed SDK tarballs (unreleased)\n└── lexicons/                  # ATProto lexicon definitions\n```\n\n### New Key Files table:\n| File | Purpose |\n|---|---|\n| `lib/config.ts` | Centralized configuration — base URLs, OAuth client IDs, redirect URIs, scopes |\n| `lib/hypercerts-sdk.ts` | SDK initialization with OAuth config, Redis storage, handle resolver |\n| `lib/redis-state-store.ts` | Three Redis-backed stores: sessions, standard OAuth state, ePDS OAuth state |\n| `lib/repo-context.ts` | Helper to get authenticated repository context in server components |\n| `lib/epds-config.ts` | Derives ePDS OAuth endpoints (PAR, auth, token) from `NEXT_PUBLIC_EPDS_URL` |\n| `lib/epds-helpers.ts` | PKCE code verifier/challenge, DPoP key generation and proof creation |\n| `components/login-dialog.tsx` | Dual-mode login UI with Handle/Email pill toggle |\n| `app/client-metadata.json/route.ts` | OAuth client metadata (RFC 7591) — serves client_id, redirect_uris, branding |\n\n## Dont\n- Do NOT list every single file — keep it to the most important ones\n- Do NOT include .beads/ directory in the tree\n- Do NOT include node_modules, .next, or other build artifacts\n- Keep the tree to ~25 lines max","acceptance_criteria":"1. Project structure tree includes app/api/oauth/ directory with comment about ePDS\n2. Project structure includes lib/epds-config.ts and lib/epds-helpers.ts\n3. Project structure includes lib/redis.ts and lib/redis-state-store.ts\n4. Project structure includes app/client-metadata.json/ and app/jwks.json/\n5. Key Files table has 8 entries (up from 3)\n6. Key Files table includes epds-config.ts, epds-helpers.ts, redis-state-store.ts, and login-dialog.tsx\n7. Tree is ~25 lines or fewer\n8. No .beads/ directory in the tree","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-26T19:01:22.305267006+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T19:07:20.224881713+06:00","closed_at":"2026-02-26T19:07:20.224881713+06:00","close_reason":"d1396f6 update Project Structure and Key Files sections in README","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fh1.4","depends_on_id":"hypercerts-fh1","type":"parent-child","created_at":"2026-02-26T19:01:22.307094929+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fh1.5","title":"Add Redis Data Model section to README","description":"## Files\n- README.md (modify)\n\n## What to do\nAdd a new section \"### Redis Data Model\" inside the existing \"Architecture Overview\" section (after the PDS explanation, around line 153). This explains what Redis stores and why.\n\n### Content to add:\n\n```markdown\n### Redis Data Model\n\nRedis stores three types of data, managed by dedicated store classes in `lib/redis-state-store.ts`:\n\n| Key Pattern | Store Class | TTL | Purpose |\n|---|---|---|---|\n| `oauth-state:{state}` | `RedisStateStore` | 10 min | Standard ATProto OAuth state (managed by SDK) |\n| `epds-oauth-state:{state}` | `RedisEpdsStateStore` | 10 min | ePDS OAuth state (PKCE code verifier + DPoP private key) |\n| `session:{did}` | `RedisSessionStore` | None | Persisted OAuth sessions for both flows (tokens, DPoP JWK) |\n\n- OAuth state entries auto-expire after 10 minutes (login must complete within this window)\n- ePDS state uses atomic get-and-delete to prevent replay attacks\n- Sessions persist until the user logs out or the session is revoked\n```\n\n## Dont\n- Do NOT add this as a top-level section — nest it under Architecture Overview\n- Do NOT document Redis connection configuration here (thats in Environment Configuration)\n- Do NOT mention implementation details like getDel() — keep it user-facing\n- Keep it to the table + 3 bullet points max","acceptance_criteria":"1. A \"Redis Data Model\" subsection exists under Architecture Overview\n2. Table has 3 rows: oauth-state, epds-oauth-state, session\n3. TTLs are documented (10 min for state, None for sessions)\n4. The three bullet points about expiry, replay prevention, and persistence are present\n5. Section is concise (table + 3 bullets, no more)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-26T19:01:35.44478287+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T19:04:40.000921493+06:00","closed_at":"2026-02-26T19:04:40.000928617+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-fh1.5","depends_on_id":"hypercerts-fh1","type":"parent-child","created_at":"2026-02-26T19:01:35.44697109+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-fh1.5","depends_on_id":"hypercerts-fh1.3","type":"blocks","created_at":"2026-02-26T19:01:59.659284888+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fh1.6","title":"Add Features Overview section to README","description":"## Files\n- README.md (modify)\n\n## What to do\nAdd a new top-level section \"## Features\" right after the intro paragraph (line 3) and before \"## Prerequisites\" (line 5). This gives readers a quick overview of what the scaffold can do.\n\n### Content:\n\n```markdown\n## Features\n\n- **Dual Authentication** — Sign in via ATProto handle (standard OAuth) or email (ePDS with OTP verification)\n- **Hypercert CRUD** — Create, view, and list verifiable impact certificates with images, date ranges, and work scope\n- **Rich Metadata** — Attach evidence, geospatial locations, quantitative measurements, and evaluations to hypercerts\n- **Profile Management** — Edit both Certified (Hypercerts) and Bluesky profiles\n- **Decentralized Storage** — All data stored on the users AT Protocol Personal Data Server\n- **Custom OAuth Branding** — Branded login pages and email templates for OTP codes\n```\n\n## Dont\n- Do NOT make this more than 6-8 bullet points\n- Do NOT include technical implementation details (no mention of Redis, DPoP, etc.)\n- Do NOT duplicate information from other sections — this is a quick scan list\n- Keep each bullet to one line","acceptance_criteria":"1. A \"Features\" section exists between the intro paragraph and Prerequisites\n2. Contains 6 bullet points covering: dual auth, hypercert CRUD, metadata, profiles, decentralized storage, branding\n3. Each bullet is one line (no multi-line descriptions)\n4. No technical implementation details (no Redis, DPoP, PKCE mentions)\n5. Dual authentication is the first bullet and mentions both handle and email","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-26T19:01:48.364312309+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T19:04:40.059434869+06:00","closed_at":"2026-02-26T19:04:40.059439147+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-fh1.6","depends_on_id":"hypercerts-fh1","type":"parent-child","created_at":"2026-02-26T19:01:48.365637525+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fpyy","title":"Epic: Add lexicon validation before all AT Protocol record writes","description":"## Why\nCurrently 22 createRecord/putRecord calls across 6 files write AT Protocol records with zero lexicon validation. The @hypercerts-org/lexicon package provides validateRecord functions for every collection type, and lib/utils.ts has 3 dead validation wrappers that are never called. Invalid records can be written to repos, causing downstream issues.\n\n## What success looks like\nEvery createRecord and putRecord call validates the record against its lexicon schema before writing. Invalid records return a 400 error (in API routes) or throw a descriptive error (in server actions/lib functions). The dead validation wrappers in lib/utils.ts are either used or removed.\n\n## Key constraints\n- Use validateRecord from @hypercerts-org/lexicon (already a dependency)\n- For app.bsky.actor.profile, use AppBskyActorProfile.validateRecord from @atproto/api\n- For app.certified.actor.profile, use AppCertifiedActorProfile.validateRecord from @hypercerts-org/lexicon\n- Don't change record construction logic — only add validation gates\n- Validation errors should be descriptive (include which fields failed)\n- npm run build must pass","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T12:18:02.424455834+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:06:12.755973619+06:00","closed_at":"2026-03-04T14:06:12.755973619+06:00","close_reason":"d61eed3 All 6 tasks + 4 review fixes complete — lexicon validation on all 22 write sites","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-fpyy.1","title":"Create assertValidRecord helper and clean up dead validation wrappers in lib/utils.ts","description":"## Files\n- lib/record-validation.ts (create)\n- lib/utils.ts (modify)\n\n## What to do\n\n### 1. Create lib/record-validation.ts\nCreate a generic validation helper that can be used across all write sites. The function should:\n\n\\`\\`\\`ts\nimport type { ValidationResult } from \"@atproto/lexicon\";\n\n/**\n * Validates a record against its lexicon schema and throws if invalid.\n * @param label - Human-readable label for error messages (e.g. \"activity\", \"contribution\")\n * @param record - The record to validate\n * @param validateFn - The validateRecord function from the lexicon namespace\n * @throws Error with descriptive message if validation fails\n */\nexport function assertValidRecord\u003cT\u003e(\n  label: string,\n  record: unknown,\n  validateFn: (v: unknown) =\u003e ValidationResult\u003cT\u003e,\n): asserts record is T {\n  const result = validateFn(record);\n  if (!result.success) {\n    throw new Error(\\`Invalid \\${label} record: \\${result.error.message}\\`);\n  }\n}\n\\`\\`\\`\n\nCheck the actual shape of \\`ValidationResult\\` from \\`@atproto/lexicon\\` to make sure the error access pattern is correct. Look at \\`node_modules/@atproto/lexicon/dist/types.d.ts\\` or similar. The validation result is a discriminated union: \\`{ success: true; value: T } | { success: false; error: ValidationError }\\`. \\`ValidationError\\` extends \\`Error\\` so \\`.message\\` is available.\n\n### 2. Clean up lib/utils.ts\nRemove the 3 dead validation wrappers (lines 90-121): \\`validateHypercert\\`, \\`validateContribution\\`, \\`validateEvaluation\\`. Also remove their now-unused imports:\n- \\`OrgHypercertsClaimActivity as Hypercert\\` (line 4) — check if used elsewhere in the file first\n- \\`OrgHypercertsClaimContributionDetails as Contribution\\` (line 5)\n- \\`OrgHypercertsClaimEvaluation as Evaluation\\` (line 6)\n\nOnly remove imports that become unused after removing the dead functions.\n\n## Don't\n- Don't add validation calls to any write sites yet (that's separate tasks)\n- Don't change any other functions in lib/utils.ts\n- Don't modify the parseAtUri or other utility functions","acceptance_criteria":"1. lib/record-validation.ts exists with an assertValidRecord function that takes (label, record, validateFn) and throws on invalid records\n2. The function signature uses asserts return type for TypeScript narrowing\n3. The 3 dead validation wrappers (validateHypercert, validateContribution, validateEvaluation) are removed from lib/utils.ts\n4. Any imports that became unused after removing the dead wrappers are also removed\n5. npm run build succeeds with no type errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T12:18:22.240099959+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T12:26:08.134665613+06:00","closed_at":"2026-03-04T12:26:08.134665613+06:00","close_reason":"eb2d634 add assertValidRecord helper and remove dead validation wrappers","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fpyy.1","depends_on_id":"hypercerts-fpyy","type":"parent-child","created_at":"2026-03-04T12:18:22.242180262+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fpyy.2","title":"Add lexicon validation to lib/queries.ts write functions","description":"## Files\n- lib/queries.ts (modify)\n\n## What to do\nAdd assertValidRecord calls before every createRecord/putRecord in lib/queries.ts. This file has 5 write functions that accept pre-built typed records. Even though they're typed, the records could have been constructed incorrectly upstream.\n\n### Functions to add validation to:\n\n1. **createHypercert** (line 58-68) — collection: org.hypercerts.claim.activity\n   \\`\\`\\`ts\n   import { OrgHypercertsClaimActivity } from \"@hypercerts-org/lexicon\";\n   // Before createRecord:\n   assertValidRecord(\"activity\", record, OrgHypercertsClaimActivity.validateRecord);\n   \\`\\`\\`\n\n2. **updateHypercert** (line 70-82) — collection: org.hypercerts.claim.activity\n   Same validation as createHypercert, before putRecord.\n\n3. **createContribution** (line 84-94) — collection: org.hypercerts.claim.contribution\n   \\`\\`\\`ts\n   // Note: the type is OrgHypercertsClaimContributionDetails (already imported as HypercertContribution)\n   assertValidRecord(\"contributionDetails\", record, HypercertContribution.validateRecord);\n   \\`\\`\\`\n   Wait — check the actual collection name. The function uses \\`Collections.contribution\\`. Look at the Collections object (around line 10-20) to see what collection string it maps to. The validateRecord function must match the collection's lexicon.\n\n4. **createEvaluation** (line 96-106) — collection: org.hypercerts.claim.evaluation\n   \\`\\`\\`ts\n   assertValidRecord(\"evaluation\", record, HypercertEvaluation.validateRecord);\n   \\`\\`\\`\n\n5. **createEvidence** (line 108-118) — collection: org.hypercerts.claim.evidence\n   The type is \\`HypercertEvidence.Record\\` which is aliased from \\`OrgHypercertsClaimAttachment\\`. Check if \\`OrgHypercertsClaimAttachment\\` has a \\`validateRecord\\`. If so:\n   \\`\\`\\`ts\n   assertValidRecord(\"evidence\", record, HypercertEvidence.validateRecord);\n   \\`\\`\\`\n\n6. **createLocation** (line 120-130) — collection: app.certified.location\n   \\`\\`\\`ts\n   assertValidRecord(\"location\", record, HypercertLocation.validateRecord);\n   \\`\\`\\`\n\n### Imports to add:\n\\`\\`\\`ts\nimport { assertValidRecord } from \"@/lib/record-validation\";\n\\`\\`\\`\nThe lexicon namespace imports are already present (HypercertClaim, HypercertContribution, etc.) — just use their .validateRecord.\n\nFor HypercertClaim (OrgHypercertsClaimActivity), it's currently imported as a type-only import. You'll need to change it to a value import since validateRecord is a runtime function. Check the current import style and adjust.\n\n## Don't\n- Don't change function signatures or return types\n- Don't change how records are constructed (they come from callers)\n- Don't add try/catch — let the error propagate to the caller","acceptance_criteria":"1. All 6 write functions (createHypercert, updateHypercert, createContribution, createEvaluation, createEvidence, createLocation) call assertValidRecord before their createRecord/putRecord call\n2. Each validation uses the correct lexicon namespace's validateRecord function matching the collection\n3. assertValidRecord is imported from @/lib/record-validation\n4. Lexicon imports are value imports (not type-only) since validateRecord is used at runtime\n5. npm run build succeeds with no type errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T12:18:44.595502864+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T12:28:58.295273666+06:00","closed_at":"2026-03-04T12:28:58.295273666+06:00","close_reason":"dc6f9d8 Add lexicon validation to lib/queries.ts write functions","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fpyy.2","depends_on_id":"hypercerts-fpyy","type":"parent-child","created_at":"2026-03-04T12:18:44.597080762+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-fpyy.2","depends_on_id":"hypercerts-fpyy.1","type":"blocks","created_at":"2026-03-04T12:18:44.60095583+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fpyy.3","title":"Add lexicon validation to lib/create-actions.ts write operations","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\nAdd assertValidRecord calls before every createRecord/putRecord in lib/create-actions.ts. This file constructs records inline and writes them.\n\n### Write operations to validate:\n\n1. **Line ~125** — createRecord for \\`org.hypercerts.claim.contributionDetails\\`\n   Record is typed as \\`OrgHypercertsClaimContributionDetails.Record\\`. Validate before write:\n   \\`\\`\\`ts\n   assertValidRecord(\"contributionDetails\", record, OrgHypercertsClaimContributionDetails.validateRecord);\n   \\`\\`\\`\n\n2. **Line ~143** — createRecord for \\`org.hypercerts.claim.contributorInformation\\`\n   Record is typed as \\`OrgHypercertsClaimContributorInformation.Record\\`. Validate before write:\n   \\`\\`\\`ts\n   assertValidRecord(\"contributorInformation\", record, OrgHypercertsClaimContributorInformation.validateRecord);\n   \\`\\`\\`\n\n3. **Line ~181** — putRecord for \\`org.hypercerts.claim.activity\\` (addContribution updates hypercert to append contributors)\n   The record is fetched, cast to \\`Record\u003cstring, unknown\u003e\\`, mutated, then written back. Validate the final record before putRecord:\n   \\`\\`\\`ts\n   import { OrgHypercertsClaimActivity } from \"@hypercerts-org/lexicon\";\n   assertValidRecord(\"activity\", updatedRecord, OrgHypercertsClaimActivity.validateRecord);\n   \\`\\`\\`\n   Check if OrgHypercertsClaimActivity is already imported. If not, add it.\n\n4. **Line ~246** — createRecord for \\`org.hypercerts.claim.evaluation\\`\n   Already imported as OrgHypercertsClaimEvaluation. Validate before write:\n   \\`\\`\\`ts\n   assertValidRecord(\"evaluation\", record, OrgHypercertsClaimEvaluation.validateRecord);\n   \\`\\`\\`\n\n5. **Line ~324** — createRecord for \\`org.hypercerts.claim.measurement\\`\n   Already imported as OrgHypercertsClaimMeasurement. Validate before write:\n   \\`\\`\\`ts\n   assertValidRecord(\"measurement\", record, OrgHypercertsClaimMeasurement.validateRecord);\n   \\`\\`\\`\n\n6. **Line ~522** — putRecord for \\`org.hypercerts.claim.measurement\\` (updateMeasurement)\n   Fetches existing, merges, writes back. Validate the merged record before putRecord:\n   \\`\\`\\`ts\n   assertValidRecord(\"measurement\", updatedRecord, OrgHypercertsClaimMeasurement.validateRecord);\n   \\`\\`\\`\n\n### Imports to add:\n\\`\\`\\`ts\nimport { assertValidRecord } from \"@/lib/record-validation\";\n\\`\\`\\`\nAlso add \\`OrgHypercertsClaimActivity\\` to the existing import from \\`@hypercerts-org/lexicon\\` if not already there.\n\n### Error handling\nThis file uses \"use server\" (server actions). The assertValidRecord will throw, which will propagate as a server action error to the client. This is the desired behavior — the client already handles errors from these actions.\n\n## Don't\n- Don't change record construction logic\n- Don't change function signatures\n- Don't wrap in try/catch (let errors propagate naturally)\n- Don't modify the blob upload or strong ref resolution logic","acceptance_criteria":"1. All 6 write operations in create-actions.ts have assertValidRecord called before their createRecord/putRecord\n2. Each validation uses the correct lexicon namespace's validateRecord function\n3. assertValidRecord is imported from @/lib/record-validation\n4. OrgHypercertsClaimActivity is imported (value import, not type-only) for the activity validation\n5. npm run build succeeds with no type errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T12:19:05.376665298+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T12:28:30.39789592+06:00","closed_at":"2026-03-04T12:28:30.39789592+06:00","close_reason":"623dd2d Add lexicon validation to lib/create-actions.ts write operations","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fpyy.3","depends_on_id":"hypercerts-fpyy","type":"parent-child","created_at":"2026-03-04T12:19:05.378138188+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-fpyy.3","depends_on_id":"hypercerts-fpyy.1","type":"blocks","created_at":"2026-03-04T12:19:05.381952411+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fpyy.4","title":"Add lexicon validation to app/api/certs/route.ts write operations","description":"## Files\n- app/api/certs/route.ts (modify)\n\n## What to do\nAdd assertValidRecord calls before every createRecord/putRecord in the certs API route. This file has both POST (create) and PUT (update) handlers.\n\n### POST handler writes:\n\n1. **Line ~109** — createRecord for \\`org.hypercerts.claim.rights\\`\n   Record typed as \\`OrgHypercertsClaimRights.Record\\`. Validate before write:\n   \\`\\`\\`ts\n   assertValidRecord(\"rights\", rightsRecord, OrgHypercertsClaimRights.validateRecord);\n   \\`\\`\\`\n   OrgHypercertsClaimRights is already imported. Check if it's a type-only import and change to value import if needed.\n\n2. **Line ~133** — createRecord for \\`org.hypercerts.claim.activity\\`\n   Record typed as \\`OrgHypercertsClaimActivity.Record\\`. Validate before write:\n   \\`\\`\\`ts\n   assertValidRecord(\"activity\", certRecord, OrgHypercertsClaimActivity.validateRecord);\n   \\`\\`\\`\n   OrgHypercertsClaimActivity is already imported. Check if it's a type-only import.\n\n### PUT handler writes:\n\n3. **Line ~251** — putRecord for \\`org.hypercerts.claim.activity\\`\n   Fetches existing record, merges with form data updates, writes back. Validate the merged record before putRecord:\n   \\`\\`\\`ts\n   assertValidRecord(\"activity\", updatedRecord, OrgHypercertsClaimActivity.validateRecord);\n   \\`\\`\\`\n\n### Error handling\nThis is an API route. Wrap the assertValidRecord call in the existing try/catch, or add a specific catch that returns a 400 response with the validation error message. The file already has try/catch blocks — the thrown error from assertValidRecord will be caught and returned as a 500. Ideally, catch validation errors specifically and return 400:\n\n\\`\\`\\`ts\n// Option: check before write and return 400\ntry {\n  assertValidRecord(\"activity\", record, OrgHypercertsClaimActivity.validateRecord);\n} catch (e) {\n  return NextResponse.json({ error: e instanceof Error ? e.message : \"Validation failed\" }, { status: 400 });\n}\n\\`\\`\\`\n\nOr simply let the existing try/catch handle it — the error message will still be descriptive. Either approach is acceptable, but returning 400 for validation errors is preferred over 500.\n\n### Imports to add:\n\\`\\`\\`ts\nimport { assertValidRecord } from \"@/lib/record-validation\";\n\\`\\`\\`\nEnsure lexicon imports are value imports (not type-only).\n\n## Don't\n- Don't change record construction logic\n- Don't change form data parsing\n- Don't change the response shape","acceptance_criteria":"1. All 3 write operations in app/api/certs/route.ts have assertValidRecord called before their createRecord/putRecord\n2. Validation errors return HTTP 400 (not 500) with a descriptive error message\n3. Each validation uses the correct lexicon namespace's validateRecord function\n4. assertValidRecord is imported from @/lib/record-validation\n5. npm run build succeeds with no type errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T12:19:24.185619612+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T12:28:02.172160782+06:00","closed_at":"2026-03-04T12:28:02.172160782+06:00","close_reason":"b3210a7 Add lexicon validation to certs API route write operations","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fpyy.4","depends_on_id":"hypercerts-fpyy","type":"parent-child","created_at":"2026-03-04T12:19:24.187369864+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-fpyy.4","depends_on_id":"hypercerts-fpyy.1","type":"blocks","created_at":"2026-03-04T12:19:24.191319011+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fpyy.5","title":"Add lexicon validation to remaining API routes (add-attachment, add-location, atproto-writes, profile routes)","description":"## Files\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n- lib/atproto-writes.ts (modify)\n- app/api/profile/update/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n\n## What to do\nAdd assertValidRecord calls before every createRecord/putRecord in these remaining files.\n\n### 1. app/api/certs/add-attachment/route.ts\n**Line ~187** — createRecord for \\`org.hypercerts.claim.attachment\\`\nRecord typed as \\`OrgHypercertsClaimAttachment.Record\\`. Already imported from \\`@hypercerts-org/lexicon\\`.\n\\`\\`\\`ts\nassertValidRecord(\"attachment\", attachmentRecord, OrgHypercertsClaimAttachment.validateRecord);\n\\`\\`\\`\nEnsure import is value import (not type-only). Return 400 on validation error.\n\n### 2. app/api/certs/add-location/route.ts\n**Line ~115** — putRecord for \\`org.hypercerts.claim.activity\\`\nFetches existing record, appends location ref, writes back. Validate before putRecord:\n\\`\\`\\`ts\nimport { OrgHypercertsClaimActivity } from \"@hypercerts-org/lexicon\";\nassertValidRecord(\"activity\", updatedRecord, OrgHypercertsClaimActivity.validateRecord);\n\\`\\`\\`\nOrgHypercertsClaimActivity is already imported. Ensure it's a value import. Return 400 on validation error.\n\n### 3. lib/atproto-writes.ts\n**Line ~81** — createRecord for \\`app.certified.location\\`\nRecord typed as \\`AppCertifiedLocation.Record\\`. Already imported from \\`@hypercerts-org/lexicon\\`.\n\\`\\`\\`ts\nassertValidRecord(\"location\", locationRecord, AppCertifiedLocation.validateRecord);\n\\`\\`\\`\nEnsure import is value import. This is a library function — let the error propagate (no try/catch needed).\n\n### 4. app/api/profile/update/route.ts\n**Line ~82** — createRecord for \\`app.certified.actor.profile\\`\n**Line ~130** — putRecord for \\`app.certified.actor.profile\\`\nRecord is constructed as untyped \\`Record\u003cstring, unknown\u003e\\`.\n\\`\\`\\`ts\nimport { AppCertifiedActorProfile } from \"@hypercerts-org/lexicon\";\nassertValidRecord(\"certifiedProfile\", record, AppCertifiedActorProfile.validateRecord);\n\\`\\`\\`\nReturn 400 on validation error.\n\n### 5. app/api/profile/bsky/update/route.ts\n**Line ~69** — createRecord for \\`app.bsky.actor.profile\\`\n**Line ~105** — putRecord for \\`app.bsky.actor.profile\\`\nRecord is constructed as untyped \\`Record\u003cstring, unknown\u003e\\`.\n\\`\\`\\`ts\nimport { AppBskyActorProfile } from \"@atproto/api\";\nassertValidRecord(\"bskyProfile\", record, AppBskyActorProfile.validateRecord);\n\\`\\`\\`\nCheck that \\`AppBskyActorProfile\\` is exported from \\`@atproto/api\\` and has \\`validateRecord\\`. Look at \\`node_modules/@atproto/api/dist/client/index.d.ts\\` for the export. If the namespace export doesn't include validateRecord, check \\`node_modules/@atproto/api/dist/client/types/app/bsky/actor/profile.d.ts\\` directly.\nReturn 400 on validation error.\n\n### Error handling pattern for API routes\nFor all API route files, catch validation errors and return 400:\n\\`\\`\\`ts\ntry {\n  assertValidRecord(\"label\", record, Namespace.validateRecord);\n} catch (e) {\n  return NextResponse.json(\n    { error: e instanceof Error ? e.message : \"Validation failed\" },\n    { status: 400 }\n  );\n}\n\\`\\`\\`\n\n### Imports to add to each file:\n\\`\\`\\`ts\nimport { assertValidRecord } from \"@/lib/record-validation\";\n\\`\\`\\`\n\n## Don't\n- Don't change record construction logic\n- Don't change form data parsing or blob upload logic\n- Don't change response shapes\n- Don't modify any other files","acceptance_criteria":"1. app/api/certs/add-attachment/route.ts validates the attachment record before createRecord\n2. app/api/certs/add-location/route.ts validates the activity record before putRecord\n3. lib/atproto-writes.ts validates the location record before createRecord\n4. app/api/profile/update/route.ts validates the certified profile record before both createRecord and putRecord\n5. app/api/profile/bsky/update/route.ts validates the bsky profile record before both createRecord and putRecord\n6. All API route validation errors return HTTP 400 with descriptive error messages\n7. assertValidRecord is imported from @/lib/record-validation in all files\n8. npm run build succeeds with no type errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-03-04T12:19:47.050558306+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T12:29:35.946236452+06:00","closed_at":"2026-03-04T12:29:35.946236452+06:00","close_reason":"1e83b8d Add lexicon validation to remaining API routes","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-fpyy.5","depends_on_id":"hypercerts-fpyy","type":"parent-child","created_at":"2026-03-04T12:19:47.052438813+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-fpyy.5","depends_on_id":"hypercerts-fpyy.1","type":"blocks","created_at":"2026-03-04T12:19:47.057474643+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-fpyy.6","title":"Type bsky profile records as AppBskyActorProfile.Record instead of Record\u003cstring, unknown\u003e","description":"## Files\n- app/api/profile/bsky/update/route.ts (modify)\n- app/api/profile/update/route.ts (modify)\n\n## What to do\nReplace untyped \\`Record\u003cstring, unknown\u003e\\` with proper lexicon types in both profile update routes.\n\n### 1. app/api/profile/bsky/update/route.ts\n\n**Line 62 (create path):**\nChange \\`const record: Record\u003cstring, unknown\u003e\\` to \\`const record: AppBskyActorProfile.Record\\`.\n\\`AppBskyActorProfile\\` is already imported on line 4.\n\n**Line 104 (update path):**\nChange \\`const record: Record\u003cstring, unknown\u003e\\` to \\`const record: AppBskyActorProfile.Record\\`.\nThe null-then-delete pattern (\\`displayName: displayName || null\\` then \\`if (record.displayName === null) delete record.displayName\\`) won't type-check since the type defines \\`displayName?: string\\` (no null). Refactor to:\n\\`\\`\\`ts\nconst record: AppBskyActorProfile.Record = {\n  ...existingProfile,\n  \\$type: \"app.bsky.actor.profile\" as const,\n};\nif (displayName) {\n  record.displayName = displayName;\n} else {\n  delete record.displayName;\n}\nif (description) {\n  record.description = description;\n} else {\n  delete record.description;\n}\n\\`\\`\\`\n\n### 2. app/api/profile/update/route.ts\n\n**Line 56 — existing profile cast:**\nChange \\`as CertifiedActorProfile | undefined\\` to \\`as AppCertifiedActorProfile.Record | undefined\\`.\n\\`AppCertifiedActorProfile\\` is already imported on line 8. Then remove the \\`CertifiedActorProfile\\` import from \\`@/lib/types\\` (line 7) if it's no longer used in this file.\n\n**Line 74 (create path):**\nChange \\`const record: Record\u003cstring, unknown\u003e\\` to \\`const record: AppCertifiedActorProfile.Record\\`.\n\n**Line 118 (update path):**\nChange \\`const updateRecord: Record\u003cstring, unknown\u003e\\` to \\`const updateRecord: AppCertifiedActorProfile.Record\\`.\nThe update path already uses the if/else delete pattern (lines 124-139), so it should type-check cleanly.\n\n**Line 174 — updated profile cast:**\nChange \\`as CertifiedActorProfile | undefined\\` to \\`as AppCertifiedActorProfile.Record | undefined\\`.\n\nAfter all changes, check if \\`CertifiedActorProfile\\` from \\`@/lib/types\\` is still used anywhere in the file. If not, remove the import on line 7.\n\n## Don't\n- Don't change the logic or behavior — same fields, same conditions\n- Don't touch the validation or response code\n- Don't change blob upload logic\n- Don't remove CertifiedActorProfile from lib/types.ts itself (it may be used elsewhere)","acceptance_criteria":"1. Both record variables in app/api/profile/bsky/update/route.ts are typed as AppBskyActorProfile.Record, not Record\u003cstring, unknown\u003e\n2. Both record variables in app/api/profile/update/route.ts are typed as AppCertifiedActorProfile.Record, not Record\u003cstring, unknown\u003e\n3. The existingProfile and updated casts in app/api/profile/update/route.ts use AppCertifiedActorProfile.Record instead of CertifiedActorProfile\n4. No null assignments to typed string fields — use if/else with delete instead\n5. Behavior is unchanged: empty string fields are removed, non-empty are set\n6. Unused imports are removed from both files\n7. npm run build succeeds with no type errors","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T12:54:58.323993316+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T12:59:13.229878858+06:00","closed_at":"2026-03-04T12:59:13.229878858+06:00","close_reason":"fc975b8 type profile records as lexicon types instead of Record\u003cstring, unknown\u003e","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-fpyy.6","depends_on_id":"hypercerts-fpyy","type":"parent-child","created_at":"2026-03-04T12:54:58.325527082+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-gtp6","title":"Fix: console.log(imageUri) left in production code in hypercert detail page (from hypercerts-02w.4)","description":"Review of hypercerts-02w.4 found: app/hypercerts/[hypercertUri]/page.tsx (line 198) has a console.log(imageUri) statement left in production code. This was present in the original code before the migration and was not cleaned up. Fix: remove the console.log statement.","status":"open","priority":4,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:56.164382875+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:56.164382875+06:00","dependencies":[{"issue_id":"hypercerts-gtp6","depends_on_id":"hypercerts-02w.4","type":"discovered-from","created_at":"2026-03-04T11:17:56.234898373+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-h4f","title":"Fix: stale 'Step 7' comment in CompletionStepSkeleton (from hypercerts-dcb)","description":"Review of hypercerts-dcb found: The comment on line 343 of components/form-skeletons.tsx reads '// Step 7: Completion Step Skeleton' but the flow is now a 5-step flow (contributions step disabled). The comment should read '// Completion Step Skeleton' or simply remove the step number. All other step labels in the file correctly say 'of 5'. This is a cosmetic/documentation issue — no functional impact. Evidence: form-skeletons.tsx:343 still has the old step number from before the 6→5 step reduction.","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-24T14:52:04.295346478+06:00","created_by":"kzoeps","updated_at":"2026-02-24T15:03:43.535557145+06:00","closed_at":"2026-02-24T15:03:43.535557145+06:00","close_reason":"9270bd5 fix: remove stale step number from CompletionStepSkeleton comment","dependencies":[{"issue_id":"hypercerts-h4f","depends_on_id":"hypercerts-dcb","type":"discovered-from","created_at":"2026-02-24T14:52:07.631370032+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-hfuo","title":"Fix: addEvaluation passes raw AT-URI strings for measurements/location instead of resolved StrongRefs (from hypercerts-phiw)","description":"Review of hypercerts-phiw found: In lib/create-actions.ts, the addEvaluation function accepts measurements as string[] (AT-URIs) and location as string (AT-URI), but passes them directly to the record with 'as unknown as' casts instead of resolving them to StrongRefs first.\n\nEvidence: lib/create-actions.ts lines 263-275:\n  // measurements and location are passed as-is (AT-URIs); the index signature accepts them\n  ...(evaluationData.measurements\n    ? {\n        measurements:\n          evaluationData.measurements as unknown as OrgHypercertsClaimEvaluation.Record[\"measurements\"],\n      }\n    : {}),\n  ...(evaluationData.location\n    ? {\n        location:\n          evaluationData.location as unknown as OrgHypercertsClaimEvaluation.Record[\"location\"],\n      }\n    : {}),\n\nThe evaluation lexicon (org/hypercerts/context/evaluation.json) defines:\n  measurements: { type: 'array', items: { type: 'ref', ref: 'com.atproto.repo.strongRef' } }\n  location: { type: 'ref', ref: 'com.atproto.repo.strongRef' }\n\nBoth fields expect { uri: string, cid: string } objects, not raw AT-URI strings.\n\nThe assertValidRecord call at line 278 will throw at runtime when measurements or location are provided, because the validator will see strings instead of StrongRef objects.\n\nFix: Resolve measurements and location to StrongRefs before building the record, using resolveStrongRef() (already imported). Pattern:\n  const measurementRefs = evaluationData.measurements\n    ? await Promise.all(evaluationData.measurements.map(uri =\u003e resolveStrongRef(ctx.agent, uri, 'org.hypercerts.context.measurement')))\n    : undefined;\n  const locationRef = evaluationData.location\n    ? await resolveStrongRef(ctx.agent, evaluationData.location, 'app.certified.location')\n    : undefined;","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T16:49:02.674338796+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:56:06.401789129+06:00","closed_at":"2026-03-04T16:56:06.401789129+06:00","close_reason":"e2ce130 Fix addEvaluation to resolve measurements/location AT-URIs to StrongRefs","dependencies":[{"issue_id":"hypercerts-hfuo","depends_on_id":"hypercerts-phiw","type":"discovered-from","created_at":"2026-03-04T16:49:05.392166302+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-hu1","title":"Fix: Navbar nav links have no aria-current='page' for active state (navbar.tsx)","description":"## File\\ncomponents/navbar.tsx lines 72-93\\n\\n## Severity\\nMEDIUM — Accessibility\\n\\n## Problem\\nThe navbar uses conditional CSS classes to visually indicate the active route, but does not set aria-current='page' on the active link. Screen readers cannot identify which navigation link corresponds to the current page.\\n\\n## Evidence\\n```tsx\\n\u003cLink\\n  href=\"/hypercerts\"\\n  className={pathname === '/hypercerts' ? 'bg-create-accent/10 text-create-accent' : 'text-muted-foreground'}\\n  // Missing: aria-current\\n\u003e\\n  Hypercerts\\n\u003c/Link\u003e\\n```\\n\\n## Fix\\n```tsx\\n\u003cLink\\n  href=\"/hypercerts\"\\n  aria-current={pathname === '/hypercerts' || (pathname?.startsWith('/hypercerts') \u0026\u0026 pathname !== '/hypercerts/create') ? 'page' : undefined}\\n  className={...}\\n\u003e\\n  Hypercerts\\n\u003c/Link\u003e\\n```\\n\\nApply the same pattern to the 'Create' link.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:40.802451071+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:40.802451071+06:00"}
{"id":"hypercerts-i1m","title":"Epic: Clean up loopback OAuth fix — remove debug code, conditional application_type, dead imports","description":"## Context\nThree changes were made to fix ePDS OAuth for loopback mode:\n1. Added application_type: native to PAR request body\n2. Created separate epdsClientId using the ePDS redirect URI\n3. Fixed getEpdsClientId() to return config.epdsClientId\n\nThe fix works but leaves loose ends: a debug console.log, application_type hardcoded to native (breaks production), dead imports/comments from deleted loopback scope code, and a stale fallback scope in the callback route.\n\n## Scope\n- Remove debug log\n- Make application_type conditional on loopback vs production\n- Clean up dead TRANSITION_SCOPES import and stale doc comment\n- Fix callback fallback scope to use OAUTH_SCOPE instead of hardcoded transition:generic\n- All changes scoped to 3 files: app/api/oauth/login/route.ts, lib/config.ts, app/api/oauth/callback/route.ts\n\n## Success Criteria\n- No debug console.log in login route\n- PAR request sends application_type: native for loopback, web for production\n- No unused imports in config.ts\n- Doc comments accurate (no references to deleted loopback scope)\n- Callback fallback scope uses OAUTH_SCOPE constant\n- Production flow unchanged","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-26T15:48:16.510617829+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T18:54:08.427512497+06:00","closed_at":"2026-02-26T18:54:08.427521434+06:00","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-i1m.1","title":"Clean up loopback OAuth: remove debug log, conditional application_type, dead imports, fix fallback scope","description":"## Files\n- app/api/oauth/login/route.ts (modify)\n- lib/config.ts (modify)\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\n\n### 1. Remove debug console.log (app/api/oauth/login/route.ts)\nDelete line 33: `console.log(redirectUri)` — this is a leftover debug log.\n\n### 2. Make application_type conditional on loopback (app/api/oauth/login/route.ts)\nLine 41 currently hardcodes `application_type: \"native\"` in the PAR body. This must be conditional:\n- Import `config` (already imported on line 15)\n- Change the PAR body construction to use `config.isLoopback`:\n\n```ts\nconst parBody = new URLSearchParams({\n  client_id: clientId,\n  redirect_uri: redirectUri,\n  response_type: \"code\",\n  scope: OAUTH_SCOPE,\n  application_type: config.isLoopback ? \"native\" : \"web\",\n  state,\n  code_challenge: codeChallenge,\n  code_challenge_method: \"S256\",\n});\n```\n\n### 3. Remove unused TRANSITION_SCOPES import (lib/config.ts)\nLine 14 imports `TRANSITION_SCOPES` from `@hypercerts-org/sdk-core`. This is no longer used anywhere (LOOPBACK_SCOPE was deleted). Remove it from the import statement. The import should become:\n```ts\nimport {\n  ATPROTO_SCOPE,\n  HYPERCERT_COLLECTIONS,\n} from \"@hypercerts-org/sdk-core\";\n```\n\n### 4. Update stale doc comment (lib/config.ts)\nLines 193-197 (the OAUTH_SCOPE export comment) still reference loopback scope behavior that no longer exists:\n```ts\n/**\n * OAuth scope configuration — environment-aware:\n * - Loopback (local dev): \"atproto transition:generic\" (ATProto requirement for loopback clients)\n * - Production: granular scopes (repo, rpc, blob) for precise permission requests\n */\nexport const OAUTH_SCOPE = GRANULAR_SCOPE;\n```\nReplace with:\n```ts\n/**\n * OAuth scope — granular scopes (repo, rpc, blob) for all environments.\n */\nexport const OAUTH_SCOPE = GRANULAR_SCOPE;\n```\n\n### 5. Fix callback fallback scope (app/api/oauth/callback/route.ts)\nLine 126 has a hardcoded fallback:\n```ts\nscope: (tokenData.scope ?? \"atproto transition:generic\") as NodeSavedSession[\"tokenSet\"][\"scope\"],\n```\nChange to use the OAUTH_SCOPE constant:\n```ts\nscope: (tokenData.scope ?? OAUTH_SCOPE) as NodeSavedSession[\"tokenSet\"][\"scope\"],\n```\nAdd `OAUTH_SCOPE` to the import from `@/lib/config` on line 9:\n```ts\nimport { config, OAUTH_SCOPE } from \"@/lib/config\";\n```\n\n## Don't\n- Change the epdsClientId or clientId logic (that is correct)\n- Change the buildClientMetadata() function\n- Change any other OAuth flow logic\n- Add new dependencies or files","acceptance_criteria":"1. No console.log(redirectUri) in app/api/oauth/login/route.ts\n2. PAR body sends application_type: \"native\" when config.isLoopback is true, \"web\" when false\n3. TRANSITION_SCOPES is not imported in lib/config.ts\n4. The OAUTH_SCOPE doc comment does not reference loopback or transition:generic\n5. Callback route uses OAUTH_SCOPE constant as fallback scope, not hardcoded \"atproto transition:generic\"\n6. OAUTH_SCOPE is imported in app/api/oauth/callback/route.ts\n7. npx tsc --noEmit passes with no errors\n8. The existing unstaged changes in lib/config.ts and lib/epds-config.ts (epdsClientId addition, getEpdsClientId fix) are preserved — do NOT revert those","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-26T15:48:38.938306182+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T15:54:05.343890074+06:00","closed_at":"2026-02-26T15:54:05.343890074+06:00","close_reason":"f880d7b clean up loopback OAuth: remove debug log, conditional application_type, dead imports, fix fallback scope","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-i1m.1","depends_on_id":"hypercerts-i1m","type":"parent-child","created_at":"2026-02-26T15:48:38.941947817+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-i9ej","title":"Fix: getBlobURL has dead-code condition blobRef.$type === 'string' (from hypercerts-73o.3)","description":"Review of hypercerts-73o.3 found: lib/utils.ts getBlobURL() (line 25) has the condition 'blobRef.$type === \"string\"' which checks if the $type field of the object equals the literal string 'string'. This is almost certainly dead code — no ATProto record type uses $type: 'string'. If it was intended to handle a string-typed blobRef, the first guard 'typeof blobRef === \"string\"' already covers that. This condition will never be true and returns blobRef.$type (the string 'string') as a URL, which would be a broken URL. Fix: remove this dead-code branch entirely.","status":"open","priority":3,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:51.363536789+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:51.363536789+06:00","dependencies":[{"issue_id":"hypercerts-i9ej","depends_on_id":"hypercerts-73o.3","type":"discovered-from","created_at":"2026-03-04T11:17:51.42838333+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-igi","title":"Fix: loopback client metadata sets application_type='web' but comment says 'native'","description":"## Problem\nIn `lib/config.ts` line 263, the comment says:\n```\n// Loopback mode: no branding, application_type is \"native\"\n```\nBut line 281 actually sets:\n```ts\napplication_type: \"web\",\n```\n\nRFC 8252 (OAuth for Native Apps) specifies that loopback redirect clients should use `application_type: \"native\"`. The ATProto spec for loopback clients also expects `native`. Setting `web` for a loopback client may cause PDS servers to reject the OAuth flow or apply incorrect security policies.\n\n## Fix\nChange line 281 to:\n```ts\napplication_type: \"native\",\n```\nAnd verify the PDS accepts this value. The comment is correct — the code is wrong.\n\n## Evidence\n`lib/config.ts` line 263 (comment) vs line 281 (actual value).","notes":"NOTE from doc review 2026-02-27: The comment was already fixed in commit 0369b4b (2026-02-26) — current lib/config.ts line 263 reads '// Loopback mode: minimal metadata, application_type is \"web\"' which matches the code. However, the application_type: 'web' value itself may still be wrong per RFC 8252 (loopback clients should use 'native'). The code issue (wrong value) may still be valid even though the comment-code mismatch is resolved. See hypercerts-p2a for the remaining comment accuracy issue.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:29.809942969+06:00","created_by":"kzoeps","updated_at":"2026-02-27T14:19:53.723000705+06:00"}
{"id":"hypercerts-ijw","title":"Fix: add-location route missing ownership check before putRecord (from hypercerts-c8i.1)","description":"Review of hypercerts-c8i.1 found: app/api/certs/add-location/route.ts (lines 100-119) fetches the hypercert from hypercertParsed.did but then calls putRecord with repo: personalRepository.assertDid without first checking that hypercertParsed.did === personalRepository.assertDid. If a user submits a hypercertUri belonging to another user, the ATProto server will reject the write with an opaque error. The original SDK (personalRepository.hypercerts.attachLocation) handled this internally. Fix: add an explicit check after parsing the URI: if (hypercertParsed.did !== personalRepository.assertDid) return NextResponse.json({ error: 'Forbidden' }, { status: 403 });","status":"open","priority":1,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:15.610520807+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:15.610520807+06:00","dependencies":[{"issue_id":"hypercerts-ijw","depends_on_id":"hypercerts-c8i.1","type":"discovered-from","created_at":"2026-03-04T11:17:15.680329037+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-iuc","title":"Fix open redirect via //evil.com bypass in OAuth callback","description":"## Files\n- app/api/oauth/callback/route.ts (modify)\n\n## What to do\nLine 45 has an open redirect vulnerability:\n```ts\nconst redirectPath = returnTo.startsWith(\"/\") ? returnTo : \"/\";\n```\n\nThe problem: `//evil.com` starts with `/` so it passes the check. When used in `new URL(redirectPath, config.baseUrl)` on line 47, the browser interprets `//evil.com` as a protocol-relative URL and redirects to `https://evil.com`.\n\nReplace the `startsWith(\"/\")` check with proper origin validation:\n```ts\nconst resolved = new URL(returnTo, config.baseUrl);\nconst base = new URL(config.baseUrl);\nconst redirectPath = resolved.origin === base.origin\n  ? resolved.pathname + resolved.search\n  : \"/\";\n```\n\nThis ensures:\n- Normal relative paths like `/hypercerts/create` still work\n- Paths with query strings like `/profile?tab=settings` still work\n- `//evil.com` gets rejected because `resolved.origin` would be `https://evil.com` which !== base origin\n- `https://evil.com` gets rejected for the same reason\n- Empty/null returnTo falls back to `/` (the `|| \"/\"` on line 42 already handles this)\n\n## Don't\n- Change any other logic in the callback route\n- Modify the error handling paths (lines 9-27)\n- Touch the cookie setting logic (lines 34-39)\n- Add any new dependencies","acceptance_criteria":"1. returnTo='/' redirects to base origin + '/'\n2. returnTo='/hypercerts/create' redirects to base origin + '/hypercerts/create'\n3. returnTo='/profile?tab=settings' redirects to base origin + '/profile?tab=settings'\n4. returnTo='//evil.com' redirects to base origin + '/' (blocked)\n5. returnTo='//evil.com/path' redirects to base origin + '/' (blocked)\n6. returnTo='https://evil.com' redirects to base origin + '/' (blocked)\n7. returnTo='' redirects to base origin + '/' (fallback from line 42)\n8. returnTo=null (missing param) redirects to base origin + '/' (fallback from line 42)\n9. No other lines in the file are changed beyond lines 44-45\n10. File passes eslint and prettier","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-27T13:15:38.915020423+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T13:16:43.864732073+06:00","closed_at":"2026-02-27T13:16:43.864732073+06:00","close_reason":"d8e5f9f fix: prevent open redirect via protocol-relative URL in OAuth callback","labels":["scope:trivial"]}
{"id":"hypercerts-jgh","title":"Fix: dead `color: #ffffff` rule on ::after pseudo-element in atproto-branding.ts (from hypercerts-nyk.15)","description":"Review of hypercerts-nyk.15 found a dead CSS rule at line 167 of lib/atproto-branding.ts.\n\nThe rule:\n```css\n.grid h1.text-primary::after {\n  ...\n  color: #ffffff !important;  /* line 167 — DEAD RULE */\n}\n```\n\nThe `::after` pseudo-element has `content: \"\"` (empty string) and renders only a background-image (the certified-signin SVG). The `color` property has no effect on a pseudo-element with no text content. This is a leftover artifact from a previous iteration where the pseudo-element may have had text content.\n\nEvidence: lib/atproto-branding.ts lines 156-168. The pseudo-element uses background-image, not text, so color is irrelevant.\n\nFix: Remove the `color: #ffffff !important` line from `.grid h1.text-primary::after`.","status":"open","priority":3,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-02-17T19:45:42.302832017+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:45:42.302832017+06:00","dependencies":[{"issue_id":"hypercerts-jgh","depends_on_id":"hypercerts-nyk.15","type":"discovered-from","created_at":"2026-02-17T19:45:46.485905551+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-jk5","title":"Fix: atproto-branding.ts generateBrandingCss uses fragile CSS class selectors that will break if PDS updates its markup","description":"## File\\nlib/atproto-branding.ts lines 109, 143, 167, 199\\n\\n## Severity\\nMEDIUM — Fragility / maintainability\\n\\n## Problem\\nThe branding CSS targets PDS OAuth page elements using highly specific structural selectors:\\n- .grid h1.text-primary (line 109)\\n- main.flex.flex-col.items-center:not(:has([role='alert'])) \u003e h1.text-primary::before (line 143)\\n- button[type='submit'].bg-primary (line 167)\\n- main.flex.flex-col.items-center \u003e button:nth-of-type(2) (line 167)\\n- .grid.grow.content-center (line 199)\\n\\nThese selectors depend on the PDS's internal Tailwind class names and DOM structure. Any PDS update that changes class names, layout, or element order will silently break the branding without any error. The :nth-of-type(2) selector is especially fragile.\\n\\n## Fix\\nThis is a known limitation of CSS injection into third-party pages. Document the fragility explicitly with a comment block at the top of the function:\\n```ts\\n/**\\n * WARNING: These selectors target internal PDS OAuth page markup.\\n * They will break if the PDS updates its Tailwind classes or DOM structure.\\n * When branding breaks, check the PDS OAuth page source and update selectors.\\n * Last verified against: [PDS version/date]\\n */\\n```\\n\\nAlso consider adding a CHANGELOG entry or test that verifies the selectors against a known PDS HTML snapshot.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:58.33925359+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:58.33925359+06:00"}
{"id":"hypercerts-jr3","title":"Fix: react/cache used in API Route handlers — wrong caching primitive","description":"## Problem\n`getRepoContext` (lib/repo-context.ts line 41) and `getAuthenticatedRepo` / `getSession` (lib/atproto-session.ts lines 7, 27) are wrapped with React's `cache()` function. React `cache()` is designed for React Server Components and deduplicates calls within a single render pass. It is NOT designed for Next.js API Route handlers (which are plain Node.js request handlers, not RSC renders).\n\nIn API Routes, `cache()` provides no deduplication guarantee and may silently return stale data across requests in some Next.js versions. The correct primitive for per-request deduplication in API Routes is either:\n- No caching (just call the function directly — each request is isolated)\n- `unstable_cache` from `next/cache` (for cross-request caching with explicit keys)\n\nThe current code works in Server Actions (which do run in an RSC-like context) but is semantically wrong in the API Route handlers that call `getAuthenticatedRepo()` directly.\n\n## Affected files\n- `lib/repo-context.ts` line 41: `export const getRepoContext = cache(...)`\n- `lib/atproto-session.ts` lines 7, 27: `getAuthenticatedRepo` and `getSession` wrapped with `cache()`\n- `app/api/certs/add-attachment/route.ts`, `app/api/certs/create/route.ts` call `getRepoContext()`\n- `app/api/certs/add-location/route.ts`, `app/api/profile/update/route.ts`, `app/api/profile/bsky/update/route.ts` call `getAuthenticatedRepo()`\n\n## Fix\nRemove `cache()` wrapping from `getRepoContext`, `getAuthenticatedRepo`, and `getSession`. Each API Route request is already isolated — no deduplication is needed. If deduplication within a single Server Action invocation is desired, keep `cache()` only on the Server Action path and export a separate uncached version for API Routes.","status":"open","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:21.22176763+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:21.22176763+06:00"}
{"id":"hypercerts-jsu","title":"Epic: Handle OAuth rejection gracefully","description":"When a user rejects the ATProto OAuth authorization, the callback URL includes error=access_denied. Currently the callback route lets sdk.callback() throw and returns raw JSON ({error, details}) to the browser — terrible UX. Goal: detect the error param early in the callback route, redirect the user back to / with a query param like ?auth_error=access_denied, and show a toast on the client side explaining what happened. The user should land back on the login screen seamlessly. Success: rejecting auth on the PDS shows a toast and the login dialog, not raw JSON. Scope: callback route early-return + client-side toast. Out of scope: new error pages, middleware, changes to the login flow itself.","status":"closed","priority":2,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-02-17T18:03:59.465100302+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:07:04.042012473+06:00","closed_at":"2026-02-17T18:07:04.042012473+06:00","close_reason":"5443dae All tasks completed — OAuth rejection redirects to / with toast","labels":["scope:small"]}
{"id":"hypercerts-jsu.1","title":"Handle OAuth error params in callback route and show client-side toast","description":"## Files\n- app/api/auth/callback/route.ts (modify)\n- providers/SignedInProvider.tsx (modify)\n\n## What to do\n\n### 1. Callback route: early-detect error param (app/api/auth/callback/route.ts)\n\nAt the top of the GET handler, BEFORE calling `sdk.callback(searchParams)`, check if `searchParams` has an `error` param. If it does:\n\n```ts\nconst error = searchParams.get(\"error\");\nif (error) {\n  const description = searchParams.get(\"error_description\") || \"Authorization was cancelled\";\n  console.warn(\"OAuth authorization error:\", error, description);\n  // Redirect back to home with error info for the client to display\n  const redirectUrl = new URL(\"/\", config.baseUrl);\n  redirectUrl.searchParams.set(\"auth_error\", error);\n  redirectUrl.searchParams.set(\"auth_error_description\", description);\n  return NextResponse.redirect(redirectUrl);\n}\n```\n\nAlso update the existing catch block to redirect instead of returning JSON:\n```ts\ncatch (e) {\n  console.error(\"Authentication failed:\", e);\n  const redirectUrl = new URL(\"/\", config.baseUrl);\n  redirectUrl.searchParams.set(\"auth_error\", \"callback_failed\");\n  redirectUrl.searchParams.set(\"auth_error_description\", e instanceof Error ? e.message : \"An unexpected error occurred\");\n  return NextResponse.redirect(redirectUrl);\n}\n```\n\n### 2. Client-side toast in SignedInProvider (providers/SignedInProvider.tsx)\n\nThis component already wraps all pages. Add a `useEffect` that:\n- Reads `auth_error` and `auth_error_description` from the URL search params (use `useSearchParams()` from next/navigation)\n- If `auth_error` is present, show a toast:\n  - If `auth_error === \"access_denied\"`: `toast.error(\"Login cancelled. You can try again when you're ready.\")`\n  - Otherwise: `toast.error(auth_error_description || \"Authentication failed. Please try again.\")`\n- Clean the URL by calling `router.replace(pathname)` (use `usePathname()` and `useRouter()` from next/navigation) to remove the query params\n- The useEffect should depend on searchParams\n\nNote: SignedInProvider currently receives `session` as a prop from the server. The toast logic should run regardless of whether session exists (the user will have no session after a rejection). If SignedInProvider is a server component, you may need to extract the toast logic into a small client component like `AuthErrorToast` and render it inside SignedInProvider.\n\nImport `toast` from `sonner` (already used elsewhere in the project, e.g. in components/navbar.tsx).\n\n## Don't\n- Do NOT create new pages or error boundary files\n- Do NOT modify the login flow (api/auth/login, use-login-mutation, etc.)\n- Do NOT add middleware\n- Do NOT change the successful auth flow at all\n- Do NOT install new dependencies — sonner and next/navigation are already available","acceptance_criteria":"1. When a user rejects OAuth authorization on the PDS, they are redirected to / (not shown raw JSON)\n2. A toast notification appears explaining the login was cancelled\n3. The URL is cleaned (no auth_error params remain in the address bar)\n4. When sdk.callback() throws for any other reason, the user is also redirected to / with a toast (not raw JSON)\n5. The successful auth flow (approve on PDS → cookie set → redirect to /) is completely unchanged\n6. No new dependencies added\n7. TypeScript compiles without errors: npx tsc --noEmit passes (or next build succeeds)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T18:04:23.910121958+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:06:18.748960162+06:00","closed_at":"2026-02-17T18:06:18.748960162+06:00","close_reason":"1f18629 Handle OAuth error params in callback route and show client-side toast","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-jsu.1","depends_on_id":"hypercerts-jsu","type":"parent-child","created_at":"2026-02-17T18:04:23.911881812+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-jw5","title":"Epic: Remove all SDS references and organization features","description":"Remove all Shared Data Server (SDS) infrastructure and organization-related features from the codebase. This includes environment configuration, type definitions, organization pages/components/queries, profile switching, and all related documentation. The application will only support PDS (Personal Data Server) going forward.","status":"closed","priority":1,"issue_type":"epic","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:19.625374856+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:08:54.570180551+06:00","closed_at":"2026-02-13T11:08:54.570180551+06:00","close_reason":"Completed: All SDS references and organization features removed"}
{"id":"hypercerts-jw5.1","title":"Remove SDS from environment configuration","description":"Remove NEXT_PUBLIC_SDS_URL environment variable from .env.example and .env.local. Remove sdsUrl field and validation from lib/config.ts (lines 194, 210, 236). Files affected: .env.example (lines 52-53), .env.local (line 20), lib/config.ts (lines 194, 210, 236).","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:25.0772292+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:01:21.906727001+06:00","closed_at":"2026-02-13T11:01:21.906727001+06:00","close_reason":"Completed: 970322f3161f84168f0130778997293b3c3cc853","dependencies":[{"issue_id":"hypercerts-jw5.1","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:57:25.078964487+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.10","title":"Remove profile switching dialog","description":"Delete components/profile-switch-dialog.tsx entirely. Remove this component from any layouts or pages that import it (likely in app/layout.tsx or similar). Remove the SignedInProvider usage from providers/SignedInProvider.tsx (line 27) that fetches organization data with getAuthenticatedRepo('sds'). This task depends on hypercerts-jw5.3 (session management) because the provider uses serverOverride.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:58:14.830834235+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:06:47.965297704+06:00","closed_at":"2026-02-13T11:06:47.965297704+06:00","close_reason":"Completed: af38f1b","dependencies":[{"issue_id":"hypercerts-jw5.10","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:58:14.832712721+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.10","depends_on_id":"hypercerts-jw5.3","type":"blocks","created_at":"2026-02-13T10:58:36.962725875+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.11","title":"Update repo context to handle PDS only","description":"Simplify lib/repo-context.ts to only handle PDS. Remove the server determination logic at line 61 (targetDid === userDid ? 'pds' : 'sds'). Remove RepoServer type field from interfaces if no longer needed. This task depends on hypercerts-jw5.2 (type definitions) and hypercerts-jw5.3 (session management) because we need the core types and session logic updated first.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:58:20.278269871+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:06:32.355814639+06:00","closed_at":"2026-02-13T11:06:32.355814639+06:00","close_reason":"Completed: 024d9b8b1dfe9cd5b49a45aeb7cae96cc95e2667","dependencies":[{"issue_id":"hypercerts-jw5.11","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:58:20.279708313+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.11","depends_on_id":"hypercerts-jw5.2","type":"blocks","created_at":"2026-02-13T10:58:37.013357307+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.11","depends_on_id":"hypercerts-jw5.3","type":"blocks","created_at":"2026-02-13T10:58:37.06325177+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.12","title":"Clean up organization query keys","description":"Remove organization-related query key definitions from lib/api/query-keys.ts (lines 41-48). This task depends on hypercerts-jw5.7 (delete organization queries) because we need to ensure no queries are using these keys before removing them.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:58:24.864366819+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:05:14.468657802+06:00","closed_at":"2026-02-13T11:05:14.468657802+06:00","close_reason":"Completed: 2e96aef","dependencies":[{"issue_id":"hypercerts-jw5.12","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:58:24.866971645+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.12","depends_on_id":"hypercerts-jw5.7","type":"blocks","created_at":"2026-02-13T10:58:37.11335342+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.13","title":"Update README to remove SDS documentation","description":"Remove all SDS references from README.md: line 10 (PDS/SDS account mention), line 45 (environment variable table entry), line 63 (test server URL example), lines 119-127 (architecture diagram and PDS vs SDS explanation), line 196 (organizations on SDS mention), line 254 (link to SDS GitHub repo). Update documentation to reflect PDS-only architecture. This task depends on all previous tasks (hypercerts-jw5.1 through hypercerts-jw5.12) because documentation should be updated last after all code changes are complete.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:58:31.437345428+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:08:36.620756909+06:00","closed_at":"2026-02-13T11:08:36.620756909+06:00","close_reason":"Completed: README already updated by previous commits","dependencies":[{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:58:31.438988373+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.1","type":"blocks","created_at":"2026-02-13T10:58:42.980883411+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.2","type":"blocks","created_at":"2026-02-13T10:58:43.031383969+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.3","type":"blocks","created_at":"2026-02-13T10:58:43.083619404+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.4","type":"blocks","created_at":"2026-02-13T10:58:43.135155641+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.5","type":"blocks","created_at":"2026-02-13T10:58:43.197015061+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.6","type":"blocks","created_at":"2026-02-13T10:58:43.246763992+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.7","type":"blocks","created_at":"2026-02-13T10:58:43.295935142+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.8","type":"blocks","created_at":"2026-02-13T10:58:43.345300956+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.9","type":"blocks","created_at":"2026-02-13T10:58:43.395508806+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.10","type":"blocks","created_at":"2026-02-13T10:58:43.443782516+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.11","type":"blocks","created_at":"2026-02-13T10:58:43.492942164+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.13","depends_on_id":"hypercerts-jw5.12","type":"blocks","created_at":"2026-02-13T10:58:43.545747096+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.2","title":"Update type definitions to remove SDS","description":"Change RepoServer type from 'pds' | 'sds' to just 'pds' in lib/repo-context.ts (line 8). Remove serverOverride parameter type that includes 'sds' from lib/atproto-session.ts (line 8). Update all type usage to reflect PDS-only infrastructure. Files affected: lib/repo-context.ts (lines 8, 23, 30, 61), lib/atproto-session.ts (lines 8, 19).","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:31.387671487+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:01:52.916414734+06:00","closed_at":"2026-02-13T11:01:52.916414734+06:00","close_reason":"Completed: 7cb57ff4f0e9545e9fd8b27d194de5236d269061","dependencies":[{"issue_id":"hypercerts-jw5.2","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:57:31.390035933+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.3","title":"Simplify session management by removing SDS logic","description":"Remove serverOverride parameter from getAuthenticatedRepo function in lib/atproto-session.ts (line 8). Remove server determination logic that defaults organizations to SDS (lines 19-26). Simplify to always use PDS. Remove comment about SDS default for organizations (line 23). This task depends on hypercerts-jw5.2 (type definition updates) because we need the types updated first.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:36.812241339+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:03:28.27736993+06:00","closed_at":"2026-02-13T11:03:28.27736993+06:00","close_reason":"Completed: f904aed717e18e851fcd2d6e590836fa9b7dc689","dependencies":[{"issue_id":"hypercerts-jw5.3","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:57:36.814164408+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.3","depends_on_id":"hypercerts-jw5.2","type":"blocks","created_at":"2026-02-13T10:58:36.651339481+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.4","title":"Update SDK initialization to remove SDS","description":"Remove 'sds: config.sdsUrl' from SDK server configuration in lib/hypercerts-sdk.ts (line 39). Update SDK initialization to only use PDS. This task depends on hypercerts-jw5.1 (environment config removal) because we need config.sdsUrl removed first.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:41.609155705+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:03:51.936722151+06:00","closed_at":"2026-02-13T11:03:51.936722151+06:00","close_reason":"Completed: 0ee8d8c967f42b720a0203022ce99e33647cb12a","dependencies":[{"issue_id":"hypercerts-jw5.4","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:57:41.611008192+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.4","depends_on_id":"hypercerts-jw5.1","type":"blocks","created_at":"2026-02-13T10:58:36.701434689+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.5","title":"Delete organization pages","description":"Delete the entire app/organizations/ directory, which includes: page.tsx (organization list), [orgDid]/page.tsx (organization detail), create/page.tsx (create organization), create/layout.tsx, loading.tsx, and [orgDid]/loading.tsx. This task depends on hypercerts-jw5.3 (session management simplification) because those files use the serverOverride parameter that will be removed.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:46.81007388+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:04:11.172066695+06:00","closed_at":"2026-02-13T11:04:11.172066695+06:00","close_reason":"Completed: 3428fe02f55fe17c9193be3d78e01b24903474a5","dependencies":[{"issue_id":"hypercerts-jw5.5","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:57:46.811920897+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.5","depends_on_id":"hypercerts-jw5.3","type":"blocks","created_at":"2026-02-13T10:58:36.75669208+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.6","title":"Delete organization components","description":"Delete organization-related components: components/organization-form.tsx, components/organization-detail-view.tsx, components/organization-creation-success.tsx, components/collaborators-list-view.tsx, components/add-contributors-form.tsx. Remove any imports of these components from other files. This task can run in parallel with hypercerts-jw5.5 (delete pages) since they are independent.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:52.089516982+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:02:17.815171991+06:00","closed_at":"2026-02-13T11:02:17.815171991+06:00","close_reason":"Completed: 4299fe74ca5d3a6bc7dcd16db4478e21495c7e5c","dependencies":[{"issue_id":"hypercerts-jw5.6","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:57:52.091170987+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.7","title":"Delete organization queries","description":"Delete the entire queries/organizations/ directory, which includes use-check-handle-query.ts and index.ts. Also remove checkHandleAvailability function from lib/api/external/bluesky.ts (lines 42-60) that checks handle availability on SDS server. This task can run in parallel with hypercerts-jw5.5 and hypercerts-jw5.6.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:57:57.491629768+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:02:44.401504275+06:00","closed_at":"2026-02-13T11:02:44.401504275+06:00","close_reason":"Completed: f20bf2f4eb234d67c9e06a9b71b103be938c90f9","dependencies":[{"issue_id":"hypercerts-jw5.7","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:57:57.493747823+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.8","title":"Remove organization operations from create-actions","description":"Remove organization-related functions from lib/create-actions.ts: lines 202 (create org with serverOverride), 214 (add collaborators), 232 (remove collaborators), 246 (list organizations). This task depends on hypercerts-jw5.3 (session management) because these functions use serverOverride parameter.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:58:02.724974343+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:07:24.646163697+06:00","closed_at":"2026-02-13T11:07:24.646163697+06:00","close_reason":"Completed: 8b6439f","dependencies":[{"issue_id":"hypercerts-jw5.8","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:58:02.726703099+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.8","depends_on_id":"hypercerts-jw5.3","type":"blocks","created_at":"2026-02-13T10:58:36.812943121+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-jw5.9","title":"Simplify blob utilities to remove SDS URL handling","description":"Remove SDS URL handling from lib/blob-utils.ts (line 25) - simplify blob URL resolution to only use PDS. Remove SDS URL usage from app/hypercerts/[hypercertUri]/page.tsx (line 86) for image blob resolution. This task depends on hypercerts-jw5.1 (environment config) and hypercerts-jw5.5 (delete org pages) because we need config cleaned up and no org pages referencing blobs.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T10:58:09.274201803+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:07:42.730303409+06:00","closed_at":"2026-02-13T11:07:42.730303409+06:00","close_reason":"Completed: Already cleaned up in previous commits","dependencies":[{"issue_id":"hypercerts-jw5.9","depends_on_id":"hypercerts-jw5","type":"parent-child","created_at":"2026-02-13T10:58:09.276322082+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.9","depends_on_id":"hypercerts-jw5.1","type":"blocks","created_at":"2026-02-13T10:58:36.863175646+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-jw5.9","depends_on_id":"hypercerts-jw5.5","type":"blocks","created_at":"2026-02-13T10:58:36.913677646+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-k0w","title":"Fix: Collections enum in lib/types.ts uses stale collection names not matching SDK","description":"## Problem\n`lib/types.ts` defines a `Collections` enum (lines 47-54) with hardcoded collection name strings that are out of sync with the SDK's `HYPERCERT_COLLECTIONS` constants:\n\n| Enum value | Enum string | SDK constant |\n|---|---|---|\n| `contribution` | `\"org.hypercerts.claim.contribution\"` | `HYPERCERT_COLLECTIONS.CONTRIBUTION_DETAILS = \"org.hypercerts.claim.contributionDetails\"` |\n| `evidence` | `\"org.hypercerts.claim.evidence\"` | No matching SDK constant (renamed to ATTACHMENT) |\n\nThe `Collections` enum is used in `lib/queries.ts` (lines 90, 114) for `createContribution` and `createEvidence` calls. These functions will write records to the wrong collection names, causing them to be invisible to any consumer that uses the correct SDK collection names.\n\n## Fix\nReplace the `Collections` enum with references to `HYPERCERT_COLLECTIONS` from `@hypercerts-org/sdk-core`, or update the hardcoded strings to match the SDK:\n- `contribution` → `\"org.hypercerts.claim.contributionDetails\"`\n- `evidence` → `\"org.hypercerts.claim.attachment\"`\n\nNote: `lib/queries.ts` appears to be legacy code (the new SDK-based approach uses `ctx.scopedRepo.hypercerts.*`). Verify whether these functions are still called anywhere before fixing, and consider removing them if they are dead code.\n\n## Evidence\n- `lib/types.ts` lines 47-54: stale enum values\n- `node_modules/@hypercerts-org/sdk-core/dist/lexicons.d.ts` lines 322, 339: correct SDK values\n- `lib/queries.ts` lines 90, 114: callers using the stale enum","status":"open","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:42.655904675+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:42.655904675+06:00"}
{"id":"hypercerts-kae","title":"Fix: email template logo squished — wrong width/height for horizontal logo","description":"## Files\n- public/email-template.html (modify)\n\n## What to do\nThe email template logo `\u003cimg\u003e` tag (line 44-50) has `width=\"40\" height=\"44\"` which is nearly square. But `{{logo_uri}}` resolves to `/certified.png` which is 530×100px — a wide horizontal logo with a ~5.3:1 aspect ratio. This causes the logo to be squished/distorted.\n\n### Fix the img tag dimensions:\nChange the logo `\u003cimg\u003e` on lines 44-50 from:\n```html\n\u003cimg\n  src=\"{{logo_uri}}\"\n  alt=\"{{app_name}}\"\n  width=\"40\"\n  height=\"44\"\n  style=\"display: block; margin: 0 auto\"\n/\u003e\n```\n\nTo:\n```html\n\u003cimg\n  src=\"{{logo_uri}}\"\n  alt=\"{{app_name}}\"\n  style=\"display: block; margin: 0 auto; max-width: 180px; height: auto;\"\n/\u003e\n```\n\n### Why these values:\n- `max-width: 180px` keeps the logo reasonably sized in the 460px-wide email card (about 40% of card width) — not too big, not too small\n- `height: auto` preserves the natural aspect ratio so the logo is never squished\n- Removed the fixed `width` and `height` HTML attributes because they force a specific aspect ratio\n- For email client compatibility, inline styles are used (email clients strip `\u003cstyle\u003e` blocks)\n\n### Alternative if a square icon is preferred:\nIf the intent was to show a small square icon (not the full wordmark), then the `logo_uri` in `lib/config.ts` (line 287) should be changed to point to a square image like `/certified-logo.svg` or `/favicon.ico` instead of `/certified.png`. But the simpler fix is to just respect the actual image dimensions.\n\n## Don't\n- Change the logo_uri in lib/config.ts (that is a separate concern)\n- Change any other part of the email template layout\n- Add external CSS (email clients don't support it)\n- Use CSS properties that are poorly supported in email clients (stick to inline styles, avoid flexbox/grid)","acceptance_criteria":"1. The logo img tag no longer has fixed width=\"40\" height=\"44\" attributes that distort the image\n2. The logo img tag uses max-width and height:auto to preserve aspect ratio\n3. The logo displays at a reasonable size (roughly 140-200px wide) centered in the email card\n4. The rest of the email template is unchanged\n5. The HTML is valid","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-25T16:16:22.336361262+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T16:17:10.670181611+06:00","closed_at":"2026-02-25T16:17:10.670181611+06:00","close_reason":"0fc0d33 fix: email template logo aspect ratio with max-width and height:auto","labels":["scope:trivial"]}
{"id":"hypercerts-khg","title":"Fix: revert login UI from Dialog back to Popover (pre-doors layout)","description":"## Files\n- components/navbar.tsx (modify)\n\n## What to do\nThe login UI was changed from a Popover (anchored to the Sign In button, w-80, right-aligned) to a Dialog (centered modal, full-width on mobile). The user wants the original Popover layout back.\n\nRevert navbar.tsx to use Popover instead of Dialog for the sign-in flow:\n\n1. Replace Dialog/DialogContent/DialogTrigger/DialogTitle imports with Popover/PopoverContent/PopoverTrigger\n2. Add back the \\`open\\`/\\`setOpen\\` state that the Popover needs\n3. Use \\`\u003cPopoverContent align=\\\"end\\\" className=\\\"w-80 glass-panel border-border/60 p-4\\\"\u003e\\` — this gives the compact, right-aligned dropdown\n4. Keep \\`\u003cLoginDialog /\u003e\\` inside the PopoverContent — the pill toggle component works fine in a Popover\n5. Remove the sr-only DialogTitle (not needed for Popover)\n\nReference: the pre-doors navbar at commit 85505ad used Popover with w-80 and align=\\\"end\\\". The current LoginDialog component (pill toggle with HandleForm/EmailForm) should be kept as-is — only the container changes from Dialog to Popover.\n\n## Don't\n- Don't modify login-dialog.tsx — only navbar.tsx changes\n- Don't change any other part of the navbar (logo, nav links, user dropdown)\n- Don't add any new dependencies\n- Don't change the LoginDialog component's internal layout","acceptance_criteria":"1. Sign In button opens a Popover anchored to the button (right-aligned), NOT a centered Dialog modal\n2. Popover is approximately w-80 (320px) wide, not full-screen\n3. Pill toggle (Handle | Email) renders correctly inside the Popover\n4. Handle login flow works (enter handle, click Sign In)\n5. Email login flow works (switch to Email tab, enter email, click Continue)\n6. Popover closes when clicking outside\n7. No Dialog/DialogContent/DialogTrigger/DialogTitle imports remain in navbar.tsx\n8. No TypeScript errors: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","estimated_minutes":30,"created_at":"2026-02-24T17:25:11.383273869+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:49:44.943507259+06:00","closed_at":"2026-02-24T17:49:44.943507259+06:00","close_reason":"Wrong diagnosis — the issue is in LoginDialog's root div missing max-w-sm, not the navbar Dialog vs Popover.","labels":["scope:small"]}
{"id":"hypercerts-kjo","title":"Fix: LoginDialog stretches full-width — restore max-w-sm constraint","description":"## Files\n- components/login-dialog.tsx (modify)\n\n## What to do\nThe root div of LoginDialog (line 158) has className=\\\"w-full space-y-5\\\" but is missing max-w-sm. This causes it to stretch full-width when rendered directly in the page by SignedInProvider (providers/SignedInProvider.tsx line 48-50), which wraps it in a flex-grow centered container.\n\nBefore the pill-toggle rewrite (commit 85505ad), the root element was:\n  \u003cform className=\\\"grid w-full max-w-sm gap-6 py-10\\\"\u003e\n\nThe current root element is:\n  \u003cdiv className=\\\"w-full space-y-5\\\"\u003e\n\nFix: change line 158 from:\n  \u003cdiv className=\\\"w-full space-y-5\\\"\u003e\nto:\n  \u003cdiv className=\\\"w-full max-w-sm space-y-5\\\"\u003e\n\nThis constrains the LoginDialog to 384px max width, matching the pre-rewrite layout. It still works inside the navbar Dialog (which is sm:max-w-sm anyway) because max-w-sm just becomes redundant there.\n\n## Don't\n- Don't change anything else in login-dialog.tsx\n- Don't modify navbar.tsx or SignedInProvider.tsx","acceptance_criteria":"1. On the main page (not signed in), LoginDialog renders at max 384px wide (max-w-sm), centered — NOT full viewport width\n2. In the navbar Dialog, LoginDialog still renders correctly (max-w-sm is redundant but harmless)\n3. Pill toggle and both forms (Handle/Email) still work\n4. No TypeScript errors","status":"closed","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","estimated_minutes":10,"created_at":"2026-02-24T17:49:56.942251627+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:51:35.710608503+06:00","closed_at":"2026-02-24T17:51:35.710608503+06:00","close_reason":"Added max-w-sm to LoginDialog root div","labels":["scope:trivial"]}
{"id":"hypercerts-knj","title":"Fix: eslint-config-next version (16.0.1) does not match next version (16.0.7) in package.json","description":"Review of package.json found a minor version mismatch between next and eslint-config-next.\n\nEvidence:\n- package.json line 48: 'next': '16.0.7'\n- package.json line 66: 'eslint-config-next': '16.0.1'\n\nThe eslint-config-next package should match the next version to ensure lint rules are consistent with the Next.js version in use. A 6-patch-version gap (16.0.1 vs 16.0.7) is unlikely to cause issues but is a maintenance smell — if Next.js added new lint rules in 16.0.2-16.0.7, they won't be enforced.\n\nSuggested fix:\nUpdate eslint-config-next to match next:\n  'eslint-config-next': '16.0.7'\n\nOr use a caret range that tracks next:\n  'eslint-config-next': '^16.0.7'","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:48:34.510860306+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:48:34.510860306+06:00"}
{"id":"hypercerts-kpe","title":"Fix: .vscode/settings.json commits personal color theme preference — should be gitignored","description":"Review of .vscode/settings.json found it only contains a personal color theme preference:\n  { \"workbench.colorTheme\": \"Kimbie Dark\" }\n\nThis is a personal developer preference, not a project-wide setting. Committing it forces all contributors to use 'Kimbie Dark' theme (or see a diff every time they change their theme).\n\nEvidence:\n- .vscode/settings.json line 2: 'workbench.colorTheme': 'Kimbie Dark'\n- This is the only setting in the file — no formatter, no editor config, no extension recommendations\n\nSuggested fix:\nOption A (preferred): Remove workbench.colorTheme from .vscode/settings.json entirely. Add useful project settings instead:\n  {\n    \"editor.formatOnSave\": true,\n    \"editor.defaultFormatter\": \"esbenp.prettier-vscode\",\n    \"editor.codeActionsOnSave\": { \"source.fixAll.eslint\": \"explicit\" },\n    \"typescript.tsdk\": \"node_modules/typescript/lib\"\n  }\n\nOption B: Add .vscode/settings.json to .gitignore and use .vscode/settings.json.example for project settings.\n\nThe current state provides no DX value and actively harms contributors who use a different theme.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:03.771520922+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:03.771520922+06:00"}
{"id":"hypercerts-l0q","title":"Change logout endpoint from GET to POST","description":"## Files\n- app/api/oauth/logout/route.ts (modify)\n- lib/api/auth.ts (modify)\n\n## What to do\n\n### 1. app/api/oauth/logout/route.ts\nRename the exported function from `GET` to `POST`. No other changes to this file.\n\nCurrent (line 5):\n```ts\nexport async function GET() {\n```\n\nChange to:\n```ts\nexport async function POST() {\n```\n\n### 2. lib/api/auth.ts\nChange the method from GET to POST in the logout function.\n\nCurrent (lines 21-24):\n```ts\nexport async function logout(): Promise\u003cvoid\u003e {\n  await apiClient\u003cvoid\u003e(\"/api/oauth/logout\", {\n    method: \"GET\",\n  });\n}\n```\n\nChange to:\n```ts\nexport async function logout(): Promise\u003cvoid\u003e {\n  await apiClient\u003cvoid\u003e(\"/api/oauth/logout\", {\n    method: \"POST\",\n  });\n}\n```\n\nThat's it. Two lines changed across two files. The rest of the chain (useLogoutMutation -\u003e navbar button click) already works because it calls the `logout()` function from lib/api/auth.ts which handles the HTTP method.\n\n## Don't\n- Change the response format or status codes in the route handler\n- Modify the error handling logic\n- Touch the navbar or useLogoutMutation — they don't need changes\n- Add CSRF tokens or any other complexity — POST is sufficient protection against cross-origin img/script tag attacks","acceptance_criteria":"1. GET /api/oauth/logout returns 405 Method Not Allowed (Next.js default for missing GET handler)\n2. POST /api/oauth/logout returns 200 with { message: \"Signed out successfully\" } when authenticated\n3. POST /api/oauth/logout returns 200 with { message: \"Signed out successfully\" } when not authenticated (session is null, no-op)\n4. lib/api/auth.ts logout() uses method: \"POST\"\n5. Only 2 lines changed total (one per file)\n6. Files pass eslint and prettier","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-27T13:15:48.653712303+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-27T13:16:37.833347277+06:00","closed_at":"2026-02-27T13:16:37.833347277+06:00","close_reason":"c20e556 fix: change logout endpoint from GET to POST to prevent CSRF","labels":["scope:trivial"]}
{"id":"hypercerts-l4v","title":"Fix: Contributors label in contributions-form.tsx has no htmlFor — Label not associated with any input","description":"## File\\ncomponents/contributions-form.tsx line 184\\n\\n## Severity\\nMEDIUM — Accessibility\\n\\n## Problem\\nThe Contributors Label element has no htmlFor attribute, so it is not programmatically associated with any input. Screen readers cannot identify which control this label describes. The same issue exists for the Evaluators label in evaluation-form.tsx (line 231) and the Measurers label in measurement-form.tsx (line 325).\\n\\n## Evidence\\n```tsx\\n// contributions-form.tsx line 184\\n\u003cLabel className=\"font-[family-name:var(--font-outfit)] text-sm font-medium\"\u003e\\n  Contributors *\\n\u003c/Label\u003e\\n\\n// evaluation-form.tsx line 231\\n\u003cLabel className=\"text-muted-foreground ...\"\u003e\\n  Evaluators *\\n\u003c/Label\u003e\\n\\n// measurement-form.tsx line 325\\n\u003cLabel className=\"text-muted-foreground ...\"\u003e\\n  Measurers\\n\u003c/Label\u003e\\n```\\n\\n## Fix\\nSince these labels describe a Tabs component (not a single input), use aria-labelledby on the TabsList or wrap in a fieldset with legend:\\n```tsx\\n\u003cfieldset\u003e\\n  \u003clegend className=\"font-[family-name:var(--font-outfit)] text-sm font-medium\"\u003e\\n    Contributors *\\n  \u003c/legend\u003e\\n  \u003cTabs defaultValue=\"search\" className=\"w-full\"\u003e\\n    ...\\n  \u003c/Tabs\u003e\\n\u003c/fieldset\u003e\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:28.183819408+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:28.183819408+06:00"}
{"id":"hypercerts-la0","title":"Fix: AuthErrorToast missing Suspense boundary (from hypercerts-jsu.1)","description":"Review of hypercerts-jsu.1 found: AuthErrorToast uses useSearchParams() without a wrapping \u003cSuspense\u003e boundary. Next.js App Router requires Suspense around any component using useSearchParams() to avoid opting the entire page out of static rendering. See: https://nextjs.org/docs/app/api-reference/functions/use-search-params#static-rendering. Evidence: providers/AuthErrorToast.tsx uses useSearchParams() at line 10; providers/SignedInProvider.tsx renders \u003cAuthErrorToast /\u003e at line 37 with no Suspense wrapper. Fix: wrap \u003cAuthErrorToast /\u003e in \u003cSuspense fallback={null}\u003e in SignedInProvider.tsx. All routes are currently dynamic so this does not cause a runtime failure today, but it is a correctness violation that will break if any route becomes static.","acceptance_criteria":"1. AuthErrorToast is wrapped in \u003cSuspense fallback={null}\u003e in SignedInProvider.tsx\n2. No other changes to AuthErrorToast.tsx or the callback route\n3. TypeScript compiles: npx tsc --noEmit passes\n4. The toast still works correctly (auth_error in URL → toast shown → URL cleaned)","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoeps","estimated_minutes":10,"created_at":"2026-02-17T18:26:35.114159758+06:00","created_by":"kzoeps","updated_at":"2026-02-17T18:29:38.595674259+06:00","closed_at":"2026-02-17T18:29:38.595674259+06:00","close_reason":"cdb3096 fix: wrap AuthErrorToast in Suspense boundary","dependencies":[{"issue_id":"hypercerts-la0","depends_on_id":"hypercerts-jsu.1","type":"discovered-from","created_at":"2026-02-17T18:26:38.850583224+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-lhr","title":"Fix: getContentTypeColor is a dead function — always returns same value (hypercert-evidence-view.tsx)","description":"## File\\ncomponents/hypercert-evidence-view.tsx lines 27-30\\n\\n## Severity\\nLOW — Code quality / dead code\\n\\n## Problem\\ngetContentTypeColor() is defined as a function that always returns the same hardcoded string regardless of contentType. The comment says 'All content types use the same color' but the function signature implies it was intended to be content-type-aware. This is dead code that adds noise.\\n\\n## Evidence\\n```tsx\\nconst getContentTypeColor = () =\u003e {\\n  // All content types use the same color\\n  return 'bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-100';\\n};\\n```\\n\\n## Fix\\nEither:\\n1. Remove the function and inline the class string directly on the Badge:\\n```tsx\\n\u003cBadge className=\"bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-100 shrink-0 font-[family-name:var(--font-outfit)]\"\u003e\\n```\\n2. Or implement actual per-type colors if that was the intent.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:45.667014068+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:45.667014068+06:00"}
{"id":"hypercerts-lr4","title":"Fix: email login_hint parameter not validated — arbitrary value forwarded to ePDS auth server (from security review)","description":"## File\napp/api/oauth/epds/login/route.ts, lines 20, 81-83\n\n## Problem\nThe `email` query parameter is read from the request and forwarded directly to the ePDS authorization server as `login_hint` without any validation:\n```ts\nconst email = req.nextUrl.searchParams.get('email');\n// ... no validation ...\nif (email) {\n  authUrl.searchParams.set('login_hint', email);\n}\n```\n\nIssues:\n1. **No length limit**: An arbitrarily long email value is forwarded, potentially causing oversized URLs or log pollution.\n2. **No format validation**: Non-email values (e.g. script tags, SQL injection attempts, very long strings) are forwarded to the ePDS server. While the ePDS server should validate this, the app should not blindly forward unvalidated user input to third-party servers.\n3. **No encoding concern**: `URLSearchParams.set()` handles encoding correctly, so injection into the URL itself is not a risk — but the value is still forwarded verbatim to the ePDS server.\n\n## Fix\nAdd basic validation before forwarding:\n```ts\nconst rawEmail = req.nextUrl.searchParams.get('email');\n// Basic email format check + length limit\nconst email = rawEmail \u0026\u0026 /^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(rawEmail) \u0026\u0026 rawEmail.length \u003c= 254\n  ? rawEmail\n  : null;\n```\n\nThis follows RFC 5321 (max email length 254 chars) and rejects obviously malformed values before they reach the ePDS server.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:31.704790728+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:31.704790728+06:00","dependencies":[{"issue_id":"hypercerts-lr4","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.888135401+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-lv8","title":"Fix: secure cookie flag uses NODE_ENV, not HTTPS detection — breaks Vercel preview (from hypercerts-dhz)","description":"## Review finding from hypercerts-dhz (ePDS OAuth integration)\n\n**Files:**\n- app/api/oauth/login/route.ts, line 90: `secure: config.isProduction`\n- app/api/oauth/callback/route.ts, lines 142, (also app/api/auth/callback/route.ts line 14)\n- lib/config.ts, line 172: `const isProduction = process.env.NODE_ENV === 'production'`\n\n**Problem:**\n`config.isProduction` is `process.env.NODE_ENV === 'production'`. On Vercel preview deployments, `NODE_ENV` is typically `'production'` so this usually works. However, the check is semantically wrong — the `Secure` cookie flag should be set based on whether the connection is HTTPS, not on `NODE_ENV`.\n\nMore concretely: if someone runs the app with `NODE_ENV=development` on an HTTPS server (e.g., a staging environment with a custom domain), the `Secure` flag will be absent, making the cookies transmittable over HTTP. Conversely, the loopback check in `config.ts` already handles the HTTP/HTTPS distinction correctly for the base URL — the cookie flag should use the same logic.\n\n**Fix:**\nUse `!config.isLoopback` instead of `config.isProduction` for the `secure` flag:\n```ts\nsecure: !config.isLoopback,\n```\n\nThis correctly sets `Secure` on any non-loopback deployment (which must be HTTPS per the existing `validateBaseUrl` check) and omits it on loopback (which uses HTTP).\n\nThis affects all three cookie-setting locations:\n1. `app/api/oauth/login/route.ts` — epds-oauth-session cookie\n2. `app/api/oauth/callback/route.ts` — user-did cookie\n3. `app/api/auth/callback/route.ts` — user-did cookie (existing flow, same issue)","status":"open","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:29:11.409478979+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:29:11.409478979+06:00","dependencies":[{"issue_id":"hypercerts-lv8","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:29:16.792540541+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mbf","title":"Fix: Manual contributor/DID inputs in forms have no accessible label (contributions-form.tsx, evaluation-form.tsx, measurement-form.tsx)","description":"## Files\\n- components/contributions-form.tsx lines 218-229\\n- components/evaluation-form.tsx lines 268-287\\n- components/measurement-form.tsx lines 365-384\\n\\n## Severity\\nMEDIUM — Accessibility\\n\\n## Problem\\nThe dynamic list of manual contributor/DID input fields have no accessible label. Each Input is rendered inside a div with no associated label element and no aria-label. Screen readers cannot identify what these inputs are for.\\n\\n## Evidence\\n```tsx\\n// contributions-form.tsx lines 218-229\\n{manualContributors.map((uri, index) =\u003e (\\n  \u003cdiv key={index} className=\"flex items-center gap-2\"\u003e\\n    \u003cInput\\n      type=\"text\"\\n      placeholder=\"at://did:plc:..., https://..., did:eth:...\"\\n      value={uri}\\n      onChange={(e) =\u003e updateManualContributor(index, e.target.value)}\\n      // No aria-label, no associated label element\\n    /\u003e\\n    \u003cButton ...\u003e\u003cTrash /\u003e\u003c/Button\u003e\\n  \u003c/div\u003e\\n))}\\n```\\n\\n## Fix\\nAdd aria-label to each dynamic input:\\n```tsx\\n\u003cInput\\n  type=\"text\"\\n  aria-label={}\\n  placeholder=\"at://did:plc:..., https://..., did:eth:...\"\\n  value={uri}\\n  onChange={(e) =\u003e updateManualContributor(index, e.target.value)}\\n/\u003e\\n```\\n\\nApply the same pattern to evaluation-form.tsx (DID inputs) and measurement-form.tsx (DID inputs and evidence URI inputs).","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:19.669279003+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:19.669279003+06:00"}
{"id":"hypercerts-mdo7","title":"Epic: Wire up contributor addition to activity claims","description":"Contributors are not being added to activity claims due to three compounding issues:\n\n1. **PRIMARY BUG**: In `app/api/certs/route.ts` POST handler (line 69-73), contributions data IS sent from the client form but the server has a TODO comment and silently discards it. The parsed result is never assigned or used.\n\n2. **Disabled creation wizard step**: In `app/hypercerts/create/page.tsx`, the `HypercertContributionForm` component is commented out with `{/* TODO commented out for now while SDK addContribution stabilized */}`.\n\n3. **No detail-view path**: In `components/add-record-form.tsx`, the contribution case shows 'Contribution form coming soon' placeholder instead of the actual form.\n\n**What success looks like:**\n- Contributors added during hypercert creation are persisted to the activity claim record\n- Contributors can be added to existing hypercerts from the detail view\n- The creation wizard includes a Contributors step\n- The display logic (already working) shows the contributors correctly\n\n**Key constraints:**\n- The `addContribution` server action in `lib/create-actions.ts` already works correctly — reuse it\n- The `contributions-form.tsx` component already exists and works — reuse it\n- The display components (`hypercert-contributors-section.tsx`, `hypercert-contributor-view.tsx`) already work\n- Must validate records with `assertValidRecord()` before every write\n- Must follow existing ATProto patterns (StrongRef for cross-record references)","status":"open","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-05T11:19:14.357375852+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-05T11:19:14.357375852+06:00","labels":["scope:medium"]}
{"id":"hypercerts-mdo7.1","title":"Process contributions in POST /api/certs after hypercert creation","description":"## Files\n- app/api/certs/route.ts (modify)\n\n## What to do\nIn the POST handler, after the claim record is successfully created (after line 180 where `claimResult` is returned), process the contributions data that is currently being silently discarded.\n\n1. Parse the `contributionsRaw` FormData field into a typed array. The client sends it as `JSON.stringify(params.contributions)` where each contribution has shape: `{ contributors: string[], role: string, contributionDescription?: string, startDate?: string, endDate?: string }`.\n\n2. If contributions are present and non-empty, call `addContribution()` from `lib/create-actions.ts` for each contribution entry, passing:\n   - `hypercertUri`: the `claimResult.data.uri` just created\n   - `contributors`: the array of DID strings from the contribution\n   - `contributionDetails`: `{ role, contributionDescription, startDate, endDate }`\n\n3. **IMPORTANT**: `addContribution` is a `\"use server\"` action. You CANNOT call it directly from an API route. Instead, extract the core logic into a shared helper function. Create a new exported function `processContributions(ctx, hypercertUri, contributions)` in `lib/create-actions.ts` that contains the logic from `addContribution` lines 108-218 but takes a `RepoContext` instead of calling `getRepoContext()` internally. Then call this from both the API route and the existing `addContribution` action.\n\n   Alternatively, since the API route already has `ctx` (the repo context), you can inline the contribution processing logic directly in the route, following the same pattern as `addContribution`: create `contributorInformation` records, create `contribution` record, build contributor entries with StrongRefs, then update the claim via `putRecord`.\n\n4. Contribution processing should be best-effort — if it fails, still return the created hypercert (log the error). The hypercert creation itself should not fail because of a contribution error.\n\n5. Remove the TODO comment on line 69.\n\n## Dont\n- Do not change the FormData parsing or client-side code\n- Do not change the claim record creation logic (lines 140-180)\n- Do not make contribution processing block the response — if contributions fail, still return the hypercert data\n- Do not remove the `parseOptionalJson` validation call (it catches malformed JSON early)","acceptance_criteria":"1. When creating a hypercert with contributions via POST /api/certs, the contributions are persisted as child records (contributorInformation + contribution) and the activity claim record is updated with a contributors array\n2. When creating a hypercert WITHOUT contributions, behavior is unchanged\n3. If contribution processing fails, the hypercert is still returned successfully (no regression)\n4. The TODO comment on line 69 is removed\n5. pnpm run build passes","status":"open","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":60,"created_at":"2026-03-05T11:19:36.48163324+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-05T11:30:06.98973051+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mdo7.1","depends_on_id":"hypercerts-mdo7","type":"parent-child","created_at":"2026-03-05T11:19:36.483391711+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mdo7.2","title":"Re-enable Contributors step in the create wizard","description":"## Files\n- app/hypercerts/create/page.tsx (modify)\n- components/edit-cert-stepper.tsx (modify)\n- components/form-skeletons.tsx (modify — may need to add ContributionFormSkeleton)\n\n## What to do\nRe-enable the Contributors step in the hypercert creation wizard. It was commented out with `{/* TODO commented out for now while SDK addContribution stabilized */}`.\n\n### In `app/hypercerts/create/page.tsx`:\n1. Uncomment the dynamic import for `HypercertContributionForm` (lines 23-26). The skeleton `ContributionFormSkeleton` may not exist yet — check `components/form-skeletons.tsx`. If it does not exist, create a simple skeleton matching the pattern of other form skeletons (e.g., `MeasurementFormSkeleton`).\n2. Uncomment the contribution step rendering (lines 88-95).\n3. Shift all subsequent steps by +1:\n   - Evidence becomes step 3 (was step 2)\n   - Location becomes step 4 (was step 3)\n   - Measurement becomes step 5 (was step 4)\n   - Evaluation becomes step 6 (was step 5)\n   - Completion becomes step 7 (was step 6)\n\n### In `components/edit-cert-stepper.tsx`:\n1. Add a \"Contributors\" step to the `STEPS` array at position 2 (between Details and Evidence). Use the `Users` icon from lucide-react (already used in `contributions-form.tsx`).\n2. The STEPS array should become:\n   ```\n   { id: 1, label: \"Details\", icon: FileText },\n   { id: 2, label: \"Contributors\", icon: Users },\n   { id: 3, label: \"Evidence\", icon: Paperclip },\n   { id: 4, label: \"Location\", icon: MapPin },\n   { id: 5, label: \"Measurement\", icon: BarChart3 },\n   { id: 6, label: \"Evaluation\", icon: ClipboardCheck },\n   ```\n\n### In `components/contributions-form.tsx`:\n1. Update the `stepLabel` prop on line 139 from `\"Step 2 of 5\"` to `\"Step 2 of 6\"` (or remove the hardcoded step label if it is not needed — check how other forms handle this).\n\n## Dont\n- Do not modify the `HypercertContributionForm` component logic itself\n- Do not change the form submission behavior\n- Do not change the step numbering logic (it uses simple integer state)","acceptance_criteria":"1. The creation wizard shows 6 steps in the stepper sidebar: Details → Contributors → Evidence → Location → Measurement → Evaluation\n2. After creating a hypercert (step 1), the user sees the Contributors form (step 2)\n3. The Contributors form has Skip and Save \u0026 Next buttons that advance to step 3 (Evidence)\n4. All subsequent steps (Evidence through Completion) work correctly with the shifted numbering\n5. pnpm run build passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-05T11:19:53.202351143+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-05T11:28:21.087973697+06:00","closed_at":"2026-03-05T11:28:16.86717399+06:00","labels":["cancelled","scope:small"],"dependencies":[{"issue_id":"hypercerts-mdo7.2","depends_on_id":"hypercerts-mdo7","type":"parent-child","created_at":"2026-03-05T11:19:53.204206756+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mdo7.2","depends_on_id":"hypercerts-mdo7.1","type":"blocks","created_at":"2026-03-05T11:20:23.578887267+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mdo7.3","title":"Wire up contribution form in the detail-view add-record page","description":"## Files\n- components/add-record-form.tsx (modify)\n\n## What to do\nReplace the \"Contribution form coming soon\" placeholder with the actual `HypercertContributionForm` component.\n\n1. Add a dynamic import for `HypercertContributionForm` from `@/components/contributions-form`, following the same pattern as the other form imports (lines 16-27). Use a skeleton loader — either create a `ContributionFormSkeleton` in `form-skeletons.tsx` or reuse `MeasurementFormSkeleton` as a reasonable approximation.\n\n2. In the `renderForm()` switch statement, replace the `case \"contribution\"` block (lines 90-95) with:\n   ```tsx\n   case \"contribution\":\n     return (\n       \u003cContributionsForm\n         hypercertInfo={hypercertInfo}\n         onNext={goBack}\n         onBack={goBack}\n       /\u003e\n     );\n   ```\n\n3. The `ContributionsForm` component expects `BaseHypercertFormProps \u0026 { onBack?: () =\u003e void; onNext?: () =\u003e void }`. The `hypercertInfo` is already constructed on line 54-57 as a `CreateHypercertResult` which satisfies `BaseHypercertFormProps`. The `onNext` and `onBack` callbacks should both call `goBack` (navigate back to the hypercert detail page), matching the pattern used by measurement/evaluation/evidence forms.\n\n## Dont\n- Do not modify the ContributionsForm component itself\n- Do not change the routing or URL structure\n- Do not remove the comment on line 28 about ContributionsForm being disabled (update it to say it is now enabled)","acceptance_criteria":"1. Navigating to /hypercerts/{uri}/add/contribution shows the actual contribution form (not the placeholder)\n2. The form renders with role input, contributor search/manual entry, description, and date fields\n3. Submitting the form calls addContribution and navigates back to the hypercert detail page\n4. pnpm run build passes","status":"open","priority":1,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-05T11:20:06.700787111+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-05T11:20:06.700787111+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mdo7.3","depends_on_id":"hypercerts-mdo7","type":"parent-child","created_at":"2026-03-05T11:20:06.702854552+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mdo7.4","title":"Add 'Add Contributor' button to the hypercert detail view contributors section","description":"## Files\n- components/hypercert-contributors-section.tsx (modify)\n\n## What to do\nAdd an \"Add Contributor\" button to the contributors section on the hypercert detail page, so owners can add contributors to existing hypercerts.\n\n1. The component already receives `hypercertUri` and `isOwner` props (lines 18-19) but does not use them. There is a comment on line 126: `{/* Add Contributor button is intentionally hidden until the contribution form is implemented */}`.\n\n2. Replace that comment with an actual button that:\n   - Only renders when `isOwner` is `true`\n   - Links to `/hypercerts/${encodeURIComponent(hypercertUri)}/add/contribution`\n   - Uses the existing UI patterns: a `Button` component with `asChild` wrapping a `Link`\n   - Has a `PlusCircle` icon and text \"Add Contributor\"\n   - Uses the same styling patterns as other add buttons in the detail view (check `hypercert-detail-view.tsx` for reference)\n\n3. Import `Link` from `next/link`, `Button` from `@/components/ui/button`, and `PlusCircle` from `lucide-react`.\n\n## Dont\n- Do not change the contributor display logic\n- Do not change the props interface (hypercertUri and isOwner are already there)\n- Do not add the button if isOwner is false or undefined","acceptance_criteria":"1. When viewing a hypercert you own, the Contributors section shows an \"Add Contributor\" button\n2. When viewing a hypercert you do NOT own, no button is shown\n3. Clicking the button navigates to /hypercerts/{uri}/add/contribution\n4. pnpm run build passes","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-05T11:20:17.890675755+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-05T11:20:17.890675755+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mdo7.4","depends_on_id":"hypercerts-mdo7","type":"parent-child","created_at":"2026-03-05T11:20:17.892845934+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mdo7.4","depends_on_id":"hypercerts-mdo7.3","type":"blocks","created_at":"2026-03-05T11:20:24.163646842+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mfr","title":"Fix: add-attachment route starts getRepoContext() before auth check — wasted work on unauthenticated requests","description":"## Problem\nIn `app/api/certs/add-attachment/route.ts` line 7, `getRepoContext()` is called immediately (before any validation):\n\n```ts\nconst data = await req.formData();\nconst ctxPromise = getRepoContext();  // ← starts here\n// ... 60+ lines of validation ...\nconst ctx = await ctxPromise;         // ← awaited here\n```\n\nThis is the same pattern used in `app/api/certs/create/route.ts` (line 8) and `app/api/profile/bsky/update/route.ts` (line 8). The intent is to parallelize auth with form parsing, which is a valid optimization.\n\nHowever, `getRepoContext()` reads cookies and restores a Redis session. If the request fails validation (e.g. missing `hypercertUri`, invalid `evidenceMode`), the session restore work is wasted. More importantly, if the session restore fails with an unhandled error, it could mask the real validation error.\n\nThe pattern is inconsistent: `app/api/certs/add-location/route.ts` uses the same optimization (line 8: `const repoPromise = getAuthenticatedRepo()`) but `app/api/profile/update/route.ts` also uses it (line 8).\n\nThis is LOW severity but worth noting for consistency and to ensure the Promise rejection from `getRepoContext()` is always handled (it is — the outer try/catch covers it).\n\n## Recommendation\nDocument the intentional parallelization pattern with a comment, or move auth to before validation if the overhead is acceptable. The current approach is safe but could confuse future maintainers.\n\n## Evidence\n- `app/api/certs/add-attachment/route.ts` lines 7, 64\n- `app/api/certs/create/route.ts` lines 8, 45\n- `app/api/profile/bsky/update/route.ts` lines 8, 10","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:44.389886933+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:44.389886933+06:00"}
{"id":"hypercerts-mo7","title":"Epic: Fix loopback OAuth — scope, client ID, and PAR request corrections","description":"## Context\nThe ePDS OAuth login flow is broken in loopback (local dev) mode. Three changes were made to fix it:\n1. Added `application_type: native` to the PAR request body\n2. Created a separate `epdsClientId` using the ePDS redirect URI\n3. Fixed `getEpdsClientId()` to return `config.epdsClientId` instead of `config.clientId`\n\n## Problem\nThese changes partially fix the issue but leave several bugs and loose ends:\n- `OAUTH_SCOPE` is hardcoded to `GRANULAR_SCOPE` — the `getOAuthScope()` function that returns `LOOPBACK_SCOPE` for loopback is dead code. ATProto spec requires loopback clients to use `atproto transition:generic`, not granular scopes.\n- `application_type: native` is hardcoded in the PAR body unconditionally — should only be sent for loopback, not production.\n- A debug `console.log(redirectUri)` was left in the login route.\n- The ePDS state in Redis is never cleaned up after a successful callback (replay risk).\n- `buildClientMetadata()` still uses `config.clientId` (regular auth) not `config.epdsClientId` — loopback metadata has wrong redirect_uri in client_id.\n- The loopback client_id embeds granular scopes instead of loopback scopes because `OAUTH_SCOPE` is always granular.\n\n## Success Criteria\n- Loopback mode uses `LOOPBACK_SCOPE` everywhere: client ID, PAR request, client metadata\n- Production mode uses `GRANULAR_SCOPE` everywhere\n- `application_type` is `native` for loopback, `web` for production in PAR requests\n- No debug logs left in code\n- ePDS state cleaned up after successful callback\n- All changes are scoped to loopback behavior only — production flow unchanged","notes":"Superseded — re-scoped after clarification that granular scopes work for loopback","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-26T15:42:27.418049087+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-26T15:48:04.172906736+06:00","closed_at":"2026-02-26T15:48:04.172915382+06:00","labels":["scope:medium"]}
{"id":"hypercerts-mrb","title":"Epic: Display contributors on hypercert detail page","description":"## Why\nThe hypercert detail page currently shows measurements, evidence, and evaluations but completely omits contributor information. The upstream hypercerts-lexicon (https://github.com/hypercerts-org/hypercerts-lexicon) has evolved the activity schema: contributors are now inline on the activity record as an array of contributor objects (each with contributorIdentity, contributionWeight, contributionDetails). The local scaffold's generated lexicon types still use the old schema (contributions as StrongRef[] to separate records). We need to:\n\n1. Update the local lexicon types to match the upstream activity.json schema\n2. Build the full contributor display pipeline on the detail page\n3. Support both inline contributor identities (DID strings) and StrongRef-based contributorInformation records\n4. Resolve contributor DIDs to profile info (display name, avatar) via Bluesky public API\n\n## What success looks like\n- The hypercert detail page shows a 'Contributors' section between the metadata grid and the description\n- Each contributor displays: identity (resolved to profile name/avatar when possible), role, and weight\n- Contributors with DID identities link to their Bluesky profile\n- The section gracefully handles: no contributors, contributors with only DID strings, contributors with StrongRef to contributorInformation records\n- Loading and error states match the existing section patterns (measurements, evidence, evaluations)\n\n## Key constraints\n- Follow the exact same architectural patterns as measurements/evidence/evaluations sections\n- Use the upstream lexicon schema from https://github.com/hypercerts-org/hypercerts-lexicon/blob/develop/lexicons/org/hypercerts/claim/activity.json as the source of truth\n- Contributors are inline on the activity record (NOT fetched via Constellation backlinks like measurements/evidence)\n- Must handle both contributorIdentity string format and StrongRef format\n- Build and lint must pass after all tasks","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-23T18:00:48.70775239+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:35:40.629531872+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-mrb.1","title":"Update activity lexicon types to match upstream contributor schema","description":"## Files\n- lexicons/types/org/hypercerts/claim/activity.ts (modify)\n\n## What to do\nThe upstream lexicon at https://github.com/hypercerts-org/hypercerts-lexicon/blob/develop/lexicons/org/hypercerts/claim/activity.json defines contributors as inline objects on the activity record. The local generated type still uses the old schema (`contributions?: ComAtprotoRepoStrongRef.Main[]`). Update the `Main` interface to match the upstream schema.\n\n### Current (old) schema in activity.ts:\n```typescript\ncontributions?: ComAtprotoRepoStrongRef.Main[]\n```\n\n### New schema to add (from upstream activity.json):\nThe `contributors` field is an array of `Contributor` objects. Add these interfaces to the file:\n\n```typescript\n/** Contributor entry on an activity record */\nexport interface Contributor {\n  /** Identity as inline string or StrongRef to contributorInformation record */\n  contributorIdentity: ContributorIdentity | ComAtprotoRepoStrongRef.Main | { $type: string }\n  /** Relative weight (string to avoid float precision) */\n  contributionWeight?: string\n  /** Role as inline string or StrongRef to contributionDetails record */\n  contributionDetails?: ContributorRole | ComAtprotoRepoStrongRef.Main | { $type: string }\n  [k: string]: unknown\n}\n\nexport interface ContributorIdentity {\n  $type?: 'org.hypercerts.claim.activity#contributorIdentity'\n  /** DID or identifier string */\n  identity: string\n  [k: string]: unknown\n}\n\nexport interface ContributorRole {\n  $type?: 'org.hypercerts.claim.activity#contributorRole'\n  /** Role or details string */\n  role: string\n  [k: string]: unknown\n}\n\nexport interface WorkScopeString {\n  $type?: 'org.hypercerts.claim.activity#workScopeString'\n  scope: string\n  [k: string]: unknown\n}\n```\n\nOn the `Main` interface:\n1. KEEP the existing `contributions?: ComAtprotoRepoStrongRef.Main[]` field for backward compat\n2. ADD `contributors?: Contributor[]` field\n3. RENAME `workTimeFrameFrom` → keep as-is but also add `startDate?: string` and `endDate?: string` aliases (the upstream uses startDate/endDate)\n4. ADD `locations?: ComAtprotoRepoStrongRef.Main[]` (upstream uses plural `locations` array, local has singular `location`)\n5. Update `workScope` to be a union: `workScope: string | ComAtprotoRepoStrongRef.Main | WorkScopeString`\n\nAlso add `isContributor`, `isContributorIdentity`, `isContributorRole` type guard functions following the existing pattern of `isMain`.\n\n## Don't\n- Delete the old `contributions` field (backward compat)\n- Delete `workTimeFrameFrom`/`workTimeFrameTo` (backward compat)\n- Modify any other lexicon type files\n- Change the `id` constant or hash values","acceptance_criteria":"1. The file compiles without TypeScript errors\n2. `Contributor`, `ContributorIdentity`, `ContributorRole`, `WorkScopeString` interfaces are exported\n3. `Main` interface has both `contributions?` (old) and `contributors?` (new) fields\n4. `Main` interface has `startDate?` and `endDate?` fields alongside existing `workTimeFrameFrom`/`workTimeFrameTo`\n5. Type guard functions `isContributor`, `isContributorIdentity`, `isContributorRole` are exported\n6. `npm run build` passes (or `npx tsc --noEmit`)","notes":"Cancelled: lexicon types come from @hypercerts-org/lexicon package, not local files. Replaced by mrb.7 (upgrade package).","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-23T18:01:15.420290777+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:06:56.639807487+06:00","closed_at":"2026-02-23T18:06:56.639815312+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.1","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:01:15.422631065+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.10","title":"Fix: contributor card link missing aria-label for accessibility (from hypercerts-mrb.4)","description":"Review of hypercerts-mrb.4 found: In hypercert-contributor-view.tsx lines 86-93, the anchor tag wrapping the card for DID contributors has no aria-label. Screen readers will announce the full concatenated card text as the link label (name + handle + role + weight), which is verbose and confusing. The link should have an aria-label like 'View {name or identity} on Bluesky' to give screen reader users a concise, meaningful label. Additionally, the hover state only changes border color (transition-colors on the Card) but there is no visible focus ring for keyboard navigation — the card needs a focus-visible outline. Evidence: components/hypercert-contributor-view.tsx lines 84-97.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:43:55.029755832+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:43:55.029755832+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.10","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:43:55.031959654+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.10","depends_on_id":"hypercerts-mrb.4","type":"discovered-from","created_at":"2026-02-23T18:43:57.802031574+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.11","title":"Fix: long displayName overflows contributor card on mobile (from hypercerts-mrb.4)","description":"Review of hypercerts-mrb.4 found: In hypercert-contributor-view.tsx line 41, the name \u003cp\u003e element has no overflow/truncation classes. When profile.displayName is present (the non-truncated branch, lines 42-44), a long display name (e.g. 80+ characters) will overflow the card and break the flex layout, especially on mobile where the card is narrow. The spec (mrb.4 description) says to use truncate class with max-w-[200px] for the identity fallback, but the displayName branch has no such constraint. Fix: add truncate and a max-width to the outer \u003cp\u003e or the displayName span. Evidence: components/hypercert-contributor-view.tsx lines 41-49.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:44:04.032981168+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:44:04.032981168+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.11","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:44:04.034786519+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.11","depends_on_id":"hypercerts-mrb.4","type":"discovered-from","created_at":"2026-02-23T18:44:06.660092889+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.12","title":"Fix: contributor count subtitle shows '0 contributors' in empty state (from hypercerts-mrb.5)","description":"Review of hypercerts-mrb.5 found: In hypercert-contributors-section.tsx lines 45-48, the count subtitle is always rendered, even when displayContributors.length === 0. This means the section header reads 'Contributors / 0 contributors' before showing the empty state panel below. The measurements section (hypercert-measurements-section.tsx lines 71-75) and evidence section (hypercert-evidence-section.tsx lines 66-70) both conditionally render the count subtitle only when length \u003e 0. Fix: wrap the count \u003cp\u003e in a conditional: {displayContributors.length \u003e 0 \u0026\u0026 (...)}. Evidence: components/hypercert-contributors-section.tsx lines 45-48 vs hypercert-measurements-section.tsx lines 71-75.","status":"open","priority":4,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:44:12.650483045+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:44:12.650483045+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.12","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:44:12.652452662+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.12","depends_on_id":"hypercerts-mrb.5","type":"discovered-from","created_at":"2026-02-23T18:44:15.565083991+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.13","title":"Fix: contributor list uses array index as React key (from hypercerts-mrb.5)","description":"Review of hypercerts-mrb.5 found: In hypercert-contributors-section.tsx lines 56 and 66, both the skeleton list and the contributor view list use key={index}. Using array index as key causes React reconciliation issues if the list is ever filtered, sorted, or reordered — components will not remount correctly and may show stale data. The contributor identity string (contributor.identity) is a more stable and semantically meaningful key. Fix: change key={index} to key={contributor.identity} on line 66 (and key={} is acceptable for skeletons since they are transient). Evidence: components/hypercert-contributors-section.tsx lines 55-57 and 66-72.","status":"open","priority":4,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:44:22.271260288+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:44:22.271260288+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.13","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:44:22.272814683+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.13","depends_on_id":"hypercerts-mrb.5","type":"discovered-from","created_at":"2026-02-23T18:44:25.670430208+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.14","title":"Fix: unused Award import in hypercert-detail-view.tsx (from hypercerts-mrb.6)","description":"Review of hypercerts-mrb.6 found: In hypercert-detail-view.tsx line 12, the Award icon is imported from lucide-react but never used anywhere in the file. The mrb.6 spec explicitly says 'Do not remove the unused Award import' but this will cause a lint warning (no-unused-vars / @typescript-eslint/no-unused-vars) and may fail the build if the project has strict lint rules. The import should either be removed (if truly unused) or used. Evidence: components/hypercert-detail-view.tsx line 12 — Award is imported but does not appear in the JSX.","status":"open","priority":4,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:44:32.718226604+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:44:32.718226604+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.14","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:44:32.719871968+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.14","depends_on_id":"hypercerts-mrb.6","type":"discovered-from","created_at":"2026-02-23T18:44:35.379448467+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.15","title":"Fix: profileMap rebuilt on every render in useContributorProfilesQuery (from hypercerts-mrb.3)","description":"Review of hypercerts-mrb.3 found: In `queries/hypercerts/use-contributor-profiles-query.ts` lines 30-35, a new `Map\u003cstring, BlueskyProfile\u003e` is constructed unconditionally on every render. Because the Map is a new object reference each render, any component that receives `profileMap` as a prop will re-render even when the underlying data has not changed. This is a performance regression that worsens with the number of contributors.\n\nEvidence: `use-contributor-profiles-query.ts` lines 30-35:\n```ts\nconst profileMap = new Map\u003cstring, BlueskyProfile\u003e();\nqueries.forEach((q, i) =\u003e {\n  if (q.isSuccess \u0026\u0026 q.data) {\n    profileMap.set(uniqueDids[i], q.data);\n  }\n});\n```\n\nFix: wrap the Map construction in `useMemo` keyed on `queries` (or on the serialised success data). Example:\n```ts\nconst profileMap = useMemo(() =\u003e {\n  const map = new Map\u003cstring, BlueskyProfile\u003e();\n  queries.forEach((q, i) =\u003e {\n    if (q.isSuccess \u0026\u0026 q.data) map.set(uniqueDids[i], q.data);\n  });\n  return map;\n}, [queries, uniqueDids]);\n```\nNote: `uniqueDids` itself should also be memoised (or derived inside the same memo) to avoid the memo invalidating on every render.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:44:38.992493606+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:44:38.992493606+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.15","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:44:38.994518828+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.15","depends_on_id":"hypercerts-mrb.3","type":"discovered-from","created_at":"2026-02-23T18:44:42.590733763+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.16","title":"Fix: type guards in contributor-utils.ts are ambiguous — ContributorIdentity and StrongRef can both match (from hypercerts-mrb.2)","description":"Review of hypercerts-mrb.2 found: In `lib/contributor-utils.ts`, `isContributorIdentity` (line 22) checks for an `identity` string field, and `isStrongRef` (line 42) checks for `uri` + `cid` string fields. These are structurally disjoint, which is correct. However, the `isContributorRole` guard (line 32) only checks for a `role` string field — it would also match any arbitrary object that happens to have a `role` property (e.g. a future schema extension). More critically, the `parseContributor` function (line 79) falls through to `String(rawIdentity)` for the third union arm `{ $type: string }`. At runtime, if the AT Protocol SDK returns a typed object with an unknown $type (e.g. a future variant), the identity will be serialised as `[object Object]`, which is then rendered in the UI and potentially used as a Bluesky profile lookup key.\n\nEvidence: `lib/contributor-utils.ts` lines 85-92:\n```ts\nif (isContributorIdentity(rawIdentity)) {\n  identity = rawIdentity.identity;\n} else if (isStrongRef(rawIdentity)) {\n  identityRef = rawIdentity;\n  identity = extractDidFromAtUri(rawIdentity.uri);\n} else {\n  identity = String(rawIdentity);  // ← produces \"[object Object]\" for unknown typed objects\n}\n```\n\nFix: in the fallback branch, check if `rawIdentity` has a `$type` field and log a warning; set `identity` to an empty string or a safe sentinel rather than `String(rawIdentity)`. Also add a `$type` discriminant check to `isContributorIdentity` and `isContributorRole` to make them more precise (check `$type` starts with `org.hypercerts.claim.activity#`).","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:45:00.875088707+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:45:00.875088707+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.16","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:45:00.876991593+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.16","depends_on_id":"hypercerts-mrb.2","type":"discovered-from","created_at":"2026-02-23T18:45:04.642965257+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.17","title":"Fix: Collections enum uses wrong NSID for 'contribution' and 'evidence' (from hypercerts-mrb.7)","description":"Review of hypercerts-mrb.7 found: `lib/types.ts` lines 48-53 defines the `Collections` enum. Two entries are mismatched against the installed `@hypercerts-org/lexicon` package:\n\n1. `contribution = \"org.hypercerts.claim.contribution\"` — The package has BOTH `org.hypercerts.claim.contribution` (old schema: `contributors: string[]`, `description`) AND `org.hypercerts.claim.contributionDetails` (new schema: `role`, `contributionDescription`, `startDate`, `endDate`). `lib/queries.ts` line 4 imports `OrgHypercertsClaimContributionDetails as HypercertContribution` and uses it for `createContribution` (line 84-94), but `Collections.contribution` still points to the OLD collection NSID `org.hypercerts.claim.contribution`. Records created via `createContribution` will be written to the wrong collection.\n\n2. `evidence = \"org.hypercerts.claim.evidence\"` — The package's `OrgHypercertsClaimAttachment` (imported as `HypercertEvidence` in `lib/queries.ts` line 5) has `$type: 'org.hypercerts.claim.attachment'`, but `Collections.evidence` is `\"org.hypercerts.claim.evidence\"`. Records created via `createEvidence` (line 108-118) will be written to `org.hypercerts.claim.evidence` but the type system expects `org.hypercerts.claim.attachment`.\n\nEvidence:\n- `lib/types.ts` line 49: `contribution = \"org.hypercerts.claim.contribution\"`\n- `lib/types.ts` line 50: `evidence = \"org.hypercerts.claim.evidence\"`\n- `lib/queries.ts` line 4: `OrgHypercertsClaimContributionDetails as HypercertContribution`\n- `lib/queries.ts` line 5: `OrgHypercertsClaimAttachment as HypercertEvidence`\n- Package NSID constants: `CONTRIBUTION_DETAILS_NSID = \"org.hypercerts.claim.contributionDetails\"`, `ATTACHMENT_NSID = \"org.hypercerts.claim.attachment\"`\n\nFix: Update `Collections` enum:\n```ts\ncontribution = \"org.hypercerts.claim.contributionDetails\",\nevidence = \"org.hypercerts.claim.attachment\",\n```\nVerify all callers of `Collections.contribution` and `Collections.evidence` still work after the rename. This is a runtime data-corruption bug — records are being written to the wrong AT Protocol collection.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:45:19.917453568+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:45:19.917453568+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.17","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:45:19.91906402+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.17","depends_on_id":"hypercerts-mrb.7","type":"discovered-from","created_at":"2026-02-23T18:45:23.236033066+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.18","title":"Fix: blob-display.tsx $type check uses wrong value for Uri type (from hypercerts-mrb.7)","description":"Review of hypercerts-mrb.7 found: `components/blob-display.tsx` line 19 checks:\n```ts\nif (type === \"app.certified.defs#uri\") {\n```\n\nBut the installed `@hypercerts-org/lexicon` package defines `OrgHypercertsDefs.Uri` with:\n```ts\n$type?: 'org.hypercerts.defs#uri';\n```\n(see `node_modules/@hypercerts-org/lexicon/dist/types/org/hypercerts/defs.d.ts` line 7)\n\nThe check uses `app.certified.defs#uri` but the actual $type value in records is `org.hypercerts.defs#uri`. This means the URI branch will never match, and all URI-type content will fall through to the final `return \"—\"`, silently hiding evidence/attachment links.\n\nSimilarly, the blob type check on line 24:\n```ts\nif ([\"smallBlob\", \"largeBlob\", \"blob\"].includes(type || \"\")) {\n```\nThis checks for bare names like `\"smallBlob\"` but the actual $type values are `\"org.hypercerts.defs#smallBlob\"` and `\"org.hypercerts.defs#largeBlob\"`. This branch also never matches.\n\nEvidence:\n- `components/blob-display.tsx` lines 19, 24\n- `node_modules/@hypercerts-org/lexicon/dist/types/org/hypercerts/defs.d.ts` lines 7, 15, 23\n\nFix: Update the type checks to use the correct fully-qualified $type strings:\n```ts\nif (type === \"org.hypercerts.defs#uri\") { ... }\nif ([\"org.hypercerts.defs#smallBlob\", \"org.hypercerts.defs#largeBlob\"].includes(type || \"\")) { ... }\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:45:35.090855854+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:45:35.090855854+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.18","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:45:35.092588645+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.18","depends_on_id":"hypercerts-mrb.7","type":"discovered-from","created_at":"2026-02-23T18:45:38.52934298+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.19","title":"Fix: query key namespace collision — contributorProfile shares 'hypercerts' root with record-scoped keys (from hypercerts-mrb.3)","description":"Review of hypercerts-mrb.3 found: `lib/api/query-keys.ts` line 38-39 adds:\n```ts\ncontributorProfile: (did: string) =\u003e\n  [...queryKeys.hypercerts.all, \"contributor-profile\", did] as const,\n```\n\nThis produces keys like `[\"hypercerts\", \"contributor-profile\", \"did:plc:abc\"]`. However, there is already a separate `external.bluesky.profile` key (line 46-48) for the same Bluesky profile data:\n```ts\nprofile: (actor: string) =\u003e\n  [...queryKeys.external.bluesky.all, \"profile\", actor] as const,\n```\n\nTwo different query keys now cache the same underlying data (Bluesky profile for a DID). If any other part of the app uses `queryKeys.external.bluesky.profile(did)` to fetch or invalidate a profile, the contributor profile cache will not be invalidated, and vice versa. This is a cache coherence issue.\n\nAdditionally, the `contributorProfile` key is semantically an external Bluesky API call, not a hypercert record operation. Placing it under `queryKeys.hypercerts` is a namespace violation that will confuse future developers.\n\nFix: Either (a) reuse `queryKeys.external.bluesky.profile(did)` in `useContributorProfilesQuery` instead of introducing a new key, or (b) move `contributorProfile` under `queryKeys.external.bluesky` and document the deduplication intent.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:45:51.347170337+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:45:51.347170337+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.19","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:45:51.34935941+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.19","depends_on_id":"hypercerts-mrb.3","type":"discovered-from","created_at":"2026-02-23T18:45:54.586397763+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.2","title":"Add contributor profile resolution utility","description":"## Files\n- lib/contributor-utils.ts (create)\n\n## What to do\nCreate a utility module that extracts contributor display information from the new inline contributor schema. This module bridges the gap between raw lexicon data and what the UI needs.\n\n**Import types from the package** (NOT from local `@/lexicons/`):\n```typescript\nimport type { OrgHypercertsClaimActivity, ComAtprotoRepoStrongRef } from \"@hypercerts-org/sdk-core\";\n\ntype Contributor = OrgHypercertsClaimActivity.Contributor;\ntype ContributorIdentity = OrgHypercertsClaimActivity.ContributorIdentity;\ntype ContributorRole = OrgHypercertsClaimActivity.ContributorRole;\ntype StrongRef = ComAtprotoRepoStrongRef.Main;\n```\n\n### Functions to implement:\n\n```typescript\n/** Parsed contributor ready for display */\nexport interface DisplayContributor {\n  identity: string;\n  isDid: boolean;\n  role?: string;\n  weight?: string;\n  identityRef?: StrongRef;\n  detailsRef?: StrongRef;\n}\n\nexport function parseContributor(contributor: Contributor): DisplayContributor;\nexport function parseContributors(contributors?: Contributor[]): DisplayContributor[];\n```\n\n### Logic for `parseContributor`:\n1. Check `contributor.contributorIdentity`:\n   - If it has an `identity` field (ContributorIdentity) → use `identity` as the identity string\n   - If it has `uri` and `cid` fields (StrongRef) → set `identityRef`, extract DID from URI (`at://did:xxx/...` → `did:xxx`)\n   - Fallback: stringify it\n2. Check `contributor.contributionDetails`:\n   - If it has a `role` field (ContributorRole) → use `role`\n   - If it has `uri` and `cid` (StrongRef) → set `detailsRef`, role stays undefined\n3. Set `isDid = identity.startsWith(\"did:\")`\n4. Set `weight = contributor.contributionWeight`\n\nAlso export type guard helpers: `isContributorIdentity`, `isContributorRole`, `isStrongRef`.\n\n## Don't\n- Import from local `@/lexicons/types/...` (use `@hypercerts-org/sdk-core` or `@hypercerts-org/lexicon`)\n- Import or call any server actions\n- Fetch any data (no API calls)\n- Modify any existing files","acceptance_criteria":"1. File `lib/contributor-utils.ts` exists and exports `DisplayContributor`, `parseContributor`, `parseContributors`\n2. `parseContributors(undefined)` returns `[]`\n3. `parseContributors([])` returns `[]`\n4. `parseContributor({ contributorIdentity: { identity: \"did:plc:abc\" } })` returns `{ identity: \"did:plc:abc\", isDid: true, role: undefined, weight: undefined }`\n5. `parseContributor({ contributorIdentity: { identity: \"Alice\" }, contributionDetails: { role: \"Developer\" }, contributionWeight: \"50\" })` returns `{ identity: \"Alice\", isDid: false, role: \"Developer\", weight: \"50\" }`\n6. `parseContributor({ contributorIdentity: { uri: \"at://did:plc:xyz/org.hypercerts.claim.contributorInformation/abc\", cid: \"bafy...\" } })` returns `{ identity: \"did:plc:xyz\", isDid: true, identityRef: { uri: ..., cid: ... } }`\n7. TypeScript compiles without errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-23T18:01:36.447502979+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:30:11.531734809+06:00","closed_at":"2026-02-23T18:30:11.531734809+06:00","close_reason":"4f33871 add contributor parsing utility","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.2","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:01:36.449670256+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mrb.2","depends_on_id":"hypercerts-mrb.1","type":"blocks","created_at":"2026-02-23T18:03:02.683477765+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mrb.2","depends_on_id":"hypercerts-mrb.7","type":"blocks","created_at":"2026-02-23T18:07:54.028930157+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.20","title":"Fix: getProfile() throws on non-200 but retry:false leaves error state permanent (from hypercerts-mrb.3)","description":"Review of hypercerts-mrb.3 found: `queries/hypercerts/use-contributor-profiles-query.ts` line 24 sets `retry: false`. The `getProfile` function in `lib/api/external/bluesky.ts` line 13 calls `externalApiClient` which throws an `APIError` on any non-2xx response (see `lib/api/client.ts` lines 132-136). With `retry: false`, a transient network error or a 429 rate-limit from the Bluesky public API will permanently mark the query as `isError` for the lifetime of the component. The contributor profile will never be retried even after the rate limit window expires.\n\nThe spec comment says 'Failed lookups are silently ignored' — this is partially correct (the Map simply won't contain the DID), but the query stays in error state and will not re-attempt on the next mount.\n\nEvidence:\n- `use-contributor-profiles-query.ts` line 24: `retry: false`\n- `lib/api/client.ts` lines 132-136: throws `APIError` on non-ok response\n- No `throwOnError: false` or error boundary present\n\nFix: Consider `retry: 1` with exponential backoff for transient errors, or at minimum add `staleTime: 0` so a remount will re-attempt. Alternatively, wrap `queryFn` to catch and return `null` instead of throwing, making the error truly silent and allowing future retries.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:46:06.529621804+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:46:06.529621804+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.20","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:46:06.531688148+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.20","depends_on_id":"hypercerts-mrb.3","type":"discovered-from","created_at":"2026-02-23T18:46:10.367726109+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.21","title":"Fix: resolve StrongRef contributor identities by fetching contributorInformation records","description":"## Bug\nAll 6 contributors on hypercert `at://did:plc:cqdsge7nhwx3jt77qdyq75jr/org.hypercerts.claim.activity/3mfjk4if5qc2b` display as \"kzoeps\" because `parseContributor()` calls `extractDidFromAtUri()` on the StrongRef URI, which extracts the **repo owner DID** (same for all), not the actual contributor DID stored inside the referenced `contributorInformation` record.\n\n## Files\n- lib/contributor-utils.ts (modify)\n- lib/create-actions.ts (modify)\n- queries/hypercerts/use-contributor-profiles-query.ts (modify)\n- components/hypercert-contributors-section.tsx (modify)\n\n## What to do\n\n### 1. Add server action to fetch contributorInformation records (`lib/create-actions.ts`)\n\nAdd a new server action following the exact pattern of `getMeasurementRecord`:\n\n```typescript\nexport const getContributorInformationRecord = async (params: {\n  did: string;\n  collection: string;\n  rkey: string;\n}) =\u003e {\n  const { did, collection, rkey } = params;\n  const ctx = await getRepoContext({ targetDid: did });\n  if (!ctx) {\n    throw new Error(\"Unable to get repository context\");\n  }\n  const data = await ctx.scopedRepo.records.get({ collection, rkey });\n  if (data?.value) {\n    data.value = await resolveRecordBlobs(data.value, did);\n  }\n  return JSON.parse(JSON.stringify(data));\n};\n```\n\n### 2. Update `parseContributor` in `lib/contributor-utils.ts`\n\nWhen `contributorIdentity` is a StrongRef, do NOT use `extractDidFromAtUri()` to derive the identity. Instead:\n- Set `identity` to a placeholder like the rkey or the full URI (something unique per contributor)\n- Set `isDid = false` (we dont know the DID yet)\n- Store the `identityRef` as before\n- Add a new field `needsResolution: boolean` to `DisplayContributor`\n\nUpdated `DisplayContributor`:\n```typescript\nexport interface DisplayContributor {\n  identity: string;\n  isDid: boolean;\n  role?: string;\n  weight?: string;\n  identityRef?: StrongRef;\n  detailsRef?: StrongRef;\n  needsResolution: boolean; // true when identity comes from a StrongRef that needs fetching\n}\n```\n\nUpdated StrongRef branch in `parseContributor`:\n```typescript\n} else if (isStrongRef(rawIdentity)) {\n  identityRef = rawIdentity;\n  // Use the full URI as a unique placeholder — do NOT extract DID from it\n  identity = rawIdentity.uri;\n  needsResolution = true;\n}\n```\n\n### 3. Add resolution hook in `queries/hypercerts/use-contributor-profiles-query.ts`\n\nAdd a new hook `useResolveContributorIdentities` that:\n1. Takes `DisplayContributor[]`\n2. For each contributor with `needsResolution === true`, parse the `identityRef.uri` to extract `did`, `collection`, `rkey`\n3. Use `useQueries` to call `getContributorInformationRecord` for each\n4. Return a `Map\u003cstring, ResolvedContributorInfo\u003e` keyed by the placeholder identity (the URI)\n\n```typescript\ninterface ResolvedContributorInfo {\n  identifier: string; // the actual DID from the contributorInformation record\n  displayName?: string;\n}\n\nexport function useResolveContributorIdentities(contributors: DisplayContributor[]) {\n  const needsResolution = contributors.filter((c) =\u003e c.needsResolution \u0026\u0026 c.identityRef);\n\n  const queries = useQueries({\n    queries: needsResolution.map((c) =\u003e {\n      const ref = c.identityRef!;\n      const { did, collection, rkey } = parseAtUri(ref.uri);\n      return {\n        queryKey: queryKeys.hypercerts.contributorInformation(did, rkey),\n        queryFn: () =\u003e getContributorInformationRecord({ did, collection, rkey }),\n        retry: 1,\n        staleTime: 10 * 60 * 1000,\n      };\n    }),\n  });\n\n  const resolvedMap = new Map\u003cstring, ResolvedContributorInfo\u003e();\n  queries.forEach((q, i) =\u003e {\n    if (q.isSuccess \u0026\u0026 q.data?.value) {\n      const val = q.data.value;\n      resolvedMap.set(needsResolution[i].identity, {\n        identifier: val.identifier || \"\",\n        displayName: val.displayName,\n      });\n    }\n  });\n\n  const isLoading = queries.some((q) =\u003e q.isLoading);\n  return { resolvedMap, isLoading };\n}\n```\n\nAdd a helper to parse AT URIs:\n```typescript\nfunction parseAtUri(uri: string): { did: string; collection: string; rkey: string } {\n  const withoutScheme = uri.replace(/^at:\\/\\//, \"\");\n  const [did, collection, rkey] = withoutScheme.split(\"/\");\n  return { did, collection, rkey };\n}\n```\n\nAdd query key in `lib/api/query-keys.ts`:\n```typescript\ncontributorInformation: (did: string, rkey: string) =\u003e\n  [...queryKeys.hypercerts.all, \"contributor-information\", did, rkey] as const,\n```\n\n### 4. Update `HypercertContributorsSection` (`components/hypercert-contributors-section.tsx`)\n\nThe section now needs a two-phase resolution:\n1. First resolve StrongRef identities → get actual DIDs\n2. Then resolve DIDs → Bluesky profiles\n\n```typescript\nexport default function HypercertContributorsSection({ contributors }: Props) {\n  const displayContributors = parseContributors(contributors);\n\n  // Phase 1: Resolve StrongRef identities to actual DIDs\n  const { resolvedMap, isLoading: isResolvingIdentities } = useResolveContributorIdentities(displayContributors);\n\n  // Build resolved contributors list — replace placeholder identities with actual DIDs\n  const resolvedContributors = useMemo(() =\u003e {\n    return displayContributors.map((c) =\u003e {\n      if (c.needsResolution \u0026\u0026 resolvedMap.has(c.identity)) {\n        const resolved = resolvedMap.get(c.identity)!;\n        return {\n          ...c,\n          identity: resolved.identifier,\n          isDid: resolved.identifier.startsWith(\"did:\"),\n          displayName: resolved.displayName, // add this field to DisplayContributor too\n          needsResolution: false,\n        };\n      }\n      return c;\n    });\n  }, [displayContributors, resolvedMap]);\n\n  // Phase 2: Resolve DIDs to Bluesky profiles\n  const { profileMap, isLoading: isLoadingProfiles } = useContributorProfilesQuery(resolvedContributors);\n\n  const isLoading = isResolvingIdentities || isLoadingProfiles;\n\n  // ... render using resolvedContributors and profileMap\n}\n```\n\nAlso add `displayName?: string` to `DisplayContributor` interface so the resolved displayName from contributorInformation can be used as a fallback when Bluesky profile lookup fails.\n\n### 5. Update `HypercertContributorView` to use resolved displayName\n\nIn the name display logic, add a fallback:\n```\nprofile?.displayName → contributor.displayName → contributor.identity\n```\n\n## Don't\n- Change how inline `ContributorIdentity` objects are handled (those work correctly)\n- Remove `extractDidFromAtUri` (it may be useful elsewhere, but dont use it for identity resolution)\n- Break the existing flow for non-StrongRef contributors","acceptance_criteria":"1. On hypercert `at://did:plc:cqdsge7nhwx3jt77qdyq75jr/org.hypercerts.claim.activity/3mfjk4if5qc2b`, all 6 contributors display with DIFFERENT names/avatars (not all kzoeps)\n2. Contributors with StrongRef identities are resolved by fetching the `contributorInformation` record\n3. The resolved `identifier` DID is used for Bluesky profile lookup (not the repo owner DID)\n4. Contributors with inline `ContributorIdentity` still work correctly\n5. `displayName` from `contributorInformation` is used as fallback when Bluesky profile has no displayName\n6. Loading states work correctly (show skeletons during both resolution phases)\n7. `npx tsc --noEmit` passes\n8. `npm run build` passes","status":"closed","priority":0,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":60,"created_at":"2026-02-23T18:51:30.314753324+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:54:57.071818272+06:00","closed_at":"2026-02-23T18:54:57.071818272+06:00","close_reason":"d51b4f8 fix: resolve StrongRef contributor identities via contributorInformation records","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.21","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:51:30.316264633+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.3","title":"Add contributor profile query key and Bluesky profile resolution hook","description":"## Files\n- lib/api/query-keys.ts (modify)\n- queries/hypercerts/use-contributor-profiles-query.ts (create)\n- queries/hypercerts/index.ts (modify)\n\n## What to do\n\n### 1. Add query key for contributor profiles in `lib/api/query-keys.ts`\n\nAdd to the `hypercerts` section:\n```typescript\ncontributorProfile: (did: string) =\u003e\n  [...queryKeys.hypercerts.all, \"contributor-profile\", did] as const,\n```\n\n### 2. Create `queries/hypercerts/use-contributor-profiles-query.ts`\n\nThis hook takes an array of `DisplayContributor` objects (from task mrb.2) and resolves DID-based identities to Bluesky profiles for display (avatar, displayName, handle).\n\n```typescript\n\"use client\";\n\nimport { useQueries } from \"@tanstack/react-query\";\nimport { getProfile } from \"@/lib/api/external/bluesky\";\nimport { queryKeys } from \"@/lib/api/query-keys\";\nimport type { DisplayContributor } from \"@/lib/contributor-utils\";\nimport type { BlueskyProfile } from \"@/lib/api/types\";\n\n/**\n * For each DisplayContributor with isDid=true, fetch their Bluesky profile.\n * Returns a Map\u003cstring, BlueskyProfile\u003e keyed by DID.\n * Non-DID contributors are skipped (no query created).\n * Failed lookups are silently ignored (retry: false, no throw).\n */\nexport function useContributorProfilesQuery(contributors: DisplayContributor[]) {\n  const didContributors = contributors.filter((c) =\u003e c.isDid);\n  // Deduplicate DIDs\n  const uniqueDids = [...new Set(didContributors.map((c) =\u003e c.identity))];\n\n  const queries = useQueries({\n    queries: uniqueDids.map((did) =\u003e ({\n      queryKey: queryKeys.hypercerts.contributorProfile(did),\n      queryFn: () =\u003e getProfile(did),\n      retry: false,\n      staleTime: 5 * 60 * 1000, // 5 min cache\n    })),\n  });\n\n  // Build a Map\u003cdid, BlueskyProfile\u003e from successful queries\n  const profileMap = new Map\u003cstring, BlueskyProfile\u003e();\n  queries.forEach((q, i) =\u003e {\n    if (q.isSuccess \u0026\u0026 q.data) {\n      profileMap.set(uniqueDids[i], q.data);\n    }\n  });\n\n  const isLoading = queries.some((q) =\u003e q.isLoading);\n\n  return { profileMap, isLoading };\n}\n```\n\n### 3. Export from `queries/hypercerts/index.ts`\n\nAdd:\n```typescript\nexport { useContributorProfilesQuery } from \"./use-contributor-profiles-query\";\n```\n\n## Don't\n- Modify any component files\n- Create server actions (profile resolution happens client-side via public Bluesky API)\n- Add any new dependencies","acceptance_criteria":"1. `lib/api/query-keys.ts` has `contributorProfile` key under `hypercerts`\n2. `queries/hypercerts/use-contributor-profiles-query.ts` exists and exports `useContributorProfilesQuery`\n3. `queries/hypercerts/index.ts` re-exports `useContributorProfilesQuery`\n4. The hook deduplicates DIDs (if 3 contributors share the same DID, only 1 query is made)\n5. The hook returns `{ profileMap: Map\u003cstring, BlueskyProfile\u003e, isLoading: boolean }`\n6. Non-DID contributors do not trigger any queries\n7. Failed profile lookups do not throw (retry: false)\n8. TypeScript compiles without errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-23T18:01:54.121292087+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:31:31.82162002+06:00","closed_at":"2026-02-23T18:31:31.82162002+06:00","close_reason":"71a7093 add contributor profile query hook","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.3","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:01:54.123114897+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mrb.3","depends_on_id":"hypercerts-mrb.2","type":"blocks","created_at":"2026-02-23T18:03:03.486314501+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.4","title":"Create HypercertContributorView component","description":"## Files\n- components/hypercert-contributor-view.tsx (create)\n\n## What to do\nCreate a single-contributor card component that displays one contributor's information. This follows the same pattern as `components/hypercert-measurement-view.tsx`.\n\n### Component signature:\n```typescript\n\"use client\";\n\nimport type { DisplayContributor } from \"@/lib/contributor-utils\";\nimport type { BlueskyProfile } from \"@/lib/api/types\";\n\ninterface HypercertContributorViewProps {\n  contributor: DisplayContributor;\n  profile?: BlueskyProfile; // resolved Bluesky profile, if available\n}\n\nexport default function HypercertContributorView({ contributor, profile }: HypercertContributorViewProps) { ... }\n```\n\n### Layout:\nUse a Card (from `components/ui/card`) with `glass-panel rounded-xl border border-border/50` classes (matching measurement-view pattern).\n\n**Card content (single row layout):**\n1. **Avatar area** (left):\n   - If `profile?.avatar` exists: render a 40x40 rounded-full `\u003cimg\u003e` with the avatar URL\n   - Else: render a 40x40 rounded-full div with `bg-create-accent/10` containing a `User` icon from lucide-react (size-5, text-create-accent)\n\n2. **Info area** (center, flex-1):\n   - **Name line**: \n     - If `profile?.displayName` → show displayName in `text-sm font-[family-name:var(--font-syne)] font-semibold`\n     - Else → show `contributor.identity` truncated (use `truncate` class, max-w-[200px])\n   - **Handle/DID line** (below name, smaller):\n     - If `profile?.handle` → show `@{handle}` in `text-xs text-muted-foreground font-[family-name:var(--font-outfit)]`\n     - Else if `contributor.isDid` → show truncated DID in same style\n   - **Role badge** (below handle, optional):\n     - If `contributor.role` → show in a small pill: `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-[family-name:var(--font-outfit)] bg-create-accent/10 text-create-accent border border-create-accent/20`\n\n3. **Weight area** (right, optional):\n   - If `contributor.weight` → show weight value in `text-lg font-[family-name:var(--font-syne)] font-bold text-create-accent` with a small \"weight\" label below in `text-xs text-muted-foreground`\n\n4. **Link behavior**:\n   - If `contributor.isDid` → wrap the entire card content in an anchor tag linking to `https://bsky.app/profile/${contributor.identity}` with `target=\"_blank\" rel=\"noopener noreferrer\"`\n   - Add hover state: `hover:border-create-accent/40 transition-colors`\n\n### Icons to import:\n- `User` from lucide-react\n\n## Don't\n- Fetch any data (all data comes via props)\n- Use Next.js Image component for avatars (use plain `\u003cimg\u003e` since avatars are external Bluesky CDN URLs)\n- Add any loading/error states (parent handles those)\n- Import from `@/queries/` (this is a pure presentational component)","acceptance_criteria":"1. File `components/hypercert-contributor-view.tsx` exists and default-exports the component\n2. Component accepts `{ contributor: DisplayContributor, profile?: BlueskyProfile }` props\n3. Shows avatar (from profile or fallback icon)\n4. Shows name (from profile.displayName or contributor.identity)\n5. Shows role badge when contributor.role is present, hidden when absent\n6. Shows weight when contributor.weight is present, hidden when absent\n7. DID-based contributors link to bsky.app/profile/{did}\n8. Non-DID contributors do not have a link\n9. Uses \"use client\" directive\n10. TypeScript compiles without errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-23T18:02:16.61823666+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:31:27.27952916+06:00","closed_at":"2026-02-23T18:31:27.27952916+06:00","close_reason":"71a7093 add contributor view component","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.4","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:02:16.620241846+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mrb.4","depends_on_id":"hypercerts-mrb.2","type":"blocks","created_at":"2026-02-23T18:03:04.102718949+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.5","title":"Create HypercertContributorsSection component","description":"## Files\n- components/hypercert-contributors-section.tsx (create)\n\n## What to do\nCreate the contributors section component. Contributors come from the activity record itself (passed as props), NOT from Constellation backlinks.\n\n**Import types from the package** (NOT from local `@/lexicons/`):\n```typescript\nimport type { OrgHypercertsClaimActivity } from \"@hypercerts-org/sdk-core\";\ntype Contributor = OrgHypercertsClaimActivity.Contributor;\n```\n\n### Component signature:\n```typescript\n\"use client\";\n\ninterface HypercertContributorsSectionProps {\n  contributors?: Contributor[];\n}\n\nexport default function HypercertContributorsSection({ contributors }: HypercertContributorsSectionProps) { ... }\n```\n\n### Implementation:\n1. Parse contributors using `parseContributors(contributors)` from `@/lib/contributor-utils`\n2. Resolve DID profiles using `useContributorProfilesQuery(displayContributors)` from `@/queries/hypercerts`\n3. Render section matching the measurements section pattern:\n\n**Section header:**\n- Icon: `Users` from lucide-react in a `size-10 rounded-full bg-create-accent/10` circle\n- Title: \"Contributors\" in `text-xl font-[family-name:var(--font-syne)] font-semibold`\n- Count subtitle: \"{n} contributor(s)\"\n\n**Loading state** (while profiles resolve):\n- Show `ContributorSkeleton` components (glass-panel div with Skeleton for avatar + text lines)\n- Only show if there ARE DID contributors to resolve\n\n**Content:**\n- If contributors exist: render `HypercertContributorView` for each, passing resolved profile from `profileMap.get(contributor.identity)`\n- If empty: show empty state with Users icon + \"No contributors listed for this hypercert.\"\n\n**Skeleton:**\n```typescript\nconst ContributorSkeleton = () =\u003e (\n  \u003cdiv className=\"glass-panel p-4 border border-border/50 rounded-xl flex items-center gap-3\"\u003e\n    \u003cSkeleton className=\"size-10 rounded-full\" /\u003e\n    \u003cdiv className=\"space-y-2 flex-1\"\u003e\n      \u003cSkeleton className=\"h-4 w-1/3\" /\u003e\n      \u003cSkeleton className=\"h-3 w-1/4\" /\u003e\n    \u003c/div\u003e\n  \u003c/div\u003e\n);\n```\n\n## Don't\n- Import from local `@/lexicons/types/...`\n- Fetch data from Constellation backlinks\n- Create any server actions\n- Modify any existing component files","acceptance_criteria":"1. File `components/hypercert-contributors-section.tsx` exists and default-exports the component\n2. Component accepts `{ contributors?: Contributor[] }` props\n3. When `contributors` is undefined or empty, shows empty state with \"No contributors listed\" message\n4. When contributors exist, renders HypercertContributorView for each\n5. Passes resolved Bluesky profiles to each ContributorView via profileMap\n6. Shows loading skeletons while DID profiles are being resolved\n7. Section header shows \"Contributors\" title with count\n8. Uses \"use client\" directive\n9. Visual style matches the measurements/evidence/evaluations sections\n10. TypeScript compiles without errors","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-23T18:02:37.049525361+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:32:47.497312436+06:00","closed_at":"2026-02-23T18:32:47.497312436+06:00","close_reason":"21f40e1 add contributors section component","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.5","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:02:37.051145243+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mrb.5","depends_on_id":"hypercerts-mrb.3","type":"blocks","created_at":"2026-02-23T18:03:05.09820068+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mrb.5","depends_on_id":"hypercerts-mrb.4","type":"blocks","created_at":"2026-02-23T18:03:05.158885354+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.6","title":"Wire HypercertContributorsSection into the detail page","description":"## Files\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\nAdd the `HypercertContributorsSection` to the hypercert detail view, passing the contributors data from the record.\n\n### Changes to `components/hypercert-detail-view.tsx`:\n\n1. **Add import** at the top:\n```typescript\nimport HypercertContributorsSection from \"./hypercert-contributors-section\";\n```\n\n2. **Pass contributors to the section**. The record type is `HypercertClaim` from `@hypercerts-org/sdk-core`. The new `contributors` field may not exist on the SDK type yet, so cast as needed:\n```typescript\n// Access contributors from the record (may be on the raw record even if SDK type hasn't been updated)\nconst contributors = (record as any).contributors;\n```\n\n3. **Add the section** in the render tree. Place it AFTER the metadata grid (line ~134, after the closing `\u003c/div\u003e` of the metadata grid section) and BEFORE the full description section. This puts contributors right after the basic info and before the long text.\n\nThe insertion point is between the `{/* Metadata Grid */}` section and the `{/* Full Description */}` section (around line 134-137 in the current file):\n\n```tsx\n{/* Contributors Section */}\n\u003cdiv className=\"animate-fade-in-up [animation-delay:150ms]\"\u003e\n  \u003cHypercertContributorsSection contributors={contributors} /\u003e\n\u003c/div\u003e\n```\n\n4. **Update animation delays**: Bump the description section delay from `200ms` to `250ms` and the sections div delay from `300ms` to `350ms` to maintain the stagger effect.\n\n### Complete diff summary:\n- Add 1 import line\n- Add ~5 lines for the contributors section between metadata and description\n- Change 2 animation delay values\n\n## Don't\n- Change the component props interface (no new props needed — contributors come from the existing `record`)\n- Move or reorder existing sections (measurements, evidence, evaluations stay at the bottom)\n- Modify the page.tsx route file\n- Remove the unused `Award` import from lucide-react (it may be used elsewhere or planned)","acceptance_criteria":"1. `HypercertContributorsSection` is imported in `hypercert-detail-view.tsx`\n2. Contributors section appears between the metadata grid and the description section\n3. `contributors` is extracted from the record and passed to the section\n4. Animation delays maintain a staggered sequence (no two sections share the same delay)\n5. Existing sections (measurements, evidence, evaluations) remain unchanged and in the same order\n6. The page renders without runtime errors when `record.contributors` is undefined (backward compat)\n7. The page renders without runtime errors when `record.contributors` is an array of contributor objects\n8. TypeScript compiles without errors (cast to any is acceptable for the contributors field)\n9. `npm run build` passes","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-23T18:02:57.550838241+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:34:35.517495716+06:00","closed_at":"2026-02-23T18:34:35.517495716+06:00","close_reason":"3a8d276 wire contributors section into detail page","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-mrb.6","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:02:57.552605742+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-mrb.6","depends_on_id":"hypercerts-mrb.5","type":"blocks","created_at":"2026-02-23T18:03:05.88521224+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.7","title":"Upgrade @hypercerts-org/lexicon to beta.15 and migrate local lexicon imports","description":"## Files\n- package.json (modify)\n- lib/utils.ts (modify)\n- lib/queries.ts (modify)\n- lib/types.ts (modify)\n- components/hypercerts-base-form.tsx (modify)\n- components/blob-display.tsx (modify)\n\n## What to do\nThe local `lexicons/` directory contains generated types from an outdated version of `@hypercerts-org/lexicon` (0.10.0-beta.3). The installed `@hypercerts-org/lexicon` package needs upgrading to `0.10.0-beta.15` which has the `contributors` field on the activity record. After upgrading, migrate all imports from local `@/lexicons/types/...` to the package.\n\n### Step 1: Upgrade the package\n```bash\nnpm install @hypercerts-org/lexicon@0.10.0-beta.15\n```\n\n### Step 2: Migrate imports in 5 files\n\nThe SDK re-exports all lexicon types. Use `@hypercerts-org/sdk-core` or `@hypercerts-org/lexicon` for imports. The SDK already re-exports everything via:\n```typescript\nexport { OrgHypercertsClaimActivity, OrgHypercertsClaimAttachment, OrgHypercertsClaimCollection, OrgHypercertsClaimContributionDetails, OrgHypercertsClaimContributorInformation, OrgHypercertsClaimEvaluation, OrgHypercertsClaimMeasurement, OrgHypercertsClaimRights, OrgHypercertsDefs, AppCertifiedLocation, ComAtprotoRepoStrongRef } from \"@hypercerts-org/lexicon\";\n```\n\n**Migration mapping:**\n| Old import | New import |\n|---|---|\n| `import * as Hypercert from \"@/lexicons/types/org/hypercerts/claim/activity\"` | `import { OrgHypercertsClaimActivity as Hypercert } from \"@hypercerts-org/sdk-core\"` |\n| `import * as HypercertClaim from \"@/lexicons/types/org/hypercerts/claim/activity\"` | `import { OrgHypercertsClaimActivity as HypercertClaim } from \"@hypercerts-org/sdk-core\"` |\n| `import * as Contribution from \"@/lexicons/types/org/hypercerts/claim/contribution\"` | `import { OrgHypercertsClaimContributionDetails as Contribution } from \"@hypercerts-org/sdk-core\"` |\n| `import * as HypercertContribution from \"@/lexicons/types/org/hypercerts/claim/contribution\"` | `import { OrgHypercertsClaimContributionDetails as HypercertContribution } from \"@hypercerts-org/sdk-core\"` |\n| `import * as Evaluation from \"@/lexicons/types/org/hypercerts/claim/evaluation\"` | `import { OrgHypercertsClaimEvaluation as Evaluation } from \"@hypercerts-org/sdk-core\"` |\n| `import * as HypercertEvaluation from \"@/lexicons/types/org/hypercerts/claim/evaluation\"` | `import { OrgHypercertsClaimEvaluation as HypercertEvaluation } from \"@hypercerts-org/sdk-core\"` |\n| `import * as HypercertEvidence from \"@/lexicons/types/org/hypercerts/claim/evidence\"` | `import { OrgHypercertsClaimAttachment as HypercertEvidence } from \"@hypercerts-org/sdk-core\"` |\n| `import * as HypercertRights from \"@/lexicons/types/org/hypercerts/claim/rights\"` | `import { OrgHypercertsClaimRights as HypercertRights } from \"@hypercerts-org/sdk-core\"` |\n| `import * as HypercertLocation from \"@/lexicons/types/app/certified/location\"` | `import { AppCertifiedLocation as HypercertLocation } from \"@hypercerts-org/sdk-core\"` |\n| `import { SmallBlob, Uri } from \"@/lexicons/types/org/hypercerts/defs\"` | `import { OrgHypercertsDefs } from \"@hypercerts-org/sdk-core\"` then use `OrgHypercertsDefs.SmallBlob` and `OrgHypercertsDefs.Uri` |\n\n**IMPORTANT**: The old local `contribution.ts` had a different schema (single `org.hypercerts.claim.contribution` record with `hypercert`, `role`, `contributors[]`, `description`). The new package has `OrgHypercertsClaimContributionDetails` (with `role`, `contributionDescription`, `startDate`, `endDate`). Check that the usages in `lib/types.ts` and `lib/queries.ts` still compile — the `HypercertContributionData` type alias may need its `value` type updated.\n\n### Step 3: Fix any type mismatches\nAfter migration, run `npx tsc --noEmit` and fix any type errors caused by schema differences between the old local types and the new package types. Common issues:\n- `workScope` changed from `string` to a union type\n- `workTimeFrameFrom`/`workTimeFrameTo` renamed to `startDate`/`endDate`\n- `contributions` (old StrongRef[]) vs `contributors` (new inline Contributor[])\n- `location` (singular) vs `locations` (plural array)\n- `evidence` field removed from activity (evidence is now via attachments)\n\nFor each breaking change, update the consuming code to handle both old and new field names where possible, or update to the new names if the old ones are unused.\n\n### Step 4: Verify\n```bash\nnpx tsc --noEmit\nnpm run build\n```\n\n## Don't\n- Delete the local `lexicons/` directory yet (other tasks may still reference it during transition; a separate cleanup task can handle removal)\n- Upgrade `@hypercerts-org/sdk-core` (only upgrade the lexicon package)\n- Change any component rendering logic (only imports and type references)\n- Break existing functionality — if a type mismatch is too complex, add a `// TODO: update when SDK catches up` comment and use type assertion","acceptance_criteria":"1. `@hypercerts-org/lexicon` is at version `0.10.0-beta.15` in package.json\n2. All 5 files (`lib/utils.ts`, `lib/queries.ts`, `lib/types.ts`, `components/hypercerts-base-form.tsx`, `components/blob-display.tsx`) import from `@hypercerts-org/sdk-core` or `@hypercerts-org/lexicon` instead of `@/lexicons/types/...`\n3. `npx tsc --noEmit` passes (or only has pre-existing errors unrelated to this change)\n4. `npm run build` passes\n5. The `OrgHypercertsClaimActivity.Contributor` type is available via the upgraded package\n6. No runtime behavior changes — this is a pure import migration","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-23T18:07:47.633327042+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-23T18:28:17.958658229+06:00","closed_at":"2026-02-23T18:28:17.958658229+06:00","close_reason":"217f5a5 upgrade lexicon to beta.15 and migrate local imports","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.7","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:07:47.635070576+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-mrb.8","title":"Fix: non-DID contributors hidden during DID profile loading (from hypercerts-mrb.5)","description":"Review of hypercerts-mrb.5 found: In hypercert-contributors-section.tsx line 62, the entire content block is gated on `!isLoading`. When a list has both DID and non-DID contributors, the non-DID contributors (which need no async resolution) are hidden until all DID profile queries settle. This causes an unnecessary blank period for mixed lists. Fix: render non-DID contributors immediately; only show skeletons in place of DID contributors that are still loading. Evidence: lines 53-83 of components/hypercert-contributors-section.tsx.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:43:32.640870765+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:43:32.640870765+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.8","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:43:32.642577961+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.8","depends_on_id":"hypercerts-mrb.5","type":"discovered-from","created_at":"2026-02-23T18:43:36.792129427+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mrb.9","title":"Fix: missing error state in HypercertContributorsSection (from hypercerts-mrb.5)","description":"Review of hypercerts-mrb.5 found: hypercert-contributors-section.tsx has no error handling UI. The measurements section (hypercert-measurements-section.tsx lines 87-93) and evidence section (hypercert-evidence-section.tsx lines 82-88) both render a red error panel when queries fail. The contributors section silently swallows profile-resolution failures (by design in the hook), but if parseContributors throws or the component itself errors, there is no fallback. At minimum, add a consistent error state matching the other sections for parity. Evidence: compare hypercert-contributors-section.tsx (no error branch) vs hypercert-measurements-section.tsx lines 87-93.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-23T18:43:43.12924321+06:00","created_by":"kzoeps","updated_at":"2026-02-23T18:43:43.12924321+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-mrb.9","depends_on_id":"hypercerts-mrb","type":"parent-child","created_at":"2026-02-23T18:43:43.13117229+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-mrb.9","depends_on_id":"hypercerts-mrb.5","type":"discovered-from","created_at":"2026-02-23T18:43:47.181713984+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mt9","title":"Fix: Authenticate page H1 text invisible (white on light background) from hypercerts-nyk.10","description":"Review of hypercerts-nyk.10 found: The commit changed 'main.flex.flex-col.items-center \u003e h1.text-primary' color to #ffffff (white). This selector targets the Authenticate page, which has a light background (#fafafa). The H1 'Authenticate' text is now white on a light background — invisible to users. The spec intended to make text white only for the dark left panel on the sign-in page, but the selector used targets a different page entirely. Fix: either (a) remove the color override for this selector (the scoped .md:\\\\:bg-slate-100 .text-primary rule at line ~300 already handles the sign-in left panel), or (b) add a dark background to the Authenticate page main element. Evidence: git show 7669337 -- lib/atproto-branding.ts shows the change at line ~209-210. The Authenticate page background is #fafafa (line ~274, 282, 289 of atproto-branding.ts). File to fix: lib/atproto-branding.ts in branch karma/hyper-83-use-the-certified-branding-in-the-scaffold-app.","status":"open","priority":2,"issue_type":"bug","owner":"kzoeps","created_at":"2026-02-17T17:00:46.818177053+06:00","created_by":"kzoeps","updated_at":"2026-02-17T17:00:46.818177053+06:00","dependencies":[{"issue_id":"hypercerts-mt9","depends_on_id":"hypercerts-nyk.10","type":"discovered-from","created_at":"2026-02-17T17:00:50.990751787+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-mz1","title":"Fix: Stale JSDoc comments say 'rpc' scope is included but it isn't (from hypercerts-02l)","description":"Review of the OAuth scope migration (quick-fixes-test branch, epic hypercerts-02l) found stale JSDoc comments in lib/config.ts. Two comments still mention 'rpc' as part of the granular scope, but RPC_SCOPE was correctly removed from GRANULAR_SCOPE (per task hypercerts-02l.4). The comments are now misleading and will confuse future maintainers.\\n\\nEvidence:\\n- Line 76: '* - Production: uses granular scopes (repo, rpc, blob) for precise permission requests'\\n- Line 201: '* - Production: granular scopes (repo, rpc, blob) for precise permission requests'\\n\\nActual GRANULAR_SCOPE (lines 38-42) only contains: ATPROTO_SCOPE, HYPERCERT_REPO_SCOPE, BLOB_SCOPE — no rpc: scope.\\n\\nFix: Update both comments to remove 'rpc' from the list:\\n  '* - Production: uses granular scopes (repo, blob) for precise permission requests'","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-17T23:48:31.326185266+06:00","created_by":"kzoeps","updated_at":"2026-02-18T00:11:57.857454599+06:00","closed_at":"2026-02-18T00:11:57.857454599+06:00","close_reason":"510d690 cancelled: RPC_SCOPE being added back, JSDoc mentioning rpc is now correct","dependencies":[{"issue_id":"hypercerts-mz1","depends_on_id":"hypercerts-02l","type":"discovered-from","created_at":"2026-02-17T23:48:35.282622088+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-nax","title":"Fix: Contributors section count shows displayContributors.length before resolution — may show wrong count during Phase 1 (hypercert-contributors-section.tsx)","description":"## File\\ncomponents/hypercert-contributors-section.tsx lines 76-78\\n\\n## Severity\\nLOW — UX\\n\\n## Problem\\nThe contributor count in the section header always shows displayContributors.length (the raw parsed count before StrongRef resolution). This is correct for the final count, but during Phase 1 resolution, some contributors may have needsResolution=true and their identity is a placeholder AT URI, not a real DID. The count is accurate but the label may be confusing if some contributors fail to resolve (they would still be counted but shown with a placeholder identity).\\n\\nMore importantly, the count uses displayContributors.length (pre-resolution) while the rendered list uses resolvedContributors (post-resolution). If resolution fails for some contributors, the count and list length will match, but the displayed identities may be AT URIs instead of DIDs.\\n\\n## Evidence\\n```tsx\\n\u003cp className=\"text-muted-foreground ...\"\u003e\\n  {displayContributors.length}{' '}\\n  {displayContributors.length === 1 ? 'contributor' : 'contributors'}\\n\u003c/p\u003e\\n```\\n\\n## Fix\\nUse resolvedContributors.length for the count (post-resolution), or add a loading indicator next to the count during resolution:\\n```tsx\\n\u003cp className=\"text-muted-foreground ...\"\u003e\\n  {isResolvingIdentities ? '...' : resolvedContributors.length}{' '}\\n  {resolvedContributors.length === 1 ? 'contributor' : 'contributors'}\\n\u003c/p\u003e\\n```","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:09.535259772+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:09.535259772+06:00"}
{"id":"hypercerts-nfy","title":"Fix: isSaving \u0026\u0026 \u003cSpinner /\u003e patterns not converted to ternary in hypercerts-base-form.tsx (from hypercerts-dqf.8/dqf.9)","description":"## Files\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\nConvert 3 remaining `{isSaving \u0026\u0026 \u003cSpinner .../\u003e}` patterns to ternary.\n\n### Lines ~643, ~656, ~671:\n```tsx\n// Before:\n{isSaving \u0026\u0026 \u003cSpinner className=\"mr-2\" /\u003e}\n\n// After:\n{isSaving ? \u003cSpinner className=\"mr-2\" /\u003e : null}\n```\n\nDo this for all 3 occurrences. Search for `isSaving \u0026\u0026` to find them all.\n\n## Do not\n- Change any other code in the file\n- Change the Spinner props or className\n- Change any other conditional renders (already fixed by dqf.9/dqf.10)","acceptance_criteria":"1. All 3 isSaving \u0026\u0026 \u003cSpinner\u003e patterns are replaced with isSaving ? \u003cSpinner\u003e : null\n2. No \u0026\u0026 conditional rendering patterns remain in hypercerts-base-form.tsx\n3. Build passes: pnpm build exits 0\n4. Lint passes: pnpm lint exits 0","status":"closed","priority":4,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-17T23:50:37.348436282+06:00","created_by":"kzoeps","updated_at":"2026-02-17T23:56:22.49094935+06:00","closed_at":"2026-02-17T23:56:22.49094935+06:00","close_reason":"1c47e58 Fix: convert isSaving \u0026\u0026 \u003cSpinner\u003e to ternary","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nfy","depends_on_id":"hypercerts-dqf.9","type":"discovered-from","created_at":"2026-02-17T23:50:40.419629847+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-nfy","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:52:53.741724637+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk","title":"Epic: Add ATProto OAuth CSS branding to scaffold","description":"Port the custom OAuth branding implementation from maearth-app to hypercerts-scaffold. The scaffold uses the same PDS (certified.app) but currently serves bare client metadata with no branding. Goal: when users authenticate via the scaffold in production mode, the PDS OAuth pages show custom branding (logo, colors, 'Sign in with Certified' imagery) instead of default Hypercerts styling. The PDS itself stays neutral — each app injects its own branding via CSS in client metadata. Loopback mode cannot support branding (ATProto spec limitation) so branding only applies in production. Scope: create branding CSS module, update metadata endpoint, add placeholder assets. Out of scope: changing token_endpoint_auth_method, modifying the PDS itself.","status":"open","priority":2,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-16T12:29:31.349871338+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:57:58.566194364+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-nyk.1","title":"Create sanitizeUrlForCss utility function","description":"## Files\n- lib/atproto-branding.ts (create)\n\n## What to do\nCreate a new file `lib/atproto-branding.ts`. For now, only add the `sanitizeUrlForCss` function — the CSS generation function will be added in a later task.\n\nCopy the `sanitizeUrlForCss` function from the reference implementation at `maearth-app/src/lib/atproto-branding.ts` (lines 20-45). This function:\n\n1. Takes a URL string as input\n2. Validates it is a proper URL via `new URL()`\n3. Rejects non-http/https protocols\n4. Escapes dangerous CSS characters: backslash, single quote, double quote, closing paren, newlines, carriage returns\n5. Returns the sanitized href string\n\nExport the function (it will be used by `generateBrandingCss` in the next task).\n\n## Dont\n- Do not add the `generateBrandingCss` function yet (separate task)\n- Do not add any CSS strings\n- Do not modify any other files","acceptance_criteria":"1. lib/atproto-branding.ts exists and exports sanitizeUrlForCss\n2. Function throws on invalid URLs (not parseable by new URL())\n3. Function throws on non-http/https protocols (e.g. javascript:, data:)\n4. Function escapes: backslash, single quote, double quote, closing paren, newline, carriage return\n5. Function returns the sanitized URL href for valid http/https URLs\n6. TypeScript compiles without errors","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-16T12:29:44.660638545+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-16T12:36:02.551717428+06:00","closed_at":"2026-02-16T12:36:02.551717428+06:00","close_reason":"9e6630a Create sanitizeUrlForCss utility function","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.1","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-16T12:29:44.661946519+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.10","title":"Change PDS sign-in left panel background to dark navy (#0F2544) and adjust text contrast","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## What to do\nThe PDS OAuth sign-in page has a two-panel layout on desktop. The left panel contains the H1 heading (logo/branding area) and is currently styled with a light gray background (`#f0f0f3`). Change it to dark navy `#0F2544`.\n\n### 1. Left panel background (line ~270-274)\n```css\n/* Left panel styling */\n.md\\\\:bg-slate-100,\n.dark .md\\\\:bg-slate-100,\n.md\\\\:dark\\\\:bg-slate-800 {\n  background-color: #f0f0f3 !important;  /* CHANGE to #0F2544 */\n}\n```\n\n### 2. Text contrast in the left panel\nSince the background is now dark, any text rendered in the left panel needs to be light. The H1 text color is currently `#2b2d31` (dark). Update:\n\n- Line ~137: `.grid h1.text-primary::after` color → `#ffffff` (or remove if task .8 replaces this with an image — but set it to white as a fallback)\n- Line ~203: `main.flex.flex-col.items-center \u003e h1.text-primary` color → `#ffffff`\n\nAlso check the `.text-primary` override at line ~317-318:\n```css\n.text-primary {\n  color: #757780 !important;\n}\n```\nThis is a global override that affects text across ALL pages (sign-in, authorize, error). Since only the left panel is dark, scope the light text to the left panel only. Add a new rule:\n```css\n/* Light text for dark left panel */\n.md\\\\:bg-slate-100 .text-primary,\n.md\\\\:bg-slate-100 h1.text-primary {\n  color: #ffffff !important;\n}\n```\n\n### 3. Mobile background\nOn mobile the layout is single-column. The left panel content stacks on top. Check the mobile section (line ~690-727) — the `main` background is `#f8f8fa`. The mobile grid content container (`.grid.grow.content-center`) should also get the dark navy background:\n```css\n@media (max-width: 767px) {\n  .grid.grow.content-center {\n    background-color: #0F2544 !important;\n    padding: 24px !important;\n    border-radius: 12px !important;\n  }\n}\n```\n\n## Don't\n- Change the right panel background (form area) — it stays light\n- Change button colors or form input styling\n- Modify the authorize page or error page backgrounds\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. The left panel background on the PDS sign-in page is #0F2544 (dark navy)\n2. Text in the left panel (H1, any .text-primary elements) is white (#ffffff) for contrast\n3. The right panel (form area) background remains light (#fafafa)\n4. The global .text-primary color (#757780) is NOT changed — only scoped overrides for the dark panel\n5. Mobile layout has appropriate dark background for the heading area\n6. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T16:46:49.635564674+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:56:36.625245941+06:00","closed_at":"2026-02-17T16:56:36.625245941+06:00","close_reason":"7669337 Change PDS sign-in left panel background to dark navy and adjust text contrast","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.10","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T16:46:49.637638507+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.11","title":"Fix dark-on-dark text contrast in PDS sign-in dark panel","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## Context\nThe PDS sign-in page left panel background was changed to dark navy (`#0F2544`) in task .10. There is a small subtitle/label text right underneath the H1 heading (e.g. 'Enter your password' or 'Enter your identifier'). This text uses the class `text-slate-600` which the branding CSS globally overrides to `#4a4b53` (dark gray) at line ~327-334. On the dark navy panel, this dark text is invisible.\n\nThe form fields, inputs, and buttons in the right panel are fine — this is ONLY about the subtitle text in the dark left panel.\n\n## What to do\nAdd a scoped CSS override for the subtitle text inside the dark left panel container. The left panel container is `.md\\\\:bg-slate-100` on desktop.\n\nAdd this rule right after the existing scoped text override block (after line ~303):\n\n```css\n/* Subtitle text in dark left panel (e.g. 'Enter your password') */\n.md\\\\:bg-slate-100 .text-slate-600,\n.md\\\\:bg-slate-100 .text-slate-700,\n.md\\\\:bg-slate-100 .text-slate-400,\n.md\\\\:bg-slate-100 p {\n  color: rgba(255, 255, 255, 0.7) !important;\n}\n```\n\nUse `rgba(255, 255, 255, 0.7)` (70% white) so it reads as a muted subtitle, not as prominent as the H1 heading which is full `#ffffff`.\n\nAlso add the mobile equivalent inside the existing `@media (max-width: 767px)` block, scoped to `.grid.grow.content-center`:\n\n```css\n.grid.grow.content-center .text-slate-600,\n.grid.grow.content-center .text-slate-700,\n.grid.grow.content-center .text-slate-400,\n.grid.grow.content-center p {\n  color: rgba(255, 255, 255, 0.7) !important;\n}\n```\n\n## Don't\n- Change any global text color rules (lines ~319-334 must stay as-is)\n- Change the right panel (form area) styling\n- Change button colors or form input colors\n- Modify the dark panel background color (`#0F2544`)\n- Add overrides for form elements, inputs, or anything in the right panel\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. The subtitle text (e.g. 'Enter your password') below the H1 in the dark left panel is legible — light colored (rgba white ~70%)\n2. The H1 heading itself remains full white (#ffffff)\n3. The right panel form area text colors remain dark (unchanged)\n4. Global text color rules at lines ~319-334 are NOT modified\n5. Mobile layout subtitle text in the dark heading area is also light\n6. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-17T18:04:19.192441679+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:19:30.219680222+06:00","closed_at":"2026-02-17T18:19:30.219680222+06:00","close_reason":"02247ea fix dark-on-dark subtitle text contrast in PDS sign-in dark panel","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.11","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T18:04:19.193991709+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.12","title":"Replace certified-logo.svg with hypercerts_logo_horizontal.svg for the logo above H1 heading in PDS branding CSS","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## Context\nThe logo displayed above the H1 heading on the PDS sign-in page currently uses `certified-logo.svg` (the small C checkmark icon). It should instead use `hypercerts_logo_horizontal.svg` — the full horizontal Hypercerts wordmark with badge icon (1929x340px, black fill).\n\nNote: `certified-logo.svg` is STILL correct for the submit button icon (the small C next to 'Sign in with Certified' text). This task ONLY changes the large logo displayed above the heading.\n\n## What to do\n\n### 1. Add a new URL variable for the horizontal logo\nAt the top of `generateBrandingCss()`, alongside the existing `logoUrl` and `signinUrl`, add:\n```typescript\nconst horizontalLogoUrl = \\`\\${baseUrl}/hypercerts_logo_horizontal.svg\\`;\nconst sanitizedHorizontalLogoUrl = sanitizeUrlForCss(horizontalLogoUrl);\n```\n\n### 2. Update the logo replacement section (lines ~85-109)\nChange `background-image: url('${sanitizedLogoUrl}')` to `background-image: url('${sanitizedHorizontalLogoUrl}')`.\n\nSince the horizontal logo is wide (1929:340 ≈ 5.67:1 aspect ratio), revert the container dimensions back to wide rectangles:\n- Desktop: `width: 200px; height: 40px` (was 54x54)\n- Mobile: `width: 150px; height: 30px` (was 40x40)\n\n### 3. Update the Authenticate page logo (lines ~187-206)\nChange `background-image: url('${sanitizedLogoUrl}')` to `background-image: url('${sanitizedHorizontalLogoUrl}')`.\n- Desktop: `width: 200px; height: 40px` (was 54x54 from task .7)\n- Mobile: `width: 150px; height: 30px` (was 42x42)\n\n### 4. SVG visibility on dark background\nThe SVG has `fill: #000` (black). On the dark navy panel (`#0F2544`), black is invisible. Apply a CSS filter to invert the logo to white:\n```css\nimg[alt=\"Hypercerts Logo\"],\nimg[alt*=\"Logo\"] {\n  /* ... existing styles ... */\n  filter: invert(1) !important;\n}\n```\nFor the `::before` pseudo-element on the Authenticate page, also add `filter: invert(1)`.\n\n### 5. Do NOT change the button logo\nThe `sanitizedLogoUrl` (`certified-logo.svg`) is still used for the submit button `::before` icon at lines ~635-644. Leave that unchanged.\n\n## Don't\n- Change the submit button logo (keep `certified-logo.svg` for buttons)\n- Change the `certified-signin.svg` H1 image replacement\n- Modify the `logoUrl` variable itself — add a NEW `horizontalLogoUrl` variable\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. The logo above the H1 heading uses `hypercerts_logo_horizontal.svg` (not `certified-logo.svg`)\n2. The submit button icon still uses `certified-logo.svg` (the small C)\n3. The horizontal logo is visible (white/light) on the dark navy panel — uses CSS filter or equivalent\n4. Logo container dimensions match the wide aspect ratio (~5.67:1) of the horizontal SVG\n5. Both desktop and mobile logo sizes are appropriate\n6. The Authenticate page logo also uses the horizontal logo\n7. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T18:06:56.176917231+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:20:09.712838205+06:00","closed_at":"2026-02-17T18:20:09.712838205+06:00","close_reason":"43a6bab use hypercerts_logo_horizontal.svg for logo above H1 heading","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.12","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T18:06:56.179266145+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.13","title":"Retheme PDS branding CSS to flow with PDS design system instead of fighting it","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## Context\nThe current branding CSS forces `color-scheme: light only` and overrides nearly every color class with hardcoded values and `!important`. This fights the PDS's own design system and causes issues like dark-on-dark text when we set the left panel to a dark color.\n\nThe PDS layout uses hardcoded Tailwind `slate-*` classes (NOT the `--color-contrast-*` scale) for the sign-in page. The left panel is `md:bg-slate-100` (`#f1f5f9`), the right panel inherits `bg-white`. The `--branding-color-primary` variable controls `text-primary` (H1 title), `bg-primary` (buttons), and the contrast scale hue for nav/footer/cards.\n\n## What to do\nRefactor `generateBrandingCss()` to work WITH the PDS instead of against it. The goal: set the right CSS variables so the PDS derives a navy-blue-tinted theme naturally, then only add targeted overrides for things the PDS can't do on its own.\n\n### 1. Set branding CSS variables to navy theme\nReplace the current `:root` block (lines ~68-83) with:\n```css\n:root {\n  --branding-color-primary: 15 37 68;\n  --branding-color-primary-contrast: 255 255 255;\n  --branding-color-primary-hue: 216;\n  --branding-color-error: 229 72 77;\n  --branding-color-error-contrast: 255 255 255;\n  --branding-color-error-hue: 358;\n  --branding-color-warning: 245 159 0;\n  --branding-color-warning-contrast: 43 45 49;\n  --branding-color-warning-hue: 38;\n  --branding-color-success: 43 138 62;\n  --branding-color-success-contrast: 255 255 255;\n  --branding-color-success-hue: 134;\n}\n```\nThis makes:\n- `text-primary` / `bg-primary` = dark navy (`#0F2544`)\n- `text-primary-contrast` = white\n- The contrast scale gets a subtle navy-blue tint (hue 216)\n- Buttons become navy instead of charcoal\n\n### 2. Remove `color-scheme: light only !important`\nDelete the `color-scheme` forcing. Let the PDS respect the user's system preference or its own defaults.\n\n### 3. Left panel dark override (KEEP)\nKeep the `md:bg-slate-100` override to `#0F2544` — this is necessary because the PDS hardcodes `slate-100` for the left panel.\n\n### 4. Left panel text overrides (KEEP)\nKeep the scoped white text rules for `.md\\\\:bg-slate-100` — these are necessary for the dark panel.\n\n### 5. REMOVE most global color overrides\nDelete the following sections that fight the PDS's native styling:\n- `/* FORCE LIGHT COLOR SCHEME */` — remove `color-scheme` forcing\n- `/* MAIN BACKGROUNDS */` — remove body, `.bg-white`, `.dark .bg-white` overrides. Let the PDS handle these.\n- `/* TEXT COLORS - LIGHT THEME */` — remove `.text-slate-900`, `.text-slate-600`, etc. overrides. The PDS's own Tailwind slate colors are fine.\n- `/* ACCENT/PRIMARY COLORS */` — remove `.text-primary`, `.bg-primary`, `.border-primary` overrides. These now come from the CSS variables.\n- `/* FORM INPUTS */` — remove input/textarea/select overrides. Let PDS style them.\n- `/* USERNAME PREVIEW \u0026 PASSWORD STRENGTH */` — remove `.bg-gray-200`, `.bg-gray-300`, text-gray-* overrides.\n- `/* ACCOUNT SELECTION \u0026 FORM ITEMS */` — remove `.bg-gray-100` overrides.\n- `/* CHECKBOXES */` — remove. PDS handles these via `accent-color` from primary.\n- `/* INFO/WARNING/ERROR BOXES */` — remove `.bg-contrast-*` overrides. The contrast scale now has the right hue.\n- `/* BUTTONS */` — remove the generic button color overrides (`.bg-primary`, hover states). Keep ONLY the button text/icon replacement rules (the `::before` and `::after` for 'Sign in with Certified').\n- `/* LINKS */` — remove. Let PDS handle link colors.\n- `/* FORM TEXT ELEMENTS */` — remove.\n- `/* DROPDOWN/SELECT */` — remove.\n- `/* ERROR PAGE */` — remove `.text-4xl`, `.text-5xl` overrides.\n- `/* Input container */` overrides — remove all the `.rounded-lg` input container styling.\n- `/* Text that should be dark on light backgrounds */` — remove the `.text-white` override (line ~444) which forces `.text-white` to dark. This was needed when forcing light mode but is harmful now.\n\n### 6. KEEP these sections\n- Logo replacement (`img[alt=\"Hypercerts Logo\"]`) — PDS can't do this natively\n- H1 text replacement with `certified-signin.svg` — PDS can't do this\n- Button text/icon replacement (`::before`/`::after`) — PDS can't do this\n- Left panel dark background + scoped light text\n- Mobile dark heading area\n- Authenticate page logo and button replacements\n- Error page logo suppression\n- Desktop/mobile alignment rules\n\n### 7. Secondary button overrides for the submit button\nThe submit button `button[type=\"submit\"]` styling can be simplified. Since `bg-primary` now resolves to navy via the CSS variable, the button will naturally be navy. Keep only the text/icon replacement (`font-size: 0`, `::before`, `::after`).\n\n### 8. Right panel lighter shade\nThe right panel (`bg-white`) will stay white by default. If you want it to be a lighter shade of navy instead of pure white, add:\n```css\n.bg-white,\n.dark .bg-white {\n  background-color: hsl(216 20% 97%) !important;\n}\n```\nThis gives a very subtle navy-tinted off-white that flows with the left panel. But this is optional — pure white also works.\n\n## Don't\n- Remove the logo/image replacement CSS\n- Remove the button text replacement CSS\n- Remove the left panel dark background override\n- Change the SVG asset URLs\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. The `:root` block sets `--branding-color-primary: 15 37 68` and `--branding-color-primary-hue: 216`\n2. No `color-scheme: light only` forcing remains\n3. The left panel is still dark navy (`#0F2544`) with white text\n4. The right panel uses the PDS's native light styling (no hardcoded overrides)\n5. Buttons are navy (derived from primary variable) with the C logo + 'Sign in with Certified' text\n6. The H1 still shows `certified-signin.svg` image\n7. The horizontal logo still shows above the H1 with `filter: invert(1)`\n8. Form inputs, text, links on the right panel look correct (PDS native styling)\n9. The generated CSS is significantly shorter than before (most global overrides removed)\n10. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":60,"created_at":"2026-02-17T18:53:05.142415492+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:58:49.42997752+06:00","closed_at":"2026-02-17T18:58:49.42997752+06:00","close_reason":"ce6a9cf refactor: retheme PDS branding CSS to flow with PDS design system","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-nyk.13","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T18:53:05.145413066+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-nyk.13","depends_on_id":"hypercerts-nyk.14","type":"blocks","created_at":"2026-02-17T18:53:28.893634687+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.14","title":"Align horizontal logo to the left in PDS branding CSS","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## What to do\nThe horizontal Hypercerts logo above the H1 heading is currently right-aligned on desktop. It should be left-aligned to match the `certified-signin.svg` heading image alignment.\n\n### 1. Desktop logo alignment (line ~96)\nChange `background-position: right center` to `background-position: left center`:\n```css\nimg[alt=\"Hypercerts Logo\"],\nimg[alt*=\"Logo\"] {\n  /* ... */\n  background-position: left center !important;  /* was: right center */\n}\n```\n\n### 2. Desktop alignment section (line ~780-782)\nThe desktop alignment section forces `.grid.grow.content-center` to `justify-items: end`. Change to `justify-items: start`:\n```css\n@media (min-width: 768px) {\n  .grid.grow.content-center {\n    justify-items: start !important;  /* was: end */\n  }\n}\n```\n\n### 3. H1 image alignment (line ~146)\nThe `certified-signin.svg` background-position is `right center`. Change to `left center`:\n```css\n.grid h1.text-primary::after {\n  /* ... */\n  background-position: left center !important;  /* was: right center */\n}\n```\n\n### 4. Desktop text alignment (line ~785-787)\nChange `.md\\\\:text-right` override from `text-align: right` to `text-align: left`:\n```css\n.md\\\\:text-right {\n  text-align: left !important;  /* was: right */\n}\n```\n\n## Don't\n- Change mobile alignment (mobile is already left-aligned)\n- Change the Authenticate page logo alignment (that's centered, which is correct)\n- Change any colors or dimensions\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. The horizontal logo above the H1 is left-aligned on desktop\n2. The certified-signin.svg H1 image is left-aligned on desktop\n3. Mobile alignment remains left-aligned (unchanged)\n4. Authenticate page logo remains centered (unchanged)\n5. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T18:53:22.281135938+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:56:21.811918955+06:00","closed_at":"2026-02-17T18:56:21.811918955+06:00","close_reason":"9a98af1 align horizontal logo and H1 to left on desktop","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.14","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T18:53:22.283093746+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.15","title":"Remove forced dark left panel — let PDS derive panel colors naturally for both light and dark mode","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## Context\nThe branding CSS currently forces the left panel to dark navy (`#0F2544`) in ALL modes. This works great in dark mode but clashes in light mode. We want:\n\n- **Light mode**: PDS native light panel. Buttons stay dark navy. Light variant of certified-signin SVG.\n- **Dark mode**: Dark navy (`#0F2544`) panel (our brand color). Buttons need to pop (brighter blue). Dark variant of certified-signin SVG (existing).\n\nTwo SVG variants are now available:\n- `/public/certified-signin.svg` — white text, for dark backgrounds\n- `/public/certified-signin-light.svg` — dark navy text (`#0F2544`), for light backgrounds\n\n## What to do\n\n### 1. Scope the left panel dark override to dark mode ONLY\nReplace the current unconditional override (lines ~183-189):\n```css\n/* REPLACE THIS */\n.md\\\\:bg-slate-100,\n.dark .md\\\\:bg-slate-100,\n.md\\\\:dark\\\\:bg-slate-800 {\n  background-color: #0F2544 !important;\n}\n```\nWith a dark-mode-only override:\n```css\n@media (prefers-color-scheme: dark) {\n  .md\\\\:bg-slate-100,\n  .md\\\\:dark\\\\:bg-slate-800 {\n    background-color: #0F2544 !important;\n  }\n}\n```\nIn light mode, the PDS native `slate-100` (`#f1f5f9`) takes over.\n\n### 2. Scope white text overrides to dark mode ONLY\nMove the existing scoped text overrides (lines ~191-203) inside a `@media (prefers-color-scheme: dark)` block:\n```css\n@media (prefers-color-scheme: dark) {\n  .md\\\\:bg-slate-100 .text-primary,\n  .md\\\\:bg-slate-100 h1.text-primary {\n    color: #ffffff !important;\n  }\n  .md\\\\:bg-slate-100 .text-slate-600,\n  .md\\\\:bg-slate-100 .text-slate-700,\n  .md\\\\:bg-slate-100 .text-slate-400,\n  .md\\\\:bg-slate-100 p {\n    color: rgba(255, 255, 255, 0.7) !important;\n  }\n}\n```\n\n### 3. Make logo `filter: invert(1)` dark-mode only\nRemove `filter: invert(1)` from the base logo rules (lines ~100, ~223). Add inside a dark mode media query:\n```css\n@media (prefers-color-scheme: dark) {\n  img[alt=\"Hypercerts Logo\"],\n  img[alt*=\"Logo\"] {\n    filter: invert(1) !important;\n  }\n  main.flex.flex-col.items-center \u003e h1.text-primary::before {\n    filter: invert(1) !important;\n  }\n}\n```\n\n### 4. Use light/dark certified-signin SVG variants\nAdd a new URL variable:\n```typescript\nconst signinLightUrl = \\`\\${baseUrl}/certified-signin-light.svg\\`;\nconst sanitizedSigninLightUrl = sanitizeUrlForCss(signinLightUrl);\n```\n\nUse the light variant as the default H1 image (for light mode):\n```css\n.grid h1.text-primary::after {\n  background-image: url('${sanitizedSigninLightUrl}') !important;\n}\n```\n\nOverride with the dark variant in dark mode:\n```css\n@media (prefers-color-scheme: dark) {\n  .grid h1.text-primary::after {\n    background-image: url('${sanitizedSigninUrl}') !important;\n  }\n}\n```\n\n### 5. Dark mode button visibility\nButtons are `bg-primary` = `#0F2544` which blends into the dark background. Add a dark-mode override:\n```css\n@media (prefers-color-scheme: dark) {\n  button[type=\"submit\"].bg-primary,\n  button.bg-primary,\n  [role=\"button\"].bg-primary {\n    background-color: #3B82F6 !important;\n  }\n  button[type=\"submit\"].bg-primary:hover,\n  button.bg-primary:hover,\n  [role=\"button\"].bg-primary:hover {\n    background-color: #2563EB !important;\n  }\n}\n```\n\n### 6. Scope mobile dark heading to dark mode only\nMove the mobile dark background from unconditional to dark-mode-only:\n```css\n@media (prefers-color-scheme: dark) and (max-width: 767px) {\n  .grid.grow.content-center {\n    background-color: #0F2544 !important;\n    padding: 24px !important;\n    border-radius: 12px !important;\n  }\n  .grid.grow.content-center .text-slate-600,\n  .grid.grow.content-center .text-slate-700,\n  .grid.grow.content-center .text-slate-400,\n  .grid.grow.content-center p {\n    color: rgba(255, 255, 255, 0.7) !important;\n  }\n}\n```\nIn light mode mobile, remove the dark background/text overrides entirely.\n\n### 7. Authenticate page H1 color — dark mode only\nMove `color: #ffffff` to dark mode:\n```css\nmain.flex.flex-col.items-center \u003e h1.text-primary {\n  /* no color override — PDS handles it */\n  font-size: 1.5rem !important;\n  line-height: 2rem !important;\n}\n@media (prefers-color-scheme: dark) {\n  main.flex.flex-col.items-center \u003e h1.text-primary {\n    color: #ffffff !important;\n  }\n}\n```\n\n## Don't\n- Remove the logo/image replacement CSS structure\n- Remove the button text/icon replacement CSS (`::before`/`::after`)\n- Change the `:root` CSS variables\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. Light mode: left panel is PDS native light blue-gray (slate-100), NOT dark navy\n2. Dark mode: left panel is dark navy (#0F2544)\n3. Light mode: H1 shows certified-signin-light.svg (dark navy text on light bg)\n4. Dark mode: H1 shows certified-signin.svg (white text on dark bg)\n5. Light mode: logo is black (no filter), visible on light panel\n6. Dark mode: logo is white (filter: invert), visible on dark panel\n7. Light mode: buttons are dark navy (from primary variable)\n8. Dark mode: buttons are bright blue (#3B82F6) that pops against dark background\n9. Mobile heading area is only dark in dark mode\n10. Button text/icon replacement (::before/::after) works in both modes\n11. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T19:16:37.98413166+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:28:21.933392724+06:00","closed_at":"2026-02-17T19:28:21.933392724+06:00","close_reason":"5b1f9da scope left panel dark override and logo invert to dark mode only","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.15","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T19:16:37.985831116+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.16","title":"Clean up PDS branding CSS: fix broad selector, remove dead code, consolidate dark mode blocks","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## What to do\nAddress 6 CSS quality issues found during review. Bundle them into a single cleanup pass.\n\n### 1. Fix overly broad `img[alt*=\"Logo\"]` selector (P3 — hypercerts-5jk)\nThe substring match `alt*=\"Logo\"` could accidentally affect any image with 'Logo' in its alt text on PDS pages. Change to exact match only:\n```css\n/* BEFORE */\nimg[alt=\"Hypercerts Logo\"],\nimg[alt*=\"Logo\"] {\n\n/* AFTER */\nimg[alt=\"Hypercerts Logo\"] {\n```\nRemove the `img[alt*=\"Logo\"]` selector everywhere it appears (base rule, mobile rule, and the dark mode `filter: invert` rule). The exact `img[alt=\"Hypercerts Logo\"]` match is sufficient.\n\n### 2. Remove dead `color: #ffffff` on `::after` (P3 — hypercerts-jgh)\nThe `.grid h1.text-primary::after` has `color: #ffffff !important` but `content: \"\"` — no text to color. Remove the dead property:\n```css\n.grid h1.text-primary::after {\n  content: \"\" !important;\n  /* ... */\n  /* color: #ffffff !important;  \u003c-- DELETE */\n}\n```\n\n### 3. Consolidate dark mode blocks (P4 — hypercerts-c3t)\nThere are 5 scattered `@media (prefers-color-scheme: dark)` blocks throughout the file. Merge them into a single block at the end of the CSS (before the closing backtick). This makes dark mode behavior easy to find and maintain.\n\nThe consolidated block should contain ALL dark-mode-only rules:\n- Left panel background override (`#0F2544`)\n- Left panel white text overrides\n- Left panel subtitle text overrides\n- Logo `filter: invert(1)`\n- Authenticate page logo `filter: invert(1)`\n- Authenticate page H1 `color: #ffffff`\n- H1 `certified-signin.svg` (dark variant) override\n- Button bright blue override (`#3B82F6`)\n- Mobile dark heading (combined with `max-width: 767px`)\n\n### 4. Remove unnecessary `position: relative` (P4 — hypercerts-84k)\nThe `main.flex.flex-col.items-center \u003e h1.text-primary { position: relative !important; }` rule is unnecessary — the `::before` pseudo-element is block-flow, not absolutely positioned. Remove it.\n\n### 5. Remove redundant `width: 32px` override (P4 — hypercerts-ua9)\nThe `div.w-8` container is already 32px in Tailwind. The override `width: 32px !important` is a no-op. Also remove the dead `object-position: left center` on `img[alt=\"Ma Earth\"]` (32x32 square, object-position has no effect with `object-fit: contain`).\n\n### 6. Remove redundant mobile `background-position` (P4 — hypercerts-c7v)\nThe mobile override repeats `background-position: left center` which is already set in the base rule. Remove the redundant line from the mobile media query.\n\n## Don't\n- Change any functional behavior (colors, sizes, images, selectors that work correctly)\n- Change the `:root` CSS variables\n- Change SVG asset URLs\n- Reorder rules in a way that changes specificity/cascade behavior\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. No `img[alt*=\"Logo\"]` substring selector remains — only exact `img[alt=\"Hypercerts Logo\"]`\n2. No dead `color` property on empty `content: \"\"` pseudo-elements\n3. At most 2 `@media (prefers-color-scheme: dark)` blocks in the entire CSS (one for general dark mode, one combined with max-width for mobile)\n4. No `position: relative` on the Authenticate page H1\n5. No redundant `width: 32px` on `div.w-8` container\n6. No redundant `background-position` in mobile override\n7. All existing visual behavior is preserved (light mode and dark mode look identical before and after)\n8. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-17T19:53:27.001182526+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:56:02.98746847+06:00","closed_at":"2026-02-17T19:56:02.98746847+06:00","close_reason":"0aeb7a0 Clean up PDS branding CSS: fix broad selector, remove dead code, consolidate dark mode blocks","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.16","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T19:53:27.003120929+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.17","title":"Remove Ma Earth leftovers and fix stale comments in branding CSS","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## What to do\nClean up leftover references from the Ma Earth app port and fix stale comments.\n\n### 1. Remove or fix Ma Earth selectors (lines ~115-127)\nThe `img[alt=\"Ma Earth\"]` selector is a leftover from the Ma Earth app. The Hypercerts PDS Authorize page uses a different alt text. Either:\n- If the Authorize page uses `img[alt=\"Hypercerts Logo\"]` (same as the sign-in page), then the existing logo replacement rule already covers it — DELETE the entire Ma Earth section (lines 115-127).\n- If the Authorize page uses a different alt text, update the selector to match.\n\nMost likely the Ma Earth section is dead code since the scaffold app is not Ma Earth. **Delete lines 115-127 entirely.**\n\n### 2. Fix stale comments\n- Line 93: `Replace Hypercerts logo with horizontal Hypercerts wordmark` → `Replace default PDS logo with Hypercerts horizontal wordmark`\n- Line 130: `Replace \"Sign in with Hypercerts\" with certified-signin SVG image` → `Replace \"Sign in with Hypercerts\" heading with Certified sign-in SVG image`\n- Line 184: `Add Scaffold logo above the H1 using ::before` → `Add Hypercerts wordmark above the Authenticate H1 using ::before`\n- Line 219: `Replace \"Sign in with Hypercerts\" button text on Authenticate page` → `Replace \"Sign in with Hypercerts\" button text with Certified branding on Authenticate page`\n- Line 74: `Hypercerts Scaffold Custom Branding` → `Certified Custom Branding for PDS OAuth Pages`\n\n## Don't\n- Change any CSS selectors, properties, or values (other than removing the Ma Earth block)\n- Change SVG URLs or CSS variables\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. No references to 'Ma Earth' remain in the file\n2. Comments accurately describe what the CSS does (reference 'Certified' branding, not generic 'Scaffold')\n3. No functional CSS behavior changes\n4. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T20:08:32.524067658+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T20:10:09.061239754+06:00","closed_at":"2026-02-17T20:10:09.061239754+06:00","close_reason":"66e4025 Remove Ma Earth leftovers and fix stale comments in branding CSS","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.17","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T20:08:32.525896579+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.18","title":"Slim down PDS branding CSS: consolidate duplicates, remove redundant sections, prune media queries","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## Context\nCSS review found the branding CSS has ~328 lines of CSS but could be ~190 with zero visual change. This task consolidates duplicates, removes sections that are redundant with the PDS's own styling, and prunes unnecessary media queries.\n\n## What to do\n\n### 1. Consolidate duplicate button replacement blocks (~28 lines saved)\nThe Authenticate page button (lines ~207-232) and submit button (lines ~236-261) are character-for-character identical except for the selector. Merge them by comma-separating selectors:\n\n```css\n/* Button text/icon replacement — both sign-in page submit and Authenticate page button */\nbutton[type=\"submit\"].bg-primary,\nmain.flex.flex-col.items-center \u003e button:nth-of-type(2) {\n  font-size: 0 !important;\n  line-height: 0 !important;\n  display: flex !important;\n  align-items: center !important;\n  justify-content: center !important;\n  gap: 8px !important;\n}\n\nbutton[type=\"submit\"].bg-primary::before,\nmain.flex.flex-col.items-center \u003e button:nth-of-type(2)::before {\n  content: \"\" !important;\n  display: inline-block !important;\n  width: 20px !important;\n  height: 22px !important;\n  background-image: url('${sanitizedLogoUrl}') !important;\n  background-size: contain !important;\n  background-repeat: no-repeat !important;\n  background-position: center !important;\n  flex-shrink: 0 !important;\n}\n\nbutton[type=\"submit\"].bg-primary::after,\nmain.flex.flex-col.items-center \u003e button:nth-of-type(2)::after {\n  content: \"Sign in with Certified\" !important;\n  font-size: 1rem !important;\n  line-height: 1.5rem !important;\n}\n```\n\n### 2. Remove error page section entirely (~20 lines saved)\nDelete lines ~263-282 (the entire `ERROR/SESSION EXPIRY PAGE` section). The `:root` CSS variables already set `--branding-color-error` which the PDS uses to style error pages. The font-size and font-weight overrides are redundant with PDS defaults.\n\nThe `::before { display: none }` rule (which hides the logo on error pages) is only needed because the Authenticate page `::before` selector (`main.flex.flex-col.items-center \u003e h1.text-primary::before`) is broad enough to match error pages too. To fix this properly, make the Authenticate page `::before` selector more specific by excluding error pages:\n\n```css\nmain.flex.flex-col.items-center:not(:has([role=\"alert\"])) \u003e h1.text-primary::before {\n  /* ... existing ::before logo rules ... */\n}\n```\n\nThis way the logo only appears on the Authenticate page, not on error pages, and we don't need the separate error page `display: none` rule.\n\n### 3. Remove mobile fixes section (~33 lines saved)\nDelete lines ~284-317 (the entire `MOBILE SPECIFIC FIXES` section). This contains:\n- `.grid.grow.content-center` justify-items/gap — cosmetic spacing\n- Logo margin-bottom — cosmetic\n- H1 margin adjustments — cosmetic\n- Language selector repositioning — over-engineering (PDS already positions it)\n- Header container `position: relative` — only needed for the language selector hack above\n\nKeep ONLY the `.grid.grow.content-center { justify-items: start }` rule but move it into the desktop alignment section (it's needed for left-alignment consistency).\n\n### 4. Prune H1 media queries (~20 lines saved)\nThe H1 SVG image has 3 breakpoints (default 280x98, 768px 334x117, 1024px 400x140). The SVG scales naturally with `background-size: contain` and `max-width: 100%`. Keep only one desktop override:\n\nDelete the `@media (min-width: 1024px)` block entirely (lines ~160-165).\n\nSimplify the `@media (min-width: 768px)` block — keep the H1 `::after` size override but remove the H1 `margin-top` override (cosmetic).\n\nAlso delete the mobile `@media (max-width: 767px)` block for H1 spacing (lines ~140-145) — cosmetic margin/padding.\n\n### 5. Drop Authenticate page font-size overrides (~12 lines saved)\nDelete lines ~192-203 — the `main.flex.flex-col.items-center \u003e h1.text-primary` font-size/line-height rules and their desktop media query. The PDS already styles its H1 elements. Keep only the `::before` logo insertion.\n\n### 6. Simplify dark mode mobile block (~14 lines saved)\nDelete the `@media (prefers-color-scheme: dark) and (max-width: 767px)` block entirely (lines ~388-402). The dark navy background, padding, and border-radius on mobile are cosmetic polish. The dark mode text overrides for the mobile heading area are also unnecessary if we remove the mobile dark background — the PDS handles dark mode text natively.\n\n### 7. Drop error/warning/success from :root (~6 lines saved)\nThe PDS has sensible defaults for error, warning, and success colors. Only the primary color variables are needed for branding. Delete lines ~83-89 (the error, warning, success variable groups).\n\n## Don't\n- Change any visual behavior that's currently working (logo replacement, H1 image, button text, dark mode panel)\n- Remove the `:root` primary color variables\n- Remove the dark mode block (the main one at lines ~332-386)\n- Remove the desktop alignment section\n- Change SVG asset URLs\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. Button replacement CSS appears only ONCE (consolidated selectors), not duplicated\n2. No error page section exists — the Authenticate `::before` selector excludes error pages via `:not(:has([role=\"alert\"]))`\n3. No mobile fixes section (language selector repositioning, header container positioning removed)\n4. At most ONE desktop media query for H1 sizing (no 1024px breakpoint, no mobile spacing query)\n5. No Authenticate page font-size overrides\n6. No dark mode + mobile combined media query block\n7. Only primary color variables in `:root` (no error/warning/success)\n8. Total CSS inside the template literal is under 200 lines\n9. All existing visual behavior preserved: logo replacement, H1 image (light/dark), button text/icon, dark mode panel, desktop left-alignment\n10. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-17T20:49:43.082641677+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T21:03:39.944790522+06:00","closed_at":"2026-02-17T21:03:39.944790522+06:00","close_reason":"7533c19 slim down PDS branding CSS: consolidate duplicates, remove redundant sections","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.18","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T20:49:43.084620156+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.19","title":"Remove ngrok workaround from DEVELOPMENT.md and clarify certified PDS + trusted client requirement for branding","description":"## Files\n- DEVELOPMENT.md (modify)\n\n## What to do\nRewrite the 'Testing OAuth Branding Locally' section (lines 341-399) and remove the 'ngrok Issues' troubleshooting section (lines 412-419).\n\n### 1. Remove the ngrok workaround entirely\nDelete the entire 'The ngrok workaround:' subsection (lines 351-385). This includes:\n- The ngrok install instructions\n- The 6-step ngrok tunnel setup\n- The paragraph about verifying metadata via ngrok URL\n- The 'Important:' paragraph about PDS not rendering branding for untrusted ngrok URLs\n- The 'Note:' about CORS/allowedDevOrigins for ngrok\n\n### 2. Remove the 'ngrok Issues' troubleshooting section\nDelete lines 412-419 (the entire 'ngrok Issues' subsection under 'Common Issues \u0026 Troubleshooting'). Since we're removing the ngrok workaround, this troubleshooting section is no longer relevant.\n\n### 3. Rewrite the section header and intro\nReplace the current 'Testing OAuth Branding Locally' header and intro with a section titled 'OAuth Branding' that clearly states:\n\n**OAuth branding (custom logo, colors, CSS on the PDS sign-in pages) only works when BOTH conditions are met:**\n\n1. **The PDS is a certified PDS** — branding is a feature of certified PDS instances (e.g. `pds-eu-west4.test.certified.app`). Standard/uncertified PDS instances do not support custom OAuth branding.\n2. **Your app's client URL has been added to the PDS's trusted OAuth clients list** — the PDS only applies branding CSS for client URLs registered in its `PDS_OAUTH_TRUSTED_CLIENTS` environment variable. Even if your app serves correct branding metadata at `/client-metadata.json`, the PDS will ignore it unless your URL is explicitly trusted.\n\n### 4. Keep the loopback limitation explanation\nKeep the existing paragraph about ATProto loopback mode limitation (line 347-349) — it's still correct. Reword slightly to fit the new section structure:\n\n'Additionally, branding cannot work in local development (loopback mode). When using a loopback `client_id` (localhost/127.0.0.1), the PDS auto-generates minimal client metadata and ignores custom branding fields. This is part of the ATProto specification.'\n\n### 5. Keep 'Getting your domain trusted by the PDS' subsection\nKeep lines 387-395 as-is. This is still the correct process.\n\n### 6. Simplify the Vercel deployments note\nKeep the Vercel note (lines 397-399) but simplify it:\n\n'**Vercel deployments:** Your Vercel production URL must also be added to the PDS trusted clients list for branding to appear. The app serves branding metadata automatically — the only requirement is that the URL is trusted by the PDS.'\n\n### Final structure should be:\n```\n### OAuth Branding\n\n\u003ctwo conditions paragraph\u003e\n\n\u003cloopback limitation paragraph\u003e\n\n#### Getting your domain trusted by the PDS\n\u003cexisting content\u003e\n\n**Vercel deployments:**\n\u003csimplified note\u003e\n```\n\n## Don't\n- Modify any code files\n- Remove the 'Getting your domain trusted by the PDS' subsection\n- Change the loopback limitation explanation (just reword to fit)\n- Add any new ngrok instructions or references","acceptance_criteria":"1. The ngrok workaround section (install, tunnel setup, 6 steps) is completely removed from DEVELOPMENT.md\n2. The 'ngrok Issues' troubleshooting section is completely removed\n3. The section clearly states branding requires BOTH: (a) a certified PDS and (b) the app URL being in the trusted OAuth clients list\n4. The ATProto loopback limitation is still explained\n5. The 'Getting your domain trusted by the PDS' subsection is preserved\n6. The Vercel note is simplified but present\n7. No references to ngrok remain anywhere in the file\n8. No code files are modified\n9. The markdown renders correctly (no broken formatting)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-17T22:54:38.78423021+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:06:38.277961093+06:00","closed_at":"2026-02-17T23:06:38.277961093+06:00","close_reason":"9836495 docs: remove ngrok workaround, clarify certified PDS + trusted client requirement","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.19","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T22:54:38.785988145+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.2","title":"Create generateBrandingCss function with neutral scaffold theme","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## What to do\nAdd the `generateBrandingCss(baseUrl: string): string` function to `lib/atproto-branding.ts`. This is the main function that generates ~700 lines of CSS injected into PDS OAuth pages.\n\n**Reference:** Copy the structure from `maearth-app/src/lib/atproto-branding.ts` `generateBrandingCss()` (lines 47-730).\n\n**Function signature:** `export function generateBrandingCss(baseUrl: string): string`\n\n**Parameter:** `baseUrl` — the app base URL (e.g. `https://hypercerts-scaffold.vercel.app`). Used to construct image URLs for logo and branding assets.\n\n**Image URLs to construct (using sanitizeUrlForCss):**\n- Logo: `${baseUrl}/favicon.ico` (placeholder — will be replaced with proper logo later)\n\n**Color mapping — replace Ma Earth colors with scaffold neutral theme:**\n\n| Ma Earth (source) | Scaffold (target) | Usage |\n|---|---|---|\n| #21201F | #2b2d31 | Primary dark (buttons, headings) |\n| #FAF8F6 | #f8f8fa | Primary contrast (button text) |\n| #F2ECE4 | #fafafa | Page background |\n| #1A1715 | #1a1b1e | Dark text |\n| #D4B08A | #4e5058 | Focus rings, accent |\n| #8E6F54 | #757780 | Links, secondary accent |\n| #EAE1D7 | #f0f0f3 | Secondary background |\n| #DDD4C8 | #eaeaed | Hover states |\n| #D4C9BC | #e4e4e8 | Borders |\n| #4C4139 | #4a4b53 | Secondary text |\n| #6B6259 | #5c5d66 | Muted text |\n| #8C8279 | #9a9ba3 | Placeholder text |\n| #B25032 | #e5484d | Error color |\n| #3D6B3D | #2b8a3e | Success color |\n| #D4A84B | #f59f00 | Warning color |\n\n**CSS sections to include (same structure as maearth-app):**\n1. Force light color scheme (`:root` with `--branding-color-*` variables)\n2. Logo replacement (replace Hypercerts logo via background-image trick)\n3. H1 text replacement — use `content: \"Sign in with Certified\"` for now (image replacement will be a follow-up task when SVG assets are provided)\n4. Authenticate page styling\n5. Error/session expiry page styling\n6. Background colors\n7. Text colors\n8. Form inputs\n9. Buttons (primary, secondary, text-only)\n10. Links\n11. Responsive breakpoints (mobile ≤767px, tablet ≥768px, desktop ≥1024px)\n\n**Key difference from maearth-app:** The function takes `baseUrl` instead of `logoUrl`. Construct the logo URL internally as `${baseUrl}/favicon.ico`.\n\n**Text replacements (keep same as maearth-app for now):**\n- H1: \"Sign in with Certified\"\n- Submit button: \"Sign in with Certified\"\n- Authenticate page button: \"Sign in with Certified\"\n\n## Dont\n- Do not modify any other files\n- Do not change the metadata endpoint yet (separate task)\n- Do not use Ma Earth brown/beige colors — use the neutral blue-gray palette above\n- Do not remove or modify the sanitizeUrlForCss function from the previous task","acceptance_criteria":"1. generateBrandingCss is exported from lib/atproto-branding.ts\n2. Function takes baseUrl string parameter and returns a CSS string\n3. All Ma Earth hex colors are replaced with scaffold neutral equivalents per the mapping table\n4. CSS includes all 10 sections listed (color scheme, logo, H1, authenticate page, error page, backgrounds, text, inputs, buttons, responsive)\n5. Logo URL uses ${baseUrl}/favicon.ico\n6. Text replacements say \"Sign in with Certified\"\n7. All image URLs in CSS are sanitized via sanitizeUrlForCss\n8. TypeScript compiles without errors\n9. Returned CSS string is valid CSS (no syntax errors, all braces matched)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-16T12:30:12.357526519+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-16T12:40:41.909439729+06:00","closed_at":"2026-02-16T12:40:41.909439729+06:00","close_reason":"4b1e809 Add generateBrandingCss function with neutral scaffold theme","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.2","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-16T12:30:12.358740596+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-nyk.2","depends_on_id":"hypercerts-nyk.1","type":"blocks","created_at":"2026-02-16T12:30:12.361567421+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.20","title":"Fix: Remove Ma Earth CSS selectors from atproto-branding.ts (copy-paste artifact)","description":"Review of the scaffold codebase found Ma Earth references in lib/atproto-branding.ts — a copy-paste artifact from the maearth-app project.\n\n## Findings\n\nAll 4 references are in `lib/atproto-branding.ts`:\n\n- **Line 112** (comment): `/* The small logo on the Authorize page has alt=\"Ma Earth\" and is in a constrained container */`\n- **Line 113** (functional CSS selector): `img[alt=\"Ma Earth\"] {`\n- **Line 121** (functional CSS selector): `.flex.items-center.justify-start.gap-2 \u003e div.w-8:has(img[alt=\"Ma Earth\"]),`\n- **Line 122** (functional CSS selector): `div.w-8:has(img[alt=\"Ma Earth\"]) {`\n\nLines 113, 121, and 122 are **functional code** — CSS selectors that target an image with `alt=\"Ma Earth\"`. In this scaffold, the authorize-page logo will have a different alt text, so these selectors are dead code that will never match. The comment on line 112 also incorrectly references Ma Earth.\n\n## Fix\n\nReplace the `alt=\"Ma Earth\"` selectors with a generic selector that matches any small logo image on the Authorize page, or use the actual alt text that the scaffold's PDS OAuth page renders (e.g., `img[alt*=\"Logo\"]` or `img[alt=\"Hypercerts\"]`). Update the comment accordingly.\n\nThe block spans lines 111–125 of `lib/atproto-branding.ts`.\n\n## Acceptance Criteria\n\n1. `rg -i 'ma.?earth|maearth|ma_earth' lib/atproto-branding.ts` returns no matches\n2. The CSS block that previously targeted `img[alt=\"Ma Earth\"]` is replaced with a generic or scaffold-appropriate selector\n3. The comment on line 112 no longer mentions Ma Earth\n4. The file still compiles without TypeScript errors (`pnpm tsc --noEmit`)\n5. The branding CSS still covers the Authorize page small logo use-case (selector targets something meaningful)","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-17T23:11:47.004739498+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:14:15.767504895+06:00","closed_at":"2026-02-17T23:14:15.767504895+06:00","close_reason":"66e4025 Fix already applied in nyk.17: removed Ma Earth CSS selectors, replaced with img[alt=\"Hypercerts Logo\"] selector; all acceptance criteria pass","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.20","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T23:11:47.007153317+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.3","title":"Add buildClientMetadata function to lib/config.ts","description":"## Files\n- lib/config.ts (modify)\n\n## What to do\nAdd a `buildClientMetadata()` function to `lib/config.ts` that returns the full OAuth client metadata object. This function replaces the inline object currently in `app/client-metadata.json/route.ts`.\n\n**Why:** The metadata endpoint currently builds its response inline. We need a reusable function that conditionally includes branding CSS in production mode.\n\n**Function signature:** `export function buildClientMetadata(): Record\u003cstring, unknown\u003e`\n\n**Behavior:**\n1. If `config.isLoopback` is true (local dev), return metadata WITHOUT branding:\n   ```\n   {\n     client_id: config.clientId,\n     client_name: \"Hypercerts Scaffold\",\n     client_uri: config.baseUrl,\n     redirect_uris: [config.redirectUri],\n     scope: OAUTH_SCOPE,\n     logo_uri: `${config.baseUrl}/favicon.ico`,\n     grant_types: [\"authorization_code\", \"refresh_token\"],\n     response_types: [\"code\"],\n     token_endpoint_auth_method: \"none\",\n     application_type: \"native\",\n     dpop_bound_access_tokens: true,\n   }\n   ```\n\n2. If NOT loopback (production/preview), return metadata WITH branding:\n   ```\n   {\n     client_id: config.clientId,\n     client_name: \"Hypercerts Scaffold\",\n     client_uri: config.baseUrl,\n     redirect_uris: [config.redirectUri],\n     scope: OAUTH_SCOPE,\n     logo_uri: `${config.baseUrl}/favicon.ico`,\n     grant_types: [\"authorization_code\", \"refresh_token\"],\n     response_types: [\"code\"],\n     token_endpoint_auth_method: \"none\",\n     application_type: \"web\",\n     dpop_bound_access_tokens: true,\n     branding: {\n       css: generateBrandingCss(config.baseUrl),\n     },\n   }\n   ```\n\n**Important:** Keep `token_endpoint_auth_method: \"none\"` in BOTH modes. Upgrading to `private_key_jwt` is a separate concern and out of scope for this task.\n\n**Import:** Add `import { generateBrandingCss } from \"./atproto-branding\";` at the top of the file.\n\n**Placement:** Add the function after the `config` export (around line 204), before the env var validation block.\n\n## Dont\n- Do not change token_endpoint_auth_method (keep \"none\" in both modes)\n- Do not modify the config object itself\n- Do not modify the metadata route file (separate task)\n- Do not change any existing functions (isLoopback, getBaseUrl, etc.)\n- Do not remove any existing exports","acceptance_criteria":"1. buildClientMetadata is exported from lib/config.ts\n2. Function returns object with all required OAuth metadata fields\n3. In loopback mode: no branding field, application_type is \"native\"\n4. In non-loopback mode: branding.css field is present with CSS string from generateBrandingCss, application_type is \"web\"\n5. token_endpoint_auth_method is \"none\" in both modes\n6. Import of generateBrandingCss is added\n7. Existing config export and all other functions are unchanged\n8. TypeScript compiles without errors","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-16T12:30:29.822673508+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-16T12:44:53.362497107+06:00","closed_at":"2026-02-16T12:44:53.362497107+06:00","close_reason":"8fda308 Add buildClientMetadata function with conditional branding","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.3","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-16T12:30:29.824424827+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-nyk.3","depends_on_id":"hypercerts-nyk.2","type":"blocks","created_at":"2026-02-16T12:30:29.826498062+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.4","title":"Update client-metadata.json route to use buildClientMetadata","description":"## Files\n- app/client-metadata.json/route.ts (modify)\n\n## What to do\nReplace the inline metadata object in the route handler with a call to `buildClientMetadata()`.\n\n**Current code (replace entirely):**\n```typescript\nimport { config, OAUTH_SCOPE } from \"@/lib/config\";\nimport { NextResponse } from \"next/server\";\n\nexport async function GET() {\n  return NextResponse.json({\n    client_id: config.clientId,\n    client_name: \"Hypercert Scaffold\",\n    client_uri: config.baseUrl,\n    redirect_uris: [config.redirectUri],\n    scope: OAUTH_SCOPE,\n    logo_uri:`${config.baseUrl}/favicon.ico`,\n    grant_types: [\"authorization_code\", \"refresh_token\"],\n    response_types: [\"code\"],\n    token_endpoint_auth_method: \"none\",\n    application_type: config.isLoopback ? \"native\" : \"web\",\n    dpop_bound_access_tokens: true,\n  });\n}\n```\n\n**New code:**\n```typescript\nimport { buildClientMetadata } from \"@/lib/config\";\nimport { NextResponse } from \"next/server\";\n\n/**\n * OAuth Client Metadata Endpoint\n *\n * Returns OAuth client metadata according to RFC 7591.\n * In production mode, includes custom branding CSS for PDS OAuth pages.\n * In loopback mode, returns minimal metadata (PDS ignores it anyway).\n *\n * @see https://datatracker.ietf.org/doc/html/rfc7591\n */\nexport async function GET() {\n  return NextResponse.json(buildClientMetadata());\n}\n```\n\n## Dont\n- Do not add any logic to this file — all logic lives in buildClientMetadata\n- Do not import config or OAUTH_SCOPE directly (buildClientMetadata handles it)\n- Do not add error handling (the metadata is static/deterministic)","acceptance_criteria":"1. Route file imports buildClientMetadata from @/lib/config\n2. Route file does NOT import config or OAUTH_SCOPE directly\n3. GET handler returns NextResponse.json(buildClientMetadata())\n4. No inline metadata object remains\n5. JSDoc comment explains production vs loopback behavior\n6. TypeScript compiles without errors\n7. Visiting /client-metadata.json returns valid JSON with all required OAuth fields","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-02-16T12:30:43.916980721+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-16T14:24:30.247312679+06:00","closed_at":"2026-02-16T14:24:30.247312679+06:00","close_reason":"56cd67e refactor: update client-metadata.json route to use buildClientMetadata","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.4","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-16T12:30:43.918836947+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-nyk.4","depends_on_id":"hypercerts-nyk.3","type":"blocks","created_at":"2026-02-16T12:30:43.921011202+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.5","title":"Document ngrok setup for local branding testing","description":"## Files\n- DEVELOPMENT.md (modify)\n\n## What to do\nAdd a section to DEVELOPMENT.md documenting how to test OAuth branding locally using ngrok.\n\n**Context:** ATProto OAuth loopback mode (localhost) cannot support branding CSS — the PDS auto-generates minimal metadata and ignores custom fields. To test branding locally, developers need a public HTTPS URL via a tunnel service.\n\n**Section to add (under a new heading \"Testing OAuth Branding Locally\"):**\n\n1. Explain WHY branding does not appear in local dev (ATProto spec limitation — loopback client_id only supports scope and redirect_uri params, PDS auto-generates metadata)\n2. Explain the ngrok workaround:\n   - Install ngrok: `npm install -g ngrok` or download from ngrok.com\n   - Start tunnel: `ngrok http 3000`\n   - Copy the HTTPS URL (e.g. `https://abc123.ngrok-free.app`)\n   - Set `NEXT_PUBLIC_BASE_URL=https://abc123.ngrok-free.app` in `.env.local`\n   - Restart dev server\n   - The app now enters production mode and serves full metadata with branding at `/client-metadata.json`\n3. Note that `next.config.ts` may need `allowedDevOrigins` configured for ngrok domains (reference existing task hypercerts-scaffold-r25 if it exists)\n4. Note that branding is automatically active on Vercel preview/production deployments — no ngrok needed there\n\n**Placement:** Add after the existing OAuth/authentication section in DEVELOPMENT.md. If no such section exists, add near the end before any appendix/reference sections.\n\n## Dont\n- Do not modify any code files\n- Do not add ngrok as a dependency\n- Do not add NGROK_DOMAIN to .env.example (optional, not required)","acceptance_criteria":"1. DEVELOPMENT.md contains a new section about testing OAuth branding locally\n2. Section explains the ATProto loopback limitation (why branding does not appear locally)\n3. Section includes step-by-step ngrok instructions\n4. Section mentions Vercel preview deployments work automatically\n5. No code files are modified","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-16T12:31:00.683521639+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-16T12:35:15.086210654+06:00","closed_at":"2026-02-16T12:35:15.086210654+06:00","close_reason":"76835d7 Document ngrok setup for OAuth branding testing","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.5","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-16T12:31:00.685341788+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.6","title":"Update DEVELOPMENT.md to clarify PDS trusted client requirement for branding","description":"## Files\n- DEVELOPMENT.md (modify)\n\n## What to do\nUpdate the \"Testing OAuth Branding Locally\" section (around line 341) to clarify that branding CSS is only applied by the PDS for trusted client URLs.\n\n**Key corrections:**\n\n1. **Add a note at the top of the section** explaining that the PDS (`pds-eu-west4.test.certified.app`) only applies branding CSS for client URLs registered in its `PDS_OAUTH_TRUSTED_CLIENTS` environment variable. Random ngrok URLs or unregistered Vercel preview URLs will NOT show branding even though the metadata is served correctly.\n\n2. **Update the ngrok instructions** to clarify that:\n   - Ngrok is still useful for testing that the metadata endpoint serves branding CSS correctly (you can verify by visiting `/client-metadata.json` and checking the `branding` field)\n   - However, the PDS will NOT render the branding on its OAuth pages unless the ngrok URL is added to `PDS_OAUTH_TRUSTED_CLIENTS`\n   - For a stable ngrok domain (paid plan), you can request it be added to the trusted list\n\n3. **Add a subsection \"Getting your domain trusted by the PDS\"** explaining:\n   - To get a domain added to the PDS trusted clients list, contact @aspiers on GitHub\n   - Provide the full client_id URL (e.g. `https://your-app.vercel.app/client-metadata.json`)\n   - Production/stable URLs are preferred over ephemeral ngrok URLs\n\n4. **Update the Vercel section** to note that Vercel preview/production URLs also need to be in the trusted list for branding to appear on the PDS OAuth pages.\n\n5. **Keep the existing content about the ATProto loopback limitation** — that is still correct and separate from the trusted client issue.\n\n## Dont\n- Do not modify any code files\n- Do not remove the existing loopback limitation explanation (it is still correct)\n- Do not change the ngrok setup steps themselves (they are still valid for metadata testing)\n- Do not add any PDS configuration details beyond what is described above","acceptance_criteria":"1. DEVELOPMENT.md explains that PDS only applies branding for URLs in PDS_OAUTH_TRUSTED_CLIENTS\n2. Ngrok section clarifies branding will not render on PDS unless URL is trusted\n3. New subsection explains how to get a domain trusted (contact @aspiers on GitHub)\n4. Vercel section notes that preview/production URLs also need to be trusted\n5. Existing loopback limitation explanation is preserved unchanged\n6. No code files modified","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-16T15:41:38.035046062+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-16T15:43:00.583249811+06:00","closed_at":"2026-02-16T15:43:00.583249811+06:00","close_reason":"1ab09c9 clarify PDS trusted client requirement for OAuth branding","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-nyk.6","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-16T15:41:38.036527347+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.7","title":"Replace favicon.ico logo references with certified-logo.svg in PDS branding CSS and client metadata","description":"## Files\n- lib/atproto-branding.ts (modify)\n- lib/config.ts (modify)\n\n## What to do\nThe PDS branding CSS currently uses `favicon.ico` as a placeholder logo. Now that the real Certified logo SVG is available at `/public/certified-logo.svg`, update all logo references:\n\n1. In `generateBrandingCss()` (lib/atproto-branding.ts):\n   - Line 57: Change `const logoUrl = \\`\\${baseUrl}/favicon.ico\\`` to `const logoUrl = \\`\\${baseUrl}/certified-logo.svg\\``\n   - This automatically propagates to all CSS `background-image: url(...)` references that use `sanitizedLogoUrl` (lines 89, 186)\n\n2. In `buildClientMetadata()` (lib/config.ts):\n   - Line 226: Change `logo_uri: \\`\\${config.baseUrl}/favicon.ico\\`` to `logo_uri: \\`\\${config.baseUrl}/certified-logo.svg\\``\n   - Line 246: Same change for the production metadata block\n\nThe SVG is 39x43px with a blue checkmark + white partial circle. The existing CSS sizes (180x54 desktop, 120x40 mobile for the main logo; 100x32 for authorize page) should work with `background-size: contain` but may need aspect ratio adjustments since the SVG is roughly square (39:43) while the CSS containers are wide rectangles. Adjust the container dimensions to be more square-ish (e.g., 54x54 desktop, 40x40 mobile, 32x32 authorize page) to avoid distortion.\n\n## Don't\n- Change any CSS selectors or color values\n- Modify sanitizeUrlForCss logic\n- Touch any files other than the two listed","acceptance_criteria":"1. `generateBrandingCss('https://example.com')` output contains `certified-logo.svg` in all url() values and does NOT contain `favicon.ico`\n2. `buildClientMetadata()` returns `logo_uri` ending in `/certified-logo.svg` in both loopback and production modes\n3. Logo container dimensions are adjusted for the roughly-square SVG aspect ratio (no stretched/distorted logo)\n4. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T16:40:54.592940434+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:52:43.176298095+06:00","closed_at":"2026-02-17T16:52:43.176298095+06:00","close_reason":"3e4b912 Replace favicon.ico with certified-logo.svg in branding CSS and client metadata","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.7","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T16:40:54.594543123+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.8","title":"Replace text-based 'Sign in with Certified' with certified-signin.svg image in PDS branding CSS","description":"## Files\n- lib/atproto-branding.ts (modify)\n\n## What to do\nThe PDS branding CSS currently replaces the H1 text and submit button text with plain CSS `content: \"Sign in with Certified\"`. Now that the SVG assets are available, update the text replacements as follows:\n\n### H1 heading → replace with certified-signin.svg image\nThe `certified-signin.svg` (334x117px) is a stylistic image containing the words 'Sign in with Certified' in a designed layout with green background section and checkmark. Use it as an image replacement for the H1 heading only.\n\nThere are 2 H1-related places to update:\n\n**1. Sign-in page H1 (line ~134)**\n```css\n.grid h1.text-primary::after {\n  content: \"Sign in with Certified\" !important;  /* CHANGE THIS */\n}\n```\nChange to use the SVG as a background image:\n```css\n.grid h1.text-primary::after {\n  content: \"\" !important;\n  display: block !important;\n  width: 280px !important;\n  height: 98px !important;\n  max-width: 100% !important;\n  background-image: url('${sanitizedSigninUrl}') !important;\n  background-size: contain !important;\n  background-repeat: no-repeat !important;\n  background-position: right center !important;\n  margin-top: 8px !important;\n}\n```\n\n**2. Desktop H1 variant (line ~157)**\nUpdate the `@media (min-width: 768px)` and `@media (min-width: 1024px)` versions with larger dimensions, keeping the 334:117 (~2.85:1) aspect ratio.\n\n### Submit button → certified-logo.svg icon + text\nThe submit button should show the C logo icon (`certified-logo.svg`, 39x43px) alongside the text \"Sign in with Certified\". This is NOT an image replacement — it's an icon + text combo.\n\nThere are 2 button-related places to update:\n\n**3. Submit button (line ~597-606)**\n```css\nbutton[type=\"submit\"].bg-primary {\n  font-size: 0 !important;\n  line-height: 0 !important;\n}\nbutton[type=\"submit\"].bg-primary::after {\n  content: \"Sign in with Certified\" !important;  /* CHANGE THIS */\n}\n```\nChange to show the C logo as a ::before pseudo-element and keep the text in ::after:\n```css\nbutton[type=\"submit\"].bg-primary {\n  font-size: 0 !important;\n  line-height: 0 !important;\n  display: flex !important;\n  align-items: center !important;\n  justify-content: center !important;\n  gap: 8px !important;\n}\nbutton[type=\"submit\"].bg-primary::before {\n  content: \"\" !important;\n  display: inline-block !important;\n  width: 20px !important;\n  height: 22px !important;\n  background-image: url('${sanitizedLogoUrl}') !important;\n  background-size: contain !important;\n  background-repeat: no-repeat !important;\n  background-position: center !important;\n  flex-shrink: 0 !important;\n}\nbutton[type=\"submit\"].bg-primary::after {\n  content: \"Sign in with Certified\" !important;\n  font-size: 1rem !important;\n  line-height: 1.5rem !important;\n}\n```\n\n**4. Authenticate page button (line ~217-226)**\nSame approach — ::before with C logo icon, ::after with text.\n\n### Implementation\nAdd a new `signinUrl` variable alongside the existing `logoUrl`:\n```typescript\nconst signinUrl = \\`\\${baseUrl}/certified-signin.svg\\`;\nconst sanitizedSigninUrl = sanitizeUrlForCss(signinUrl);\n```\nThe `sanitizedLogoUrl` (for certified-logo.svg) is already available from task .7 — if .7 hasn't run yet, use `\\${baseUrl}/certified-logo.svg` directly and sanitize it.\n\n## Don't\n- Change any color values or non-text-replacement CSS\n- Use certified-signin.svg for the buttons (that's only for the H1)\n- Use certified-logo.svg for the H1 (that's only for the buttons)\n- Remove the H1 text-hiding CSS (`font-size: 0`, `color: transparent`) — still needed to hide original text\n- Touch any files other than lib/atproto-branding.ts","acceptance_criteria":"1. `generateBrandingCss('https://example.com')` output contains `certified-signin.svg` in url() values for H1 ::after pseudo-elements only\n2. `generateBrandingCss('https://example.com')` output contains `certified-logo.svg` in url() values for button ::before pseudo-elements\n3. Submit buttons still show text \"Sign in with Certified\" via ::after (NOT replaced with image)\n4. H1 heading does NOT show text — only the certified-signin.svg image via ::after background-image\n5. The H1 SVG dimensions maintain the 334:117 aspect ratio at all breakpoints\n6. The H1 text-hiding CSS (`font-size: 0`, `color: transparent`) is preserved\n7. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-17T16:41:15.429364709+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:54:02.047505676+06:00","closed_at":"2026-02-17T16:54:02.047505676+06:00","close_reason":"52ce32a feat: replace H1 text with certified-signin.svg image, add C logo to buttons","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.8","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T16:41:15.431100348+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-nyk.9","title":"Add Certified C logo next to sign-in buttons in scaffold UI","description":"## Files\n- components/login-dialog.tsx (modify)\n- components/navbar.tsx (modify)\n\n## What to do\nAdd the Certified 'C' logo (`/public/certified-logo.svg`) next to the sign-in buttons in the scaffold's own UI. This is the app's internal branding (separate from the PDS OAuth page branding).\n\nThe SVG is at `/certified-logo.svg` (39x43px, blue checkmark + white partial circle on transparent background). Since the scaffold UI has a light background, the white paths in the SVG won't be visible. The blue checkmark (#60A1E2) will be visible.\n\n### 1. Login Dialog (`components/login-dialog.tsx`)\nThe main sign-in form shown when unauthenticated. Add the logo next to the 'Sign In' button (line 70-77):\n\n```tsx\nimport Image from 'next/image';\n\n// Inside the Sign In button (line 70-77):\n\u003cButton type=\"submit\" disabled={loginMutation.isPending} className=\"...\"\u003e\n  {loginMutation.isPending \u0026\u0026 \u003cSpinner /\u003e}\n  \u003cImage src=\"/certified-logo.svg\" alt=\"Certified\" width={20} height={22} className=\"inline-block\" /\u003e\n  Sign In\n\u003c/Button\u003e\n```\n\nAlso add it next to the 'Create an account' button (line 78-86):\n```tsx\n\u003cButton ... variant=\"ghost\" type=\"button\" className=\"...\"\u003e\n  \u003cImage src=\"/certified-logo.svg\" alt=\"Certified\" width={16} height={18} className=\"inline-block\" /\u003e\n  Create an account\n\u003c/Button\u003e\n```\n\n### 2. Navbar (`components/navbar.tsx`)\nThe compact sign-in popover in the navbar. Add the logo next to the 'Sign In' submit button (line 214-221):\n\n```tsx\nimport Image from 'next/image';\n\n// Inside the popover Sign In button:\n\u003cButton type=\"submit\" size=\"sm\" disabled={loginMutation.isPending} className=\"...\"\u003e\n  \u003cImage src=\"/certified-logo.svg\" alt=\"Certified\" width={16} height={18} className=\"inline-block\" /\u003e\n  {loginMutation.isPending ? \"Signing in...\" : \"Sign In\"}\n\u003c/Button\u003e\n```\n\nUse `next/image` for the Image component (already available in the project). Size the logo proportionally — roughly 20x22px for the main button, 16x18px for smaller buttons. The logo should appear to the left of the button text.\n\n## Don't\n- Change button colors, fonts, or other styling\n- Add the logo to the navbar trigger button (the small 'Sign In' button that opens the popover) — only add it inside the popover form and the login dialog\n- Modify any other components\n- Change the SVG file itself","acceptance_criteria":"1. The login dialog 'Sign In' button shows the Certified C logo to the left of the text\n2. The login dialog 'Create an account' button shows the Certified C logo to the left of the text\n3. The navbar popover 'Sign In' button shows the Certified C logo to the left of the text\n4. Logo uses next/image with appropriate width/height props\n5. Logo is visible and not distorted on both light and dark backgrounds\n6. TypeScript compiles without errors (`npx tsc --noEmit`)\n7. `npm run build` succeeds","status":"closed","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T16:41:34.035095764+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T16:44:44.187600046+06:00","closed_at":"2026-02-17T16:44:44.187600046+06:00","close_reason":"d49032c cancelled: misunderstood requirement, no scaffold UI changes needed","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-nyk.9","depends_on_id":"hypercerts-nyk","type":"parent-child","created_at":"2026-02-17T16:41:34.037815811+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-o6n","title":"Add sign-up button to Email login form","description":"## Files\n- components/login-dialog.tsx (modify — EmailForm component only)\n\n## What to do\nAdd a \"Create an account\" ghost button below the Continue button in EmailForm, mirroring the pattern in HandleForm (lines 94-102).\n\nThe button triggers the same ePDS redirect as Continue but without an email (Flow 2). ePDS handles new account creation automatically — there's no separate signup endpoint.\n\nSpecific changes to EmailForm (after the existing Continue Button, before the closing \u003c/form\u003e):\n\n```tsx\n\u003cButton\n  type=\"button\"\n  disabled={isRedirecting}\n  onClick={() =\u003e {\n    setIsRedirecting(true);\n    window.location.href = \"/api/oauth/login\";\n  }}\n  variant=\"ghost\"\n  className=\"w-full font-[family-name:var(--font-outfit)] text-muted-foreground hover:text-create-accent hover:bg-muted/50 transition-colors\"\n\u003e\n  {isRedirecting \u0026\u0026 \u003cSpinner /\u003e}\n  Don't have an account? Create one\n\u003c/Button\u003e\n```\n\nAlso wrap the Continue button and the new Create button in a `\u003cdiv className=\"space-y-2\"\u003e` to match HandleForm's button spacing pattern (see lines 85-103).\n\nThe resulting button section should look like:\n```tsx\n\u003cdiv className=\"space-y-2\"\u003e\n  \u003cButton type=\"submit\" disabled={isRedirecting} className=\"...\"\u003e\n    {isRedirecting \u0026\u0026 \u003cSpinner /\u003e}\n    Continue\n  \u003c/Button\u003e\n  \u003cButton type=\"button\" disabled={isRedirecting} onClick={...} variant=\"ghost\" className=\"...\"\u003e\n    Don't have an account? Create one\n  \u003c/Button\u003e\n\u003c/div\u003e\n```\n\n## Don't\n- Don't change HandleForm, PillToggle, or the main LoginDialog component\n- Don't change the Continue button's behavior\n- Don't change any other files\n- Don't add the Spinner inside the ghost button — only the Continue button needs it (the ghost button sets isRedirecting which disables both and shows spinner on Continue)","acceptance_criteria":"1. EmailForm has a ghost button below Continue that reads \"Don't have an account? Create one\"\n2. Clicking the ghost button navigates to /api/oauth/login (no email param — Flow 2)\n3. The ghost button is disabled while isRedirecting is true\n4. The ghost button uses variant=\"ghost\" and matches HandleForm's \"Create an account\" styling\n5. Both buttons are wrapped in a div with space-y-2 for consistent spacing\n6. HandleForm is unchanged\n7. No TypeScript errors: npx tsc --noEmit passes","status":"closed","priority":1,"issue_type":"task","owner":"kzoepa@gmail.com","estimated_minutes":15,"created_at":"2026-02-24T22:52:51.578659749+06:00","created_by":"kzoeps","updated_at":"2026-02-24T22:53:48.803135845+06:00","closed_at":"2026-02-24T22:53:48.803135845+06:00","close_reason":"73df2bc Added Create an account ghost button to EmailForm","labels":["scope:trivial"]}
{"id":"hypercerts-or3","title":"Fix: spurious color: #ffffff added to .grid h1.text-primary::after (from hypercerts-nyk.8)","description":"Review of hypercerts-nyk.8 found: commit 52ce32a added 'color: #ffffff !important;' to the .grid h1.text-primary::after pseudo-element. This property is meaningless since content is empty string (the pseudo-element only shows a background-image). It was not in the spec and violates the 'Don't: Change any color values' constraint. Evidence: git show 52ce32a -- lib/atproto-branding.ts shows the ::after block has 'color: #ffffff !important;' added. Low severity since it has no visual effect, but it is dead code that should be removed.","status":"open","priority":4,"issue_type":"bug","owner":"kzoeps","created_at":"2026-02-17T17:01:11.702386167+06:00","created_by":"kzoeps","updated_at":"2026-02-17T17:01:11.702386167+06:00","dependencies":[{"issue_id":"hypercerts-or3","depends_on_id":"hypercerts-nyk.8","type":"discovered-from","created_at":"2026-02-17T17:01:14.578791497+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-p2a","title":"Nit: buildClientMetadata loopback comment misleadingly highlights application_type as the key difference","description":"Review of lib/config.ts found a misleading inline comment in buildClientMetadata.\n\nEvidence:\n- lib/config.ts line 263: '// Loopback mode: minimal metadata, application_type is \"web\"'\n- lib/config.ts line 281: 'application_type: \"web\"' (loopback block)\n- lib/config.ts line 303: 'application_type: \"web\"' (production block)\n\nThe comment implies application_type is the distinguishing characteristic of loopback mode, but application_type: 'web' is IDENTICAL in both the loopback and production blocks. The actual difference is that the production block includes 'branding: { css: generateBrandingCss(...) }' while the loopback block does not.\n\nThe JSDoc above the function correctly says 'In loopback mode, returns minimal metadata without branding' — but the inline comment contradicts this by pointing to application_type instead.\n\nFix: Change line 263 to:\n// Loopback mode: same metadata as production but without branding.css (PDS ignores branding for loopback clients)","status":"open","priority":4,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T14:19:45.327825127+06:00","created_by":"kzoeps","updated_at":"2026-02-27T14:19:45.327825127+06:00"}
{"id":"hypercerts-p3a","title":"Fix: DEVELOPMENT.md Key Files table references non-existent providers/OAuthProvider.tsx","description":"Review of DEVELOPMENT.md found a stale file reference in the Key Files table.\n\nEvidence:\n- DEVELOPMENT.md line 512: '| `providers/OAuthProvider.tsx` | Client-side OAuth state management |'\n- git ls-tree origin/loopback -- providers/ shows: AllProviders.tsx, AuthErrorToast.tsx, SignedInProvider.tsx\n- providers/OAuthProvider.tsx does NOT exist on the loopback branch\n\nThe file was likely renamed or restructured. The Key Files table should reference the actual provider files (AllProviders.tsx or SignedInProvider.tsx) with accurate descriptions.\n\nFix: Update DEVELOPMENT.md Key Files table to reference the correct provider file(s).","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T14:19:28.989934518+06:00","created_by":"kzoeps","updated_at":"2026-02-27T14:19:28.989934518+06:00"}
{"id":"hypercerts-p9n","title":"Epic: Document SDK Packaging and Enhance Developer Experience","status":"closed","priority":1,"issue_type":"epic","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:18:09.88485984+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:28:40.447887994+06:00","closed_at":"2026-02-13T11:28:40.447887994+06:00","close_reason":"Completed all 7 documentation tasks: created DEVELOPMENT.md, added SDK warnings, enhanced loopback docs, improved prerequisites, updated .env.example, reorganized footer, and reviewed code examples"}
{"id":"hypercerts-p9n.1","title":"Create DEVELOPMENT.md with SDK version warnings and development guide","description":"Create a comprehensive DEVELOPMENT.md file covering: (1) Critical warning about unreleased SDK version (0.10.0-beta.8 from vendor/), (2) Explanation of packed packages and why we use them for dogfooding, (3) Warning that installing latest SDK may break scaffold, (4) Step-by-step instructions to update vendor SDK package manually, (5) Local development workflow and setup, (6) Contributing guidelines encouraging users to file issues for @kzoeps. This file serves as the technical reference for developers working with the scaffold.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:18:18.009152902+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:23:15.860547626+06:00","closed_at":"2026-02-13T11:23:15.860547626+06:00","close_reason":"Completed: 5ec8a6e - Created comprehensive DEVELOPMENT.md covering SDK version warnings, packed package explanation, update instructions, development workflow, troubleshooting, and contributing guidelines","dependencies":[{"issue_id":"hypercerts-p9n.1","depends_on_id":"hypercerts-p9n","type":"parent-child","created_at":"2026-02-13T11:18:18.010805084+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-p9n.2","title":"Add SDK version warning section to README.md","description":"Add a prominent '⚠️ Important: SDK Version \u0026 Breaking Changes' section to README.md immediately after the Quick Start section (around line 32). This section must explain: (1) Uses unreleased v0.10.0-beta.8 from vendor/, (2) Built from github.com/hypercerts-org/hypercerts-sdk not npm, (3) Purpose is dogfooding latest features, (4) Contains unreleased/unmerged changes, (5) Breaking changes expected, (6) Installing latest SDK may break scaffold, (7) Link to DEVELOPMENT.md for details, (8) Link to issues at github.com/hypercerts-org/hypercerts-scaffold-atproto with @kzoeps as maintainer. Depends on hypercerts-p9n.1 because DEVELOPMENT.md must exist to link to it.","status":"closed","priority":1,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:18:25.62233731+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:23:59.686592067+06:00","closed_at":"2026-02-13T11:23:59.686592067+06:00","close_reason":"Completed: 1e8c516 - Added SDK version warning section to README after Quick Start with all required details","dependencies":[{"issue_id":"hypercerts-p9n.2","depends_on_id":"hypercerts-p9n","type":"parent-child","created_at":"2026-02-13T11:18:25.623979997+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-p9n.2","depends_on_id":"hypercerts-p9n.1","type":"blocks","created_at":"2026-02-13T11:19:05.137659881+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-p9n.3","title":"Enhance README loopback configuration documentation","description":"Improve the existing loopback configuration docs in README.md: (1) Make Quick Start note more prominent (line 31) - emphasize OAuth requirement for 127.0.0.1, (2) Add TL;DR to 'Localhost Redirect' section (line 127) explaining that .env.local MUST use 127.0.0.1 for OAuth to work, (3) Add troubleshooting subsection with common OAuth issues (callback failed, invalid redirect_uri, redirect loops, ngrok issues). The loopback docs are already comprehensive, this task just enhances clarity and adds troubleshooting.","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:18:33.299950647+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:24:57.350432049+06:00","closed_at":"2026-02-13T11:24:57.350432049+06:00","close_reason":"Completed: 2a3277f - Enhanced loopback docs with prominent OAuth requirement, TL;DR, and troubleshooting section","dependencies":[{"issue_id":"hypercerts-p9n.3","depends_on_id":"hypercerts-p9n","type":"parent-child","created_at":"2026-02-13T11:18:33.30151496+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-p9n.4","title":"Improve README prerequisites and installation section","description":"Update Prerequisites section (lines 5-10) in README.md to be more detailed and helpful: (1) Specify Node.js 20+ with nvm recommendation, (2) Mention pnpm explicitly with install command, (3) Expand Redis section with both local Docker and cloud options, (4) Clarify PDS account requirement, (5) Add note referencing DEVELOPMENT.md for SDK details. Update Quick Start section to mention pnpm explicitly and add Redis check before running dev server. Depends on hypercerts-p9n.1 because needs DEVELOPMENT.md to reference it.","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:18:41.139193052+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:25:37.166639803+06:00","closed_at":"2026-02-13T11:25:37.166639803+06:00","close_reason":"Completed: e31646b - Updated prerequisites with detailed requirements and Quick Start with pnpm/Redis clarity","dependencies":[{"issue_id":"hypercerts-p9n.4","depends_on_id":"hypercerts-p9n","type":"parent-child","created_at":"2026-02-13T11:18:41.141044172+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-p9n.4","depends_on_id":"hypercerts-p9n.1","type":"blocks","created_at":"2026-02-13T11:19:05.725129883+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-p9n.5","title":"Update .env.example with SDK version reference","description":"Add a header comment block at the top of .env.example file explaining: (1) This scaffold uses unreleased packed SDK version, (2) Reference DEVELOPMENT.md for SDK details and breaking changes, (3) Reference README.md for setup instructions. This provides context for developers when they first copy the file. Depends on hypercerts-p9n.1 because needs DEVELOPMENT.md to reference it.","status":"closed","priority":2,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:18:47.282193967+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:26:10.199986381+06:00","closed_at":"2026-02-13T11:26:10.199986381+06:00","close_reason":"Completed: 4818387 - Added header comment with SDK version warning and references to DEVELOPMENT.md","dependencies":[{"issue_id":"hypercerts-p9n.5","depends_on_id":"hypercerts-p9n","type":"parent-child","created_at":"2026-02-13T11:18:47.283647269+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-p9n.5","depends_on_id":"hypercerts-p9n.1","type":"blocks","created_at":"2026-02-13T11:19:06.370792289+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-p9n.6","title":"Add DEVELOPMENT.md link to README footer","description":"Update the 'Learn More' section (around line 242) in README.md to add DEVELOPMENT.md as the first link under a new 'Scaffold Documentation' subsection. Also add links to report issues (github.com/hypercerts-org/hypercerts-scaffold-atproto/issues) and mention @kzoeps as maintainer. Reorganize external docs links into a separate subsection. Depends on hypercerts-p9n.1 because DEVELOPMENT.md must exist to link to it.","status":"closed","priority":3,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:18:53.526270655+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:26:50.075541431+06:00","closed_at":"2026-02-13T11:26:50.075541431+06:00","close_reason":"Completed: 9aee7d0 - Reorganized Learn More section with DEVELOPMENT.md, external docs, and issue reporting links","dependencies":[{"issue_id":"hypercerts-p9n.6","depends_on_id":"hypercerts-p9n","type":"parent-child","created_at":"2026-02-13T11:18:53.528019597+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-p9n.6","depends_on_id":"hypercerts-p9n.1","type":"blocks","created_at":"2026-02-13T11:19:06.902665893+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-p9n.7","title":"Review and update code examples in README","description":"Review all code snippets throughout README.md for accuracy with current codebase: (1) Check Authentication section code example (lines 170-187), (2) Check Repository Context section (lines 193-210), (3) Verify all imports are correct, (4) Verify function signatures match current SDK version, (5) Add helpful comments where appropriate, (6) Ensure examples follow best practices. This is a review/polish task to ensure documentation matches implementation.","status":"closed","priority":3,"issue_type":"task","assignee":"kzoeps","owner":"kzoeps","created_at":"2026-02-13T11:19:00.53603122+06:00","created_by":"kzoeps","updated_at":"2026-02-13T11:28:18.636827322+06:00","closed_at":"2026-02-13T11:28:18.636827322+06:00","close_reason":"Completed: 91d11f0 - Reviewed and improved all code examples, added simpler alternatives, fixed authentication flow description","dependencies":[{"issue_id":"hypercerts-p9n.7","depends_on_id":"hypercerts-p9n","type":"parent-child","created_at":"2026-02-13T11:19:00.537513614+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-pdf","title":"Fix: SignedInProvider fires two concurrent sdk.restoreSession() calls (from hypercerts-dqf.4)","description":"## Files\n- providers/SignedInProvider.tsx (modify)\n\n## What to do\nRevert the dqf.4 change that added getAuthenticatedRepo() to the initial Promise.all. The problem: both getSession() and getAuthenticatedRepo() call sdk.restoreSession(userDid) internally, and they are separate cache() entries, so React does NOT deduplicate them. This means every render fires two concurrent sdk.restoreSession() calls — a regression.\n\n### Current code (lines 14-18):\n```ts\nconst [session, cookieStore, repo] = await Promise.all([\n  getSession(),\n  cookies(),\n  getAuthenticatedRepo(),\n]);\n```\n\n### Fix:\nRevert to the original pattern where getAuthenticatedRepo() is called AFTER getSession() succeeds, inside the if (session) block:\n```ts\nconst [session, cookieStore] = await Promise.all([\n  getSession(),\n  cookies(),\n]);\nconst activeDid = cookieStore.get(\"active-did\")?.value || session?.did;\n\nlet avatarUrl: string | undefined = undefined;\nlet handle: string | undefined = undefined;\nconst activeProfileName: string | undefined = undefined;\nconst activeProfileHandle: string | undefined = undefined;\n\nif (session) {\n  const repo = await getAuthenticatedRepo();\n  if (repo) {\n    const profile = await repo.profile.getCertifiedProfile().catch(() =\u003e null);\n    avatarUrl = convertBlobUrlToCdn(profile?.avatar) || \"\";\n    handle = profile?.handle || \"\";\n  }\n}\n```\n\n## Do not\n- Change any JSX rendering\n- Change any imports\n- Change the Navbar props\n- Touch any other files","acceptance_criteria":"1. getAuthenticatedRepo() is NOT in the initial Promise.all\n2. getAuthenticatedRepo() is called only inside the if (session) block\n3. Only one sdk.restoreSession() call fires per render (via getAuthenticatedRepo)\n4. Build passes: pnpm build exits 0\n5. Lint passes: pnpm lint exits 0\n6. No visual or behavioral changes","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-17T23:50:52.598467413+06:00","created_by":"kzoeps","updated_at":"2026-02-17T23:54:24.471133649+06:00","closed_at":"2026-02-17T23:54:24.471133649+06:00","close_reason":"a80a0da fix: revert concurrent restoreSession calls in SignedInProvider","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-pdf","depends_on_id":"hypercerts-dqf.4","type":"discovered-from","created_at":"2026-02-17T23:50:55.568078405+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-pdf","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:52:35.285889173+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw","title":"Epic: Migrate codebase to @hypercerts-org/lexicon v0.10.0 stable","description":"## Why\nThe project upgraded from @hypercerts-org/lexicon 0.10.0-beta.14 to 0.10.0 stable. This introduced several breaking changes:\n\n1. **Renamed type namespaces**: OrgHypercertsClaimAttachment → OrgHypercertsContextAttachment, OrgHypercertsClaimEvaluation → OrgHypercertsContextEvaluation, OrgHypercertsClaimMeasurement → OrgHypercertsContextMeasurement, OrgHypercertsClaimContributionDetails → OrgHypercertsClaimContribution\n2. **Renamed collection NSIDs**: org.hypercerts.claim.attachment → org.hypercerts.context.attachment, org.hypercerts.claim.evaluation → org.hypercerts.context.evaluation, org.hypercerts.claim.measurement → org.hypercerts.context.measurement, org.hypercerts.claim.contributionDetails → org.hypercerts.claim.contribution\n3. **New required fields**: AppCertifiedActorProfile.Record now requires createdAt\n4. **Field type changes**: description field on activity records changed from string to PubLeafletPagesLinearDocument.Main\n5. **Record type changes**: putRecord/createRecord now require typed record objects instead of unknown\n\n## Success Criteria\n- npx tsc --noEmit passes with zero lexicon-related errors (ignore .next/ cache errors)\n- All collection NSID strings match v0.10.0 schema\n- All type imports resolve correctly\n- Runtime validation (validateRecord, isRecord) works with new namespaces\n\n## Scope\n22 files across lib/, components/, app/api/, and app/hypercerts/ directories. ~30 TypeScript errors to fix.\n\n## Key Constraints\n- Do NOT change any business logic or behavior\n- Do NOT change API route signatures or response shapes\n- Preserve all existing runtime validation patterns\n- Keep backward-compatible AT-URI parsing (existing records in repos still use old collection names)","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T15:56:53.766477894+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:41:49.0510038+06:00","labels":["needs-integration-review","scope:medium"]}
{"id":"hypercerts-phiw.1","title":"Update renamed type imports across codebase (Attachment, Evaluation, Measurement, ContributionDetails)","description":"## Files\n- lib/types.ts (modify) — lines 2-3\n- lib/queries.ts (modify) — lines 3-5\n- lib/create-actions.ts (modify) — lines 12, 14-15\n- components/hypercert-evidence-view.tsx (modify) — line 15\n- components/hypercert-evidence-section.tsx (modify) — line 12\n- app/api/certs/add-attachment/route.ts (modify) — line 11\n\n## What to do\nReplace all renamed type namespace imports from @hypercerts-org/lexicon:\n\n1. `OrgHypercertsClaimAttachment` → `OrgHypercertsContextAttachment` (6 files)\n2. `OrgHypercertsClaimEvaluation` → `OrgHypercertsContextEvaluation` (2 files: lib/queries.ts, lib/create-actions.ts)\n3. `OrgHypercertsClaimMeasurement` → `OrgHypercertsContextMeasurement` (1 file: lib/create-actions.ts)\n4. `OrgHypercertsClaimContributionDetails` → `OrgHypercertsClaimContribution` (3 files: lib/types.ts, lib/queries.ts, lib/create-actions.ts)\n\nFor each file:\n- Update the import statement to use the new namespace name\n- Keep the local alias unchanged (e.g., `import { OrgHypercertsContextAttachment as HypercertEvidence }` preserves the alias)\n- Verify that `.Record`, `.Main`, `.validateRecord`, `.isRecord` still exist on the new namespace\n\n## Dont\n- Do NOT rename local aliases (keep HypercertEvidence, HypercertContribution, etc.)\n- Do NOT change any usage sites beyond the import line\n- Do NOT change collection NSID strings (that is a separate task)\n- Do NOT modify business logic","acceptance_criteria":"npx tsc --noEmit 2\u003e\u00261 | grep -c \"has no exported member named\" should return 0. All 6 files compile without import resolution errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T15:57:08.220620766+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:27:19.387631513+06:00","closed_at":"2026-03-04T16:27:19.387631513+06:00","close_reason":"080f33b Update renamed type imports to @hypercerts-org/lexicon v0.10.0 stable","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.1","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:57:08.222988098+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.10","title":"Delete or replace stale local lexicons/ directory with v0.10.0 canonical types","description":"## Files\n- lexicons/lexicons.ts (delete or replace)\n- lexicons/types/ (delete entire directory)\n- Any files importing from local lexicons/ instead of @hypercerts-org/lexicon\n\n## What to do\nThe project has a local `lexicons/` directory with hand-written/generated lexicon schemas and TypeScript types that are **completely out of sync** with the installed `@hypercerts-org/lexicon` v0.10.0 package.\n\n### Key divergences in local lexicons/:\n1. `lexicons/lexicons.ts` defines `OrgHypercertsClaimActivity` with:\n   - `workScope` as required string (v0.10.0: optional WorkScopeString|CEL union)\n   - `workTimeFrameFrom`/`workTimeFrameTo` fields (v0.10.0: `startDate`/`endDate`)\n   - Missing `contributors`, `locations`, `rights` fields\n2. `lexicons/lexicons.ts` defines `OrgHypercertsClaimContribution` with:\n   - `hypercert` (StrongRef), `contributors` (string[]), `description`, `workTimeframeFrom/To` fields\n   - v0.10.0 has: `role`, `contributionDescription`, `startDate`, `endDate`\n3. `lexicons/types/org/hypercerts/claim/activity.ts` defines `workScope: string` (required)\n\n### What to do:\n1. Search for any imports from the local `lexicons/` directory (e.g., `from \"@/lexicons/...\"` or `from \"../../lexicons/...\"`)\n2. If any files import from local lexicons, migrate them to import from `@hypercerts-org/lexicon` instead\n3. Delete the entire `lexicons/` directory — all types should come from the npm package\n4. If the local lexicons are used for anything else (e.g., PDS lexicon registration), document that as a follow-up issue\n\n### Investigation needed:\nRun `grep -r \"from.*lexicons/\" --include=\"*.ts\" --include=\"*.tsx\" lib/ app/ components/ providers/` to find any imports from the local directory.\n\n## Dont\n- Do NOT delete if the local lexicons are actively imported by source files — migrate imports first\n- Do NOT delete if the local lexicons serve a purpose beyond type definitions (e.g., PDS schema registration)\n- If unsure, just add a TODO comment at the top of lexicons/lexicons.ts noting it is stale","acceptance_criteria":"No source files in lib/, app/, components/, or providers/ import from the local lexicons/ directory. If lexicons/ is deleted, the project still compiles. If lexicons/ is kept, it has a clear TODO comment noting it is stale.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T16:06:42.596234915+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:11:37.872891855+06:00","closed_at":"2026-03-04T16:11:37.872895602+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.10","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T16:06:42.597936966+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.11","title":"Update Constellation backlink source strings to v0.10.0 collection names","description":"## Files\n- lib/api/external/constellation.ts (modify) — lines 38, 50, 62\n\n## What to do\nThe Constellation backlinks API uses NSID-qualified source strings to query for records that reference a given hypercert. Three of these use old collection names.\n\n### Changes needed:\n1. Line 38: `\"org.hypercerts.claim.attachment:subjects[com.atproto.repo.strongRef].uri\"` → `\"org.hypercerts.context.attachment:subjects[com.atproto.repo.strongRef].uri\"`\n2. Line 50: `\"org.hypercerts.claim.evaluation:subject.uri\"` → `\"org.hypercerts.context.evaluation:subject.uri\"`\n3. Line 62: `\"org.hypercerts.claim.measurement:subject.uri\"` → `\"org.hypercerts.context.measurement:subject.uri\"`\n\nThese are string-only changes. The field paths after the colon (`:subjects[...]`, `:subject.uri`) remain the same.\n\n## Dont\n- Do NOT change the Constellation API endpoint URL or query structure\n- Do NOT change the field path portion after the colon\n- Do NOT change any other strings in this file","acceptance_criteria":"grep -n \"org.hypercerts.claim.attachment\\|org.hypercerts.claim.evaluation\\|org.hypercerts.claim.measurement\" lib/api/external/constellation.ts returns zero results.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-04T16:19:31.872946336+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:34:52.920948241+06:00","closed_at":"2026-03-04T16:34:52.920948241+06:00","close_reason":"dfabab7 Update Constellation backlink source strings to v0.10.0 collection names","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-phiw.11","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T16:19:31.875456487+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.11","depends_on_id":"hypercerts-phiw.2","type":"blocks","created_at":"2026-03-04T16:19:31.881235884+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.12","title":"Add missing $type on contributor union sub-objects in addContribution","description":"## Files\n- lib/create-actions.ts (modify) — lines 193-196\n\n## What to do\nIn v0.10.0, union fields on `OrgHypercertsClaimActivity.Contributor` require `$type` to be present on every value. The ATProto lexicon validator rejects objects without `$type` in union positions with: \"must be an object which includes the \\\"\\$type\\\" property\".\n\n### Current code (lines 193-196):\n```ts\nconst newContributors = contributorRefs.map((ref) =\u003e ({\n  contributorIdentity: ref,          // ref = { uri: string, cid: string } — MISSING $type\n  contributionDetails: detailsRef,   // detailsRef = { uri: string, cid: string } — MISSING $type\n}));\n```\n\n### Fix:\n```ts\nconst newContributors = contributorRefs.map((ref) =\u003e ({\n  contributorIdentity: { $type: \"com.atproto.repo.strongRef\" as const, ...ref },\n  contributionDetails: { $type: \"com.atproto.repo.strongRef\" as const, ...detailsRef },\n}));\n```\n\n### Why this matters:\nThe `Contributor` type in v0.10.0 defines these fields as unions:\n```ts\ncontributorIdentity: $Typed\u003cContributorIdentity\u003e | $Typed\u003cComAtprotoRepoStrongRef.Main\u003e | { $type: string };\ncontributionDetails?: $Typed\u003cContributorRole\u003e | $Typed\u003cComAtprotoRepoStrongRef.Main\u003e | { $type: string };\n```\n\nEvery branch requires `$type`. Without it, `OrgHypercertsClaimActivity.validateRecord()` at line 202 will throw at runtime.\n\n### Also check:\nAre there any OTHER places in the codebase where contributor objects are constructed without `$type`? Search for `contributorIdentity` and `contributionDetails` assignments.\n\n## Dont\n- Do NOT change the contributor parsing/reading code in lib/contributor-utils.ts (reading is fine — stored records already have $type)\n- Do NOT change the Contributor type definition\n- Do NOT change how refs are resolved (resolveStrongRef already returns {uri, cid})","acceptance_criteria":"After this fix plus tasks .1 and .2, calling addContribution should pass OrgHypercertsClaimActivity.validateRecord without throwing. Verify by checking that the contributor objects include $type: \"com.atproto.repo.strongRef\".","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T16:19:46.551446492+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:28:23.71247344+06:00","closed_at":"2026-03-04T16:28:23.71247344+06:00","close_reason":"b311cb6 Add $type on contributor union sub-objects in addContribution","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-phiw.12","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T16:19:46.553209836+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.12","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T16:19:46.559304768+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.2","title":"Update collection NSID strings to match v0.10.0 schema","description":"## Files\n- lib/config.ts (modify) — lines 17-31\n- lib/types.ts (modify) — lines 93-100 (Collections enum)\n- lib/create-actions.ts (modify) — string literals throughout\n- app/api/certs/add-attachment/route.ts (modify) — string literals\n- app/api/certs/route.ts (modify) — string literals (if any changed)\n\n## What to do\nUpdate all hardcoded collection NSID strings to match v0.10.0:\n\n### In lib/config.ts HYPERCERT_COLLECTIONS:\n1. `ATTACHMENT: \"org.hypercerts.claim.attachment\"` → `ATTACHMENT: \"org.hypercerts.context.attachment\"`\n2. `EVALUATION: \"org.hypercerts.claim.evaluation\"` → `EVALUATION: \"org.hypercerts.context.evaluation\"`\n3. `MEASUREMENT: \"org.hypercerts.claim.measurement\"` → `MEASUREMENT: \"org.hypercerts.context.measurement\"`\n4. `CONTRIBUTION_DETAILS: \"org.hypercerts.claim.contributionDetails\"` → `CONTRIBUTION: \"org.hypercerts.claim.contribution\"`\n5. `COLLECTION: \"org.hypercerts.claim.collection\"` → `COLLECTION: \"org.hypercerts.collection\"`\n6. `WORK_SCOPE_TAG: \"org.hypercerts.helper.workScopeTag\"` → `WORK_SCOPE_TAG: \"org.hypercerts.workscope.tag\"`\n\n### In lib/types.ts Collections enum:\n1. `contribution = \"org.hypercerts.claim.contributionDetails\"` → `contribution = \"org.hypercerts.claim.contribution\"`\n2. `evidence = \"org.hypercerts.claim.attachment\"` → `evidence = \"org.hypercerts.context.attachment\"`\n3. `evaluation = \"org.hypercerts.claim.evaluation\"` → `evaluation = \"org.hypercerts.context.evaluation\"`\n\n### In lib/create-actions.ts string literals:\n1. All `\"org.hypercerts.claim.contributionDetails\"` → `\"org.hypercerts.claim.contribution\"`\n2. All `\"org.hypercerts.claim.contributorInformation\"` — verify this is still valid in v0.10.0 (it is: org.hypercerts.claim.contributorInformation)\n3. All `\"org.hypercerts.claim.evaluation\"` → `\"org.hypercerts.context.evaluation\"`\n4. All `\"org.hypercerts.claim.measurement\"` → `\"org.hypercerts.context.measurement\"`\n\n### In app/api/certs/add-attachment/route.ts:\n1. `\"org.hypercerts.claim.attachment\"` → `\"org.hypercerts.context.attachment\"`\n\n### $type fields on records must also be updated:\n1. `$type: \"org.hypercerts.claim.attachment\"` → `$type: \"org.hypercerts.context.attachment\"`\n2. `$type: \"org.hypercerts.claim.evaluation\"` → `$type: \"org.hypercerts.context.evaluation\"`\n3. `$type: \"org.hypercerts.claim.measurement\"` → `$type: \"org.hypercerts.context.measurement\"`\n4. `$type: \"org.hypercerts.claim.contributionDetails\"` → `$type: \"org.hypercerts.claim.contribution\"`\n\n## Dont\n- Do NOT change `org.hypercerts.claim.activity` — it is unchanged\n- Do NOT change `org.hypercerts.claim.rights` — it is unchanged\n- Do NOT change `app.certified.location` — it is unchanged\n- Do NOT change `app.certified.actor.profile` — it is unchanged\n- Do NOT change `app.bsky.actor.profile` — it is unchanged\n- Do NOT change `org.hypercerts.defs#*` $type strings — they are unchanged\n- Do NOT change import statements (that is task .1)","acceptance_criteria":"All NSID strings in the codebase match v0.10.0 lexicon schema. grep -r \"org.hypercerts.claim.attachment\\|org.hypercerts.claim.evaluation\\|org.hypercerts.claim.measurement\\|org.hypercerts.claim.contributionDetails\\|org.hypercerts.claim.collection\\|org.hypercerts.helper.workScopeTag\" --include=\"*.ts\" --include=\"*.tsx\" lib/ app/ components/ returns zero results (excluding comments and example AT-URIs in placeholder text).","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T15:57:25.896301292+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:30:41.560124836+06:00","closed_at":"2026-03-04T16:30:41.560124836+06:00","close_reason":"6a4ecda Update collection NSID strings to match v0.10.0 schema","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.2","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:57:25.898390779+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.2","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T15:57:25.903760737+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.3","title":"Fix description field type: string → PubLeafletPagesLinearDocument on activity records","description":"## Files\n- app/api/certs/route.ts (modify) — line ~137 (POST handler) AND line ~241 (PUT handler)\n- lib/api/types.ts (modify) — CreateHypercertParams type if description is typed as string\n- components/hypercerts-base-form.tsx (modify) — where description field is constructed\n- components/hypercert-detail-view.tsx (modify) — line ~229 where description is rendered (TS error: Main not assignable to ReactNode)\n- components/hypercert-evidence-view.tsx (modify) — line ~84 where evidence.description is rendered (will show [object Object] after task .1 fixes imports)\n\n## What to do\nIn v0.10.0, the `description` field changed from `string` to `PubLeafletPagesLinearDocument.Main` on BOTH:\n- `OrgHypercertsClaimActivity.Record.description`\n- `OrgHypercertsContextAttachment.Main.description` (evidence/attachment records)\n\n### Step 1: Understand the LinearDocument structure\nImport `PubLeafletPagesLinearDocument` from `@hypercerts-org/lexicon` (it re-exports from @atcute/leaflet). The structure is:\n```ts\ninterface Main {\n  blocks: { block: Block; $type?: string }[];\n  $type?: \"pub.leaflet.pages.linearDocument\";\n}\n```\nWhere Block can be various block types (paragraph, blockquote, etc.). For simple text, wrap in a paragraph block.\n\n### Step 2: Create a shared helper for LinearDocument \u003c-\u003e string conversion\nCreate a utility (e.g., in lib/utils.ts or lib/linear-document.ts):\n```ts\nimport type { PubLeafletPagesLinearDocument } from \"@hypercerts-org/lexicon\";\n\nexport function stringToLinearDocument(text: string): PubLeafletPagesLinearDocument.Main {\n  return {\n    blocks: [{ block: { plaintext: text, $type: \"pub.leaflet.blocks.paragraph\" } }],\n    $type: \"pub.leaflet.pages.linearDocument\",\n  };\n}\n\nexport function linearDocumentToString(doc: PubLeafletPagesLinearDocument.Main | undefined): string {\n  if (!doc?.blocks) return \"\";\n  return doc.blocks.map(b =\u003e (b.block as any)?.plaintext ?? \"\").filter(Boolean).join(\"\\n\");\n}\n```\nCheck the actual @atcute/leaflet block types to get the exact structure right.\n\n### Step 3: Fix record construction in app/api/certs/route.ts POST handler\nThe line `description: hypercertParams.description` currently passes a string. Convert it:\n```ts\ndescription: hypercertParams.description ? stringToLinearDocument(hypercertParams.description) : undefined\n```\n\n### Step 4: Fix record construction in app/api/certs/route.ts PUT handler (~line 241)\nThe PUT handler spreads the existing record and merges updates. The `description` field from form data is a string, but the existing record has a LinearDocument. Ensure the update also converts:\n```ts\n...(updates.description ? { description: stringToLinearDocument(updates.description) } : {}),\n```\n\n### Step 5: Fix rendering in components/hypercert-detail-view.tsx\nThe description field is no longer a string, so rendering it directly as ReactNode fails. Use the helper:\n```ts\nconst descriptionText = linearDocumentToString(record.description);\n```\nThen render `descriptionText` instead of `record.description`.\n\n### Step 6: Fix rendering in components/hypercert-evidence-view.tsx\nSame issue — `evidence.description` is now a LinearDocument. After task .1 fixes the import, this will render [object Object]. Use the same helper:\n```ts\nconst descriptionText = linearDocumentToString(evidence.description);\n```\n\n### Step 7: Fix the form construction\nIn hypercerts-base-form.tsx, if description is being set as a string for the API params, ensure the API types accept string (and the route converts it), OR convert at the form level.\n\n## Dont\n- Do NOT change the shortDescription field — it remains a string\n- Do NOT change the API request/response shape if possible — convert at the server route level\n- Do NOT remove description support — just adapt the type","acceptance_criteria":"npx tsc --noEmit 2\u003e\u00261 | grep -c \"description\\|linearDocument\\|LinearDocument\\|not assignable to type.*ReactNode\\|not assignable to type.*blocks\" should return 0. The description field compiles correctly in both record construction and rendering contexts.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-04T15:57:46.262478044+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:33:31.831332112+06:00","closed_at":"2026-03-04T16:33:31.831332112+06:00","close_reason":"8a07a8a Fix description field type: string → LinearDocument on activity/attachment records","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.3","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:57:46.264589029+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.3","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T15:57:46.268621605+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.4","title":"Add required createdAt field to AppCertifiedActorProfile record construction","description":"## Files\n- app/api/profile/update/route.ts (modify) — line ~106\n\n## What to do\nIn v0.10.0, `AppCertifiedActorProfile.Record` now requires a `createdAt: string` field (ISO 8601 datetime).\n\nThe TypeScript error is:\n```\napp/api/profile/update/route.ts(106,13): error TS2741: Property \"createdAt\" is missing in type \"{ $type: \\\"app.certified.actor.profile\\\"; }\" but required in type \"Main\".\n```\n\n### Fix\nIn the CREATE branch (when no existing profile exists), add `createdAt`:\n```ts\nconst record: AppCertifiedActorProfile.Record = {\n  $type: \"app.certified.actor.profile\",\n  createdAt: new Date().toISOString(),  // ADD THIS\n};\n```\n\nIn the UPDATE branch, ensure `createdAt` is preserved from the existing record (it likely already is via spread `...existingProfile`). Verify this.\n\n## Dont\n- Do NOT change the UPDATE branch if createdAt is already preserved via spread\n- Do NOT change any other profile fields\n- Do NOT modify the bsky profile route (app.bsky.actor.profile has its own schema)","acceptance_criteria":"npx tsc --noEmit 2\u003e\u00261 | grep -c \"createdAt.*missing\" should return 0. The profile creation compiles without the TS2741 error.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-04T15:57:57.262097446+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:23:02.961901611+06:00","closed_at":"2026-03-04T16:23:02.961901611+06:00","close_reason":"183523b Add createdAt field to AppCertifiedActorProfile record creation","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-phiw.4","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:57:57.264109518+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.5","title":"Fix putRecord/createRecord record parameter type errors (unknown → typed record)","description":"## Files\n- lib/queries.ts (modify) — lines 97, 110, 123\n- lib/create-actions.ts (modify) — lines 163, 280, 363, 567\n- app/api/certs/add-attachment/route.ts (modify) — line 200\n\n## What to do\nIn v0.10.0, the `record` parameter in `com.atproto.repo.createRecord` and `com.atproto.repo.putRecord` now expects `{ [x: string]: unknown }` instead of `unknown`. Several places pass `unknown` typed values.\n\nThe TypeScript errors are all variants of:\n```\nerror TS2322: Type \"unknown\" is not assignable to type \"{ [x: string]: unknown; }\"\n```\n\n### Root cause\nThese errors occur where records are passed through generic functions or where `assertValidRecord` narrows the type but the variable was originally typed as `unknown`.\n\n### Fix approach\nFor each error site, add an explicit type assertion `as Record\u003cstring, unknown\u003e` or better, type the variable correctly from the start:\n\n1. **lib/queries.ts lines 97, 110, 123**: The `createEvaluation`, `createEvidence`, `createLocation` functions pass `record` to `createRecord`. Ensure the `record` parameter is typed as the specific `.Record` type (which extends `{ [x: string]: unknown }`).\n\n2. **lib/create-actions.ts lines 163, 280, 363, 567**: These are in `addContribution`, `addEvaluation`, `addMeasurement`, `updateMeasurement`. The record objects should already be typed correctly after construction. Check if the issue is that `assertValidRecord` narrows to `T` but `T` doesnt satisfy `{ [x: string]: unknown }`. If so, add `as Record\u003cstring, unknown\u003e` at the createRecord/putRecord call site, OR adjust the assertValidRecord return type.\n\n3. **app/api/certs/add-attachment/route.ts line 200**: Same pattern — the record is typed but the createRecord call doesnt accept it.\n\n### Preferred fix\nThe cleanest fix is to update `assertValidRecord` in `lib/record-validation.ts` to narrow to `T \u0026 Record\u003cstring, unknown\u003e` instead of just `T`. This would fix all call sites at once:\n```ts\nexport function assertValidRecord\u003cT extends Record\u003cstring, unknown\u003e\u003e(\n  label: string,\n  record: unknown,\n  validateFn: (v: unknown) =\u003e ValidationResult\u003cT\u003e,\n): asserts record is T {\n```\n\nIf that causes cascading type issues, fall back to adding `as Record\u003cstring, unknown\u003e` at each createRecord/putRecord call site.\n\n## Dont\n- Do NOT change the actual record data being passed\n- Do NOT remove validation calls\n- Do NOT change the createRecord/putRecord API signatures\n- Prefer fixing the root cause (assertValidRecord signature) over per-site casts","acceptance_criteria":"npx tsc --noEmit 2\u003e\u00261 | grep -c \"not assignable to type.*x: string.*unknown\" should return 0. All 8 error sites compile cleanly.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T15:58:18.746782759+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:29:21.09094607+06:00","closed_at":"2026-03-04T16:29:21.09094607+06:00","close_reason":"f941820 Fix assertValidRecord type constraint to T extends Record\u003cstring, unknown\u003e","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.5","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:58:18.749065229+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.5","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T15:58:18.754093613+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.6","title":"Fix hypercert-evidence-view.tsx implicit any parameters and content rendering","description":"## Files\n- components/hypercert-evidence-view.tsx (modify) — line 91\n\n## What to do\nTwo TypeScript errors in this file:\n\n### Error 1: Implicit any on map callback parameters (line 91)\n```\nerror TS7006: Parameter \"contentItem\" implicitly has an \"any\" type.\nerror TS7006: Parameter \"index\" implicitly has an \"any\" type.\n```\n\nThe `content` field on `OrgHypercertsContextAttachment.Main` (formerly OrgHypercertsClaimAttachment.Main) is now typed as `($Typed\u003cUri\u003e | $Typed\u003cSmallBlob\u003e)[] | undefined`. The map callback needs explicit types.\n\nFix: Add type annotation to the map callback:\n```ts\nevidence.content?.map((contentItem: $Typed\u003cOrgHypercertsDefs.Uri\u003e | $Typed\u003cOrgHypercertsDefs.SmallBlob\u003e, index: number) =\u003e {\n```\n\nOr use the simpler approach — since the array type is known, just annotate the parameters:\n```ts\nevidence.content?.map((contentItem, index: number) =\u003e {\n```\nTypeScript should infer contentItem from the array type. If it doesnt (because the import changed), ensure the `Attachment` type alias on line 17 uses `OrgHypercertsContextAttachment.Main` (after task .1 updates the import).\n\n### Error 2: This may be a cascading error from the import rename\nAfter task .1 fixes the import from OrgHypercertsClaimAttachment to OrgHypercertsContextAttachment, verify that the `.Main` type still has the same `content` field structure. In v0.10.0, `OrgHypercertsContextAttachment.Main.content` is `($Typed\u003cUri\u003e | $Typed\u003cSmallBlob\u003e)[] | undefined`.\n\n## Dont\n- Do NOT change the rendering logic\n- Do NOT change the component props interface\n- Do NOT remove the content map iteration","acceptance_criteria":"npx tsc --noEmit 2\u003e\u00261 | grep -c \"hypercert-evidence-view\" should return 0.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T15:58:32.48142543+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:29:53.628942341+06:00","closed_at":"2026-03-04T16:29:53.628942341+06:00","close_reason":"22691e4 Fix implicit any and type errors in hypercert-evidence-view.tsx","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-phiw.6","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:58:32.483115623+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.6","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T15:58:32.487767906+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.7","title":"Update measurement record: subject → subjects (singular to array)","description":"## Files\n- lib/create-actions.ts (modify) — lines 336-338 (addMeasurement), lines 530-540 (updateMeasurement)\n- components/measurement-form.tsx (modify) — if it references subject field\n\n## What to do\nIn v0.10.0, `OrgHypercertsContextMeasurement.Record` changed `subject` (singular StrongRef) to `subjects` (optional array of StrongRefs).\n\n### In lib/create-actions.ts addMeasurement (~line 336):\nCurrent code:\n```ts\nconst record: OrgHypercertsClaimMeasurement.Record = {\n  $type: \"org.hypercerts.claim.measurement\",\n  subject,  // singular StrongRef\n  ...\n};\n```\n\nChange to:\n```ts\nconst record: OrgHypercertsContextMeasurement.Record = {\n  $type: \"org.hypercerts.context.measurement\",\n  subjects: [subject],  // wrap in array\n  ...\n};\n```\n\n### In lib/create-actions.ts updateMeasurement (~line 530-540):\nCheck if `subject` is read from existing record and update accordingly. The existing record may have `subject` (old) or `subjects` (new). Handle both:\n```ts\n// Preserve existing subjects, or migrate from old subject field\nconst existingSubjects = existing.subjects ?? (existing.subject ? [existing.subject] : undefined);\n```\n\n### In components/measurement-form.tsx:\nCheck if the form references `subject` field. The example AT-URI placeholder on line 154 uses `org.hypercerts.claim.evidence` which is also outdated — but thats just placeholder text, low priority.\n\n## Dont\n- Do NOT change the addMeasurement function signature (params.subject stays singular — the wrapping happens internally)\n- Do NOT break backward compatibility with existing measurement records that have singular subject\n- Do NOT change the measurement form UI","acceptance_criteria":"After tasks .1 and .2 are complete, the measurement record construction compiles without type errors. OrgHypercertsContextMeasurement.validateRecord succeeds with subjects array.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T15:59:30.25152756+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:36:26.691284514+06:00","closed_at":"2026-03-04T16:36:26.691284514+06:00","close_reason":"3ae420a Update measurement record: subject → subjects (singular to array)","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.7","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:59:30.253238372+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.7","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T15:59:30.258015118+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.7","depends_on_id":"hypercerts-phiw.2","type":"blocks","created_at":"2026-03-04T15:59:30.263294044+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.8","title":"Final verification: run tsc --noEmit and fix any remaining type errors","description":"## Files\n- Any file with remaining TypeScript errors after tasks .1-.7\n\n## What to do\nAfter all preceding tasks are complete, run `npx tsc --noEmit` and fix any remaining lexicon-related TypeScript errors.\n\nExpected error categories that may remain:\n1. **Cascading type errors** from the import/NSID changes that were not caught in individual tasks\n2. **Field name mismatches** between old and new lexicon schemas (e.g., fields renamed, made optional/required)\n3. **$Typed wrapper issues** — v0.10.0 uses `$Typed\u003cT\u003e` wrappers more consistently; some casts may need updating\n4. **.next/ cache errors** — these are NOT lexicon-related; ignore them (TS2307 errors referencing .next/dev/types/ or .next/types/)\n\n### Verification steps:\n1. Run `npx tsc --noEmit 2\u003e\u00261 | grep -v \".next/\"` to see only source errors\n2. For each error, determine if its a lexicon migration issue or a pre-existing issue\n3. Fix only lexicon migration issues\n4. Run again to confirm zero lexicon-related errors\n\n## Dont\n- Do NOT fix pre-existing errors unrelated to the lexicon upgrade\n- Do NOT change business logic\n- Do NOT add new features","acceptance_criteria":"npx tsc --noEmit 2\u003e\u00261 | grep -v \".next/\" should show zero errors. If there are pre-existing non-lexicon errors, document them but do not fix them.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T15:59:50.091143496+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:39:23.346598068+06:00","closed_at":"2026-03-04T16:39:23.346598068+06:00","close_reason":"8b1bb3c Final verification: tsc --noEmit passes with zero lexicon-related errors","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T15:59:50.093398079+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T15:59:50.099055414+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.2","type":"blocks","created_at":"2026-03-04T15:59:50.104270115+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.3","type":"blocks","created_at":"2026-03-04T15:59:50.109233486+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.4","type":"blocks","created_at":"2026-03-04T15:59:50.114171162+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.5","type":"blocks","created_at":"2026-03-04T15:59:50.119787314+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.6","type":"blocks","created_at":"2026-03-04T15:59:50.125224524+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.7","type":"blocks","created_at":"2026-03-04T15:59:50.130188526+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.9","type":"blocks","created_at":"2026-03-04T16:06:48.70632933+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.10","type":"blocks","created_at":"2026-03-04T16:06:48.893040714+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.11","type":"blocks","created_at":"2026-03-04T16:20:19.393444019+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.8","depends_on_id":"hypercerts-phiw.12","type":"blocks","created_at":"2026-03-04T16:20:19.573423886+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-phiw.9","title":"Implement workScope field migration: string/array → WorkScopeString object","description":"## Files\n- app/api/certs/route.ts (modify) — lines 27, 40-41, 76, 133-143: uncomment workScope parsing and convert to WorkScopeString\n- components/hypercerts-base-form.tsx (modify) — lines 63-65, 154-204: update workScope construction\n- components/hypercert-detail-view.tsx (modify) — line 58, 192-208: fix workScope rendering\n- app/hypercerts/page.tsx (modify) — lines 108-110, 157-171: fix workScope rendering on list page\n- lib/types.ts (modify) — line 67: update CreateHypercertParams.workScope type\n- lib/api/types.ts (modify) — lines 28-32: update CreateHypercertRequest.workScope type\n\n## What to do\nIn v0.10.0, `OrgHypercertsClaimActivity.Record.workScope` changed from a plain string to a union type:\n```ts\nworkScope?: $Typed\u003cOrgHypercertsWorkscopeCel.Main\u003e | $Typed\u003cWorkScopeString\u003e | { $type: string }\n```\n\nWhere `WorkScopeString` is:\n```ts\ninterface WorkScopeString {\n  $type?: \"org.hypercerts.claim.activity#workScopeString\";\n  scope: string;  // comma-separated tags\n}\n```\n\n### Step 1: Fix the API route (app/api/certs/route.ts)\nThe workScope parsing is currently commented out (lines 40-41, 76). Uncomment it and convert the string[] from FormData into a WorkScopeString:\n```ts\nconst workScopeRaw = getStringField(formData, \"workScope\");\nconst workScopeTags: string[] = workScopeRaw ? JSON.parse(workScopeRaw) : [];\n```\n\nWhen building the claim record, add:\n```ts\n...(workScopeTags.length \u003e 0 ? {\n  workScope: {\n    $type: \"org.hypercerts.claim.activity#workScopeString\" as const,\n    scope: workScopeTags.join(\", \"),\n  }\n} : {}),\n```\n\n### Step 2: Fix the detail view (components/hypercert-detail-view.tsx)\nLine 58 currently does `Array.isArray(record.workScope)` which will always be false. Replace with:\n```ts\nimport { OrgHypercertsClaimActivity } from \"@hypercerts-org/lexicon\";\n\n// Extract workScope tags\nlet workScopeTags: string[] = [];\nif (record.workScope) {\n  if (OrgHypercertsClaimActivity.isWorkScopeString(record.workScope)) {\n    workScopeTags = record.workScope.scope.split(\",\").map(s =\u003e s.trim()).filter(Boolean);\n  }\n  // Future: handle OrgHypercertsWorkscopeCel if needed\n}\n```\nThen use `workScopeTags` in the rendering (lines 192-208).\n\n### Step 3: Fix the list page (app/hypercerts/page.tsx)\nSame pattern as Step 2 — line 108 does `Array.isArray(cert.workScope)`. Replace with the same WorkScopeString parsing.\n\n### Step 4: Update type definitions\nIn lib/types.ts line 67, update:\n```ts\nworkScope?: string[];  // tags as array of strings (converted to WorkScopeString at API level)\n```\n\nIn lib/api/types.ts lines 28-32, simplify:\n```ts\nworkScope?: string[];  // array of tag strings\n```\n\n### Step 5: Verify form construction\nIn components/hypercerts-base-form.tsx, the form already collects workScope as string[] (line 73). Verify that the `getRecord()` function (lines 154-204) includes workScope in the returned params. Currently it does NOT — add it:\n```ts\nconst record: CreateHypercertParams = {\n  ...existing fields...,\n  workScope: workScope.filter(s =\u003e s.trim()),  // ADD THIS\n};\n```\n\nAlso verify the initialWorkScope parsing on line 63-65 uses `Hypercert.isWorkScopeString()` correctly — it already does.\n\n## Dont\n- Do NOT implement CEL expression support — only WorkScopeString for now\n- Do NOT change the form UI (tag input fields are fine)\n- Do NOT change how workScope is sent via FormData (string[] serialized as JSON)","acceptance_criteria":"WorkScope tags entered in the form are saved to the activity record as a WorkScopeString object. WorkScope tags display correctly on detail and list pages. OrgHypercertsClaimActivity.validateRecord succeeds with the workScope field. npx tsc --noEmit 2\u003e\u00261 | grep -c \"workScope\\|WorkScope\" should return 0 for source files.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-04T16:06:25.0749429+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:38:13.360064085+06:00","closed_at":"2026-03-04T16:38:13.360064085+06:00","close_reason":"ca50094 Implement workScope field migration: string/array → WorkScopeString object","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-phiw.9","depends_on_id":"hypercerts-phiw","type":"parent-child","created_at":"2026-03-04T16:06:25.076766421+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.9","depends_on_id":"hypercerts-phiw.1","type":"blocks","created_at":"2026-03-04T16:06:25.08194259+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-phiw.9","depends_on_id":"hypercerts-phiw.2","type":"blocks","created_at":"2026-03-04T16:06:25.086913337+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-pk6a","title":"Epic: Update README.md and DEVELOPMENT.md to reflect native ATProto migration","description":"The codebase has been fully migrated from @hypercerts-org/sdk-core to native ATProto (direct @atproto/api Agent calls + @hypercerts-org/lexicon for validation). Both README.md and DEVELOPMENT.md are heavily outdated — they still reference the SDK everywhere, show SDK-based code examples, mention the vendor SDK tarball, and the architecture diagram is wrong. Success: both docs accurately describe the current architecture with zero references to the removed SDK dependency. Key constraints: keep the same overall structure/sections, update content in-place, don't remove sections that are still relevant (OAuth, Redis, env vars, troubleshooting).","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-04T15:45:43.966802272+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T15:55:11.559869241+06:00","closed_at":"2026-03-04T15:55:11.559869241+06:00","close_reason":"4659b92 docs updated — zero SDK references, architecture diagram fixed, code examples updated","labels":["scope:medium"]}
{"id":"hypercerts-pk6a.1","title":"Update README.md — remove SDK references, fix architecture diagram, update code examples","description":"## Files\n- README.md (modify)\n\n## What to do\n\n### 1. Update the intro paragraph (line 3)\nChange \"using the Hypercerts SDK\" to \"using native ATProto\". Remove \"SDK\" from the description.\n\n### 2. Remove the \"Important: SDK Version \u0026 Breaking Changes\" section (lines 48-58)\nDelete the entire section. Replace with a short note:\n\n```markdown\n## Architecture Note\n\nThis scaffold uses **native ATProto** — all record operations go through `@atproto/api` directly, with `@hypercerts-org/lexicon` for type definitions and record validation. There is no SDK wrapper layer.\n```\n\n### 3. Update the Architecture Overview diagram (lines 134-159)\nReplace the current diagram with one that reflects the actual architecture:\n- Client (Browser) → API routes → Native ATProto (Agent from @atproto/api)\n- Remove \"Hypercerts SDK (sdk-core)\" box entirely\n- Show: Agent → PDS (XRPC calls: getRecord, createRecord, putRecord, deleteRecord, uploadBlob)\n- Show: Redis for OAuth sessions/state\n- Show: @hypercerts-org/lexicon for validation\n- Keep the OAuth routes (standard + ePDS)\n\n### 4. Update \"Server-Side Authentication\" section (lines 251-294)\nThe `getRepoContext()` example (lines 253-277) is wrong. Update to reflect actual return type:\n```typescript\nimport { getRepoContext } from \"@/lib/repo-context\";\n\nexport async function GET() {\n  const ctx = await getRepoContext();\n  if (!ctx) {\n    return Response.json({ error: \"Not authenticated\" }, { status: 401 });\n  }\n  // ctx.userDid - the authenticated users DID\n  // ctx.activeDid - currently active profile DID\n  // ctx.targetDid - the DID this operation targets\n  // ctx.agent - Agent instance bound to the OAuth session\n\n  // Use agent for direct ATProto calls\n  const result = await ctx.agent.com.atproto.repo.getRecord({\n    repo: ctx.activeDid,\n    collection: \"app.certified.actor.profile\",\n    rkey: \"self\",\n  });\n  return Response.json(result.data.value);\n}\n```\n\nRemove the \"Simpler alternative\" `getAuthenticatedRepo()` example (lines 279-294) — that function no longer exists. Replace with `getAgent()` example:\n```typescript\nimport { getAgent } from \"@/lib/atproto-session\";\n\nexport async function GET() {\n  const agent = await getAgent();\n  if (!agent) {\n    return Response.json({ error: \"Not authenticated\" }, { status: 401 });\n  }\n  // agent is an @atproto/api Agent bound to the OAuth session\n}\n```\n\n### 5. Update \"Working with Repository Context\" section (lines 298-325)\nRemove SDK method calls like `ctx.scopedRepo.profile.getCertifiedProfile()`, `ctx.scopedRepo.hypercert.create()`, `ctx.scopedRepo.hypercert.list()`. Replace with native ATProto examples using `ctx.agent.com.atproto.repo.*`.\n\n### 6. Update \"Project Structure\" tree (lines 329-361)\n- Remove `vendor/` line referencing \"Packed SDK tarballs (unreleased)\"\n- Add `vendor/` with description \"Packed lexicon package (type definitions)\"\n- Change `lib/hypercerts-sdk.ts` description to \"OAuth client initialization (NodeOAuthClient)\"\n- Add entries for new files: `lib/atproto-writes.ts`, `lib/record-validation.ts`\n- Update `lib/repo-context.ts` description\n\n### 7. Update \"Key Files\" table (lines 365-375)\n- Change `lib/hypercerts-sdk.ts` purpose to \"OAuth client initialization with NodeOAuthClient, Redis stores\"\n- Add `lib/atproto-writes.ts` — \"Shared write utilities (StrongRef resolution, location creation, blob upload)\"\n- Add `lib/record-validation.ts` — \"Generic lexicon record validation assertion\"\n- Add `lib/types.ts` — \"TypeScript types, Collections enum, type guards\"\n- Add `lib/blob-utils.ts` — \"Blob reference to URL resolution for rendering\"\n- Update `lib/repo-context.ts` purpose to \"Helper to get authenticated Agent + DID context\"\n\n### 8. Update Authentication section (lines 209-249)\n- Line 213: Change \"implemented via the Hypercerts SDK\" to \"implemented via @atproto/oauth-client-node\"\n- Line 221: Change \"SDK exchanges code for session credentials\" to \"NodeOAuthClient exchanges code for session credentials\"\n\n### 9. Update \"PDS\" description (lines 165-167)\n- Remove \"managed by the Hypercerts SDK\" from the PDS description\n- Change to \"Standard ATProto login authenticates against the users PDS using handle-based OAuth managed by NodeOAuthClient.\"\n\n## Dont\n- Do not remove sections about OAuth, Redis, env vars, localhost redirect, troubleshooting, or ePDS — those are still accurate\n- Do not change the env var table — those vars are still correct\n- Do not add implementation details about internal functions — keep it user-facing\n- Do not reference specific task IDs or beads in the docs","acceptance_criteria":"1. Zero occurrences of \"sdk-core\" or \"Hypercerts SDK\" in README.md (case-insensitive search for \"sdk-core\" returns 0 hits)\n2. Architecture diagram shows Agent/ATProto instead of SDK box\n3. Code examples use ctx.agent.com.atproto.repo.* instead of ctx.scopedRepo.*\n4. getAuthenticatedRepo() is not referenced anywhere\n5. Key Files table includes atproto-writes.ts, record-validation.ts, types.ts, blob-utils.ts\n6. vendor/ description says \"lexicon\" not \"SDK\"\n7. pnpm run build succeeds (docs dont affect build, but verify no syntax issues in markdown)\n8. All code examples in README are valid TypeScript that matches actual function signatures","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-04T15:46:19.950112523+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T15:53:20.094177627+06:00","closed_at":"2026-03-04T15:53:20.094177627+06:00","close_reason":"1391b2d docs: update README to reflect native ATProto migration","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-pk6a.1","depends_on_id":"hypercerts-pk6a","type":"parent-child","created_at":"2026-03-04T15:46:19.951858537+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-pk6a.2","title":"Update DEVELOPMENT.md — remove SDK vendor workflow, update architecture references","description":"## Files\n- DEVELOPMENT.md (modify)\n\n## What to do\n\n### 1. Replace the \"CRITICAL: Unreleased SDK Version\" section (lines 1-35)\nThe entire top section about the unreleased SDK is obsolete. Replace with:\n\n```markdown\n# Development Guide\n\n## Architecture Overview\n\nThis scaffold uses **native ATProto** for all record operations:\n\n- **`@atproto/api`** — `Agent` class for XRPC calls (`getRecord`, `createRecord`, `putRecord`, `deleteRecord`, `uploadBlob`)\n- **`@atproto/oauth-client-node`** — `NodeOAuthClient` for OAuth session management\n- **`@hypercerts-org/lexicon`** — TypeScript types and `validateRecord` functions for all Hypercerts collections\n\nThere is no SDK wrapper layer. The vendor directory contains a packed `@hypercerts-org/lexicon` tarball (type definitions and validators only).\n```\n\n### 2. Remove \"What is a Packed Package?\" section (lines 38-54)\nDelete entirely. The concept of a packed SDK is no longer relevant. If you want, keep a brief note about the lexicon package:\n\n```markdown\n## Vendor Package\n\nThe `vendor/` directory contains a packed `@hypercerts-org/lexicon` tarball. This provides TypeScript types and `validateRecord` functions for Hypercerts collections. It is installed as a local dependency in `package.json`.\n```\n\n### 3. Remove \"Updating the Vendor SDK Package\" section (lines 58-204)\nDelete the entire section (steps 1-9, troubleshooting). Replace with a shorter section about updating the lexicon package:\n\n```markdown\n## Updating the Lexicon Package\n\nThe lexicon package provides TypeScript types and record validators. To update:\n\n1. Clone/pull the [Hypercerts SDK repository](https://github.com/hypercerts-org/hypercerts-sdk)\n2. Build: `pnpm install \u0026\u0026 pnpm build`\n3. Pack: `cd packages/lexicon \u0026\u0026 pnpm pack`\n4. Copy the `.tgz` to `vendor/`\n5. Update `package.json` to point to the new tarball\n6. Run `pnpm install`\n7. Check for type errors: `npx tsc --noEmit`\n\nIf types have changed, update code in `lib/types.ts`, `lib/create-actions.ts`, and `lib/queries.ts` to match.\n```\n\n### 4. Update \"Project Structure\" section (lines 480-515)\n- Remove `vendor/` line about \"Packed SDK versions\" — change to \"Packed lexicon package (types + validators)\"\n- Change `lib/hypercerts-sdk.ts` to \"OAuth client initialization (NodeOAuthClient)\"\n- Add `lib/atproto-writes.ts` — \"ATProto write utilities\"\n- Add `lib/record-validation.ts` — \"Lexicon record validation\"\n- Add `lib/types.ts` — \"TypeScript types and Collections enum\"\n- Add `lib/blob-utils.ts` — \"Blob URL resolution\"\n\n### 5. Update \"Key Files\" table (lines 506-515)\n- Change `lib/hypercerts-sdk.ts` purpose to \"Initializes NodeOAuthClient with OAuth config, Redis storage\"\n- Update `lib/create-actions.ts` purpose to \"Server actions for CRUD operations via native ATProto\"\n- Add new files to the table\n\n### 6. Update \"Testing Against SDK Changes\" section (lines 518-548)\nRename to \"Testing Against Lexicon Changes\" and update the quick test cycle to reference the lexicon package instead of the SDK. Remove the `pnpm link` section or update it for the lexicon package.\n\n### 7. Update \"Contributing\" section (lines 400-477)\n- In \"SDK-Dependent Changes\" (lines 451-458): rename to \"Lexicon-Dependent Changes\" and update references\n- In \"Test thoroughly\" list (lines 436-439): keep the same test items, they are still valid\n- Remove references to \"SDK version\" — change to \"lexicon version\"\n\n### 8. Update \"Common Issues\" section\n- In \"Build Errors After SDK Update\" (lines 376-386): rename to \"Build Errors After Lexicon Update\" and update text\n- Keep all other troubleshooting sections (Redis, OAuth, ngrok) — they are still accurate\n\n## Dont\n- Do not remove the Local Development Workflow section — it is still accurate\n- Do not remove the Prerequisites section — it is still accurate\n- Do not remove the Contributing section — just update SDK references\n- Do not remove troubleshooting sections for Redis, OAuth, ngrok — still valid\n- Do not add internal implementation details — keep it developer-facing\n- Do not reference specific task IDs or beads in the docs","acceptance_criteria":"1. Zero occurrences of \"sdk-core\" in DEVELOPMENT.md (case-insensitive)\n2. Zero occurrences of \"Hypercerts SDK\" in DEVELOPMENT.md (the phrase, not individual words)\n3. The \"Updating the Vendor SDK Package\" multi-step section is replaced with a shorter lexicon update section\n4. Project structure tree includes atproto-writes.ts, record-validation.ts, types.ts, blob-utils.ts\n5. Key Files table references NodeOAuthClient instead of SDK\n6. vendor/ description says \"lexicon\" not \"SDK\"\n7. \"Testing Against SDK Changes\" is renamed/updated to reference lexicon\n8. pnpm run build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":40,"created_at":"2026-03-04T15:46:49.587326276+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T15:54:37.186037737+06:00","closed_at":"2026-03-04T15:54:37.186037737+06:00","close_reason":"61f4346 docs: update DEVELOPMENT.md to reflect native ATProto migration","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-pk6a.2","depends_on_id":"hypercerts-pk6a","type":"parent-child","created_at":"2026-03-04T15:46:49.600881132+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-pu92","title":"Fix: bsky-profile page missing convertBlobUrlToCdn for avatar/banner URLs (from hypercerts-02w.1)","description":"Review of hypercerts-02w.1 found: app/bsky-profile/page.tsx (lines 34-45) calls getBlobURL() for avatar and banner but does NOT call convertBlobUrlToCdn() on the result, unlike app/profile/page.tsx which does. For users on bsky.network PDSes, the raw XRPC getBlob URL will be returned instead of the CDN URL, causing images to load from the PDS directly rather than the CDN. The original SDK returned pre-resolved string URLs so this inconsistency was not visible before. Fix: wrap both getBlobURL() calls with convertBlobUrlToCdn() as done in app/profile/page.tsx.","status":"open","priority":3,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:44.069649802+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:44.069649802+06:00","dependencies":[{"issue_id":"hypercerts-pu92","depends_on_id":"hypercerts-02w.1","type":"discovered-from","created_at":"2026-03-04T11:17:44.140636072+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-pyq","title":"Fix: REPO_COLLECTIONS missing badge collections — add comment documenting intentional exclusion (from hypercerts-02l)","description":"Review of the OAuth scope migration (quick-fixes-test branch, epic hypercerts-02l) found that REPO_COLLECTIONS in lib/config.ts includes 13 of the 16 HYPERCERT_COLLECTIONS from the SDK. Three badge collections are silently absent with no documentation:\\n\\nMissing:\\n- BADGE_AWARD (app.certified.badge.award)\\n- BADGE_DEFINITION (app.certified.badge.definition)\\n- BADGE_RESPONSE (app.certified.badge.response)\\n\\nThe app does not currently write to badge collections (confirmed by reviewing all API routes and create-actions.ts), so this is not a runtime bug today. However:\\n1. There is no comment explaining WHY these are excluded.\\n2. If badge-writing is added to the app in the future, the OAuth scope will silently be too narrow, causing runtime authorization errors.\\n\\nFix: Add a comment above REPO_COLLECTIONS documenting the intentional exclusion:\\n\\n\\nIf badge support is planned in the near term, consider adding the badge collections proactively instead.","status":"open","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-17T23:48:46.324342591+06:00","created_by":"kzoeps","updated_at":"2026-02-17T23:48:46.324342591+06:00","dependencies":[{"issue_id":"hypercerts-pyq","depends_on_id":"hypercerts-02l","type":"discovered-from","created_at":"2026-02-17T23:48:49.530485899+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-q0t","title":"Fix: useEvaluationLinksQuery / useEvidenceLinksQuery / useMeasurementLinksQuery missing enabled guard","description":"## Problem\nThree query hooks fire immediately with whatever `hypercertUri` they receive, including empty strings:\n\n- `queries/hypercerts/use-evaluations-query.ts` line 9: `useEvaluationLinksQuery(hypercertUri)`\n- `queries/hypercerts/use-evidence-query.ts` line 9: `useEvidenceLinksQuery(hypercertUri)`\n- `queries/hypercerts/use-measurements-query.ts` line 9: `useMeasurementLinksQuery(hypercertUri)`\n\nNone of these hooks have an `enabled: !!hypercertUri` guard. If a parent component renders before the URI is available (e.g. during loading), these hooks will fire a request to the Constellation API with an empty or undefined subject, wasting network requests and potentially causing API errors.\n\nCompare with `queries/external/use-user-search-query.ts` which correctly uses `enabled: enabled \u0026\u0026 !!query.trim()`.\n\n## Fix\nAdd an `enabled` guard to each hook:\n```ts\nexport function useEvaluationLinksQuery(hypercertUri: string) {\n  return useQuery({\n    queryKey: queryKeys.hypercerts.evaluations(hypercertUri),\n    queryFn: () =\u003e getEvaluationLinks(hypercertUri),\n    enabled: !!hypercertUri,\n  });\n}\n```\nApply the same fix to `useEvidenceLinksQuery` and `useMeasurementLinksQuery`.\n\n## Evidence\n- `queries/hypercerts/use-evaluations-query.ts` lines 8-13\n- `queries/hypercerts/use-evidence-query.ts` lines 8-13\n- `queries/hypercerts/use-measurements-query.ts` lines 8-13","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:14.375148377+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:14.375148377+06:00"}
{"id":"hypercerts-qq8","title":"Fix: missed {!isLoading \u0026\u0026 !isError \u0026\u0026 (} pattern in 3 section components (from hypercerts-dqf.8)","description":"## Files\n- components/hypercert-evaluations-section.tsx (modify)\n- components/hypercert-evidence-section.tsx (modify)\n- components/hypercert-measurements-section.tsx (modify)\n\n## What to do\nConvert the remaining `{!isLoading \u0026\u0026 !isError \u0026\u0026 (` pattern to ternary in all 3 section components. These were missed by dqf.8.\n\n### hypercert-evaluations-section.tsx (line ~89):\n```tsx\n// Before:\n{!isLoading \u0026\u0026 !isError \u0026\u0026 (\n  \u003c\u003e...\u003c/\u003e\n)}\n\n// After:\n{!isLoading \u0026\u0026 !isError ? (\n  \u003c\u003e...\u003c/\u003e\n) : null}\n```\n\n### hypercert-evidence-section.tsx (line ~95):\nSame pattern — replace `\u0026\u0026 (` with `? (` and add `) : null}`.\n\n### hypercert-measurements-section.tsx (line ~99):\nSame pattern.\n\n## Do not\n- Change any other conditional renders (the simpler ones were already fixed by dqf.8)\n- Change any component logic\n- Change any CSS or styling","acceptance_criteria":"1. All three files have {!isLoading \u0026\u0026 !isError ? (...) : null} instead of {!isLoading \u0026\u0026 !isError \u0026\u0026 (...)}\n2. No \u0026\u0026 conditional rendering patterns remain in these 3 files\n3. Build passes: pnpm build exits 0\n4. Lint passes: pnpm lint exits 0","status":"closed","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-17T23:50:26.25593081+06:00","created_by":"kzoeps","updated_at":"2026-02-17T23:56:37.567317854+06:00","closed_at":"2026-02-17T23:56:37.567317854+06:00","close_reason":"ff602b7 Fix: convert \u0026\u0026 to ternary in 3 section components","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-qq8","depends_on_id":"hypercerts-dqf.8","type":"discovered-from","created_at":"2026-02-17T23:50:29.777999482+06:00","created_by":"kzoeps"},{"issue_id":"hypercerts-qq8","depends_on_id":"hypercerts-dqf","type":"parent-child","created_at":"2026-02-17T23:52:45.875790909+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant","title":"Epic: Resolve CodeRabbit review for PR #14","description":"Resolve all open CodeRabbit inline review comments on PR #14. 8 comments across 6 files covering ownership checks, orphaned record cleanup, type safety, and dead code.","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-04T14:11:43.671869871+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:34:11.682549873+06:00","closed_at":"2026-03-04T14:34:11.682549873+06:00","close_reason":"e6f1aa8 All 8 CodeRabbit review comments resolved — ownership checks, orphan compensation, type guards, dead code removal","labels":["scope:medium"]}
{"id":"hypercerts-rant.1","title":"Add ownership check before contribution child record writes in create-actions.ts","description":"## Files\n- lib/create-actions.ts (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190517\n\n## What to do\nIn the \\`addContribution\\` function (~line 108-203), the code creates \\`contributionDetails\\` and \\`contributorInformation\\` records BEFORE verifying that the caller owns the target hypercert. If the hypercert belongs to another user, those child records are created but the final putRecord fails, leaving dangling records.\n\nMove the ownership validation to BEFORE any child record writes:\n\n1. Parse the hypercertUri and validate it immediately after getting repo context\n2. Check \\`hypercertParsed.did !== ctx.activeDid\\` — if mismatch, throw an error like \\`\"addContribution failed: cannot modify another user's hypercert\"\\`\n3. Fetch the existing hypercert record (getRecord) before creating child records\n4. Only THEN proceed to create contributionDetails and contributorInformation records\n5. The existing \\`resolveStrongRef\\` call can be removed since we're already fetching the record for ownership validation\n\nAlso: when using \\`hypercertParsed.collection\\`, don't fall back to empty string — use the validated collection value directly (it was already checked to be non-empty).\n\n## Don't\n- Don't change the record construction logic for contributionDetails or contributorInformation\n- Don't change the contributor array merging logic\n- Don't remove the assertValidRecord calls","acceptance_criteria":"1. Ownership check (hypercertParsed.did === ctx.activeDid) happens before any createRecord calls\n2. If DID mismatch, function throws a descriptive error without creating any child records\n3. The existing hypercert record is fetched before child record creation\n4. No fallback to empty string for collection — uses validated value\n5. npm run build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T14:12:00.78743237+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:18:39.473217084+06:00","closed_at":"2026-03-04T14:18:39.473217084+06:00","close_reason":"0dc7535 fix: move ownership check before child record writes in addContribution","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-rant.1","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:00.78939856+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant.2","title":"Add ownership check before hypercert update in add-location route","description":"## Files\n- app/api/certs/add-location/route.ts (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190478\n\n## What to do\nAfter parsing the hypercertUri (~line 100-110), add an ownership check before fetching/updating the hypercert:\n\n\\`\\`\\`ts\nif (hypercertParsed.did !== personalRepository.assertDid) {\n  return NextResponse.json(\n    { error: \"Cannot modify another user's hypercert\" },\n    { status: 403 },\n  );\n}\n\\`\\`\\`\n\nPlace this check right after \\`parseAtUri\\` and before the \\`getRecord\\` call.\n\n## Don't\n- Don't change the location creation or putRecord logic\n- Don't change the response shape","acceptance_criteria":"1. Ownership check comparing parsed DID to personalRepository.assertDid exists before getRecord\n2. Returns 403 with descriptive error message on DID mismatch\n3. npm run build succeeds","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T14:12:06.264298426+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:16:18.86056992+06:00","closed_at":"2026-03-04T14:16:18.86056992+06:00","close_reason":"0578cc2 Add ownership check before hypercert update in add-location route","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-rant.2","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:06.265695846+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant.3","title":"Compensate orphaned rights record on claim creation failure in certs route","description":"## Files\n- app/api/certs/route.ts (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190484\n\n## What to do\nIn the POST handler, the rights record is created (~line 122) before the claim record (~line 146-162). If claim validation or creation fails, the rights record is left orphaned.\n\nWrap the claim creation (validation + createRecord) in a try/catch. On failure, delete the previously created rights record as compensation, then re-throw or return the error:\n\n\\`\\`\\`ts\ntry {\n  assertValidRecord(\"activity\", claimRecord, OrgHypercertsClaimActivity.validateRecord);\n  const claimResult = await ctx.agent.com.atproto.repo.createRecord({...});\n  return NextResponse.json({...});\n} catch (e) {\n  // Compensating delete of orphaned rights record\n  const parsedRights = parseAtUri(rightsResult.data.uri);\n  if (parsedRights) {\n    await ctx.agent.com.atproto.repo.deleteRecord({\n      repo: ctx.activeDid,\n      collection: parsedRights.collection || \"org.hypercerts.claim.rights\",\n      rkey: parsedRights.rkey,\n    }).catch(() =\u003e undefined); // best-effort cleanup\n  }\n  if (e instanceof Error \u0026\u0026 e.message.startsWith(\"Invalid\")) {\n    return NextResponse.json({ error: e.message }, { status: 400 });\n  }\n  throw e;\n}\n\\`\\`\\`\n\nImport \\`parseAtUri\\` from \\`@/lib/utils\\` if not already imported.\n\n## Don't\n- Don't change the rights record creation logic\n- Don't change the PUT handler\n- Don't change the response shape for successful creation","acceptance_criteria":"1. If claim validation or createRecord fails, the previously created rights record is deleted (best-effort)\n2. Validation errors still return 400 with descriptive message\n3. Other errors re-throw to the outer catch (500)\n4. parseAtUri is used to extract collection/rkey from the rights URI\n5. npm run build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T14:12:16.016676495+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:15:54.978918629+06:00","closed_at":"2026-03-04T14:15:54.978918629+06:00","close_reason":"9e97a19 Compensate orphaned rights record on claim creation failure","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-rant.3","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:16.028632929+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant.4","title":"Compensate orphaned artifacts on attachment creation failure","description":"## Files\n- app/api/certs/add-attachment/route.ts (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190471\n\n## What to do\nIn the POST handler, blob uploads and optional location creation happen (~lines 143-173) before the final attachment record validation/creation (~lines 188-205). If the final step fails, uploaded blobs and created location records are left orphaned.\n\nBlobs cannot be deleted via the AT Protocol API (no deleteBlob endpoint), so focus on compensating the location record. Track whether a new location record was created (vs resolving an existing one):\n\n1. Add a \\`createdLocationRef\\` variable that's only set when a NEW location is created (not when resolving an existing URI)\n2. Wrap the validation + createRecord in a try/catch\n3. On failure, if \\`createdLocationRef\\` exists, delete it (best-effort):\n\\`\\`\\`ts\nif (createdLocationRef) {\n  const parsed = parseAtUri(createdLocationRef.uri);\n  if (parsed) {\n    await ctx.agent.com.atproto.repo.deleteRecord({\n      repo: ctx.activeDid,\n      collection: parsed.collection || \"app.certified.location\",\n      rkey: parsed.rkey,\n    }).catch(() =\u003e undefined);\n  }\n}\n\\`\\`\\`\n4. Return the appropriate error (400 for validation, re-throw for others)\n\nImport \\`parseAtUri\\` from \\`@/lib/utils\\` if not already imported.\n\n## Don't\n- Don't try to delete uploaded blobs (no API for that)\n- Don't change the blob upload or location creation logic\n- Don't change the response shape for successful creation","acceptance_criteria":"1. A createdLocationRef variable tracks newly created (not resolved) location records\n2. If attachment validation or createRecord fails, the created location record is deleted (best-effort)\n3. Validation errors return 400, other errors re-throw\n4. npm run build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T14:12:27.89615408+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:17:02.149730718+06:00","closed_at":"2026-03-04T14:17:02.149730718+06:00","close_reason":"8fc9e5c Compensate orphaned location records on attachment creation failure","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-rant.4","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:27.897935202+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant.5","title":"Replace unsafe double-cast with type guard for evaluation records","description":"## Files\n- components/hypercert-evaluations-section.tsx (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190489\n\n## What to do\nAt line 63, \\`q.data.value\\` is double-cast with \\`as unknown as Evaluation\\` which bypasses structural checks. If the record is malformed, \\`HypercertEvaluationView\\` will crash accessing \\`evaluation.summary\\`.\n\nAdd a type guard function and use it instead of the double-cast:\n\n\\`\\`\\`ts\nfunction isEvaluation(value: unknown): value is Evaluation {\n  if (!value || typeof value !== \"object\") return false;\n  const v = value as Record\u003cstring, unknown\u003e;\n  return typeof v.summary === \"string\" \u0026\u0026 typeof v.createdAt === \"string\";\n}\n\\`\\`\\`\n\nThen in the mapping loop, replace:\n\\`\\`\\`ts\n// Before\nitems.push({\n  evaluation: q.data.value as unknown as Evaluation,\n  recordUri,\n});\n\n// After\nif (isEvaluation(q.data.value)) {\n  items.push({\n    evaluation: q.data.value,\n    recordUri,\n  });\n}\n// Skip invalid records silently (or log a warning)\n\\`\\`\\`\n\nCheck the \\`Evaluation\\` interface (defined in the same file or imported) to see what fields are required, and validate those in the type guard.\n\n## Don't\n- Don't change the Evaluation interface\n- Don't change HypercertEvaluationView\n- Don't change the query logic","acceptance_criteria":"1. A type guard function (isEvaluation or similar) validates the record shape before use\n2. The double-cast \\`as unknown as Evaluation\\` is removed\n3. Invalid records are skipped (not pushed into items)\n4. npm run build succeeds","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T14:12:34.420318022+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:18:17.597132949+06:00","closed_at":"2026-03-04T14:18:17.597132949+06:00","close_reason":"74184a7 Replace unsafe double-cast with isEvaluation type guard","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-rant.5","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:34.421649007+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant.6","title":"Tighten blob validation to check ref.$link structure","description":"## Files\n- lib/blob-utils.ts (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190500\n\n## What to do\nAt line 44-45, the blob detection check \\`obj.\\$type === \"blob\" || (obj.ref \u0026\u0026 obj.mimeType)\\` is too loose. An object like \\`{ref: \"string\", mimeType: \"text\"}\\` would pass but produce a malformed URL.\n\nAdd a proper type guard that validates the AT Protocol BlobRef structure:\n\n\\`\\`\\`ts\nfunction isBlobRefLike(v: Record\u003cstring, unknown\u003e): v is BlobRef {\n  if (v.\\$type !== \"blob\") return false;\n  if (typeof v.mimeType !== \"string\") return false;\n  const ref = v.ref as Record\u003cstring, unknown\u003e | undefined;\n  return !!ref \u0026\u0026 typeof ref.\\$link === \"string\";\n}\n\\`\\`\\`\n\nReplace the loose check:\n\\`\\`\\`ts\n// Before\nif (obj.\\$type === \"blob\" || (obj.ref \u0026\u0026 obj.mimeType)) {\n  return await resolveBlobToUrl(obj as unknown as BlobRef, ownerDid);\n}\n\n// After\nif (isBlobRefLike(obj)) {\n  return await resolveBlobToUrl(obj, ownerDid);\n}\n\\`\\`\\`\n\nThis also removes the \\`as unknown as BlobRef\\` cast.\n\n## Don't\n- Don't change resolveBlobToUrl or getBlobURL\n- Don't change the recursive traversal logic in resolveRecordBlobs","acceptance_criteria":"1. A type guard (isBlobRefLike or similar) validates \\$type === \"blob\", typeof mimeType === \"string\", and ref.\\$link is a string\n2. The loose check (obj.ref \u0026\u0026 obj.mimeType) is replaced with the type guard\n3. The \\`as unknown as BlobRef\\` cast is removed\n4. npm run build succeeds","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T14:12:42.92449204+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:22:55.780307946+06:00","closed_at":"2026-03-04T14:22:55.780307946+06:00","close_reason":"1071fe7 tighten blob validation with isBlobRefLike type guard","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-rant.6","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:42.925922392+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant.7","title":"Fail fast when collection cannot be resolved in resolveStrongRef","description":"## Files\n- lib/atproto-writes.ts (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190498\n\n## What to do\nAt line 34-37, \\`resolveStrongRef\\` can send an empty collection string to \\`getRecord\\` when both \\`parsed.collection\\` and \\`fallbackCollection\\` are falsy. This produces a confusing upstream error.\n\nAdd a guard before the getRecord call:\n\n\\`\\`\\`ts\nconst collection = parsed.collection || fallbackCollection;\nif (!collection) {\n  throw new Error(\\`Missing collection in AT-URI and no fallback provided: \\${uri}\\`);\n}\nconst result = await agent.com.atproto.repo.getRecord({\n  repo: parsed.did,\n  collection,\n  rkey: parsed.rkey,\n});\n\\`\\`\\`\n\n## Don't\n- Don't change the function signature\n- Don't change the return type","acceptance_criteria":"1. If both parsed.collection and fallbackCollection are falsy, throws a descriptive error mentioning the URI\n2. getRecord is never called with an empty collection string\n3. npm run build succeeds","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-04T14:12:48.723734387+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:15:06.08208863+06:00","closed_at":"2026-03-04T14:15:06.08208863+06:00","close_reason":"5a15ee6 Fail fast when collection cannot be resolved in resolveStrongRef","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-rant.7","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:48.725001983+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rant.8","title":"Remove dead !records loader branch in hypercerts page","description":"## Files\n- app/hypercerts/page.tsx (modify)\n\n## Context\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/14#discussion_r2882190487\n\n## What to do\nThe \\`records\\` variable is assigned from a \\`.filter().map()\\` chain (~lines 47-52) which always returns an array. The condition \\`!records\\` on line 72 is always false, making the \\`\u003cLoader /\u003e\\` branch unreachable dead code.\n\nRemove the dead branch:\n\\`\\`\\`tsx\n// Before\n{!records ? (\n  \u003cdiv className=\"animate-fade-in-up [animation-delay:100ms]\"\u003e\n    \u003cdiv className=\"glass-panel mx-auto max-w-md rounded-2xl p-12\"\u003e\n      \u003cLoader /\u003e\n    \u003c/div\u003e\n  \u003c/div\u003e\n) : records.length === 0 ? (\n  ...\n) : (\n  ...\n)}\n\n// After\n{records.length === 0 ? (\n  ...\n) : (\n  ...\n)}\n\\`\\`\\`\n\nAlso remove the \\`Loader\\` import if it becomes unused after this change.\n\n## Don't\n- Don't change the empty state or records rendering logic\n- Don't change the data fetching","acceptance_criteria":"1. The \\`!records\\` ternary branch with \u003cLoader /\u003e is removed\n2. The records.length === 0 empty state and records list rendering are preserved\n3. Loader import is removed if unused\n4. npm run build succeeds","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-04T14:12:55.878997004+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:18:49.206613948+06:00","closed_at":"2026-03-04T14:18:49.206613948+06:00","close_reason":"f3189b3 Remove dead !records loader branch in hypercerts page","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-rant.8","depends_on_id":"hypercerts-rant","type":"parent-child","created_at":"2026-03-04T14:12:55.880422056+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rbuv","title":"Fix: addContribution makes redundant resolveStrongRef call whose result is discarded (from hypercerts-czy.2)","description":"Review of hypercerts-czy.2 found: addContribution() in lib/create-actions.ts (lines 130-135) calls resolveStrongRef() to validate the hypercertUri, but the returned StrongRef value is discarded. Immediately after, getRecord() is called again on the same URI (lines 137-144), making two sequential network round-trips to the PDS for the same record. Fix: either (a) use the result of resolveStrongRef() to get the CID and skip the second getRecord, or (b) remove the resolveStrongRef() call and just use getRecord() directly (adding a null check on the result).","status":"open","priority":3,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:36.273907725+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:36.273907725+06:00","dependencies":[{"issue_id":"hypercerts-rbuv","depends_on_id":"hypercerts-czy.2","type":"discovered-from","created_at":"2026-03-04T11:17:36.341823467+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rxj","title":"Epic: Add signup mode to login dialog instead of redirecting to PDS","description":"## Why\nCurrently when users click 'Create an account' in the login dialog, they get redirected away to the PDS URL (handle tab) or to /api/oauth/login without email (email tab). This is confusing because users leave the app and don't understand they're creating an account.\n\n## What success looks like\n- The login dialog supports a 'signup' mode that shows the same form fields but with signup-oriented wording\n- 'Create an account' buttons switch the dialog to signup mode instead of redirecting\n- In signup mode: heading says 'Create Account' instead of 'Sign In', subheading says 'Get started with your account', submit buttons say 'Create Account' / 'Sign Up', and there's a link back to sign-in mode ('Already have an account? Sign in')\n- The underlying authentication logic remains IDENTICAL — same API calls, same OAuth flows, same email/handle submission\n- Both the navbar dialog and the full-page LoginDialog (in SignedInProvider) support the mode\n- The home page 'Sign In to Continue' / 'Sign In to Get Started' placeholder buttons should also be able to trigger signup mode\n\n## Key constraints\n- NO changes to API routes or authentication logic — this is purely a UI/UX change\n- The LoginDialog component must accept an optional 'mode' prop (or use internal state) to toggle between sign-in and sign-up wording\n- The PillToggle (Handle/Email tabs) should still work in both modes\n- All existing sign-in functionality must continue to work exactly as before","status":"closed","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-02-25T14:52:31.398637796+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T15:36:28.462859856+06:00","closed_at":"2026-02-25T15:36:28.462859856+06:00","close_reason":"f7b4606 All 3 children completed — signup mode for login dialog","labels":["scope:medium"]}
{"id":"hypercerts-rxj.1","title":"Refactor LoginDialog to support sign-in vs sign-up mode via internal state","description":"## Files\n- components/login-dialog.tsx (modify)\n\n## What to do\nAdd a `mode` state to `LoginDialog` that toggles between `\"signin\"` and `\"signup\"`. The mode controls ALL text/wording in the dialog and also controls which tabs are visible.\n\n### State changes:\n1. Add `type AuthMode = \"signin\" | \"signup\"` type alias near the existing `Tab` type (line 16)\n2. Add `const [mode, setMode] = useState\u003cAuthMode\u003e(\"signin\")` to `LoginDialog` (alongside existing `activeTab` state)\n3. Pass `mode` and `setMode` as props to `HandleForm` and `EmailForm`\n\n### Key behavior: Hide Handle tab in signup mode\n- When `mode === \"signup\"`, do NOT render the `PillToggle` at all, and ALWAYS render `EmailForm` (never `HandleForm`)\n- When `mode === \"signin\"`, render the `PillToggle` (if `hasEpds`) and show `HandleForm` or `EmailForm` based on `activeTab` — exactly as today\n- When switching from signup back to signin, restore the `activeTab` state (it should persist across mode switches)\n\n### Wording changes based on mode:\n\n**LoginDialog heading:**\n- signin: \"Sign In\"\n- signup: \"Create Account\"\n\n**LoginDialog subheading:**\n- signin: \"Choose how to continue\"\n- signup: \"Get started with your account\"\n\n**HandleForm (only shown in signin mode):**\n- Submit button: \"Sign In\" (unchanged)\n- Bottom link: \"Create an account\" button that calls `setMode(\"signup\")` instead of `redirectToAccountCreation()`\n- Remove the `redirectToAccountCreation` function entirely\n\n**EmailForm changes based on mode:**\n- Submit button: signin → \"Continue\", signup → \"Create Account\"\n- Helper text: signin → \"Enter your email for a direct code, or continue without\", signup → \"Enter your email to create your account\"\n- Bottom link:\n  - signin: \"Don't have an account? Create one\" → calls `setMode(\"signup\")`\n  - signup: \"Already have an account? Sign in\" → calls `setMode(\"signin\")`\n- In BOTH modes, the submit logic is identical: navigate to `/api/oauth/login?email=...` (or without email if empty). The ePDS handles both login and signup.\n\n### Logic changes:\n- **HandleForm submit:** calls `loginMutation.mutate(handle)` — UNCHANGED (only shown in signin mode anyway)\n- **EmailForm submit (both modes):** calls `handleContinue()` which navigates to `/api/oauth/login?email=...` — UNCHANGED\n- Remove the `redirectToAccountCreation` function from HandleForm\n- Remove the `onClick` handler that does `window.location.href = \"/api/oauth/login\"` from EmailForm bottom button (replace with `setMode(\"signup\")` in signin, `setMode(\"signin\")` in signup)\n\n### Component signatures:\n```tsx\ntype AuthMode = \"signin\" | \"signup\";\n\nfunction HandleForm({ mode, setMode }: { mode: AuthMode; setMode: (m: AuthMode) =\u003e void }) { ... }\nfunction EmailForm({ mode, setMode }: { mode: AuthMode; setMode: (m: AuthMode) =\u003e void }) { ... }\n```\n\n### Rendering logic in LoginDialog:\n```tsx\n{/* Only show PillToggle in signin mode when ePDS is available */}\n{mode === \"signin\" \u0026\u0026 hasEpds \u0026\u0026 \u003cPillToggle active={activeTab} onChange={setActiveTab} /\u003e}\n\n{/* In signup mode, always show EmailForm. In signin mode, show based on activeTab */}\n{mode === \"signup\" ? (\n  \u003cEmailForm mode={mode} setMode={setMode} /\u003e\n) : activeTab === \"handle\" || !hasEpds ? (\n  \u003cHandleForm mode={mode} setMode={setMode} /\u003e\n) : (\n  \u003cEmailForm mode={mode} setMode={setMode} /\u003e\n)}\n```\n\n## Don't\n- Change any API routes or backend logic\n- Change the PillToggle component itself (it stays the same, just conditionally rendered)\n- Change any CSS classes or styling (only text content and conditional rendering changes)\n- Remove the existing `useLoginMutation` or `handleContinue` logic\n- Add any new dependencies or imports (except the AuthMode type)\n- Show HandleForm in signup mode\n- Change the component's default export signature — LoginDialog should still be a no-props default export for now","acceptance_criteria":"1. LoginDialog renders with \"Sign In\" heading, \"Choose how to continue\" subheading, and PillToggle visible by default (signin mode)\n2. Clicking \"Create an account\" in HandleForm switches to signup mode\n3. In signup mode: heading is \"Create Account\", subheading is \"Get started with your account\"\n4. In signup mode: PillToggle is NOT rendered (hidden)\n5. In signup mode: only EmailForm is shown (HandleForm is never rendered)\n6. In signup mode: EmailForm shows \"Create Account\" submit button and \"Enter your email to create your account\" helper text\n7. In signup mode: EmailForm shows \"Already have an account? Sign in\" link that switches back to signin mode\n8. Switching back to signin mode restores the PillToggle and the previously selected tab\n9. In signin mode: EmailForm bottom link says \"Don't have an account? Create one\" and switches to signup mode (no redirect)\n10. EmailForm submit logic is unchanged in both modes (navigates to /api/oauth/login with optional email param)\n11. HandleForm submit logic is unchanged (only visible in signin mode)\n12. The redirectToAccountCreation function is removed\n13. No TypeScript errors, app builds successfully","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-02-25T14:52:57.548034619+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T15:30:35.228805417+06:00","closed_at":"2026-02-25T15:30:35.228805417+06:00","close_reason":"e9fc907 Refactor LoginDialog to support signin/signup mode via internal state","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-rxj.1","depends_on_id":"hypercerts-rxj","type":"parent-child","created_at":"2026-02-25T14:52:57.54960428+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rxj.2","title":"Add initialMode prop to LoginDialog and wire up navbar + SignedInProvider","description":"## Files\n- components/login-dialog.tsx (modify)\n- components/navbar.tsx (modify)\n\n## What to do\nAfter hypercerts-rxj.1 adds internal mode state to LoginDialog, this task adds an optional `initialMode` prop so callers can open the dialog directly in signup mode.\n\n### 1. LoginDialog prop (components/login-dialog.tsx)\n- Change the default export signature to accept an optional prop:\n```tsx\nexport default function LoginDialog({ initialMode = \"signin\" }: { initialMode?: AuthMode } = {}) {\n```\n- Initialize the mode state with `initialMode`:\n```tsx\nconst [mode, setMode] = useState\u003cAuthMode\u003e(initialMode);\n```\n- Export the `AuthMode` type so other files can import it:\n```tsx\nexport type AuthMode = \"signin\" | \"signup\";\n```\n\n### 2. Navbar dialog (components/navbar.tsx)\n- Add state to control the dialog open/close: `const [dialogOpen, setDialogOpen] = useState(false)`\n- Add state for the initial mode: `const [loginMode, setLoginMode] = useState\u003cAuthMode\u003e(\"signin\")`\n- Import `AuthMode` from `@/components/login-dialog`\n- Import `useState` from react\n- Replace the uncontrolled `\u003cDialog\u003e` with a controlled one: `\u003cDialog open={dialogOpen} onOpenChange={setDialogOpen}\u003e`\n- Replace the single \"Sign In\" DialogTrigger button with two buttons:\n```tsx\n\u003cdiv className=\"flex items-center gap-2\"\u003e\n  \u003cButton\n    disabled={isLoading}\n    size=\"sm\"\n    variant=\"outline\"\n    onClick={() =\u003e { setLoginMode(\"signup\"); setDialogOpen(true); }}\n    className=\"font-[family-name:var(--font-outfit)] font-medium\"\n  \u003e\n    Create Account\n  \u003c/Button\u003e\n  \u003cButton\n    disabled={isLoading}\n    size=\"sm\"\n    onClick={() =\u003e { setLoginMode(\"signin\"); setDialogOpen(true); }}\n    className=\"bg-create-accent hover:bg-create-accent/90 text-create-accent-foreground font-[family-name:var(--font-outfit)] font-medium shadow-sm\"\n  \u003e\n    Sign In\n  \u003c/Button\u003e\n\u003c/div\u003e\n```\n- Remove the `\u003cDialogTrigger\u003e` wrapper (since we are now controlling open state manually via onClick handlers)\n- Pass `initialMode={loginMode}` to `\u003cLoginDialog initialMode={loginMode} /\u003e`\n- Use a `key={loginMode}` on LoginDialog to reset internal state when mode changes:\n```tsx\n\u003cLoginDialog key={loginMode} initialMode={loginMode} /\u003e\n```\n- Update the `\u003cDialogTitle className=\"sr-only\"\u003e` to be dynamic: `{loginMode === \"signin\" ? \"Sign In\" : \"Create Account\"}`\n\n### 3. SignedInProvider (providers/SignedInProvider.tsx)\n- No changes needed — the full-page LoginDialog defaults to signin mode, and users can switch to signup mode using the internal toggle added in rxj.1.\n\n### Important: \"Create Account\" opens directly into Email-only signup mode\nBecause rxj.1 hides the Handle tab in signup mode, clicking \"Create Account\" in the navbar will open the dialog showing only the EmailForm with signup wording. The user can click \"Already have an account? Sign in\" to get back to the full signin view with Handle/Email tabs.\n\n## Don't\n- Change any API routes or backend logic\n- Change the NavbarProps interface (no new server-side props needed)\n- Add any new server components or data fetching\n- Change the dropdown menu or signed-in state UI\n- Change providers/SignedInProvider.tsx","acceptance_criteria":"1. LoginDialog accepts an optional initialMode prop that defaults to \"signin\"\n2. AuthMode type is exported from components/login-dialog.tsx\n3. Navbar shows two buttons when not signed in: \"Create Account\" (outline) and \"Sign In\" (accent)\n4. Clicking \"Sign In\" opens dialog in signin mode with PillToggle visible and \"Sign In\" heading\n5. Clicking \"Create Account\" opens dialog in signup mode with NO PillToggle, only EmailForm, and \"Create Account\" heading\n6. Switching between modes inside the dialog still works (via the \"Already have an account?\" / \"Create an account\" toggle links from rxj.1)\n7. The full-page LoginDialog in SignedInProvider still works and defaults to signin mode\n8. DialogTitle sr-only text matches the current mode\n9. No TypeScript errors, app builds successfully","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-25T14:53:19.887727452+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T15:32:52.227366704+06:00","closed_at":"2026-02-25T15:32:52.227366704+06:00","close_reason":"f450c30 Add initialMode prop to LoginDialog and wire up navbar with two auth buttons","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-rxj.2","depends_on_id":"hypercerts-rxj","type":"parent-child","created_at":"2026-02-25T14:53:19.889314646+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-rxj.2","depends_on_id":"hypercerts-rxj.1","type":"blocks","created_at":"2026-02-25T14:53:19.891485039+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rxj.3","title":"Update home page CTA buttons to open login dialog instead of href=#","description":"## Files\n- app/page.tsx (modify)\n- components/navbar.tsx (modify — only if needed for scroll-to-top behavior)\n\n## What to do\nThe home page (app/page.tsx) has two placeholder buttons that link to `href=\"#\"` when the user is not signed in:\n1. Line 97: \"Sign In to Continue\" in the hero section\n2. Line 277: \"Sign In to Get Started\" in the footer CTA section\n\nThese buttons currently do nothing useful. Since the page already shows the full-page LoginDialog via SignedInProvider when not signed in, these buttons are actually never visible to unauthenticated users (SignedInProvider replaces children with LoginDialog).\n\n### What to change:\nSince `app/page.tsx` is a **server component** and the LoginDialog is already shown by SignedInProvider when not signed in, these placeholder buttons are dead code — they can never be seen by an unauthenticated user.\n\n**Option: Remove the dead code**\n- In the hero section (lines 87-103): Remove the entire `else` branch of the `isSignedIn` ternary. The `!isSignedIn` case is unreachable because SignedInProvider already intercepts and shows LoginDialog.\n- In the footer CTA section (lines 271-282): Same — remove the `else` branch.\n- Simplify both sections to only render the signed-in buttons (no conditional needed, since the page only renders when signed in).\n\n### Verification:\n- Check that `providers/SignedInProvider.tsx` wraps the page layout and confirms: when `!session`, it renders `\u003cLoginDialog /\u003e` instead of `{children}`. This means `app/page.tsx` only ever renders when the user IS signed in.\n- If SignedInProvider does NOT wrap the home page (check `app/layout.tsx`), then instead of removing the buttons, convert them to client-side buttons that scroll to the LoginDialog or show a message.\n\n## Don't\n- Change SignedInProvider behavior\n- Add client-side interactivity to app/page.tsx (it's a server component)\n- Change any API routes\n- Remove the signed-in state buttons (Create Hypercert, View My Hypercerts)","acceptance_criteria":"1. The home page no longer contains any href=\"#\" placeholder links\n2. If the page is wrapped by SignedInProvider (unreachable when not signed in), the !isSignedIn branches are removed and the page only renders signed-in content\n3. If the page is NOT wrapped by SignedInProvider, the buttons are converted to meaningful actions (scroll to login, etc.)\n4. No TypeScript errors, app builds successfully\n5. The signed-in hero and footer CTA sections still work correctly","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-02-25T14:53:39.635050462+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T15:35:18.405700259+06:00","closed_at":"2026-02-25T15:35:18.405700259+06:00","close_reason":"f7b4606 Remove dead !isSignedIn branches and href=# placeholder links from home page","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-rxj.3","depends_on_id":"hypercerts-rxj","type":"parent-child","created_at":"2026-02-25T14:53:39.637060313+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-rxj.3","depends_on_id":"hypercerts-rxj.2","type":"blocks","created_at":"2026-02-25T14:53:39.639549318+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rxj.4","title":"Fix: HandleForm 'Create an account' button should redirect to PDS URL, not switch to signup mode","description":"## Files\n- components/login-dialog.tsx (modify)\n\n## What to do\nIn the current code, the HandleForm \"Create an account\" button calls `setMode(\"signup\")` which switches to the email-only signup view. This is wrong for the Handle tab — ATProto account creation requires redirecting to the PDS.\n\n### Restore the PDS redirect in HandleForm:\n1. Re-add the `redirectToAccountCreation` function to `HandleForm`:\n```tsx\nconst redirectToAccountCreation = () =\u003e {\n  if (!process.env.NEXT_PUBLIC_PDS_URL) {\n    throw new Error(\"NEXT_PUBLIC_PDS_URL is not defined\");\n  }\n  loginMutation.mutate(process.env.NEXT_PUBLIC_PDS_URL);\n};\n```\n\n2. Change the \"Create an account\" button onClick back to `redirectToAccountCreation`:\n```tsx\n\u003cButton\n  disabled={loginMutation.isPending}\n  onClick={redirectToAccountCreation}\n  variant=\"ghost\"\n  type=\"button\"\n  className=\"w-full font-[family-name:var(--font-outfit)] text-muted-foreground hover:text-create-accent hover:bg-muted/50 transition-colors\"\n\u003e\n  Create an account\n\u003c/Button\u003e\n```\n\n3. Since HandleForm no longer needs `setMode`, remove it from the props. HandleForm only needs `mode` (to keep the type signature consistent, though it is only ever rendered in signin mode). Actually, HandleForm does not need `mode` or `setMode` at all anymore since it is only shown in signin mode and its \"Create an account\" button always redirects. Simplify:\n```tsx\nfunction HandleForm() {\n  // ... same as original, with redirectToAccountCreation restored\n}\n```\n\n4. Update the `HandleForm` call site in `LoginDialog` — remove the `mode` and `setMode` props:\n```tsx\n\u003cHandleForm /\u003e\n```\n\n### Leave EmailForm unchanged:\n- EmailForm still uses `setMode(\"signup\")` / `setMode(\"signin\")` for its bottom link — this is correct\n- EmailForm still accepts `mode` and `setMode` props — this is correct\n\n## Don't\n- Change EmailForm behavior\n- Change the signup mode rendering logic (PillToggle hidden, only EmailForm shown)\n- Change the navbar buttons\n- Change any API routes","acceptance_criteria":"1. In signin mode on the Handle tab, clicking \"Create an account\" redirects to the PDS URL via loginMutation.mutate(process.env.NEXT_PUBLIC_PDS_URL) — NOT switching to signup mode\n2. HandleForm no longer accepts mode/setMode props\n3. EmailForm \"Don't have an account? Create one\" still switches to signup mode (unchanged)\n4. The navbar \"Create Account\" button still opens the dialog in signup mode showing only EmailForm (unchanged)\n5. No TypeScript errors, app builds successfully","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-25T15:57:53.708410005+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-25T15:59:26.415291386+06:00","closed_at":"2026-02-25T15:59:26.415291386+06:00","close_reason":"1839917 Fix HandleForm 'Create an account' to redirect to PDS URL","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-rxj.4","depends_on_id":"hypercerts-rxj","type":"parent-child","created_at":"2026-02-25T15:57:53.709896926+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-rxt","title":"Fix: Evaluations/Evidence/Measurements sections use array index as React key for fetched records","description":"## Files\\n- components/hypercert-evaluations-section.tsx line 94\\n- components/hypercert-evidence-section.tsx line 100\\n- components/hypercert-measurements-section.tsx line 105\\n- components/hypercert-evaluation-view.tsx lines 86, 111, 136\\n- components/hypercert-evidence-view.tsx line 88\\n- components/hypercert-measurement-view.tsx lines 89, 112\\n\\n## Severity\\nMEDIUM — React correctness\\n\\n## Problem\\nAll three section components use array index as the React key when rendering fetched records. If records are reordered, added, or removed, React will incorrectly reuse DOM nodes, causing stale UI or animation glitches. The inner view components also use index as key for evaluators, content URIs, and evidence sources.\\n\\n## Evidence\\n```tsx\\n// hypercert-evaluations-section.tsx line 94\\n{evaluations.map((evaluation, index) =\u003e (\\n  \u003cHypercertEvaluationView key={index} evaluation={evaluation} /\u003e\\n))}\\n\\n// hypercert-evaluation-view.tsx line 86\\n{evaluation.evaluators.map((evaluator, index) =\u003e (\\n  \u003cdiv key={index} ...\u003e\\n```\\n\\n## Fix\\nUse stable identifiers as keys. For top-level records, use the AT URI or CID if available. For evaluators/measurers/content URIs (which are strings), use the string value itself:\\n```tsx\\n// Section level — use URI or CID from the record\\n{evaluations.map((evaluation) =\u003e (\\n  \u003cHypercertEvaluationView key={evaluation.createdAt + evaluation.summary.slice(0, 20)} evaluation={evaluation} /\u003e\\n))}\\n\\n// Inner lists — use the string value\\n{evaluation.evaluators.map((evaluator) =\u003e {\\n  const did = typeof evaluator === 'object' ? evaluator.did : evaluator;\\n  return \u003cdiv key={did} ...\u003e;\\n})}\\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:01.665915684+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:01.665915684+06:00"}
{"id":"hypercerts-s11","title":"Fix: vendor/hypercerts-org-lexicon-0.10.0-beta.15.tgz is in vendor/ but package.json still installs beta.14 from npm","description":"Review of the loopback branch found a mismatch between the vendored lexicon tarball and the installed version.\n\nEvidence:\n- vendor/ directory contains: hypercerts-org-lexicon-0.10.0-beta.15.tgz (new file added in this branch)\n- package.json line 28: '@hypercerts-org/lexicon': '0.10.0-beta.14' (installs from npm registry)\n- pnpm-lock.yaml confirms beta.14 is resolved from npm (not from the local tgz)\n- The beta.15 tgz in vendor/ is never referenced anywhere in package.json\n\nThis means the beta.15 tarball is dead weight — it was added to vendor/ but the package.json was never updated to use it. Developers will install beta.14 from npm, not the vendored beta.15.\n\nTwo possible intended states:\n1. If beta.15 should be used: update package.json to 'file:./vendor/hypercerts-org-lexicon-0.10.0-beta.15.tgz' and run pnpm install\n2. If beta.14 from npm is correct: remove the beta.15 tgz from vendor/ to avoid confusion\n\nAlso: DEVELOPMENT.md documents how to update the sdk-core vendor package but says nothing about the lexicon vendor package. If lexicon is also being vendored, the docs should cover it.\n\nNote: @hypercerts-org/sdk-core (beta.8) depends on @hypercerts-org/lexicon beta.14, so the versions are currently consistent — but the orphaned beta.15 tgz is confusing.","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:54.005302362+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:54.005302362+06:00"}
{"id":"hypercerts-scaffold-0go","title":"Simplify JWK key generation with script","status":"in_progress","priority":2,"issue_type":"feature","owner":"aspiers","created_at":"2026-01-28T17:13:26.340137246+13:00","created_by":"Adam Spiers","updated_at":"2026-01-28T17:13:32.702507218+13:00"}
{"id":"hypercerts-scaffold-9rb","title":"Migrate OAuth scopes from transitional to granular permissions","description":"Code is using transitional OAuth scopes (transition:generic). Should migrate to granular permissions like repo:* or use ScopePresets.FULL_ACCESS from @hypercerts-org/sdk-core.","notes":"Superseded by new epic with proper decomposition","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"aspiers","created_at":"2026-01-29T00:00:26.475561542+13:00","created_by":"Adam Spiers","updated_at":"2026-02-17T19:10:10.058147023+06:00","closed_at":"2026-02-17T19:10:10.058154027+06:00"}
{"id":"hypercerts-scaffold-cw2","title":"Add lock mechanism for ATProto credentials","description":"ATProto SDK warning: No lock mechanism provided. Credentials might get revoked. Need to implement a lock mechanism to prevent credential revocation.","status":"open","priority":3,"issue_type":"task","owner":"aspiers","created_at":"2026-01-29T00:00:28.611778943+13:00","created_by":"Adam Spiers","updated_at":"2026-01-29T00:00:28.611778943+13:00"}
{"id":"hypercerts-scaffold-mk1","title":"Fix private JWK format - replace 'use' with 'key_ops'","description":"CRITICAL: Private JWK with 'use' property is causing authentication to fail completely. Error chain: ERR_CRYPTO_INVALID_JWK → ERR_JWK_INVALID → Unable to create JWT. Users cannot log in. Need to replace 'use' with valid 'key_ops' in the JWK configuration.","status":"closed","priority":0,"issue_type":"task","owner":"aspiers","created_at":"2026-01-29T00:00:27.640799834+13:00","created_by":"Adam Spiers","updated_at":"2026-01-29T00:05:01.57288757+13:00","closed_at":"2026-01-29T00:05:01.57288757+13:00","close_reason":"Fixed by regenerating JWK with npm run generate-jwk. The script already correctly sets key_ops and removes the deprecated use property."}
{"id":"hypercerts-scaffold-r25","title":"Configure allowedDevOrigins in next.config for ngrok","description":"Next.js detected cross-origin request from ngrok domain to /_next/* resources. Future versions will require explicit configuration of allowedDevOrigins. See: https://nextjs.org/docs/app/api-reference/config/next-config-js/allowedDevOrigins","status":"open","priority":2,"issue_type":"task","owner":"aspiers","created_at":"2026-01-29T00:00:24.942442259+13:00","created_by":"Adam Spiers","updated_at":"2026-01-29T00:00:24.942442259+13:00"}
{"id":"hypercerts-sis","title":"Fix: double-callback not handled — second callback with same code succeeds silently (from hypercerts-dhz.6)","description":"Review of hypercerts-dhz found: The ePDS callback route (app/api/oauth/callback/route.ts) does not delete the epds-oauth-session cookie before the token exchange. The cookie is only cleared AFTER a successful exchange (line 148). This means:\n\n1. If the user hits the callback URL twice (browser back button, duplicate tab, network retry), the second request will find the same cookie, pass state verification, and attempt a second token exchange with the same authorization code.\n2. Authorization codes are single-use — the second exchange will fail with 'invalid_grant' from the token endpoint, returning a 502 to the user even though they are already logged in.\n\nThe correct pattern is to delete the session cookie immediately after reading it (before the token exchange), so that any duplicate callback gets a 'No OAuth session found' 400 instead of a confusing 502.\n\nAdditionally, the cookie deletion at line 148 uses `maxAge: 0` but does not set `httpOnly: true` or `path: '/'` consistently — some browsers may not honor the deletion if the attributes don't match the original cookie exactly.\n\nFix: (a) delete the cookie immediately after reading it (before token exchange), (b) ensure the deletion cookie attributes match the original (httpOnly, secure, path, sameSite).","status":"open","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:27:35.454705492+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:27:35.454705492+06:00","dependencies":[{"issue_id":"hypercerts-sis","depends_on_id":"hypercerts-dhz.6","type":"discovered-from","created_at":"2026-02-24T16:27:42.248644021+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-t9hw","title":"Epic: hypercerts-next-optimization — Vercel React Best Practices remediation","description":"## Context\nComprehensive audit of the codebase against Vercel's 57-rule React/Next.js performance guide found **53 violations** across 7 categories. No violations found in client-side data fetching (React Query is well-used), localStorage, event listeners, or advanced patterns.\n\n## Audit Summary by Category\n| Category | Impact | Violations | Key Issues |\n|----------|--------|------------|------------|\n| Eliminating Waterfalls | CRITICAL | 14 | Sequential awaits in providers, pages, API routes; missing Suspense boundaries |\n| Bundle Size | CRITICAL | 12 | Barrel imports (queries/, lucide-react, @hypercerts-org/lexicon); missing optimizePackageImports; static imports of conditional components |\n| Server-Side Perf | HIGH | 14 | React.cache() misuse (object args), missing LRU cache, duplicate serialization, sequential fetches, blocking revalidatePath |\n| Re-render Optimization | MEDIUM | 13 | 10x functional setState violations, 3x useRouter defer-reads |\n| Rendering Performance | MEDIUM | 6 | Conditional render safety, hoistable JSX, manual loading state |\n| JavaScript Performance | LOW-MEDIUM | 11 | Array.find for dedup (4x), chained filter/map (3x), unhoist regex, Set lookups |\n\n## Success Criteria\n- All CRITICAL and HIGH violations resolved\n- MEDIUM violations resolved where practical\n- LOW-MEDIUM violations batched into sweep tasks\n- No regressions: build passes, existing tests pass\n\n## Scope\nAll source files in app/, lib/, components/, providers/, queries/. Does NOT include node_modules, vendor/, or UI primitives (components/ui/).","status":"open","priority":2,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T14:43:19.67105944+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:49:31.533982862+06:00","labels":["scope:medium"]}
{"id":"hypercerts-t9hw.1","title":"Add optimizePackageImports to next.config.ts for barrel-heavy libraries","description":"## Files\n- next.config.ts (modify)\n\n## What to do\nAdd `experimental.optimizePackageImports` array to the Next.js config. Include these libraries that are imported via barrel files across 45+ source files:\n- `lucide-react` (45 files import icons from barrel)\n- `@hypercerts-org/lexicon` (27 files import types/validators from barrel)\n- `@radix-ui/react-dialog`\n- `@radix-ui/react-dropdown-menu`\n- `@radix-ui/react-select`\n- `@radix-ui/react-popover`\n- `@radix-ui/react-accordion`\n- `@radix-ui/react-tabs`\n- `@radix-ui/react-radio-group`\n\nAdd the array inside the existing `experimental` block (or create it if absent). Do NOT remove any existing config keys.\n\n## Don't\n- Remove or modify any existing next.config.ts settings\n- Add libraries that aren't actually used in the project\n- Change import statements in source files (that's a separate task)","acceptance_criteria":"1. next.config.ts contains experimental.optimizePackageImports with at least lucide-react and @hypercerts-org/lexicon\n2. pnpm build succeeds without errors\n3. No existing config keys are removed or modified","status":"open","priority":1,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T14:43:32.158293228+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:43:32.158293228+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-t9hw.1","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:43:32.160071593+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.10","title":"Wrap resolveSessionPds with React.cache() and add LRU cache for PDS URL","description":"## Files\n- lib/server-utils.ts (modify)\n\n## What to do\n\n### 1. React.cache() wrapper (per-request dedup)\n`resolveSessionPds()` is called multiple times per request (e.g., SignedInProvider calls it, then the page component calls it again). Unlike `getSession()` and `getRepoContext()` which are wrapped in `React.cache()`, this function has no deduplication.\n\nHowever, `React.cache()` uses reference equality, and the `session` object may differ across calls. The cleanest fix is to create a no-arg cached version that internally calls `getSession()`:\n\n```ts\nimport { cache } from 'react';\n\n// Cached version: resolves PDS URL for the current session (no args needed)\nexport const getCachedPdsUrl = cache(async function getCachedPdsUrl(): Promise\u003cstring | undefined\u003e {\n  const session = await getSession();\n  if (!session) return undefined;\n  return resolveSessionPds(session);\n});\n```\n\nKeep the original `resolveSessionPds` for cases where a specific session object is passed.\n\n### 2. Module-level LRU cache (cross-request)\nThe PDS URL for a given DID virtually never changes within a session's lifetime. Add a simple module-level Map cache keyed by DID:\n\n```ts\nconst pdsUrlCache = new Map\u003cstring, { url: string; expiry: number }\u003e();\nconst PDS_CACHE_TTL = 5 * 60 * 1000; // 5 minutes\n\nexport async function resolveSessionPds(session: OAuthSession): Promise\u003cstring\u003e {\n  const did = session.did;\n  const cached = pdsUrlCache.get(did);\n  if (cached \u0026\u0026 Date.now() \u003c cached.expiry) return cached.url;\n  \n  const tokenInfo = await session.getTokenInfo();\n  const url = tokenInfo.aud;\n  pdsUrlCache.set(did, { url, expiry: Date.now() + PDS_CACHE_TTL });\n  return url;\n}\n```\n\nCap the Map size at 100 entries (evict oldest on overflow) to prevent memory leaks.\n\n## Don't\n- Remove the existing resolveSessionPds export (other code may depend on it)\n- Change the return type\n- Use external caching libraries — keep it simple with a Map","acceptance_criteria":"1. A cached no-arg version (getCachedPdsUrl or similar) exists and is wrapped in React.cache()\n2. resolveSessionPds uses a module-level Map cache with TTL for cross-request dedup\n3. Map is capped at a reasonable size (e.g., 100 entries)\n4. pnpm build succeeds","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T14:45:30.69237601+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:45:30.69237601+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.10","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:45:30.694544066+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.11","title":"Use after() for non-blocking revalidatePath in profile update routes","description":"## Files\n- app/api/profile/update/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n\n## What to do\nBoth profile update routes call `revalidatePath()` synchronously before returning the response. This blocks the response while Next.js invalidates the cache. Use the `after()` API from `next/server` to defer this to after the response is sent.\n\n### profile/update/route.ts (~line 205)\n```ts\n// Before\nrevalidatePath('/profile');\n// ... build response ...\nreturn NextResponse.json({ ok: true, profile: { ... } });\n\n// After\nimport { after } from 'next/server';\n// ... build response ...\nafter(() =\u003e {\n  revalidatePath('/profile');\n});\nreturn NextResponse.json({ ok: true, profile: { ... } });\n```\n\n### profile/bsky/update/route.ts (~line 143)\n```ts\n// Before\nrevalidatePath('/bsky-profile');\n\n// After\nimport { after } from 'next/server';\nafter(() =\u003e {\n  revalidatePath('/bsky-profile');\n});\n```\n\nNote: `after()` is available in Next.js 15+. Check the project's Next.js version first. If it's \u003c 15, skip this task and mark as cancelled with a note.\n\n## Don't\n- Move any response-building logic into the after() callback\n- Change the response shape\n- Remove the revalidatePath calls entirely","acceptance_criteria":"1. Both routes use after() from next/server for revalidatePath calls\n2. revalidatePath no longer blocks the response\n3. pnpm build succeeds\n4. Response shapes are unchanged","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T14:45:39.080193614+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:45:39.080193614+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-t9hw.11","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:45:39.081852062+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.12","title":"Replace closure-based setState toggles with functional updater form across forms","description":"## Files\n- components/hypercerts-base-form.tsx (modify) — line 476\n- components/evaluation-form.tsx (modify) — lines 334, 413, 479, 559\n- components/measurement-form.tsx (modify) — lines 475, 533, 596, 675, 971\n\n## What to do\nReplace all boolean toggle setState calls that reference the closure value with the functional updater form.\n\nFind and replace pattern:\n```ts\n// Before (10 occurrences)\nsetShowContributions(!showContributions)\nsetUseScore(!useScore)\nsetUseContent(!useContent)\nsetUseMeasurements(!useMeasurements)\nsetUseLocation(!useLocation)\nsetUseDates(!useDates)\nsetUseMethod(!useMethod)\nsetUseEvidence(!useEvidence)\nsetUseLocations(!useLocations)\nsetUseComment(!useComment)\n\n// After\nsetShowContributions(prev =\u003e !prev)\nsetUseScore(prev =\u003e !prev)\nsetUseContent(prev =\u003e !prev)\nsetUseMeasurements(prev =\u003e !prev)\nsetUseLocation(prev =\u003e !prev)\nsetUseDates(prev =\u003e !prev)\nsetUseMethod(prev =\u003e !prev)\nsetUseEvidence(prev =\u003e !prev)\nsetUseLocations(prev =\u003e !prev)\nsetUseComment(prev =\u003e !prev)\n```\n\n### Special case: measurement-form.tsx lines 674-678\nThis has a compound toggle where the conditional also reads the stale closure value:\n```ts\nonClick={() =\u003e {\n  setUseLocations(!useLocations);\n  if (!useLocations \u0026\u0026 locationEntries.length === 0) {\n    setLocationEntries([createEmptyLocationEntry()]);\n  }\n}}\n```\nFix: Use the functional form and move the conditional inside:\n```ts\nonClick={() =\u003e {\n  setUseLocations(prev =\u003e {\n    if (!prev \u0026\u0026 locationEntries.length === 0) {\n      setLocationEntries([createEmptyLocationEntry()]);\n    }\n    return !prev;\n  });\n}}\n```\n\n## Don't\n- Change any other logic in these components\n- Modify the UI or styling\n- Change the state variable names","acceptance_criteria":"1. All 10 toggle setState calls use the functional updater form (prev =\u003e !prev)\n2. The compound toggle in measurement-form.tsx correctly handles the conditional inside the updater\n3. No closure-captured state values are used in setState toggle calls\n4. pnpm build succeeds","status":"open","priority":3,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T14:45:50.635615952+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:45:50.635615952+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.12","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:45:50.636970162+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.13","title":"Use useRef for useRouter in components that only use router in callbacks","description":"## Files\n- components/hypercert-detail-view.tsx (modify) — line 52\n- components/hypercerts-edit-form.tsx (modify) — line 29\n- components/add-record-form.tsx (modify) — line 46\n\n## What to do\nThese three components call `useRouter()` but only use the router object inside callbacks (mutation onSuccess, goBack handlers), never in the render output. `useRouter()` subscribes the component to router state changes, causing unnecessary re-renders on navigation events.\n\nFix: Store the router in a ref to avoid re-render subscription:\n\n```ts\n// Before\nconst router = useRouter();\n// ... later in callback:\nrouter.push('/hypercerts');\n\n// After\nimport { useRef } from 'react';\nconst routerRef = useRef(useRouter());\n// ... later in callback:\nrouterRef.current.push('/hypercerts');\n```\n\n### hypercert-detail-view.tsx (line 52)\n`router` is only used in `useDeleteHypercertMutation({ onSuccess: () =\u003e router.push('/hypercerts') })`\n\n### hypercerts-edit-form.tsx (line 29)\n`router` is only used in `useUpdateHypercertMutation({ onSuccess: () =\u003e router.push(detailHref) })`\n\n### add-record-form.tsx (line 46)\n`router` is only used in `goBack = () =\u003e router.push(hypercertDetailHref)`\n\n## Don't\n- Change the navigation destinations\n- Add useCallback or useMemo wrappers (the ref approach is simpler)\n- Change components that DO use router in render output (e.g., navbar.tsx uses usePathname for active link highlighting — leave it alone)","acceptance_criteria":"1. All three components store useRouter() result in a ref\n2. Callbacks access router via ref.current\n3. Components no longer re-render on router state changes\n4. pnpm build succeeds","status":"open","priority":3,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T14:46:04.483209732+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:46:04.483209732+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-t9hw.13","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:46:04.484852837+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.14","title":"Add preload-on-hover for Create Hypercert navigation links","description":"## Files\n- components/navbar.tsx (modify) — line ~72-93\n- app/page.tsx (modify) — lines ~71, 82\n\n## What to do\nThe 'Create' nav link and homepage CTA buttons link to `/hypercerts/create`, which uses `next/dynamic` for heavy form components. Users see skeleton loaders while chunks download. Preloading the first step's component on hover/focus improves perceived speed.\n\n### navbar.tsx\nAdd onMouseEnter and onFocus handlers to the 'Create' Link:\n```tsx\n\u003cLink\n  href=\"/hypercerts/create\"\n  onMouseEnter={() =\u003e import(\"@/components/hypercerts-create-form\")}\n  onFocus={() =\u003e import(\"@/components/hypercerts-create-form\")}\n  className={...}\n\u003e\n  Create\n\u003c/Link\u003e\n```\n\n### app/page.tsx\nAdd the same handlers to the 'Create Hypercert' CTA button/link:\n```tsx\n\u003cLink\n  href=\"/hypercerts/create\"\n  onMouseEnter={() =\u003e import(\"@/components/hypercerts-create-form\")}\n  onFocus={() =\u003e import(\"@/components/hypercerts-create-form\")}\n  className=\"gap-2\"\n\u003e\n  \u003cSparkles className=\"h-5 w-5\" /\u003e\n  Create Hypercert\n\u003c/Link\u003e\n```\n\n## Don't\n- Add preloading to every link (only the create flow benefits)\n- Change the link destinations or styling\n- Import the component statically","acceptance_criteria":"1. Hovering over the Create link in navbar triggers a dynamic import of hypercerts-create-form\n2. Hovering over the Create Hypercert CTA on the homepage does the same\n3. The import is fire-and-forget (no await needed)\n4. pnpm build succeeds","status":"open","priority":3,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T14:46:13.400128537+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:46:13.400128537+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-t9hw.14","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:46:13.401521445+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.15","title":"Hoist regex and static function out of hypercerts-edit-form component body","description":"## Files\n- components/hypercerts-edit-form.tsx (modify) — lines 104-118\n\n## What to do\nThe `recordDateString` function is defined inside the component body and contains two regex patterns that are recreated on every render. Hoist both the regexes and the function to module scope.\n\n```ts\n// Before (inside component)\nconst recordDateString = (raw: string | undefined | null): string =\u003e {\n  if (!raw) return \"\";\n  if (/^\\d{4}-\\d{2}-\\d{2}$/.test(raw)) return raw;\n  const isoMatch = raw.match(/^(\\d{4}-\\d{2}-\\d{2})T/);\n  return isoMatch ? isoMatch[1] : \"\";\n};\n\n// After (module scope)\nconst DATE_ONLY_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\nconst ISO_DATE_PREFIX_RE = /^(\\d{4}-\\d{2}-\\d{2})T/;\n\nfunction recordDateString(raw: string | undefined | null): string {\n  if (!raw) return \"\";\n  if (DATE_ONLY_RE.test(raw)) return raw;\n  const isoMatch = raw.match(ISO_DATE_PREFIX_RE);\n  return isoMatch ? isoMatch[1] : \"\";\n}\n```\n\nAlso check for any other functions or constants defined inside the component body that don't depend on props/state and could be hoisted. The `getContentTypeColor` function in `hypercert-evidence-view.tsx` (lines 30-33) is another candidate — it always returns the same static string. Replace it with a module-level constant.\n\n## Additional file\n- components/hypercert-evidence-view.tsx (modify) — lines 30-33: Replace `getContentTypeColor()` function with a module-level constant string.\n\n## Don't\n- Change the logic of any function\n- Modify the component's props or state","acceptance_criteria":"1. recordDateString and its regex patterns are at module scope in hypercerts-edit-form.tsx\n2. getContentTypeColor in hypercert-evidence-view.tsx is replaced with a module-level constant\n3. No functions that depend on props/state are accidentally hoisted\n4. pnpm build succeeds","status":"open","priority":3,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T14:46:24.072694508+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:46:24.072694508+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-t9hw.15","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:46:24.074914232+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.2","title":"Replace barrel imports from queries/ with direct module imports","description":"## Files\n- components/hypercert-evaluations-section.tsx (modify) — line 12: `@/queries/hypercerts` → `@/queries/hypercerts/use-evaluations-query`\n- components/hypercert-measurements-section.tsx (modify) — line 12: `@/queries/hypercerts` → `@/queries/hypercerts/use-measurements-query`\n- components/hypercert-detail-view.tsx (modify) — line 25: `@/queries/hypercerts` → `@/queries/hypercerts/use-delete-hypercert-mutation`\n- components/hypercerts-edit-form.tsx (modify) — line 8: `@/queries/hypercerts` → `@/queries/hypercerts/use-update-hypercert-mutation`\n- components/hypercert-contributors-section.tsx (modify) — line 10: `@/queries/hypercerts` → `@/queries/hypercerts/use-contributor-profiles-query`\n- components/hypercert-evidence-section.tsx (modify) — line 10: `@/queries/hypercerts` → `@/queries/hypercerts/use-evidence-query`\n- components/hypercerts-create-form.tsx (modify) — line 4: `@/queries/hypercerts` → `@/queries/hypercerts/use-create-hypercert-mutation`\n- components/locations-form.tsx (modify) — line 13: `@/queries/hypercerts` → `@/queries/hypercerts/use-add-location-mutation`\n- components/evidence-form.tsx (modify) — line 20: `@/queries/hypercerts` → `@/queries/hypercerts/use-add-attachment-mutation`\n- components/navbar.tsx (modify) — line 19: `@/queries/auth` → `@/queries/auth/use-logout-mutation`\n- components/login-dialog.tsx (modify) — line 12: `@/queries/auth` → `@/queries/auth/use-login-mutation`\n- components/user-selection.tsx (modify) — line 16: `@/queries/external` → `@/queries/external/use-user-search-query`\n- components/profile-form.tsx (modify) — line 14: `@/queries/profile` → `@/queries/profile/use-update-profile-mutation`\n\n## What to do\nFor each file listed above, change the import from the barrel index file to import directly from the specific module file. Each import should point to the exact file that exports the hook being used.\n\nExample transformation:\n```ts\n// Before\nimport { useDeleteHypercertMutation } from \"@/queries/hypercerts\";\n// After\nimport { useDeleteHypercertMutation } from \"@/queries/hypercerts/use-delete-hypercert-mutation\";\n```\n\nIf a file imports multiple hooks from the same barrel, split into separate direct imports or import from the same direct file if both hooks live there.\n\n## Don't\n- Delete the barrel index files themselves (they may be used elsewhere or in tests)\n- Change any logic, only import paths\n- Add new dependencies","acceptance_criteria":"1. No source file in components/ imports from @/queries/hypercerts, @/queries/auth, @/queries/external, or @/queries/profile (barrel index files)\n2. All imports resolve correctly — pnpm build succeeds\n3. No functional changes — only import paths changed","status":"open","priority":1,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T14:43:45.112673356+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:43:45.112673356+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.2","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:43:45.114189381+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.3","title":"Parallelize sequential awaits in SignedInProvider and profile page","description":"## Files\n- providers/SignedInProvider.tsx (modify)\n- app/profile/page.tsx (modify)\n\n## What to do\n\n### SignedInProvider.tsx\nLines 15-38: The provider has sequential awaits that should be parallelized.\n\nCurrent pattern:\n```ts\nconst [session] = await Promise.all([getSession()]); // unnecessary Promise.all wrapper\nif (session) {\n  const agent = await getAgent();           // sequential\n  if (agent) {\n    const profileResult = await agent.com.atproto.repo.getRecord({...}); // sequential\n    const pdsUrl = await resolveSessionPds(session);  // independent of profile fetch\n  }\n}\n```\n\nFix:\n1. Remove the unnecessary `Promise.all` wrapper around the single `getSession()` call\n2. Inside the `if (session)` block, await `getAgent()` first (needed for profile fetch)\n3. Then parallelize the profile fetch and PDS resolution with `Promise.all`:\n```ts\nconst session = await getSession();\nif (session) {\n  const agent = await getAgent();\n  if (agent) {\n    const [profileResult, pdsUrl] = await Promise.all([\n      agent.com.atproto.repo.getRecord({...}).catch(() =\u003e null),\n      resolveSessionPds(session),\n    ]);\n  }\n}\n```\n\n### app/profile/page.tsx\nLines 21-35: Sequential awaits where getSession() is independent of the profile fetch.\n\nCurrent pattern:\n```ts\nconst repo = await getAgent();\nif (!repo) redirect('/');\nconst profileResult = await repo.com.atproto.repo.getRecord({...}); // blocks session fetch\nconst session = await getSession();                                  // independent\nconst pdsUrl = session ? await resolveSessionPds(session) : undefined;\n```\n\nFix: Start getSession() in parallel with the profile fetch:\n```ts\nconst repo = await getAgent();\nif (!repo) redirect('/');\nconst [profileResult, session] = await Promise.all([\n  repo.com.atproto.repo.getRecord({...}).catch(() =\u003e null),\n  getSession(),\n]);\nconst pdsUrl = session ? await resolveSessionPds(session) : undefined;\n```\n\n## Don't\n- Change the error handling behavior (keep .catch(() =\u003e null) for profile fetch)\n- Remove the redirect('/') guard\n- Change what data is passed to child components","acceptance_criteria":"1. SignedInProvider.tsx uses Promise.all to parallelize profile fetch and PDS resolution\n2. profile/page.tsx uses Promise.all to parallelize profile fetch and getSession()\n3. pnpm build succeeds\n4. The same data is still passed to Navbar and ProfileForm components","status":"open","priority":1,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T14:43:57.384783555+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:43:57.384783555+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.3","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:43:57.386190177+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-t9hw.3","depends_on_id":"hypercerts-t9hw.9","type":"blocks","created_at":"2026-03-04T14:46:40.056105618+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.4","title":"Parallelize sequential awaits in API routes (profile update, bsky update, add-attachment, add-location)","description":"## Files\n- app/api/profile/update/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n\n## What to do\n\n### profile/update/route.ts\n1. **Lines 93-104 and 141-151 (both create and update branches):** Avatar and banner blob uploads are sequential. Parallelize:\n```ts\nconst [avatarBlob, bannerBlob] = await Promise.all([\n  avatar ? repo.com.atproto.repo.uploadBlob(new Blob([avatar], { type: avatar.type })).then(r =\u003e r.data.blob) : undefined,\n  banner ? repo.com.atproto.repo.uploadBlob(new Blob([banner], { type: banner.type })).then(r =\u003e r.data.blob) : undefined,\n]);\n```\nApply this pattern in BOTH the create branch (~line 93) and the update branch (~line 141).\n\n2. **Lines 207-220 (response building):** getRecord and getSession are sequential but independent. Parallelize:\n```ts\nconst [updatedResult, session] = await Promise.all([\n  repo.com.atproto.repo.getRecord({...}).catch(() =\u003e null),\n  getSession(),\n]);\n```\n\n### profile/bsky/update/route.ts\n**Lines 50-60 and 91-101:** Same avatar/banner blob upload pattern. Apply the same Promise.all fix in both branches.\n\n### certs/add-attachment/route.ts\n**Lines 137-175:** Three sequential operations (resolveStrongRef for subject, content resolution, location resolution) are independent. Parallelize:\n```ts\nconst [subjectRef, contentField, locationRef] = await Promise.all([\n  resolveStrongRef(ctx.agent, hypercertUri, 'org.hypercerts.claim.activity'),\n  resolveContent(ctx.agent, content),  // extract content resolution into helper\n  location ? resolveLocation(ctx.agent, ctx.activeDid, location) : undefined,\n]);\n```\nIf extracting helpers is too complex, at minimum parallelize the subject ref and location resolution.\n\n### certs/add-location/route.ts\n**Lines 85-115:** The location record creation and existing hypercert fetch are sequential but independent. Parallelize:\n```ts\nconst [locationRef, existingResult] = await Promise.all([\n  createLocationRecord(personalRepository, personalRepository.assertDid, locationPayload),\n  personalRepository.com.atproto.repo.getRecord({\n    repo: hypercertParsed.did,\n    collection: hypercertParsed.collection || 'org.hypercerts.claim.activity',\n    rkey: hypercertParsed.rkey,\n  }),\n]);\n```\n\n## Don't\n- Change error handling semantics\n- Modify request parsing or validation logic\n- Change response shapes","acceptance_criteria":"1. All four route files use Promise.all for independent async operations\n2. No sequential awaits remain for operations that don't depend on each other\n3. pnpm build succeeds\n4. Response shapes are unchanged","status":"open","priority":1,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":45,"created_at":"2026-03-04T14:44:16.653093416+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:44:16.653093416+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.4","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:44:16.654654881+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.5","title":"Parallelize sequential awaits in create-actions.ts (addMeasurement, addContribution)","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\n\n### addMeasurement (around lines 312-334)\n`resolveStrongRef` and `processLocations` are called sequentially but are independent (both only need ctx.agent and ctx.activeDid which are already available). Parallelize:\n```ts\nconst [subject, locationRefs] = await Promise.all([\n  resolveStrongRef(ctx.agent, params.subject, 'org.hypercerts.claim.activity'),\n  params.locations?.length\n    ? processLocations(ctx.agent, ctx.activeDid, params.locations)\n    : undefined,\n]);\n```\n\n### addContribution (around lines 101-164)\nAfter getting repo context and validating ownership, the existing hypercert record fetch (line ~126) and the contributionDetails record creation (line ~160) are independent. The details record doesn't depend on the existing hypercert content. Parallelize where possible.\n\nNote: Be careful here — review the actual data flow. If the details record creation truly doesn't need data from the existing hypercert, parallelize. If it does, leave sequential.\n\n## Don't\n- Change the ownership validation logic (hypercertParsed.did !== ctx.activeDid check)\n- Change error handling behavior\n- Modify the record schemas or field values","acceptance_criteria":"1. addMeasurement uses Promise.all for resolveStrongRef and processLocations\n2. addContribution parallelizes independent operations where safe\n3. pnpm build succeeds\n4. Ownership checks remain intact","status":"open","priority":1,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T14:44:25.12621191+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:44:25.12621191+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.5","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:44:25.12828663+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.6","title":"Start resolveSessionPds early in hypercert detail and edit pages (defer-await pattern)","description":"## Files\n- app/hypercerts/[hypercertUri]/page.tsx (modify)\n- app/hypercerts/[hypercertUri]/edit/page.tsx (modify)\n\n## What to do\n\n### [hypercertUri]/page.tsx\nLines 106-186: After resolving viewCtx and session via Promise.all, the code fetches the cert record (line ~141) and only later resolves the PDS URL (line ~186). The PDS resolution only depends on `session`, not on the cert record.\n\nFix: Start the PDS resolution promise immediately after session is available, await it later:\n```ts\nconst [viewCtx, session] = await Promise.all([...]);\nif (!viewCtx) return ...;\n\n// Start PDS resolution early — don't await yet\nconst pdsPromise = session ? resolveSessionPds(session) : undefined;\n\nconst certResult = await viewCtx.agent.com.atproto.repo.getRecord({...});\n// ... validity checks ...\n\nif (image \u0026\u0026 session) {\n  const pdsUrl = await pdsPromise;  // await here where needed\n}\n```\n\n### [hypercertUri]/edit/page.tsx\nLines 54-114: Identical pattern. PDS resolution (line ~114) happens after cert record fetch (line ~69-77) but only depends on session.\n\nApply the same defer-await pattern: start the promise early, await late.\n\n## Don't\n- Change the redirect logic or early return guards\n- Change what data is passed to child components\n- Remove any null/error checks","acceptance_criteria":"1. Both page.tsx and edit/page.tsx start resolveSessionPds promise before the cert record fetch\n2. The PDS URL promise is awaited only where the result is actually used\n3. pnpm build succeeds\n4. No changes to redirect logic or component props","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T14:44:35.724322745+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:44:35.724322745+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.6","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:44:35.726378804+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-t9hw.6","depends_on_id":"hypercerts-t9hw.10","type":"blocks","created_at":"2026-03-04T14:46:39.384999497+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.7","title":"Add Suspense boundaries to home page for streaming","description":"## Files\n- app/page.tsx (modify)\n\n## What to do\nThe home page is an async server component that fetches session, agent, and profile data before rendering ANY JSX. The hero section, features grid, and footer CTA are entirely static but are blocked from rendering until auth/profile fetches complete. Only the 'Session Info' section at the bottom needs the fetched data.\n\n1. Make the default export a non-async component that renders the static parts immediately\n2. Extract the session-dependent section into a separate async server component (e.g., `SessionInfo`)\n3. Wrap `SessionInfo` in `\u003cSuspense\u003e` with an appropriate fallback (a skeleton or spinner)\n\n```tsx\nimport { Suspense } from 'react';\n\nexport default function Home() {\n  return (\n    \u003cdiv className=\"min-h-screen ...\"\u003e\n      {/* Hero section — renders immediately */}\n      \u003csection\u003e...\u003c/section\u003e\n      \n      {/* Features grid — renders immediately */}\n      \u003csection\u003e...\u003c/section\u003e\n      \n      {/* Session info — streams in */}\n      \u003cSuspense fallback={\u003cSessionInfoSkeleton /\u003e}\u003e\n        \u003cSessionInfo /\u003e\n      \u003c/Suspense\u003e\n      \n      {/* Footer CTA — renders immediately */}\n      \u003csection\u003e...\u003c/section\u003e\n    \u003c/div\u003e\n  );\n}\n\nasync function SessionInfo() {\n  const [personalRepo, session] = await Promise.all([getAgent(), getSession()]);\n  // ... render session info section\n}\n\nfunction SessionInfoSkeleton() {\n  // Simple skeleton matching the session info section layout\n}\n```\n\nThe `SessionInfoSkeleton` can be a simple div with the same dimensions as the session info section, using Skeleton components from components/ui/skeleton.\n\n## Don't\n- Change the visual layout or styling of any section\n- Remove any existing functionality\n- Add new dependencies","acceptance_criteria":"1. The home page renders the hero and features sections without waiting for auth data\n2. The session-dependent section is wrapped in Suspense with a fallback\n3. pnpm build succeeds\n4. The page looks identical once fully loaded","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-04T14:44:50.646706557+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:44:50.646706557+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.7","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:44:50.648596291+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.8","title":"Dynamic import DeleteConfirmDialog and HypercertContributorsSection in detail view","description":"## Files\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\n\n### 1. Dynamic import DeleteConfirmDialog (bundle-conditional)\nLine 26: `DeleteConfirmDialog` is statically imported but only rendered when `isOwner` is true. Most page views are by non-owners, so the AlertDialog bundle (radix-ui dialog system) is loaded unnecessarily.\n\nChange from:\n```ts\nimport { DeleteConfirmDialog } from \"@/components/delete-confirm-dialog\";\n```\nTo:\n```ts\nimport dynamic from \"next/dynamic\";\nconst DeleteConfirmDialog = dynamic(\n  () =\u003e import(\"@/components/delete-confirm-dialog\").then(m =\u003e ({ default: m.DeleteConfirmDialog })),\n);\n```\n\n### 2. Dynamic import HypercertContributorsSection (consistency)\nLine 13: `HypercertContributorsSection` is statically imported while the other three sections (Measurements, Evidence, Evaluations) are already dynamically imported with skeleton fallbacks. Make it consistent.\n\nChange from:\n```ts\nimport HypercertContributorsSection from \"./hypercert-contributors-section\";\n```\nTo:\n```ts\nimport { ContributorsSectionSkeleton } from \"./detail-view-skeletons\";\nconst HypercertContributorsSection = dynamic(\n  () =\u003e import(\"./hypercert-contributors-section\"),\n  { loading: () =\u003e \u003cContributorsSectionSkeleton /\u003e }\n);\n```\n\nNote: Check if `ContributorsSectionSkeleton` exists in detail-view-skeletons.tsx. If not, create a simple skeleton matching the contributors section layout (a card with 2-3 skeleton rows).\n\n## Don't\n- Change the rendering logic or props passed to these components\n- Remove the static import of other components that ARE needed on initial render\n- Change the order of sections in the detail view","acceptance_criteria":"1. DeleteConfirmDialog uses next/dynamic and is not in the initial bundle for non-owner views\n2. HypercertContributorsSection uses next/dynamic with a skeleton fallback, consistent with other sections\n3. ContributorsSectionSkeleton exists (created if needed)\n4. pnpm build succeeds\n5. Detail view renders correctly for both owner and non-owner views","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":25,"created_at":"2026-03-04T14:45:02.208020724+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:45:02.208020724+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.8","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:45:02.209513188+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-t9hw.9","title":"Fix React.cache() object argument in getRepoContext — cache key never matches","description":"## Files\n- lib/repo-context.ts (modify)\n\n## What to do\n`getRepoContext` is wrapped with `React.cache()` but accepts an options object `{ targetDid?: string }` as its argument. React.cache() uses reference equality for arguments, so `getRepoContext({ targetDid: 'did:plc:abc' })` called twice with the same DID creates two different object references and the cache NEVER hits.\n\nThis is called in both `generateMetadata` and the default page export for `app/hypercerts/[hypercertUri]/page.tsx`, causing the OAuth session restore to execute twice per request.\n\nFix: Change the function signature to accept `targetDid` as a primitive string parameter (or undefined) instead of an options object:\n\n```ts\n// Before\nexport const getRepoContext = cache(async function getRepoContext(\n  options?: { targetDid?: string }\n): Promise\u003cRepoContext | null\u003e {\n  const targetDid = options?.targetDid;\n  // ...\n});\n\n// After\nexport const getRepoContext = cache(async function getRepoContext(\n  targetDid?: string\n): Promise\u003cRepoContext | null\u003e {\n  // ...\n});\n```\n\nThen update ALL callers to pass the string directly:\n- app/hypercerts/[hypercertUri]/page.tsx: `getRepoContext({ targetDid: ownerDid })` → `getRepoContext(ownerDid)`\n- app/hypercerts/[hypercertUri]/edit/page.tsx: same change\n- Any other callers: `getRepoContext()` (no args) stays the same\n- `getRepoContext({})` → `getRepoContext()`\n\nSearch the entire codebase for `getRepoContext` calls to find all callers.\n\n## Don't\n- Change the internal logic of getRepoContext\n- Remove the React.cache() wrapper\n- Change the return type","acceptance_criteria":"1. getRepoContext accepts targetDid as an optional string parameter, not an object\n2. All callers updated to pass string directly\n3. React.cache() now correctly deduplicates calls with the same targetDid within a request\n4. pnpm build succeeds","status":"open","priority":1,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-04T14:45:13.300563111+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T14:45:13.300563111+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-t9hw.9","depends_on_id":"hypercerts-t9hw","type":"parent-child","created_at":"2026-03-04T14:45:13.302281321+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tj0","title":"Epic: Fix build failure — missing HypercertRecord import","description":"The Next.js production build (npm run build) fails with a TypeScript error: 'Cannot find name HypercertRecord' in components/hypercerts-base-form.tsx:38. This blocks all deployments. Success = npm run build passes cleanly.","status":"open","priority":1,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-02-24T16:58:53.258655043+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T16:58:53.258655043+06:00","labels":["scope:trivial"]}
{"id":"hypercerts-tj0.1","title":"Fix: add missing HypercertRecord import in hypercerts-base-form.tsx","description":"## Files\n- components/hypercerts-base-form.tsx (modify)\n\n## What to do\nLine 38 uses `HypercertRecord` as a type but it is not imported. The type is a namespace re-export defined in `lib/types.ts` line 1:\n\n```ts\nimport type { OrgHypercertsClaimActivity as HypercertRecord } from \"@hypercerts-org/sdk-core\";\n```\n\nAdd this import to the top of `components/hypercerts-base-form.tsx`:\n\n```ts\nimport type { OrgHypercertsClaimActivity as HypercertRecord } from \"@hypercerts-org/sdk-core\";\n```\n\nThis is the same pattern already used on line 11 for the `Hypercert` import (non-type import of the same module). You may combine them or keep them separate — either is fine.\n\nThe `certInfo` prop on line 38 is typed `HypercertRecord` (the namespace). It is used on lines 64-65 to call `Hypercert.isWorkScopeString(certInfo.workScope)` and access `.workScope.scope`, and on lines 67-80 to read `.title`, `.shortDescription`, `.startDate`, `.endDate`. This is consistent with `OrgHypercertsClaimActivity.Record` fields, so the type is correct — it just needs the import.\n\n## Do not\n- Change the type of `certInfo` — the namespace type is correct\n- Rename `HypercertRecord` to anything else\n- Modify any logic or other files","acceptance_criteria":"npm run build completes successfully (exit code 0). The only change is the added import — no other lines modified.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-24T16:59:06.096722167+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-24T17:02:53.842975287+06:00","closed_at":"2026-02-24T17:02:53.842975287+06:00","close_reason":"aa400e7 fix: add missing HypercertRecord import and use .Record type","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-tj0.1","depends_on_id":"hypercerts-tj0","type":"parent-child","created_at":"2026-02-24T16:59:06.098822853+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tlw","title":"Epic: Resolve CodeRabbit review for PR #7","description":"Resolve all 10 open CodeRabbit inline review comments on PR #7 (quick-fixes branch). 10 comments across 7 files covering: unsafe error casts, bare catch blocks swallowing errors, unsanitized OAuth input, missing JWK validation, duplicate evaluator DIDs, and dead variables in SignedInProvider.","status":"open","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-18T01:56:57.299624626+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T02:06:24.505889646+06:00","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-tlw.1","title":"Sanitize OAuth error_description in auth callback","description":"## Files\n- app/api/auth/callback/route.ts (modify)\n\n## What to do\nCodeRabbit flagged: security issue at line 18.\n\n**Original comment:**\n\u003e The error_description on line 12 is passed through from the authorization server verbatim. While React will safely render it as text (no XSS), the content is controlled by the external OAuth provider and could be unexpectedly verbose or confusing to end users. Consider capping or sanitizing it, or using a fixed user-friendly message like the catch block does.\n\n**Suggested fix:**\nOn line 12, after getting the error_description from searchParams, sanitize it before using it in the redirect:\n1. Trim whitespace\n2. Enforce a max length of 200 characters — if it exceeds, truncate and append '...'\n3. If empty after trimming, use the fallback 'Authorization was cancelled' (already the current fallback)\n4. Keep the full raw provider message in the console.warn on line 13 for server-side debugging\n5. Use the sanitized version in the redirect URL searchParams on line 16\n\n**Context:**\nLine 12 currently does: `searchParams.get('error_description') || 'Authorization was cancelled'`. The value goes into a URL redirect param on line 16 that the client reads to show a toast. The console.warn on line 13 should keep the original unsanitized value.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065641\n\n## Don't\n- Don't change the catch block on lines 39-48 (that's a separate error path)\n- Don't change HTTP status codes or redirect behavior\n- Don't introduce new dependencies","acceptance_criteria":"1. error_description values longer than 200 chars are truncated to 200 chars with '...' appended. 2. The console.warn still logs the full original error_description. 3. Empty/whitespace-only error_description falls back to 'Authorization was cancelled'. 4. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-18T01:57:18.843683889+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T02:02:27.251591276+06:00","closed_at":"2026-02-18T02:02:27.251591276+06:00","close_reason":"acb98e9 Sanitize OAuth error_description in auth callback","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-tlw.1","depends_on_id":"hypercerts-tlw","type":"parent-child","created_at":"2026-02-18T01:57:18.845872938+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tlw.2","title":"Narrow bare catch blocks in profile update routes","description":"## Files\n- app/api/profile/bsky/update/route.ts (modify)\n- app/api/profile/update/route.ts (modify)\n\n## What to do\nCodeRabbit flagged 2 related error-handling issues:\n\n1. [error-handling] Line 38-41 in bsky/update/route.ts: Bare `catch {}` around `repo.profile.getBskyProfile()` silently swallows ALL errors (network, auth, server faults), not just 'profile not found'. This could cause duplicate profile creation on transient failures.\n\n2. [error-handling] Line 46-49 in profile/update/route.ts: Same bare `catch {}` around `repo.profile.getCertifiedProfile()` — masks non-'not found' errors.\n\n**Suggested fixes:**\n\nFor both files, change the bare catch to inspect the error and only set existingProfile = null for 'not found' scenarios. Re-throw unexpected errors.\n\nIn bsky/update/route.ts (lines 36-41):\n```typescript\ntry {\n  existingProfile = await repo.profile.getBskyProfile();\n} catch (err: unknown) {\n  const isNotFound = err instanceof Error \u0026\u0026 /not found/i.test(err.message);\n  if (isNotFound) {\n    existingProfile = null;\n  } else {\n    throw err;\n  }\n}\n```\n\nIn profile/update/route.ts (lines 44-49): Same pattern but with `getCertifiedProfile()`.\n\n**Context:**\nBoth routes use the existingProfile check to decide whether to call create vs update. If a network error is swallowed, the code falls through to create, potentially causing duplicate profiles or data loss.\n\nCodeRabbit comments:\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065671\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065692\n\n## Don't\n- Don't change the outer try/catch blocks (lines 99-105 in bsky, lines 117-123 in profile)\n- Don't change the create/update logic itself\n- Don't introduce new dependencies\n- Don't change HTTP status codes","acceptance_criteria":"1. In bsky/update/route.ts, the catch block around getBskyProfile() catches the error as a named variable, checks for 'not found' pattern, sets existingProfile=null only for not-found, and re-throws other errors. 2. In profile/update/route.ts, the catch block around getCertifiedProfile() has the same selective handling. 3. Neither file has a bare 'catch {}' or 'catch {' without error inspection. 4. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-18T01:57:31.715195846+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T02:01:22.138816838+06:00","closed_at":"2026-02-18T02:01:22.138816838+06:00","close_reason":"a1d0b40 Narrow bare catch blocks in profile update routes","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-tlw.2","depends_on_id":"hypercerts-tlw","type":"parent-child","created_at":"2026-02-18T01:57:31.717004269+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tlw.3","title":"Deduplicate evaluator DIDs in evaluation form","description":"## Files\n- components/evaluation-form.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug at line 177.\n\n**Original comment:**\n\u003e No deduplication between search-selected and manually-entered evaluator DIDs. If the same DID is added via the search tab and also typed in the manual DIDs tab, it will appear twice in allEvaluatorDids. The addEvaluator function (line 113) deduplicates within evaluators, but there's no cross-check against manualDids.\n\n**Suggested fix:**\nReplace lines 173-177 with a Set-based deduplication:\n```typescript\nconst didSet = new Set\u003cstring\u003e();\nfor (const e of evaluators) didSet.add(e.did);\nfor (const did of manualDids) {\n  const trimmed = did.trim();\n  if (trimmed !== '') didSet.add(trimmed);\n}\nconst allEvaluatorDids = Array.from(didSet);\n```\n\n**Context:**\nThe handleSubmit function (line 166) builds allEvaluatorDids by concatenating evaluators (search-selected) and manualDids (typed). The addEvaluator function on line 113 deduplicates within evaluators, but there's no cross-check when combining with manualDids.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818491522\n\n## Don't\n- Don't change the addEvaluator function\n- Don't change the evaluationPayload structure\n- Don't introduce new dependencies","acceptance_criteria":"1. allEvaluatorDids is built using a Set (or equivalent dedup logic) so duplicate DIDs between evaluators and manualDids are removed. 2. manualDids entries are trimmed before comparison. 3. Empty strings from manualDids are still excluded. 4. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-18T01:57:40.448695166+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T02:00:48.263055424+06:00","closed_at":"2026-02-18T02:00:48.263055424+06:00","close_reason":"bcb125e deduplicate evaluator DIDs using Set","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-tlw.3","depends_on_id":"hypercerts-tlw","type":"parent-child","created_at":"2026-02-18T01:57:40.450283414+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tlw.4","title":"Populate activeProfileName and activeProfileHandle in SignedInProvider","description":"## Files\n- providers/SignedInProvider.tsx (modify)\n\n## What to do\nCodeRabbit flagged: bug at line 20.\n\n**Original comment:**\n\u003e Change activeProfileName and activeProfileHandle from const to let and populate them from the profile data inside the if (repo) block. These variables are always undefined because they're declared as const and never assigned. Unlike avatarUrl and handle (which are let and populated from the profile), these remain unused, yet are passed to \u003cNavbar\u003e.\n\n**Suggested fix:**\n1. Change line 19 from `const activeProfileName` to `let activeProfileName`\n2. Change line 20 from `const activeProfileHandle` to `let activeProfileHandle`\n3. Inside the `if (repo)` block (after line 30), add:\n   ```typescript\n   activeProfileName = profile?.displayName;\n   activeProfileHandle = profile?.handle;\n   ```\n\n**Context:**\nLines 19-20 declare these as `const` initialized to `undefined`. They are passed to `\u003cNavbar\u003e` on lines 45-46 but are always undefined. The `avatarUrl` and `handle` variables on lines 17-18 follow the correct pattern (declared as `let`, populated inside the `if (repo)` block).\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2818491529\n\n## Don't\n- Don't change the Navbar component props interface\n- Don't change the getCertifiedProfile call or its catch behavior\n- Don't introduce new dependencies","acceptance_criteria":"1. activeProfileName is declared with let and assigned profile?.displayName inside the if (repo) block. 2. activeProfileHandle is declared with let and assigned profile?.handle inside the if (repo) block. 3. Both variables are still passed to Navbar. 4. npm run build succeeds.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-18T01:57:49.314889076+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T02:01:18.445903139+06:00","closed_at":"2026-02-18T02:01:18.445903139+06:00","close_reason":"a1d0b40 fix: populate activeProfileName and activeProfileHandle in SignedInProvider","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-tlw.4","depends_on_id":"hypercerts-tlw","type":"parent-child","created_at":"2026-02-18T01:57:49.316799841+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tlw.5","title":"Fix unsafe error casts across 4 API routes","description":"## Files\n- app/api/certs/add-attachment/route.ts (modify)\n- app/api/certs/add-location/route.ts (modify)\n- app/api/profile/bsky/update/route.ts (modify)\n- app/api/profile/update/route.ts (modify)\n\n## What to do\nCodeRabbit flagged 4 related error-handling issues — all are unsafe `(e as Error).message` casts that produce `undefined` when a non-Error value is thrown.\n\n1. [error-handling] Line 141 in add-attachment/route.ts: `(e as Error).message` in catch block\n2. [error-handling] Line 97 in add-location/route.ts: `(e as Error).message` in catch block — also leaks internal details via `details` field\n3. [error-handling] Line 102 in bsky/update/route.ts: `(error as Error).message` in catch block\n4. [error-handling] Line 120 in profile/update/route.ts: `(error as Error).message` in catch block\n\n**Suggested fixes:**\n\nFor all 4 files, replace the unsafe cast with a safe pattern:\n```typescript\ne instanceof Error ? e.message : String(e)\n```\n\nSpecific changes:\n\n1. add-attachment/route.ts line 141:\n   ```diff\n   -{ error: \\`Failed to add attachment: ${(e as Error).message}\\` }\n   +{ error: \\`Failed to add attachment: ${e instanceof Error ? e.message : String(e)}\\` }\n   ```\n\n2. add-location/route.ts line 97:\n   ```diff\n   -{ error: 'Internal server error', details: (e as Error).message }\n   +{ error: \\`Internal server error: ${e instanceof Error ? e.message : String(e)}\\` }\n   ```\n   Note: also remove the separate `details` field to avoid leaking internals — fold it into the error string.\n\n3. bsky/update/route.ts line 102:\n   ```diff\n   -{ error: \\`Profile update failed: ${(error as Error).message}\\` }\n   +{ error: \\`Profile update failed: ${error instanceof Error ? error.message : String(error)}\\` }\n   ```\n\n4. profile/update/route.ts line 120:\n   ```diff\n   -{ error: \\`Profile update failed: ${(error as Error).message}\\` }\n   +{ error: \\`Profile update failed: ${error instanceof Error ? error.message : String(error)}\\` }\n   ```\n\nCodeRabbit comments:\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065643\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065659\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065682\n- https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065694\n\n## Don't\n- Don't change HTTP status codes\n- Don't change the console.error logging lines\n- Don't change the response field name 'error' (keep it as 'error')\n- Don't introduce new dependencies\n- Don't change unrelated code in these files","acceptance_criteria":"1. None of the 4 files contain '(e as Error).message' or '(error as Error).message'. 2. All 4 catch blocks use 'instanceof Error ? .message : String()' pattern. 3. add-location/route.ts no longer has a separate 'details' field in the error response. 4. npm run build succeeds.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-18T01:58:02.237328481+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T02:05:22.228665197+06:00","closed_at":"2026-02-18T02:05:22.228665197+06:00","close_reason":"81cca2f fix: replace unsafe error casts with instanceof Error pattern","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-tlw.5","depends_on_id":"hypercerts-tlw","type":"parent-child","created_at":"2026-02-18T01:58:02.23924113+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-tlw.5","depends_on_id":"hypercerts-tlw.2","type":"blocks","created_at":"2026-02-18T01:58:19.501468546+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tlw.6","title":"Add JWK structure validation in jwks.json route","description":"## Files\n- app/jwks.json/route.ts (modify)\n\n## What to do\nCodeRabbit flagged: error-handling issue at line 54.\n\n**Original comment:**\n\u003e Unvalidated privateKey.keys will throw an unhelpful 500 if the JWK structure is unexpected. If ATPROTO_JWK_PRIVATE is valid JSON but doesn't contain a keys array (e.g., it's a single JWK object instead of a JWK Set), (privateKey.keys as Array\u003c...\u003e).map(...) will throw a TypeError that isn't caught.\n\n**Suggested fix:**\nAfter the JSON.parse on line 25 and before the PRIVATE_KEY_FIELDS line on line 37, add a guard:\n```typescript\nif (\n  !privateKey.keys ||\n  !Array.isArray(privateKey.keys) ||\n  privateKey.keys.length === 0\n) {\n  return NextResponse.json(\n    { error: \"ATPROTO_JWK_PRIVATE must contain a JWKS with a non-empty 'keys' array.\" },\n    { status: 500 },\n  );\n}\n```\n\n**Context:**\nThe route serves the public JWKS endpoint. Line 38 does `(privateKey.keys as Array\u003c...\u003e).map(...)` which will throw a raw TypeError if keys is undefined or not an array. The existing validation checks for missing env var (line 16) and invalid JSON (line 26) but not for invalid JWK structure.\n\nCodeRabbit comment: https://github.com/hypercerts-org/hypercerts-scaffold-atproto/pull/7#discussion_r2817065698\n\n## Don't\n- Don't change the existing env var check or JSON parse logic\n- Don't change the key transformation logic (PRIVATE_KEY_FIELDS filtering)\n- Don't change the response headers or cache control\n- Don't introduce new dependencies","acceptance_criteria":"1. A guard exists before the .map() call that checks privateKey.keys is a non-empty array. 2. If privateKey.keys is missing, not an array, or empty, a 500 JSON response with a descriptive error message is returned. 3. The existing env var and JSON parse checks are unchanged. 4. npm run build succeeds.","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-18T01:58:13.068644936+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-18T02:02:27.016877878+06:00","closed_at":"2026-02-18T02:02:27.016877878+06:00","close_reason":"acb98e9 Add JWK structure validation guard in jwks.json route","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-tlw.6","depends_on_id":"hypercerts-tlw","type":"parent-child","created_at":"2026-02-18T01:58:13.07034743+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-tq2","title":"Remove personal email addresses from .beads/issues.jsonl — replace with GitHub handles","description":"## Files\n- .beads/issues.jsonl (modify)\n\n## Context\nThe issues.jsonl file contains personal email addresses in the 'owner' fields that are committed to the public git repo, exposing PII:\n- `kzoepa@gmail.com` appears as owner on ~26 issues (lines 1, 6-19, 20, 32-40, 46)\n- `adam@hypercerts.org` appears as owner on 5 issues (lines 41-45)\n\nThe JSONL is auto-exported from the beads SQLite DB. However, the JSONL also serves as an import source (config says `no-auto-import: false`). So editing the JSONL and re-importing should work.\n\n## What to do\n\n### 1. Replace email addresses in issues.jsonl\nUse sed or similar to do a global find-and-replace in `.beads/issues.jsonl`:\n- Replace `\"owner\":\"kzoepa@gmail.com\"` with `\"owner\":\"kzoeps\"`\n- Replace `\"owner\":\"adam@hypercerts.org\"` with `\"owner\":\"aspiers\"`\n\n### 2. Also check and replace in created_by fields\nCheck if `created_by` fields also contain emails. From the data I see `\"created_by\":\"kzoeps\"` and `\"created_by\":\"Adam Spiers\"` — these are names/handles, not emails, so they should be fine. But verify.\n\n### 3. Check interactions.jsonl too\nSearch `.beads/interactions.jsonl` for any email addresses and replace similarly.\n\n### 4. Verify the JSONL is still valid\nAfter editing, run `hb list` to verify the beads DB can still read the data. If `hb sync` overwrites your changes, you may need to:\n- Delete the SQLite DB (`rm .beads/beads.db`)\n- Run `hb init` to re-initialize from the JSONL\n- Run `hb sync` to verify\n\n### 5. Do a repo-wide search\nRun `rg -i 'kzoepa@gmail|adam@hypercerts' --type-not binary` to check for any other occurrences outside .beads/.\n\n### 6. Note about git history\nThe emails are already in git history. A `git filter-repo` rewrite would be needed to fully remove them from history, but that's destructive and out of scope for this task. Just fix the current files. Add a comment in the commit message noting that history rewriting may be needed if this is a concern.\n\n## Don't\n- Run git filter-repo or any history-rewriting commands\n- Modify any code files\n- Change the beads config\n- Delete or recreate issues — only change the owner field values","acceptance_criteria":"1. No email addresses (containing @) remain in .beads/issues.jsonl owner fields\n2. No email addresses remain in .beads/interactions.jsonl (if any existed)\n3. kzoepa@gmail.com is replaced with kzoeps everywhere in .beads/\n4. adam@hypercerts.org is replaced with aspiers everywhere in .beads/\n5. hb list still works after the changes (JSONL is valid)\n6. A repo-wide search for the email addresses returns zero results in tracked files\n7. Changes are committed and pushed","status":"closed","priority":1,"issue_type":"chore","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T23:24:05.096515221+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T23:29:34.764918626+06:00","closed_at":"2026-02-17T23:29:34.764918626+06:00","close_reason":"c311694 Remove personal email addresses from .beads/issues.jsonl","labels":["scope:trivial"]}
{"id":"hypercerts-tvr","title":"Fix: raw error leak in certs/create route (from hypercerts-1hr.2)","description":"Review of hypercerts-1hr.2 found the same raw-error-leak pattern in app/api/certs/create/route.ts that was fixed in the logout route.\n\nLine 58: `return NextResponse.json({ error: e.message }, { status: 500 });` — leaks internal error messages (e.g. SDK internals, network errors) to the client when `e instanceof Error`.\n\nLine 61: `{ error: `Failed to create hypercert: ${(e as Error).message}` }` — the fallback branch also uses the unsafe cast; if `e` is not an Error instance, `.message` is `undefined`, producing a confusing response.\n\nFix: replace both branches with a single generic message, keep console.error for server-side logging. Pattern to follow: app/api/auth/logout/route.ts (fixed by hypercerts-1hr.2).\n\nEvidence: app/api/certs/create/route.ts lines 55-63.","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-20T22:16:28.664171728+06:00","created_by":"kzoeps","updated_at":"2026-02-20T22:24:04.710678218+06:00","closed_at":"2026-02-20T22:24:04.710678218+06:00","close_reason":"9173a17 fix: replace raw error leak with generic message in certs/create","dependencies":[{"issue_id":"hypercerts-tvr","depends_on_id":"hypercerts-1hr.2","type":"discovered-from","created_at":"2026-02-20T22:16:32.219622798+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-txj","title":"Fix: ePDS email login section always visible even when NEXT_PUBLIC_EPDS_URL is unset (from hypercerts-dhz.8/dhz.10)","description":"Review of hypercerts-dhz.8 and dhz.10 found: The email login section (the 'or' divider + email input + 'Sign in with email' button) is always rendered in LoginDialog regardless of whether NEXT_PUBLIC_EPDS_URL is set. If a deployer does not set NEXT_PUBLIC_EPDS_URL, clicking 'Sign in with email' navigates to /api/oauth/login which throws an unhandled Error('NEXT_PUBLIC_EPDS_URL is not set') from getEpdsEndpoints() — this results in a 500 error page with no user-friendly message. The email section should be conditionally rendered: only show it when process.env.NEXT_PUBLIC_EPDS_URL is truthy. Since this is a 'use client' component and NEXT_PUBLIC_ vars are inlined at build time, the check is: {process.env.NEXT_PUBLIC_EPDS_URL \u0026\u0026 (\u003c\u003e...email section...\u003c/\u003e)}. Evidence: login-dialog.tsx has no NEXT_PUBLIC_EPDS_URL guard; lib/epds-config.ts line 19-24 throws if epdsUrl is falsy.","status":"closed","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:27:04.318836579+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:13:05.694608184+06:00","closed_at":"2026-02-24T17:13:05.694608184+06:00","close_reason":"Already fixed in dhz.16 (commit dcac8b3). login-dialog.tsx line 155: hasEpds = !!process.env.NEXT_PUBLIC_EPDS_URL. Email tab and PillToggle only render when hasEpds is true (lines 168-176).","dependencies":[{"issue_id":"hypercerts-txj","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:27:08.500451294+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-ua9","title":"Fix: redundant `width: 32px` on `div.w-8` container and dead `object-position` on Ma Earth logo in atproto-branding.ts (from hypercerts-nyk.15)","description":"Review of hypercerts-nyk.15 found two redundant CSS rules in lib/atproto-branding.ts.\n\n**Issue 1: Redundant width override (lines 139-143)**\n```css\n.flex.items-center.justify-start.gap-2 \u003e div.w-8:has(img[alt=\"Ma Earth\"]),\ndiv.w-8:has(img[alt=\"Ma Earth\"]) {\n  width: 32px !important;  /* w-8 in Tailwind = 8 × 4px = 32px — same value! */\n  flex-shrink: 0 !important;\n}\n```\nThe selector targets `div.w-8` (Tailwind class meaning width: 2rem = 32px) and then overrides it with `width: 32px` — the exact same value. The `!important` makes no difference here. Only `flex-shrink: 0` is doing real work.\n\n**Issue 2: Ineffective object-position (line 135)**\n```css\nimg[alt=\"Ma Earth\"] {\n  width: 32px !important;\n  height: 32px !important;\n  object-fit: contain !important;\n  object-position: left center !important;  /* no effect */\n}\n```\nThe element is a 32×32px square with `object-fit: contain`. For a square container with a square (or near-square) logo, `object-position` only matters if the image's intrinsic aspect ratio differs from 1:1. Even then, the container is already constrained to 32×32 by the parent `div.w-8`. The `object-position` is effectively a no-op.\n\nEvidence: lib/atproto-branding.ts lines 131-143.\n\nFix:\n1. Remove `width: 32px !important` from the container rule (keep `flex-shrink: 0`)\n2. Remove `object-position: left center !important` from the img rule (or document why it's needed if the logo is non-square)","status":"open","priority":4,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-02-17T19:46:59.09081134+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T19:46:59.09081134+06:00","dependencies":[{"issue_id":"hypercerts-ua9","depends_on_id":"hypercerts-nyk.15","type":"discovered-from","created_at":"2026-02-17T19:47:03.489472674+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-uzx","title":"Fix: inconsistent indentation in buildClientMetadata() production block (from hypercerts-nyk.7)","description":"Review of hypercerts-nyk.7 found: In lib/config.ts, the production return block of buildClientMetadata() has inconsistent indentation. The properties logo_uri, grant_types, response_types, token_endpoint_auth_method, and application_type use 6-space indentation instead of 4-space like the surrounding properties (client_id, client_name, client_uri, redirect_uris, scope, dpop_bound_access_tokens, branding). This was introduced in commit 3e4b912 when the logo_uri line was changed and the surrounding lines were accidentally re-indented. Fix: normalize all properties in the production return block to 4-space indentation. File: lib/config.ts, function buildClientMetadata(), production return block (~lines 235-250).","status":"open","priority":3,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoeps","created_at":"2026-02-17T17:01:13.410081576+06:00","created_by":"kzoeps","updated_at":"2026-02-17T17:01:13.410081576+06:00","dependencies":[{"issue_id":"hypercerts-uzx","depends_on_id":"hypercerts-nyk.7","type":"discovered-from","created_at":"2026-02-17T17:01:16.58517044+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-v67","title":"Epic: Add Prettier, Husky, and lint-staged for pre-commit checks","description":"The project has ESLint configured (flat config, eslint-config-next) but no Prettier installed (the existing .prettierrc.json has a typo and prettier isn't even a dependency), no husky, and no lint-staged. Goal: install prettier with a proper config, install husky + lint-staged, and wire them together so that on every commit: (1) staged .ts/.tsx/.mjs files are auto-formatted with prettier --write, (2) staged .ts/.tsx files are linted with eslint --fix. The existing .prettierrc.json needs to be fixed (single → singleQuote). Success: git commit triggers pre-commit hook that formats and lints staged files. Scope: prettier config, .prettierignore, husky setup, lint-staged config, package.json scripts. Out of scope: changing ESLint rules, adding new ESLint plugins, reformatting the entire codebase in this epic.","status":"closed","priority":2,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-02-17T18:27:43.973345937+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:54:21.275368642+06:00","closed_at":"2026-02-17T18:54:21.275368642+06:00","close_reason":"ec7549c Prettier, Husky, lint-staged installed and codebase formatted","labels":["scope:small"]}
{"id":"hypercerts-v67.1","title":"Install and configure Prettier, Husky, and lint-staged","description":"## Files\n- package.json (modify — add devDependencies, scripts, lint-staged config)\n- .prettierrc.json (modify — fix typo, add full config)\n- .prettierignore (create)\n- .husky/pre-commit (create via husky init)\n- eslint.config.mjs (modify — add eslint-config-prettier to disable formatting rules that conflict with prettier)\n\n## What to do\n\n### 1. Install dependencies\n\n```bash\nnpm install --save-dev prettier husky lint-staged eslint-config-prettier\n```\n\n### 2. Fix and expand .prettierrc.json\n\nReplace the current broken config with:\n```json\n{\n  \"semi\": true,\n  \"singleQuote\": false,\n  \"tabWidth\": 2,\n  \"trailingComma\": \"all\",\n  \"printWidth\": 80,\n  \"plugins\": [],\n  \"endOfLine\": \"lf\"\n}\n```\n\nNote: `singleQuote: false` preserves the existing codebase convention (double quotes are used everywhere). The old config had `\"single\": false` which is not a valid Prettier key.\n\n### 3. Create .prettierignore\n\n```\nnode_modules/\n.next/\nout/\nbuild/\ncoverage/\n*.tgz\npnpm-lock.yaml\npackage-lock.json\n.beads/\nvendor/\n```\n\n### 4. Add eslint-config-prettier to ESLint config\n\nIn `eslint.config.mjs`, import and add `eslint-config-prettier` to disable ESLint rules that conflict with Prettier. The config should look like:\n\n```js\nimport { defineConfig, globalIgnores } from \"eslint/config\";\nimport nextVitals from \"eslint-config-next/core-web-vitals\";\nimport nextTs from \"eslint-config-next/typescript\";\nimport prettierConfig from \"eslint-config-prettier\";\n\nconst eslintConfig = defineConfig([\n  ...nextVitals,\n  ...nextTs,\n  prettierConfig,\n  globalIgnores([\n    \".next/**\",\n    \"out/**\",\n    \"build/**\",\n    \"next-env.d.ts\",\n  ]),\n]);\n\nexport default eslintConfig;\n```\n\n### 5. Initialize Husky\n\n```bash\nnpx husky init\n```\n\nThis creates `.husky/pre-commit`. Replace its contents with:\n\n```bash\nnpx lint-staged\n```\n\n### 6. Add lint-staged config to package.json\n\nAdd this to package.json at the top level:\n\n```json\n\"lint-staged\": {\n  \"*.{ts,tsx,mjs}\": [\n    \"prettier --write\"\n  ],\n  \"*.{ts,tsx}\": [\n    \"eslint --fix\"\n  ]\n}\n```\n\n### 7. Add scripts to package.json\n\nAdd these scripts (keep existing ones):\n\n```json\n\"format\": \"prettier --write .\",\n\"format:check\": \"prettier --check .\",\n\"prepare\": \"husky\"\n```\n\nThe `prepare` script ensures husky hooks are installed after `npm install`.\n\n### 8. Verify\n\n- Run `npx prettier --check .` — it will likely show files that need formatting (that is OK, we are not reformatting in this task)\n- Run `npm run lint` — should pass\n- Run `npx tsc --noEmit` — should pass\n- Test the hook: stage a file, commit, verify lint-staged runs\n\n## Don't\n- Do NOT reformat the entire codebase (that is a separate task)\n- Do NOT change any ESLint rules (only add eslint-config-prettier to disable conflicting formatting rules)\n- Do NOT add prettier plugins (like prettier-plugin-tailwindcss) — keep it simple\n- Do NOT modify any source files (.ts, .tsx) in this task","acceptance_criteria":"1. prettier is in devDependencies and npx prettier --version works\n2. husky is in devDependencies and .husky/pre-commit exists with \"npx lint-staged\"\n3. lint-staged is in devDependencies and configured in package.json\n4. eslint-config-prettier is in devDependencies and added to eslint.config.mjs\n5. .prettierrc.json has valid config (no \"single\" typo — uses \"singleQuote\")\n6. .prettierignore exists and ignores node_modules, .next, build artifacts\n7. package.json has \"format\", \"format:check\", and \"prepare\" scripts\n8. npm run lint passes (no ESLint errors)\n9. npx tsc --noEmit passes\n10. The pre-commit hook fires on git commit (test with a dummy commit)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-02-17T18:28:10.155234891+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:51:18.798142903+06:00","closed_at":"2026-02-17T18:51:18.798142903+06:00","close_reason":"d1d8dc4 feat: install and configure Prettier, Husky, and lint-staged","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-v67.1","depends_on_id":"hypercerts-v67","type":"parent-child","created_at":"2026-02-17T18:28:10.157058697+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-v67.2","title":"Format entire codebase with Prettier","description":"## Files\n- All .ts, .tsx, .mjs, .json, .css files in the project (modify)\n\n## What to do\n\nRun `npm run format` (which runs `prettier --write .`) to format the entire codebase with the Prettier config established in hypercerts-v67.1.\n\nThis should be a single commit with ONLY formatting changes — no logic changes.\n\n### Steps\n\n1. Run `npm run format`\n2. Review the diff to confirm it is ONLY whitespace/formatting changes (quotes, semicolons, trailing commas, line width wrapping, etc.)\n3. Run `npm run lint` to verify ESLint still passes after formatting\n4. Run `npx tsc --noEmit` to verify TypeScript still compiles\n5. Commit with message \"style: format codebase with prettier\"\n\n## Don't\n- Do NOT change any logic, imports, or behavior\n- Do NOT modify .prettierrc.json or any config files\n- Do NOT fix ESLint warnings/errors — only formatting","acceptance_criteria":"1. npm run format:check passes (all files are formatted)\n2. npm run lint passes\n3. npx tsc --noEmit passes\n4. The commit contains ONLY formatting changes (no logic changes)\n5. The app still builds: npm run build passes (or next build)","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-02-17T18:28:24.894020822+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:53:37.62642619+06:00","closed_at":"2026-02-17T18:53:37.62642619+06:00","close_reason":"ec7549c style: format codebase with prettier","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-v67.2","depends_on_id":"hypercerts-v67","type":"parent-child","created_at":"2026-02-17T18:28:24.895496835+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-v67.2","depends_on_id":"hypercerts-v67.1","type":"blocks","created_at":"2026-02-17T18:28:24.897686609+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-vcu","title":"Fix: user-did cookie missing SameSite attribute in standard OAuth callback (from security review)","description":"## File\napp/api/oauth/callback/route.ts, lines 34-40\n\n## Problem\nThe `user-did` cookie set in the standard ATProto OAuth callback is missing the `sameSite` attribute:\n```ts\ncookieStore.set('user-did', session.did, {\n  httpOnly: true,\n  secure: config.isProduction,\n  maxAge: 60 * 60 * 24 * 7, // 1 week\n  path: '/',\n  // sameSite is MISSING\n});\n```\n\nWithout `sameSite`, browsers default to `Lax` in modern browsers, but this is not explicit and may differ across browser versions. More importantly, the ePDS callback (app/api/oauth/epds/callback/route.ts, line 145-150) explicitly sets `sameSite: 'lax'` — the two code paths are inconsistent.\n\nThe missing attribute also means the cookie could be sent on cross-site top-level navigations in ways that differ from the intended `lax` behavior, depending on browser implementation.\n\n## Fix\nAdd `sameSite: 'lax'` to match the ePDS callback cookie:\n```ts\ncookieStore.set('user-did', session.did, {\n  httpOnly: true,\n  secure: config.isProduction,  // also see hypercerts-lv8 for the secure flag issue\n  sameSite: 'lax',\n  maxAge: 60 * 60 * 24 * 7,\n  path: '/',\n});\n```","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:43:42.526252181+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:43:42.526252181+06:00","dependencies":[{"issue_id":"hypercerts-vcu","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.564130844+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-vmn","title":"Fix: authEndpoint subdomain construction is injection-vulnerable if epdsUrl has a path (from hypercerts-dhz.3)","description":"Review of hypercerts-dhz found: In lib/epds-config.ts line 31, the auth endpoint is constructed as:\n\n```typescript\nauthEndpoint: `https://auth.${hostname}/oauth/authorize`\n```\n\nwhere hostname = new URL(epdsUrl).hostname. This is correct when epdsUrl is a bare origin like 'https://pds-eu-west4.test.certified.app'. However:\n\n1. If NEXT_PUBLIC_EPDS_URL is set with a trailing path (e.g. 'https://pds.example.com/v1'), hostname is still 'pds.example.com' and the auth endpoint becomes 'https://auth.pds.example.com/oauth/authorize' — which may be wrong if the PDS is path-mounted.\n\n2. More critically: if NEXT_PUBLIC_EPDS_URL contains a port (e.g. 'https://pds.example.com:8080'), hostname is 'pds.example.com' (no port), so the auth endpoint drops the port. The auth service at auth.pds.example.com:8080 would be unreachable.\n\n3. The epdsUrl is not validated to be a valid HTTPS URL at config load time (unlike baseUrl which has validateBaseUrl()). A misconfigured value like 'not-a-url' would cause new URL(epdsUrl) to throw an unhandled error at request time.\n\nFix: (a) validate NEXT_PUBLIC_EPDS_URL format in config.ts when it is set, (b) document that it must be a bare origin with no path, (c) consider using URL.origin instead of hostname to preserve port.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:27:16.822516267+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:47.314644037+06:00","closed_at":"2026-02-24T17:12:47.314644037+06:00","close_reason":"Already fixed in dhz.14 (commit 98de1d5). Port is now preserved in auth endpoint construction using URL.host instead of hostname.","dependencies":[{"issue_id":"hypercerts-vmn","depends_on_id":"hypercerts-dhz.3","type":"discovered-from","created_at":"2026-02-24T16:27:20.483292089+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-vrra","title":"Fix: certs POST always creates rights record even when no rights provided (from hypercerts-czy.4)","description":"Review of hypercerts-czy.4 found: app/api/certs/route.ts (lines 92-114) always creates an org.hypercerts.claim.rights record unconditionally, even when the 'rights' field is not provided in the form data. This creates an orphaned, empty rights record (containing only createdAt) in the user's repo for every hypercert creation. If the subsequent claim createRecord fails, the rights record is left stranded with no cleanup. Fix: (1) Only create the rights record when hypercertParams.rights is defined. (2) Make the rights field on the claim record optional. (3) Consider using an ATProto applyWrites transaction to make the two creates atomic.","status":"open","priority":2,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:17:29.514571101+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:17:29.514571101+06:00","dependencies":[{"issue_id":"hypercerts-vrra","depends_on_id":"hypercerts-czy.4","type":"discovered-from","created_at":"2026-03-04T11:17:29.579230853+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-vtf","title":"Fix: PillToggle missing ARIA tablist/tab roles (login-dialog.tsx)","description":"## File\\ncomponents/login-dialog.tsx lines 26-51\\n\\n## Severity\\nHIGH — Accessibility\\n\\n## Problem\\nThe PillToggle component renders two plain \u003cbutton\u003e elements as a tab switcher but has no ARIA roles. Screen readers cannot identify this as a tab interface. The active tab has no aria-selected attribute, and there is no aria-controls linking each tab to its panel.\\n\\n## Evidence\\n```tsx\\n// Current (lines 28-50)\\n\u003cdiv className=\"bg-muted flex w-full rounded-full p-1\"\u003e\\n  \u003cbutton type=\"button\" onClick={() =\u003e onChange(\"handle\")} ...\u003e\\n    Handle\\n  \u003c/button\u003e\\n  \u003cbutton type=\"button\" onClick={() =\u003e onChange(\"email\")} ...\u003e\\n    Email\\n  \u003c/button\u003e\\n\u003c/div\u003e\\n```\\n\\n## Fix\\nAdd role=\"tablist\" to the container div, role=\"tab\" + aria-selected={active === 'handle'} + aria-controls=\"handle-panel\" to each button, and id=\"handle-panel\" / id=\"email-panel\" to the corresponding form containers.\\n\\n```tsx\\n\u003cdiv role=\"tablist\" aria-label=\"Login method\" className=\"bg-muted flex w-full rounded-full p-1\"\u003e\\n  \u003cbutton\\n    role=\"tab\"\\n    aria-selected={active === 'handle'}\\n    aria-controls=\"login-handle-panel\"\\n    id=\"login-handle-tab\"\\n    ...\\n  \u003e\\n    Handle\\n  \u003c/button\u003e\\n  \u003cbutton\\n    role=\"tab\"\\n    aria-selected={active === 'email'}\\n    aria-controls=\"login-email-panel\"\\n    id=\"login-email-tab\"\\n    ...\\n  \u003e\\n    Email\\n  \u003c/button\u003e\\n\u003c/div\u003e\\n// And on the form containers:\\n\u003cdiv id=\"login-handle-panel\" role=\"tabpanel\" aria-labelledby=\"login-handle-tab\"\u003e\\n  \u003cHandleForm /\u003e\\n\u003c/div\u003e\\n```","status":"open","priority":1,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:08.143354673+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:08.143354673+06:00"}
{"id":"hypercerts-wlq","title":"Fix: lint-staged runs prettier before eslint on .ts/.tsx files — order may cause double-write","description":"Review of package.json lint-staged config (lines 15-22) found a potential ordering issue.\n\nEvidence:\npackage.json lines 15-22:\n  'lint-staged': {\n    '*.{ts,tsx,mjs}': ['prettier --write'],\n    '*.{ts,tsx}': ['eslint --fix']\n  }\n\nIssues:\n1. For .ts and .tsx files, BOTH glob patterns match. lint-staged runs them in order: prettier first, then eslint --fix. If eslint --fix reformats code differently from prettier, the file will be written twice. This is generally fine but can cause unexpected diffs.\n\n2. More importantly: eslint --fix runs AFTER prettier --write. If eslint --fix makes changes that violate prettier rules (e.g., adds trailing commas in a way prettier would reformat), the staged file will have eslint's output but NOT prettier's final pass. The committed code may not match 'pnpm format:check'.\n\nRecommended fix:\nRun prettier AFTER eslint, or combine into a single command:\n  '*.{ts,tsx}': ['eslint --fix', 'prettier --write'],\n  '*.mjs': ['prettier --write']\n\nThis ensures the final staged content always passes 'prettier --check'.\n\nAlternatively, use the pattern recommended by prettier docs: run prettier last so it always wins on formatting.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:48:25.78904766+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:48:25.78904766+06:00"}
{"id":"hypercerts-x2b","title":"Fix: upstream error bodies leaked to client in ePDS OAuth routes (from hypercerts-dhz)","description":"## Review finding from hypercerts-dhz (ePDS OAuth integration)\n\n**Files:**\n- app/api/oauth/login/route.ts, lines 56-61\n- app/api/oauth/callback/route.ts, lines 91-96\n\n**Problem:**\nBoth routes return raw upstream error response bodies in the `details` field of the JSON error response:\n```ts\n// login/route.ts\nconst errorBody = await parResponse.text().catch(() =\u003e '(unreadable)');\nreturn NextResponse.json(\n  { error: `PAR request failed: ${parResponse.statusText}`, details: errorBody },\n  { status: 502 },\n);\n\n// callback/route.ts\nconst errorBody = await tokenResponse.text().catch(() =\u003e tokenResponse.statusText);\nreturn NextResponse.json(\n  { error: 'Token exchange failed', details: errorBody },\n  { status: 502 },\n);\n```\n\nThe upstream error body from the ePDS server is sent directly to the browser. This can expose internal server details, stack traces, or configuration information from the ePDS server to end users.\n\n**Fix:**\nLog the error body server-side (already done via `console.error`) but return only a generic error to the client:\n```ts\nreturn NextResponse.json(\n  { error: 'PAR request failed' },\n  { status: 502 },\n);\n```\n\nThe `details` field should be removed from the client-facing response. The full error is already logged via `console.error`.","status":"open","priority":3,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:28:52.281410502+06:00","created_by":"kzoeps","updated_at":"2026-02-24T16:28:52.281410502+06:00","dependencies":[{"issue_id":"hypercerts-x2b","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:28:56.822931886+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-x2y","title":"Fix: logout route leaks internal error message to client (from security review)","description":"## File\napp/api/oauth/logout/route.ts, line 19\n\n## Problem\nThe logout error handler returns the raw exception message to the client:\n```ts\nreturn NextResponse.json(\n  { error: `Logout failed: ${e instanceof Error ? e.message : String(e)}` },\n  { status: 500 },\n);\n```\n\nThis can expose internal implementation details, Redis connection strings, SDK internals, or stack traces to the browser. The error is already logged server-side via `console.error` on line 17.\n\nNote: hypercerts-x2b covers the same pattern in the ePDS routes (login/callback). This issue covers the standard logout route which was not included in x2b's scope.\n\n## Fix\nReturn a generic error message to the client:\n```ts\nreturn NextResponse.json(\n  { error: 'Logout failed. Please try again.' },\n  { status: 500 },\n);\n```\n\nThe full error is already logged via console.error and will appear in server logs.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:43:33.337821177+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:43:33.337821177+06:00","dependencies":[{"issue_id":"hypercerts-x2y","depends_on_id":"hypercerts-a48","type":"discovered-from","created_at":"2026-02-27T11:44:49.508209357+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-x63","title":"Fix: console.log left in production code (contributions-form.tsx line 105)","description":"## File\\ncomponents/contributions-form.tsx line 105\\n\\n## Severity\\nLOW — Code quality\\n\\n## Problem\\nA bare console.log(contributionData) is left in the handleSubmit success path. This leaks potentially sensitive contribution data (contributor DIDs, hypercert URIs) to the browser console in production.\\n\\n## Evidence\\n```tsx\\ntry {\\n  const contributionData = await handleContributionCreation();\\n  console.log(contributionData); // \u003c-- debug log, should be removed\\n  toast.success('Contribution created!');\\n  onNext?.();\\n}\\n```\\n\\n## Fix\\nRemove line 105: console.log(contributionData);","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:25.544090461+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:25.544090461+06:00"}
{"id":"hypercerts-x8l","title":"Fix: workScope sent by client but silently ignored by /api/certs/create route","description":"## Problem\nThere is a mismatch between what the client sends and what the server processes for `workScope`:\n\n**Client side** (`lib/api/hypercerts.ts` lines 27, 52):\n```ts\nformData.append('workScope', JSON.stringify(params.workScope));\n```\n\n**Server side** (`app/api/certs/create/route.ts` lines 15-16):\n```ts\n// TODO map to proper workscope\n// const workScopeRaw = formData.get('workScope') as string | null;\n```\n\nThe server completely ignores the `workScope` field. The client always sends it (even serializing it to JSON), but the server never reads or passes it to `CreateHypercertParams`. This means hypercerts are created without their work scope, which is a core field of the hypercert data model.\n\n## Fix\nEither:\n1. Uncomment and implement the workScope parsing on the server (parse JSON, validate, pass to `hypercertParams`)\n2. Remove the `workScope` append from the client if it is intentionally not supported yet\n\nThe TODO comment suggests this is known but the client-side code was not updated to match, creating a silent data loss bug.\n\n## Evidence\n- `lib/api/hypercerts.ts` lines 27, 52: client sends workScope\n- `app/api/certs/create/route.ts` lines 15-16: server ignores workScope with a TODO comment","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:45:47.768091365+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:45:47.768091365+06:00"}
{"id":"hypercerts-xcb1","title":"Fix: rights field names mismatch between CreateHypercertParams and API route (from hypercerts-73o)","description":"Review of hypercerts-73o found: CreateHypercertParams.rights uses field names {name, type, description} (lib/types.ts lines 61-65) but the API route (app/api/certs/route.ts lines 6-10, 97-104) reads {rightsName, rightsType, rightsDescription}. The form serializes rights as JSON and POSTs it; the route parses it into HypercertRights which expects the 'rights*' prefixed names. Result: rights records are always created with empty fields at runtime — silent data loss. The lexicon schema (OrgHypercertsClaimRights.Main) uses rightsName/rightsType/rightsDescription. Fix: rename the rights sub-fields in CreateHypercertParams to match the lexicon and API route, OR update the API route to read name/type/description. Evidence: lib/types.ts:61-65 vs app/api/certs/route.ts:6-10,97-104 vs node_modules/@hypercerts-org/lexicon/dist/types/org/hypercerts/claim/rights.d.ts:10-14.","status":"open","priority":1,"issue_type":"bug","owner":"karma.gainforest.id","created_at":"2026-03-04T11:18:25.972551059+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T11:18:25.972551059+06:00","dependencies":[{"issue_id":"hypercerts-xcb1","depends_on_id":"hypercerts-73o","type":"discovered-from","created_at":"2026-03-04T11:18:26.029496992+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-xezr","title":"Fix: Constellation measurement backlink uses singular subject.uri instead of subjects array (from hypercerts-phiw.11)","description":"Review of hypercerts-phiw.11 found: The Constellation backlink source string for measurements was updated from 'org.hypercerts.claim.measurement:subject.uri' to 'org.hypercerts.context.measurement:subject.uri' (collection NSID corrected), but the field path was NOT updated. In v0.10.0, the measurement record changed from singular 'subject' to plural 'subjects' (array). The correct source string should be 'org.hypercerts.context.measurement:subjects[com.atproto.repo.strongRef].uri'.\n\nEvidence: lib/api/external/constellation.ts line 62:\n  'org.hypercerts.context.measurement:subject.uri'  ← WRONG field path\n\nThe measurement lexicon (org/hypercerts/context/measurement.json) defines:\n  subjects: { type: 'array', items: { type: 'ref', ref: 'com.atproto.repo.strongRef' } }\n\nThe attachment backlink correctly uses the array syntax:\n  'org.hypercerts.context.attachment:subjects[com.atproto.repo.strongRef].uri'\n\nFix: Change line 62 to:\n  'org.hypercerts.context.measurement:subjects[com.atproto.repo.strongRef].uri'\n\nImpact: getMeasurementLinks() returns zero results for all hypercerts, so the measurements section never loads.","status":"closed","priority":1,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T16:48:48.043969551+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T16:54:15.285873411+06:00","closed_at":"2026-03-04T16:54:15.285873411+06:00","close_reason":"2dc273b Fix measurement backlink to use subjects array","dependencies":[{"issue_id":"hypercerts-xezr","depends_on_id":"hypercerts-phiw.11","type":"discovered-from","created_at":"2026-03-04T16:48:51.389784603+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-xje","title":"Spec inconsistency: task dqf.2 description contradicts its own code example (from hypercerts-dqf.2)","description":"Review of hypercerts-dqf.2 found: The task description says 'Use start-early-await-late pattern to avoid parsing body for unauthenticated requests' but the code example it provides does the opposite — it awaits req.formData() BEFORE awaiting the repo promise, meaning the body is always parsed regardless of auth status.\n\nThe implementation (d26d1ef) correctly follows the code example, but the stated goal is not achieved. If the intent was truly to skip body parsing for unauthenticated requests, the pattern should be:\n  const repoPromise = getAuthenticatedRepo();\n  const repo = await repoPromise;\n  if (!repo) return 401;\n  const formData = await req.formData();\n\nHowever, this changes behavior (early auth check before body parse). The spec's code example and description are contradictory. The Lead should clarify the intent.","status":"closed","priority":4,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"kzoepa@gmail.com","created_at":"2026-02-17T23:51:11.902296504+06:00","created_by":"kzoeps","updated_at":"2026-02-17T23:52:12.166678214+06:00","closed_at":"2026-02-17T23:52:12.166682062+06:00","dependencies":[{"issue_id":"hypercerts-xje","depends_on_id":"hypercerts-dqf.2","type":"discovered-from","created_at":"2026-02-17T23:51:14.992095042+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-xml","title":"Fix: Buttons without type='button' inside forms may accidentally submit (contributions-form.tsx, evaluation-form.tsx)","description":"## Files\\n- components/contributions-form.tsx lines 230, 241\\n- components/evaluation-form.tsx lines 251, 278, 289, 447, 460, 513, 528\\n\\n## Severity\\nMEDIUM — Correctness / UX\\n\\n## Problem\\nSeveral \u003cButton\u003e components inside \u003cform\u003e elements are missing type=\"button\". In HTML, a button inside a form defaults to type=\"submit\" if no type is specified. This means clicking 'Remove contributor', 'Add DID', 'Remove evaluator', 'Add Content URI', 'Remove Measurement URI', etc. can accidentally trigger form submission.\\n\\n## Evidence\\n```tsx\\n// contributions-form.tsx line 230 — no type prop\\n\u003cButton\\n  variant=\"ghost\"\\n  size=\"icon\"\\n  onClick={() =\u003e removeManualContributor(index)}\\n  disabled={manualContributors.length === 1 || saving}\\n  className=\"text-muted-foreground hover:text-destructive\"\\n\u003e\\n  \u003cTrash className=\"h-4 w-4\" /\u003e\\n\u003c/Button\u003e\\n\\n// evaluation-form.tsx line 278 — no type prop\\n\u003cButton\\n  variant=\"ghost\"\\n  size=\"icon\"\\n  onClick={() =\u003e removeManualDid(index)}\\n  disabled={manualDids.length === 1 || mutation.isPending}\\n  className=\"text-muted-foreground hover:text-destructive\"\\n\u003e\\n  \u003cTrash className=\"h-4 w-4\" /\u003e\\n\u003c/Button\u003e\\n```\\n\\n## Fix\\nAdd type=\"button\" to all Button components inside forms that are not intended to submit:\\n```tsx\\n\u003cButton type=\"button\" variant=\"ghost\" size=\"icon\" onClick={...}\u003e\\n  \u003cTrash className=\"h-4 w-4\" /\u003e\\n\u003c/Button\u003e\\n```\\n\\nAffected buttons: all Trash/remove/add buttons in contributions-form.tsx and evaluation-form.tsx that lack type=\"button\".","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:57.219446723+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:57.219446723+06:00"}
{"id":"hypercerts-xp2","title":"Fix: token response sub field not validated before use as DID (from hypercerts-dhz.6)","description":"Review of hypercerts-dhz found: In app/api/oauth/callback/route.ts, tokenData.sub is used directly as a DID (lines 122, 134, 140) without any validation that it is actually a DID string (did:plc:... or did:web:...).\n\nIf the ePDS token endpoint returns a malformed sub (empty string, null, or a non-DID value due to a server bug or MITM), the code will:\n1. Write a session to Redis keyed by the invalid sub\n2. Set the user-did cookie to the invalid value\n3. All subsequent getSession() calls will fail silently or return undefined\n\nThe fix is to validate sub before use:\n```typescript\nif (!tokenData.sub || (!tokenData.sub.startsWith('did:plc:') \u0026\u0026 !tokenData.sub.startsWith('did:web:'))) {\n  return NextResponse.json({ error: 'Invalid token response: missing or malformed sub' }, { status: 502 });\n}\n```\n\nThis also prevents the user-did cookie from being set to an attacker-controlled value if the token endpoint is somehow compromised.","status":"closed","priority":2,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:26:54.632314446+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:45.768877266+06:00","closed_at":"2026-02-24T17:12:45.768877266+06:00","close_reason":"Already fixed in dhz.13 (commit 6da38d4). DID validation at callback/route.ts lines 99-103 checks for did:plc: and did:web: prefixes before use.","dependencies":[{"issue_id":"hypercerts-xp2","depends_on_id":"hypercerts-dhz.6","type":"discovered-from","created_at":"2026-02-24T16:27:01.659798235+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-xqh","title":"Fix: next.config.ts remotePatterns are hardcoded test/staging hostnames — not suitable for production","description":"## Problem\n`next.config.ts` lines 14-41 hardcode specific hostnames in `images.remotePatterns`:\n\n```ts\n{ hostname: 'pds-eu-west4.test.certified.app' },  // test environment\n{ hostname: 'epds1.test.certified.app' },          // test environment\n{ hostname: 'climateai.org' },                     // specific production PDS\n{ hostname: 'bsky.social' },                       // Bluesky PDS\n{ hostname: 'cdn.bsky.app' },                      // Bluesky CDN\n```\n\nIssues:\n1. Test/staging hostnames (`*.test.certified.app`) should not be in production config\n2. The list is not exhaustive — any PDS not on this list will have its images blocked by Next.js Image Optimization\n3. `bsky.social` is a PDS hostname but blob URLs use `bsky.network` subdomains (e.g. `shimeji.us-east.host.bsky.network`), so Bluesky user avatars from non-CDN paths will be blocked\n\n## Fix\n1. Use a wildcard pattern for `*.bsky.network` to cover all Bluesky PDS shards\n2. Use environment-based configuration to separate test vs production hostnames\n3. Consider using `remotePatterns` with a wildcard for `*.certified.app` if that's the PDS domain pattern\n\n```ts\n{ protocol: 'https', hostname: '*.bsky.network', pathname: '/**' },\n{ protocol: 'https', hostname: '*.certified.app', pathname: '/**' },\n```\n\n## Evidence\n`next.config.ts` lines 14-41","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:46:57.101867959+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:46:57.101867959+06:00"}
{"id":"hypercerts-ycl","title":"Fix: unhandled exceptions in login/callback routes crash with 500 (from hypercerts-dhz)","description":"Review of hypercerts-dhz found: Both app/api/oauth/login/route.ts and app/api/oauth/callback/route.ts have no top-level try/catch. Any unexpected exception (network error on PAR/token fetch, JSON parse failure on PAR response, getEpdsEndpoints() throwing because NEXT_PUBLIC_EPDS_URL is missing at runtime, sessionStore.set() throwing a Redis error, restoreDpopKeyPair() throwing on a malformed JWK) will propagate as an unhandled rejection and produce a Next.js 500 HTML error page instead of a structured JSON error response.\n\nSpecific unguarded paths:\n1. login/route.ts line 65: `await parResponse.json()` — if PAR returns non-JSON (e.g. a proxy error page), this throws and crashes the handler.\n2. login/route.ts line 31-33: `getEpdsEndpoints()`, `getEpdsClientId()`, `getEpdsRedirectUri()` all throw if NEXT_PUBLIC_EPDS_URL is missing — no catch.\n3. callback/route.ts line 65: `restoreDpopKeyPair()` is called outside any try/catch — a malformed JWK in the cookie (e.g. truncated by browser) throws here.\n4. callback/route.ts line 134: `await sessionStore.set()` — Redis errors are not caught; if Redis is down the user gets a 500 after a successful token exchange, and the session is lost.\n5. callback/route.ts line 100: `await tokenResponse.json()` — if token endpoint returns non-JSON, this throws.\n\nFix: wrap each route handler body in try/catch and return NextResponse.json({ error: ... }, { status: 502 }) for upstream failures and { status: 500 } for internal ones. Separately catch sessionStore.set() and log the error without crashing (the user-did cookie is already set, so the session can be recovered on next request).","status":"closed","priority":1,"issue_type":"bug","assignee":"kzoeps","owner":"kzoepa@gmail.com","created_at":"2026-02-24T16:25:21.692276925+06:00","created_by":"kzoeps","updated_at":"2026-02-24T17:12:26.181766376+06:00","closed_at":"2026-02-24T17:12:26.181766376+06:00","close_reason":"Already fixed in dhz.11 (commit b4f7ed9). Both routes have top-level try/catch, cookie decode has separate try/catch, PAR error returns 502.","dependencies":[{"issue_id":"hypercerts-ycl","depends_on_id":"hypercerts-dhz","type":"discovered-from","created_at":"2026-02-24T16:25:29.398521518+06:00","created_by":"kzoeps"}]}
{"id":"hypercerts-yhh","title":"Epic: Add contributor management and editor access from hypercert detail view","description":"## Goals\nEnable owners to add/remove contributors and manage editor access directly from the hypercert detail view page, instead of only during creation.\n\n## Context\n- Contributors can currently only be added during hypercert creation (via the base form's optional contributors section)\n- The standalone contribution form exists (contributions-form.tsx) but is disabled in add-record-form.tsx (shows 'coming soon')\n- The addContribution server action already works (lib/create-actions.ts:56-78)\n- Editor/collaborator management requires SDS (Shared Data Server) — the app currently uses PDS only (lib/repo-context.ts:57)\n- The SDK exposes collaborators.grant/revoke/list on Repository but throws SDSRequiredError on PDS\n- The detail view already passes isOwner to sections and shows edit/delete buttons for owners\n\n## Scope\n1. Enable the contribution form in add-record-form.tsx so owners can add contributors from the detail view\n2. Add an 'Add Contributor' button to the contributors section on the detail view (owner-only)\n3. Add server actions for listing/granting/revoking editor access (requires SDS repo context)\n4. Build an editors management section on the detail view (owner-only, SDS-only)\n5. Handle the PDS-only case gracefully (hide editor management when not on SDS)\n\n## Key Constraints\n- The SDK's UpdateHypercertParams supports updating contributors inline on the record\n- The SDK's addContribution creates a separate contribution record linked to the hypercert\n- Editor management (collaborators) is SDS-only — must detect isSDS and hide/show accordingly\n- Reuse existing UserSelection component for searching Bluesky users\n- Follow existing patterns: server actions in create-actions.ts, React Query mutations in queries/hypercerts/","status":"open","priority":2,"issue_type":"epic","owner":"karma.gainforest.id","created_at":"2026-03-02T20:08:41.433422689+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:08:41.433422689+06:00","labels":["scope:medium"]}
{"id":"hypercerts-yhh.1","title":"Enable contribution form in add-record-form.tsx","description":"## Files\n- components/add-record-form.tsx (modify)\n\n## What to do\nReplace the \"Contribution form coming soon\" placeholder (lines 90-95) with the actual `HypercertContributionForm` component, matching the pattern used for measurement/evaluation/evidence forms.\n\n1. Add a dynamic import for `contributions-form.tsx` at the top of the file, following the same pattern as MeasurementForm/EvaluationForm/HypercertEvidenceForm (use `dynamic(() =\u003e import(\"@/components/contributions-form\"))`)\n2. Add a loading skeleton for the contribution form. Create a simple `ContributionFormSkeleton` in `components/form-skeletons.tsx` following the same pattern as `MeasurementFormSkeleton` — a glass-panel div with a few Skeleton blocks.\n3. In the `renderForm()` switch statement, replace the `case \"contribution\"` block (lines 90-95) with:\n   ```tsx\n   case \"contribution\":\n     return (\n       \u003cContributionsForm\n         hypercertInfo={hypercertInfo}\n         onNext={goBack}\n         onBack={goBack}\n       /\u003e\n     );\n   ```\n4. Remove the `// ContributionsForm is currently disabled` comment (line 28)\n\n## Don't\n- Modify contributions-form.tsx itself\n- Change any other case in the switch statement\n- Add new props to ContributionsForm","acceptance_criteria":"1. Navigating to /hypercerts/[uri]/add/contribution renders the ContributionsForm component (not the \"coming soon\" placeholder)\n2. The form loads with a skeleton while the dynamic import resolves\n3. The form's onBack and onNext both navigate back to the hypercert detail page\n4. TypeScript compiles without errors (`npx tsc --noEmit`)\n5. The contribution form submits successfully (calls addContribution server action)","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-02T20:08:56.913756752+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:08:56.913756752+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-yhh.1","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:08:56.915396736+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-yhh.1","depends_on_id":"hypercerts-yhh.10","type":"blocks","created_at":"2026-03-02T20:11:57.881908391+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.10","title":"Add ContributionFormSkeleton to form-skeletons.tsx","description":"## Files\n- components/form-skeletons.tsx (modify)\n\n## What to do\nAdd a `ContributionFormSkeleton` component to the existing form-skeletons file. This is used as the loading fallback for the dynamically imported ContributionsForm in add-record-form.tsx.\n\n1. Open `components/form-skeletons.tsx` and examine the existing skeleton patterns (MeasurementFormSkeleton, EvaluationFormSkeleton, EvidenceFormSkeleton).\n2. Add a new exported component `ContributionFormSkeleton` that follows the same pattern. The contribution form has these fields: role input, contributors section (with tabs), description textarea, and two date pickers. The skeleton should approximate this layout:\n\n```tsx\nexport function ContributionFormSkeleton() {\n  return (\n    \u003cdiv className=\"glass-panel space-y-6 rounded-2xl p-8\"\u003e\n      {/* Step label + title */}\n      \u003cdiv className=\"space-y-2\"\u003e\n        \u003cSkeleton className=\"h-4 w-20\" /\u003e\n        \u003cSkeleton className=\"h-7 w-48\" /\u003e\n        \u003cSkeleton className=\"h-4 w-64\" /\u003e\n      \u003c/div\u003e\n      {/* Role input */}\n      \u003cdiv className=\"space-y-2\"\u003e\n        \u003cSkeleton className=\"h-4 w-24\" /\u003e\n        \u003cSkeleton className=\"h-10 w-full\" /\u003e\n      \u003c/div\u003e\n      {/* Contributors section with tabs */}\n      \u003cdiv className=\"space-y-3\"\u003e\n        \u003cSkeleton className=\"h-4 w-28\" /\u003e\n        \u003cSkeleton className=\"h-10 w-full\" /\u003e\n        \u003cSkeleton className=\"h-24 w-full\" /\u003e\n      \u003c/div\u003e\n      {/* Description */}\n      \u003cdiv className=\"space-y-2\"\u003e\n        \u003cSkeleton className=\"h-4 w-32\" /\u003e\n        \u003cSkeleton className=\"h-24 w-full\" /\u003e\n      \u003c/div\u003e\n      {/* Date pickers */}\n      \u003cdiv className=\"grid grid-cols-1 gap-4 md:grid-cols-2\"\u003e\n        \u003cSkeleton className=\"h-10 w-full\" /\u003e\n        \u003cSkeleton className=\"h-10 w-full\" /\u003e\n      \u003c/div\u003e\n      {/* Footer buttons */}\n      \u003cdiv className=\"flex justify-between\"\u003e\n        \u003cSkeleton className=\"h-10 w-20\" /\u003e\n        \u003cSkeleton className=\"h-10 w-32\" /\u003e\n      \u003c/div\u003e\n    \u003c/div\u003e\n  );\n}\n```\n\n## Don't\n- Modify existing skeleton components\n- Change imports or exports of existing skeletons","acceptance_criteria":"1. `ContributionFormSkeleton` is exported from `components/form-skeletons.tsx`\n2. It renders a skeleton layout approximating the contribution form structure\n3. It uses the same Skeleton component and styling patterns as the other form skeletons\n4. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-02T20:11:52.903633728+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:11:52.903633728+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-yhh.10","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:11:52.905266516+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.11","title":"Update contributions-form.tsx step label and make it context-aware","description":"## Files\n- components/contributions-form.tsx (modify)\n\n## What to do\nThe contributions form currently shows \"Step 2 of 5\" (line 139) which only makes sense in the multi-step creation flow. When used standalone from the detail view (via add-record-form.tsx), this label is misleading. Update it to be context-aware.\n\n1. The `FormInfo` component wrapping the form receives a `stepLabel` prop. Change the hardcoded \"Step 2 of 5\" to be conditional:\n   - If `onBack` is provided (multi-step flow), show \"Step 2 of 5\"\n   - If `onBack` is not provided (standalone), show nothing or a simple label like \"Add Contribution\"\n\nReplace line 139:\n```tsx\n// Before\nstepLabel=\"Step 2 of 5\"\n\n// After  \nstepLabel={onBack ? \"Step 2 of 5\" : undefined}\n```\n\n2. Also remove the `// NOTE: contributions step is currently disabled` comment on line 139 since it is no longer disabled.\n\n## Don't\n- Change the form fields or submission logic\n- Modify FormInfo component\n- Change the title or description props","acceptance_criteria":"1. When rendered standalone (onBack is undefined), no \"Step 2 of 5\" label appears\n2. When rendered in multi-step flow (onBack is provided), \"Step 2 of 5\" still appears\n3. The stale comment about contributions being disabled is removed\n4. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"open","priority":3,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-02T20:12:16.094259303+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:12:16.094259303+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-yhh.11","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:12:16.095682701+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.2","title":"Add 'Add Contributor' button to contributors section on detail view","description":"## Files\n- components/hypercert-contributors-section.tsx (modify)\n\n## What to do\nAdd an \"Add Contributor\" button at the bottom of the contributors section that is only visible when `isOwner` is true. The button should link to the add-contribution page for this hypercert.\n\n1. The component already receives `hypercertUri` and `isOwner` props (see interface on line 16-20) but `isOwner` is destructured but unused (line 33 only destructures `contributors`). Update the destructuring to also extract `hypercertUri` and `isOwner`.\n\n2. After the content section (after the closing `\u003c/\u003e` on line 123, before the comment on line 126), add an owner-only \"Add Contributor\" button:\n   ```tsx\n   {isOwner \u0026\u0026 hypercertUri \u0026\u0026 (\n     \u003cdiv className=\"pt-2\"\u003e\n       \u003cButton\n         asChild\n         variant=\"outline\"\n         size=\"sm\"\n         className=\"gap-2 font-[family-name:var(--font-outfit)]\"\n       \u003e\n         \u003cLink href={`/hypercerts/${encodeURIComponent(hypercertUri)}/add/contribution`}\u003e\n           \u003cUserPlus className=\"h-4 w-4\" /\u003e Add Contributor\n         \u003c/Link\u003e\n       \u003c/Button\u003e\n     \u003c/div\u003e\n   )}\n   ```\n\n3. Add necessary imports at the top:\n   - `import { Button } from \"@/components/ui/button\";`\n   - `import Link from \"next/link\";`\n   - Add `UserPlus` to the lucide-react import (alongside existing `Users`)\n\n## Don't\n- Change the contributor display logic\n- Add inline contributor editing (that's a separate task)\n- Modify the empty state message","acceptance_criteria":"1. When `isOwner` is true, an \"Add Contributor\" button appears below the contributors list\n2. When `isOwner` is false or undefined, no button appears\n3. Clicking the button navigates to `/hypercerts/[encodedUri]/add/contribution`\n4. The button uses the same styling conventions as other buttons in the detail view (outline variant, sm size, Outfit font)\n5. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-02T20:09:11.118029062+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:09:11.118029062+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-yhh.2","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:09:11.119764008+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.3","title":"Add /hypercerts/[hypercertUri]/add/[type] route page","description":"## Files\n- app/hypercerts/[hypercertUri]/add/[type]/page.tsx (create)\n\n## What to do\nCreate a dynamic route page that renders the AddRecordForm component for adding records (measurement, evaluation, evidence, contribution) to a hypercert. This route is needed because the AddRecordForm component exists but there is no page routing to it.\n\n1. Create the file at `app/hypercerts/[hypercertUri]/add/[type]/page.tsx`\n2. It should be a server component that:\n   - Extracts `hypercertUri` and `type` from `params`\n   - Decodes the `hypercertUri` using `decodeURIComponent`\n   - Validates `type` is one of: \"measurement\", \"evaluation\", \"evidence\", \"contribution\"\n   - If invalid type, use `notFound()` from `next/navigation`\n   - Renders `\u003cAddRecordForm hypercertUri={decodedUri} type={type} /\u003e`\n\n3. Follow the same pattern as `app/hypercerts/[hypercertUri]/page.tsx` for param handling:\n   ```tsx\n   import { notFound } from \"next/navigation\";\n   import AddRecordForm from \"@/components/add-record-form\";\n\n   const VALID_TYPES = new Set([\"measurement\", \"evaluation\", \"evidence\", \"contribution\"]);\n\n   export default async function AddRecordPage({\n     params,\n   }: {\n     params: Promise\u003c{ hypercertUri: string; type: string }\u003e;\n   }) {\n     const { hypercertUri, type } = await params;\n     const decodedUri = decodeURIComponent(hypercertUri);\n\n     if (!VALID_TYPES.has(type)) {\n       notFound();\n     }\n\n     return (\n       \u003cmain className=\"noise-bg relative min-h-screen\"\u003e\n         \u003cdiv className=\"relative z-10 mx-auto max-w-4xl px-4 py-8\"\u003e\n           \u003cAddRecordForm hypercertUri={decodedUri} type={type} /\u003e\n         \u003c/div\u003e\n       \u003c/main\u003e\n     );\n   }\n   ```\n\nNote: Check if this route already exists. If it does, verify it handles the \"contribution\" type correctly. The AddRecordForm component already wraps content in its own `\u003cmain\u003e` tag — if so, do NOT wrap in another `\u003cmain\u003e`, just render `\u003cAddRecordForm\u003e` directly.\n\n## Don't\n- Add authentication checks here (AddRecordForm handles that)\n- Modify AddRecordForm component\n- Add layout.tsx (use the parent layout)","acceptance_criteria":"1. GET /hypercerts/[encodedUri]/add/contribution renders the contribution form\n2. GET /hypercerts/[encodedUri]/add/measurement renders the measurement form\n3. GET /hypercerts/[encodedUri]/add/invalidtype returns 404\n4. TypeScript compiles without errors (`npx tsc --noEmit`)\n5. No duplicate `\u003cmain\u003e` tags in the rendered HTML","status":"closed","priority":2,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":20,"created_at":"2026-03-02T20:09:26.513463326+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:09:50.687728342+06:00","closed_at":"2026-03-02T20:09:50.687734263+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-yhh.3","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:09:26.51527029+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.4","title":"Add SDS detection helper and expose isSDS in RepoContext","description":"## Files\n- lib/repo-context.ts (modify)\n\n## What to do\nAdd an `isSDS` boolean to the `RepoContext` interface and populate it from the Repository instance. This is needed so that downstream components can conditionally show/hide editor management features (which require SDS).\n\n1. Add `isSDS: boolean` to the `RepoContext` interface (after `server: RepoServer` on line 23):\n   ```typescript\n   export interface RepoContext {\n     userDid: string;\n     activeDid: string;\n     targetDid: string;\n     server: RepoServer;\n     isSDS: boolean;  // \u003c-- add this\n     repository: Repository;\n     scopedRepo: ReturnType\u003cRepository[\"repo\"]\u003e;\n   }\n   ```\n\n2. In the `getRepoContext` function, after creating the repository (line 63), read `repository.isSDS` and include it in the return object:\n   ```typescript\n   const isSDS = repository.isSDS;\n   return { userDid, activeDid, targetDid, server, isSDS, repository, scopedRepo };\n   ```\n\n3. Also update the `RepoServer` type to include `\"sds\"` as a possible value (for future use):\n   ```typescript\n   export type RepoServer = \"pds\" | \"sds\";\n   ```\n\n## Don't\n- Change the default server from \"pds\" to \"sds\"\n- Add any collaborator operations in this task\n- Modify any other files","acceptance_criteria":"1. `RepoContext.isSDS` is a boolean field available on the context object\n2. When server is \"pds\", `isSDS` is false\n3. `RepoServer` type accepts both \"pds\" and \"sds\"\n4. TypeScript compiles without errors (`npx tsc --noEmit`)\n5. No runtime behavior changes — existing code continues to work","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-02T20:10:04.029254784+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:10:04.029254784+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-yhh.4","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:10:04.031002437+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.5","title":"Add server actions for editor (collaborator) management","description":"## Files\n- lib/create-actions.ts (modify)\n\n## What to do\nAdd four new server actions for managing repository collaborators (editors). These wrap the SDK's `collaborators` API. All actions must check that the repo context is SDS-capable before calling collaborator methods.\n\n### 1. `listEditors(params: { repoDid: string })`\n```typescript\nexport const listEditors = async (params: { repoDid: string }) =\u003e {\n  const ctx = await getRepoContext({ targetDid: params.repoDid });\n  if (!ctx) {\n    throw new Error(\"listEditors failed: could not establish repository context.\");\n  }\n  if (!ctx.isSDS) {\n    // PDS repos don't support collaborators — return empty list\n    return { collaborators: [], cursor: undefined };\n  }\n  return ctx.scopedRepo.collaborators.list();\n};\n```\n\n### 2. `grantEditorAccess(params: GrantAccessParams)`\nUses the existing `GrantAccessParams` interface (already defined on line 11-15).\n```typescript\nexport const grantEditorAccess = async (params: GrantAccessParams) =\u003e {\n  const ctx = await getRepoContext({ targetDid: params.repoDid });\n  if (!ctx) {\n    throw new Error(\"grantEditorAccess failed: could not establish repository context.\");\n  }\n  if (!ctx.isSDS) {\n    throw new Error(\"grantEditorAccess failed: editor management requires a Shared Data Server (SDS).\");\n  }\n  if (ctx.activeDid !== params.repoDid) {\n    throw new Error(\"grantEditorAccess failed: Forbidden — you can only manage editors on your own repository.\");\n  }\n  return ctx.scopedRepo.collaborators.grant({\n    userDid: params.userDid,\n    role: params.role,\n  });\n};\n```\n\n### 3. `revokeEditorAccess(params: { repoDid: string; userDid: string })`\n```typescript\nexport const revokeEditorAccess = async (params: { repoDid: string; userDid: string }) =\u003e {\n  const ctx = await getRepoContext({ targetDid: params.repoDid });\n  if (!ctx) {\n    throw new Error(\"revokeEditorAccess failed: could not establish repository context.\");\n  }\n  if (!ctx.isSDS) {\n    throw new Error(\"revokeEditorAccess failed: editor management requires a Shared Data Server (SDS).\");\n  }\n  if (ctx.activeDid !== params.repoDid) {\n    throw new Error(\"revokeEditorAccess failed: Forbidden — you can only manage editors on your own repository.\");\n  }\n  return ctx.scopedRepo.collaborators.revoke({ userDid: params.userDid });\n};\n```\n\n### 4. `checkIsSDS()`\nA simple action that returns whether the current repo context is SDS:\n```typescript\nexport const checkIsSDS = async (): Promise\u003cboolean\u003e =\u003e {\n  const ctx = await getRepoContext();\n  if (!ctx) return false;\n  return ctx.isSDS;\n};\n```\n\n### Imports needed\nThe `RepositoryRole` import already exists on line 6. The `GrantAccessParams` interface already exists on lines 11-15. No new imports needed.\n\n## Don't\n- Modify existing server actions\n- Add UI components\n- Change the default server from PDS to SDS","acceptance_criteria":"1. `listEditors` returns `{ collaborators: [], cursor: undefined }` when on PDS (no error thrown)\n2. `grantEditorAccess` throws a clear error message when called on PDS\n3. `revokeEditorAccess` throws a clear error message when called on PDS\n4. All three editor actions validate ownership (activeDid === repoDid) before proceeding\n5. `checkIsSDS` returns false when on PDS\n6. TypeScript compiles without errors (`npx tsc --noEmit`)\n7. All actions use \"use server\" directive (already at top of file)","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-02T20:10:24.153446428+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:10:24.153446428+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-yhh.5","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:10:24.155179884+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-yhh.5","depends_on_id":"hypercerts-yhh.4","type":"blocks","created_at":"2026-03-02T20:10:24.157488538+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.6","title":"Add React Query hooks for editor management","description":"## Files\n- queries/hypercerts/use-editors-query.ts (create)\n- queries/hypercerts/index.ts (modify)\n\n## What to do\nCreate React Query hooks for listing, granting, and revoking editor access. These wrap the server actions from create-actions.ts.\n\n### 1. Create `queries/hypercerts/use-editors-query.ts`\n\n```typescript\n\"use client\";\n\nimport { useQuery, useMutation, useQueryClient } from \"@tanstack/react-query\";\nimport { toast } from \"sonner\";\nimport {\n  listEditors,\n  grantEditorAccess,\n  revokeEditorAccess,\n} from \"@/lib/create-actions\";\nimport type { RepositoryRole } from \"@hypercerts-org/sdk-core\";\n\n// Query key factory\nconst editorKeys = {\n  all: [\"editors\"] as const,\n  list: (repoDid: string) =\u003e [...editorKeys.all, \"list\", repoDid] as const,\n};\n\n/**\n * Query hook to list editors (collaborators) for a repository.\n * Returns empty list when on PDS (non-SDS).\n */\nexport function useEditorsQuery(repoDid: string | undefined) {\n  return useQuery({\n    queryKey: editorKeys.list(repoDid ?? \"\"),\n    queryFn: () =\u003e listEditors({ repoDid: repoDid! }),\n    enabled: !!repoDid,\n  });\n}\n\n/**\n * Mutation hook to grant editor access to a user.\n */\nexport function useGrantEditorMutation(options?: {\n  onSuccess?: () =\u003e void;\n}) {\n  const queryClient = useQueryClient();\n  return useMutation({\n    mutationFn: (params: { repoDid: string; userDid: string; role: RepositoryRole }) =\u003e\n      grantEditorAccess(params),\n    onSuccess: (_data, variables) =\u003e {\n      queryClient.invalidateQueries({\n        queryKey: editorKeys.list(variables.repoDid),\n      });\n      toast.success(\"Editor access granted!\");\n      options?.onSuccess?.();\n    },\n    onError: (error) =\u003e {\n      console.error(\"Grant editor error:\", error);\n      toast.error(\n        error instanceof Error ? error.message : \"Failed to grant editor access.\"\n      );\n    },\n  });\n}\n\n/**\n * Mutation hook to revoke editor access from a user.\n */\nexport function useRevokeEditorMutation(options?: {\n  onSuccess?: () =\u003e void;\n}) {\n  const queryClient = useQueryClient();\n  return useMutation({\n    mutationFn: (params: { repoDid: string; userDid: string }) =\u003e\n      revokeEditorAccess(params),\n    onSuccess: (_data, variables) =\u003e {\n      queryClient.invalidateQueries({\n        queryKey: editorKeys.list(variables.repoDid),\n      });\n      toast.success(\"Editor access revoked.\");\n      options?.onSuccess?.();\n    },\n    onError: (error) =\u003e {\n      console.error(\"Revoke editor error:\", error);\n      toast.error(\n        error instanceof Error ? error.message : \"Failed to revoke editor access.\"\n      );\n    },\n  });\n}\n```\n\n### 2. Update `queries/hypercerts/index.ts`\nAdd re-exports for the new hooks:\n```typescript\nexport { useEditorsQuery, useGrantEditorMutation, useRevokeEditorMutation } from \"./use-editors-query\";\n```\n\n## Don't\n- Modify existing query hooks\n- Add UI components\n- Import from lib/api/ (these use server actions directly, not API routes)","acceptance_criteria":"1. `useEditorsQuery(repoDid)` returns `{ data, isLoading, error }` with the collaborators list\n2. `useEditorsQuery(undefined)` is disabled (does not fire a request)\n3. `useGrantEditorMutation` invalidates the editors list query on success and shows a toast\n4. `useRevokeEditorMutation` invalidates the editors list query on success and shows a toast\n5. Both mutations show error toasts on failure\n6. All hooks are exported from `queries/hypercerts/index.ts`\n7. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-02T20:10:41.34090148+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:10:41.34090148+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-yhh.6","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:10:41.342478459+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-yhh.6","depends_on_id":"hypercerts-yhh.5","type":"blocks","created_at":"2026-03-02T20:10:41.356115998+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.7","title":"Create HypercertEditorsSection component","description":"## Files\n- components/hypercert-editors-section.tsx (create)\n\n## What to do\nCreate a new component that displays and manages editors (collaborators) for a hypercert. This section is only visible to the owner and only when the repository is on SDS. It follows the same visual pattern as HypercertContributorsSection.\n\n### Component signature\n```typescript\ninterface HypercertEditorsSectionProps {\n  ownerDid: string;\n  isOwner?: boolean;\n  isSDS?: boolean;\n}\n\nexport default function HypercertEditorsSection({\n  ownerDid,\n  isOwner,\n  isSDS,\n}: HypercertEditorsSectionProps)\n```\n\n### Behavior\n1. **Guard**: If `!isOwner` or `!isSDS`, return `null` (render nothing).\n2. **Data fetching**: Use `useEditorsQuery(ownerDid)` to fetch the list of editors.\n3. **Display**: Show a section with:\n   - Section header with Shield icon (from lucide-react) and title \"Editors\"\n   - Subtitle showing count: \"N editors\" / \"1 editor\"\n   - List of editors, each showing:\n     - The editor's DID (truncated to first 20 chars + \"...\")\n     - Their role badge (viewer/editor/admin)\n     - A \"Revoke\" button (only for non-owner editors)\n   - Loading state: show Skeleton placeholders (same pattern as ContributorSkeleton in hypercert-contributors-section.tsx)\n   - Empty state: \"No editors have been added yet.\"\n4. **Add editor**: An \"Add Editor\" button that opens a Dialog (from `@/components/ui/dialog`) containing:\n   - A UserSelection component for searching Bluesky users\n   - A role selector (Radio group or Select) with options: \"viewer\", \"editor\", \"admin\"\n   - A \"Grant Access\" submit button\n   - Uses `useGrantEditorMutation` on submit\n5. **Revoke editor**: Each editor row has a \"Revoke\" button that calls `useRevokeEditorMutation`\n\n### Styling\nFollow the same glass-panel pattern used in hypercert-contributors-section.tsx:\n- `glass-panel border-border/50 rounded-xl border p-4` for editor rows\n- `font-[family-name:var(--font-outfit)]` for text\n- `font-[family-name:var(--font-syne)]` for headings\n- Use `bg-create-accent/10` and `text-create-accent` for the icon circle\n\n### Imports needed\n```typescript\n\"use client\";\nimport { useState } from \"react\";\nimport { Shield, UserPlus, Trash2 } from \"lucide-react\";\nimport { Button } from \"@/components/ui/button\";\nimport { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from \"@/components/ui/dialog\";\nimport { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from \"@/components/ui/select\";\nimport { Skeleton } from \"@/components/ui/skeleton\";\nimport UserSelection from \"./user-selection\";\nimport { useEditorsQuery, useGrantEditorMutation, useRevokeEditorMutation } from \"@/queries/hypercerts\";\nimport type { RepositoryRole } from \"@hypercerts-org/sdk-core\";\nimport type { ProfileView } from \"@atproto/api/dist/client/types/app/bsky/actor/defs\";\n```\n\nNote: If `@/components/ui/select` does not exist, use a simple `\u003cselect\u003e` HTML element styled with Tailwind instead. Check `components/ui/` directory first.\n\n## Don't\n- Show this section to non-owners\n- Show this section when not on SDS\n- Add transfer ownership functionality (out of scope)\n- Modify any existing components","acceptance_criteria":"1. Component renders nothing when `isOwner` is false\n2. Component renders nothing when `isSDS` is false\n3. When both are true, shows the editors list with loading/empty/populated states\n4. \"Add Editor\" button opens a dialog with user search and role selection\n5. Granting access calls `useGrantEditorMutation` and closes the dialog on success\n6. \"Revoke\" button calls `useRevokeEditorMutation` for the selected editor\n7. Owner's own entry does not show a \"Revoke\" button\n8. TypeScript compiles without errors (`npx tsc --noEmit`)\n9. Follows the same visual style as HypercertContributorsSection","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":60,"created_at":"2026-03-02T20:11:06.052821744+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:11:06.052821744+06:00","labels":["scope:medium"],"dependencies":[{"issue_id":"hypercerts-yhh.7","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:11:06.054780622+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-yhh.7","depends_on_id":"hypercerts-yhh.6","type":"blocks","created_at":"2026-03-02T20:11:06.058369191+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.8","title":"Pass isSDS and ownerDid to detail view and wire up editors section","description":"## Files\n- app/hypercerts/[hypercertUri]/page.tsx (modify)\n- components/hypercert-detail-view.tsx (modify)\n\n## What to do\nThread `isSDS` and `ownerDid` from the server page through to the detail view component, and add the HypercertEditorsSection to the detail view.\n\n### 1. Update `app/hypercerts/[hypercertUri]/page.tsx`\nThe server page already calls `getRepoContext()` and has access to the context. After the existing code that determines `isOwner`, also extract `isSDS` from the context:\n\n```typescript\n// After existing code that gets viewCtx\nconst isSDS = viewCtx?.isSDS ?? false;\n```\n\nPass `isSDS` and `ownerDid` (already available as a variable) to the detail view:\n```tsx\n\u003cHypercertDetailsView\n  record={record}\n  imageUri={imageUri}\n  hypercertUri={decodedUri}\n  isOwner={isOwner}\n  isSDS={isSDS}\n  ownerDid={ownerDid}\n/\u003e\n```\n\n### 2. Update `components/hypercert-detail-view.tsx`\n\na. Add `isSDS` and `ownerDid` to the component props:\n```typescript\nexport default function HypercertDetailsView({\n  hypercertUri,\n  record,\n  imageUri,\n  isOwner,\n  isSDS,\n  ownerDid,\n}: {\n  hypercertUri: string;\n  record: HypercertClaim;\n  imageUri?: string;\n  isOwner?: boolean;\n  isSDS?: boolean;\n  ownerDid?: string;\n})\n```\n\nb. Import and add the HypercertEditorsSection component. Place it after the Contributors section (after line 220) and before the Full Description section:\n```tsx\n{/* Editors Section (SDS only, owner only) */}\n\u003cdiv className=\"animate-fade-in-up [animation-delay:200ms]\"\u003e\n  \u003cHypercertEditorsSection\n    ownerDid={ownerDid ?? \"\"}\n    isOwner={isOwner}\n    isSDS={isSDS}\n  /\u003e\n\u003c/div\u003e\n```\n\nc. Add the import at the top:\n```typescript\nimport HypercertEditorsSection from \"./hypercert-editors-section\";\n```\n\n## Don't\n- Modify the HypercertEditorsSection component (created in a separate task)\n- Change any existing sections or their order\n- Remove any existing props or functionality","acceptance_criteria":"1. `isSDS` and `ownerDid` are passed from the server page to the detail view component\n2. HypercertEditorsSection is rendered between Contributors and Description sections\n3. On PDS (isSDS=false), the editors section renders nothing (no visual change)\n4. On SDS (isSDS=true) with isOwner=true, the editors section is visible\n5. TypeScript compiles without errors (`npx tsc --noEmit`)\n6. No visual regression on existing sections","status":"open","priority":2,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":30,"created_at":"2026-03-02T20:11:27.907317757+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:11:27.907317757+06:00","labels":["scope:small"],"dependencies":[{"issue_id":"hypercerts-yhh.8","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:11:27.908987063+06:00","created_by":"karma.gainforest.id"},{"issue_id":"hypercerts-yhh.8","depends_on_id":"hypercerts-yhh.7","type":"blocks","created_at":"2026-03-02T20:11:27.911790234+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yhh.9","title":"Remove autofill demo button from contributions-form.tsx","description":"## Files\n- components/contributions-form.tsx (modify)\n\n## What to do\nRemove the \"Autofill Demo\" button and its handler from the contributions form. This was scaffolding/demo code that should not ship in the final product.\n\n1. Remove the `handleAutofill` function (lines 120-135)\n2. Remove the \"Autofill\" button JSX block (lines 145-155, the `\u003cdiv className=\"flex justify-end\"\u003e` wrapper and its Button child)\n3. Remove the `Wand2` icon from the lucide-react import (line 8)\n\n## Don't\n- Change any form fields or validation logic\n- Modify the form submission handler\n- Remove any other functionality","acceptance_criteria":"1. The \"Autofill Demo\" button no longer appears in the contribution form\n2. The `handleAutofill` function is removed\n3. The `Wand2` import is removed\n4. All form fields still work correctly (role, contributors, description, dates)\n5. TypeScript compiles without errors (`npx tsc --noEmit`)","status":"open","priority":3,"issue_type":"task","owner":"karma.gainforest.id","estimated_minutes":10,"created_at":"2026-03-02T20:11:38.76122662+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-02T20:11:38.76122662+06:00","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-yhh.9","depends_on_id":"hypercerts-yhh","type":"parent-child","created_at":"2026-03-02T20:11:38.762659966+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yz33","title":"Epic: Resolve CodeRabbit review for PR #16","description":"Resolve all 3 open CodeRabbit inline review comments on PR #16 (docs: update README, DEVELOPMENT, and AGENTS for native ATProto migration). 3 comments across 3 files: .beads/issues.jsonl, DEVELOPMENT.md, README.md.","status":"closed","priority":1,"issue_type":"epic","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-03-04T22:35:23.552227042+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T22:38:31.026413211+06:00","closed_at":"2026-03-04T22:38:31.026413211+06:00","close_reason":"4c7c854 All 3 CodeRabbit review comments for PR #16 resolved","labels":["needs-integration-review","scope:small"]}
{"id":"hypercerts-yz33.1","title":"Fix stale blocks dependency on closed issue hypercerts-phiw.10","description":"## Files\n- .beads/issues.jsonl (modify — via hb CLI, not direct edit)\n\n## What to do\nCodeRabbit flagged: bug issue at line 406.\n\n**Original comment:**\n\u003e Line 404 marks hypercerts-phiw.10 as closed, but it still depends on hypercerts-phiw.1 via type:\"blocks\" (Line 403), which is open. Please either remove/adjust the blocker relation or reopen .10 to keep dependency state consistent.\n\n**Suggested fix:**\nRemove the stale blocks dependency from hypercerts-phiw.10. Since the issue is already closed (completed), the dependency relation is no longer meaningful and should be cleaned up.\n\nRun: hb dep remove hypercerts-phiw.10 hypercerts-phiw.1\nThen: hb sync\n\n**Context:**\nhypercerts-phiw.10 was closed after completion. The blocks dependency on hypercerts-phiw.1 was left in place. Both issues are actually closed, so the dependency is stale bookkeeping. The fix is simply removing the dependency relation.\n\n## Don't\n- Don't edit .beads/issues.jsonl directly — use hb CLI commands\n- Don't reopen any issues\n- Don't change any other dependencies or issue statuses","acceptance_criteria":"hb show hypercerts-phiw.10 --json shows no blocks dependency on hypercerts-phiw.1. hb sync succeeds without errors.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T22:35:36.142685847+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T22:37:22.317177398+06:00","closed_at":"2026-03-04T22:37:22.317177398+06:00","close_reason":"ddfc98a fix: remove stale blocks dependency on hypercerts-phiw.10","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-yz33.1","depends_on_id":"hypercerts-yz33","type":"parent-child","created_at":"2026-03-04T22:35:36.144775645+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yz33.2","title":"Fix DEVELOPMENT.md reference to non-existent OAuthProvider.tsx","description":"## Files\n- DEVELOPMENT.md (modify)\n\n## What to do\nCodeRabbit flagged: bug issue at line 350.\n\n**Original comment:**\n\u003e providers/OAuthProvider.tsx does not exist in the codebase. The providers/ directory contains AllProviders.tsx, AuthErrorToast.tsx, and SignedInProvider.tsx. The documentation needs to reference the correct file(s) that handle OAuth state management, or this entry should be removed.\n\n**Suggested fix:**\nReplace the table row at line 350 in DEVELOPMENT.md:\n\nCurrent (line 350):\n| providers/OAuthProvider.tsx | Client-side OAuth state management |\n\nReplace with two rows that reflect the actual files:\n| providers/AllProviders.tsx | Composes all client-side providers (OAuth, query, theme) |\n| providers/SignedInProvider.tsx | Server component that restores OAuth session and passes auth state to client |\n\n**Context:**\nThe table is in the 'Key Files' section around lines 341-352. The providers/ directory contains: AllProviders.tsx, AuthErrorToast.tsx, and SignedInProvider.tsx. AuthErrorToast.tsx is a UI component (not a provider), so it does not need a table entry here.\n\n## Don't\n- Don't change any other rows in the table\n- Don't change any other sections of DEVELOPMENT.md\n- Don't add AuthErrorToast.tsx to the table (it's a UI component, not a provider)","acceptance_criteria":"Line 350 of DEVELOPMENT.md no longer references OAuthProvider.tsx. The table includes entries for AllProviders.tsx and SignedInProvider.tsx with accurate descriptions. pnpm run format:check passes.","status":"closed","priority":1,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T22:35:44.329466422+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T22:36:58.996265691+06:00","closed_at":"2026-03-04T22:36:58.996265691+06:00","close_reason":"f9a28d7 fix: correct OAuthProvider.tsx reference in DEVELOPMENT.md","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-yz33.2","depends_on_id":"hypercerts-yz33","type":"parent-child","created_at":"2026-03-04T22:35:44.331190181+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-yz33.3","title":"Fix duplicated phrasing in README.md intro sentence","description":"## Files\n- README.md (modify)\n\n## What to do\nCodeRabbit flagged: style issue at line 3.\n\n**Original comment:**\n\u003e Line 3 repeats \"using native ATProto,\" which reads awkwardly in the first paragraph.\n\n**Suggested fix:**\nApply this diff to line 3 of README.md:\n\n```diff\n-A Next.js scaffold for building applications on ATProto using native ATProto. This project demonstrates authentication, profile management, and hypercert creation on the ATProto network.\n+A Next.js scaffold for building applications on native ATProto. This project demonstrates authentication, profile management, and hypercert creation on the ATProto network.\n```\n\nRemove the redundant \"on ATProto using native\" and simplify to \"on native ATProto\".\n\n**Context:**\nThis is the very first descriptive sentence of the README. The phrase \"on ATProto using native ATProto\" is redundant.\n\n## Don't\n- Don't change any other lines in README.md\n- Don't rewrite the sentence beyond removing the duplication","acceptance_criteria":"Line 3 of README.md contains exactly one occurrence of the word 'ATProto' before the period. The sentence reads naturally without redundancy. pnpm run format:check passes.","status":"closed","priority":3,"issue_type":"task","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","estimated_minutes":15,"created_at":"2026-03-04T22:35:50.751491104+06:00","created_by":"karma.gainforest.id","updated_at":"2026-03-04T22:36:47.106937667+06:00","closed_at":"2026-03-04T22:36:47.106937667+06:00","close_reason":"ce33629 style: fix duplicated phrasing in README intro","labels":["scope:trivial"],"dependencies":[{"issue_id":"hypercerts-yz33.3","depends_on_id":"hypercerts-yz33","type":"parent-child","created_at":"2026-03-04T22:35:50.753443213+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-zff","title":"Fix: No file size limit on evidence/location file uploads in add-attachment and add-location routes","description":"## Problem\n`app/api/profile/update/route.ts` and `app/api/profile/bsky/update/route.ts` correctly enforce a 1MB limit on avatar/banner uploads (lines 21-32 in each). However, the other two upload routes have no equivalent limit:\n\n- `app/api/certs/add-attachment/route.ts`: accepts `evidenceFile` (line 48) with only a `file.size === 0` check — no upper bound\n- `app/api/certs/add-location/route.ts`: accepts `locationFile` (line 58) with only a `file.size === 0` check — no upper bound\n\nAn attacker or misbehaving client can upload arbitrarily large files, exhausting server memory and causing OOM crashes or denial of service.\n\n## Fix\nAdd a maximum file size check immediately after the `file.size === 0` check in both routes. Choose a reasonable limit (e.g. 10MB for evidence files, 50MB for location data) and document it:\n\n```ts\nconst MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB\nif (file.size \u003e MAX_FILE_SIZE) {\n  return NextResponse.json(\n    { error: 'File must be less than 10MB' },\n    { status: 400 },\n  );\n}\n```\n\n## Evidence\n- `app/api/certs/add-attachment/route.ts` line 50: only checks `file.size === 0`\n- `app/api/certs/add-location/route.ts` line 60: only checks `file.size === 0`\n- Compare with `app/api/profile/update/route.ts` lines 23-32 which correctly enforces 1MB limit","status":"open","priority":2,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:44:54.452497206+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:44:54.452497206+06:00"}
{"id":"hypercerts-zpo","title":"Fix: add-attachment API catch block exposes error in 'details' field not 'error' field — toast shows generic message (from hypercerts-0h4.2)","description":"Review of hypercerts-0h4.2 found: app/api/certs/add-attachment/route.ts was listed in the spec as a file to modify but was NOT changed by the 0h4.2 commit. The catch block currently returns { error: 'Internal server error', details: (e as Error).message } (line 122). The apiClient in lib/api/client.ts only reads the 'error' field from the JSON response (line 56), so the 'details' field with the actual error message is silently dropped. The mutation hook onError toast will show 'Internal server error' instead of the actual error reason. Fix: change the catch block to return { error: `Failed to add attachment: ${(e as Error).message}` } — drop the 'details' field and put the message in 'error'. Evidence: git show 4a0ef47 --name-only (add-attachment not listed); cat app/api/certs/add-attachment/route.ts line 122; lib/api/client.ts line 56.","status":"closed","priority":2,"issue_type":"bug","assignee":"karma.gainforest.id","owner":"karma.gainforest.id","created_at":"2026-02-17T18:26:26.571299814+06:00","created_by":"karma.gainforest.id","updated_at":"2026-02-17T18:30:43.057850292+06:00","closed_at":"2026-02-17T18:30:43.057850292+06:00","close_reason":"96729ad fix: put actual error message in 'error' field in add-attachment catch block","dependencies":[{"issue_id":"hypercerts-zpo","depends_on_id":"hypercerts-0h4.2","type":"discovered-from","created_at":"2026-02-17T18:26:30.543462831+06:00","created_by":"karma.gainforest.id"}]}
{"id":"hypercerts-zuj","title":"Fix: stagger-children CSS animation only covers 10 children — 11th+ contributor/measurement/evidence card gets no animation delay","description":"## File\\napp/globals.css lines 247-276\\n\\n## Severity\\nLOW — Visual polish\\n\\n## Problem\\nThe .stagger-children CSS class defines animation delays for :nth-child(1) through :nth-child(10). Any list with more than 10 items will have the 11th+ items animate simultaneously with no delay, breaking the stagger effect. This affects contributor lists, measurement lists, evidence lists, and evaluation lists.\\n\\n## Evidence\\n```css\\n.stagger-children \u003e *:nth-child(10) {\\n  animation-delay: 540ms;\\n}\\n/* No rule for :nth-child(11) and beyond */\\n```\\n\\n## Fix\\nEither extend the CSS to cover more children (e.g., up to 20), or use a JavaScript-based approach with inline style animation-delay:\\n```tsx\\n{items.map((item, index) =\u003e (\\n  \u003cdiv\\n    key={item.id}\\n    style={{ animationDelay:  }}\\n    className=\"animate-fade-in-up\"\\n  \u003e\\n    \u003cItemView item={item} /\u003e\\n  \u003c/div\u003e\\n))}\\n```\\n\\nThe JS approach scales to any number of items without CSS maintenance.","status":"open","priority":3,"issue_type":"bug","owner":"kzoepa@gmail.com","created_at":"2026-02-27T11:47:47.862914819+06:00","created_by":"kzoeps","updated_at":"2026-02-27T11:47:47.862914819+06:00"}
